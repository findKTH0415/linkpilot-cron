공공데이터 API 실측 진단
────────────────────────────────────────────────────────────
대상 주소 : 인천광역시 남동구 남동대로 215
VWORLD_KEY     : 형식 정상 (36자)
VWORLD_DOMAIN  : 설정됨 (61자 · 스킴 있음 · 경로 있음)
DATA_GO_KR_KEY : 형식 정상 (64자)
ECOS 열쇠       : 설정됨 — ECOS_BOK_KEY (시장금리 + 생산자물가)
DART_API_KEY   : 설정됨

● 한국은행 ECOS 시장금리
  국고채(3년) 3.9연% (2026-09-07 고시)
  ※ 이 값은 기준선(debt.benchmark_rate)이다 — 차입금리를 대체하지 않는다

● 한국은행 ECOS 생산자물가 업종목록 (404Y014)
  업종 500종 (월별 167종) · 예: *AA 총지수 / *AA 총지수 / *AA 총지수
  ※ 업종은 자동으로 고르지 않는다 — 「기타 기계 및 장비」와 「1차 금속」은 몇 년 새 두 자릿수로 갈린다
  ※ 계수까지 보려면: npm run im:smoke -- --ppi-item <항목코드> [--ppi-from YYYYMM] [--ppi-to YYYYMM]

✕ 통계청 KOSIS 통계표 검색 (가동률)
  fetch failed (4회 시도 실패)
  → 엔드포인트·응답 필드가 문서 기준이고 아직 실측되지 않았다 (등록부 D-44)
  ※ 항목·대조까지 보려면: npm run im:smoke -- --kosis-tbl <통계표ID>
  ※ 공장등록을 보려면: npm run im:smoke -- --factory-name <상호> [--factory-sido <시도>]
  ※ 수출입 단가를 보려면: npm run im:smoke -- --hs <HS코드> [--hs-from/to YYYYMM] [--hs-dir export|import]
  ※ 환경 인허가를 보려면: npm run im:smoke -- --enviro-name <상호> [--enviro-addr <주소>]
  ※ 계통 여유를 보려면: npm run im:smoke -- --grid-region <시·군·구> [--grid-mw <MW>]
  ※ 휴폐업을 보려면: npm run im:smoke -- --biz-no <사업자등록번호 10자리>
    ↳ **법인등록번호(13자리)가 아니다** — 넣으면 거절한다
  ※ 사업장 인원을 보려면: npm run im:smoke -- --workplace <사업장명>
  ※ 납세증명(국세완납증명)·지방세 납세증명·체납 내역은 **어떤 공개 API 로도 조회할 수 없다** — 「국세기본법」 제81조의13(비밀 유지)이 과세정보의 제3자 제공을 막는다. **본인이 발급하거나 본인 동의를 증명한 제3자만** 신청할 수 있다. 4대보험 완납증명도 같다. **당사자에게 서류로 받는다** (corp.tax_clearance)

✕ DART 시행사 대조 (삼성물산)
  타임아웃 60000ms (4회 시도 실패)
KMA_APIHUB_KEY : 설정됨
REB_API_KEY    : 설정됨
조달청 낙찰     : DATA_GO_KR_KEY 로 조회 (⚠ 응답 필드 미검증 — 등록부 D-36)
RHINO_COMPUTE  : 미설정 — 매스 스터디 건너뜀 (Rhino 라이선스 필요, D-38)
KOSIS_API_KEY  : 설정됨 (⚠ 엔드포인트 미검증 — 등록부 D-44)
공장등록        : DATA_GO_KR_KEY 로 조회 (⚠ 기관·응답 필드 미검증 — 등록부 D-45)
수출입 단가     : DATA_GO_KR_KEY 로 조회 (⚠ 누적 여부·응답 필드 미검증 — 등록부 D-46)
환경 인허가     : DATA_GO_KR_KEY 로 조회 (⚠ 세 자료 모두 미검증 — 등록부 D-47)
PEXELS_API_KEY : 설정됨 — 참고 이미지 (점검은 npm run im:image)
법인 실재       : DATA_GO_KR_KEY 로 조회 — 휴폐업·인원 (⚠ 응답 필드 미검증 — 등록부 D-60)
법령 OC         : 설정됨 — 읽은 이름 **LAW_OC**
               ↳ **조례 한도는 조례에만 있다** — 시행령 상한을 조례 한도로 쓰면 그 문서는 틀린다
KICT_API_KEY   : 키는 설정됨 · **엔드포인트 미확정 (D-106)** — 어느 KICT 서비스인지 정해지면 KICT_API_BASE 만 넣으면 된다. 그때까지 호출하지 않는다
               ↳ 어디서 받으신 키입니까? ① cost.kict.re.kr (공사비원가관리센터 — 표준품셈·표준시장단가)
                                        ② 공공데이터포털 「건설공사비지수」 (apis.data.go.kr 계열)
               ↳ 정해지면 KICT_API_BASE 에 주소만 넣습니다. 코드는 안 건드립니다
               ↳ **납세증명·완납증명은 어떤 키로도 안 된다** (국세기본법 §81조의13) — 당사자에게 서류로 받는다
KEPCO_BIGDATA  : 설정됨 (⚠ 응답 필드 미검증 — 등록부 D-54)
               ↳ 변전소 **거리는 어느 키로도 안 나온다** — 좌표가 비식별 처리된다. 사전검토 회신을 사람이 넣는다
법령 API        : ❌ fetch failed (4회 시도 실패)

  ※ 주소를 안 주셔서 **예시 주소**로 잽니다 — 인천광역시 남동구 남동대로 215
     실제 부지로 재려면: npm run im:smoke -- --address "실제 주소"
     매번 치기 번거로우면 .env 에 IM_SMOKE_ADDRESS=실제 주소

✕ VWorld 지오코딩
  지오코딩 실패 — HTTP 502 (4회 시도 실패) (주소: 인천광역시 남동구 남동대로 215)
    · ROAD 시도 → HTTP 502 (4회 시도 실패)
    · PARCEL 시도 → HTTP 502 (4회 시도 실패)
  → IM_AGENT_DEBUG_HTTP=1 로 다시 실행해 VWorld 원문 응답을 확인한다
  ※ 원문 응답을 보려면: IM_AGENT_DEBUG_HTTP=1 npm run im:smoke

✕ REC 현물시장 (전력거래소)
  fetch failed (4회 시도 실패)

● 지가지수 (부동산원)
  인천>남동구 지수 99.4823628384759 → 100.1234962352 (202601~202607, 6개월 경과) → 시점수정 계수 1.006445 (+0.6%)

✕ 기업기본정보 (금융위)
  fetch failed (4회 시도 실패)

✕ 공사 낙찰 (조달청)
  fetch failed (4회 시도 실패)

────────────────────────────────────────────────────────────
결과: 3/9 통과
실패: 통계청 KOSIS 통계표 검색 (가동률), DART 시행사 대조 (삼성물산), VWorld 지오코딩, REC 현물시장 (전력거래소), 기업기본정보 (금융위), 공사 낙찰 (조달청)

실패 내용과 위의 ⚠ 표시를 그대로 전달하면 Connector 를 실제 응답에 맞게 고칠 수 있다.

※ 이 스크립트는 아무것도 저장하지 않았다. 키는 출력에 포함되지 않는다.
