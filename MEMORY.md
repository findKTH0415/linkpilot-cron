# MEMORY.md — 재발 방지 기록

같은 실수를 두 번 하지 않기 위한 파일이다.

**쓰는 법**
- **문제가 생기면 먼저 여기를 찾는다.** 이미 겪은 일이면 해법이 아래에 있다.
  ("이건 처음 보는 문제 같다"는 느낌은 자주 틀린다 — 검색부터 한다)
- 새 사고가 나면 **그날 바로** 항목을 추가한다. 나중에 쓰면 경위가 사라진다.
- 항목은 지우지 않는다. 해결됐으면 `✅ 막힘` 으로 바꾸고 **무엇이 막고 있는지**
  (테스트·검사 코드) 를 적는다. 막는 장치가 없으면 해결된 것이 아니다.

---

## 색인

| ID | 무엇 | 상태 | 막는 장치 |
|---|---|---|---|
| [M-01](#m-01-화면을-보냈는데-사용자가-못-본다) | 화면을 보냈는데 사용자가 못 본다 (내려받기 카드) | ✅ 막힘 | `publishable()` · `flow.test.js` |
| [M-02](#m-02-상태를-바꾸면서-갱신시각을-같이-안-바꿨다) | 상태를 바꾸면서 갱신시각을 같이 안 바꿨다 | 🟠 규칙만 | 체크리스트 (아래) |
| [M-03](#m-03-화면이-통째로-비는데-오류는-안-뜬다) | 화면이 통째로 비는데 오류는 안 뜬다 | ✅ 막힘 | `preview.test.js` 태그 짝 검사 |
| [M-04](#m-04-그럴듯하게-틀린-값이-출처까지-달고-들어온다) | 그럴듯하게 틀린 값이 출처까지 달고 들어온다 | ✅ 막힘 | `reliable:false` · 읽을 수 있는 글자 비율 |
| [M-05](#m-05-확인-화면이-손으로-적은-옛말을-한다) | 확인 화면이 손으로 적은 옛말을 한다 | ✅ 막힘 | `flow.test.js` — 빌더에 결과 문자열 금지 |
| [M-06](#m-06-키가-로그에-평문으로-남았다) | 짧은 인증키가 로그에 평문으로 남았다 | ✅ 막힘 | `redact()` + `SECRET_ENV` |
| [M-07](#m-07-화면은-보이는데-스크롤이-안-된다) | 화면은 보이는데 스크롤이 안 된다 | ✅ 막힘 | `inlineScreen()` 스타일 사본 제거 + 테스트 |
| [M-08](#m-08-테스트-733건이-전부-통과하는데-http-호출은-하나도-안-된다) | 테스트가 다 통과하는데 HTTP 호출은 하나도 안 된다 | ✅ 막힘 | `http-live.test.js` (localhost 실호출) |
| [M-09](#m-09-설치했다는-선언만-보고-산출물을-안-봤다) | 「설치했다」는 선언만 보고 산출물을 안 봤다 (한글이 일본어 글꼴로) | ✅ 막힘 | `pdf-fonts.test.js` — 실제 PDF 에 박힌 글꼴 이름 |
| [M-10](#m-10-아무도-안-고쳤는데-자정에-ci-가-빨개졌다) | 아무도 안 고쳤는데 **자정에** CI 가 빨개졌다 (커밋 산출물에 시계가 샜다) | ✅ 막힘 | `flow.test.js` — 확인 패널에 오늘 날짜 금지 |

---

## M-01. 화면을 보냈는데 사용자가 못 본다

**등급: 심각 (네 번 반복)**

### 경위

2026-08-16. 작업을 마칠 때마다 미리보기 HTML 을 **파일로** 보냈다.
사용자 환경에서는 그 파일이 **내려받기 카드로만** 떴다 — 옆 창에 열리지 않는다.

사용자가 네 번에 걸쳐 "옆 창에서 열어 달라"고 말했고, 나는 그때마다
**같은 방식(파일 전달)을 반복**하면서 문구만 바꿨다. 마지막에 사용자가
화면을 캡처해 보여 준 뒤에야 무엇을 원하는지 알았다 — **주소로 여는 페이지**였다.

### 무엇이 잘못이었나

1. **증상을 듣고도 수단을 안 바꿨다.** "안 열린다"는 말은 "다시 보내 달라"가
   아니라 "다른 방법으로 달라"는 뜻이다. 두 번째 요청부터 방법을 바꿨어야 했다.
2. **전달이 됐는지 확인할 방법이 없었다.** 파일을 보낸 결과는 "보냈다"까지만
   보이고, 그것이 열렸는지는 안 보인다. 확인할 수 없는 경로를 기본값으로 썼다.
3. **되묻지 않았다.** "혹시 다운로드 카드로만 보이나요"를 첫 번째에 물었으면
   한 번에 끝났다.

### 지금 어떻게 막고 있나

- 전달 수단이 셋으로 갈렸고 **언제 무엇을 쓸지** CLAUDE.md §8 에 적었다.

  | 상황 | 무엇 |
  |---|---|
  | 눌러 봐야 한다 | `npm run im:section` → `section-preview.html` (파일) |
  | 보이기만 하면 된다 | `npm run im:static` → `section-static.html` (스크립트 0줄) |
  | **내려받기 카드로만 뜬다** | `npm run im:artifact` → 조각을 **주소로 여는 페이지로 올린다** |

- `publishable()` 이 올리기 전에 막는다 — doctype·`<html>`·`<head>`·`<body>`·
  `<script>`·`<iframe>`·바깥 주소가 있으면 **올리지 않고 실패한다.**
  이 조건을 어긴 조각은 옆 창에서 **빈 화면**이 되는데, 빈 화면은 무엇이
  잘못됐는지 아무 말도 해 주지 않는다.
- `flow.test.js` 가 `publishable()` 의 여섯 가지 위반을 각각 검사한다.

### 다음에 같은 일이 생기면

**같은 수단을 두 번 반복하지 않는다.** 한 번 안 통했으면 그 다음은 다른 수단이다.
그리고 "지금 어떻게 보이는지" 를 묻는다 — 화면 한 장이 열 문장보다 빠르다.

---

## M-02. 상태를 바꾸면서 갱신시각을 같이 안 바꿨다

**등급: 심각 (조용히 틀린다)**

### 규칙 — 상태 변경 시 단일 출처 `updatedAt` 필수

**상태를 바꾸는 코드는 갱신시각을 함께 남긴다. 그 시각은 한 곳에서만 만든다.**

- 값·상태·산출물이 바뀌었는데 갱신시각이 그대로면, 화면은 **옛 시각을 보여주면서
  새 값을 보여준다.** 보는 사람은 "안 바뀌었네" 하고 옛 기준으로 판단한다.
  값이 틀린 것보다 나쁘다 — 틀린 줄을 모르기 때문이다.
- 시각을 여러 곳에서 만들면 **어느 것이 진짜인지 알 수 없게 된다.** 토글은
  A 에서 찍고 저장은 B 에서 찍으면, 되돌린 뒤 화면마다 다른 시각이 뜬다.
- 이 저장소의 단일 출처:
  - `core/kst.js` 의 `kstStamp()` — **모든 시각은 여기서 나온다** (Asia/Seoul 명시)
  - `core/facts.js` 의 `Fact.lastUpdated` — 값 하나의 갱신시각
  - `ui/platform/changes.js` 의 `latestAt()` — 화면 변경 내역의 최신 반영일
    (목록에서 뽑는다. 손으로 적으면 안 맞는 날이 온다)
- **화면이 스스로 시각을 만들지 않는다.** 서버가 준 것을 그리기만 한다.
  브라우저 로컬타임으로 찍으면 사용자 시간대에 따라 값이 달라진다 (CLAUDE.md §5).

### 상태 토글 기능 검증 체크리스트

상태를 켜고 끄는 기능(아코디언·잠금·승인·발행·선택 등)을 만들거나 고칠 때 **전부** 확인한다.

- [ ] **켠 뒤 끄면 원래대로 돌아오는가** — 두 번 눌러 처음 상태와 같은지 본다
- [ ] **갱신시각이 같이 바뀌는가** — 단일 출처(`kstStamp()`)에서 나온 값인가
- [ ] **되돌렸을 때 갱신시각도 되돌아가는가, 아니면 새로 찍히는가** — 어느 쪽이
      맞는지 정하고 **코드에 적는다.** 정하지 않으면 사람마다 다르게 읽는다
- [ ] **새로 고치면 상태가 유지되는가** — 화면에만 있고 서버에 없으면 사라진다
- [ ] **두 곳에서 같은 상태를 보여주는가** — 목록과 상세, 레일과 화면 안 칩처럼
      같은 것을 두 번 그리면 한쪽만 바뀌는 날이 온다 (실제로 있었다 — §M-03 옆)
- [ ] **접힌 상태로도 필요한 정보가 보이는가** — 목록은 접어도 되지만 **이유는
      접지 않는다.** 접었을 때 "왜 못 누르는가"가 사라지면 고장으로 읽힌다
- [ ] **미리 그려 넣는 판에서는 펴 두었는가** — 눌러 볼 수 없는 판이므로 접힌 채
      그리면 목록이 영영 안 보인다 (`build-static.js` 의 `EXPAND`)
- [ ] **aria-expanded 가 실제 상태와 같은가** — 화면과 보조기기가 갈리면 안 된다
- [ ] **테스트가 상태 두 가지를 다 보는가** — 켠 것만 검사하면 끄는 쪽이 깨져도 통과한다

### 아직 안 된 것

이 규칙을 **코드에서 강제하는 장치가 없다.** 지금은 체크리스트뿐이다.
상태를 바꾸는 함수가 늘어나면 "갱신시각을 안 남기는 상태 변경"을 잡는 검사를
넣어야 한다 (예: 상태 저장 경로를 한 함수로 모으고, 그 함수만 시각을 찍게 한다).

---

## M-03. 화면이 통째로 비는데 오류는 안 뜬다

**경위**: 화면 문서를 `<script>` 안에 심을 때 닫는 태그만 이스케이프했다.
문서 안에 `<!--` 와 여는 `<script` 가 들어 있어 브라우저의 파싱 상태가 바뀌었고,
블록 전체가 문법 오류가 되어 **화면이 통째로 비었다.** 오류는 콘솔에만 떴다.

**막는 법**: 심을 때 `<` 를 전부 `<` 로 바꾼다. 테스트가 `</script`·`<script`·
`<!--` 가 날것으로 남지 않았는지 검사한다. 그리고 **전달 전에 헤드리스로 실제
렌더를 확인한다** — DOM 이 비었는데 오류가 안 나는 경우가 있다.

---

## M-04. 그럴듯하게 틀린 값이 출처까지 달고 들어온다

**경위**: 한글 PDF 는 대부분 CID 폰트라 ToUnicode 없이는 코드값이 글자가 아니다.
그대로 뽑으면 「연면적 4」 같은 값이 파일명·페이지까지 달고 보고서에 실린다.
조회는 성공하므로 **화면에는 아무 경고도 안 뜬다.**

**막는 법**: 파서가 자신 없으면 던지거나 `reliable:false` 로 내린다.
기준 둘 — ToUnicode 없는 글자 15% 초과(PDF), 읽을 수 있는 글자 75% 미만(OLE).
그러면 자동으로 OCR 경로로 넘어간다. **틀린 글자를 내놓느니 못 읽었다고 한다.**

---

## M-05. 확인 화면이 손으로 적은 옛말을 한다

**경위**: 화면이 없는 작업(파서·커넥터)은 "됐습니다"라는 말밖에 안 남는다.
확인용 결과를 손으로 적어 두면 **코드가 바뀐 날부터 화면만 옛말을 하고,
그때는 아무도 눈치채지 못한다.**

**막는 법**: 확인 패널의 결과는 **빌드할 때 실제로 돌려서** 넣는다.
`flow.test.js` 가 빌더 소스에 결과 문자열(`land.area_sqm = 12345` 등)이
박혀 있지 않은지 검사한다.

---

## M-06. 키가 로그에 평문으로 남았다

**경위**: `redact()` 가 40자 이상 문자열만 가렸는데, VWORLD_KEY 는 36자(UUID),
ECOS_API_KEY 는 20자다. **길이 규칙에 안 걸려 로그에 그대로 남았다.**
이 저장소는 public 이다.

**막는 법**: 길이로 판단하지 않는다. `SECRET_ENV` 에 이름을 적어 두고
**환경변수 값 자체를 문자열 치환**으로 가린다 (URL 인코딩된 형태까지).

---

## M-07. 화면은 보이는데 스크롤이 안 된다

**경위**: 화면 넷을 한 문서로 이어 붙일 때, 각 화면의 CSS 는 `#scr-xx` 안으로
가뒀다. 그런데 **문서 끝에 덧붙인 `<style>` 은 브라우저가 `body` 안에 넣는다.**
본문만 꺼내 오면서 그 사본이 **가두지 않은 채로** 딸려 왔고, 그 안의
`body{overflow-y:hidden!important}` 가 **바깥 페이지의 스크롤을 잠갔다.**

화면은 멀쩡히 그려지고 오류도 없다. 아래로 안 내려갈 뿐이다 — 고장으로 읽힌다.

**막는 법**: 본문에서 `<style>` 을 걷어낸다 (스타일은 이미 따로 모아 가뒀다).
테스트가 `inlineScreen()` 결과에 스타일 사본이 없는지, `css` 에 가두지 않은
`body{` 규칙이 없는지 본다. 그리고 **칸 자체에 `overflow:hidden` 을 걸지 않는다** —
안쪽이 잘린다.

**교훈**: 이어 붙일 때 위험한 것은 CSS 만이 아니다. **어디에 있던 것을 옮기는지**
를 봐야 한다. head 에 있던 것과 body 에 있던 것은 다르게 다뤄야 한다.

## M-08. 테스트 733건이 전부 통과하는데 HTTP 호출은 하나도 안 된다

**경위**: Rhino.Compute 를 붙이면서 `connectors/http.js` 의 `request()` 에 POST
본문 파라미터를 더했다. 이름을 `body` 로 뒀는데, 같은 함수 안에 **응답 본문**을
받는 `const body` 가 이미 있었다.

```js
const init = { method, headers, signal };
if (body !== undefined) init.body = body;      // ← 여기서 죽는다
const r = await fetch(url, init);
const body = binary ? ... : await r.text();    // ← 이 선언이 위를 가린다 (TDZ)
```

**섀도잉이라 문법 오류가 안 난다.** 런타임에 `Cannot access 'body' before
initialization` 만 나오는데, 이 메시지는 HTTP 문제처럼 보이지도 않는다.
그리고 **커넥터 15개가 전부 이 함수를 지난다** — 엔진의 모든 외부 조회가 죽었다.

**왜 테스트가 못 잡았나**: 733건 중 `request()` 를 실제로 부르는 것이 하나도
없었다. 커넥터 테스트는 전부 「키가 없으면 unavailable」 분기에서 끝나고,
`npm run im:demo` 는 `IM_AGENT_OFFLINE=1` 로 돈다. **키가 없는 환경에서는
이 결함이 영원히 안 보인다.**

**막는 법**: `test/http-live.test.js` — Node 내장 `http` 로 **localhost 서버**를
띄우고 실제로 부른다 (의존성 0, 바깥 네트워크 안 탐). GET·POST 본문·재시도·
타임아웃·binary 를 전부 실호출로 확인한다. 고치기 전 코드로 돌리면 8건 중
**7건이 실패**하는 것을 확인했다.

**교훈**: **「부르지 않는 테스트」는 그 코드가 도는지 말해 주지 않는다.**
키가 필요한 코드일수록 그렇다 — 키가 없으면 분기에서 빠져나가므로, 진짜 경로는
한 번도 안 밟힌다. 외부와 말을 섞는 함수는 **가짜 상대라도 세워 두고 실제로
불러 봐야** 한다.

---

## M-09. 「설치했다」는 선언만 보고 산출물을 안 봤다

**경위**: 한글 글꼴 문제(등록부 D-52)를 「CI 에서 `fonts-noto-cjk` 를 설치한다」로
닫았고, 그것을 지키는 테스트도 넣었다.

```js
assert.match(wf, /fonts-noto-cjk/);   // ← 워크플로에 그 글자가 있는지만 본다
```

**설치는 실제로 됐다.** 그런데 그 꾸러미가 심는 이름은 `Noto Sans CJK KR` 이고
문서가 요청한 이름은 `Noto Sans KR` 이었다. **이름이 달라 요청이 통하지 않았고**,
브라우저는 말없이 다른 CJK 글꼴을 골랐다 — CI 에서는 일본어(`Noto Sans CJK JP`),
CJK 글꼴이 여럿 깔린 기계에서는 중국어(`WenQuanYi Zen Hei`).

**하루 만에 같은 증상으로 돌아왔는데 테스트는 전부 초록이었다.**

**어떻게 잡았나**: 초록으로 끝난 CI 실행의 **본문**을 읽다가 나왔다.

```
⚠ 글꼴: 요청한 글꼴이 없다: Noto Serif KR, Noto Sans KR
   — 대신 Noto Sans CJK JP 로 그려진다
```

코드는 제 할 일을 하고 있었다 — **경고를 냈다.** 아무도 안 봤을 뿐이다.
성공한 실행의 로그는 보통 안 읽는다.

**막는 법**: 선언이 아니라 **산출물을 본다.** 실제로 PDF 를 만들어 열고, 거기
박힌 글꼴 이름에 `WenQuanYi`·`CJKjp`·`CJKsc`·`CJKtc` 가 있으면 실패시킨다.
한국어 글꼴이 **있는지**도 함께 본다 — 「없다」만 보면 글자가 통째로 빠져도
통과한다. 같은 기계에서 전후를 쟀다: 전 `WenQuanYiZenHei` → 후 `NotoSansCJKkr`.

**교훈 둘.**

1. **환경을 맞추는 명령과 그 환경이 산출물에 닿는 것은 다른 일이다.** 둘을 잇는
   검사가 없으면 그 사이는 반드시 벌어지고, 벌어진 것은 **초록 화면 뒤에서**
   벌어진다.
2. **성공한 실행의 로그에도 답이 있다.** 실패했을 때만 로그를 보면, 코드가
   애써 남긴 경고를 아무도 읽지 않는다.

---

## M-10. 아무도 안 고쳤는데 자정에 CI 가 빨개졌다

**경위**: 다른 세션이 파이프라인 배선을 푸시했더니 CI 가 실패했다.
실패한 것은 **「커밋된 `section-preview.html` 이 소스와 같다」** 하나였고,
그 사람이 만진 파일과는 아무 상관이 없었다.

재생성해서 차이를 봤더니 **한 줄뿐이었다.**

```
- … · 2026-08-17 읽음 · sha256 c43506c113d3 · 사본 보관 안 함 · 원본 재확인 불가
+ … · 2026-08-18 읽음 · sha256 c43506c113d3 · 사본 보관 안 함 · 원본 재확인 불가
```

**날짜가 바뀐 것이 원인이었다.** [눈으로 확인] 패널은 규칙대로(§8) 코드를
**실제로 돌려** 그 출력을 싣는데, 그 출력에 **지금 시각**이 섞여 있었다.
그래서 산출물이 **날마다 달라진다** — 만든 그날은 통과하고, **자정을 넘기는
순간 깨진다.**

**왜 나쁜가**: 깨지는 시점이 **원인과 무관한 때**다. 코드를 하나도 안 고친
사람이 빨간 CI 를 받고, 원인이 자기 변경에 있다고 믿고 헤맨다. 그리고
**다시 만들어 커밋해도 다음 날 또 터진다** — 고쳐도 고쳐지지 않는다.

**막는 법**: 두 가지를 같이 한다.

1. **시각만 고정하고 코드는 진짜로 돌린다** (`build-preview.js` 의 `DEMO_AT`
   → `oneshot.accept(..., { at })`). 손으로 결과를 적는 것과 다르다 —
   값·지문·판정은 전부 실행 결과 그대로고, 움직이는 것만 묶었다.
   화면에도 **「읽은 시각만 고정값입니다」**라고 적는다.
2. **넣는 그날 걸리는 검사를 둔다** — 확인 패널 안에 오늘 날짜가 있으면 실패.
   자정까지 기다렸다가 남의 변경에 붙어 터지지 않는다.
   (고치기 전 코드로 돌려 **실제로 실패하는 것을 확인**했다)

**교훈**: **커밋되는 산출물에 시계·난수·경로·`readdir` 순서가 새면 안 된다.**
「재생성 결과 = 커밋본」 검사는 그 넷에 전부 걸리는데, **걸리는 시점이 제각각이라
원인을 찾는 데 드는 시간이 결함의 크기와 전혀 비례하지 않는다.**
(순서 문제는 2026-08-17 에 `.sort()` 로 이미 한 번 겪었다 — 같은 계열이다)

---

## 배포 전 교차검증 (건너뛰지 않는다)

**"됐다"고 말하기 전에 전부 통과해야 한다.** 하나라도 못 하면 무엇을 못 했는지
**말하고** 내보낸다 — 조용히 넘어가지 않는다.

1. `npm test` — 전부 통과 (실패 0). 숫자를 답변에 적는다
2. 바꾼 화면을 **헤드리스로 실제 렌더** — DOM 이 비지 않았는지, 바꾼 것이
   화면에 실제로 보이는지 (문자열로 확인)
2-1. **스크롤이 되는지** — `body` 의 `overflow-y` 가 `visible` 인지, 내용 높이가
   화면보다 큰지 재 본다. 보이는데 안 내려가는 화면은 고장으로 읽힌다 (M-07)
3. 미리보기 **재생성** 후 커밋본과 같은지 (테스트가 본다)
4. 화면이 없는 작업이면 **[눈으로 확인] 패널에 실제 실행 결과**가 들어갔는지
5. 전달 수단이 **열리는 것인지** — 파일이면 `publishable()` 이 아니라 상대가
   여는 방식을 확인, 주소로 올릴 것이면 `publishable()` 통과
6. `git status` 가 깨끗한지 — 산출물이 커밋본과 갈리지 않았는지
7. 시크릿이 새지 않았는지 — 새 로그·새 오류 문구에 키가 들어갈 길이 없는지
8. **바꾼 코드가 실제로 불리는지** — 「키가 없으면 건너뛴다」 분기만 타고 있지
   않은지. 외부와 말을 섞는 코드는 가짜 상대라도 세워 두고 불러 본다 (M-08)
9. **초록으로 끝난 CI 의 로그 본문을 읽는다** — 통과했다고 다 맞은 것이 아니다.
   경고 줄(`⚠`)을 훑고, 하나라도 있으면 그것이 무엇인지 답변에 적는다.
   글꼴이 일본어로 바뀐 것은 **성공한 실행의 로그 안에** 적혀 있었다 (M-09)

**그럼에도 문제가 생기면**: 이 파일(MEMORY.md)에서 먼저 찾는다.
같은 종류를 이미 겪었을 가능성이 높고, 해법과 막는 장치가 위에 적혀 있다.
