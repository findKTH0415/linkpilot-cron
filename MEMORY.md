# MEMORY.md — 재발 방지 기록

같은 실수를 두 번 하지 않기 위한 파일이다.

**쓰는 법**
- **문제가 생기면 먼저 여기를 찾는다.** 이미 겪은 일이면 해법이 아래에 있다.
  ("이건 처음 보는 문제 같다"는 느낌은 자주 틀린다 — 검색부터 한다)
- 새 사고가 나면 **그날 바로** 항목을 추가한다. 나중에 쓰면 경위가 사라진다.
- 항목은 지우지 않는다. 해결됐으면 `✅ 막힘` 으로 바꾸고 **무엇이 막고 있는지**
  (테스트·검사 코드) 를 적는다. 막는 장치가 없으면 해결된 것이 아니다.

---

## 색인

| ID | 무엇 | 상태 | 막는 장치 |
|---|---|---|---|
| [M-01](#m-01-화면을-보냈는데-사용자가-못-본다) | 화면을 보냈는데 사용자가 못 본다 (내려받기 카드) | ✅ 막힘 | `publishable()` · `flow.test.js` |
| [M-02](#m-02-상태를-바꾸면서-갱신시각을-같이-안-바꿨다) | 상태를 바꾸면서 갱신시각을 같이 안 바꿨다 — **2026-08-20 재발**(저장소는 고쳤는데 서버는 옛 판) | 🟠 규칙만 | 규칙 「상태 변경 시 단일 출처 `updatedAt` 필수」 + 상태 토글 검증 체크리스트 + 지문 대조(`verify:nas`) |
| [M-03](#m-03-화면이-통째로-비는데-오류는-안-뜬다) | 화면이 통째로 비는데 오류는 안 뜬다 | ✅ 막힘 | `preview.test.js` 태그 짝 검사 |
| [M-04](#m-04-그럴듯하게-틀린-값이-출처까지-달고-들어온다) | 그럴듯하게 틀린 값이 출처까지 달고 들어온다 | ✅ 막힘 | `reliable:false` · 읽을 수 있는 글자 비율 |
| [M-05](#m-05-확인-화면이-손으로-적은-옛말을-한다) | 확인 화면이 손으로 적은 옛말을 한다 | ✅ 막힘 | `flow.test.js` — 빌더에 결과 문자열 금지 |
| [M-06](#m-06-키가-로그에-평문으로-남았다) | 짧은 인증키가 로그에 평문으로 남았다 | ✅ 막힘 | `redact()` + `SECRET_ENV` |
| [M-07](#m-07-화면은-보이는데-스크롤이-안-된다) | 화면은 보이는데 스크롤이 안 된다 | ✅ 막힘 | `inlineScreen()` 스타일 사본 제거 + 테스트 |
| [M-08](#m-08-테스트-733건이-전부-통과하는데-http-호출은-하나도-안-된다) | 테스트가 다 통과하는데 HTTP 호출은 하나도 안 된다 | ✅ 막힘 | `http-live.test.js` (localhost 실호출) |
| [M-09](#m-09-설치했다는-선언만-보고-산출물을-안-봤다) | 「설치했다」는 선언만 보고 산출물을 안 봤다 (한글이 일본어 글꼴로) | ✅ 막힘 | `pdf-fonts.test.js` — 실제 PDF 에 박힌 글꼴 이름 |
| [M-10](#m-10-아무도-안-고쳤는데-자정에-ci-가-빨개졌다) | 아무도 안 고쳤는데 **자정에** CI 가 빨개졌다 (커밋 산출물에 시계가 샜다) | ✅ 막힘 | `flow.test.js` — 확인 패널에 오늘 날짜 금지 |
| [M-11](#m-11-남이-우리-표를-손으로-옮겨-적고-있었다) | 남이 우리 표를 **손으로 옮겨 적고** 있었다 → 라우트 11개 누락 404 | ✅ 막힘 | `routes.test.js` — 표=등록 대조 + 순수 http 실호출 |
| [M-12](#m-12-막으라고-만든-검사가-옛-판을-초록으로-냈다) | **막으라고 만든 검사**가 옛 판을 초록으로 냈다 (`200` 만 보고 지문을 안 댐) | ✅ 막힘 | `nas-guard.test.js` — 옛 판 서버를 세워 **실패하는지** 재 본다 |
| [M-13](#m-13-편하려고-넣은-기본값이-곧-공개였다) | **편하려고 넣은 기본값**이 곧 공개였다 (public 저장소에 NAS 계정·주소) | ✅ 막힘 | `secrets.test.js` — 추적 파일 전부를 훑는다 |
| [M-14](#m-14-방금-넣은-사람에게-안-넣었다고-말했다) | **방금 넣은 사람에게** 「자료를 먼저 넣어 주십시오」라고 말했다 | ✅ 막힘 | `scan.test.js` · `files-tab.test.js` — 신고된 흐름을 그대로 재현 |
| [M-15](#m-15-ssh-는-보이는데-scp-는-없다고-한다) | **ssh 는 보이는데 scp 는 「없다」고 한다** — 같은 경로, 다른 창구 | ✅ 막힘 | `workflow-default.test.js` — `scp -O` 가 빠지면 잡는다 |
| [M-16](#m-16-받은-것과-읽은-것을-같은-말로-했다) | **받은 것과 읽은 것을 같은 말로 했다** — 「2개를 올렸습니다」인데 둘 다 못 읽었다 | ✅ 막힘 | `upload-unread.test.js` — 실제로 올려 보고 화면을 읽는다 |
| [M-17](#m-17-모른다가-바뀌었다로-둔갑했다) | **「모른다」가 「바뀌었다」로 둔갑했다** — 멀쩡한 자료에 거짓 경보 | ✅ 막힘 | `linked.test.js` — 실려 나가는 구현이 주는 모양(`rev: null`)으로 잰다 |
| [M-18](#m-18-실패-하나가-화면을-통째로-갈아치웠다) | **실패 하나가 화면을 통째로 갈아치웠다** — 올리기 401 이 「로그인하세요」로 둔갑 | ✅ 막힘 | `upload-unread.test.js` · `upload-stall.test.js` |
| [M-19](#m-19-서버는-숫자로-세고-화면은-목록으로-세었다) | 서버는 **숫자**로 세고 화면은 **목록**으로 세었다 — 「값 undefined개」 | ✅ 막힘 | `upload-e2e.test.js` — 진짜 응답을 화면 함수에 먹인다 |
| [M-20](#m-20-초록으로-끝난-배포가-아무것도-올리지-않았다) | **초록으로 끝난 배포**가 아무것도 올리지 않았다 (dry run 기본값) | ✅ 막힘 | `workflow-default.test.js` · dry run 을 `::warning::` 으로 |
| [M-21](#m-21-진단-도구가-진단을-못-하게-만들었다) | **진단 도구가 진단을 못 하게** 만들었다 — 없는 함수에서 터져 뒤가 안 돌았다 | ✅ 막힘 | `smoke-tool.test.js` — 호출하는 함수가 실재하는지 |
| [M-22](#m-22-16개-중-1개만-올려서-화면이-통째로-비었다) | **16개 중 1개만** 올려서 화면이 통째로 비었다 (짝이 깨진 배포) | ✅ 막힘 | `workflow-default.test.js` — 기본값 = `required()` |
| [M-23](#m-23-덜-읽힌-파일이-조용히-빠진-채로-올라갔다) | **덜 읽힌 파일이 조용히 빠진 채로** 올라갔다 — 2개를 골랐는데 「1개를 올렸습니다」 | ✅ 막힘 | `upload-limits.test.js` — 읽는 중이면 단추를 잠근다 |
| [M-24](#m-24-fetch-만-덮어서-올리기만-401-이-됐다) | **`fetch` 만 덮어서 올리기만 401** — 목록은 되는데 자료만 「로그인이 필요합니다」 | ✅ 막힘 | `embed.test.js` — 진짜 XHR 을 보내 헤더를 받아 본다 |
| [M-25](#m-25-디스크와-http-가-달랐고-나는-원인을-하나로-단정했다) | **디스크 ≠ HTTP 를 「캐시」로 단정했다** — 틀렸다. 잰 길이 앱이 쓰는 길과 달랐다 | 🟠 잡는 장치만 | `deploy-nas` 의 「Check served build」 — 세 지문을 재되 **원인은 셋으로 열어 둔다** |
| [M-46](#m-46-근거-없는-합격선을-넣어-정상-완료된-agent-를-막았다) | **근거 없는 「요구 90점」**을 넣어 정상 완료된 재무모델(0.76)이 막혔다 | ✅ 막힘 | `orchestrator.test.js` — 합격선은 `registry.confidenceThreshold` 만 쓴다 |
| [M-47](#m-47-무한회전은-실패보다-나쁘다-빨갛지-않기-때문이다) | **무한회전** — 못 하는 전이에서 예외가 나고 catch 가 상태를 되돌려 같은 Task 가 23번 돌았다 | ✅ 막힘 | `orchestrator.test.js` — 한 회전에 아무것도 안 바뀌면 멈춘다 |
| [M-48](#m-48-갈래-넷이-각자-번호를-붙여-네-벌이-겹쳤다-세-번째-재발이다) | **번호가 네 벌 겹쳤다 (D·M 양쪽)** — 갈래마다 같은 번호가 다른 뜻. 세 번째 재발 | ✅ 막힘 | `npm run d:check -- --all` · `registry-check.test.js` |
| [M-49](#m-49-판-지문이-미리보기를-덮지-않는다-셋이-같은-지문을-찍고-있었다) | **판 지문이 미리보기를 안 덮는다** — 갈래 셋이 내용은 다른데 같은 `3f9e0360` 을 찍었다 | 🟠 등록만 (D-114) | 실측: 묶음 16/16 동일인데 미리보기는 달라짐 |

| [M-26](#m-26-막으라고-만든-검사가-바꾼-것이-있는-날에만-막았다) | **막으라고 만든 검사가 「바꾼 것이 있는 날」에만 배포를 막았다** — 올리기 전에 잣대를 뒤와 똑같이 썼다 | ✅ 막힘 | `workflow-default.test.js` — 앞 탐침은 `LP_BEFORE=1`, 뒤 확인은 그대로 치명 |
| [M-27](#m-27-판을-가리라고-만든-장치를-아무도-못-보고-있었다) | **판 지문을 속성에만 박아 사진에 안 찍혔다** — 판을 가리라고 만든 장치가 정작 안 보였다 | ✅ 막힘 | `build-stamp.test.js` — **실제로 그려서** 화면 여섯 전부를 잰다 |
| [M-28](#m-28-null-을-주소에-붙여서-404-를-만들고-서버를-뒤졌다) | **`null` 을 주소에 붙여 404 를 만들고 서버를 뒤졌다** — 「서버 오류」라고 적은 것은 우리다 | ✅ 막힘 | `files-tab.test.js` — 문지기가 `fetch` 앞인가 · XHR 길에도 있나 · 두 404 를 가르나 (빼고 돌려 빨개지는 것 확인) |
| [M-29](#m-29-배포는-초록인데-브라우저만-옛-스크립트를-들고-있었다) | **배포는 초록인데 브라우저만 옛 스크립트를 들고 있었다** — 화면은 멀쩡히 뜨고 「… is not a function」 하나만 남았다 | ✅ 막힘 | 형제 주소에 판 표시(`?v=`) + 화면 안 짝 확인 + `pair-check.test.js` (되돌려 빨개지는 것 확인) |
| [M-30](#m-30-재는-장치는-만들었는데-재고-나서-그래서-왜를-안-말했다) | **재는 장치는 만들었는데 재고 나서 「그래서 왜」를 안 말했다** — 「OCR: 꺼짐」 한 마디로 원인 다섯이 구분이 안 됐다. 그리고 「켜짐」은 **열쇠가 살아 있다는 뜻이 아니었다** | ✅ 막힘 | `tools/env-doctor.js` 가 엔진의 파서로 읽어 보고 무엇이 걸렸는지 말한다 · `--live` 로 실제로 물어본다 · **손으로 놓는 길은 세 번 실패한 뒤 폐기**(배포가 Secret 을 파일로 놓는다) |
| [M-31](#m-31-교차검증-넷을-손으로-챙기다-빠뜨렸다) | **교차검증 넷을 손으로 챙기다 빠뜨렸다** — 미리보기 재생성 세 번, 지문 두 번, 헤드리스 한 번. 「할 줄 몰라서」가 아니라 「빠뜨려서」다 | ✅ 막힘 | `npm run guard` 한 줄로 넷을 돈다 · 못 잰 것은 통과로 안 센다 · 배포는 `dry_run=false`·`engine=yes` 를 반드시 준다 |
| [M-32](#m-32-생성-중-문제가-생겼습니다-까닭이-한-줄도-없었다) | **「생성 중 문제가 생겼습니다」만 뜨고 까닭이 없었다** — 실패한 Agent 와 그 사유는 monitor 가 들고 있었는데 화면도 활동 기록도 안 꺼내 썼다. 사진 두 장을 받고도 원인을 못 찾았다 | ✅ 막힘 | 실패·막힘은 `activity.jsonl` 에 ERROR 로 남긴다 · 화면이 **단계 이름 + 사유**를 머리말 밑에 빨갛게 적는다 |
| [M-33](#m-33-열기가-또-아무것도-안-열었다-이번엔-틀-안이라서다) | **[열기] 가 또 아무것도 안 열었다** — 앱 화면은 틀(iframe) 안이라 스크립트가 여는 새 창이 막힌다. 게다가 안내가 원인을 브라우저 탓으로 단정하고, 붙여 넣을 수도 없는 상대 주소를 줬다 | ✅ 막힘 | 단추를 **진짜 링크**로 만들어 여는 일을 브라우저에 맡긴다 · 막혀도 링크의 기본 동작을 안 막는다 · 안내 주소는 **절대 주소이고 누를 수 있어야** 한다 |
| [M-35](#m-35-고치다-새-사고를-냈다-blob-으로-열면-글자표가-사라진다) | **고치다 새 사고를 냈다** — M-33 을 고치며 `r.blob()` 로 바꿨더니 IM 본문이 통째로 `蹂묒뿭` 로 깨졌다. blob 은 응답의 charset 을 브라우저가 무시할 수 있다 | ✅ 막힘 | 글자 파일은 `r.text()` 로 받아 **UTF-8 로 다시 담는다** · 딱지는 브라우저가 아는 형식으로 · **고친 자리를 실제로 열어 본다** |
| [M-36](#m-36-저장-막대가-표에서-1946px-떨어져-섰다-틀-안에서만-틀린다) | **[저장]이 표에서 1946px 떨어져 섰다** — 앱의 틀(iframe)은 내용만큼 늘어나므로 `position: fixed; bottom: 0` 이 「화면 아래」가 아니라 **「틀 아래」**가 된다. 글자 대조로는 안 갈리고 **좌표로만** 갈린다 | ✅ 막힘 | 틀 안에서는 흐름 안에 두고 `.main` 안에 넣는다 · **좌표로 재는 검사**(`probe-save-bar.js`)를 `npm run guard` 가 부른다 |
| [M-37](#m-37-빈-목록이-읽을-것이-없다로-넘어갔다-자료가-있는데-0건으로-성공했다) | **자료가 폴더에 있는데 0건으로 성공했다** — 읽으라고 부르며 빈 배열을 넘겼고, 받는 쪽은 **빈 배열도 참**이라 폴더를 안 훑었다. 오류도 경고도 없이 「추출 0건 / 문서 0건 / 미지원 0건」 | ✅ 막힘 | 줄 것이 있을 때만 준다 · 빈 목록이 오면 **말한다** · 실제 프로젝트로 끝까지 따라가는 검사를 붙였다 |
| [M-34](#m-34-자료에서-읽었습니다가-거짓말이었다) | **「N개의 값을 자료에서 읽었습니다」가 거짓말이었다** — 그 값들은 전부 요청문(`user_request`)에서 뽑은 것이고 문서에서 읽은 것은 **하나도 없었다.** 이 저장소의 존재 이유인 구분을 화면이 뭉갰다 | ✅ 막힘 | **수집자료**(문서에서 읽음)와 **자체분석자료**(요청문·공공데이터·계산)를 나란히 센다 · **출처 등급을 모르면 「읽었다」로 안 센다** |
| [M-38](#m-38-끝냈다는-말만-있고-반영된-모습이-없었다) | **끝냈다는 말만 있고 반영된 모습이 없었다** — 반영 샘플 제시·패턴 지시 자기 일반화·개선안 상시 추천을 규칙화 (2026-08-25 사용자 지시) | 🟠 규칙만 | CLAUDE.md §7·§8 규칙 + 이 색인(`memory-index.test.js` 가 지킨다) |
| [M-39](#m-39-3d-를-만들고도-보여주지-않았다) | **3D 를 만들고도 보여주지 않았다** — 모델이 생기면 언제나 드래그 회전 HTML 뷰어(같은 주소)로 연다 (2026-08-25 사용자 지시) | 🟠 규칙만 | CLAUDE.md §8 규칙 — 「3D 모델 뷰어」 아티팩트를 같은 파일 경로로 재발행해 주소 유지 |
| [M-50](#m-50-미리보기-표의-셀이-두세-줄로-접혀-읽기-어려웠다) | **미리보기 표의 셀이 두세 줄로 접혀 읽기 어려웠다** — 표 셀은 한 줄(nowrap), 모자라면 표가 가로 스크롤, 긴 말은 표 밖으로 (2026-08-25 사용자 지시) | 🟠 규칙만 | CLAUDE.md §6-3 규칙 + 이 색인(`memory-index.test.js` 가 지킨다) |
| [M-51](#m-51-실사-톤-조감도를-계산으로-그렸더니-세-군데가-거짓말을-했다) | **실사 톤 조감도를 계산으로 그렸더니 세 군데가 거짓말을 했다** — 셀마다 그림자를 겹쳐 칠해 줄무늬 · 가로수를 맨 나중에 그려 타워 앞으로 튀어나옴 · 그림자가 바닥 밖 하늘 위에 떠 있음 (2026-08-25) | 🟢 고침 | 그림자 한 경로에 모아 한 번 칠하기 · 판때기를 면과 같은 깊이 목록에 넣기 · 바닥 폴리곤으로 clip |
| [M-52](#m-52-갈래를-삭제하라는-지시를-받았는데-아티팩트를-지울-수단이-없었다) | **갈래를 삭제하라는 지시를 받았는데 아티팩트를 지울 수단이 없었다** — 조용히 남겨 두면 옛 산출물이 본건인 척 돌아다닌다 (2026-08-25 사장님 지시) | 🟠 규칙만 | 내용을 「본건 제외」 안내로 덮고, 되살릴 길(판 기록·작업 파일)을 그 안내에 적는다 — CLAUDE.md §8 |
| [M-53](#m-53-설계-도면을-그렸더니-css-한-줄이-해칭을-전부-지웠다) | **설계 도면을 그렸더니 CSS 한 줄이 해칭을 전부 지웠다** — `fill:none` 을 CSS 에 적으면 `fill` **속성**을 이긴다. 되살렸더니 이번엔 문 열림 호가 먹칠이 됐다 (2026-08-25 사장님 지시 「설계사 퀄리티」) | 🟢 고침 | 선은 CSS·채움은 속성 · 치수 오프셋은 시트 mm · 축척마다 표현이 다르다 |
| [M-54](#m-54-도로가-도면과-3d-에서-서로-다른-변에-붙어-있었다) | **도로가 도면과 3D 에서 서로 다른 변에 붙어 있었다** — 둘 다 「남측」이라 부르는데 한쪽은 y=0, 한쪽은 y=100 이었다. 이름이 같아서 아무도 못 봤다 (2026-08-25) | 🟢 고침 | 방위는 이름이 아니라 좌표로 맞춘다 · 차량은 관례 표기 · 16:9 팩 규격은 CLAUDE.md §6-1-2 |
| [M-55](#m-55-법규-판정이-내-손에만-있었다-그리고-열쇠-이름이-또-갈렸다) | **법규 판정이 내 손에만 있었다 — 그리고 열쇠 이름이 또 갈렸다** — 손으로 낸 판정은 딜이 바뀌면 재현이 안 되고, Secrets 에는 `LAW_OC` 와 `LAW_OPEN_DATA` 가 **둘 다** 있었다 (2026-08-25 사장님 지시) | 🟢 고침 | 판정을 `core/codecheck.js` 로 내리고 시험이 고정 · 열쇠는 **두 이름을 다 읽는다** |
| [M-40](#m-40-secrets-에-넣은-열쇠-열하나가-nas-로-안-가고-있었다) | **Secrets 에 넣은 열쇠 열하나가 NAS 로 안 가고 있었다** — 엔진이 아는 22개 중 배포가 싣던 것은 11개뿐. 넣은 사람은 넣었다 하고, 엔진은 없다 하고, 배포는 초록 (2026-08-25) | ✅ 막았다 | `secrets.test.js` 가 두 목록이 같은지 지킨다 · D-12 |
| [M-41](#m-41-진단이-틀렸다-읽고-있던-저장소가-옛-판이었다) | **진단이 틀렸다 — 읽고 있던 저장소가 옛 판이었다** — 작업 컨테이너가 열닷새 전 커밋으로 되감겨 있었다. `git status` 는 깨끗했다 (2026-08-25 · 세 번) | 🟠 규칙만 | 파일을 읽기 전에 `git fetch` + `merge --ff-only` · 테스트 수가 줄면 그것이 신호 |
| [M-64](#m-64-읽지-못한-파일을-지웠다-얻은-것은-없고-파일만-사라졌다) | 1회성 업로드가 **읽기 실패한 9건까지** 지웠다 — 열쇠가 전부 429 라 얻은 것이 없었다 | ✅ 고침 | 「안 보관한다」는 **읽은 것**에 대한 약속이다. 실패한 것은 **보관으로 옮겨** 다시 읽게 한다 (D-173) |
| [M-63](#m-63-이름을-베껴-적어-화면만-옛말을-했다) | 단계 이름을 화면 둘이 **손으로 적어** 두어, 이름을 바꾸자 화면만 옛말을 했다 | ✅ 고침 | 목록은 **읽어 온다**. 검사도 「글자가 같은가」가 아니라 **「읽어 오는가」**를 잰다 |
| [M-62](#m-62-재는-장치가-배포를-세워-하루치-일이-안-올라갔다) | 배포 **넷**(#147~#150)이 빨갛게 끝나 화면·엔진이 안 올라갔다 — 죽은 자리는 전부 **재는 단계** | ✅ 고침 | **문턱은 하나**(`Verify deployed`)다. 재는 단계·느린 검사는 배포를 못 세운다 |
| [M-61](#m-61-안내에-적은-설명-문구가-값-칸에-그대로-들어갔다) | 「…주소 (…/im-flow)」라고 적은 **설명 문구**가 GitHub Secret 값 칸에 그대로 붙여 넣어졌다 | ✅ 고침 | 값 칸을 안내할 때는 **「찾는 법」을 먼저** 주고, 배포가 생김새를 보고 사람 말로 알린다 |
| [M-60](#m-60-재려고-넣은-단계가-재기를-막았다-set-e) | 새 배포 단계가 `set +e` 없이 들어와 죽었고, **뒤의 「닿았는가」 확인이 통째로 건너뛰어졌다** | ✅ 고침 | 재는 단계는 `set +e` · 끝에 `exit 0` · **확인이 끝난 뒤에** 둔다 |
| [M-59](#m-59-같은-고장을-어떤-화면은-견디고-어떤-화면은-멈췄다-서버-주소) | 앱이 화면을 **통째로** 열자 `intake` 만 멈췄다 — 주소를 정하는 자리가 화면마다 달랐다 | ✅ 고침 | 읽는 화면 다섯이 `resolveApi()` 한 곳에서 받고, **짐작했다는 사실을 문장에 붙인다** |
| [M-58](#m-58-잠깐-나는-고장을-내가-뭘-잘못했나로-보이게-했다-http-502) | 잠깐 나는 고장(HTTP 502)을 「내가 뭘 잘못했나」로 보이게 해 **같은 자료를 두 번** 올리게 했다 | ✅ 고침 | 5xx 를 일시적이라고 말하고 **고른 파일을 둔 채 [다시 보내기]** · 자동 재시도는 안 한다 |
| [M-57](#m-57-화면이-멀쩡히-뜨는데-글자만-틀렸다-object-object) | 화면이 멀쩡히 뜨는데 **글자만 틀렸다** (`[object Object]`) — 오류를 안 내는 고장 | ✅ 장치 | 교차검증이 **그려진 글자**를 읽어 `[object Object]`·`undefined`·`NaN` 을 잡는다 |
| [M-56](#m-56-잘-설명하는-고장은-고장이다-같은-빨간-띠가-네-번-났다) | **잘 설명하는 고장은 고장이다** — `/im-flow/im-flow/…` 앱이 얹는 자리가 틀려 화면이 안 뜬 일이 **네 번**. 앞 세 번을 「고쳤다」고 보고했다 (겹침만 보고 빠짐을 놓쳤다) (2026-08-26) | ✅ 막힘 | 받기 전에 `<base>` 로 자리를 바로잡는다 · 헤드리스로 **버튼 수**를 센다 (`dup-path-heal.test.js`) |
> **⚠️ 두 작업선을 합쳤다** 〈2026-08-25 · D-101〉. 번호를 비워 두는 것으로는
> 못 막았다 — 저쪽이 M-26~M-32 를 비워 뒀는데 이쪽이 M-33~M-37 까지 써서
> **M-33·M-34 가 두 뜻을 갖게 됐다.** 나중에 온 둘을 M-38·M-39 로 옮겼다.
> ★ 다음부터는 **새 번호를 딸 때 열려 있는 다른 갈래를 함께 본다** (D-102).

---

## M-01. 화면을 보냈는데 사용자가 못 본다

**등급: 심각 (네 번 반복)**

### 경위

2026-08-16. 작업을 마칠 때마다 미리보기 HTML 을 **파일로** 보냈다.
사용자 환경에서는 그 파일이 **내려받기 카드로만** 떴다 — 옆 창에 열리지 않는다.

사용자가 네 번에 걸쳐 "옆 창에서 열어 달라"고 말했고, 나는 그때마다
**같은 방식(파일 전달)을 반복**하면서 문구만 바꿨다. 마지막에 사용자가
화면을 캡처해 보여 준 뒤에야 무엇을 원하는지 알았다 — **주소로 여는 페이지**였다.

### 무엇이 잘못이었나

1. **증상을 듣고도 수단을 안 바꿨다.** "안 열린다"는 말은 "다시 보내 달라"가
   아니라 "다른 방법으로 달라"는 뜻이다. 두 번째 요청부터 방법을 바꿨어야 했다.
2. **전달이 됐는지 확인할 방법이 없었다.** 파일을 보낸 결과는 "보냈다"까지만
   보이고, 그것이 열렸는지는 안 보인다. 확인할 수 없는 경로를 기본값으로 썼다.
3. **되묻지 않았다.** "혹시 다운로드 카드로만 보이나요"를 첫 번째에 물었으면
   한 번에 끝났다.

### 지금 어떻게 막고 있나

- 전달 수단이 셋으로 갈렸고 **언제 무엇을 쓸지** CLAUDE.md §8 에 적었다.

  | 상황 | 무엇 |
  |---|---|
  | 눌러 봐야 한다 | `npm run im:section` → `section-preview.html` (파일) |
  | 보이기만 하면 된다 | `npm run im:static` → `section-static.html` (스크립트 0줄) |
  | **내려받기 카드로만 뜬다** | `npm run im:artifact` → 조각을 **주소로 여는 페이지로 올린다** |

- `publishable()` 이 올리기 전에 막는다 — doctype·`<html>`·`<head>`·`<body>`·
  `<script>`·`<iframe>`·바깥 주소가 있으면 **올리지 않고 실패한다.**
  이 조건을 어긴 조각은 옆 창에서 **빈 화면**이 되는데, 빈 화면은 무엇이
  잘못됐는지 아무 말도 해 주지 않는다.
- `flow.test.js` 가 `publishable()` 의 여섯 가지 위반을 각각 검사한다.

### 다음에 같은 일이 생기면

**같은 수단을 두 번 반복하지 않는다.** 한 번 안 통했으면 그 다음은 다른 수단이다.
그리고 "지금 어떻게 보이는지" 를 묻는다 — 화면 한 장이 열 문장보다 빠르다.

---

## M-02. 상태를 바꾸면서 갱신시각을 같이 안 바꿨다

**등급: 심각 (조용히 틀린다)**

### 규칙 — 상태 변경 시 단일 출처 `updatedAt` 필수

**상태를 바꾸는 코드는 갱신시각을 함께 남긴다. 그 시각은 한 곳에서만 만든다.**

- 값·상태·산출물이 바뀌었는데 갱신시각이 그대로면, 화면은 **옛 시각을 보여주면서
  새 값을 보여준다.** 보는 사람은 "안 바뀌었네" 하고 옛 기준으로 판단한다.
  값이 틀린 것보다 나쁘다 — 틀린 줄을 모르기 때문이다.
- 시각을 여러 곳에서 만들면 **어느 것이 진짜인지 알 수 없게 된다.** 토글은
  A 에서 찍고 저장은 B 에서 찍으면, 되돌린 뒤 화면마다 다른 시각이 뜬다.
- 이 저장소의 단일 출처:
  - `core/kst.js` 의 `kstStamp()` — **모든 시각은 여기서 나온다** (Asia/Seoul 명시)
  - `core/facts.js` 의 `Fact.lastUpdated` — 값 하나의 갱신시각
  - `ui/platform/changes.js` 의 `latestAt()` — 화면 변경 내역의 최신 반영일
    (목록에서 뽑는다. 손으로 적으면 안 맞는 날이 온다)
- **화면이 스스로 시각을 만들지 않는다.** 서버가 준 것을 그리기만 한다.
  브라우저 로컬타임으로 찍으면 사용자 시간대에 따라 값이 달라진다 (CLAUDE.md §5).

### 상태 토글 기능 검증 체크리스트

상태를 켜고 끄는 기능(아코디언·잠금·승인·발행·선택 등)을 만들거나 고칠 때 **전부** 확인한다.

- [ ] **켠 뒤 끄면 원래대로 돌아오는가** — 두 번 눌러 처음 상태와 같은지 본다
- [ ] **갱신시각이 같이 바뀌는가** — 단일 출처(`kstStamp()`)에서 나온 값인가
- [ ] **되돌렸을 때 갱신시각도 되돌아가는가, 아니면 새로 찍히는가** — 어느 쪽이
      맞는지 정하고 **코드에 적는다.** 정하지 않으면 사람마다 다르게 읽는다
- [ ] **새로 고치면 상태가 유지되는가** — 화면에만 있고 서버에 없으면 사라진다
- [ ] **두 곳에서 같은 상태를 보여주는가** — 목록과 상세, 레일과 화면 안 칩처럼
      같은 것을 두 번 그리면 한쪽만 바뀌는 날이 온다 (실제로 있었다 — §M-03 옆)
- [ ] **접힌 상태로도 필요한 정보가 보이는가** — 목록은 접어도 되지만 **이유는
      접지 않는다.** 접었을 때 "왜 못 누르는가"가 사라지면 고장으로 읽힌다
- [ ] **미리 그려 넣는 판에서는 펴 두었는가** — 눌러 볼 수 없는 판이므로 접힌 채
      그리면 목록이 영영 안 보인다 (`build-static.js` 의 `EXPAND`)
- [ ] **aria-expanded 가 실제 상태와 같은가** — 화면과 보조기기가 갈리면 안 된다
- [ ] **테스트가 상태 두 가지를 다 보는가** — 켠 것만 검사하면 끄는 쪽이 깨져도 통과한다

### 이번 사고 경위 (2026-08-20)

**같은 종류가 또 났다 — 이번엔 화면이 아니라 배포에서.**

1. 「자료 업로드」탭에서 빈 목록 문구 다섯 줄을 지웠다.
   저장소 판을 헤드리스로 띄워 **렌더된 글자를 세어** 다섯 줄 전부 `0회`를 확인했다.
2. 그런데 사용자가 실서버 화면을 캡처해 보냈고, **그 문구가 그대로 있었다.**
3. 원인: 고친 것은 저장소이고, NAS 의 `/volume1/web/im-flow/files.html` 은
   **옛 사본**이었다. 서버는 옛 판을 200 으로 잘 돌려주고 있었다.

**왜 이것이 M-02 와 같은 종류인가**: 값(화면 내용)은 바뀌었는데 **그것을 담고 있는
쪽의 판이 갱신되지 않았고, 어디에도 그 사실이 안 남았다.** 화면은 오류를 내지
않는다 — 그냥 **옛말을 한다.** 보는 사람은 "안 고쳐졌네"로 읽고, 고친 사람은
"고쳤는데"로 읽는다. 둘 다 상대가 틀렸다고 생각한다.

**무엇으로 가려냈나**: `npm run verify:nas` 의 **지문 대조**.
저장소 `files.html` 의 sha256 과 서버가 준 바이트를 대면 옛 판이 그 자리에서 드러난다.
`200` 만 보던 판으로는 이것을 못 잡았다 (M-12).

**교훈**: **「고쳤다」는 저장소의 사실이고, 「반영됐다」는 서버의 사실이다.**
둘은 다른 사실이고, 확인하는 방법도 다르다. 저장소에서 확인한 것을 반영됐다고
말하면, 그 순간부터 사람은 서버를 안 본다.

### 아직 안 된 것

이 규칙을 **코드에서 강제하는 장치가 없다.** 지금은 체크리스트뿐이다.
상태를 바꾸는 함수가 늘어나면 "갱신시각을 안 남기는 상태 변경"을 잡는 검사를
넣어야 한다 (예: 상태 저장 경로를 한 함수로 모으고, 그 함수만 시각을 찍게 한다).

### 이어지는 항목 (2026-08-23)

이 종류가 **한 줄 더 뒤에서** 또 났다 — [M-29](#m-29-배포는-초록인데-브라우저만-옛-스크립트를-들고-있었다).
여기 M-02 는 「고쳤다 ≠ 반영됐다」였고, M-29 는 **「반영됐다 ≠ 사용자가 보고
있다」**다. 배포가 초록이고 지문도 새 판인데 **브라우저만 옛 스크립트를 들고**
있었다. M-29 는 그 갈래를 재는 장치까지 넣었으므로 ✅ 이다.

---

## M-03. 화면이 통째로 비는데 오류는 안 뜬다

**경위**: 화면 문서를 `<script>` 안에 심을 때 닫는 태그만 이스케이프했다.
문서 안에 `<!--` 와 여는 `<script` 가 들어 있어 브라우저의 파싱 상태가 바뀌었고,
블록 전체가 문법 오류가 되어 **화면이 통째로 비었다.** 오류는 콘솔에만 떴다.

**막는 법**: 심을 때 `<` 를 전부 `<` 로 바꾼다. 테스트가 `</script`·`<script`·
`<!--` 가 날것으로 남지 않았는지 검사한다. 그리고 **전달 전에 헤드리스로 실제
렌더를 확인한다** — DOM 이 비었는데 오류가 안 나는 경우가 있다.

---

## M-04. 그럴듯하게 틀린 값이 출처까지 달고 들어온다

**경위**: 한글 PDF 는 대부분 CID 폰트라 ToUnicode 없이는 코드값이 글자가 아니다.
그대로 뽑으면 「연면적 4」 같은 값이 파일명·페이지까지 달고 보고서에 실린다.
조회는 성공하므로 **화면에는 아무 경고도 안 뜬다.**

**막는 법**: 파서가 자신 없으면 던지거나 `reliable:false` 로 내린다.
기준 둘 — ToUnicode 없는 글자 15% 초과(PDF), 읽을 수 있는 글자 75% 미만(OLE).
그러면 자동으로 OCR 경로로 넘어간다. **틀린 글자를 내놓느니 못 읽었다고 한다.**

---

## M-05. 확인 화면이 손으로 적은 옛말을 한다

**경위**: 화면이 없는 작업(파서·커넥터)은 "됐습니다"라는 말밖에 안 남는다.
확인용 결과를 손으로 적어 두면 **코드가 바뀐 날부터 화면만 옛말을 하고,
그때는 아무도 눈치채지 못한다.**

**막는 법**: 확인 패널의 결과는 **빌드할 때 실제로 돌려서** 넣는다.
`flow.test.js` 가 빌더 소스에 결과 문자열(`land.area_sqm = 12345` 등)이
박혀 있지 않은지 검사한다.

---

## M-06. 키가 로그에 평문으로 남았다

**경위**: `redact()` 가 40자 이상 문자열만 가렸는데, VWORLD_KEY 는 36자(UUID),
ECOS_API_KEY 는 20자다. **길이 규칙에 안 걸려 로그에 그대로 남았다.**
이 저장소는 public 이다.

**막는 법**: 길이로 판단하지 않는다. `SECRET_ENV` 에 이름을 적어 두고
**환경변수 값 자체를 문자열 치환**으로 가린다 (URL 인코딩된 형태까지).

---

## M-07. 화면은 보이는데 스크롤이 안 된다

**경위**: 화면 넷을 한 문서로 이어 붙일 때, 각 화면의 CSS 는 `#scr-xx` 안으로
가뒀다. 그런데 **문서 끝에 덧붙인 `<style>` 은 브라우저가 `body` 안에 넣는다.**
본문만 꺼내 오면서 그 사본이 **가두지 않은 채로** 딸려 왔고, 그 안의
`body{overflow-y:hidden!important}` 가 **바깥 페이지의 스크롤을 잠갔다.**

화면은 멀쩡히 그려지고 오류도 없다. 아래로 안 내려갈 뿐이다 — 고장으로 읽힌다.

**막는 법**: 본문에서 `<style>` 을 걷어낸다 (스타일은 이미 따로 모아 가뒀다).
테스트가 `inlineScreen()` 결과에 스타일 사본이 없는지, `css` 에 가두지 않은
`body{` 규칙이 없는지 본다. 그리고 **칸 자체에 `overflow:hidden` 을 걸지 않는다** —
안쪽이 잘린다.

**교훈**: 이어 붙일 때 위험한 것은 CSS 만이 아니다. **어디에 있던 것을 옮기는지**
를 봐야 한다. head 에 있던 것과 body 에 있던 것은 다르게 다뤄야 한다.

## M-08. 테스트 733건이 전부 통과하는데 HTTP 호출은 하나도 안 된다

**경위**: Rhino.Compute 를 붙이면서 `connectors/http.js` 의 `request()` 에 POST
본문 파라미터를 더했다. 이름을 `body` 로 뒀는데, 같은 함수 안에 **응답 본문**을
받는 `const body` 가 이미 있었다.

```js
const init = { method, headers, signal };
if (body !== undefined) init.body = body;      // ← 여기서 죽는다
const r = await fetch(url, init);
const body = binary ? ... : await r.text();    // ← 이 선언이 위를 가린다 (TDZ)
```

**섀도잉이라 문법 오류가 안 난다.** 런타임에 `Cannot access 'body' before
initialization` 만 나오는데, 이 메시지는 HTTP 문제처럼 보이지도 않는다.
그리고 **커넥터 15개가 전부 이 함수를 지난다** — 엔진의 모든 외부 조회가 죽었다.

**왜 테스트가 못 잡았나**: 733건 중 `request()` 를 실제로 부르는 것이 하나도
없었다. 커넥터 테스트는 전부 「키가 없으면 unavailable」 분기에서 끝나고,
`npm run im:demo` 는 `IM_AGENT_OFFLINE=1` 로 돈다. **키가 없는 환경에서는
이 결함이 영원히 안 보인다.**

**막는 법**: `test/http-live.test.js` — Node 내장 `http` 로 **localhost 서버**를
띄우고 실제로 부른다 (의존성 0, 바깥 네트워크 안 탐). GET·POST 본문·재시도·
타임아웃·binary 를 전부 실호출로 확인한다. 고치기 전 코드로 돌리면 8건 중
**7건이 실패**하는 것을 확인했다.

**교훈**: **「부르지 않는 테스트」는 그 코드가 도는지 말해 주지 않는다.**
키가 필요한 코드일수록 그렇다 — 키가 없으면 분기에서 빠져나가므로, 진짜 경로는
한 번도 안 밟힌다. 외부와 말을 섞는 함수는 **가짜 상대라도 세워 두고 실제로
불러 봐야** 한다.

---

## M-09. 「설치했다」는 선언만 보고 산출물을 안 봤다

**경위**: 한글 글꼴 문제(등록부 D-52)를 「CI 에서 `fonts-noto-cjk` 를 설치한다」로
닫았고, 그것을 지키는 테스트도 넣었다.

```js
assert.match(wf, /fonts-noto-cjk/);   // ← 워크플로에 그 글자가 있는지만 본다
```

**설치는 실제로 됐다.** 그런데 그 꾸러미가 심는 이름은 `Noto Sans CJK KR` 이고
문서가 요청한 이름은 `Noto Sans KR` 이었다. **이름이 달라 요청이 통하지 않았고**,
브라우저는 말없이 다른 CJK 글꼴을 골랐다 — CI 에서는 일본어(`Noto Sans CJK JP`),
CJK 글꼴이 여럿 깔린 기계에서는 중국어(`WenQuanYi Zen Hei`).

**하루 만에 같은 증상으로 돌아왔는데 테스트는 전부 초록이었다.**

**어떻게 잡았나**: 초록으로 끝난 CI 실행의 **본문**을 읽다가 나왔다.

```
⚠ 글꼴: 요청한 글꼴이 없다: Noto Serif KR, Noto Sans KR
   — 대신 Noto Sans CJK JP 로 그려진다
```

코드는 제 할 일을 하고 있었다 — **경고를 냈다.** 아무도 안 봤을 뿐이다.
성공한 실행의 로그는 보통 안 읽는다.

**막는 법**: 선언이 아니라 **산출물을 본다.** 실제로 PDF 를 만들어 열고, 거기
박힌 글꼴 이름에 `WenQuanYi`·`CJKjp`·`CJKsc`·`CJKtc` 가 있으면 실패시킨다.
한국어 글꼴이 **있는지**도 함께 본다 — 「없다」만 보면 글자가 통째로 빠져도
통과한다. 같은 기계에서 전후를 쟀다: 전 `WenQuanYiZenHei` → 후 `NotoSansCJKkr`.

**교훈 둘.**

1. **환경을 맞추는 명령과 그 환경이 산출물에 닿는 것은 다른 일이다.** 둘을 잇는
   검사가 없으면 그 사이는 반드시 벌어지고, 벌어진 것은 **초록 화면 뒤에서**
   벌어진다.
2. **성공한 실행의 로그에도 답이 있다.** 실패했을 때만 로그를 보면, 코드가
   애써 남긴 경고를 아무도 읽지 않는다.

---

## M-10. 아무도 안 고쳤는데 자정에 CI 가 빨개졌다

**경위**: 다른 세션이 파이프라인 배선을 푸시했더니 CI 가 실패했다.
실패한 것은 **「커밋된 `section-preview.html` 이 소스와 같다」** 하나였고,
그 사람이 만진 파일과는 아무 상관이 없었다.

재생성해서 차이를 봤더니 **한 줄뿐이었다.**

```
- … · 2026-08-17 읽음 · sha256 c43506c113d3 · 사본 보관 안 함 · 원본 재확인 불가
+ … · 2026-08-18 읽음 · sha256 c43506c113d3 · 사본 보관 안 함 · 원본 재확인 불가
```

**날짜가 바뀐 것이 원인이었다.** [눈으로 확인] 패널은 규칙대로(§8) 코드를
**실제로 돌려** 그 출력을 싣는데, 그 출력에 **지금 시각**이 섞여 있었다.
그래서 산출물이 **날마다 달라진다** — 만든 그날은 통과하고, **자정을 넘기는
순간 깨진다.**

**왜 나쁜가**: 깨지는 시점이 **원인과 무관한 때**다. 코드를 하나도 안 고친
사람이 빨간 CI 를 받고, 원인이 자기 변경에 있다고 믿고 헤맨다. 그리고
**다시 만들어 커밋해도 다음 날 또 터진다** — 고쳐도 고쳐지지 않는다.

**막는 법**: 두 가지를 같이 한다.

1. **시각만 고정하고 코드는 진짜로 돌린다** (`build-preview.js` 의 `DEMO_AT`
   → `oneshot.accept(..., { at })`). 손으로 결과를 적는 것과 다르다 —
   값·지문·판정은 전부 실행 결과 그대로고, 움직이는 것만 묶었다.
   화면에도 **「읽은 시각만 고정값입니다」**라고 적는다.
2. **넣는 그날 걸리는 검사를 둔다** — 확인 패널 안에 오늘 날짜가 있으면 실패.
   자정까지 기다렸다가 남의 변경에 붙어 터지지 않는다.
   (고치기 전 코드로 돌려 **실제로 실패하는 것을 확인**했다)

**교훈**: **커밋되는 산출물에 시계·난수·경로·`readdir` 순서가 새면 안 된다.**
「재생성 결과 = 커밋본」 검사는 그 넷에 전부 걸리는데, **걸리는 시점이 제각각이라
원인을 찾는 데 드는 시간이 결함의 크기와 전혀 비례하지 않는다.**
(순서 문제는 2026-08-17 에 `.sort()` 로 이미 한 번 겪었다 — 같은 계열이다)

---

## M-11. 남이 우리 표를 손으로 옮겨 적고 있었다

**경위**: 본체(NAS)가 보내 준 환경 분석서에 이렇게 적혀 있었다 —
**「`im-engine-server.cjs` 가 라우팅 표를 손으로 옮김(28개). 그 결과 새 라우트
11개가 빠져 404 가 났다」**(2026-08-18 본체 실측).

우리 저장소는 잘못한 것이 없어 보였다. `createRouter()` 는 28개를 정확히 걸고
테스트도 초록이었다. **문제는 그 목록을 우리만 갖고 있었다는 것이다** —
express 라우터 등록 코드 안에만 있어서, 다른 서버는 **읽을 방법이 없고
베껴 적는 수밖에 없었다.**

**왜 늦게 드러나나**: 오류가 「없는 주소(404)」다. 서버가 옛 표를 들고 있다는
사실은 **두 목록을 나란히 놓고 세어 보기 전까지** 보이지 않는다. 그리고 그
비교를 할 생각이 들려면 이미 원인을 짐작하고 있어야 한다.

**막는 법**: 표를 **데이터로** 내준다 (`ui/routes.cjs`).

- `ROUTES` 배열이 단일 출처이고, `createRouter()` 도 **그 표에서 만든다** —
  표와 실제 등록이 갈릴 수가 없다. 가짜 라우터를 걸어 **무엇을 등록하는지 받아
  적고 표와 대조**하는 검사를 뒀다.
- `match()` 하나로 **express 없이도** 길을 찾는다. 프레임워크를 강요하면
  결국 또 손으로 옮기게 된다.
- 「부르지 않는 테스트」로 두지 않았다(M-08) — **순수 http 서버를 실제로 세워**
  401·201·501·400·404 를 확인한다.

**교훈**: **우리가 가진 사실을 남이 복사해서 들고 있으면, 그것은 우리 문제다.**
「우리 코드는 맞다」로 끝내면 갈라짐은 상대 쪽에서만 터지고, 우리는 초록 화면을
보고 있다. 남이 베낄 수밖에 없는 모양으로 두지 않는 것까지가 단일 출처다.

---

## M-12. 막으라고 만든 검사가 옛 판을 초록으로 냈다

**경위**: `verify:nas` 는 「앱·CI 는 초록인데 NAS 만 옛 판」을 막으려고 만든
스크립트다. 그런데 화면 파일을 **`200` 이 오는지만** 보고 있었다.

화면 열넷을 전부 3주 전 판으로 돌려주는 가짜 서버를 세워 돌려 봤더니
**「통과 11 · 실패 0」 · 종료코드 0** 이 나왔다. 라우트도 같았다 — 스물여덟 중
**둘만** 물어보고 있어서, 앱 프록시 목록에서 열한 개가 빠졌던 그 사고(M-11)를
이 스크립트로는 못 잡았다.

**왜 늦게 드러나나**: 검사 도구는 **자기가 통과시킨 것을 다시 검사받지 않는다.**
초록을 보면 거기서 멈추고, 그 초록이 무엇을 재고 나온 것인지는 아무도 안 본다.
게다가 `build-embed.js` 주석에는 **「지문을 남긴다 — `verify:nas` 가 대조한다」**
라고 적혀 있었다. 지문은 만들어 두고 **아무도 대조하지 않았다.** 선언이 있으면
사람은 그것이 돌아간다고 믿는다.

**막는 법**:

- 화면은 **서버가 준 바이트의 sha256** 을 `im:embed` 의 manifest 와 댄다.
  `200` 은 「파일이 있다」이지 「그 파일이 지금 판이다」가 아니다.
- 라우트는 **스물여덟을 전부 두드린다.** 판정은 「404 냐 아니냐」 — 401·400 은
  라우트가 있다는 뜻이므로 통과다. 여기서 재는 것은 권한이 아니라 **닿느냐**다.
- 프로젝트가 없어 나는 404(엔진, **JSON**)와 길이 없어 나는 404(프록시, **HTML**)를
  응답 형식으로 가른다. 안 가르면 헛울음이 나고, **헛울음 한 번이면 아무도 안 믿는다.**
- 검사를 **가짜 NAS 를 세워 진짜 돌린다**(`nas-guard.test.js`). 함수 반환값만 보는
  검사였다면 위의 「11통과 0실패」를 똑같이 못 잡았다 (M-08).

**교훈**: **막는 장치도 뚫려 있는지 재 봐야 한다 — 통과가 아니라 실패를 재서.**
「이 검사는 통과한다」는 검사가 아무것도 안 하게 된 날에도 통과한다. 그래서
막혀야 할 것을 **실제로 만들어 넣고**, 그때 빨개지는지를 본다. 이번에 고아 검사도
같은 방법으로 확인했는데, 심어 둔 고아를 **테스트 자신의 주석**이 이어 버려
두 번 헛통과했다.

---

## M-13. 편하려고 넣은 기본값이 곧 공개였다

**경위**: `deploy/nas.sh` 에 NAS **계정과 tailnet IP 를 기본값으로** 박아 두었다.
`LP_NAS_HOST` 를 안 넣어도 돌게 하려던 것이었다. 작업지시서 본문에도 tailnet
주소가 세 곳 있었다. **이 저장소는 public 이다**(D-10) — CLAUDE.md §2 는 키뿐
아니라 **NAS 접속정보**를 함께 금지한다.

**왜 늦게 드러나나**: 시크릿 검사는 보통 **키 모양**(긴 무작위 문자열)을 찾는다.
계정 이름과 주소는 그렇게 안 생겨서 아무 검사에도 안 걸린다. 그리고 기본값은
**잘 도는 동안 아무도 안 본다** — 안 도는 날에만 읽는 줄이다.

**막는 법**:

- 주소가 없으면 **exit 64 로 멈춘다.** 기본값을 두지 않는 것이 곧 막는 장치다.
- `secrets.test.js` 가 **추적되는 파일 전부**를 훑어 tailnet 주소 · `100.64/10`
  대역 IP · `계정@주소` 를 찾는다. 키가 아니라 **접속정보**를 찾는 검사다.

**그 검사도 처음엔 초록이었다**: `git ls-files` 는 한글 파일 이름을
`"docs/\354\236..."` 로 따옴표 이스케이프해서 내준다. 그 경로는 열리지 않아
`catch { continue }` 로 **조용히 건너뛰어졌고**, 하필 주소가 들어 있던 지시서가
그중 하나였다. `-z` 로 받아 NUL 로 끊고, **못 읽은 파일 수를 세서 0 이 아니면
실패**로 본다.

**교훈**: **못 읽은 것을 「깨끗하다」로 세지 않는다.** 훑는 검사는 대상이 조용히
줄어드는 방식으로 망가진다 — 그때 결과는 「없다」로 나오고, 그것은 「찾았는데
없다」와 화면에서 구분되지 않는다. 세어서 드러내야 구분된다. (M-12 와 같은 병이다.)

---

## 배포 전 교차검증 (건너뛰지 않는다)

**"됐다"고 말하기 전에 전부 통과해야 한다.** 하나라도 못 하면 무엇을 못 했는지
**말하고** 내보낸다 — 조용히 넘어가지 않는다.

1. `npm test` — 전부 통과 (실패 0). 숫자를 답변에 적는다
   ★★ **`npm test | grep … && git commit` 으로 엮지 않는다** 〈2026-08-23 · 실제로 밀었다〉.
   `grep` 은 **실패한 줄을 찾아도 0 으로 끝난다** — 뒤의 `&&` 가 그대로 이어져
   **빨간 채로 커밋·푸시된다.** 막는 것처럼 보이는데 안 막는다 (M-26 · M-28 과 같은 종류).
   → `npm test` 를 **따로 돌려 초록을 눈으로 본 뒤** 커밋한다. 엮어야 하면
   `npm test && git commit …` 처럼 **테스트의 종료코드에** 매단다.
2. 바꾼 화면을 **헤드리스로 실제 렌더** — DOM 이 비지 않았는지, 바꾼 것이
   화면에 실제로 보이는지 (문자열로 확인)
2-1. **스크롤이 되는지** — `body` 의 `overflow-y` 가 `visible` 인지, 내용 높이가
   화면보다 큰지 재 본다. 보이는데 안 내려가는 화면은 고장으로 읽힌다 (M-07)
3. 미리보기 **재생성** 후 커밋본과 같은지 (테스트가 본다)
4. 화면이 없는 작업이면 **[눈으로 확인] 패널에 실제 실행 결과**가 들어갔는지
5. 전달 수단이 **열리는 것인지** — 파일이면 `publishable()` 이 아니라 상대가
   여는 방식을 확인, 주소로 올릴 것이면 `publishable()` 통과
6. `git status` 가 깨끗한지 — 산출물이 커밋본과 갈리지 않았는지
7. 시크릿이 새지 않았는지 — 새 로그·새 오류 문구에 키가 들어갈 길이 없는지
8. **바꾼 코드가 실제로 불리는지** — 「키가 없으면 건너뛴다」 분기만 타고 있지
   않은지. 외부와 말을 섞는 코드는 가짜 상대라도 세워 두고 불러 본다 (M-08)
9. **초록으로 끝난 CI 의 로그 본문을 읽는다** — 통과했다고 다 맞은 것이 아니다.
   경고 줄(`⚠`)을 훑고, 하나라도 있으면 그것이 무엇인지 답변에 적는다.
   글꼴이 일본어로 바뀐 것은 **성공한 실행의 로그 안에** 적혀 있었다 (M-09)

10. **막는 장치를 새로 만들거나 고쳤으면, 막혀야 할 것을 실제로 넣어 본다** —
   통과를 재지 말고 **실패를 잰다.** 「이 검사는 통과한다」는 검사가 아무것도
   안 하게 된 날에도 통과한다 (M-12)

11. **같은 일을 하는 길이 둘이면 둘 다 밟아 본다** — 읽기는 `fetch`, 올리기는
   `XMLHttpRequest` 처럼 **길이 갈리는 자리**가 있다. 한쪽만 고치면 다른 쪽이
   조용히 빠지고, 증상은 **엉뚱한 얼굴**로 나온다 (인증이 빠졌는데 「로그인이
   필요합니다」로 보였다 — M-24). 인증·헤더·재시도·로깅처럼 **모든 요청에
   붙어야 하는 것**은 붙이는 자리를 세고, 개수가 길의 개수와 같은지 본다.

12-1. **「반영이 안 된다」는 세 가지다. 셋을 갈라 놓고 말한다** — 저장소·디스크·
   HTTP 의 지문을 각각 잰다. **셋은 화면에서 똑같이 보인다.** 갈라 보기 전에
   「새로고침해 보십시오」라고 말하지 않는다 — 세 번 그렇게 말해서 세 번 틀렸다
   (M-25). `deploy-nas` 를 `dry_run` 을 끄고 돌리면 세 지문이 나온다.
   ★ 〈2026-08-23 정정〉 그 워크플로는 이제 **올리기도 한다** — D-84 가 재결정되어
   쓰기가 되살아났다. 앞 판 이 줄은 「재기만 한다」로 남아 있었다.

12-2. **판 지문이 화면에 실제로 보이는지 눈으로 본다** 〈2026-08-23 · M-27〉 —
   속성에만 있으면 **사진에 안 찍힌다.** 그러면 사장님이 화면을 보내셔도
   「이것이 그 판인가」를 가릴 수가 없다. 화면 맨 아래 「판 xxxxxxxx」 를 확인하고,
   **답변에 같은 값을 적는다.**

12-4. **막는 장치가 「돌기는 하는가」를 먼저 묻는다** 〈2026-08-23 · M-28〉 —
   「이 검사가 뚫려 있나」(M-12)보다 앞선 질문이다. 사람이 손으로 쳐야 도는
   검사는 **안 친 날이 대부분**이고, 그날은 아무것도 안 막는다. 새 검사를
   만들면 **어디서 자동으로 도는지**를 같은 커밋에 적는다.

12-3. **「배포했다」는 `Verify deployed` 가 초록일 때만 하는 말이다**
   〈2026-08-23〉 — 워크플로를 **건 것**과 **닿은 것**은 다르다. 줄을 서면
   앞의 것이 끝나야 시작하므로, 걸자마자 「반영했습니다」라고 말하면
   사장님은 그 사이에 옛 화면을 보신다. 실제로 오늘 그렇게 세 번 어긋났다.

12. **사용자가 「또 그러네」라고 하면 내 진단부터 의심한다** — 같은 증상이
   세 번 나오면 고친 것이 원인이 아니었다는 뜻이다. 고칠 자리를 또 찾지 말고
   **가르는 사실**을 먼저 잡는다: 무엇은 되고 무엇은 안 되는가. M-24 는
   「목록은 되는데 올리기만 안 된다」 한 줄에서 자리가 좁혀졌다.

**그럼에도 문제가 생기면**: 이 파일(MEMORY.md)에서 먼저 찾는다.
같은 종류를 이미 겪었을 가능성이 높고, 해법과 막는 장치가 위에 적혀 있다.

---

## M-14. 방금 넣은 사람에게 「안 넣었다」고 말했다

**언제** 2026-08-21 · **어디** `report-api.cjs` `scanSources` · `files.html` 스캔 칸

### 무슨 일이 있었나

사용자가 「자료 붙이기」에서 **파일업로드**로 파일 셋을 넣고 「자료 스캔」을 눌렀다.
빨간 칸에 이렇게 떴다:

> 읽을 자료가 없습니다 — 자료를 먼저 넣어 주십시오.

**방금 넣은 사람에게 안 넣었다고 말한 것이다.** 사용자는 기능이 고장 난 줄 알고
신고했다.

### 왜 그랬나 — **고장이 아니었다**

「파일업로드」는 **1회성**이다. 서버가 올리는 그 자리에서 읽고 **파일을 버린다**
(`oneshot.dispose()` · 응답의 `removed`). 그래서 나중에 `POST /scan` 이 읽을
원본이 **우리에게 없다.** 설계대로 돈 것이다.

문제는 **화면이 그 사실을 한 번도 말하지 않은 것**이었다.

1. 스캔 칸은 갈래와 **무관하게 늘 같은 얼굴**로 떠 있었다 — 1회성으로만 넣은
   상태에서도 「자료 스캔」 단추가 멀쩡히 열려 있었다.
2. 눌렀을 때 서버는 **1회성 장부를 보지 않고** 「자료가 없다」로 답했다.
   장부에는 **무엇을 언제 올렸는지 그대로 남아 있었는데도** 그랬다.
3. 화면은 그 답을 **빨간 오류 상자**로 그렸다. 읽을 것이 없는 것은 오류가 아니다.

### 무엇이 진짜 잘못이었나

**「없다」와 「여기서 다시 읽을 수 없다」는 다른 말이다.**

- 앞엣것은 **넣으라고** 한다 → 사용자는 이미 넣었으므로 고장으로 읽는다
- 뒤엣것은 **왜 못 읽는지와 다음 수**를 준다 → 사용자가 판단할 수 있다

가진 정보로 뒤엣것을 말할 수 있었는데 앞엣것을 말했다. **정보가 없어서가 아니라
보지 않아서** 생긴 일이다.

### 고친 것

**서버** — 「자료가 없다」고 말하기 전에 **1회성 장부를 본다.**

```
여기서 다시 읽을 자료가 없습니다 — 「파일업로드」로 넣은 N건은 올리는 그 자리에서
이미 읽었고, 보관하지 않으므로 원본이 남아 있지 않습니다.
다시 읽어야 하면 「폴더를 연결해서」로 넣으십시오.
```

`oneshotOnly` · `oneshotCount` 를 함께 준다 — 화면이 짐작하지 않는다.

**화면** — 셋

1. **미리 말한다.** 1회성으로만 넣은 상태면 스캔 칸이 누르기 **전에**
   「여기 오지 않습니다」와 다음 수를 적는다.
2. **아직 안 올린 파일**을 손에 든 채면 그것부터 말한다 —
   「고른 파일 N개가 아직 안 올라갔습니다」.
3. 읽을 것이 없는 것을 **빨간 상자로 그리지 않는다.**

### 막는 장치

- `scan.test.js` — 1회성으로 올린 뒤 스캔했을 때 **「자료를 먼저 넣어」가
  안 나오는지**, 무슨 일이 있었는지와 다음 수를 말하는지. 되돌려서 신고와
  **똑같은 문장이 나오는 것**까지 확인했다.
- `files-tab.test.js` — **신고된 흐름을 브라우저로 그대로 재현한다.**
  고르기 → 올리기 → 스캔. 빨간 칸이 아닌지, 탓하지 않는지를 잰다.

### 남길 교훈

★ **가진 정보를 안 보고 「없다」로 답하지 않는다.** 장부·기록·상태를 먼저 본다.
  「없다」는 사용자에게 **자기 탓**으로 들린다.

★ **설계상 그런 것**과 **고장**을 화면이 같은 색으로 칠하면, 사용자는 둘을
  구분할 방법이 없다. 색이 곧 말이다.

★ 이 사고는 M-08(「부르지 않는 테스트」)의 사촌이다. 검사는 전부 초록이었다 —
  응답의 각 칸은 맞았고, **사용자가 걷는 순서로는 아무도 안 걸어 봤다.**

---

## M-15. ssh 는 보이는데 scp 는 「없다」고 한다

**언제** 2026-08-21 · **어디** `.github/workflows/deploy-nas.yml` `Upload`

### 무슨 일이 있었나

자동 배포를 처음 켜고 실제로 올리는데 마지막 한 걸음에서 죽었다.

```
scp: dest open "/volume1/web/report-flow.html": No such file or directory
```

경로가 틀린 줄 알고 확인했다. **있었다.** 권한도 활짝 열려 있었다.

```
ssh ... ls -ld /volume1/web   →   drwxrwxrwx+ 1 root root ... /volume1/web
```

**둘 다 참이었다.**

### 왜 그랬나

OpenSSH 9 부터 `scp` 는 속으로 **SFTP** 를 쓴다. 그리고 Synology 에는
「SFTP 사용자를 홈 폴더 안에만 가둔다」는 설정이 있다. 그 상태면 **SFTP 쪽
시야에서는 `/volume1/web` 이 존재하지 않는다.**

- `ssh` 는 평범한 셸 → 보인다
- `scp` 는 SFTP → 안 보인다

같은 계정, 같은 연결, **다른 창구**다.

### 무엇이 진짜 비쌌나

**오류 문구가 원인을 잘못 가리켰다.** `No such file or directory` 는
「경로가 틀렸다」로 읽힌다. 그래서 **멀쩡한 경로를 몇 번이고 다시 확인**하게
된다. 실제로 그렇게 한 바퀴 돌았다 — 확인할 때마다 「있다」고 나오는데
올리면 「없다」고 하니 어디를 봐야 할지 알 수가 없다.

★ 「권한 문제인가」로도 안 간다. `Permission denied` 가 아니기 때문이다.
  **두 가지 흔한 원인이 둘 다 아닌 것처럼 보인다.**

### 고친 것

`scp -O` — 옛 방식(셸 경유)으로 보낸다. `ssh` 와 **같은 눈**이 된다.
NAS 설정은 건드리지 않는다.

```
scp -O -i ~/.ssh/id_deploy "$f" "$NAS_USER@$NAS_HOST:$DEST/$BASE"
```

### ★★ 함께 드러난 것 — **재는 눈과 하는 눈이 달랐다**

이 일이 나기 직전에 「올리기 전에 목적지를 본다」는 단계를 넣었다. 그런데
그 단계는 **`ssh` 로** 잰다. `-O` 가 없으면:

- 확인 단계 → 「있다」 → **통과**
- 올리는 단계 → 「없다」 → **죽는다**

**확인이 확인 노릇을 못 한다.** 재는 쪽과 하는 쪽이 같은 창구를 써야
그 확인에 뜻이 있다. 검사가 `scp -O` 를 강제한다.

### 막는 장치

`im-agent/test/workflow-default.test.js`

- ~~`Upload` 의 모든 `scp` 에 `-O` 가 있는가~~ — **잴 대상이 없어졌다** 〈2026-08-22 ·
  D-84 로 쓰기를 지웠다〉. 함정 자체는 남아 있으므로 **다시 쓰기를 넣는 날 되살린다.**
  `deploy/nas.sh` 쪽은 여전히 이 함정에 걸릴 수 있다
- `Check destination` 이 **지문 대조**보다 앞인가 · NAS 에 아무것도 안 만드는가
- 막혔을 때 다음에 무엇을 볼지 말하는가

### 다음에 같은 냄새가 나면

**「도구 A 는 되는데 도구 B 는 안 된다」면 두 도구가 같은 길을 쓰는지부터 본다.**
`ssh`/`scp` · `curl`/브라우저 · 셸/GUI 는 이름이 비슷해도 속이 다를 수 있다.
그때 오류 문구는 **자기가 본 것**만 말하므로, 그 말을 그대로 믿으면 엉뚱한
곳을 파게 된다.

---

## M-16. 받은 것과 읽은 것을 같은 말로 했다

**언제** 2026-08-22 · **어디** `im-agent/ui/platform/files.html` `uploadCard()`

### 무슨 일이 있었나

사용자 신고는 이랬다 — 「파일업로드하면 오류가 발생 깨짐」.

재현했다. **오류 상자가 안 떴다.** 예외도 없었다(`window.onerror` 도 조용).
화면은 초록 칸에 이렇게만 띄웠다:

```
올렸습니다                    100%
2개를 올렸습니다
```

그런데 **둘 다 값이 하나도 안 나왔다.** 그다음 칸(② 자료 스캔)이 텅 비었고,
사용자는 그것을 **고장으로** 읽었다. 고장이 아니었다. 「못 읽었다」였다.

### 왜 그랬나

서버는 세 가지를 **따로** 준다.

| 필드 | 뜻 |
|---|---|
| `rejected` | 아예 안 받았다 (빈 파일·용량 초과) |
| `read.unsupported` | **받았는데 못 읽었다** (엑셀·한글 문서·손상된 ZIP) |
| `read.facts` | 읽어서 값이 됐다 |

화면은 `rejected` 만 그리고 **가운데를 통째로 빼먹고 있었다.** 그리고
`UP.summary()` 는 `accepted` 를 세므로 「2개를 올렸습니다」가 **참**이었다 —
받기는 받았으니까. 참인 말 하나로 거짓인 인상을 만든 셈이다.

**1회성(파일업로드)은 여기서 한 겹 더 나쁘다.** 서버는 읽어 보고 **원본을
바로 지운다.** 못 읽었다고 말하지 않으면 사용자는 자료가 사라진 것조차 모른다.

### 곁가지로 하나 더 나왔다

고친 것을 미리보기에 넣으려고 그 자리를 그려 봤더니 **진행 자취가 검은
덩어리**로 나왔다. 아티팩트가 `<style>` 만 걷어 가고 `<link href="tokens.css">`
는 버려서 색이 한 줄도 안 실려 있었다. SVG 의 `fill` 은 **표현 속성**이라
`var(--lime-soft)` 가 안 풀리면 초깃값인 **검정**으로 칠한다.

실측: 토큰이 있으면 `rgb(238,247,216)`, 없으면 검정.
**색이 빠지는 것은 밋밋해지는 문제가 아니라 그림이 뒤집히는 문제였다.**

### 막는 장치

`im-agent/test/upload-unread.test.js` — 문자열 검사가 아니라 **실제로 올려 본다.**

- 「파일업로드」를 누르고 · 읽히는 파일과 안 읽히는 파일을 **섞어** 넣고 · 올린다
- 화면이 「읽지 못했습니다」와 **파일 이름**을 말하는가
- 1회성이면 「원본이 남아 있지 않다」까지 말하는가
- ① · ② 번호가 실제로 그려지는가

`im-agent/test/files-tab.test.js` — 아티팩트에 `--lime-soft` 가 실리는가.
가짜 서버도 이제 **못 읽는 형식을 만든다** — 미리보기가 좋은 소식만 보여 주면
나쁜 소식을 그리는 코드는 한 번도 안 돌아 본다.

### 다음에 같은 냄새가 나면

**「성공했다」는 말이 무엇을 세어서 나온 숫자인지 본다.** 받은 개수와 쓸 수 있게
된 개수는 다르다. 둘이 다를 수 있는 자리에서 하나만 보여 주면, 화면은 거짓말을
하지 않고도 사용자를 잘못 이끈다. 그리고 **오류 상자가 없다고 정상인 것이
아니다** — 이번 신고에는 빨간 것이 하나도 없었다.

---

## M-17. 「모른다」가 「바뀌었다」로 둔갑했다

**언제** 2026-08-22 · **어디** `im-agent/core/linked.js` `verify()`

### 무슨 일이 있었나

연결 자료가 「그때 그대로인가」를 묻는 길을 **처음으로 끝까지 돌려 봤다**
(`npm run im:try-linked`). 아무것도 안 바꾼 파일이 이렇게 나왔다.

```json
"changed": [{ "name": "사업개요_v2.txt", "was": "r2", "now": "null" }]
```

화면은 「원본이 바뀌었습니다 · 다시 읽어야 합니다」라고 말했을 것이다.
**바뀐 것이 없었다.**

### 왜 그랬나

두 조각이 각각은 옳았다.

- `core/linked-fetch.js` `headLinked` — 임시 내려받기 주소로 HEAD 를 칠 뿐이라
  **판(rev)을 알 방법이 없다.** 그래서 일부러 `rev: null` 을 돌려준다.
  주석에도 적혀 있다: **「모르면 지어내지 않는다」**
- `core/linked.js` `verify` — `String(head.rev) !== String(item.rev)` 로 비교한다

`String(null)` 은 `'null'` 이다. **「모른다」가 문자열이 되는 순간 하나의 값이 되고,
그 값은 `'r2'` 와 다르다.** 모른다는 뜻이 비교 한 줄에서 사라졌다.

### 시험들이 왜 못 잡았나 — **재는 눈과 하는 눈이 달랐다** (M-15 와 같은 냄새)

`linked.test.js` 의 verify 시험 넷은 **전부 `rev` 를 넣어 주는 가짜 함수**를 쓴다.

```js
await linked.verify(p, async () => ({ ok: true, rev: REF.rev }));       // 그대로
await linked.verify(p, async () => ({ ok: true, rev: 'ffffffffffff' })); // 바뀜
```

그런데 **실제로 실려 나가는 구현만 rev 를 안 준다.** 시험이 한 번도 안 만든
조합이 곧 운영에서 늘 나오는 조합이었다.

### 곁가지 — 고치고 나니 화면이 거짓말할 뻔했다

`unread`(확인 못 함)로 보냈더니, 화면이 그것을
**「아직 한 번도 읽지 않아 견줄 것이 없습니다」**로 그렸다. 방금 읽은 자료다.
M-14(「방금 넣은 사람에게 안 넣었다고 말했다」)와 같은 종류다 —
**한 통에 두 가지 뜻이 들어가면, 그 통의 옛 이름이 새 뜻을 덮어쓴다.**
서버가 사유를 주면 화면이 그것을 그대로 쓰게 고쳤다.

### 막는 장치

`im-agent/test/linked.test.js`

- `rev: null` · `undefined` · `''` 셋 다 **「바뀌었다」로 안 가는가**
- 「확인 못 함」에 **왜 못 했는지 사유가 붙는가**
- 확인 못 한 것을 **통과(`ok:true`)로 내지 않는가**
- 화면이 서버가 준 사유를 쓰는가 (안 쓰면 옛 문장이 새 뜻을 덮는다)

그리고 `npm run im:try-linked` — 가짜 함수를 끼우지 않고 **실려 나가는 구현으로**
연결→스캔→확인을 끝까지 밟는다. 이 도구가 없었으면 못 잡았다.

### 다음에 같은 냄새가 나면

**「모른다」를 값으로 만들지 않는다.** `null`·`''`·`0`·`-1` 은 비교에 들어가는
순간 **그냥 하나의 값**이 되고, 그때 「모른다」는 조용히 사라진다. 비교하기 전에
**모르는지부터 묻는다.**

그리고 **가짜를 끼워 넣은 시험은 실려 나가는 구현이 무엇을 주는지 모른다.**
주입한 값이 실제 구현의 반환값과 같은지, 한 번은 실물로 확인한다.

---

## M-18. 실패 하나가 화면을 통째로 갈아치웠다

**언제** 2026-08-22 · **어디** `im-agent/ui/platform/files.html` `doUpload().onFail`

### 무슨 일이 있었나

신고는 「파일업로드하면 **화면이 사라지는** 오류」였다. 화면을 셋 받았고,
**순서가 답이었다.**

```
① 목록이 멀쩡히 뜬다        ← 로그인은 되어 있다
② 8.7MB 를 올리다 멎는다     ← 자취가 6.1초 동안 평평 (145번 쟀다)
③ 「로그인이 필요합니다」     ← 화면 전체가 이 카드 하나로 바뀐다
```

### 왜 그랬나 — 둘이 겹쳤다

**하나.** `onFail` 이 401·403 이면 `state.gate` 를 세웠다. 그런데 `paint()` 는
게이트가 서면 **거기서 `return` 한다.** 고른 파일 목록도, 진행 그래프도,
멈춘 자취도 **전부 사라지고** 카드 하나만 남는다. 사용자가 본 그대로다.

**둘.** 그 말이 **틀렸다.** ①에서 목록이 떴다는 것은 로그인이 되어 있었다는
뜻이다. 큰 본문이 앞단에서 잘리면 서버가 세션을 못 읽고 **401 을 돌려준다** —
로그인 문제가 아닌데 화면은 로그인을 의심하게 만들었다.

> **401 은 「누구인지 모르겠다」이지 「로그인하라」가 아니다.**
> 그 둘을 같은 말로 옮기면, 멀쩡한 로그인을 의심하며 엉뚱한 데를 고치러 간다.

### 그리고 멈춘 것을 아무도 안 재고 있었다

`upload-core.js` 머리말에는 **그때도** 이렇게 적혀 있었다.

> ★ **조용히 죽지 않는다.** 끊기면 사유를 남긴다.

맞는 말인데 `onerror` 는 **연결이 죽어야** 온다. 앞단이 본문을 안 받아 주면서
붙들고 있으면 **아무 이벤트도 안 온다** — 진행률만 평평해진다.
**적어 두는 것과 재는 것은 다른 일이다.**

### 덤으로: 넘어가는 것이 말없이 일어났다

실측하면 이랬다.

```
올리기 누름 → 905ms  이관 신호 발사
            → 931ms  「올렸습니다」 카드가 처음 그려짐   ← 26ms 늦다
            성공 카드가 보인 시간 366ms
```

**신호가 카드보다 먼저 나갔다.** 넘어가는 것 자체는 옳다 — 값이 들어갔으니
다음 칸으로 가는 게 맞다. 틀린 것은 **말없이 즉시**라는 점이다.

고친 뒤: 이관 신호 **2465ms** · 카드가 보인 시간 **2358ms**.
그리고 「여기 그대로 있기」로 사람이 멈출 수 있다.

### 막는 장치

- `upload-unread.test.js` — `onFail` 에 `state.gate` 가 없는가 · 사유를 올리기
  칸에 남기는가 · 401 을 그 칸에서 설명하는가 · 번호가 ①②③ 셋인가
- `upload-stall.test.js` — 멎으면 **사유와 크기와 함께** 말하는가 · 한 번만
  말하는가 · 다시 움직이면 거두는가 · `xhr.timeout` 천장이 있는가
- `files-tab.test.js` — 「도는 선」을 **올리기 칸 안에서만** 본다. 앞 판은 문서
  전체를 봐서, 이관 칸이 정상적으로 도는 것을 고장으로 잡았다

### 다음에 같은 냄새가 나면

**한 칸의 실패로 화면 전체를 바꾸지 않는다.** 사용자는 그 순간까지 무엇을 하고
있었고, 그것이 남아 있어야 다시 해 볼 수 있다. 그리고 **오류 코드를 사람 말로
옮길 때, 그 말이 틀릴 수 있는 경우를 먼저 생각한다** — 화면이 원인을 단정하면
사용자는 그 단정을 따라간다.

---

## M-19. 서버는 숫자로 세고, 화면은 목록으로 세었다

**언제** 2026-08-22 · **어디** `ui/report-api.cjs` `saveFacts` ↔ `files.html` `importFromApp`

### 무슨 일이 있었나

「자료 업로드」 세 갈래를 끝에서 끝까지 실제로 올려 보다가 나왔다.
「＋ 앱 프로젝트에서 가져오기」로 값을 넣으면 화면이 이렇게 말했다:

> 값 undefined개 · 첨부 2개 · 이미지 1개를 가져왔습니다.

**값은 실제로 들어갔다.** dataset 에 남아 있었고 출처도 붙어 있었다.
화면만 못 셌다.

서버는 저장 건수를 **숫자**로 준다.

```js
return ok({ saved: clean.length - failed.length, ... });   //  saved: 1
```

화면은 그것을 **목록**으로 알고 세고 있었다.

```js
rep.facts = (r.body.saved || []).length;   //  (1).length === undefined
```

### 왜 아무 검사도 못 잡았나

**양쪽 검사가 다 초록이었다.** 서버 검사는 `saved` 가 맞는 숫자인지 봤고,
화면 검사는 브라우저에서 가짜 응답으로 그렸다 — 그 가짜 응답에는 `saved` 가
**목록으로** 적혀 있었다. 둘 다 자기 쪽에서는 옳았고, **사이에서만** 갈렸다.

`undefined` 는 자바스크립트에서 던지지 않는다. 문자열로 이으면 그대로
「undefined」라고 **화면에 적힌다.** 오류도 안 나고 로그도 안 남는다.

### 무엇이 이걸 막나

- **진짜 핸들러가 낸 응답을 화면의 그 함수에 그대로 먹인다.**
  `test/upload-e2e.test.js` 가 그렇게 한다 — 응답 모양을 검사에 손으로 적어
  두지 않는다. 적어 두면 서버가 바뀐 날 검사만 옛말을 한다 (M-08 과 같은 병).
- 세는 자리를 함수 하나로 모은다 (`savedCount`). 숫자로 오든 목록으로 오든
  센다 — 한쪽만 맞춰 두면 다음에 모양이 바뀌는 날 같은 자리가 다시 빈다.
- **고친 뒤 옛 코드로 되돌려 검사가 실제로 우는지 본다.**
  이번에는 울었다: `화면이 undefined 로 셌다 (서버 응답 saved=1)`.

### 한 줄로

**두 쪽이 각자 맞는 것과, 둘을 이었을 때 맞는 것은 다른 일이다.**
가짜 응답으로 그린 화면은 「내가 상상한 서버」와만 맞춰 본 것이다.

---

## M-20. 초록으로 끝난 배포가 아무것도 올리지 않았다

**언제** 2026-08-22 · **어디** `.github/workflows/deploy-nas.yml` `dry_run` 기본값

### 무슨 일이 있었나

화면을 고치고 배포를 돌렸다. **초록이었다.** 사용자에게 「배포 성공(22회차)」
「배포 23회차 성공」이라고 두 번 보고했다.

사용자는 계속 옛 화면을 보내 왔다. 나는 **브라우저 캐시라고 두 번 말했다.**

캐시가 아니었다. `dry_run` 기본값이 `true` 라, 입력을 안 주고 실행하면
**Upload 단계가 통째로 건너뛰어진다.** 실행은 초록으로 끝난다.

```
Upload            skipped     ← 아무것도 안 올라갔다
Dry run summary   success     ← 회색 알림 한 줄
```

### 왜 안 보였나

- **초록이 「됐다」로 읽힌다.** 건너뛴 단계는 빨갛지 않다.
- 알림이 `::notice::` 였다. **초록 옆의 회색 한 줄은 안 읽힌다.**
- 그리고 나는 **로그 본문을 안 읽었다** — 「배포 전 교차검증 9번」이 정확히
  그것을 하라고 적혀 있는데도 결론(conclusion)만 보고 넘어갔다.
- 마지막으로 **내가 원인을 먼저 단정했다.** 「캐시입니다」라고 두 번 말하는
  동안 사용자는 그 말을 믿고 캐시를 지웠다. **틀린 단정은 상대의 시간까지
  가져간다** (M-18 과 같은 병 — 화면이 원인을 단정하면 사용자가 따라간다).

### 무엇이 이걸 막나

- Dry run 요약을 **노란 경고**로 바꿨다: 「아무것도 올리지 않았다. NAS 는
  그대로다 — 이 실행이 초록이어도 배포된 것이 아니다」
- 배포를 돌릴 때 **그 단계의 결론을 본다.** `success` 여야 한 것이고 `skipped` 면
  안 한 것이다. 실행 전체의 초록은 그것을 뜻하지 않는다.
  ★ 〈2026-08-22 · D-84〉 이 저장소의 `Upload` 단계는 **없어졌다.** 지금 볼 것은
    `deploy-nas` 의 **`Check served build`**(지문 대조)이고, 실제로 올리는 것은
    본체 게이트(`deploy/nas.sh`)다.
- 「반영이 안 된다」는 말을 들으면 **캐시를 먼저 의심하지 않는다.** 먼저
  **올라갔는지**를 확인한다. 순서가 반대면 없는 캐시를 지우게 만든다.

### 한 줄로

**건너뛴 단계는 초록으로 보인다.** 배포가 됐는지는 실행의 색이 아니라
`Upload` 단계로 확인한다.

---

## M-21. 진단 도구가 진단을 못 하게 만들었다

**언제** 2026-08-22 · **어디** `tools/smoke-public-data.js` §6 건축인허가

### 무슨 일이 있었나

사용자가 실제 키를 넣고 `npm run im:smoke` 를 돌렸다. 앞쪽은 줄줄이 통과했다 —
지오코딩·연속지적도·토지특성·공시지가·토지이용계획·건축물대장까지.

그리고 여기서 멈췄다.

```
× 진단 중단 : molit.buildingPermit is not a function
```

**함수 이름이 틀렸다.** 진짜 이름은 `buildingPermits`(복수)다. 그런데 이 한 줄
때문에 **그 뒤 절이 전부 안 돌았다** — 실거래가·조달청·공장등록·수출입 단가·
환경 인허가·KOSIS 표목록·건축인허가·주택인허가.

### 왜 아무도 몰랐나 — 이쪽이 핵심이다

**이 도구는 키가 있어야 그 줄에 닿는다.** 키가 없는 환경(CI·개발 저장소)에서는
앞에서 전부 건너뛰므로 죽은 줄을 **지나가지도 않는다.** 그래서 「도구가 있다」는
사실이 「도구가 끝까지 돈다」로 읽혔다 — M-08 과 같은 병이다.

그리고 화면에는 **「진단 중단」 한 줄뿐**이었다. 무엇이 안 돌았는지조차
말해 주지 않았다. **진단 도구가 진단을 못 하게 만드는 자리다.**

### 무엇이 이걸 막나

- **이름만 고치지 않고 그 절을 지웠다.** 아래 §11 이 같은 일을 제대로 하고
  있었다 — 이력 전체와 단계 분포까지. 둘 다 두면 같은 API 를 두 번 부른다(§4.5).
  「고장 난 것을 고친다」보다 **「왜 둘이 있나」를 먼저 묻는다.**
- **부르지 않고도 잡을 수 있는 것을 잡는다** (`test/smoke-tool.test.js`).
  도구가 부르는 `모듈.함수` 이름이 실제로 있는지 정적으로 대조한다 — 네트워크도
  키도 필요 없다. 옛 코드로 되돌려 실제로 우는지도 확인했다.
- **검사가 헛돌지 않는지도 함께 잰다.** 「몇 곳을 봤나」를 단언한다 — 경로가
  한 단 어긋나 require 가 전부 실패하면 검사는 **아무것도 안 세고 통과한다.**
  실제로 처음 판이 그랬다(0곳 검사, 통과).
- 터졌을 때 **자리와 「이 뒤는 안 돌았다」를 말한다.**

### 한 줄로

**한 절이 터져서 멈춘 도구는, 통과한 줄만 보여 주기 때문에 가장 잘 속인다.**

---

## M-22. 16개 중 1개만 올려서 화면이 통째로 비었다

**언제** 2026-08-22 · **어디** `.github/workflows/deploy-nas.yml` `files` 기본값

### 무슨 일이 있었나

사용자가 자료를 올리고 나면 「새 보고서 생성」으로 넘어가는데, 그 화면이
**통째로 하얗게** 떴다. 오류 메시지도 없고 배포는 **초록**이었다.

원인은 화면 코드가 아니라 **배포 목록**이었다. `files` 기본값이
`report-flow.html` **한 개**였다. 그래서 손대지 않고 [Run workflow] 를 누르면
그 파일만 새것이 되고 **옆의 15개는 옛것으로 남는다.**

`report-flow.html` 은 혼자 서지 못한다 — `tokens.css` 와 코어 7개
(`embed-bridge` `gate-core` `flow-core` `fields-core` `live-core` `catalog`
`inapp`)를 부른다. 새 화면이 **옛 코어에 없는 것**을 부르면 거기서 죽고,
죽은 자리는 **빈 `<body>`** 로만 보인다. 콘솔을 열지 않으면 알 길이 없다.

실측: 배포 30번·31번 로그의 `FILES:` 가 둘 다
`im-agent/ui/platform/report-flow.html` 하나였다.

### 왜 안 잡혔나

- **내 쪽에서는 재현되지 않는다.** 저장소 안에서는 16개가 항상 짝이 맞다.
  헤드리스로 `report-flow.html` 을 띄우면 51KB DOM 이 멀쩡히 나온다.
  **짝이 깨지는 것은 NAS 위에서뿐이다.**
- **배포는 초록이다.** `Upload` 단계도 success 다 — 시킨 대로 한 개를
  올렸으니 맞는 말이다. 「올렸다」와 「반영이 안 된다」가 둘 다 참이라 겉돈다.
  M-20 과 같은 결의 사고다 — 그때는 **아무것도** 안 올렸고, 이번엔 **하나만**
  올렸다.
- 이 종류를 만나면 **화면을 먼저 의심하게 된다.** 실제로 캐시·dry run 을
  차례로 잘못 짚었다. 배포 로그의 `FILES:` 를 보는 것이 가장 빨랐다.

### 막는 장치

- 기본값을 `build-embed.js` 의 `required()` 16개로 바꿨다.
- **목록을 손으로 적지 않는다.** `required()` 가 단일 출처다.
- `workflow-default.test.js` 가 기본값과 `required()` 가 같은지 지킨다 —
  빠진 것도, 남는 것도 없어야 통과한다.
- 같이 고친 것: `defaultOf()` 가 입력 이름 뒤 **1200자 고정 창**만 훑고 있었다.
  주석이 길어지자 `default:` 가 창 밖으로 밀려나 「기본값을 못 찾았다」가 됐다 —
  **기본값은 멀쩡한데 검사만 눈이 먼** 상태다. 이제 다음 입력 키까지 읽는다.

### 한 줄로

**서로를 부르는 파일들은 낱개로 올리면 안 된다. 짝이 깨진 화면은 오류를 내지
않고 그냥 빈 칸이 된다.**

---

## M-23. 덜 읽힌 파일이 조용히 빠진 채로 올라갔다

**언제** 2026-08-22 · **어디** `ui/platform/files.html` 올리기 단추 잠금

### 무슨 일이 있었나

파일을 고르면 **줄은 바로** 생기지만 내용은 `FileReader` 가 **나중에** 채운다.
앞 판은 「내용이 찬 것」(`ready`)이 **하나라도** 있으면 올리기 단추를 열었다.
그래서 둘을 골라 곧바로 누르면 **덜 읽힌 하나가 빠진 채로** 올라갔다.

화면은 「**1개를 올렸습니다**」라고 **맞는 말**을 한다. 사용자는 2개를 골랐다.
어긋난 것을 화면 어디서도 말하지 않는다 — 이게 가장 나쁜 종류다.

### 어떻게 잡았나 — 사람 손으로는 못 잡는다

읽기는 보통 눈 깜짝할 새라 **기계가 바쁠 때만** 재현된다. 눌러 보는 시험도
헤드리스에서는 읽기가 너무 빨리 끝나 늘 초록이었다.

실제 단서는 **미리보기 빌드의 간헐 실패**였다 — 45번에 한 번쯤 「이관 칸이
없다」로 멎었다. 그 실패는 세 번 자리를 옮겨 다녔고(`drop` → `done` → 값 0),
**발자국을 DOM 속성에 찍고 나서야** 원인이 드러났다:

```
손이 선 자리: done · 이관칸 본 적=0 · 올리기 머리말=올렸습니다
줄 1개 · 올리기 말=[1개를 올렸습니다 // 이 중 1건은 ... 읽지 못했습니다]
```

**둘을 골랐는데 하나만 올라갔다.** 그래서 값이 0이 되고 이관 칸이 안 떴다.

★ `--dump-dom` 은 DOM 만 떠 간다. `window.__lpDrove` 같은 전역은 **받는 쪽에서
볼 수가 없다.** 발자국은 반드시 **속성으로** 남긴다 (M-21 과 같은 교훈).

### 막는 장치

- `reading`(아직 안 읽힌 것)이 있으면 **단추를 잠근다.** 몇 개를 기다리는지
  화면에 적는다 — 잠긴 단추만 보면 고장으로 읽는다.
- 못 읽은 것·너무 큰 것은 **기다리지 않는다.** 그것까지 기다리면 영영 안 열린다.
- `upload-limits.test.js` 가 화면 소스에서 **판단하는 식을 그대로 떼어 와**
  값을 넣어 본다. 옛 판으로 되돌리면 실제로 빨개지는 것을 확인했다 (M-12).

### 같이 고친 흔들림 — 크기를 한 방향으로 줄 세운다

같은 사냥에서 「가끔 빨간」 자리를 셋 더 고쳤다. 전부 **한 가지 모양**이다.

| 어디 | 기다리는 쪽 | 그리기 예산 | 결과 |
|---|--:|--:|---|
| `build-files.js` 손 | 25초 | 60초 | 파일 읽기를 기다리다 되물음을 다 씀 |
| `files-tab.test.js` 탐침 | 30초 | 30초 | 크로미움이 먼저 DOM 을 뱉음 |
| `upload-unread.test.js` 탐침 | 25초 | 60초 | 손(180초)보다 먼저 끝남 |

**규칙: 손 ≤ 탐침 < 예산.** 하나만 고치면 다음에 또 같은 자리에서 흔들린다.
가상 시계는 공짜이므로 아끼지 않는다.

### 한 줄로

**「올렸습니다 1개」가 맞는 말이어도, 사용자가 2개를 골랐으면 그 화면은 거짓말이다.**


---

## M-24. `fetch` 만 덮어서 올리기만 401 이 됐다

**등급: 심각 (세 번 반복 · 증상이 원인을 가린다)**

**언제** 2026-08-22 · **어디** `ui/platform/embed-bridge.js` 의 `installAuth()`

### 경위

사용자가 자료 3.8MB 를 올리자 화면에 **「로그인이 필요합니다」**가 떴다.
그런데 **로그인은 되어 있었고 프로젝트 목록도 같은 화면에 떠 있었다.**

같은 신고가 **세 번** 왔다. 그 사이 나는 이렇게 짚었고 **전부 틀렸다**:

| 회차 | 내가 짚은 것 | 실제 |
|---|---|---|
| ① | 브라우저 캐시 | 아니다 |
| ② | 배포가 안 됐다(dry run) | 그건 M-20 의 다른 사고였다 |
| ③ | 본문이 커서 앞단에서 잘렸다 | **아니다 — 3.8MB 다** |

### 진짜 원인

**보내는 길이 둘인데 인증은 한 길에만 붙어 있었다.**

```
목록 읽기    fetch            → 브리지가 덮음 → Authorization 붙음 → 200
자료 올리기  XMLHttpRequest   → **안 덮음**   → 헤더 없음         → 401
```

`embed-bridge.js` 의 `installAuth()` 는 `window.fetch` 만 감쌌다.
`upload-core.js` 는 **진행률을 재려고** XHR 을 쓴다 — `fetch` 에는 업로드 진행
이벤트가 없어서 그렇게 만든 것이다. 화면에서 XHR 을 쓰는 곳은 **올리기 하나뿐**
이라, 딱 그 하나만 인증에서 빠졌다.

### 왜 세 번이나 헤맸나 — 이게 이 사고의 진짜 값이다

**증상이 원인을 가린다.** 서버는 사람을 못 알아봤으니 `401 로그인이 필요합니다`
라고 답하는 것이 맞다. 그 말은 **정확하지만 엉뚱한 곳을 가리킨다.**

- 사용자는 로그인을 다시 한다 → 안 된다
- 나는 세션을 의심한다 → 세션은 멀쩡하다
- 나는 본문 한도를 뒤진다 → 한도도 멀쩡하다

그리고 **화면 안내문까지 「크기 문제일 수 있다」고 틀린 방향을 적어 두었다.**
화면이 짐작을 적으면 사람은 그 짐작부터 판다 — **틀린 짐작을 적느니 아무 짐작도
안 적는 편이 낫다.**

### 자리를 좁힌 한 줄

> **목록은 되는데 올리기만 안 된다.**

이 한 줄이 「로그인 문제」를 지운다. 로그인이 문제면 목록도 안 떠야 한다.
남는 것은 **이 요청 하나만 다른 무엇** — 곧 다른 길로 나간다는 뜻이다.
같은 증상이 세 번 나오면 고칠 자리를 또 찾지 말고 **가르는 사실**을 먼저 잡는다.

### 막는 장치

- `installAuth()` 가 **`fetch` 와 XHR 을 함께** 덮는다. 둘을 한 함수에서 부른다 —
  갈라 두면 다음에 또 한쪽만 고친다.
- **같은 출처에만 붙인다.** XHR 은 프로토타입을 덮으므로 화면이 부르는 모든
  요청을 지나간다. 아무 데나 붙이면 **열쇠를 남의 서버에 넘기는 것**이다.
- `embed.test.js` 가 **실제 크로미움에서 진짜 XHR 을 보내 헤더를 받아 본다.**
  소스만 훑으면 「XMLHttpRequest 라는 글자가 있다」까지밖에 못 재고, 그건 붙었다는
  뜻이 아니다 (M-08). 되돌려서 실제로 빨개지는 것을 확인했다 (M-12).
- 화면 안내문에서 **크기를 원인으로 짚는 짐작을 뺐다.** 대신 「목록은 통과했고
  이 요청만 거절당했다」는 **가른 사실**을 적는다. `upload-limits.test.js` 가 지킨다.

### 한 줄로

**모든 요청에 붙어야 하는 것(인증·헤더·재시도·로깅)은, 요청이 나가는 길의
개수만큼 붙는지 세어 본다. 한 길만 덮으면 나머지 길은 조용히 맨몸으로 나간다.**


---

## M-25. 디스크와 HTTP 가 달랐고, 나는 원인을 하나로 단정했다

**등급: 심각 (같은 종류의 잘못을 M-24 에서 적어 놓고 여기서 또 했다)**

**언제** 2026-08-22 · **어디** NAS `/volume1/web/im-flow` · 그리고 **내 탐침 자체**

> ### ⚠️ 2026-08-22 정정 — 앞 판은 「웹서버 캐시」라고 적었다. 그것이 틀렸다.
>
> 앞 판 이 항목은 원인을 **캐시 하나로** 단정하고, 사장님께 **DSM → Web Station
> → HTTP 캐시를 끄라**고 시켰다. 본체(엔진 세션)가 앱이 실제로 쓰는 길
> (**Funnel 443 → 80**)로 다시 재니 **저장소 = 디스크 = HTTP 가 4/4 로 맞았다.**
> 캐시가 아니었다.
>
> **★ 사장님은 Web Station 설정을 바꾸실 필요가 없다.** 그 안내는 취소한다.

### 경위

같은 신고가 네 번 왔다. 「고쳤습니다 → 새로고침해 보십시오 → 여전히 그럽니다」.
코드를 고친 것은 실제로 맞았는데 **사용자 화면에는 안 닿았다.** 러너로 재 보니
갈렸다:

```
files.html        저장소 a886a231fc60 · 디스크 a886a231fc60 · HTTP 28ef65362223
report-flow.html  저장소 c915d7614402 · 디스크 c915d7614402 · HTTP 726693d2a933
```

**여기까지는 사실이다.** 그다음이 틀렸다 — 나는 이 표를 보고 **「웹서버가
캐시한다」** 라고 적었다.

### 무엇이 틀렸나 — **잰 길이 앱이 쓰는 길과 달랐다**

내 탐침(`deploy/verify-served.sh`)은 **tailnet 호스트의 포트 80** 으로 직접 받는다.
그런데 앱은 **Funnel 443 → 80** 으로 들어온다. 둘은 `Host:` 머리말이 다르고,
DSM 웹 서비스는 **Host 로 가상 호스트를 고른다** — 즉 **문서 루트가 다를 수 있다.**

본체가 앱과 같은 길로 잰 값:

```
flow-core.js 8038a16e · embed-bridge.js adfaadf5 · files.html c18323f4 · report-flow.html e064c548
→ 저장소 = 디스크 = HTTP · 4/4 일치
```

**「디스크 ≠ HTTP」는 캐시가 아니라 「다른 파일을 봤다」였을 가능성이 크다.**

### 그래도 남는 것 — 원인은 **셋**이고 탐침은 못 가른다

| 후보 | 무엇 | 어떻게 지우나 |
|---|---|---|
| ① **다른 파일을 봤다** | 잰 길과 앱이 쓰는 길의 `Host` 가 다르다 | **먼저 이것부터.** 앱과 같은 길로 다시 잰다 |
| ② **다른 이가 덮었다** | 같은 폴더에 배포 경로가 둘 | 쓰는 이를 하나로 (D-84) |
| ③ 웹서버·앞단 캐시 | 진짜 캐시 | ①②를 지운 **뒤에만** 의심한다 |

**셋은 탐침 화면에서 똑같이 보인다.** 탐침이 말할 수 있는 것은 **「다르다」까지**다.

### 진짜 교훈 — M-24 와 **같은 잘못**이다

M-24 에 이렇게 적어 두었다: 「**가르는 사실을 먼저 잡는다.** 무엇은 되고 무엇은
안 되는가.」 그런데 여기서 나는 **가르지 않은 채 하나를 골랐다.** 그 결과:

- 사장님이 **바꿀 필요 없는 NAS 설정을** 만지실 뻔했다
- 진짜 후보 ①②는 **아무도 안 봤다**
- 그리고 이것이 **네 번째 오진**이었다 — 앞의 셋도 각각 브라우저 캐시 / 배포
  안 됨 / 본문 크기로 단정했고 셋 다 틀렸다

★ **틀린 짐작을 적으면 사람이 그 짐작부터 판다.** 「모른다」보다 비싸다.
  가르지 못하면 **후보를 전부 적고 순서를 준다.**

### 막는 장치

- `deploy/verify-served.sh` 가 **잰 주소를 함께 적는다** (`HTTP 를 받은 길: …`).
  이것이 없어서 「어느 길로 쟀나」를 아무도 묻지 않았다.
- 갈렸을 때 오류 문구가 **후보 셋을 순서대로** 적는다. 하나로 단정하지 않는다.
- `deploy-nas` 의 쓰기(scp)를 **지웠다** 〈2026-08-22 결정 · D-84〉. `im-flow` 에
  쓰는 이는 **본체 게이트 하나**다 — 후보 ②(다른 이가 덮었다)를 구조에서 없앴다.
  ★ 처음에는 잠금(`allow_write` 기본 꺼짐)으로 두었는데 **잠금은 급할 때 풀린다.**
    스위치까지 지워야 되돌아가지 않는다.
  ★ 그러면서 지문 대조가 **유일한 판정**이 됐으므로 `|| true` 를 뗐고, 재는
    목록을 넷에서 배포 목록 전부(16개)로 넓혔다.
- 화면이 **판 지문을 스스로 말한다** (`build-stamp.js` · `data-lp-build`).
  사진 한 장으로 판이 갈린다.
- 표식(`이 글자가 있나`)이 아니라 **지문**을 댄다. 표식으로는 「두 판 전 파일」이
  「새 판」으로 통과한다 — 실제로 그렇게 한 번 속았다.

### 아직 안 끝난 것

- 내 탐침을 **앱과 같은 길(Funnel 443)로** 재게 할 것인가 — 러너에서 그 주소로
  닿는지부터 봐야 한다. 지금은 잰 길을 **적어만** 둔다.
- `?v=지문` 으로 캐시를 지나가려던 시도는 접었다. 검사 54개가 깨졌고,
  **HTML 자체는 그 방법으로 못 지나간다**(캐시된 HTML 이 옛 `?v=` 를 부른다).

### 한 줄로

**「올렸다」와 「닿았다」는 다른 말이다. 그리고 「닿지 않았다」와 「왜 안 닿았나」도
다른 말이다 — 가르지 못했으면 하나를 고르지 않는다.**

---

> **번호 이동 〈2026-08-25〉** — 아래 넷은 처음 `M-26`~`M-29` 로 붙였다가
> **`M-46`~`M-49` 로 옮겼다.** 다른 갈래(Engine·SketchUp)가 같은 날 먼저 그
> 번호를 다른 사고에 쓰고 있었고, 이쪽이 가장 늦게 붙였기 때문이다.
> 겹친 것은 `npm run d:check -- --registry m --all` 이 잡는다.

## M-46. 근거 없는 합격선을 넣어 정상 완료된 Agent 를 막았다

**등급: 심각 (지침 §9 를 정면으로 어겼다 — 추측값을 코드에 넣었다)**

**언제** 2026-08-25 · **어디** `core/taskplan.js` 의 `requiredQualityScore`

**증상** 재무모델 Task 가 계속 실패했다. 로그에는 `● 04_financial — complete` 가
찍히는데 Task 는 `FAILED` 였고, 재작업으로 두 번 더 돌린 뒤 `BLOCKED` 가 됐다.

**원인** 계획을 짜면서 **「재무모델은 90점 이상」이라고 내가 정했다.** 근거가
없었다. 실제 산출물의 신뢰도는 0.76 이었고, 이 저장소는 그것을 **정상**으로
본다 — `registry.js` 의 `04_financial` 은 `approvalRule: 'auto'` 라 임계를
적용하지 않는다. 즉 **합격선이 이미 있는데 내가 두 번째 합격선을 만들었다.**

**왜 못 알아봤나** 로그가 초록이었다. 실패는 Task 쪽에만 찍혔고, 그 문구가
`품질점수 76 < 요구 90` 이라 **그럴듯했다.** 「76점이면 낮은 게 맞지」로 읽힌다.

**고친 것**
- 계획에서 `requiredQualityScore` 를 **뺐다.** 합격선은 `registry.confidenceThreshold`
  하나만 쓴다.
- 임계 미달은 runtime 이 `needs_review` 로 내려보낸다 — 그것은 **실패가 아니라
  사람 검토 대기**다. 오케스트레이터도 그렇게 다룬다.

**막는 장치** `test/orchestrator.test.js` — 계획에 새 합격선이 생기면 정상 완료
Agent 가 막히는 것을 잡는다.

### 한 줄로

**합격선이 이미 있는 곳에 두 번째 합격선을 만들지 않는다. 내가 정한 숫자는
「그럴듯한 실패 사유」가 되어 아무도 의심하지 않는다.**

---

## M-47. 무한회전은 실패보다 나쁘다 — 빨갛지 않기 때문이다

**등급: 심각**

**언제** 2026-08-25 · **어디** `core/orchestrator.js` 의 실행 회전

**증상** 같은 Task 가 **23번** 돌았다. 화면에는 `── wave 6 — 1건 동시 실행: T01`
이 계속 찍혔다. 오류는 하나도 안 났다.

**원인 둘이 겹쳤다**

1. `QUEUED → RUNNING` 은 **허용되지 않는 전이**다(`READY` 를 거쳐야 한다).
   `advance()` 가 제대로 던졌다.
2. 그런데 실행기의 `catch` 가 **상태가 `RUNNING`/`VALIDATING` 일 때만** 실패로
   기록했다. 상태는 여전히 `QUEUED` 였으므로 **오류를 조용히 삼키고** 그대로
   뒀다 → 다음 회전에서 다시 뽑혀 나왔다.

**가장 나쁜 부분** 실패였으면 빨갛게 보였을 것이다. 회전은 **정상처럼 보인다** —
「도는 중」이라고 읽힌다. 회전 상한(`maxWaves`)이 없었으면 영원히 돌았다.

**고친 것**
- `runOne()` 이 `QUEUED` 면 먼저 `READY` 로 옮긴다.
- **한 회전에서 어떤 Task 의 상태도 안 바뀌면 멈추고 이유를 말한다.** 상한에
  기대지 않는다 — 상한은 「언젠가는 끝난다」일 뿐 「제대로 돈다」가 아니다.
- 검증 탈락은 **재시도하지 않는다.** 같은 입력으로 같은 답이 나오고 공공데이터
  호출만 두 배로 쓴다 (§4.5). 실행이 죽은 것(`execution`)과 검증에 걸린 것
  (`validation`)을 나눠 기록한다.

**막는 장치** `test/orchestrator.test.js` — 담당 없는 Task 를 넣고 회전이 3번
안에 멈추는지 본다.

### 한 줄로

**조용히 도는 것이 시끄럽게 죽는 것보다 나쁘다. 진전이 없으면 멈추고 이유를
말한다 — 상한에 걸려 끝나는 것은 끝난 것이 아니다.**

---

## M-48. 갈래 넷이 각자 번호를 붙여 네 벌이 겹쳤다 — 세 번째 재발이다

**등급: 심각 (D-77 이 이미 경고했고, 이번이 세 번째다)**

**언제** 2026-08-25 · **어디** `docs/미결정-사항.md` · 갈래 넷

**증상** 같은 D-번호가 갈래마다 **다른 항목**을 가리켰다.

```
D-85    Engine·SketchUp 「조달청 기초금액」    ↔  Orchestrator 「pipeline 대체」
D-86    Engine·SketchUp 「앱이 읽는 화면 폴더」 ↔  Orchestrator 「담당 없는 검토 넷」
D-104   Engine 「Gemini 열쇠」                ↔  SketchUp 「개념 동 GFA」
D-105   Engine 「관리자 구분」                ↔  SketchUp 「도로필지 동봉」
```

**왜 이것이 비싼가 — 병합해도 조용하다.** 등록부는 사람이 읽는 마크다운이라 같은
번호의 서로 다른 항목이 **나란히 남는다.** git 이 충돌로 안 볼 수도 있다. 그러면
코드 주석이 「D-104 참조」라고 가리키는데 등록부에는 **다른 뜻의 D-104 가 있다.**
반년 뒤 아무도 못 되짚는다.

**세 번째다** — MCP 갈래가 D-85·D-86 을 D-107·D-108 로 옮긴 적이 있고(같은 날),
SketchUp 갈래도 D-101·D-102 충돌로 D-104·D-105 로 옮긴 적이 있다(`def8b20`).
**규칙으로 적어 두는 것으로는 안 막힌다.** 갈래는 서로를 못 보기 때문이다.

**고친 것**
- `tools/registry-check.js` — 원격 갈래 **전부**의 등록부를 읽어 대조한다.
  `npm run d:check -- --all` · `npm run d:next`
- **충돌과 개정을 가른다.** 기준은 「뿌리(main)에 그 번호가 있느냐」다 —
  있으면 두 갈래가 같은 항목을 각자 고친 것(개정), 없으면 각자 새로 붙인 것(충돌).
  제목 비교만으로 가르면 **오탐이 섞이고, 오탐이 섞이면 아무도 안 본다** (첫 판이
  실제로 D-84 를 충돌로 냈다).
- **늦게 붙인 쪽이 양보한다** — 먼저 붙인 번호는 코드·커밋이 이미 가리킨다.
  옮긴 쪽은 옛 번호를 지우지 않고 「D-xx 에서 옮김」을 남긴다.
- 등록부 §Z 에 배정표를 두고 검사가 그 규칙이 사라지지 않게 지킨다.

### 한 줄로

**갈래는 서로를 못 본다. 그래서 「규칙을 적어 둔다」로는 못 막고, 재는 장치가
갈래 전부를 읽어야 한다.**

---

## M-49. 판 지문이 미리보기를 덮지 않는다 — 셋이 같은 지문을 찍고 있었다

**등급: 심각 (M-25 가 막으려던 바로 그 상태가 다른 자리에서 재현됐다)**

**언제** 2026-08-25 · **어디** `build-stamp.js` ↔ 미리보기 산출물

**실측**

```
배포 묶음 16개   main 과 이 갈래가 16/16 동일
미리보기 파일    section-preview.html  24e0ca68… → d52943ea…   (통째로 달라짐)
화면이 찍는 지문  main 3f9e0360  ·  이 갈래 3f9e0360           ← 같다
```

**원인** 지문은 **배포 묶음 16개**에서만 만든다(`build-embed.js` 의 `required()`).
미리보기를 만드는 `changes.js` · `build-preview.js` · `section-preview.html` 은
그 16개에 **없다.** 미리보기 내용이 아무리 바뀌어도 지문은 안 움직인다.

**왜 위험한가** CLAUDE.md §8 은 「사장님이 **사진 한 장으로** 내가 보는 것이 그
판인가를 가릴 수 있다」를 그 지문의 존재 이유로 적었다. 2026-08-25 현재 갈래 넷 중
**셋이 같은 `3f9e0360`** 을 찍는다 — 사장님이 세 아티팩트를 여시면 내용은 전부
다른데 지문은 하나다. 「고쳤습니다」와 「옛 화면을 보고 계신다」가 다시 구분되지 않는다.

**아직 안 고쳤다** — `build-preview.js` 를 **갈래 넷이 전부** 건드리고 있어(실측)
지금 손대면 병합 충돌만 키운다. 등록부 **D-114** 에 올리고 **병합이 끝난 main 에서
한 번에** 한다. 그때까지는 **지문으로 미리보기 판을 가리지 마십시오.**

### 한 줄로

**지문이 있다고 덮이는 것이 아니다. 무엇을 덮는 지문인지 재 보지 않으면,
같은 지문 세 개가 서로 다른 화면에 찍혀 있어도 아무도 모른다.**


---

## M-26. 막으라고 만든 검사가 「바꾼 것이 있는 날」에만 막았다

**등급: 심각 (배포가 통째로 안 됐는데, 나는 사장님께 「배포했다」고 말할 뻔했다)**

**언제** 2026-08-23 · **어디** `deploy-nas` 워크플로의 「Check served build」

### 경위

사장님이 「인터넷에 지금 즉시 반영해줘」라고 하셔서 배포 워크플로를 돌렸다.
빨갛게 끝났고, 로그에는 이렇게 적혀 있었다:

    intake.html — **올리기가 실제로 안 됐다.** 저장소와 디스크가 다르다.
    outputs.html — **올리기가 실제로 안 됐다.** …
    report-flow.html — … reports.html — …
    ##[error]Process completed with exit code 1

16개 중 **4개**가 그렇게 죽었고, 그 뒤의 `Upload` · `Verify deployed` 는 통째로
**건너뛰어졌다.** 즉 **아무것도 안 올라갔다.**

### 왜 그랬나 — 고장이 아니었다

그 4개는 **내가 그날 고친 바로 그 파일들**이다. 저장소와 NAS 디스크가 다른 것이
당연하다 — 그러라고 배포를 도는 것이다.

문제는 **잣대를 자리에 안 맞게 썼다**는 것이다. `verify-served.sh` 한 벌을
올리기 **전**과 **뒤**에 똑같이 썼는데, 같은 사실의 뜻이 두 자리에서 정반대다:

| 저장소 ≠ 디스크 | 올리기 **전** | 올리기 **뒤** |
|---|---|---|
| 뜻 | **올릴 이유가 있다** (정상) | **올리기가 안 됐다** (사고) |

★ 그래서 이 장치는 **바꿀 것이 없는 날에만 초록**이었다. 바꾼 것이 있으면
반드시 막았다 — 즉 **쓸 일이 있을 때만 고장 나는 장치**였다.

### 무엇이 진짜 비쌌나

빨간 것 자체가 아니다. **「초록으로 끝난 배포가 파일을 안 바꿨다」는 옛 사고
(M-20 · M-22)를 잡으라고 만든 문구가, 이번에는 정반대 상황에서 그대로 나왔다**는
것이다. 문구는 확신에 차 있었고 나는 그것을 그대로 믿을 뻔했다.

★ **잘 쓴 오류 메시지일수록 틀렸을 때 더 비싸다.** 사람이 그 문장부터 파기 때문이다
(M-24 · M-25 와 같은 종류 — 이번이 셋째다).

### 고친 것

- `verify-served.sh` 에 `LP_BEFORE=1` 을 뒀다. 켜면 **재기만 하고 절대 빨갛게
  끝내지 않으며**, 「저장소 ≠ 디스크」를 「올리기 전이므로 이것이 올릴 이유다」로 적는다.
- 워크플로의 앞 탐침만 그것을 켠다. **올린 뒤 확인은 그대로 치명이다** —
  거기서 무르면 초록이 거짓말을 한다.
- 스크립트는 여전히 **한 벌**이다. 두 벌로 가르면 갈라진다 (기존 검사가 잰다).

### 막는 장치

- `workflow-default.test.js` — 앞 단계에 `LP_BEFORE: '1'` 이 있는가, **뒤 확인에는
  없는가**. 둘을 함께 재지 않으면 무르게 만든 쪽만 남는다.
- 같은 판에 `guard` 를 넣어 **검사가 초록일 때만 배포가 시작**하게 했다.
  그리고 `alert` 가 `guard` 도 보게 했다 — 안 그러면 guard 가 막은 날
  deploy 는 「실패」가 아니라 「건너뜀」이 되어 **알림이 조용히 사라진다** (§2).

### 다음에 같은 냄새가 나면

**「이 검사는 언제 빨간가」를 먼저 묻는다.** 「무엇을 잡는가」가 아니라.
답이 「바뀐 게 있을 때」면 그 검사는 거꾸로 달려 있다.

### 한 줄로

**같은 사실도 자리가 바뀌면 뜻이 뒤집힌다. 잣대는 하나여도 되지만, 어느 자리에서
재는지는 말해 줘야 한다.**


---

## M-27. 판을 가리라고 만든 장치를 아무도 못 보고 있었다

**등급: 심각 (이것 하나 때문에 같은 왕복을 다섯 번 했다)**

**언제** 2026-08-23 · **어디** `build-stamp.js` 와 화면 여섯 전부

### 경위

사장님이 화면을 찍어 보내시고 「반영 되었는지 교차검증해줘」라고 하셨다.
나는 답변마다 「판 b76a7fb3 입니다」라고 적어 왔다 — CLAUDE.md §8 이 그렇게
하라고 적혀 있고, 그러면 **사진 한 장으로 판이 갈린다**고 되어 있었다.

그런데 사장님은 그 값을 화면 어디에서도 찾을 수 없으셨다.

**지문이 `<html data-lp-build="…">` 속성에만 있었다. 속성은 사진에 안 찍힌다.**

### 왜 여섯 달 동안 안 잡혔나

검사가 있긴 있었다. 그런데 두 가지가 겹쳐 눈이 멀어 있었다:

1. **화면 하나만** 봤다 — `files.html`. 나머지 다섯은 아무도 안 쟀다.
   하필 사장님이 실제로 찍어 보내시는 화면(`report-flow.html`)이 그 다섯에 있었다.
2. **소스를 문자열로 훑었다.** `'판 ' + build` 라는 글자가 파일에 있으면 통과였다.
   그 코드가 **언제 도는지**는 안 봤다.

그리고 유일하게 그리던 `files.html` 조차 그것을 **한도 상자 안**에 그렸다 —
서버에서 한도를 받아야 나오는 자리다. 즉 **서버가 닫혀 있을 때, 판을 가장
알고 싶은 바로 그때 지문이 사라졌다.**

### 무엇이 진짜 비쌌나

빠진 기능이 아니다. **판을 가리라고 만든 장치가 안 보이는 채로, 나는 그것이
보인다고 믿고 말해 왔다**는 것이다.

- 나: 「반영했습니다 · 판 b76a7fb3」
- 사장님: 옛 화면을 보고 계신데, 그 사실을 **가릴 방법이 화면에 없다**
- 그래서 다음 왕복이 시작된다 — 오늘만 다섯 번 (M-20 · M-22 · M-25 · M-26 · 그리고 이것)

★ **없는 장치보다 「있는 줄 아는 장치」가 비싸다.** 없으면 다른 방법을 찾는데,
있는 줄 알면 그것을 근거로 결론을 낸다.

### 고친 것

- `flow-core.js` 에 `stampInto(doc)` 하나 — 화면 맨 아래에 **조건 없이** 그린다.
  문구(`buildLabel`)도 거기서 나온다. 두 벌이면 화면마다 다르게 적힌다.
- 배포되는 화면 **여섯 전부**가 그것을 부른다.
- `files.html` 의 조건부 자리는 뺐다 — 서버가 닫히면 사라지던 자리다.

### 막는 장치

- `build-stamp.test.js` — **헤드리스로 실제로 그려 본다.** 소스에 그리는 코드가
  있는 것과 화면에 글자가 뜨는 것은 다른 말이다. 이 저장소는 그 차이로 여러 번 속았다.
- 화면 하나가 아니라 `stampMod.pages()` 전부를 돈다. 화면이 늘면 자동으로 함께 잰다.
- 문구를 화면이 따로 적으면(`'판 ' +`) 잡는다.

### 다음에 같은 냄새가 나면

**「이 검사는 무엇을 보고 통과하는가」를 묻는다.** 「무엇을 잡는가」가 아니라.
답이 「소스에 그 글자가 있으면」이면, 그 검사는 **글자를 재는 것이지 기능을
재는 것이 아니다.**

### 한 줄로

**보이라고 만든 것이 보이는지는, 그려 봐야만 안다.**


---

## M-28. `null` 을 주소에 붙여서 404 를 만들고, 서버를 뒤졌다

**등급: 심각 (원인이 우리 화면 안에 있었는데 반나절 서버를 의심했다)**

**언제** 2026-08-23 · **어디** `files.html` 의 `call()` · 「＋ 신규프로젝트 → 만들기」

### 경위

사장님이 신규 프로젝트를 만드시는데 이렇게 떴다:

    서버 오류 (404)

그리고 그 아래 ②는 「위 ① 에서 프로젝트를 먼저 지정해 주십시오」로 잠긴 채였다.
사장님 신고는 **「자료 업로드가 작동 안 됨 · 파일 선택 창이 열리지 않음」**이었다.

★ **증상과 원인이 두 칸 떨어져 있었다.** 파일 고르기는 멀쩡하다 — 프로젝트가
  없어서 그 칸이 안 열린 것이고, 프로젝트가 없는 이유는 404 다.

### 원인 — 우리 코드다

`C.api` 가 `null` 인데 **그대로 이어 붙였다.**

    fetch(C.api + '/projects')   →   fetch("null/projects")

`"null/projects"` 는 지금 페이지 옆의 없는 파일이다. 웹서버가 **404** 를 준다.

### 무엇이 진짜 비쌌나

**그 404 는 「서버가 라우트를 잃은 것」과 글자 한 자 다르지 않다.**

나는 M-11(2026-08-18 · 라우팅 표를 손으로 옮기다 11개 누락 404)을 떠올리고
**NAS 엔진부터 의심했다.** 배포 워크플로에 라우트 점검을 붙이고, 엔진 배포를
어떻게 묶을지까지 생각했다 — 원인은 화면 안 한 줄이었다.

★ M-12 가 이미 적어 두었다: **「길이 없어 나는 404 와 자원이 없어 나는 404 를
  응답 형식으로 가른다」.** 엔진 404 는 JSON 이고 `error` 가 있다. 웹서버 404 는
  HTML 이라 없다. **화면이 그 구분을 안 해서** 「서버 오류 (404)」 한 줄이 됐고,
  그 한 줄로는 어디를 봐야 하는지 알 수가 없다.

### 고친 것

- **`call()` 한 곳에서 막는다.** 부르는 자리마다 막지 않는다 — 자리마다 막으면
  새로 생긴 자리가 조용히 빠진다 (`loadProjects` 만 막고 있었다).
- **올리기(XHR)도 막는다.** 그 길은 `call()` 을 안 거친다 — 같은 일을 하는 길이
  둘이면 둘 다 막아야 한다 (M-24 가 바로 이 모양이었다: 읽기는 `fetch`,
  올리기는 `XHR`).
- **두 404 를 가른다.** `error` 없는 404 는 「그 주소가 서버에 없습니다 — 서버가
  옛 라우팅 표를 들고 있을 수 있습니다」로 적는다.
- `outputs.html` 에도 같은 구멍이 있었다.

### 막는 장치

`files-tab.test.js` 둘 —
① 소스에서 문지기가 `fetch` **앞에** 있는지, XHR 길에도 있는지, 두 404 를 가르는지
② **실제로 눌러 본다** — api 없이 열고 「만들기」를 눌러서 **404 라고 말하지
   않는지**를 잰다.

★★ **문지기를 빼고 돌려서 둘 다 빨개지는 것을 확인했다** (배포 전 교차검증 10번:
  통과가 아니라 실패를 잰다). 확인 안 했으면 「검사는 있는데 아무것도 안 재는」
  상태를 못 알아챘을 것이다 — M-12 가 그 이야기다.

### 함께 한 것 — 라우트 점검을 배포에 붙였다

이번 원인은 아니었지만, **M-11 을 잡으라고 만든 `verify:nas` 가 다섯 달 동안
한 번도 자동으로 안 돌고 있었다.** 사람이 손으로 쳐야 도는 것이었고 아무도 안 쳤다.
`deploy-nas` 에 「Check routes」를 넣어 배포마다 스물여덟을 두드리게 했다.

★ **만들어 둔 것과 도는 것은 다른 말이다.** M-12 는 「막는 장치도 뚫려 있는지
  재 봐야 한다」고 적었는데, 그보다 앞선 질문이 있었다 — **그 장치가 돌기는 하는가.**

### ★★ 뒷이야기 — **답이 로그 안에 있었는데 못 읽었다**

배포에 붙인 라우트 점검이 처음 돌면서 이렇게 적었다:

    ❌ 무인증 쓰기 → 401     404

그 줄은 `POST /projects` 를 두드린 것이다. **404 는 인증 이야기가 아니라 그
길이 없다는 뜻**인데, 검사 이름이 「무인증 쓰기 → 401」이라 나는 그것을 읽고
**인증 설정을 의심했다.** 그리고 사장님께 「`POST /projects` 가 있는지는 못
쟀습니다」라고 적었다 — **재 놓고도 못 읽은 것이다.**

★ **검사 이름이 곧 진단이 된다.** 「무인증 쓰기 → 401」은 401 을 기대하는
  이름이라, 다른 값이 오면 **전부 인증 이야기로 읽힌다.** 404 는 그 축이
  아니다. 이름이 한 축만 말하면 다른 축의 사실이 그 축으로 접혀 들어간다.

★ 고쳤다: 404 는 **「쓰기 API(POST /projects) — 그 길이 없다」**로 따로 적고,
  건너뛴 이유도 「인증이 안 막아서」가 아니라 **「라우터가 안 걸려서」**로 적는다.
  가짜 NAS 로 양쪽(길 없음 / 인증 열림)을 다 세워 재고, 갈래를 빼면 빨개지는
  것까지 확인했다.

### 한 줄로

**「서버 오류」라고 적혀 있다고 서버가 잘못한 것이 아니다. 그 말은 우리가 적었다.**
**그리고 검사가 적은 말도 우리가 적은 것이다 — 이름이 틀리면 사실이 접힌다.**


**등급: 심각 (같은 사고가 다섯 달 만에 그대로 재발했다)**

**언제** 2026-08-23 · **어디** 「＋ 신규프로젝트 → 만들기」 → `POST /projects`

### 경위

사장님이 화면에서 신규 프로젝트를 만드시는데 이렇게 떴다:

    서버 오류 (404)

그리고 그 아래 ② 자료 넣는 방법은 「위 ① 에서 프로젝트를 먼저 지정해
주십시오」로 잠긴 채였다. 사장님은 **「자료 업로드가 작동 안 됨 · 파일 선택
창이 열리지 않음」**으로 신고하셨다.

★ **증상과 원인이 두 칸 떨어져 있었다.** 파일 고르기는 멀쩡하다 — 프로젝트가
  없어서 그 칸이 아예 안 열린 것이고, 프로젝트가 없는 이유는 404 다.

### 이것은 M-11 의 재발이다

M-11(2026-08-18): 본체가 라우팅 표를 **손으로 옮기다 11개를 빠뜨려 404**.
그때 만든 막는 장치가 `npm run verify:nas` — **스물여덟을 전부 두드린다.**

**그 도구는 지금도 멀쩡하다. 아무도 안 돌렸을 뿐이다.**

- 사람이 **손으로 쳐야** 도는 것이었다.
- 배포 워크플로는 화면 지문만 재고 **라우트는 한 번도 안 물어봤다.**
- 그래서 다섯 달 동안 「도는 줄 아는 검사」로 남아 있었다.

★★ **만들어 둔 것과 도는 것은 다른 말이다.** M-12 가 「막는 장치도 뚫려 있는지
  재 봐야 한다」고 적었는데, 그보다 앞선 질문이 있었다 — **그 장치가 애초에
  돌기는 하는가.**

### 왜 화면만 보면 못 잡나

화면에는 **「서버 오류 (404)」** 한 줄뿐이다. 그 404 가

- **길이 없어서**(서버가 옛 표를 들고 있다) 난 것인지
- **그 자원이 없어서**(프로젝트가 없다) 난 것인지

화면에서는 구분되지 않는다. M-12 가 「응답 형식으로 가른다」고 적어 둔 그
구분이다 — 엔진 404 는 JSON 이고 길 없음 404 는 HTML 이다. 이번 화면 문구가
`b.error` 없이 **일반형**으로 찍혔다는 것은 **길 없음 쪽**을 가리킨다.

### 고친 것

- `deploy-nas` 에 **「Check routes」** 단계를 넣었다. 배포할 때마다 `verify:nas`
  가 스물여덟을 두드린다. **404 면 빨갛게 끝난다.**
- **못 쟀으면 초록으로 끝내지 않는다**(종료코드 2 → 경고 + 요약). 「초록인데
  안 잰 것」이 이 저장소에서 가장 비싼 상태다.
- **어느 길로 쟀는지 적는다.** `LP_BASE` 가 있으면 앱이 쓰는 길로, 없으면
  tailnet 포트 80 으로 재고 **그렇게 적는다** (M-25).
- 배포 판에 `setup-node` 를 넣었다 — 없으면 이 단계만 **조용히 죽는데**,
  하필 조용히 죽으면 안 되는 단계다.

### 막는 장치

`workflow-default.test.js` — 「Check routes」 가 있는가 · `verify:nas` 를 쓰는가 ·
못 쟀을 때 경고를 남기는가 · 404 에 `exit 1` 인가 · 잰 길을 적는가 · node 가 있는가.

### 아직 안 된 것 — **엔진은 이 길로 안 올라간다**

이 배포는 **화면만** 올린다 (D-84 에 그렇게 적혀 있다). `report-api.cjs` 같은
엔진 파일은 본체가 따로 올린다. 그래서 이 검사는 **404 를 알려 줄 뿐 고치지는
못한다.** 알려 주는 것만으로도 다섯 달치 차이가 나지만, 엔진 배포까지 한 길로
묶는 것은 별개 결정이다.

### 한 줄로

**막는 장치는 돌아야 막는다. 「만들었다」와 「돈다」 사이에 다섯 달이 있었다.**

---

## M-29. 배포는 초록인데 브라우저만 옛 스크립트를 들고 있었다

**등급: 심각 (조용히 틀린다 · M-02 와 같은 종류)**

### 경위 (2026-08-23)

1. 4단계 화면을 「자료에서 읽은 값」 중심으로 고치면서 `fields-core.js` 에
   `readEvidence()` 를 새로 넣고, `fields.html` 이 그것을 부르게 했다.
2. 배포가 **초록**으로 끝났다. `Verify deployed` 가 **저장소 = 디스크 = HTTP**
   를 전부 지문으로 대조했고 16개가 모두 맞았다.
3. 그런데 사장님 화면은 **「F.readEvidence is not a function」**으로 죽었다.
4. 원인: **브라우저가 `fields-core.js` 만 아침 것을 들고 있었다.** 새 화면이
   새 함수를 불렀고, 그 함수가 옛 스크립트에 없었다.

### 왜 이것이 가장 잡기 어려운 갈래인가

M-25 는 「반영이 안 된다」를 셋으로 갈랐다 — **저장소 ≠ 디스크 / 디스크 ≠ HTTP /
브라우저**. 앞의 둘은 `verify:nas` 와 `Check served build` 가 잰다.
**브라우저 갈래만 재는 장치가 없었다.**

그리고 이 갈래는 **화면이 안 뜨는 것보다 나쁘다:**

- 화면은 **멀쩡히 뜬다.** 레이아웃도 색도 정상이다.
- 화면 아래 **판 지문도 새 판**으로 찍혀 있다 (M-27 이 만든 그 장치가
  이 경우에는 **틀린 안심**을 준다 — HTML 은 실제로 새것이기 때문이다).
- 남는 말은 **「… is not a function」 한 줄**뿐이다. 캐시가 원인이라는
  **생각 자체가 안 든다.**

### 무엇으로 막았나 — 두 겹

**① 예방 — 형제 주소에 판 표시**

`build-stamp.js` 가 지문을 찍을 때 화면이 부르는 형제 파일 주소도 함께 고친다.

    <script src="fields-core.js?v=a74fcffc"></script>

내용이 바뀌면 주소가 바뀌므로 브라우저가 **다시 받을 수밖에 없다.**

★ 걸리는 곳이 있다: **`?v=` 는 파일 이름이 아니다.** 주소에서 파일을 찾는 자리를
전부 고쳐야 한다 (`build-embed` · `build-preview` · `build-inline` · `build-files`).
안 고치면 「그런 파일이 없다」로 죽는다. 이번에 검사 54개가 그것 때문에 빨개졌다.

★ **`bare()` 가 `?v=` 와 `LP_BUILD` 를 반드시 함께 지워야 한다.** 안 지우면
지문이 자기 자신을 물어 매번 값이 달라지고 `--check` 가 영원히 빨갛다.

**② 탐지 — 화면이 자기 짝을 확인한다**

프록시·확장·오프라인 캐시는 판 표시가 붙어도 옛것을 줄 수 있다. 그래서:

- 배포되는 스크립트마다 `LP_BUILD` 자리를 두고 `build-stamp.js` 가 채운다.
- 각 스크립트가 `BUILD` 를 밖으로 내놓는다.
- **화면 안에** 조각 하나를 두어 `window` 의 `LinkPilot*` 전역을 훑고,
  자기 판을 말하는 것만 골라 문서 지문과 댄다. 다르면 **빨간 띠**로
  「화면과 스크립트의 판이 다릅니다 — 새로고침해 주십시오」 + **어느 것이
  옛 판인지 이름**을 적는다.

★ 이 조각은 **반드시 화면 안**에 있어야 한다. 따로 파일로 빼면 **그 파일도
옛것일 수 있어** 확인 자체가 안 돈다.

★ 표를 두지 않는다. 이름을 손으로 적으면 스크립트가 늘 때마다 갈린다.

### 짝이 깨지는 배포를 막는 체크리스트

화면과 형제 스크립트를 **함께** 고칠 때 전부 확인한다.
(상태 토글 쪽은 [M-02 의 체크리스트](#m-02-상태를-바꾸면서-갱신시각을-같이-안-바꿨다)를 쓴다)

- [ ] **`npm run im:stamp` 를 돌렸는가** — 지문·형제 주소·스크립트 `LP_BUILD` 셋이 함께 바뀐다
- [ ] **`npm run im:stamp -- --check` 가 초록인가** — 속성만 맞고 **형제 주소가 옛것**일 수 있다
- [ ] **새로 만든 함수를 부르는 화면이 그 스크립트를 싣는가** — 안 실으면 배포는 초록이다
- [ ] **`required()` 에 새 파일이 들어갔는가** — 안 들어가면 **한 파일만 옛 판**으로 남는다 (M-22)
- [ ] **주소에서 파일을 찾는 자리가 `?v=` 를 떼는가** — 안 떼면 빌드가 「그런 파일이 없다」로 죽는다
- [ ] **`bare()` 가 판 표시를 전부 지우는가** — 안 지우면 지문이 흔들려 `--check` 가 영원히 빨갛다
- [ ] **짝 확인 조각이 화면 안에 있는가** — 딴 파일로 빼면 그 파일도 옛것일 수 있다
- [ ] **일부러 어긋내 보고 빨개지는가** — 스크립트 `LP_BUILD` 를 옛 값으로 바꿔 `--check` 를 돌려 본다

### 교훈

**「배포가 초록이다」는 서버까지의 사실이고, 「사용자가 새 판을 본다」는
브라우저의 사실이다.** M-02 가 「고쳤다 ≠ 반영됐다」를 말했다면, 이 항목은
그 줄이 **하나 더 있다**는 것이다 — 반영됐다 ≠ 보고 있다.

그리고 **지문이 새 판이라고 안심하지 않는다.** 지문은 HTML 의 사실이지
그 화면이 부르는 스크립트의 사실이 아니었다. 이제는 둘 다 잰다.

---

## M-30. 재는 장치는 만들었는데 재고 나서 「그래서 왜」를 안 말했다

**등급: 중대 (원인 추적이 사람 손으로 되돌아간다)**

### 경위 (2026-08-23)

자료 11건 중 7건이 안 읽혔다. OCR 열쇠가 없어서였고, 그것을 배포가 말하도록
`Check reading power (OCR)` 를 넣었다. 사장님이 열쇠 파일을 NAS 엔진 루트에
놓으셨다. 그런데 배포 #75 는 여전히 **「OCR: 꺼짐」** 한 마디였다.

**그 한 마디로는 다음에 무엇을 볼지 알 수가 없었다.** 아래 다섯이 전부 똑같이
「꺼짐」으로 보였기 때문이다:

  ① 이름이 파일이 아니라 폴더다 · ② 서식 문서(RTF)로 저장됐다 ·
  ③ 줄이 `KEY=값` 모양이 아니다 · ④ 값에 따옴표·빈칸이 섞였다 ·
  ⑤ 자리표시자를 안 바꿨다

실제 답은 **②**였다 — 파일 이름도 자리도 크기도 멀쩡했고, 한 줄만 적으셨는데
**10줄 475바이트**였다. 텍스트 편집기가 글꼴·색·여백을 아홉 줄 붙였고 그 안에
열쇠가 파묻혔다. **열어 보기 전에는 알 수가 없다.**

### 이 결이 같은 날 세 번 났다

1. OCR 확인을 **앞단(포트 80)** 에 물었다 — 앞단은 `/api/linkpilot/*` 를 안
   넘기므로 **늘 「못 쟀다」**였다. 재는 장치가 아무것도 안 쟀다.
2. 세 갈래 중 **「꺼짐」만** 로그로 나갔다. 그래서 경고가 없을 때
   「켜졌다」인지 「못 쟀다」인지 구분이 안 됐다.
3. 「꺼짐」이 **원인을 안 말했다** (이 항목).

같은 날 `deploy/engine.sh` 의 백업 정리도 같은 결이었다 — `rm` 결과를 안 보고
「N개 지움」이라고 적었는데 NAS 에는 열여섯 개가 그대로 있었다 (M-11 · M-12).

### 막는 장치

- `im-agent/tools/env-doctor.js` — **엔진 자신의 파서**(`core/env.js`)로 읽어
  보고 무엇이 걸렸는지 사람 말로 말한다. 강제 오프라인(`IM_AGENT_OFFLINE`)도 본다.
  **값은 한 글자도 안 찍는다** (§2) — 길이와 앞 네 글자가 `AIza` 인지 여부뿐.
- `deploy-nas.yml` 의 「Why is the key not read」 가 그것을 NAS 에서 돌린다.
  node 를 못 찾은 것과 꺼진 것을 **가른다** — 진단 실패가 꺼짐으로 읽히면 안 된다.
- `env-doctor.test.js` — 다섯을 **글자로 갈라 말하는지** 잰다.
- `deploy/engine.sh` 는 지운 뒤 **다시 세어** 남은 수로 말한다.

### 뒤에 하나가 더 있었다 — 「켜짐」이 「살아 있다」는 뜻이 아니었다

원인을 잡고 나서도 구멍이 하나 남아 있었다. `isOffline()` 은 **글자가 들어
있는지**만 본다. 폐기된 열쇠·지워진 프로젝트의 열쇠도 똑같이 「켜짐」으로
나오고, 그러면 **자료를 올리는 그 순간에야** 실패한다 — 화면에는
「못 읽었습니다」만 남고 배포 기록은 초록이다.

★ 실제로 저장소에 `GEMINI_API_KEY` Secret 이 **한 달 전 것**으로 남아 있었다.
  크론 시절 열쇠이고 살아 있는지 아무도 몰랐다. 그대로 두었으면 「켜짐」이
  나오고 사장님은 다시 자료를 올려 보시고, 또 안 읽혔을 것이다.

★ 그래서 `env-doctor.js --live` 가 **한 번 물어본다.** 거절 이유를 갈라
  말하고(열쇠가 죽었다 / API 가 꺼져 있다 / 권한이 없다 / 한도를 넘었다),
  **못 물어본 것은 죽은 것으로 세지 않는다.**

### 사람 손으로 하는 길은 세 번 실패한 뒤 폐기했다

열쇠 파일을 사장님이 손으로 NAS 에 놓으시게 했다. **세 번 다 다른 자리에서**
막혔다 — ① `.env` 라는 **폴더**가 만들어졌다(File Station 의 [생성] 에는
폴더뿐인데 없는 메뉴를 안내했다) ② 이름이 `linkpilot.env.env` 가 됐다
③ **서식 문서(RTF)로 저장됐고**, 고치신 뒤에도 파일 지문이 그대로였다.

★★ **막힌 자리가 매번 달랐다는 것이 신호다.** 그 길에 함정이 넷이면
  (서식 모드·확장자·업로드 건너뛰기·숨김 파일) **안내를 더 잘 쓰는 것으로는
  안 된다.** 길을 없애는 것이 고치는 것이다 — 이제 배포가 GitHub Secret 값을
  파일로 놓는다. Secret 칸은 그냥 글자 상자라 잘못 저장할 방법이 없다.

### 다음에 같은 결을 만들지 않으려면

**재는 장치를 넣을 때 「무엇을 재는가」와 함께 「빨간색이 나오면 다음에 무엇을
보는가」를 정한다.** 그 답이 「사람이 ssh 로 붙어서 본다」면 아직 덜 된 것이다.

★ **장치 자신이 재지 못한 경우를 통과로 세지 않는다.** 「못 쟀다」는 꺼진 것과
다른 사실이고, 조용히 지나가면 둘이 같아 보인다.

★★ **「있다」를 「된다」로 세지 않는다.** 열쇠가 들어 있는 것과 그 열쇠가
받아들여지는 것은 다른 사실이다. 파일이 있는 것과 그 파일이 읽히는 것도 그렇다.

★★★ **사람이 같은 일에 세 번 막히면 안내가 아니라 길을 고친다.**

---

## M-31. 교차검증 넷을 손으로 챙기다 빠뜨렸다

**등급: 중대 (빠뜨린 것이 그대로 사장님 화면으로 간다)**

### 경위 (2026-08-23 ~ 24)

CLAUDE.md §8 이 배포·전달 전에 넷을 요구한다 — `npm test` · 지문 확인 ·
미리보기 재생성 · 헤드리스 렌더. 그런데 **넷을 매번 손으로 챙기고 있었다.**
하루 반 동안 이렇게 빠졌다:

- **미리보기 재생성을 잊어** `section-preview.html` 이 소스와 갈렸다 — **세 번**
- **지문 다시 찍기를 잊어** 짝 확인이 빨개졌다 — **두 번**
- **헤드리스 렌더를 안 해서** 흰 화면을 사장님이 먼저 보셨다 — **한 번**

★★ 셋 다 「할 줄 몰라서」가 아니라 **「빠뜨려서」**다. 넷을 사람이 기억하는
  것으로는 안 된다 — **한 줄로 만들어야 안 빠진다.**

### 같은 날 배포에서도 두 번 헛돌았다

- **커밋만 하고 배포를 안 걸었다.** 「올렸습니다」라고 말했는데 NAS 에는
  안 닿았다. 푸시와 배포는 **다른 일**이다.
- **입력값 없이 배포를 걸었다.** `workflow_dispatch` 의 `dry_run` 기본값이
  `true` 라, 초록으로 끝났는데 **아무것도 안 올라갔다.**
  ★ 그 판은 「Dry run summary」만 돌고 나머지가 전부 skipped 다 — 성공으로 보인다.

### 막는 장치

- **`npm run guard`** (`im-agent/tools/guard.js`) — 넷을 한 번에 돌고 표로 낸다.
  - **못 잰 것을 통과로 세지 않는다** (M-11 · M-12 · M-30). 크로미움이 없으면
    「못 쟀다」로 적고 초록으로 안 끝난다.
  - 미리보기는 **git 을 안 본다.** `git status` 로 재면 아직 커밋 안 한 작업까지
    「갈렸다」로 잡아 헛울음이 난다 — 재려는 것은 **「다시 만들면 달라지는가」**다.
    그래서 만들기 전후의 지문을 댄다 (첫 판이 실제로 헛울음을 냈다).
  - 헤드리스는 **빈 화면**뿐 아니라 **헛울음**도 잡는다 — 멀쩡한 화면에 고장
    딱지가 붙으면 진짜 고장이 안 보이게 된다.
- 배포를 걸 때는 **`dry_run=false` · `engine=yes`** 를 반드시 준다 (CLAUDE.md §8).

### 다음에 같은 결을 만들지 않으려면

**규칙을 문서에만 적으면 그 규칙은 사람의 기억에 얹힌다.** 세 번 이상 지켜야
하는 절차는 **명령 한 줄로 만든다** — 그러면 빠뜨릴 자리가 없어진다.

★ 그리고 **「했다」와 「닿았다」를 늘 나눠 적는다.** 커밋했다 ≠ 배포를 걸었다
≠ NAS 에 닿았다. 셋이 다른 사실인데 한 마디로 말하면 그 사이에 사장님이
옛 화면을 보신다 (M-25 와 같은 결).


---

## M-32. 「생성 중 문제가 생겼습니다」 — 까닭이 한 줄도 없었다

**2026-08-24 · 사장님 휴대폰 두 장.** 보고서 생성이 47% 에서 멈추고 화면에는
「생성 중 문제가 생겼습니다」 한 줄. 아래 활동 기록에는 경고 세 줄
(`원본자료가 없다` · `소재지가 확인되지 않아 …` · `ECOS_API_KEY 미…`)뿐이었고,
6단계·7단계는 `0/1` 이었다.

### 무슨 일이 있었나

사진만으로는 **어느 Agent 가 왜 죽었는지 알 수 없었다.** 화면에 적힌 경고 셋은
전부 WARNING 이라 멈춤의 원인이 아니고, 진짜 원인은 어디에도 안 적혀 있었다.

★ 그런데 **값이 없어서 못 본 것이 아니다.** `monitor.json` 은 Agent 마다
`error` 를 이미 들고 있었다. 못 본 이유는 둘이다.

1. **활동 기록에 안 들어갔다.** 화면의 로그는 `activity.jsonl` 만 읽는데,
   거기 쌓이는 것은 `ctx.warn` 과 `ctx.activity` 뿐이었다 — 실행기가 잡아 낸
   **종료 사유는 한 줄도 안 옮겨졌다.**
2. **화면이 안 꺼내 썼다.** `live-core.js` 는 실패 여부(`hasError`)만 보고
   머리말을 바꿨을 뿐, 어느 단계가 왜 멈췄는지는 그리지 않았다.

### 왜 이것이 비싼가

**조용히 죽는 것과 결과가 같다** (CLAUDE.md §2). 사용자는 다시 눌러 보는 것
말고 할 수 있는 것이 없고, 나는 사진을 받고도 원인을 못 짚는다. 그날 실제로
그렇게 한 턴이 통째로 갔다.

★ 그리고 이 결은 **잘 만든 화면일수록 잘 생긴다.** 상태를 사람 말로 예쁘게
요약할수록 「무엇이 틀렸는가」가 요약에 먹힌다.

### 막는 장치

- `core/runtime.js` — 종료 상태가 `error`·`blocked` 면 **`monitor.activity` 로
  ERROR 한 줄을 남긴다.** 화면 로그가 읽는 곳이 거기뿐이기 때문이다.
- `ui/platform/live-core.js` — 멈춘 단계를 모아 `stopped` 로 내고,
  **첫 까닭을 머리말 바로 밑(`note`)에** 놓는다. 나머지는 아래로 내린다
  (같은 줄을 두 곳에 안 적는다).
  - **Agent 이름(`06_im_writer`)이 아니라 단계 이름(`문서 만들기`)으로** 말한다.
  - 사유가 비어 있으면 **비었다고 적는다** — 침묵하지 않는다.
- `ui/platform/reports.html` — 멈췄을 때 그 줄을 **빨갛게** 그린다. 회색
  안내문으로 적으면 「그냥 설명」으로 읽혀 넘어간다.
- `im-agent/test/failure-visible.test.js` — 위 넷을 전부 잰다. 헛울음도 잰다
  (도는 중에는 아무 말도 덧붙이지 않는다).

### 다음에 같은 결을 만들지 않으려면

**「실패했다」를 보여 주는 화면은 반드시 「무엇이 · 왜」를 같이 보여 준다.**
상태만 있고 사유가 없는 화면은 고장 신호가 아니라 **막다른 길**이다.

★ 그리고 사유는 **이미 들고 있는 값을 옮기기만 하면 되는 경우가 대부분**이다.
새로 재지 말고 먼저 **안 옮겨진 곳이 어디인지** 본다.


---

## M-33. 「열기」가 또 아무것도 안 열었다 — 이번엔 틀 안이라서다

**등급: 심각 (안내가 사람을 엉뚱한 데로 보낸다)**

**2026-08-24 사장님 화면.** 산출물 일곱이 다 「완료」인데 [열기] 를 누르니
빨간 줄이 떴다.

> 팝업이 차단되었습니다. 외부 브라우저(Chrome · Safari)에서 다시 열어 주세요.
> 주소: `/api/linkpilot/projects/LP-RE-2026-013/file?rel=12_Final%2Fim-a4.html`

### 두 겹으로 틀렸다

**① 원인을 잘못 짚었다.** 사장님은 **이미 사파리**에 계셨다. 막은 것은 브라우저의
팝업 차단기가 아니라 **앱 화면이 틀(iframe) 안**이라는 사실이다 — 틀 안에서
스크립트가 부르는 `window.open` 은 브라우저가 막는다. 화면은 인앱 브라우저
하나만 보고 있었고, 그것이 아니면 전부 「팝업 차단」으로 몰았다.

**② 시키는 대로 해도 안 되는 안내였다.** 적힌 주소는 **상대 경로**라 도메인이
없다. 다른 브라우저에 붙여 넣어도 못 간다. 그리고 **글자**라서 누를 수도 없다.

★ 두 번째가 더 나쁘다. 사람이 **그 안내를 따라 시간을 쓰고 나서** 실패한다.

### 왜 M-24 · M-28 과 같은 결인가

**원인을 단정한 안내는 없는 고장을 찾게 만든다.** 「서버 연결을 확인하세요」로
멀쩡한 서버를 뒤지게 했던 것과 같다. 화면이 아는 것은 「우리가 못 열었다」뿐인데,
**왜 못 열었는지까지 아는 척**을 했다.

### 고친 방향 — 여는 일을 브라우저에 맡긴다

- **단추를 진짜 링크(`<a href>`)로 만든다.** 사람이 누른 링크는 틀 안에서도
  안 막힌다. 스크립트가 여는 새 창만 막힌다.
- `window.open` 은 **인쇄창을 띄워야 할 때만** 쓴다. 그리고 막히면
  **막지 않는다** — 링크의 원래 동작이 그대로 이어져 어차피 열린다.
- 안내가 필요할 때는 **절대 주소로, 누를 수 있는 링크로** 준다.
- 원인을 **셋으로 가른다**: 인앱 브라우저 · 틀 안 · 맨 위 창.

### 이 자리에서 새로 배운 것 — 빈 주소를 절대 주소로 바꾸지 않는다

고치는 중에 `absUrl(fileUrlFor(path) || '')` 라고 썼다. 주소가 없을 때
빈 문자열이 **이 페이지 자신의 주소**가 되어, 누르면 새로고침만 되고 파일은
안 열리는 링크가 됐다. **검사가 잡았다** — 서버 주소가 없는 판을 함께 재고
있었기 때문이다 (표본이 성질을 지켜야 한다, CLAUDE.md §8).

### 파일 열기 기능 검증 체크리스트

파일·문서를 여는 기능을 만들거나 고칠 때 **전부** 확인한다.

- [ ] **누르는 것이 링크인가** — 스크립트가 여는 새 창은 틀 안에서 막힌다
- [ ] **막혔을 때 링크의 기본 동작을 안 막는가** — `preventDefault` 는
      **실제로 열었을 때만** 부른다
- [ ] **안내에 나오는 주소가 절대 주소인가** — 상대 경로는 붙여 넣어도 못 간다
- [ ] **그 주소를 누를 수 있는가** — 글자로 적으면 아무 소용이 없다
- [ ] **원인을 가르는가** — 인앱 · 틀 안 · 맨 위는 서로 다른 원인이고 할 일도 다르다
- [ ] **주소를 못 만들었을 때와 못 열었을 때를 가르는가** — 앞엣것은 우리 탓,
      뒤엣것은 브라우저 사정이다
- [ ] **차단된 산출물은 여전히 안 열리는가** — 링크를 달면 검증을 우회한다
- [ ] **주소가 없을 때 링크를 안 만드는가** — 빈 주소는 페이지 자신이 된다
- [ ] **검사가 두 판을 다 보는가** — 주소가 있는 판과 없는 판. 하나만 보면
      「초록인데 아무것도 안 재는」 상태가 된다

### 다음에 같은 결을 만들지 않으려면

**브라우저가 이미 하는 일을 우리가 대신 하지 않는다.** 링크를 여는 것은
브라우저의 일이고, 브라우저는 사람이 누른 것과 스크립트가 부른 것을 다르게
대접한다. 우리가 끼어드는 순간 그 구분을 잃는다.

★ 그리고 **안내는 「내가 아는 것」까지만 적는다.** 화면이 아는 것은
「못 열었다」이지 「브라우저가 막았다」가 아니다.


---

## M-34. 「자료에서 읽었습니다」가 거짓말이었다

**등급: 최상 (시스템의 존재 이유를 화면이 뒤집는다)**

**2026-08-25 사장님:** 「스캔, 읽는 흉내만 내지 실제 판독을 하지않음 거짓」.

화면은 이렇게 적고 있었다.

> **3** 개의 값을 **자료에서 읽었습니다** · 자료 1건
> `user_request` … 3개

**그 셋은 전부 사장님이 요청문에 쓰신 말이었다.** 올리신 PDF 에서 읽은 값은
**하나도 없었다.** 그런데 화면은 그것을 「자료에서 읽었다」로 적고, 그 아래
`user_request` 라는 **내부 이름을 자료 이름처럼** 나열했다.

### 왜 이것이 이 저장소에서 가장 나쁜 종류인가

`intake.html` 머리에 이 저장소의 전제가 적혀 있다 —
**「사용자가 말했다는 것은 문서로 확인됐다는 뜻이 아니다. 그 구분이 이 시스템의
전부다.」** 엔진은 그 구분을 지키고 있었다(`facts.js` 의 `origin` 등급).
**화면이 뭉갰다.**

★ 값이 틀린 것보다 나쁘다. 값은 대 보면 틀린 줄 알 수 있지만, **근거의 등급을
속이면 대 볼 것이 없다.** 그리고 화면이 「자료에서 읽었다」고 하면 사람은
그 다음부터 자료를 안 열어 본다.

### 무엇이 원인이었나

`readEvidence()` 가 **값에 `source` 문자열이 있으면 전부 「자료」로** 셌다.
`user_request` 도 문자열이라 그대로 자료가 됐다.

★ 엔진에는 이미 `origin` 이 있었다(`document` / `public` / `request` / `derived`).
**서버가 화면에 안 넘기고 있었다** — M-32 와 똑같은 결이다: **값이 없어서 못 본
것이 아니라 안 옮겨서 못 봤다.**

### 막는 장치

- `report-api.cjs` 가 값마다 `origin` 을 함께 내려보낸다.
- `fields-core.js` 의 `readEvidence()` 가 **수집자료**(`origin === 'document'`)와
  **자체분석자료**(나머지)를 따로 센다.
- **`origin` 을 모르면 `unknownOrigin` 으로 둔다** — 모르는 것을 「읽었다」로 세는
  것이 바로 이 사고다. 옛 서버가 섞여 있을 때 조용히 되돌아가지 않게 한다.
- 화면은 「N개 수집자료 + M개 자체분석자료」로 적고, **자체분석 쪽을 작고 낮은
  대비로** 그린다 — 같은 크기면 같은 무게로 읽힌다.
- 자료별 세로 나열을 **뺐다.** `user_request` 같은 내부 이름이 그대로 보였다.
- `read-evidence.test.js` 가 넷을 잰다: 갈라 세는가 · 모르면 안 세는가 ·
  화면이 두 숫자를 적는가 · 옛 문구가 안 남았는가.

### 「근거를 말하는 화면」 검증 체크리스트

값의 출처·등급·개수를 보여 주는 화면을 만들거나 고칠 때 **전부** 확인한다.

- [ ] **문서에서 읽은 값과 우리가 만든 값을 가르는가** — 뭉치면 만든 값이 증거가 된다
- [ ] **모르는 것을 아는 쪽에 넣지 않는가** — 등급이 없으면 「모른다」로 둔다
- [ ] **내부 이름이 화면에 나오지 않는가** (`user_request` · `derived` …)
- [ ] **둘의 시각 무게가 다른가** — 같은 크기면 같은 값으로 읽힌다
- [ ] **엔진이 아는 것을 서버가 화면까지 옮기는가** — 안 옮기면 없는 것과 같다 (M-32)
- [ ] **0 일 때 0 이라고 크게 말하는가** — 작게 적으면 「아직 안 그려졌나」로 읽힌다
- [ ] **검사가 두 판을 다 보는가** — 등급이 있는 판과 없는 판

### 다음에 같은 결을 만들지 않으려면

**화면이 「어디서 왔는가」를 요약할 때는 늘 가장 낮은 등급으로 말한다.**
셋 중 하나만 문서에서 왔으면 「자료에서 셋을 읽었다」가 아니라
「하나는 자료, 둘은 우리가 만든 것」이다.

★ 그리고 **엔진이 아는 것을 화면이 모르는 자리를 의심한다.** 오늘만 두 번이었다
(M-32 실패 사유 · M-34 출처 등급). 둘 다 **값은 이미 있었고 안 옮겼을 뿐**이다.


---

## M-35. 고치다 새 사고를 냈다 — blob 으로 열면 글자표가 사라진다

**등급: 심각 (멀쩡한 문서를 고장으로 보이게 한다)**

**2026-08-25 사장님 화면.** IM 본문을 열었더니 통째로 이렇게 나왔다.

> `蹂묒뿭` `�뾽怨꾪쉷��` `二쎌삸` …

사장님 말씀은 **「결과물은 오류·버그 투성이」**였다. 그런데 문서는 멀쩡했다 —
**보는 길만 틀렸다.**

### 경위 — 어제 내가 넣었다

M-33(「새 탭에 자격이 안 붙는다」)을 고치면서 **화면이 직접 받아 blob 으로
여는 길**로 바꿨다. 인증 문제는 그것으로 사라졌다.

그런데 **blob 으로 열면 글자표(charset)가 사라진다.** 서버는
`text/markdown; charset=utf-8` 을 제대로 보내는데, 브라우저는
**렌더 규칙을 모르는 형식의 charset 딱지를 무시하고 기본 인코딩으로 그린다.**
한글이 통째로 깨진다.

### 왜 이것이 특히 나쁜가

**안 열리는 것보다 나쁘다.** 안 열리면 사람은 「못 열었다」고 읽지만,
깨져서 열리면 **「만들어진 것이 잘못됐다」**고 읽는다. 그러면 멀쩡한 엔진을
뒤지게 된다 — M-24 · M-28 · M-33 과 같은 자리다.

★ 그리고 **내가 고친 자리에서 났다.** 고칠 때 「열리는가」만 보고
**「열어서 읽히는가」는 안 봤다.** M-33 에서 「열렸다」와 「열어서 보였다」가
다른 사실이라고 적어 놓고, 바로 다음 판에서 같은 종류를 냈다.

### 막는 장치

- `reports.html` — 글자 파일(`.md`·`.html`·`.json`·`.txt`)은 **`r.text()` 로
  받아 `new Blob([문자열])` 로 다시 담는다.** 그 생성자는 **언제나 UTF-8** 이다.
  딱지도 브라우저가 확실히 아는 형식으로 바꿔 단다(`text/plain`·`text/html`).
- **이진 파일(PDF)은 그대로 blob 으로 받는다** — 글자로 바꾸면 깨진다.
- `test/open-file.test.js` — 형식별 딱지에 `charset=utf-8` 이 붙는지,
  이진 파일이 글자로 안 바뀌는지, 경로가 넘어가는지를 잰다.

### 파일 열기 검증 체크리스트 — 두 줄 추가 (M-33 의 것에 잇는다)

- [ ] **열어서 글자가 읽히는가** — 「열렸다」가 아니라 **한글이 안 깨지는가**
- [ ] **이진 파일이 이진으로 가는가** — PDF 를 글자로 받으면 조용히 깨진다

### 다음에 같은 결을 만들지 않으려면

**고친 자리를 실제로 열어 본다.** 「열리는가」까지만 보면 그 다음 칸에서
깨진다. 이 저장소의 §8 이 「헤드리스로 실제 렌더를 확인한다」라고 말하는
이유가 이것인데, **파일을 여는 길에는 그 검사가 없었다.**

★ 그리고 **브라우저가 이미 하는 일을 우리가 대신할 때는 잃는 것이 있는지
본다.** 이번에 잃은 것은 charset 이었다.

---

## M-36. [저장] 막대가 표에서 1946px 떨어져 섰다 — 틀 안에서만 틀린다

**등급: 보통 (기능은 멀쩡한데 화면이 고장으로 읽힌다)**

**2026-08-25 사장님 화면 (판 `c760f626`).** 자료를 스캔한 뒤 표는 끝났는데
[저장]·[되돌리기] 막대가 **한참 아래 흰 여백 끝**에 서 있었다. 그 아래로 앱의
다음 카드(5. 출력조건)가 이어져 더 어색했다.

말씀은 **「[저장] 배치가 너무 아래 동떨어져 배치됨」**이었다.

### 경위 — 두 겹이었다

**① 붙박이가 틀의 아래에 붙었다.** 이 화면은 앱 안의 **틀(iframe)** 에서 돈다.
틀은 내용 높이만큼 늘어나므로 그 안에서 `position: fixed; bottom: 0` 은
「보이는 화면의 아래」가 아니라 **「틀의 아래」**다.

**② 붙박이를 떼는 것만으로는 안 됐다.** 막대를 `document.body` 끝에 붙이고
있었는데, `.app` 이 `min-height: 100%` 라 **항상 한 화면 높이를 채운다.**
그 뒤에 붙이면 흐름에 둬도 **그대로 저 아래**에 선다. 넣는 자리까지 옮겨야 했다.

### 왜 못 잡고 있었나 — 글자로는 안 갈린다

`position: fixed` 는 혼자 뜬 판에서도 틀 안에서도 **똑같이 생겼다.** 틀리는 것은
**좌표**다. 이 저장소의 검사는 거의 다 소스 대조라 이 결을 통째로 못 봤고,
헤드리스 렌더 검사도 「빈 화면인가」만 재고 있었다.

**실측 (헤드리스, 틀 1180×900):**

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 막대 자리 | `fixed` · `BODY` 밑 | `static` · `.main` 안 |
| 표 끝과의 거리 | **-1831px** (표 위로 뚫고 올라와 있었다) | **34px** |
| 막대 아래 남는 화면 | **1946px** | **39px** |

### 막는 장치

- `fields.html` — 그리기 **전에** 틀인지 정해 뿌리에 표시를 단다. 늦게 정하면
  막대가 아래에 한 번 붙었다가 올라와 **자리가 튄다.** 교차출처로 `window.top`
  읽기가 막히면 **그때도 틀로 본다.**
- `fields.html` — 틀 안이면 흐름 안에 두고(`position: static`), 막대를
  **`.main` 안에** 넣는다. 혼자 뜬 판은 붙박이 그대로 둔다.
- `tools/probe-save-bar.js` — **진짜 틀에 넣고 그려서 좌표를 잰다.** 틀 안에서 잰
  값은 `postMessage` 로 밖에 넘긴다(`--dump-dom` 은 틀 안을 안 준다).
  크로미움이 없으면 **되돌아오는 값 2 — 못 쟀다**로 끝난다.
- `tools/guard.js` — 위를 다섯째 칸으로 부른다. **넷은 바닥이지 천장이 아니다.**
- `test/save-bar.test.js` — 글자로 잴 수 있는 넷(순서·틀 판정·흐름·`.main`)과
  「좌표로 재는 검사가 실제로 불리는가」를 잰다.

### 다음에 같은 결을 만들지 않으려면

★ **틀 안에서 도는 화면에 `position: fixed` 를 쓰지 않는다.** 쓰려면 그 화면이
혼자 뜬 판인지 먼저 갈라야 한다. 틀은 늘어나므로 「화면 아래」라는 말이 없다.

★ **자리·크기가 틀린 것은 좌표로만 잡힌다.** 소스 대조로 재려 하면 통과한다.
이 결이 또 오면 `probe-save-bar.js` 옆에 한 칸 더 만든다.

★ **고치고 나서 「전에는 얼마였나」를 함께 잰다.** 이번에 1946 → 39 를 재 두지
않았으면 「나아졌습니다」밖에 못 적었을 것이다 (CLAUDE.md §6-3 ②).

---

## M-37. 빈 목록이 「읽을 것이 없다」로 넘어갔다 — 자료가 있는데 0건으로 성공했다

**등급: 심각 (조용히 아무것도 안 한다)**

**2026-08-25.** 사장님 화면의 **「0개 수집자료」**가 왜 0 이었는지 실제 프로젝트
하나로 끝까지 따라가다 잡았다.

### 무슨 일이 있었나

읽으라고 부르는 자리(`pipeline.extractInto`)가 이렇게 되어 있었다.

> 목록이 배열이면 그것을 쓰고, **아니면 빈 배열을 쓴다**

그리고 받는 쪽(`agents/02-extraction`)은 이렇게 판단한다.

> **목록을 받았으면** 그것을 읽고, 안 받았으면 프로젝트 폴더를 훑는다

**빈 배열도 「받은 것」이다.** 그래서 목록 없이 부르면 폴더를 아예 안 훑었다.
결과는 이렇다.

```
추출: 0건 / 문서 0건 / 미지원 0건
```

**성공으로 끝난다.** 자료가 폴더에 그대로 있는데 한 글자도 안 읽고, 오류도
경고도 없다. 화면에는 「0개 수집자료」만 남고, 사용자는 **자기가 자료를 잘못
올린 줄로** 읽는다.

### 왜 못 잡고 있었나

- **검사가 전부 목록을 주고 있었다.** 실제 화면 경로(`scanSources`)는 늘
  파일 목록을 만들어 넘기므로 그 길은 멀쩡했다. **목록 없이 부르는 길**만
  비어 있었고, 그 길을 재는 검사가 없었다.
- **실패가 성공처럼 생겼다.** 0건은 「자료가 없다」의 정상 결과이기도 하다.
  둘을 화면에서 가를 방법이 없었다.

### 막는 장치

- `pipeline.js` — **줄 것이 있을 때만 준다.** 빈 목록이면 `files` 를 아예 안
  보내고, 받는 쪽이 폴더를 훑게 둔다. 거기도 비었을 때만 「원본자료가 없다」다.
- `pipeline.js` — **빈 목록이 온 것을 말한다.** 부른 쪽의 실수일 수 있고,
  조용히 폴더를 훑으면 「0건인데 값이 나왔다」로 보여 더 헷갈린다.
- `test/collected-count.test.js` — **실제 파이프라인을 돌려** 문서에서 읽은 값이
  화면까지 「수집자료」로 오는지를 잰다. 소스 대조로는 이 결을 못 잡는다.

### 다음에 같은 결을 만들지 않으려면

★ **「빈 것」과 「안 준 것」을 같은 값으로 쓰지 않는다.** 자바스크립트에서 빈
배열·빈 문자열·0 은 서로 다르게 참거짓이 갈린다. 「안 줬다」는 `null`·`undefined`
로 두고, **받는 쪽의 판단 기준을 부르는 쪽에서 한 번 읽어 본다.**

★ **0 이 정상 결과이기도 한 자리는 특히 위험하다.** 그때는 0 옆에 **「왜 0 인가」**
를 함께 남긴다. 이번에 붙인 읽기 진행 기록(M-36 다음 판)이 그 자리를 메운다.

★ **화면에서 이상한 숫자가 보이면 실제 프로젝트 하나로 끝까지 따라간다.**
이 버그는 코드를 읽어서가 아니라 **돌려 보고** 나왔다.

---

> ★ **번호를 M-33·M-34 에서 M-38·M-39 로 옮겼다** 〈2026-08-25 · D-101 합침〉.
>   저쪽 작업선이 M-26~M-32 를 이 작업선 몫으로 비워 두었는데, 그 사이
>   이쪽이 M-33~M-37 까지 썼다. **같은 번호가 두 뜻을 갖는 것**이 D-77 이
>   말한 그 사고라, 나중에 온 쪽을 뒤로 밀었다. 내용은 그대로다.

## M-38. 끝냈다는 말만 있고 반영된 모습이 없었다

〈2026-08-25 사용자 지시 — 메타지침 셋을 규칙으로 굳혔다〉

### 경위 (핵심 포인트)

SketchUp 트랙 작업 중 사장님 지시: **「작업이 끝나면 결과물이 어떻게
반영되었는지 샘플을 만들어 보여줘 — 이건 규칙지침이야」.** 함께 둘:
반복·동일 패턴 지시는 별도 지침이 없어도 스스로 일반화해 지속 반영 구조에
넣을 것, 그리고 더 나은 개선안을 항상 추천할 것.

배경이 있다 — §8 의 「미리보기 주소」 규칙만으로는 **주소를 열어 보기 전까지
무엇이 바뀌었는지 안 보인다.** 커밋 번호로 끝내지 않는 것과 같은 결이다:
반영된 **실물의 발췌**가 답변 안에 있어야 사진 한 장으로도 확인이 된다.

### 규칙 셋 (CLAUDE.md 에 반영)

1. **반영 샘플** (§8): 끝냈다는 답변에는 실제 산출물의 발췌(바뀐 규칙 원문·
   생성된 JSON·파일 한 토막)를 싣는다. 손으로 지어 쓰지 않는다 — M-05.
2. **패턴 지시 자기 일반화** (§7): 「같은 지시 두 번」에서 **「한 번이라도
   패턴이면」**으로 문턱을 내렸다. 경위(핵심 포인트)를 함께 기록한다.
3. **개선안 상시 추천** (§7): **권장**(새로 하자는 것)과 **재확인**(이미
   있는 것)을 갈라 적는다 — 안 가르면 있는 장치를 다시 만든다.

### 재확인 — 이번 지시로 다시 살핀 기존 장치

- **「상태 변경 시 단일 출처 `updatedAt` 필수」 + 상태 토글 검증 체크리스트**
  는 [M-02](#m-02-상태를-바꾸면서-갱신시각을-같이-안-바꿨다) 에 이미 있고,
  색인에도 올라 있다. 시각은 `core/kst.js` 의 `kstStamp()` 한 곳에서만 만든다
  (§8) — SketchUp 계획/결과 파일의 `created`·`plan_created` 도 같은 형식이다.
- **색인 누락은 `memory-index.test.js` 가 잡는다** (M-19~23 다섯이 빠졌던
  사고 후 도입). 이 항목(M-38)도 그 검사가 지킨다.

### 한 줄로

**「했습니다」는 반영된 실물의 발췌와 함께 올 때만 확인 가능한 말이 된다.**

## M-39. 3D 를 만들고도 보여주지 않았다

〈2026-08-25 사용자 지시 — 「나는 항상 3D 모델이 어떻게 보여지는지가 궁금함」〉

### 경위 (핵심 포인트)

데이터센터·아파트 매스를 클라우드 SketchUp 에 만들고 **`.skp` 다운로드
주소만** 드렸다. 그 주소는 임시 토큰이라 만료되고, SketchUp 이 없는 자리에서는
열어 볼 수도 없다. 주상복합 때 처음으로 드래그 회전 HTML 뷰어를 만들었더니
그것이 정답이었다 — 사장님이 규칙으로 굳히셨다.

### 규칙 (CLAUDE.md §8 에 반영)

**모델을 만들거나 받은 턴에는 자체 렌더 3D 뷰어 아티팩트를 같은 주소에
갱신해 드린다.** 외부 라이브러리 없이(캔버스 소프트웨어 렌더) 어느 환경에서든
확실히 뜨게 하고, 「3D 모델 뷰어」 한 페이지에 모델을 모아 **같은 파일 경로
재발행으로 주소를 유지**한다 — 주소가 바뀌면 사장님이 옛 주소를 여신다(M-01).

### 한 줄로

**3D 는 말이 아니라 돌려보는 것이다 — 만들었으면 같은 주소에서 언제나 돈다.**

## M-50. 미리보기 표의 셀이 두세 줄로 접혀 읽기 어려웠다

(M-40 에서 옮김 — 번호 충돌 정리, 2026-08-25)

〈2026-08-25 사용자 지시 — 「엑셀 높이 넓이 자동맞춤 되도록 2,3줄 안되게
자동교정으로 앞으로는 맞춰줘」〉

### 경위 (핵심 포인트)

주상복합 미리보기의 「타워 기준층 세대 평면」 표에서 수치·성격 칸의 긴 문장이
**두세 줄로 접혀** 행 높이가 들쭉날쭉했다. 표는 훑는 물건인데 줄바꿈이 생기면
행과 행이 눈으로 안 갈라진다 — 엑셀의 자동 맞춤(셀은 한 줄, 폭이 내용을
따라감)처럼 보여야 한다는 지시.

### 규칙 (CLAUDE.md §6-3 에 반영)

**미리보기·아티팩트의 표 셀은 한 줄로 그린다** (`td { white-space: nowrap }`).
- 폭이 모자라면 셀을 접지 않고 **표를 가로 스크롤**시킨다 — 표 감싸개
  (`overflow-x: auto`)는 이미 규칙이다.
- 한 줄에 안 들어갈 만큼 긴 설명은 **셀에 넣지 않는다** — 표 밖 문단·각주로
  옮기고 셀에는 요지만 남긴다. 셀을 접어 맞추는 것은 교정이 아니라 회피다.
- 숫자 칸은 `tabular-nums` 로 세로를 맞춘다 (§6-3 ③ 그대로).

### 한 줄로

**표는 엑셀처럼 — 셀은 한 줄, 모자라면 표가 스크롤되고, 긴 말은 표 밖으로.**

## M-51. 실사 톤 조감도를 계산으로 그렸더니 세 군데가 거짓말을 했다

(M-41 에서 옮김 — 번호 충돌 정리, 2026-08-25)

〈2026-08-25 · 주상복합 4종 세트 ④〉

### 경위 (핵심 포인트)

AI 실사 렌더(Veras 4.0 / Gemini)가 **결제 연결에서 막혀 있어**(D-34 실측 2회 모두
`free_tier_requests, limit: 0`), 같은 모델 기하에 **빛·그림자·재질만 얹어** 실사 톤을
직접 계산했다. 형태가 모델에서 오므로 **지어내질 수 없다** — D-34 원결정을 지킨다.

그런데 첫 판이 **그럴듯하게 틀렸다.** 오류는 하나도 안 났다(`errs: []`). 셋 다
**그림을 봐야만** 잡히는 종류다.

1. **그림자가 줄무늬로 보였다.** 매스마다 따로 `fill` 하니 겹치는 자리가 두 번
   어두워졌다. 타워 한 동이 **7개 셀**이라 특히 심했다.
2. **가로수가 타워 앞으로 튀어나왔다.** 판때기(billboard)를 면 다음에 몰아 그려서
   **뒤에 선 나무가 앞 건물을 덮었다.**
3. **그림자가 하늘 위에 떠 있었다.** 127m 타워의 그림자가 부지 밖으로 나갔는데
   바닥이 없는 자리라 **유리판이 공중에 뜬 것처럼** 보였다.

거기에 넷째가 있었다 — **보이는 두 면이 전부 그늘**이라 매스가 시커멓게 나왔다.
태양 벡터를 카메라와 무관하게 골랐기 때문이다.

### 막는 법

- **그림자는 한 경로(`beginPath`)에 모아 `fill('nonzero')` 로 한 번만 칠한다.**
  겹침이 알파로 쌓이지 않는다.
- **판때기와 면을 한 목록에 넣고 깊이로 함께 정렬한다.** 「나중에 그리기」는
  정렬이 아니다.
- **그림자는 바닥 폴리곤으로 `clip` 한다.** 받을 바닥이 없는 자리에는 그림자가 없다.
- **지붕도 바닥이다.** 위에서 내려다보는 그림에서 포디움 지붕은 가장 넓게 보이는
  수평면이다 — 여기에 타워 그림자를 앉히지 않으면 매스가 공중에 뜬다.
- **태양은 카메라를 보고 놓는다.** 보이는 면 중 **하나는 밝고 하나는 그늘**이도록
  고른다. 물리적으로 「그림자가 카메라 쪽으로 오는 그림」은 역광이라 앞면이 다 죽는다.

### 한 줄로

**계산 렌더는 오류를 안 내고 틀린다 — 그림을 눈으로 보기 전에는 통과가 아니다.**

## M-52. 갈래를 삭제하라는 지시를 받았는데 아티팩트를 지울 수단이 없었다

(M-42 에서 옮김 — 번호 충돌 정리, 2026-08-25)

〈2026-08-25 사장님 지시 — 「아파트는 본건에서는 삭제」〉

### 경위 (핵심 포인트)

본건을 주상복합 단일로 좁히라는 지시를 받았는데, **발행한 아티팩트를 없애는 기능이
없다.** 그대로 두면 「아파트 4종 세트」 페이지가 살아 있는 주소로 남아 **본건인 척
돌아다닌다** — M-05(옛말 하는 화면)와 같은 결이고, 상대방에게 잘못 건네지면 더 비싸다.

### 규칙 (CLAUDE.md §8 에 반영)

**못 지우면 덮는다. 덮었으면 되살릴 길을 그 자리에 적는다.**

- 내용을 **「본건 제외」 안내**로 바꾸고, **살아 있는 본건 주소**를 링크로 준다.
- 안내 안에 **되살리는 법**을 적는다 — 판 기록(version)과 작업 폴더의 파일.
  덮는 것은 되돌릴 수 있는 일이지만, **되돌릴 수 있다는 사실이 안 보이면** 못 되돌린다.
- **같은 내용의 아티팩트가 둘 이상이면 전부 덮는다.** 하나만 덮으면 나머지가 최신인
  척한다 (실제로 중복 발행분이 하나 더 있었다).
- 뷰어·목차처럼 **그 갈래를 가리키는 다른 화면도 함께 내린다.**

### 한 줄로

**「지웠습니다」라고 말할 수 없으면 「내렸습니다 · 이렇게 되살립니다」라고 말한다.**

## M-53. 설계 도면을 그렸더니 CSS 한 줄이 해칭을 전부 지웠다

(M-43 에서 옮김 — 번호 충돌 정리, 2026-08-25)

〈2026-08-25 사장님 지시 — 「구도 단면도 및 평면도, 배치도등 실재 도면
설계사로부터 받는 퀄리티 수준으로 만들어줘」〉

### 경위 (핵심 포인트)

배치도·평면도 2·단면도 넉 장을 A2 시트에 실제 축척으로 그렸다. **오류는 한 번도
안 났다**(`errs: []`). 그런데 눈으로 보니 셋이 틀려 있었다.

**① 해칭·먹칠·백색채움이 전부 사라졌다.** 선 색을 한 곳에서 주려고
`#id rect{stroke:#1b1b1b;fill:none}` 을 적었는데, **CSS 규칙은 표현속성
(presentation attribute)을 이긴다.** 그래서 `fill="url(#hConc)"` 도
`fill="#ffffff"` 도 전부 `none` 이 됐다. 도면은 멀쩡히 그려지고 **채움만 없었다.**

**② 되살렸더니 이번엔 반대로 검게 찼다.** `fill` 을 안 적은 `<path>`·`<circle>`
의 기본값은 **검정**이다. 문 열림 호(arc)와 가로수 원이 **먹칠 초승달**이 됐다.

**③ 치수선이 도면 밖으로 날아갔다.** 오프셋을 `X(-14)`(모형 −14m)로 적었는데,
1:500 에서는 28mm 지만 **1:100 에서는 140mm** 다. 「28,000」 전체 치수가
시트 밖에 그려져 화면에 아예 없었다.

거기에 넷째 — **축척마다 옳은 표현이 다르다.** 1:500 에서 두께 0.25m 슬래브는
0.5mm 라 해칭 무늬가 들어갈 자리가 없다. 실무는 이 축척에서 **먹칠(poche)** 로
그린다. 해칭을 고집하면 그냥 빈 줄로 나온다.

### 막는 법

- **선은 CSS 로, 채움은 속성으로.** 도형 CSS 에 `fill` 을 적지 않는다.
- **채우면 안 되는 도형에는 `fill="none"` 을 명시한다** — 기본값은 검정이다.
- **치수·통심선 오프셋은 「시트 mm」로 준다** (`X(0)-42`), 모형 미터가 아니라.
  축척이 바뀌면 모형 오프셋은 배로 늘거나 준다.
- **축척이 표현을 정한다** — 1:100 은 벽 이중선·실명·문 열림, 1:500 은 먹칠·레벨.
- **면적은 그린 사각형에서 계산해 찍는다.** 손으로 적으면 도면을 고친 날부터
  숫자만 옛말을 한다.
- **도면은 다크 모드에서도 흰 종이다.** 검은 바탕 도면은 실무에 없다.

### 한 줄로

**도면은 오류를 안 내고 틀린다 — 네 장을 눈으로 보기 전에는 통과가 아니다.**

## M-54. 도로가 도면과 3D 에서 서로 다른 변에 붙어 있었다

(M-44 에서 옮김 — 번호 충돌 정리, 2026-08-25)

〈2026-08-25 · 사장님 지시로 차량을 넣다가 발견〉

### 경위 (핵심 포인트)

3D 도로 위에 차량을 놓았는데 **차가 허공에 떴다.** 좌표를 따라가 보니
설계도면 A-101 은 25m 도로를 **남측(y=100 변)** 에 그렸고, 3D 는 같은 도로를
**y=0 변**에 두고 있었다. **둘 다 이름은 「남측도로」였다.**

- 이름이 같아서 **눈으로는 안 잡힌다.** 도면과 3D 를 나란히 놓고 봐도 둘 다
  「남쪽에 25m 도로가 있는 그림」으로 보인다.
- 잡힌 것은 **차량 좌표가 안 맞아서**였다. 차를 안 넣었으면 그대로 갔다.
- 가로수·가로등도 같은 변을 따라가고 있었으므로 **셋이 함께 틀려 있었다.**

### 막는 법

- **방위는 이름이 아니라 좌표로 맞춘다.** 「남측」이라고 적는 것으로는 아무것도
  보장되지 않는다. 도면의 북쪽 화살표와 모델의 축을 **한 번은 대조한다.**
- **도로·가로수·차량은 한 정의(`ROADS`)에서 나오게 한다.** 이번에는 그 덕에
  한 줄을 고치니 배치도·3D·조감도가 함께 따라왔다.
- **차량은 관례 표기다** — 계획 대수가 아니고 주차 산정과 무관하다고 화면에 적는다.
  적지 않으면 「주차 12대 계획」으로 읽힌다.

### 곁가지 — 이날 함께 정한 것

투자자에게 가기 전 사업을 거르는 **16:9 팩** 규격을 CLAUDE.md §6-1-2 에 넣었다.
핵심은 **자동 / 반자동 / 사람을 색으로 갈라 적고, 등급을 「지금 배선된 커넥터」
기준으로만 적는 것**이다. 회색(사람) 칸이 곧 실사 항목이 된다.

### 한 줄로

**같은 이름이 붙어 있으면 틀린 것도 맞아 보인다 — 방위는 좌표로 대조한다.**

## M-55. 법규 판정이 내 손에만 있었다 — 그리고 열쇠 이름이 또 갈렸다

(M-45 에서 옮김 — 번호 충돌 정리, 2026-08-25)

〈2026-08-25 사장님 지시: 「③ 법규 검토가 해줘」 · 「OPEN API 신청 완료」〉

### 경위 (핵심 포인트)

**① 판정이 재현되지 않는 자리에 있었다.** 법규 검토를 손으로 해서 슬라이드에
박았다. 열 항목이 조문까지 붙어 그럴듯했는데, **딜이 바뀌면 처음부터 다시 해야
하고 같은 답이 나온다는 보장이 없었다.** 이 저장소가 다른 모든 값에 대해서는
「손으로 적지 않는다」를 지키면서, **법정 한도만 사람의 기억**이었다.

**② 열쇠 이름이 또 갈렸다.** 사장님 Secrets 화면을 보니 `LAW_OC`(한 달 전)와
`LAW_OPEN_DATA`(15분 전)가 **둘 다** 있었다. 나는 `LAW_OC` 로 안내했고, 사장님은
신청 후 받으신 값을 다른 이름으로도 넣으셨다. **엔진은 한 이름만 봤다** —
그대로 뒀으면 넣으신 값이 조용히 죽고, 스모크는 「미설정」이라고 말하고,
사장님은 넣으셨다고 아신다. **M-40 과 글자 그대로 같은 사고다.**

★ 같은 화면에 `WORLDWIDE_NEWS` · `WORLD_NEWS_API` 도 있었다 — 이름이 둘인 것이
이 저장소에서 **되풀이되는 패턴**이라는 뜻이다.

### 막는 법

- **판정 규칙은 코드로 내린다.** `core/codecheck.js` 가 건축 개요를 받아
  열 항목을 판정하고, 시험이 **손으로 낸 답과 같은지** 고정한다.
- **모르는 것을 적합으로 세지 않는다.** 판정에 `unknown` 을 두고, `unknown` 이
  하나라도 있으면 `clear:false` 다. 「확인되지 않음」을 통과로 세지 않는다.
- **기준값과 근거는 다르다.** 코드에 박힌 숫자(1,500㎡ 등)는 사람이 옮겨 적은
  것이라 그 자체로 출처가 아니다. `sourced:false` 로 나가고, LAW_OC 가 붙어
  원문이 실려야 `true` 가 된다. **근거 있는 척하지 않는다.**
- **열쇠는 이름을 여러 개 읽는다.** 다시 넣으시라고 하는 대신 `OC_NAMES` 로
  둘 다 읽고, `usedName()` 이 **어느 이름이 쓰였는지** 말한다.
  ★★ 새 열쇠를 안내할 때는 **이름을 하나로 못 박고**, 사장님이 다른 이름으로
  넣으셨을 가능성을 **스모크가 화면에서 갈라 주게** 한다.

### 한 줄로

**손으로 낸 판정은 다음 딜에서 사라지고, 이름이 갈린 열쇠는 넣어도 죽는다.**

---

## M-40. Secrets 에 넣은 열쇠 열하나가 NAS 로 안 가고 있었다

〈2026-08-25 · 사장님이 한전 열쇠를 확보하신 그날 발견〉

### 무엇이 있었나

사장님이 `KEPCO_BIGDATA_KEY` 를 받아 오셨다. 넣을 자리를 확인하려고 배포
워크플로를 훑다가 **엔진이 아는 열쇠와 배포가 싣는 열쇠가 다르다**는 것을 봤다.

```
엔진이 아는 열쇠  22  (커넥터 공통 모듈의 마스킹 목록)
배포가 싣는 열쇠  11  (deploy-nas.yml 의 NAMES)
```

안 실리던 열하나에 **한전 열쇠가 들어 있었다.** 그대로 넣으셨으면 이렇게 된다:

- 사장님은 **넣으셨다.** GitHub Secrets 화면에 이름이 보인다.
- 엔진은 **「키가 없다」**고 한다. NAS 의 열쇠 파일에 그 줄이 없다.
- 배포는 **초록으로 끝난다.** 안 싣는 것을 실패로 세지 않으니까.

**셋 다 맞는 말이라 겉돈다.** M-25(반영이 안 된다)·D-87(화면은 새 판, 서버는
옛 판)과 같은 결이다 — **한쪽만 보면 아무 이상이 없다.**

### 왜 이렇게 됐나

열쇠는 두 곳에 이름을 적어야 한다. 커넥터를 붙일 때는 마스킹 목록(§4.1)만
챙기게 되고, **배포 목록은 다른 파일이라 따라오지 않는다.** 커넥터를 붙인
날과 열쇠를 실제로 받는 날이 몇 주 떨어져 있어서 더 안 보인다.

### 막는 장치

`secrets.test.js` 에 **두 목록이 같아야 한다**는 검사를 넣었다. 커넥터를
붙이면서 배포 목록을 안 고치면 그 자리에서 빨개진다 — 열쇠를 실제로 받는
몇 주 뒤가 아니라.

### 곁가지로 잡힌 것 — 「켜짐」이 켜진 것이 아니었다

같은 배포에서 **Gemini 열쇠가 401 로 거절당하고 있었다.** 열쇠 파일에는
들어 있어서 로그는 「OCR 켜짐」으로 나왔다.

★ 그러니 **「들어 있다」와 「받아들여진다」는 다른 사실이다.** 배포의
`--live` 확인이 이것을 잡았다. 그 확인이 없으면 **자료를 올리는 그 순간에야**
실패하고, 그때 화면은 「읽었습니다」와 구분되지 않는다 (M-34 와 같은 결).

★★ **모양으로 판정하려다 틀렸다** 〈같은 날 · 내가 낸 오진〉. 죽은 값이
371자에 `AQ.` 로 시작하기에 「API 키는 `AIza` 로 시작하는 39자이니 이건
접속 토큰이다」라고 사장님께 말씀드렸다. **틀렸다** — 새로 받아 넣으신,
지금 살아 있는 열쇠도 **`AQ.` 로 시작하는 53자**다. 갈린 것은 앞머리가
아니라 길이였고, 그조차 뒤늦게 안 것이다.
- ★ **열쇠가 사는지 죽었는지는 모양으로 못 가른다.** 발급 형식은 제공자가
  말없이 바꾼다. 물어보는 것 하나뿐이다.
- ★ 그리고 **틀린 모양 진단은 값이 음수다.** 사장님이 멀쩡한 열쇠를 「모양이
  틀렸다」고 버리실 수 있었다. 진단이 없느니만 못하다.

### 한 줄로

**열쇠는 넣는 곳과 실리는 곳이 다르다 — 두 목록이 같은지 검사가 지킨다.**

---

## M-41. 진단이 틀렸다 — 읽고 있던 저장소가 옛 판이었다

〈2026-08-25 · 같은 일이 이 세션에서만 세 번〉

### 무엇이 있었나

사장님이 앱 화면의 빨간 띠(「화면에 필요한 파일을 못 받았습니다: embed-bridge.js」)를
보내 주셨다. 원인을 찾으려고 `report-flow.html` 을 읽었더니 형제 스크립트를
**옛 판 표시**로 부르고 있었다. 「지문이 어긋났다」로 결론을 낼 뻔했다.

**그게 아니었다.** 이 작업 컨테이너의 로컬 저장소가 **열닷새 전 커밋으로
되감겨 있었다.** 원격은 멀쩡했다. 내가 읽은 것은 저장소가 아니라 **옛 사본**이다.

### 왜 안 보였나

- `git status` 는 **깨끗하다.** 되감긴 것은 변경이 아니다.
- 파일은 다 있고 문법도 맞다. **읽히는 데 아무 문제가 없다.**
- 눈치챈 계기는 엉뚱한 곳이었다 — `guard` 가 갑자기 **여덟 칸이 아니라 네 칸**만
  돌고 테스트 수가 **1824 → 1749** 로 줄었다. 최근에 넣은 검사들이 통째로
  없었기 때문이다. **세는 것이 줄어드는 것으로만 알 수 있었다.**

### 막는 법

★ **파일을 읽어 진단하기 전에 원격과 맞춘다.**

```
git fetch origin <갈래>:refs/remotes/origin/<갈래>
git merge --ff-only refs/remotes/origin/<갈래>
```

- `git merge origin/<갈래>` 만으로는 「not something we can merge」 가 난다 —
  되감긴 컨테이너에는 그 원격 참조 자체가 없을 수 있다.
- **`--ff-only` 를 뺀 병합은 위험하다.** 앞선 세션에서 그냥 병합했다가
  내가 쓴 파일이 **로컬 HEAD 에 없다는 이유로 자동 해소되어** 통째로 사라질
  뻔했다.
- 되감김을 눈으로 잡으려 하지 않는다. **숫자를 본다** — 테스트 수와 `guard`
  칸 수가 지난번보다 줄었으면 그것이 신호다.

### 한 줄로

**진단하기 전에 원격과 맞춘다 — 되감긴 저장소는 조용하고 깨끗해 보인다.**

---

## M-59. 같은 고장을 어떤 화면은 견디고 어떤 화면은 멈췄다 — 서버 주소

〈2026-08-27 · 사장님 화면 캡처 「앱을 클릭하면 엔진화면 전체가 로딩됨」〉

**등급: 심각 (원인이 안 보인다)**

### 무엇이 있었나

앱이 「보고서 생성 입력」을 **통째로**(iframe 이 아니라 페이지 전체로) 열었고,
화면은 이렇게 멈췄다 —

```
접수 정보를 불러오지 못했습니다
API 주소가 설정되지 않았습니다
```

**화면은 정직했다.** 앱이 통째로 열면 다리(`embed-bridge.js`)가 부모에서 설정을
못 받아 서버 주소가 빈다. 화면은 지어내지 않고 멈춘 것이다.

### 진짜 결함은 **화면마다 달랐다는 것**

같은 상황에서 `files.html`·`report-flow.html` 은 **멀쩡히 돌았다.**
그 둘만 `flow-core.js` 의 `resolveApi()` 를 써서 **같은 출처의 기본 주소**로
떨어지고 있었기 때문이다. `intake`·`fields` 는 `C.api` 만 보고 있었다.

★★ `flow-core.js` 주석이 그 자리를 **미리 경고하고 있었다** —
「화면마다 따로 짐작하면 어느 화면은 되고 어느 화면은 안 된다가 되어
**원인이 안 보인다**」. 실제로 그 상태였고, 그래서 「앱이 통째로 여는 것」이
원인인데 화면 하나의 고장처럼 보였다.

### 고친 것

- 읽는 화면 다섯(`intake`·`fields`·`files`·`report-flow`·`outputs`)이 **모두**
  `resolveApi()` 한 곳에서 주소를 받는다.
- **짐작으로 불렀으면 문장에 붙인다.** 실측에서 이 자리가 「Failed to fetch」
  한 줄로 끝났다 — 읽는 사람은 서버가 죽은 줄 안다. 이제 이렇게 나온다:

```
Failed to fetch (앱이 서버 주소를 넘기지 않아 /api/linkpilot 로 불렀습니다
 — 단독으로 열렸습니다 (부모 없음))
```

- ★★★ **`reports.html` 만은 일부러 안 짐작한다.** 그 화면은 산출물 **파일
  링크**를 만든다 — 짐작한 주소로 링크를 만들면 **누르면 엉뚱한 곳으로 간다.**
  이것은 내가 처음에 함께 고쳤다가 **검사(`open-file.test.js`)가 잡아 되돌린**
  것이다. 「한 곳에서」가 「모두 똑같이」를 뜻하지는 않는다.

### 한 줄로

**같은 고장을 어떤 화면은 견디면, 고장이 아니라 그 차이가 원인을 숨긴다.**

---
## M-58. 잠깐 나는 고장을 「내가 뭘 잘못했나」로 보이게 했다 — HTTP 502

〈2026-08-27 · 사장님 화면 캡처 · 「2번이나 올려야 하는 번거로움」〉

**등급: 심각 (사람의 시간을 먹는다)**

### 무엇이 있었나

「보고서 만들기 → 관련자료 업로드」에서 —

```
서버가 읽는 중 …  지난 13.6초 · 14번 쟀습니다
올리지 못했습니다        진행률을 알 수 없습니다
HTTP 502
```

그리고 사장님은 **같은 자료를 처음부터 다시 골라 다시 올리셨다.**

### 무엇이 틀렸나 — 고장이 아니라 **안내**가 틀렸다

`502` 는 **앞단(역프록시)이 엔진에 못 붙었다**는 뜻이다. 보낸 파일이 크거나
틀려서가 아니다. 그런데 화면은 `401` 은 정성껏 갈라 설명하면서 **5xx 는
「HTTP 502」 한 줄로 끝냈다.** 그러면 읽는 사람이 할 수 있는 일은 하나뿐이다 —
**처음부터 다시 하기.**

★ **잰 것**: 이 시각(07:48 KST)은 우리 배포의 엔진 재시작 창(07:30:44~07:30:58)과
**겹치지 않는다.** 배포 탓으로 돌리지 않는다 — 원인은 아직 모른다.
**모르는 것을 아는 척 적지 않는 것**이 이 항목의 절반이다.

### 고친 것

- 5xx 를 **일시적**이라고 말한다 — 「보내신 파일 때문이 아닙니다」를 먼저 적는다.
- **고른 파일을 그대로 둔 채 [다시 보내기]** 를 그 자리에 둔다. 다시 고를 필요가 없다.
- **자동으로 다시 보내지 않는다.** 502 는 엔진이 이미 받아서 읽다가 끊긴 것일
  수도 있다 — 자동 재시도는 **같은 자료를 두 벌** 만든다. 누르는 것은 사람이 정한다.
- 세 번을 눌러도 같으면 **무엇을 전하면 되는지** 한 줄로 적는다.

### 규칙 — 되풀이되는 고장은 「다시 하기」를 사람에게 떠넘기지 않는다

- **일시적인 것과 내 잘못인 것을 화면이 갈라 준다.** 안 가르면 사용자는 늘
  자기 쪽을 의심하고, 가장 비싼 길(처음부터 다시)을 고른다.
- **다시 할 재료를 화면이 들고 있는다.** 고른 파일·입력값을 지우지 않는다.
- **자동 재시도는 「두 번 해도 안전한 일」에만.** 만들어지는 것(프로젝트·자료)이
  있으면 사람이 누른다.

### 한 줄로

**고장의 이름만 적으면, 사람은 처음부터 다시 한다.**

---
## M-57. 화면이 멀쩡히 뜨는데 글자만 틀렸다 — `[object Object]`

〈2026-08-27 · 사장님 화면 캡처〉

**등급: 심각 (조용히 틀린다)**

### 무엇이 있었나

앱 「오늘의 브리핑」의 이동 줄에 이렇게 찍혔다 —

```
선정릉 → [object Object]
```

목적지가 **덩어리(객체)**인데 그것을 문장에 그대로 이었다. 값이 없어서가 아니라
**값을 문장에 넣는 코드가 틀려서** 난다.

### 왜 우리 검사가 못 잡았나

**오류가 안 나기 때문이다.** 화면은 멀쩡히 뜨고, 스크립트도 안 죽고, 글자 수도
넉넉하다. 우리 교차검증의 「헤드리스 렌더」 칸은 **① 거의 빈 화면인가 ② 고장
딱지가 붙었는가** 둘만 봤다 — 이 결은 **둘 다 아니면서 틀린 자리**다.

M-05(옛말 하는 화면)·M-25(반영이 안 된다)와 같은 병이다: **한쪽만 보면 아무
이상이 없다.**

### 막는 장치 (2026-08-27 넣음)

`tools/guard.js` 의 헤드리스 렌더 칸이 **그려진 글자**를 보고 셋을 잡는다 —
`[object Object]` 류 · `undefined` · `NaN` (낱말 경계로 본다). 하나라도 보이면
**빨갛게 끝난다.** `test/junk-text.test.js` 가 그 눈이 실제로 잡는지, 그리고
**멀쩡한 글자를 안 잡는지**(늑대야가 되면 꺼진다) 함께 잰다.

### 한 줄로

**오류를 안 내는 고장은 「떴는가」로 못 잡는다 — 그려진 글자를 읽어야 잡힌다.**

---
## M-56. 잘 설명하는 고장은 고장이다 — 같은 빨간 띠가 네 번 났다

**등급: 심각 (고쳤다고 믿는 채로 사용자는 계속 막힌다)**

### 무슨 일이 났나 (2026-08-26 · **네 번**)

사장님 앱 화면에 빨간 띠가 떴다 —
`화면에 필요한 파일을 못 받았습니다: /im-flow/im-flow/embed-bridge.js`.
경로에 **`im-flow` 가 두 번** 들어 있다.

**네 번 났고, 앞의 세 번을 나는 「고쳤다」고 보고했다.**

| 판 | 내가 한 것 | 사장님이 본 것 |
|---|---|---|
| 1 | 띠에 **경로**를 붙였다 (이름만으로는 못 갈랐으므로) | 같은 빨간 띠 |
| 2 | 띠에 **「두 번 들어 있습니다」**를 붙였다 | 같은 빨간 띠 |
| 3 | 겹친 토막을 지워 **화면이 뜨게 했다** | 다른 꼴로 또 빨간 띠 |
| 4 | **어디에 얹히든 자리를 정하게 했다** | 화면이 뜬다 |

★★★★ **3판이 왜 또 틀렸나 — 한 증상만 보고 고쳤다.**
앱이 화면을 얹는 자리가 **두 가지로** 틀렸는데 나는 하나만 봤다.

```
겹침  /im-flow/im-flow/embed-bridge.js   앞을 두 번 붙인다   ← 3판이 고침
빠짐  /embed-bridge.js                   앞을 아예 안 붙인다  ← 3판이 못 봄
```

**증상을 고치면 남은 증상이 다음 주에 온다.** 4판은 「무엇이 틀렸나」를
고치지 않고 **「어디가 맞나」를 정한다** — 형제 파일은 늘 한 폴더에 함께 있으므로
① 앱이 알려 준 자리 → ② 주소 안의 `im-flow` 첫 번째 → ③ 뿌리면 `/im-flow/`
순으로 **그 폴더를 찾는다.** 두 증상이 같은 규칙에서 함께 풀린다.

★ ①(`<meta name="lp:base">`)을 둔 이유: 앱이 다른 자리에 얹게 되는 날
**코드를 안 고치고** 알려 줄 길을 남긴다. 없으면 그날 또 이 파일을 고친다.

### 왜 두 번이나 헛다리를 짚었나

**재는 것을 틀렸다.** 나는 「띠에 좋은 글이 있는가」를 쟀다.
사장님이 겪는 것은 **「화면이 뜨는가」**다. 둘은 다른 사실이다.

★★★ **진단은 고침이 아니다.** 잘 설명하는 고장도 고장이다.
오류 메시지를 좋게 만드는 일은 **고치는 일의 준비**이지 고치는 일이 아니다.
보고할 때 그 둘을 같은 말로 적으면, 나는 고쳤다고 믿고 사장님은 계속 막히신다.

### 왜 「실패한 뒤에 다시 부르기」로도 안 됐나

두 번째 시도로 **실패한 파일을 겹친 토막만 지워 다시 부르게** 해 봤다.
파일은 **17개 다 받았다.** 그런데 **버튼이 5개에서 0개**가 되었다.

**이미 초기화가 끝난 뒤였다.** 늦게 도착한 스크립트는 화면을 못 그린다.
즉 **「받았다」와 「그려진다」도 다른 사실**이다.

### 무엇이 실제 고침이었나

**아무것도 받기 전에** 자리를 바로잡는다. 화면 맨 위 인라인 조각이
`location.pathname` 에 같은 토막이 두 번 있으면 **한 겹을 지운 `<base>`** 를
세운다. 그러면 아래의 상대 경로들이 **처음부터 옳은 자리**를 본다 —
실패가 아예 안 난다.

실측 (진짜 서버 + 헤드리스, 같은 파일 다섯 자리에서):

```
멀쩡한 자리     | 버튼 5 | 자리 그대로                     | 띠 없음
겹친 자리       | 버튼 5 | 바로잡음(주소에서 찾음)         | 띠 없음
뿌리 자리       | 버튼 5 | 바로잡음(뿌리에서 열려 폴더 붙임) | 띠 없음
앱이 알려 준 자리 | 버튼 5 | 바로잡음(앱이 알려 줌)          | 띠 없음
파일로 열기     | 버튼 5 | 자리 그대로                     | 띠 없음
```

★★ **`file:` 로는 이것을 못 잰다.** 장치가 http(s) 일 때만 돌기 때문이다
(미리보기를 건드리면 멀쩡한 화면이 깨진다). 검사도 **진짜 서버를 띄워** 잰다.

★★★ 그 서버를 **같은 프로세스에 두면 검사가 멈춘다.** 브라우저를
`execFileSync` 로 부르면 Node 가 멈추고, 그동안 서버가 답을 못 한다 —
서로 기다리다 끝나지 않는다(실측: 10분 초과로 죽었다). **딴 프로세스로 띄운다.**

★ **근본은 여전히 앱 쪽이다** — 앞부분을 덧붙이는 것은 저쪽이다.
경계(CLAUDE.md §8-2)를 지키느라 저쪽에 쓰지 않으므로, 화면은 살리되
`console.warn` 과 `data-lp-base` 표시로 **흔적을 남긴다.**
빨간 띠는 안 띄운다 — 멀쩡히 도는 화면을 고장으로 읽으시게 되기 때문이다.

### M-02 와 어떻게 이어지나

M-02 는 **「고쳤다 ≠ 반영됐다」**, M-29 는 **「반영됐다 ≠ 사용자가 보고 있다」**,
여기 M-56 은 **「설명했다 ≠ 고쳤다」**다. 셋 다 뿌리가 같다 —
**내가 잰 것과 사장님이 겪는 것이 다른 사실인데, 같은 말로 보고했다.**

★ M-02 의 **「상태 변경 시 단일 출처 `updatedAt` 필수」 규칙**과
**상태 토글 검증 체크리스트**는 그대로 유효하다. 이번 사고로 바뀐 것은 없고,
아래 한 칸을 그 체크리스트 정신에 맞춰 **모든 고침에** 넓힌다.

### 막는 장치

- `im-agent/test/dup-path-heal.test.js` — **헤드리스로 화면을 실제로 띄워
  버튼 수를 센다.** 띠의 글이 아니라 화면이 뜨는지를 잰다.
- 그 검사는 **장치를 빼면 빨개진다** (`createElement('base')` 를 무력화하면
  버튼이 줄어드는 것을 확인한다). 통과가 아니라 실패를 잰다.

### 앞으로 — 고침 보고 체크리스트

「고쳤습니다」라고 적기 전에 **전부** 확인한다.

- [ ] **사용자가 겪는 것을 쟀는가** — 오류 메시지가 아니라 **화면이 뜨는가**
- [ ] **고치기 전에 재현했는가** — 재현 못 한 고침은 고친 것이 아니다
- [ ] **막는 장치를 빼고 돌려 빨개지는가** — 통과만 재는 검사는 아무것도 안 잰다
- [ ] **진단만 좋게 한 것은 아닌가** — 메시지 개선을 고침으로 보고하지 않는다
- [ ] **조용히 고쳤다면 흔적을 남겼는가** — 근본 원인이 남의 자리에 있어도
      그 사실이 어딘가에 남아야 언젠가 고쳐진다
- [ ] **같은 고장이 두 번째라면** — 앞 판이 왜 안 통했는지부터 적는다.
      안 적으면 세 번째에 같은 자리를 또 고친다

### 한 줄로

**오류 메시지를 좋게 만든 것은 고친 것이 아니다 — 사용자가 겪는 것을 재라.**
**그리고 증상 하나를 고치지 말고, 맞는 자리를 정하라.**

### 곁가지 — 옛 검사가 더 나은 고침을 막았다

`dup-path.test.js` 가 띠의 **문구**(「새로고침으로는 안 없어집니다」)를 고정하고
있었다. 그 문장은 **고장이 났다는 전제**의 말이라, 고장이 아예 안 나게 고치자
**검사가 빨개졌다 — 코드는 나아졌는데.**

★ **검사는 문구가 아니라 뜻을 재야 한다.** 「겹친 경로에 새로고침을 권하지
않는다」로 바꿨다. 문구를 고정하는 검사는 다음 개선을 막는 자물쇠가 된다.

## M-60. 재려고 넣은 단계가 재기를 막았다 — `set +e`

〈2026-08-27 · 배포 run #143 · 내가 낸 사고〉

**등급: 심각 (초록도 빨강도 아닌 「안 잰 배포」가 된다)**

### 무엇이 있었나

D-150 으로 「화면 캐시가 켜졌는지」를 재는 단계를 넣었다. `Upload` **바로 뒤**에
두고 `set -uo pipefail` 만 켰다. 그 단계가 **실패로 끝났고**, 뒤따르던

- `Verify deployed` (저장소 = 디스크 = HTTP 인가)
- `Check via app path` (앱이 쓰는 길로 받은 것이 저장소와 같은가)

가 **전부 skipped** 됐다. 파일은 올라갔는데 **닿았는지를 아무도 안 잰 배포**가
됐다. 재려고 넣은 것이 재기를 막았다.

### 왜 죽었나

이 저장소의 워크플로는 **`bash -e`** 로 돈다 (`shell: /usr/bin/bash -e {0}` —
GitHub 의 기본값이다). `set -uo pipefail` 은 `-e` 를 끄지 않는다. 그래서

```
[ -z "$H" ] && continue
```

이 한 줄이 — `H` 에 값이 **있을 때** — 「거짓」을 돌려주고, `-e` 가 그것을
**실패**로 읽어 단계를 끝냈다. 이 저장소에는 이미 `set +e` 가 열 곳 넘게
적혀 있다. **관례를 안 따른 것이 원인이지 셸이 이상한 것이 아니다.**

### 고친 것 셋

1. `set +e` — 「못 받음」은 여기서 정상 결과다.
2. 마지막에 `exit 0` — **재는 단계는 문턱이 아니다.**
3. **자리를 옮겼다** — `Verify deployed` · `Check via app path` **뒤로**.
   앞에 두면 이 단계가 어떤 이유로든 죽을 때 확인이 또 건너뛰어진다.

### 막는 장치

`im-agent/test/nocache-html.test.js` 가 셋을 다 잰다 — `set +e` 가 있는가,
`exit 0` 으로 끝나는가, **확인 단계보다 뒤에 있는가**.

### 핵심 교훈

- **재는 장치는 재는 것을 막을 수 있다.** 새 단계를 어디에 두는지가
  그 단계의 내용만큼 중요하다 — **확인은 언제나 맨 뒤에 남긴다.**
- **초록·빨강 말고 「안 잼」이라는 세 번째 결과가 있다.** 이번 배포는 빨갛게
  끝났지만 정작 빨간 이유는 캐시가 아니라 **확인이 안 돈 것**이었다.
- 새 셸 단계를 넣을 때는 **`set +e` 부터 적는다.** 이 저장소의 관례다.

## M-61. 안내에 적은 「설명 문구」가 값 칸에 그대로 들어갔다

〈2026-08-27 · 사장님 화면 캡처 · **네 번째**〉

**등급: 심각 (틀린 값이 들어가도 「틀렸다」로 안 보인다)**

### 무엇이 있었나

`LP_PUBLIC_BASE` 를 등록해 달라고 안내하면서 값 자리를 이렇게 적었다 —

「앱이 보고서 화면을 불러오는 주소 (…/im-flow)」

사장님은 GitHub 의 **New secret** 화면을 열고 Name 에 `LP_PUBLIC_BASE` 를 넣은 뒤,
**Secret 칸에 그 문장을 그대로** 붙여 넣으셨다. 누르기 직전에 잡았다.

### 왜 또 났나

CLAUDE.md §5 에 **「붙여 넣을 명령에 「바꿔 넣을 자리」를 두지 않는다」**가 이미
적혀 있다. 그런데 그 규칙을 **명령(터미널)에만** 적용하고 있었다. 값을 받는
**웹 화면의 입력 칸**도 똑같은 자리다 — 오히려 더 위험하다:

- 터미널은 틀리면 오류 문구라도 나온다.
- Secret 칸은 **아무 말 없이 저장된다.** 그리고 그 값을 쓰는 배포는
  17개가 전부 「앱이 쓰는 길로 받은 것이 다르다」로 빨개진다 — 그 로그만 보면
  **주소가 틀렸다는 생각이 안 들고 배포가 깨진 줄 안다.**

### 고친 것 셋

1. **배포가 값의 생김새를 본다.** `http://`·`https://` 로 시작하지 않거나 한글이
   섞여 있으면 **재기 전에** 「주소가 아니다」라고 사람 말로 적고 건너뛴다.
   빨갛게 죽지 않는다. 값은 **한 글자도 안 찍는다** — 길이만 적는다 (§2).
   (`deploy/screens.sh` 가 `LP_NAS_HOST` 에 대해 하던 것과 같은 장치다.)
2. **안내를 「찾는 법」부터** 준다. 값이 무엇인지 설명하기 전에 **어디를 눌러
   어떻게 얻는지**를 먼저 적는다 (앱 화면에서 오른쪽 단추 → 프레임 주소 복사).
3. 그 안내 문서 맨 위에 **이 사고를 그대로 적어 두었다** — 다음 사람이 같은
   문장을 다시 쓰지 않게.

### 막는 장치

`im-agent/test/nocache-html.test.js` — 배포가 ① 주소 꼴을 보는가 ② 한글을
잡는가 ③ 값을 안 찍는가 ④ `set +e` 인가를 잰다.

### 핵심 교훈

- **§5 의 「바꿔 넣을 자리」 규칙은 터미널만의 규칙이 아니다.** 값을 받는
  화면의 입력 칸에도 그대로 적용한다.
- **값이 틀렸을 때 무엇이 보이는지**까지 설계한다. 「17개가 빨갛다」는
  「주소가 틀렸다」를 못 알려 준다.
- 사람이 넣는 값을 받는 자리는 **받는 쪽이 생김새를 검사한다.** 안내를 잘
  쓰는 것으로 될 일이 아니다 (M-31 과 같은 결).

## M-62. 재는 장치가 배포를 세워, 하루치 일이 안 올라갔다

〈2026-08-27 · 배포 #147 · #148 · #149 · #150 — **넷 연속 실패**〉

**등급: 심각 (초록도 빨강도 아닌 「안 올라감」이 된다)**

### 무엇이 있었나

사장님께 「배포가 도는 중입니다」라고 말씀드린 사이에 **배포 넷이 빨갛게
끝나 있었다.** 죽은 자리는 전부 **재는 단계**였다:

| 배포 | 죽은 자리 | 무엇이 안 올라갔나 |
|---|---|---|
| #147 · #148 | `Check via app path` | 열쇠 파일 · **엔진** · 라우트 확인 (뒤 단계 전부 skipped) |
| #149 | guard — 결정기록의 건수 어긋남 | **아무것도** (deploy 작업이 시작조차 안 함) |
| #150 | guard — 느린 러너에서만 나는 화면 검사 | **아무것도** |

★ #148 은 `Upload` 도 `Verify deployed` 도 **초록**이었다. 화면은 닿았는데
**그 뒤가 통째로 건너뛰어져** 엔진이 안 올라갔다.

### 왜 났나 — 셋 다 같은 뿌리

**문턱이 여러 개였다.** 「재는 것」과 「막는 것」을 안 갈랐다.

- `Check via app path` 가 다르면 `exit "$BAD"` 로 끝났다. 그런데 **거기서 다른
  것은 이 배포가 못 고친다** — 앱 사본은 사람이 올리고(D-143), 앱이 다른
  폴더를 보면 앱 쪽 설정이다. 배포를 세워도 아무것도 나아지지 않는다.
- guard 의 화면 검사는 **4초** 예산이었다. 내 자리에서는 7/7 초록인데 느린
  러너에서는 멀쩡한 쪽이 다 그려지기 전에 시간이 끝나 뒤집혔다.

### 고친 것

1. **문턱은 하나다** — `Verify deployed`(저장소=디스크=HTTP). 나머지는 적기만 한다.
2. `Check via app path` → 경고 + 요약(몇 개 다른지·무슨 뜻인지) 후 `exit 0`.
3. 느린 자리에서 나는 빨강은 **문턱을 무르게 하지 말고 시간을 늘린다**
   (화면 렌더 예산 4초 → 12초). 재는 것은 그대로 둔다.
4. 옛 규칙을 박아 둔 검사(`workflow-default.test.js` 의 「빨갛게 끝나야 한다」)를
   **뜻에 맞게** 고쳤다 — 검사는 문구가 아니라 뜻을 재야 한다.

### 막는 장치

`im-agent/test/nocache-html.test.js` — 배포의 재는 단계가 `set +e` 이고 `exit 0`
으로 끝나며 **확인 단계보다 뒤**에 있는지 잰다 (M-60 과 같은 검사에 한 칸 더).

### 핵심 교훈

- **문턱을 늘리면 배포가 멈추는 날이 늘어난다.** 재는 장치는 재기만 한다.
- **「이 배포가 고칠 수 있는 것」만 문턱이 된다.** 사람이 해야 할 일(사본 올리기)로
  배포를 세우면, 사람이 그 일을 할 때까지 **다른 모든 것이 함께 멈춘다.**
- **내 자리에서 초록인 것은 CI 에서 초록이라는 뜻이 아니다.** 시간에 기대는
  검사는 특히 그렇다 — 느려서 나는 빨강은 「고장」이 아니라 「예산 부족」이다.
- ★ 그리고 **걸어 놓고 결과를 안 보면 이 모든 것이 안 보인다.** 오늘 두 번
  「도는 중」이라고 말하고 결과를 확인하지 않았다 (CLAUDE.md §8 이 적어 둔 자리).

### 앞서 있던 규칙과의 관계

「상태 변경 시 단일 출처 `updatedAt` 필수」와 **상태 토글 검증 체크리스트**는
**M-02** 에 이미 있다 (이 파일 §M-02). 이번 사고는 그 결의 다른 얼굴이다 —
값은 바뀌었는데 **그것을 나르는 길이 멈춰** 아무 데도 안 닿았다.

## M-63. 이름을 베껴 적어, 화면만 옛말을 했다

〈2026-08-28 · 절을 넷으로 다시 짜면서 (D-157)〉

**등급: 보통 (오류가 안 나고 화면만 틀린다)**

### 무엇이 있었나

레일의 마지막 칸 이름을 「생성」에서 **「완성 보고서」**로 바꿨다. 그런데
`reports.html` 은 그 이름 여섯을 **손으로 적어** 두고 있었다. 그래서 그 화면만
옛 이름을 말했고, **아무 오류도 안 났다.**

`live-core.js`(진행률)도 같은 이름을 따로 갖고 있어 **셋이 서로 다른 이름**을
쓸 뻔했다.

### 왜 비싼가

이름은 **틀려도 화면이 뜬다.** 사용자는 「생성」과 「완성 보고서」가 같은 자리인지
다른 자리인지 화면만 봐서는 모른다. 색(`--lime-deep`)에서 한 번, 탭 이름에서 한 번,
이번이 **세 번째**다.

### 고친 것

1. `reports.html` 이 이름을 **읽어 온다** — `flow-core.js` 의 `STEPS` 하나가 정본이다.
2. `live-core.js` 의 이름을 레일과 **맞췄다.** 한 칸만 덮는 것은 이름이 같아야 한다.
3. ★ **검사를 바꿨다.** 앞 판은 「화면에 적힌 배열의 글자가 정본과 같은가」를 쟀다.
   그 검사는 **베껴 적는 것을 전제로** 하고 있었다 — 즉 사본을 지키는 검사였다.
   이제 **「읽어 오는가」**를 잰다. 읽어 오면 갈릴 수가 없고, 다음에 이름을 바꿔도
   검사를 고칠 일이 없다.

### 핵심 교훈

- **사본을 지키는 검사는 사본을 없애지 못한다.** 글자를 맞추는 검사는 「두 벌이
  있다」를 전제한다 — 한 벌로 만들면 그 검사는 필요 없어진다.
- 이름·목록·색처럼 **여러 화면이 함께 쓰는 것**은 언제나 한 곳에서 읽어 온다.
- 관련: **M-02**(상태를 바꾸면서 갱신시각을 같이 안 바꿨다)의 「단일 출처」와 같은
  규칙이다. 그쪽은 **값**, 이쪽은 **이름**이다.

## M-64. 읽지 못한 파일을 지웠다 — 얻은 것은 없고 파일만 사라졌다

〈2026-08-28 · 사장님 화면 실측 · D-173〉

**등급: 심각 (사용자의 자료가 없어진다 · 오류는 안 난다)**

### 무엇이 있었나

사장님이 자료 **13개**를 올리셨다. **9개를 못 읽었다.** 사유는 전부 하나였다 —
`GEMINI_ALL_KEYS_UNAVAILABLE`. 열쇠 둘(`#2EED` · `#CD7C`)이 **둘 다 429**
(요청 과다)로 **600초 쉼**에 들어가 있었다. 600초는 쉼 사다리의 **마지막 칸**이다
— 연달아 네 번 이상 막혔다는 뜻이다.

그리고 화면 마지막 줄이 이렇게 적혀 있었다:

> 「파일업로드 (1회성)」는 읽고 나서 원본을 지웁니다 — 이 9건은 남아 있지 않습니다.

**그 말이 사실이었다.** `oneshotUpload` 가 읽기 성공·실패를 **안 가리고**
임시 폴더를 통째로 지웠다.

### 왜 비싼가

- **얻은 것이 없는데 잃은 것은 있다.** 실패는 사용자 잘못이 아니라 **시스템의
  열쇠가 고갈된 것**인데, 그 대가를 사용자가 **파일을 처음부터 다시 올려서** 치른다.
- **오류가 안 난다.** 업로드는 `ok:true` 로 끝나고 화면도 정상으로 뜬다.
  「9건 못 읽음」은 사유 표에 조용히 적힐 뿐이다.
- **화면이 되레 확신을 준다.** 「남아 있지 않습니다」라고 **정확히** 알려 주므로
  사용자는 그것이 **설계된 동작**이라고 읽는다. 사고로 안 보인다.

### 왜 이런 코드가 되었나

「1회성 = 안 남긴다」를 **파일 단위가 아니라 요청 단위**로 구현했기 때문이다.
`dispose()` 는 폴더를 지우는 함수이지 「다 읽었으니 지운다」는 함수가 아니다.
**사생활 약속**(안 남긴다)과 **작업 결과 폐기**를 같은 한 줄이 하고 있었다.

### 고친 것

1. `dispose()` **앞에** 읽기에 실패한 것만 `vault.put()` 으로
   `02_Source_Data`(보관 자료)에 옮긴다. 임시 폴더에 그냥 두는 것으로는 모자란다 —
   거기는 OS 임시 폴더라 **재부팅에 없어지고**, 무엇보다 **다시 읽을 길이 없다.**
   보관으로 옮겨야 화면의 **「다시 읽기」**(`/scan`)가 집는다.
2. 응답에 **`rescued`** 를 담고, 화면이 그것을 보고 **말을 바꾼다** —
   「다시 올리지 마시고 「다시 읽기」를 눌러 주십시오」.
3. **옛 문구는 안 지웠다.** 옮기기까지 실패하면 정말로 안 남으므로 그때는 참이다.
4. 검사 `oneshot-rescue.test.js` 다섯. 「안 지웠다」가 아니라
   **「보관 자료에 실제로 있다」**를 잰다.

### 아직 안 고친 것

**열쇠가 둘뿐이다.** 코드는 여덟(`GEMINI_KEY_01`~`_08`)을 받는데 등록된 것이 둘이다.
이 고침은 **잃지 않게** 할 뿐 **읽히게** 하지 않는다. 열쇠 등록은 사람 손이다.

### 핵심 교훈

- **「보관하지 않는다」와 「버린다」는 다른 일이다.** 한 줄이 둘을 다 하면,
  실패한 날에 그 한 줄은 사용자 자료를 버리는 줄이 된다.
- **성공·실패를 안 가르는 정리(cleanup)를 의심한다.** 정리는 대개 성공 경로를
  보면서 쓰인다.
- **화면이 「없습니다」라고 단언하기 전에 실제로 없는지 본다.** 확신에 찬 옛말은
  침묵보다 나쁘다 (M-05 와 같은 결).
