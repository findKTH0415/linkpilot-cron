> ## ⚠️ 최신 인수인계서는 이 파일이 아니다 (2026-08-16)
>
> 플랫폼에 붙이기 위한 **완전한 인수인계서**는
> [`docs/인수인계서-플랫폼-연동.md`](docs/인수인계서-플랫폼-연동.md) 에 있다.
> 붙이는 곳 3군데 · API 17개 · 반영 체크리스트 · 인수 조건 · 못 하는 것까지 들어 있다.
>
> 이 파일은 그 이전 세션의 기록이다. **지우지 않고 남긴다** — 왜 그렇게 됐는지는
> 여기에만 적혀 있다.

# HANDOVER — 인수인계 · 버그/오류 개선안

작성 2026-08-14 · 갱신 2026-08-16 (KST) · 대상 저장소 `findKTH0415/linkpilot-cron`

---

## 0. 결론 먼저

- **코드는 막힌 곳이 없다. 막힌 것은 전부 저장소 밖이다** — PR 병합, Secret 등록,
  본체 소스, NAS 접근. 넷 다 사람이 해야 한다.
- **테스트 534건 통과.** 새 의존성 0건 (`solapi` 하나).
- **테스트 491건 통과.** 새 의존성 0건 (`solapi` 하나).
- **가장 급한 것 하나** — 아침 크론을 옮길 곳이 정해지지 않았다 (등록부 D-19).
  PR #3 이 병합되는 순간 브리핑·모닝카드·한줄생각이 **멈춘다.**
  지금은 `main` 에 남아 있어 계속 돌고 있다.
- **공공데이터는 2026-08-16 실측으로 전부 연결되었다.** 커넥터 6개(VWorld·
  data.go.kr·기상청 API허브·한국부동산원), 스모크 14/14. 진단 도구가
  `npm run im:smoke` 한 줄로 전 경로를 확인한다.
- **실측으로만 드러나는 계산 오류 3건을 잡았다.** 개별공시지가가 11년 낡은 값
  (30% 오차) · 지역·지구 19건 중 18건 폐기 · 공시지가 시점수정 한 달 과다
  (표본 필지 28억원). 셋 다 **출처 표시는 멀쩡해서 문서만 봐서는 안 잡힌다.**
  원인을 전부 테스트로 고정했다.

---

## 1. 현재 상태

| 항목 | 값 |
|---|---|
| 작업 브랜치 | `claude/linkpilot-agent-github-plan-0af7xs` (= **PR #3**) |
| 최신 커밋 | `7994b3b` data.go.kr 오류코드에 뜻을 붙인다 |
| PR #3 | open · draft · `mergeable_state: clean` · CI green · 158파일 / +35,875 −938 · 74커밋 |
| PR #4 | open · draft · clean · 브랜치 `claude/ops-cron-alerts-and-deploy` (별개) |
| 테스트 | `npm test` → **534 pass / 0 fail** |
| 테스트 | `npm test` → **491 pass / 0 fail** |
| CI | `im-agent-ci.yml`, `alert-ci.yml` (경로 변경 시에만 실행) |

> PR #3 과 PR #4 는 **다른 브랜치**다. 이번 지침 반영 작업은 PR #3 에 들어갔다.
> 운영 알림·NAS 배포는 PR #4 에 있고, 그쪽이 병합되어야 Actions 탭에 배포 버튼이 생긴다.

### 완성도 — 만든 것 86% / 실제 도는 것 0%

범위를 보고서 생성으로 좁힌 뒤 다시 계산했다. 아침 크론(가중 30%)이 빠지면서
나머지를 100%로 재배분했고, 이후 가이드 필드 화면이 더해졌다.

| 영역 | 가중 | 코드 완성 | 운영 반영 | 근거 |
|---|--:|--:|--:|---|
| IM Agent 파이프라인 | 43% | **86%** | **0%** | 11 Agent + core 21모듈, end-to-end 실행 확인. 커넥터 6종(ECOS·DART·인허가·규제 추가). 채움 계획이 키 유무를 반영. **실키 스키마·VWorld 미검증** |
| 플랫폼 화면 | 29% | **93%** | **0%** | 접수·가이드필드·생성 4단계를 앱 섹션(`report-flow.html`)으로 묶었다. 입력란 54→8칸. 인쇄·PDF 경로 연결(B-8). 계보 모달 이식(B-11). 남은 것은 본체 배선뿐 |
| IM Agent 파이프라인 | 43% | **83%** | **0%** | 11 Agent + core 21모듈, end-to-end 실행 확인. 발행 주체 설정화. 채움 계획이 키 유무를 반영. 공공데이터 6종 실측 검증 완료(스모크 14/14). 시장조사 서술은 여전히 LLM 기억 |
| 플랫폼 화면 | 29% | **89%** | **0%** | 접수·가이드필드·결과 화면 API 연결 + 브라우저 조작 확인. 입력란 52→8칸. 보드는 `api: null`, 서버 미배선, 인쇄 경로 미연결 |
| 운영 인프라 (배포·알림·CI) | 14% | **90%** | **0%** | 워크플로 완성, **실행 0회** |
| 외부 지침 정합성 | 14% | **85%** | **0%** | 충돌 4건 해소 + 대조 테스트 17건. 등급 3건 미확정 |
| **종합** | 100% | **88%** | **0%** | |

<details><summary>산정 방식</summary>

- 완전 구현 100 / 부분 구현 50 / 별도 경로로 대체 75 / 미구현 0
- '운영 반영'은 **`origin/main` 에 있고 실제로 도는 것**만 센다. PR 상태는 0
- 가중치는 직전 산정(크론 30 / Agent 30 / 화면 20 / 인프라 10 / 지침 10)에서
  크론을 빼고 나머지를 비율대로 확대했다 (30→43 · 20→29 · 10→14 · 10→14)
- 검증 근거: `git ls-tree origin/main`, `npm test` 534건, 화면 headless 렌더 + 브라우저 조작,
- 검증 근거: `git ls-tree origin/main`, `npm test` 491건, 화면 headless 렌더 + 브라우저 조작,
  데모 파이프라인 1회 실행 후 dataset 대조 (B-18 을 이 방법으로 잡았다)
- 86% → 88% 는 커넥터 3종과 화면 배선(B-7 제외·B-8·B-11)이 더해진 몫이다.
  **틀린 채로 도는 기능은 완성으로 세지 않는다** — 공공데이터 응답을 실키로
  대조하지 못한 만큼은 여전히 깎여 있다 (B-4)

</details>

> **운영 0% 는 계산 착오가 아니다.** `main` 에는 크론밖에 없고, 그 크론은 이제
> 이 저장소의 범위 밖이다. **범위 안에서 지금 도는 것은 하나도 없다.**
> PR #3 병합이 곧 운영 반영률 그 자체다.

### 미결정 16건
### 미결정 19건

전체 목록은 **[`docs/미결정-사항.md`](docs/미결정-사항.md)** 에 ID를 붙여 등록했다.
CLAUDE.md §9 에 "세션 시작 시 읽고 🔴 항목을 먼저 알린다"는 규칙을 걸어 두었다.

🔴 4건 · 🟠 7건 · 🟡 3건 · ⚪ 2건 · 범위 외 5건 · 결정 2건(D-18 · D-15).
🔴 4건 · 🟠 7건 · 🟡 6건 · ⚪ 2건 · 범위 외 5건 · 결정 2건(D-18 · D-25).

**가장 무거운 것은 D-19** — 아침 크론을 옮길 곳이 정해지지 않았다.

### 2026-08-15 세션에서 한 일

1. **외부 업무지침 대조** — 어긋난 4건 수정. `catalog.js`(등급 단일 출처)·
   `inapp.js`(인앱 배너)·`guide.html`·`guide.test.js` 신규
2. **아침 크론 삭제(D-18)** — 저장소를 보고서 생성으로 한정
3. **가이드 필드 입력 화면** — `fields.html`·`fields-core.js`·`GET /fields`·
   `GET·PUT /projects/:id/facts`
4. **보고서 생성 입력 화면** — `intake.html` · 요청문·자료 업로드·발행 주체
5. **결과 화면 미리보기** — 실제 파이프라인 산출물을 읽는 `build-result.js`
6. **발행 주체 설정화** — 회사명이 코드에 박혀 있던 것을 `core/issuer.js` 로
7. **산업분야·판 옵션** — 사전 54항목을 산업·수준으로 줄여 보여준다
8. **입력란 자동 축소** — `core/fieldplan.js` 가 항목마다 누가 채우는지 정한다.
   IM 필수 17개 중 **직접 넣을 것은 8개**, 나머지 9개는 근거와 함께 접어 둔다.
   (~~공공데이터 키가 없는 지금은 9개~~ → 2026-08-16 키·활용신청 완료로 **8개**)
9. **입력 칸 정리** — 한 줄에 늘 4칸(값·출처·p.·출처일)이 떴다. 항목 성격에
   맞게 줄였다: 페이지는 출처가 문서일 때만, 출처일은 값이 시점에 따라 달라지는
   항목만(`def.dated`), **자동으로 채워지는 줄에는 출처를 아예 안 묻는다**
10. **교차검증 4회** — 아래 B-13~B-21 참조. 4회차에서 **자동 축소가 만든 결함 2건**을
   스스로 찾아 고쳤다 (B-18·B-19). 자동으로 채운다고 말해 놓고 안 채우는 쪽이
   많이 묻는 쪽보다 나쁘다는 원칙을, 만든 그날 어겼다
11. **미리보기 4단계 고정형** — 창 안에서 또 끌지 않는다. 1 보고서 생성 입력 →
   2 가이드 필드 → 3 출력 사양 확정 → 4 생성. 각 단계는 내용 높이만큼 펴진다
12. **한국은행 ECOS 커넥터** — 시장금리를 기존 `cache.js`+`http.js` 위에 얹었다.
   ★ 채우는 것은 **기준금리(`debt.benchmark_rate`)이지 차입금리가 아니다** —
   국고채를 `debt.rate` 로 넣으면 IRR 이 통째로 부풀려진다 (등록부 D-22)
13. **연결 승인 반영** — D-15 를 결정 기록으로 옮기고, 남은 연결은 C-01~C-05 로
   작업판에. 승인이 만든 새 미결정 D-23(기준선인가 실측치인가)을 등록했다
14. **`.env` 자동 로딩** — `core/env.js`. 셸·Secrets 값이 파일보다 세다.
   키 이름만 찍고 값은 절대 찍지 않는다
15. **커넥터 3종** — C-02 건축인허가 · C-01 DART 전자공시 · C-05 토지이용계획 규제.
   ★ 셋 다 **값을 채우는 기능이 아니라 대조 기능**이다. 나대지에는 인허가 기록이
   없고 시행사 SPC 는 DART 공시대상이 아니다 — 없는 것을 "미허가"·"법인 미확인"
   으로 적지 않는다. C-05 는 **이미 받아 오던 응답을 버리고 있던 것**을 살렸다
   (개발제한구역에 걸린 땅과 아닌 땅이 '자연녹지지역' 하나로 똑같이 보였다)
16. **B-8 인쇄·PDF 경로** — 본체를 기다리지 않고 API 가 직접 내려준다.
   `rel` 은 목록과 글자 그대로 같을 때만 통과하고, HTML 은 sandbox CSP 로 나간다
17. **B-11 계보 모달** — 순수 JS 판에 이식. 없을 때는 숫자→출처 경로가 막혀 있었다
18. **앱 [보고서 생성] 섹션** — `report-flow.html`. 값 입력을 앱 안으로 들여온다.
   4단계는 `flow-core.js` 하나가 정하고 제품·미리보기가 같은 파일을 읽는다
19. **작업지시 관련 화면·데이터 제거** (사용자 결정) — 보드·작업지시판·`작업지시.json`.
   ★ 「등록부에만 있고 보드에 없는 일」을 잡던 검사가 함께 사라졌다.
   남은 추적 장치는 `docs/미결정-사항.md` 하나다
20. **연동 문서 2종** — [`docs/작업지시서-플랫폼-연동.md`](docs/작업지시서-플랫폼-연동.md)
   (무엇을·어디까지·인수 조건) · [`docs/붙이는-법.md`](docs/붙이는-법.md) (코드 포함 절차).
   data.go.kr 오류코드에 뜻을 붙여 `im:smoke` 출력이 읽히게 했다

### 2026-08-16 세션에서 한 일 — 공공데이터 실측 연결

> **상세는 [`docs/인수인계-2026-08-15~16.md`](docs/인수인계-2026-08-15~16.md) 에 따로 정리했다.**
> 잡은 결함 5건의 원인·크기·재발 방지 장치, CLAUDE.md §4 규칙 요약,
> 키·활용신청 현황, 다음 사람이 할 일이 그쪽에 있다.

키를 넣고 **실제로 불러 본** 세션이다. 그전까지는 문서만 보고 짠 코드였고,
그래서 아래 셋이 숨어 있었다. 전부 출처 표시가 멀쩡해 문서로는 안 잡히는 종류다.

| 발견 | 실제 | 크기 |
|---|---|---|
| 개별공시지가 "최신값" | 2015년 값 (응답이 오름차순) | 30% 오차 |
| 지역·지구 조회 | 19건 받아 1건만 IM 에 | 토지거래허가·고도제한 누락 |
| 공시지가 시점수정 | 기준월 변동률 중복 계산 | 표본 필지 28억원 |

**커넥터 6개 · 스모크 14/14 · 테스트 475 → 491.**

1. **버그 4건 수정** — 공시지가 오름차순 / VWorld 도메인 가공(ned 계열 간헐
   INCORRECT_KEY, 5회 중 2회) / 폐기된 건축물대장 엔드포인트 / 대지면적 채움 경로 부재
2. **지역·지구 전량 등록** — `land.use_districts` + 딜 조건 RED/YELLOW 분류
   (분류표는 미확정 — D-24). 07 의 flags 를 05 가 받도록 배선했다 (08·09 만 받고 있었다)
3. **일사량** (`kma.js`) — 태양광 딜. **발전량은 내지 않는다** — 시스템효율이
   가정치다 (D-25 결정). 지점정보 승인 전에는 `KMA_STN` 으로 우회
4. **REC 현물시장** (`kpx.js`) — 03 시장조사에 **처음으로 출처 있는 숫자**가 들어갔다.
   서술(LLM 기억)과 실측 지표를 갈래로 나눴다
5. **공시지가 시점수정** (`reb.js`) — 8월 IM 이 1월 1일 값을 쓰고 있었다.
   지수 비로 계산한다(변동률 누적은 경계를 한 달 더 센다)
6. **시행사 실체 확인** (`fsc.js`) — 대표자 성명은 커넥터에서 버린다(개인정보,
   CLAUDE.md §6). 동명 법인은 고르지 않는다 — '삼성물산'·'현대건설' 은 실제로 2곳씩
7. **인허가 대조** — 건축법·주택법 **두 트랙**. 30세대 이상 공동주택은 건축허가를
   받지 않으므로 한쪽만 보면 오판한다. **자동으로 채우지 않는다** — 한 필지에
   이력이 여러 건이라 옛 건물 허가를 이 딜의 인허가로 적을 수 있다
8. **문서 정합성 교차검증** — README·im-agent/README·HANDOVER 가 코드보다
   뒤처져 있던 것을 맞췄다 (호출 수 5회→25회, 채움 9칸, VWORLD_DOMAIN 규칙,
   "실제 키로 검증하지 않았다" 문구 등)

> **공공 API 응답이 오름차순인 경우가 네 번 나왔다** — 개별공시지가·토지특성·
> REC·지가지수. 앞에서부터 집으면 몇 년 전 값을 최신으로 쓴다.
> 새 Connector 는 정렬 방향부터 확인한다. `im-agent/README.md` 에 규칙으로 적었다.

---

## 2. 버그 · 오류 개선안

심각도 — 🔴 확인된 결함 / 🟡 미검증(터질 수 있음) / 🟢 개선 제안 / ✅ 고침 / ⬜ 범위 밖

**남은 것 9건 · 고친 것 10건 · 범위 밖 2건.** 아래는 찾아본 순서가 아니라 급한 순서다.

| 급함 | 항목 | 한 줄 | 누가 |
|---|---|---|---|
| 🔴 | [B-2](#-b-2-git-이력에-내부-주소개인정보가-남아-있다) | git 이력에 내부 주소·개인정보 (본문은 정리됨) | 사람 (D-10 결정) |
| 🟡 | [B-3](#-b-3-vworld-지오코딩이-사용자-환경에서-실패한다-원인-미확정) | VWorld 지오코딩 실패 — **`VWORLD_DOMAIN` 이 유력** | 사람 (10분) |
| 🟡 | [B-4](#-b-4-공공데이터-응답-스키마를-실제-키로-검증하지-않았다) | 공공데이터 응답 스키마 실물 미검증 | 사람 (10분) |
| 🟡 | [B-5](#-b-5-nas-배포-경로가-한-번도-실행되지-않았다) | NAS 배포 **실행 0회** | 사람 |
| 🟡 | [B-6](#-b-6-유료-기능-3종의-등급이-확정되지-않았다) | 유료 3종 등급 미확정 | 경영 판단 |
| 🟡 | [B-15](#-b-15-카탈로그가-이-저장소에-없는-기능을-광고한다) | 지침이 약속한 기능이 저장소에서 사라짐 | D-19 결정 |
| 🟢 | B-9 · B-10 · B-12 | 결제 · 서버 PDF · IDC 기준 | 결정 |

<details><summary>이번 세션에 고친 10건</summary>

| | 무엇이 |
|---|---|
| B-8 | **[인쇄 · PDF 저장]이 안내만 띄웠다** (2026-08-15) |
| B-11 | **계보 모달이 순수 JS 판에 없었다** (2026-08-15) |
| B-13 | 값을 고쳐도 옛 값이 이겼다 |
| B-14 | 빌드 산출물이 소스와 어긋나 있었다 |
| B-16 | IM 에 특정 회사명이 박혀 있었다 |
| B-17 | 검증 실패인데 프로젝트가 만들어졌다 |
| **B-18** | **키 없이 죽는 자동 채움 경로를 화면이 몰랐다** |
| **B-19** | **FILLS 선언과 실제 코드가 갈려 있었다** |
| **B-20** | **`VWORLD_DOMAIN` 이 Secret 목록에서 빠져 있었다** |
| **B-21** | **자동으로 채워지는 줄에 출처를 물었다** |

굵은 넷은 **같은 날 만든 기능이 만든 결함**이다. 교차검증을 한 번 더 돌리지
않았으면 B-18·B-19·B-20 은 전부 살아서 나갔다.

**B-21 은 교차검증으로도 안 잡혔다** — 사용자가 화면을 보고 알려 주었다.
"규칙을 지키는가"는 테스트가 보지만 "이 칸을 물어볼 이유가 있는가"는 못 본다.

</details>

### ⬜ B-1. ~~크론 실패가 여전히 조용하다~~ → 범위 밖

아침 크론이 이 저장소에서 제외되면서 대상이 없어졌다 (등록부 D-18).
**`main` 의 크론은 그대로 돌고 있고 여전히 알림이 없다** — 크론을 옮길 저장소가
정해지면 거기서 다시 붙여야 한다 (D-19). `alert-failure` 자체는 남겨 두었다.

### 🔴 B-2. git 이력에 내부 주소·개인정보가 남아 있다

| | |
|---|---|
| 위치 | **git 이력** (본문에서는 제거 완료) |
| 증상 | tailnet 주소·내부 IP·**개인 휴대폰 번호·실명**이 공개 저장소 이력에 있다 |
| 진행 | 크론 삭제와 README 재작성으로 **본문에서는 전부 사라졌다.** 이력은 그대로다 |
| 조치 | private 전환 또는 이력 재작성 — 결정 필요 (등록부 D-10) |
| 난이도 | 결정만 하면 된다 |

본문만 고쳐도 검색 노출은 줄지만 이력은 남는다. 이 항목은 **본문 문제에서
이력 문제로 성격이 바뀌었다.**

### 🟡 B-3. VWorld 지오코딩이 사용자 환경에서 실패한다 (원인 미확정)

| | |
|---|---|
| 위치 | `im-agent/connectors/vworld.js` |
| 증상 | 지오코딩 응답이 비어 돌아온다. 07 Geo Agent 가 통째로 건너뛰어진다 |
| 이미 고친 것 | `type=json` 이 주소 서비스의 `type=ROAD/PARCEL` 을 덮어쓰던 버그 (회귀 테스트 있음) |
| 남은 것 | 그 뒤로도 실패한다는 보고. **키 문제인지 미매칭인지 구분 못 함** |
| 유력 원인 | **`VWORLD_DOMAIN` 미설정** (B-20). VWorld 키는 등록 도메인에서만 동작하는데 서버 호출에는 Referer 가 없어 `domain` 파라미터로 넘겨야 한다. 없으면 키가 맞아도 인증 거부된다 |
| 조치 | `~/linkpilot-cron` 에서 `IM_AGENT_DEBUG_HTTP=1 npm run im:smoke` 실행 후 `attempts[]` 확인 |
| 난이도 | 진단 10분 (사용자 환경에서만 가능) |

> 이전에 `~` 에서 실행해 계속 실패했다. **반드시 `~/linkpilot-cron` 안에서** 실행한다.
> 진단 도구가 키 형식(꺾쇠·따옴표·공백·UUID)을 **호출 전에** 검사하고, 6개 API 를
> 순서대로 실측하며 응답 필드명까지 대조한다.

### 🟡 B-4. 공공데이터 응답 스키마를 실제 키로 검증하지 않았다

| | |
|---|---|
| 위치 | `im-agent/connectors/*.js` |
| 증상 | 아직 없음. 필드명이 문서와 다르면 조용히 빈 값이 된다 |
| 조치 | 키 등록 후 `IM_AGENT_DEBUG_HTTP=1 npm run im:smoke` — API 를 순서대로 부르고 **응답 필드명을 Connector 기대값과 대조**한다 |
| 난이도 | 사람만 가능 (10분) |

> **★ `.env` 를 채우는 것만으로는 이 항목이 닫히지 않는다.** 키가 있다는 것과
> 응답 필드명이 맞다는 것은 다른 사실이다. **실제 호출 출력**이 있어야 대조가 된다.
> 이 저장소(에이전트 실행 환경)에서는 data.go.kr·VWorld 로 나가는 연결이
> 프록시에서 차단(403)되어 **대신 돌려 줄 수 없다.**
>
> data.go.kr 은 **API 별 활용신청**이 따로 필요하다 — 건축물대장 · 건축인허가 ·
> 토지 실거래가 · 상업업무용 실거래가. 승인 전에는 `API 오류 20/30` 이 뜨는데,
> 그건 키가 틀린 것이 아니다 (2026-08-15 부터 오류코드에 뜻을 붙여 구분해 준다).

파서는 JSON/XML(한글 태그) 양쪽을 흡수하도록 만들었지만 실물 확인이 없다.
data.go.kr 은 API 별로 **활용신청**이 따로 필요하다 — `BldRgstService_v2`(건축물대장) ·
`RTMSDataSvcLandTrade` · `RTMSDataSvcNrgTrade` · `RTMSDataSvcAptTradeDev`(실거래가 3종).
인증키는 **Decoding(일반 인증키)** 을 쓴다 — 코드가 `serviceKey` 로 그대로 넘긴다.

> **GitHub Secrets 만으로는 아무 일도 일어나지 않는다.** 지금 파이프라인을 실행하는
> 워크플로가 없고 CI 는 `IM_AGENT_OFFLINE=1` 로 돈다. 실제로 공공데이터를 쓰는 것은
> **본체(NAS)에서 도는 프로세스**이므로 환경변수는 거기에 들어가야 한다.

### 🟡 B-5. NAS 배포 경로가 한 번도 실행되지 않았다

| | |
|---|---|
| 위치 | `.github/workflows/deploy-nas.yml` (PR #4) |
| 증상 | 아직 없음. 첫 실행에서 tailnet 인증·SSH·백업 중 어디서 막힐지 모른다 |
| 원인 | 이 세션에서 NAS 에 4회 시도했으나 전부 차단(`host_not_allowed`). 검증 불가 |
| 조치 | PR #4 병합 → Secret 등록 → **`dry_run: true` 로 먼저 실행** → 출력 확인 후 실배포 |
| 난이도 | 사람만 가능 |

`dry_run` 기본값이 true 인 이유가 이것이다. 첫 실행을 실배포로 하지 말 것.

### 🟡 B-6. 유료 기능 3종의 등급이 확정되지 않았다

| | |
|---|---|
| 위치 | `im-agent/ui/platform/catalog.js:50-52` |
| 증상 | 통화음성분석=Pro / 프리미엄 투자소스 DB=Pro / 신재생에너지 분석=Business 는 **추정치**다 |
| 원인 | 지침 §2 는 '유료'라고만 적었고 등급을 나누지 않았다 |
| 위험 | Basic 회원이 쓸 수 있어야 하는 기능이 잠겨 있으면 문의가 들어온다 |
| 조치 | 결정 후 `byGuide: false` → `true` 로 바꾸고 지침도 함께 갱신 (재갱신 요청서 A-1 참조) |
| 난이도 | 결정 5분 + 반영 5분 |

### ⬜ B-7. ~~업무지시 보드가 데모 데이터로만 돈다~~ → 범위 밖 (2026-08-15)

보드 저장을 붙였다가(`ui/board-api.cjs`) **사용자 결정으로 화면·API·데이터를
저장소에서 제거했다.** 앱 사이드바의 [할일] 이 같은 역할을 한다.

되살리려면: `git log --diff-filter=D -- im-agent/ui/board-api.cjs` 로 삭제 커밋을
찾아 `git revert` 한다. 경합(`rev`)·서버 KST 완료일·원자적 쓰기가 전부 들어 있다.

### ✅ B-8. ~~[인쇄 · PDF 저장]이 안내만 띄운다~~ → 2026-08-15 수정

| | |
|---|---|
| 증상 | 지침 §7-3 이 안내하는 기능인데 "여는 경로를 본체에 연결하세요"만 떴다 — 협력사 눈에는 **고장난 기능** |
| 고침 | 본체를 기다리지 않고 API 가 직접 내려준다: `GET /projects/:id/file?rel=` |
| 경로 | 조립하지 않는다. `rel` 이 산출물 목록과 **글자 그대로 같을 때만** 통과 |
| XSS | HTML 은 sandbox CSP + nosniff 로 내려간다 — IM 본문은 업로드된 문서에서 온 글자를 담는다 |
| GATE | 배포가 막힌 산출물은 파일도 막는다 (목록은 '차단'인데 파일은 열리면 GATE 가 무의미하다) |

### 🟢 B-9. 결제 연동이 없다

| | |
|---|---|
| 위치 | `membership.html:311`, `upgrade.html:250` (`billingUrl: null`) |
| 증상 | 플랜을 골라도 "결제 연동이 아직 없습니다" 안내만 나온다 |
| 설계 | 카드정보를 이 화면에서 받지 않는다(PCI 범위 회피). 결제대행사 페이지로 보낸다 |
| 조치 | 결제대행사 선정 후 `billingUrl` 한 줄 |
| 난이도 | 코드는 5분. 결정이 오래 걸린다 |

### 🟢 B-10. PDF·PPTX·HWP 를 서버에서 만들 수 없다

| | |
|---|---|
| 위치 | `im-agent/ui/platform/reports.html:286-290` |
| 현재 | 목록에는 두되 비활성 + 이유 표시. 서버도 거부한다 |
| 원인 | PDF/PPTX 는 의존성 추가가 필요하고 승인받지 않았다. HWP 는 사실상 불가 |
| 대안 | HTML(A4) → 브라우저 인쇄로 PDF 저장 (지침 §7-3 과 동일한 방식) |
| 조치 | 서버 PDF 가 필요하면 의존성 추가 승인 필요 |

### ✅ B-11. ~~Lineage 모달이 바닐라 판에 없다~~ → 2026-08-15 수정

| | |
|---|---|
| 증상 | React 판에만 있었다. 본체가 단일 HTML 이면 **숫자를 눌러 출처를 확인하는 경로가 통째로 막힌다** |
| 고침 | `control-tower.js` 에 이식. `handle.openLineage(key)` 로 본체가 개별 숫자에 붙일 수 있다 |
| 지킨 것 | 채택되지 않은 후보도 보여준다 / 추적이 안 되면 '추적 불가'라고 쓴다 (경로를 지어내지 않는다) |
| 부수확 | 클래스명을 React 판과 대조하는 테스트가 `lp-ct__notice` 기본 클래스 누락을 잡았다 |
| 난이도 | 2~3시간 |

### ✅ B-13. ~~값을 고쳐도 옛 값이 이겼다~~ → 2026-08-15 수정

| | |
|---|---|
| 위치 | `im-agent/ui/report-api.cjs` `saveFacts` |
| 증상 | 5,000 을 5,500 으로 고치면 응답은 `saved: 1` 인데 **화면 값은 5,000 그대로** |
| 원인 | `Dataset.add` 는 값이 다르면 덮어쓰지 않고 후보로 쌓는다(설계대로). 같은 출처의 자기 정정까지 충돌로 쌓여 옛 값이 이겼다 |
| 조치 | `facts.js` 에 `dropWhere()` 추가 → **이전 화면 입력만** 지우고 새로 넣는다. 추출·공공데이터 값은 건드리지 않아 **출처 간 충돌 감지는 그대로** |
| 검증 | 회귀 테스트 3건 (`고친 값이 남는가` / `다른 출처가 살아 있는가` / `dropWhere 범위`) |

**저장은 성공했다는데 화면이 안 바뀌는 것이 가장 나쁜 실패다.** 사용자는 자기가
잘못 눌렀다고 생각하고 같은 일을 반복한다.

### ✅ B-14. ~~빌드 산출물이 소스와 어긋나 있었다~~ → 2026-08-15 수정

| | |
|---|---|
| 위치 | `im-agent/ui/platform/linkpilot-platform.html` (커밋된 빌드 결과물) |
| 증상 | 소스(`app.js`)는 고쳤는데 산출물이 옛 내용을 담고 있었다. **잡아내는 장치가 없었다** |
| 조치 | 재생성 + `fields.test.js` 에 **재빌드 결과가 커밋본과 같은지** 검사하는 테스트 추가 |
| 검증 | 일부러 한 줄 어긋내어 테스트가 실패하는 것을 확인했다 |

산출물이 소스와 갈리면 **화면에서만 옛 동작이 계속 살아 있고** 아무도 눈치채지 못한다.

### ✅ B-16. ~~IM 에 특정 회사명이 박혀 있었다~~ → 2026-08-15 수정

| | |
|---|---|
| 위치 | `im-agent/design/a4.js` `COMPANY` 상수 |
| 증상 | 표지·서명부 회사명이 코드에 고정. **다른 회사가 만든 IM 에 남의 이름이 찍혀 나간다** |
| 왜 늦게 발견됐나 | 소유사 이름이라 화면에서는 정상으로 보였다. 사용자가 스크린샷에 표시해 알려 주었다 |
| 조치 | `core/issuer.js` 로 분리 (프로젝트 → 환경변수 → 저장소 순). 접수 화면에서 입력받는다 |
| 미설정 시 | 기본 회사를 쓰지 않는다 — 문서에 「발행 주체 미설정」을 찍고 **승인 게이트가 배포를 막는다** |

기본값을 두면 **설정을 잊은 것과 일부러 그 회사인 것을 구분할 수 없다.**

### ✅ B-17. ~~검증 실패인데 프로젝트가 만들어졌다~~ → 2026-08-15 수정

| | |
|---|---|
| 위치 | `im-agent/ui/report-api.cjs` `createProject` |
| 증상 | 발행 주체 입력이 잘못돼 400 을 돌려주는데 **프로젝트 폴더는 이미 만들어져 남았다** |
| 결과 | 사용자는 실패한 줄 아는데 번호가 하나 소모된다. 다음 성공 요청이 `-002` 를 받는다 |
| 조치 | 프로젝트를 만들기 **전에** 검증한다. 회귀 테스트가 번호가 건너뛰지 않는지 확인한다 |

### ✅ B-18. ~~키 없이 죽는 자동 채움 경로를 화면이 몰랐다~~ → 2026-08-15 수정

| | |
|---|---|
| 위치 | `im-agent/core/fieldplan.js` (**같은 날 만든 기능**) |
| 증상 | 인증키가 없으면 07 Geo 가 통째로 건너뛰는데, 계획은 8개 항목을 계속 「공공데이터가 채웁니다」로 분류했다. 화면은 그 8개를 **안 물어보고 값은 빈 채로 남는다** |
| 왜 나쁜가 | 사용자는 물어보지 않았으니 **그런 항목이 있는 줄도 모른다.** 많이 묻는 것보다 훨씬 나쁘다 |
| 확인 | 데모 실행 — `geo.pnu`·`land.official_price` 는 아무것도 안 들어왔고, `land.area_sqm` 은 문서에서 뽑혀 **우연히** 채워져 있었다 |
| 조치 | `publicDataAvailable()` 이 커넥터의 `isAvailable()` 을 본다. 키가 없으면 그 경로를 빼고 계획을 만들어 **직접 입력으로 내리고 사유를 띄운다** (필수 8개 → 9개). 키를 넣으면 되돌아간다 |
| 검증 | 테스트가 두 상태를 섞지 않고 `withPublicKeys()` 로 나눠 검사한다 |

**만든 그날 스스로 어긴 원칙이다.** "자동으로 채워진다"는 말은 채워질 때만 해야 한다.

### ✅ B-19. ~~FILLS 선언과 실제 코드가 갈려 있었다~~ → 2026-08-15 수정

| | |
|---|---|
| 위치 | `im-agent/agents/07-geo.js` `FILLS` |
| 증상 | `land.official_price`(개별공시지가)가 07 의 선언에 있는데 **07 은 그 값을 한 번도 `facts.push` 하지 않는다.** 실제로는 08 Appraisal 이 채운다 |
| 왜 나쁜가 | FILLS 를 「각 Agent 가 자기가 채우는 것을 선언한다」는 단일 출처로 삼은 설계에서 **가장 위험한 형태의 드리프트**다. 07 만 도는 경로가 생기면 아무도 안 채우는데 화면은 계속 안 물어본다 |
| 조치 | 07 은 실제로 push 하는 7개만. 08 에 `FILLS = ['land.official_price']` 신규 선언. `fieldplan` 이 공공데이터 경로를 **07·08 양쪽에서** 모은다 |
| 검증 | FILLS 의 key 를 그 Agent 가 실제로 push 하는지 대조하는 테스트. **되돌려 넣어 실패하는 것을 확인**했다 |

### ✅ B-20. ~~`VWORLD_DOMAIN` 이 Secret 목록에서 빠져 있었다~~ → 2026-08-15 수정

| | |
|---|---|
| 위치 | `README.md` §5 Secrets 표 · 등록부 D-12 |
| 증상 | 코드(`connectors/vworld.js`)는 `VWORLD_DOMAIN` 을 읽고 있는데, **사람이 보고 옮겨 적는 두 곳**에서 빠져 있었다 |
| 결과 | 키만 넣고 도메인을 빠뜨리면 인증 오류만 나고 원인이 안 보인다 — **B-3 의 유력한 원인** |
| 조치 | 두 곳에 추가 + 진단 명령 병기 |

문서가 코드보다 적게 말하면, 사람은 문서만큼만 한다.

### ✅ B-21. ~~자동으로 채워지는 줄에 출처를 물었다~~ → 2026-08-15 수정

| | |
|---|---|
| 위치 | `im-agent/ui/platform/fields.html` 입력 줄 |
| 증상 | 한 줄에 늘 4칸(값·출처·p.·출처일). 「자동 · 산업 통상치」가 붙은 차입금리에도 출처(필수)와 출처일을 물었다 |
| 왜 나쁜가 | **시스템이 채울 값의 근거를 사람에게 물으면 사람은 자기가 모르는 것을 적어 넣는다.** 출처 강제가 거짓 출처를 만드는 순간이다 — 막으려던 바로 그 일 |
| 어떻게 발견됐나 | **사용자가 화면 스크린샷에 표시해 알려 주었다** (①②). 테스트로는 잡히지 않는 종류다 |
| 조치 | 자동 줄은 값 칸 하나. 사람이 값을 치면 그때 출처 칸이 나타나고 저장이 잠긴다. 페이지는 출처가 문서일 때만, 출처일은 `def.dated` 인 항목만 |
| 검증 | 헤드리스로 `debt.rate` 에 값을 쳐 출처 칸 생성·포커스 유지 확인. 판정을 되돌려 테스트 실패 확인 |

**출처 칸이 많을수록 안전한 게 아니다.**

### 🟡 B-15. 카탈로그가 이 저장소에 없는 기능을 광고한다

| | |
|---|---|
| 위치 | `im-agent/ui/platform/catalog.js` — `오늘의 한줄생각` · `아침브리핑` |
| 증상 | 유료 기능으로 표시되고 **협력사 배포 지침 §2 에도 적혀 있다.** 그러나 구현은 D-18 로 이 저장소에서 삭제되었다 |
| 지금 | `main` 에 남은 크론이 돌고 있어 **기능은 살아 있다.** PR #3 병합 시점부터 갈린다 |
| 조치 | D-19(이관처) 결정. 이관하지 않기로 하면 카탈로그와 **배포된 지침을 함께 고쳐야 한다** |

화면에서 지우는 것으로 끝나지 않는다 — 지침은 이미 협력사 손에 있다.

### 🟢 B-12. IDC(건설이자) 자본화 관행이 미확인이다

| | |
|---|---|
| 위치 | `im-agent/agents/04_financial.js` |
| 증상 | 모델 총사업비가 문서값보다 크다 (2,846 → 2,962억원). `TPC_GAP` 경고만 뜬다 |
| 원인 | 건설이자를 총사업비에 포함하는지가 회사·딜마다 다르다 |
| 조치 | 사내 기준 확정 후 상수화 |
| 난이도 | 결정 필요 |

---

## 3. 사람만 할 수 있는 것 (막힌 것 전부)

| # | 할 일 | 왜 나는 못 하나 | 걸리는 시간 |
|---|---|---|---|
| 1 | **PR #4 병합** | 권한 없음 | 2분 |
| 2 | Secret 등록 — `TS_OAUTH_CLIENT_ID`/`TS_OAUTH_SECRET`, `NAS_SSH_HOST`/`NAS_SSH_USER`/`NAS_SSH_KEY`, `ALERT_PHONE`, `VWORLD_KEY`, **`VWORLD_DOMAIN`**, `DATA_GO_KR_KEY` | 값을 모르고 알아서도 안 됨 | 10분 |
| 3 | `alert-failure` 수동 실행 → 알림 도착 확인 | Secret 필요 | 3분 |
| 4 | VWorld 진단 (B-3) | 사용자 환경에서만 재현 | 10분 |
| 5 | **본체 앱 소스를 `linkpilot-platform` 저장소에 업로드** | 접근 불가 | — |
| 6 | 등급·Free 한도·결제사 결정 | 경영 판단 | — |

### 5번이 막고 있는 것

본체 소스가 없어서 못 하는 일이 쌓여 있다.

- `server.js` 에 `report-api.cjs` + `run-queue.cjs` 배선 (코드는 다 됐고 붙이기만 하면 된다)
- `authenticate` 를 실제 로그인 세션에 연결
- `fileUrl` 연결 (B-8)
- 새로 만든 화면들을 실제 메뉴에 넣기

> 지금 만든 화면들은 **실제 앱을 보고 만든 것이 아니다.** 스크린샷과 지침을 근거로
> 테마를 맞췄다. 본체 소스가 오면 실제 컴포넌트로 다시 맞춰야 한다.

---

## 4. 다음 세션 시작법

```
HANDOVER.md 읽고 이어서 진행
```

우선순위는 위 표 순서다. **1번(PR #4 병합)이 끝나기 전까지는 다른 작업이 다 부차적이다** —
그 사이에 크론이 실패하면 아무도 모른다.

### 모바일 → 데스크탑 이어받기 (2026-08-15 기준)

모바일 세션의 작업은 **전부 커밋·푸시되어 있다.** 로컬에만 있는 것은 없다.
데스크탑에서는 받아오기만 하면 된다.

```bash
cd ~/linkpilot-cron
git fetch origin
git checkout claude/linkpilot-agent-github-plan-0af7xs
git pull                                   # 최신 커밋 해시는 PR #3 에서 확인한다
npm test                                   # 534 pass / 0 fail 이어야 한다
npm test                                   # 491 pass / 0 fail 이어야 한다
```

`node_modules` 는 필요 없다. 테스트·빌드·미리보기가 전부 표준 라이브러리로 돈다.

**화면을 보려면** — 서버 없이 파일 하나로 열린다.

```bash
npm run im:screens                         # 보고서 생성 4단계 미리보기
open im-agent/ui/platform/preview.html     # Windows 는 start, Linux 는 xdg-open
```

**모바일에서 바꾼 것 (커밋 4개)** — 데스크탑 큰 화면에서 다시 볼 것:

| 커밋 | 무엇 |
|---|---|
| `3964405` | 공공데이터 키가 없으면 그 경로를 죽이고 직접 입력으로 내린다 (B-18) |
| `c368386` | 개별공시지가는 07 이 아니라 08 이 채운다 — FILLS 선언 정정 (B-19) |
| `2270181` | 페이지·출처일을 뜻 있는 항목에만 (B-21 전반부) |
| `7c6ac02` | 자동으로 채워지는 줄에는 출처를 묻지 않는다 (B-21 후반부) |
| `97dace8` | 이 인수인계 절차 + 미리보기 산출물 gitignore |

뒤 둘은 **모바일 화면을 보고 지적해 주신 것**이다. 데스크탑 폭에서 칸이
`n1`/`n2`/`n3` 로 갈릴 때 레이아웃이 어색하지 않은지 한 번 더 보는 게 좋다.

**데스크탑에서 바로 할 수 있는 것** (모바일에서는 못 했던 것):

1. **공공데이터 키 진단** — 키를 셸에 넣고 `npm run im:smoke`. B-3 의 유력 원인은
   `VWORLD_DOMAIN` 누락이다 (B-20). 이게 풀리면 입력란이 9칸 → 8칸으로 줄고,
   무엇보다 **지적공부와 사업계획서가 다를 때 RED FLAG 가 자동으로 뜬다**
2. **PR #3 / PR #4 병합 판단** — 둘 다 clean, CI green. D-11·D-19 결정이 선행한다
3. **미결정 15건 훑기** — `docs/미결정-사항.md`. 🔴 4건이 먼저다

### 실행 명령 모음

```bash
cd ~/linkpilot-cron

npm test                                   # 534건
npm test                                   # 491건
npm run im:platform                        # 단일 HTML 재생성
npm run im:screens                         # 보고서 생성 화면 4개 미리보기
npm run im:demo                            # 샘플 자료로 파이프라인 1회 (오프라인)

# 공공데이터 (B-3·B-4) — 반드시 이 폴더 안에서
export VWORLD_KEY=... VWORLD_DOMAIN=... DATA_GO_KR_KEY=...
IM_AGENT_DEBUG_HTTP=1 npm run im:smoke     # 6개 API 실측 + 응답 필드명 대조
node im-agent/cli.js quota                 # 일일 호출 현황 (KST)
```

### 손대면 안 되는 것 (CLAUDE.md §2)

- 카카오 OAuth refresh token 갱신 로직 — 지우거나 단순화하지 않는다
- cron 은 UTC — 06:00 KST = `0 21 * * *`
- 시크릿을 코드·로그·커밋에 남기지 않는다
- 실패 알림(fallback) 경로를 항상 유지한다

### 이번에 생긴 규칙 하나

`catalog.js` 의 `byGuide: true` 는 **협력사에 배포된 지침이 등급까지 못 박은 항목**이다.
여기를 바꾸면 배포된 문서가 거짓말이 된다. 코드만 고치지 말고 문서를 먼저 고친다.
테스트(`guide.test.js`)가 이 규칙을 지킨다.
