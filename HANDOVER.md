# HANDOVER — 다음 세션 인수인계

작성 **2026-08-17 07:57 (KST)** · 브랜치 `claude/linkpilot-agent-github-plan-0af7xs` (`da11d2b`)
· main `70a4413` · 테스트 **1156/1156 통과** · 열린 PR **#6 하나**

> 첫 프롬프트: **「HANDOVER.md 읽고 이어서 진행」**
> 그다음 **반드시** `docs/미결정-사항.md` 를 읽고 🔴 항목을 사용자에게 먼저 알린다 (CLAUDE.md §9).

---

## 1. 세션 목표

전력·발전 자산군을 붙이고(계통·풍력·ESS·태양광), 평가 산출물 2종(탁상검토·법인가치)을
만들고, 전체 교차검증으로 빠진 곳을 잡은 뒤 열린 PR 을 정리했다.

---

## 2. 완료된 작업

### 커넥터 (3종 추가 — 총 23종)

| 커밋 | 파일 | 요지 |
|---|---|---|
| `4030b08` `821c62b` | `im-agent/connectors/kepco.js` | **계통 여유용량** 조회. `substationDistance()` 는 **거절한다** — 변전소 좌표가 국가중요시설 사유로 비식별 처리되어 계산할 방법이 없다 |
| `9061f08` | `im-agent/connectors/nts.js` | 국세청 **휴폐업·진위**. `taxClearance()` 는 **거절한다** (국세기본법 §81조의13) |
| `9061f08` | `im-agent/connectors/nps.js` | 국민연금 **인원·고지금액**. `insuranceClearance()` 는 **거절한다** |

### 자산군 (4종 추가 — 총 16종) · 재무 템플릿 8종

| 커밋 | 자산군 | 핵심 |
|---|---|---|
| `4ab274e` | `wind_offshore` · `wind_onshore` | **육상·해상을 나눴다.** 풍속은 **허브 높이와 짝**으로만 받는다 |
| `46e68da` | `ess` | **MWh 와 MW 는 짝.** 열화율 필수. 매출은 안 낸다 |
| `9e57592` | `solar` | 템플릿만 있고 **자산군이 없던** 자리. **DC/AC 짝** |

관련 코어: `im-agent/core/ess.js` · `im-agent/core/solar.js`

### 산출물 (2종 추가)

| 커밋 | 파일 | 요지 |
|---|---|---|
| `551d6fd` | `im-agent/core/deskappraisal.js` | **탁상검토 보고서** → `08_Appraisal/desk-review.{md,html,pdf}` |
| `b15c737` | `im-agent/core/corpvalue.js` · `corpreport.js` | **법인가치 검토** → `10_Corporate/corp-review.{md,html,pdf}` |

둘 다 `im-agent/core/outputspec.js` 에 docType 추가(`desk_appraisal` · `corp_valuation`),
`im-agent/design/a4.js` 표지를 문서 종류에 맞게 바꿈(`docLabel`·`docTitle`·`valueCaption`).

### 검토 문서

- `80b7783` — `docs/신용조회-계약검토.md` (D-61). 아티팩트 `25c2ed03-7da2-4899-a908-644b85a36c4f`

### 교차검증 수정 (`8ce17f4`)

- `im-agent/agents/05-validation.js` 에 **⑩ 짝 용량 검사** 추가
- `im-agent/connectors/cache.js` — 색인 4종 TTL 을 180일로
- `im-agent/tools/smoke-public-data.js` — `pexels` 요약 누락 채움

### PR 정리 (`a5ca942` · `da11d2b`)

**PR #4 · #2 · #1 을 닫았다** (병합 아님). 셋 다 **삭제된 크론 스크립트를 고치는 변경**이라
병합하면 없는 파일을 부르는 워크플로가 매일 실패한다.

---

## 3. 결정사항과 그 이유

### D-54 — 계통 여유는 자동, 변전소 거리는 사람이 (사용자 선택 「다」)

거리를 자동 산출하는 대신 **사람이 넣는 가이드 필드**로 뒀다 — 한전이 변전소 좌표를
**비식별 처리**해 계산할 좌표 자체가 없다. 직선거리 ≠ 선로거리(1.3~2배)라
**기준(직선/선로)을 안 밝히면 값을 받지 않는다.**

### D-55 — 풍력을 하나로 묶지 않고 육상·해상으로 나눴다

해상은 사업비가 배 이상이고 공사기간·금융조건이 다르다. 하나로 두면
**숫자는 나오는데 어느 쪽 기준인지 알 수 없는 모델**이 된다.

### D-56 — ESS 를 발전과 한 묶음으로 두지 않았다

자원이 없고 배터리가 해마다 준다. 태양광(20년)을 그대로 쓰면 **교체 없이 20년 도는
모델**이 된다.

### D-57 · D-59 — 평가 문서는 「틀리지 않아도 사고가 난다」

형식이 그럴듯하면 **정식 감정평가/평가의견서로 읽힌다.** 그래서 고지를 **다섯 곳**
(표지·머리말·1장·결론·꼬리말)에 넣고, **확인하지 않은 것을 결론보다 앞**에 뒀다.
**방식이 갈리면 하나로 좁히지 않는다** — 표지에 큰 숫자 하나가 박히면 그것만 읽힌다.

### D-58 — REC 가중치 **값**을 코드에 박지 않았다

고시가 정하고 **개정된다.** 박으면 개정된 날부터 조용히 틀리고 문서에는
「가중치 적용됨」만 남는다. 대신 **갈리는 축 셋**만 말한다. 같은 이유로 D-59 는
**법령 산식을 문서에 인쇄**해 사람이 현행 법령과 대조할 수 있게 했다.

### D-60 — 못 하는 조회를 「거절 함수」로 코드에 남겼다

납세증명·완납증명은 **법이 막는다.** 함수를 안 두면 「아직 안 붙였나 보다」로 읽히고
**누군가 상용 스크래핑을 붙이려 든다.**

### D-61 — 신용평가사와 지금 계약하지 않는다

**한신평은 우리가 필요한 쪽이 아니다** — 공모채 발행사만 평가해 SPC 는 등급이 없다.
필요한 것은 CB 3사고, **계약의 값은 금액이 아니라 재배포 허용 여부**가 정한다.
무료 소스 실측이 먼저다.

### D-11 — PR #4 를 병합하지 않고 **닫았다**

사용자 지시는 「최신 main 기준으로 병합」이었으나 **전제가 틀렸다.** 핵심 3개
(`alert-failure.js`·`alert-failure.yml`·`deploy-nas.yml`)가 **이미 main 에 있었고
파일 해시까지 같았다.** 남은 것을 병합하면 어느 쪽으로 풀어도 깨져서, 확인 후 닫았다.

### ❌ 실패한 접근 (다음 세션에서 반복 금지)

1. **`curl` 로 GitHub API 직접 조회** — 이 세션은 인증이 없어
   `{"message":"GitHub access is not enabled..."}` 가 온다. 그걸 grep 하면
   **「in_progress 0건」으로 읽혀 CI 가 끝난 것처럼 보인다.** 실제로 오판했다.
   → **CI 판정은 MCP `get_check_runs` 로만. 대기는 `sleep` 후 재조회.**
2. **`git checkout -B /tmp-merge-test`** — `/` 로 시작하는 브랜치명은 무효라 조용히
   실패하고 **작업 브랜치에서 병합이 시작될 뻔했다.**
   → 병합 시뮬레이션은 **`git worktree add --detach`** 로 격리한다.
3. **`core/ess.js`·`core/solar.js` 를 만들고 아무 데서도 안 불렀다** (D-48 재발).
   → 새 코어 모듈을 만들면 **「실제로 불리는가」 테스트를 같은 커밋에** 넣는다.

---

## 4. 미완료 작업 (우선순위 순)

### 🔴 1. D-19 — 크론 이관처 (가장 급하다)

**아침 브리핑이 지금 도는 곳이 없다.** 그리고 이번 세션에 PR 셋을 닫아
**크론이 어느 열린 PR 에도 없고 브랜치에만 남는다.**

| 브랜치 | 커밋 | 무엇 |
|---|---|---|
| `claude/linkpilot-deployment-setup-w8yiqz` | `00b0e59` | **발송 로직 최신판** (프리플라이트·수신자 추론·dry-run) |
| `claude/ops-cron-alerts-and-deploy` | `b2e4c0b` | 실패 알림·NAS 장애 대응·`cron-ci` |
| `claude/start-luizd0` | `148ec55` | 위 첫 줄에 포함됨 |

**이관 시 앞의 두 브랜치를 함께 본다. 이관처가 정해지기 전에는 브랜치를 지우지 않는다.**

### 🔴 2. D-20 협력사 지침 재발행 · D-10 저장소 public · 키 재발급 3건

키 재발급: **VWorld · data.go.kr · Pexels** (D-12 · D-50)

### 3. 실측 대기 — 키만 오면 한 번에 맞춘다

D-36 · D-43~D-47 · D-50 · **D-54** · **D-60**
→ `npm run im:smoke` · `npm run im:image` 출력 필요
- **새로 발급 필요:** `KOSIS_API_KEY` · **`KEPCO_BIGDATA_KEY`**(한전 자체 발급 — data.go.kr 키 아님)
- **활용신청은 API 하나하나 따로** (공장·수출입·환경·국세청·국민연금)
- ⚠ 이 환경에서 `kosis.kr`·`api.pexels.com`·`bigdata.kepco.co.kr`·`www.data.go.kr` 이
  망 정책으로 막혀 있다 — **403/000 은 프록시 차단이지 API 응답이 아니다**

### 4. 기본값이 추측이라 확인 필요

- **D-55 풍력** (공사기간·운영비율·LTC·금리·예비비·규모 구간)
- **D-56 ESS** (특히 **기간 중 배터리 교체를 넣을지** — 넣으면 15년이 아니라 20년)
- D-40 제조 · **D-59 법령 조문 개정 대조**

### 5. D-62 가 남긴 둘

- **D-18 · D-25 · D-27 본문 항목 복원** — 결정 기록 표에 행은 있는데 **본문이 없다**(§9 위반).
  특히 **D-25(태양광 발전량 미산출)** 는 코드 여러 곳이 근거로 인용하는데 경위가 없다
- **탁상검토·법인검토를 승인 게이트에 걸 것인가** — 지금은 `gate.js` 를 안 거친다

### 6. D-61 신용평가사 — CB 3사 질의서 발송

`docs/신용조회-계약검토.md` §5 에 12개 항목을 그대로 쓸 수 있게 적어 뒀다.

### 7. 나머지

D-39 · D-41 산업·섹터(**「에너지·발전 / 태양광·풍력·ESS」가 앱에 실제로 있는지**) ·
D-50③ · D-51 · **D-57·D-59 를 지침 §2 카탈로그에 올릴지** · kpx·reb 파서 미검증

---

## 5. 주의사항

### 반드시 알아야 할 함정

- ⚠️ **NAS 에 한글 글꼴이 아직 없다.** 없으면 **PDF 한글이 중국어 글꼴로 박힌다**(D-52).
  `im-agent/core/fonts.js` 가 경고는 하지만 **막지는 않는다**
- **`gate.canApprove` 는 `11_QC/validation.json` 이 없으면 조기 반환한다** —
  새 검사를 그 아래에 붙이면 안 걸린다
- **사용자가 맥북에서 직접 푸시한다.** 푸시 전 `git fetch` → 앞서 있으면 **병합**
  (`--force` 금지). 충돌이 `section-preview.html` 같은 **생성물**이면 손으로 합치지 말고
  `npm run im:section` 재생성으로 해소한다 — 이번 세션에 두 번 그렇게 했다
- **DART `jurir_no`(13자리) ≠ 사업자등록번호(10자리).** 섞으면 **엉뚱한 사업자가
  조회되는데 증상이 「없는 회사」와 같다.** `nts.normalizeBizNo` 가 13자리를 거절한다

### 이 저장소의 원칙 (코드에 박혀 있다)

- **거절 함수를 지우지 말 것** — `kepco.substationDistance` · `kma.hubWindSpeed` ·
  `ess.revenue` · `solar.generation` · `solar.planeOfArrayIrradiance` ·
  `corpvalue.marketMultipleValue` · `corpvalue.controlPremium` ·
  `nts.taxClearance` · `nps.insuranceClearance`.
  **값을 내려고 둔 것이 아니라 못/안 하는 이유를 코드가 들고 있으려고** 있다
- **「확인 안 됨」을 「문제 없음」으로 세지 않는다** (enviro·kepco·nts 의 네 갈래 판정)
- **모수·기준시점을 출처에 적는다** (§4.7)

### 미해결 · 임시

- `docs/미결정-사항.md` — **미결정 35건 · 결정 22건**. 헤더 숫자와 실제가 맞는지
  가끔 센다 (이번 세션에도 어긋난 적 있음)
- **예약된 PR 점검 트리거가 남아 있다** (`trig_01K2Wy6FXxDLi1k5UCGYqQYu`, 2026-08-16T21:27Z).
  PR #6 이 아직 열려 있어 그대로 뒀다 — 불필요하면 `delete_trigger` 로 지운다
- **PR #6 은 병합되지 않았다.** 이번 세션 작업 전부가 이 PR 에 있다

### 아티팩트 (주소 유지 — 새로 만들지 말 것)

| 화면 | UUID |
|---|---|
| 보고서 생성 섹션 (최신) | `1ce9a833-a1d8-4f33-ab81-3ea5adf37672` |
| 신용조회 계약검토 | `25c2ed03-7da2-4899-a908-644b85a36c4f` |
| 값 입력 | `c58c9f18-a122-41a6-a474-a9d76f8ac725` |
| 인수인계서(플랫폼 연동) | `12314313-520b-4c48-b2dd-3873ea8ca251` |
| 대시보드 | `5788c1e4-d0fc-4b41-ab99-15a7956ec843` |

빌드: `npm run im:section` · `npm run im:artifact` · `npm run im:dashboard`
