# HANDOVER — LinkPilot AGENT Engine 인계

작성 **2026-08-26 00:40 (KST)** · 브랜치 `claude/sketchup-engineering-agent-rg04zq`
· main `7339b33` (아직 미병합) · 열린 PR **#9 (draft)**
· 테스트 **1849 통과**(skip 7 · 실패 0) · `npm run guard` **7 통과 · 0 실패 · 못 잼 1**
· 화면 묶음 판 지문 **`664b0bf8`** · 등록부 미결정 **58건** · 결정 **39건**

> **이 문서는 엔진을 이어받는 사람에게 쓴 것이다.** 앞 판은 SketchUp 갈래 요약이었고,
> 이번 판은 **엔진에 실제로 배선된 것과 배선이 남은 것**을 가른다.

---

## 1. 한 줄 요약

도면·모델을 만드는 트랙을 닫고, **투자자에게 가기 전 사업을 거르는 자리**를 엔진에
붙였다. 마지막에 붙은 것이 **법규 판정(`09_massing` → `core/codecheck.js`)** 이고,
이것이 지금 IM 본문에 실제로 실린다.

## 2. 엔진에 **배선 완료** — 지금 돈다

### 2-1. 법규 판정 (`core/codecheck.js` → `09_massing`)
매스가 나온 자리에서 바로 판정한다. **판정은 fact 가 아니다** — flags 로 나가고
`codeReview` 로 밖에 내놓는다.

| 판정 | 근거 | 비고 |
|---|---|---|
| 고층 구분 | 건축법 시행령 §2 | 30층↑/120m↑ 고층 · 50층↑/200m↑ 초고층 |
| 피난안전구역 | 시행령 §34③④ | 준초고층 = 전체 1/2 층 ±5개층 1개소 |
| 직통계단 · 특별피난계단 | 시행령 §34② · §35 | 공동주택 16층↑(바닥 400㎡↑) |
| 방화구획 | 시행령 §46 | 11층↑ 200/600/500/1,500㎡ (불연·SP 조합) |
| 비상용 · 승용승강기 | 시행령 §90 · 설비기준규칙 [별표 1의2] | 31m 초과 · 거실 3,000㎡마다 |
| 정북일조 · 채광인동 | 시행령 §86① · 건축법 §61② | **일반상업·중심상업 제외** |
| 용적률 한도 | 국토계획법 시행령 §85 | `ordinance` — 적합으로 세지 않는다 |

**실측 확인** — `npm run im:demo` 결과 `09_IM/im.md` 160~161줄에 그대로 찍힌다:
```
| [YELLOW] | CODE_ORDINANCE | 용적률 한도 — **조례**가 정한다. 시행령 상한으로 적합 판정하지 않는다 |
| [YELLOW] | CODE_UNKNOWN | 법규 판정을 못 한 항목 1건 — 직통계단 (입력이 없어 적합으로 세지 않았다) |
```

★ 세 가지 규칙이 코드에 박혀 있다. **고치지 말 것**:
1. `unknown` 을 적합으로 세지 않는다 (하나라도 있으면 `clear:false`)
2. 코드에 박힌 기준값은 출처가 아니다 (`sourced:false` → 법령 API 가 붙어야 `true`)
3. 용적률은 조례가 정한다 (시행령 상한으로 적합 판정 금지)

### 2-2. 커넥터 둘 (26개로 늘었다)
| 커넥터 | 상태 | 남은 것 |
|---|---|---|
| `law` (법제처) | **배선 완료** · 두 이름(`LAW_OC` / `LAW_OPEN_DATA`) 다 읽는다 | NAS 실호출 1회 |
| `kict` (건설기술연구원) | 키는 살아 있음 · **호출 안 함** | 어느 서비스인지 확정 (D-106) |

### 2-3. AI 렌더 · 도면 표준
- `RENDER_STANDARD` = Veras 4.0 (Nano Banana Pro) — `core/outputspec.js` 한 곳
- `12_sketchup_plan` 이 계획에 실어 알리고, `13_sketchup_intake` 가 비표준을 YELLOW
- 표기가 온전한 렌더는 **IM 본문**에 실린다 (D-34 2차 개정 · `bodyRenders`)
- `npm run im:render` — Gemini 후보 생성, **채택은 사람**(`--adopt`)

## 3. 배선이 **남은 것** — 이어받을 사람이 할 일

| 우선 | 무엇 | 왜 지금 안 했나 |
|---|---|---|
| 1 | **KICT 엔드포인트 확정** (D-106) | 어느 서비스 키인지 모른다. 주소를 추측해 박으면 「키가 틀렸다」와 구분이 안 된다 |
| 2 | **NAS 스모크 1회** | 이 컨테이너는 `law.go.kr` egress 차단 + 운영 키 없음. 승인 여부·읽은 이름·KICT 주소가 한 번에 갈린다 |
| 3 | **codecheck 입력 채우기** | 지금 `09_massing` 은 층수·높이·기준층면적만 넘긴다. 계단·승강기·주거층수는 `unknown` — 지어내지 않았다 |
| 4 | **조례 한도 연결** | `law.ordinance()` 는 있으나 `09_massing` 이 아직 안 부른다. 부르면 「조례 확인」이 「○○구 건축조례 제○조」가 된다 |
| 5 | `deploy-nas.yml` 갈래 겹침 | 세 갈래가 같은 파일을 만들었다 — **사람 결정** |

## 4. 실패했던 접근 — 다시 하지 말 것

| 시도 | 왜 실패 | 기록 |
|---|---|---|
| SVG 도형 CSS 에 `fill:none` | CSS 가 `fill` **속성**을 이겨 해칭·먹칠이 전부 사라졌다. 오류 0 | M-43 |
| 치수 오프셋을 모형 미터로 | 1:100 에서 시트 밖으로 날아갔다 | M-43 |
| 도로를 이름으로 맞추기 | 도면 y=100 · 3D y=0 인데 둘 다 「남측」 | M-44 |
| 법규 판정을 손으로 | 딜이 바뀌면 재현이 안 된다 | M-45 |
| 열쇠 이름 하나만 읽기 | `LAW_OPEN_DATA` 가 죽어 있었다 | M-45 · M-40 |
| `workflow_dispatch` 로 render-smoke | 파일이 main 에 없으면 404 | — |

## 5. 주의 — 건드리면 깨지는 곳

- **`guard` 여덟 칸을 손으로 대신하지 않는다.** `npm run guard` 하나로 돈다 (M-31)
- **`changes.js` 는 평문이다** — `**` 를 쓰면 guard 가 막는다
- **아티팩트 주소를 저장소에 적지 않는다** — 이 저장소는 public (D-10)
- **키는 `.env.example` 에 이름을 넣는 순간** SECRET_ENV·인수인계서·배포목록·등록부
  네 곳을 테스트가 강제한다. 하나만 빠져도 빨개진다 (그게 안전장치다)
- **PR #9 CI 는 지금 러너 미배정으로 실패한다** — 코드 문제가 아니다.
  2초 만에 `runner_id: 0` 으로 끝나고 로그가 없다. Actions 사용량 한도 확인 필요

## 6. 산출물 (아티팩트 — 주소는 저장소에 적지 않는다)

설계도면 5매(A2 실제 축척) · 딜 스크리닝 팩 6장(16:9) · 주상복합 4종 세트 ·
3D 모델 뷰어 — 전부 **같은 좌표 한 벌**에서 나온다. 주소는 세션 답변에 있다.
