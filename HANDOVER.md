# HANDOVER — 다음 세션 인수인계

작성 **2026-08-22 20:09 (KST)** · 브랜치 `claude/linkpilot-agent-github-plan-0af7xs` (`181eb50`)
· main `70a4413` · 테스트 **1516/1516 통과** · 열린 PR **#6 하나(draft)**
· 화면 묶음 판 지문 **`e26243f7`**

> **읽는 법** — 급하면 §4(다음 할 일)와 §5(주의사항)만 본다.
> 「왜 이렇게 되어 있지」가 생기면 §3(결정사항)을 본다.

---

## 1. 세션 목표

**자료 업로드가 계속 401 로 거절당하는 원인을 잡고**, 사장님 지시대로 화면을
**탭 둘·단계 셋**으로 다시 짜고, **배포가 화면에 실제로 닿았는지**까지 재게 만든다.

---

## 2. 완료된 작업

### 2-1. 자료 업로드 401 — 진짜 원인을 잡았다 (M-24)

**증상** 3.8MB 를 올리는데 「로그인이 필요합니다」. 로그인은 되어 있고 목록도 떠 있었다.

**원인** 보내는 길이 **둘인데 인증이 한 길에만** 붙어 있었다.

```
목록 읽기    fetch            → Authorization 붙음 → 200
자료 올리기  XMLHttpRequest   → 안 붙음            → 401
```

올리기가 XHR 인 이유는 **진행률**이다 — `fetch` 에는 업로드 진행 이벤트가 없다.
화면에서 XHR 을 쓰는 곳이 올리기 하나뿐이라 딱 그 하나만 맨몸으로 나갔다.

| 파일 | 무엇을 했나 |
|---|---|
| `im-agent/ui/platform/embed-bridge.js` | `installAuth()` 가 `fetch` 와 XHR 을 **한 함수에서 함께** 덮는다. **같은 출처에만** 붙인다(프로토타입을 덮으므로 아무 데나 붙이면 열쇠가 남의 서버로 나간다) |
| `im-agent/test/embed.test.js` | 실제 크로미움에서 **진짜 XHR 을 보내 헤더를 받아 본다.** 되돌려서 빨개지는 것 확인 |
| `im-agent/ui/platform/files.html` | 실패 안내에서 **크기를 원인으로 짚던 짐작을 뺐다.** 대신 「가른 사실」을 적는다 |

커밋 `060c7d3`.

### 2-2. 「반영이 안 된다」를 셋으로 갈랐다 (M-25)

**실측**으로 확인했다 — 저장소·디스크는 같은데 **HTTP 만 옛것**이었다.

```
flow-core.js      저장소 = 디스크 ≠ HTTP
embed-bridge.js   저장소 e7379b9c · 디스크 e7379b9c · HTTP 9c21ec1b
files.html        저장소 7ca17dd7 · 디스크 7ca17dd7 · HTTP b12519a6
report-flow.html  저장소 65f8ad79 · 디스크 65f8ad79 · HTTP bd665124
```

| 파일 | 무엇을 했나 |
|---|---|
| `deploy/verify-served.sh` **(신규)** | 저장소·디스크·HTTP **세 지문**을 각각 재고 어디서 갈렸는지 이름을 붙인다. 갈리면 `exit 1` |
| `.github/workflows/deploy-nas.yml` | 올린 **직후에** 그 스크립트를 부른다(`Verify deployed`). **실패를 안 삼킨다.** 탐침(`Check served build`)도 같은 스크립트를 쓴다 |
| `im-agent/ui/platform/build-stamp.js` **(신규)** | 화면이 **판 지문**을 스스로 말한다(`data-lp-build`). 묶음 전체 내용에서 만들고 **시계는 안 넣는다**(M-10) |
| `im-agent/test/build-stamp.test.js` **(신규)** | 찍힌 지문이 옛것이 되지 않게 지킨다 |
| `im-agent/test/workflow-default.test.js` | 확인 단계가 ①있고 ②올리기 뒤이고 ③실패를 안 삼키는지 |

커밋 `07dafeb` · `9455087` · `4879b07` · `f7adab0` · `45471a5` · `181eb50`.

### 2-3. 탭 둘 · 단계 셋 · 자료 업로드는 1단계 안으로

| 파일 | 무엇을 했나 |
|---|---|
| `im-agent/ui/platform/flow-core.js` | `TABS` 셋 → **둘**(완성 보고서 · **보고서 만들기**). `STEPS` 넷 → **셋**. `SECTIONS[3].steps` 에서 `make` 제거 |
| `im-agent/ui/platform/intake.html` | 카드 차례 **발행 주체 → 무엇을 만들까요 → 자료 업로드**. 자료 칸이 `files.html` 을 **iframe 으로 품는다**(높이는 `lp-embed-height` 로 맞춘다) |
| `im-agent/ui/platform/build-embed.js` | `required()` 가 **탭이 아닌 화면도 센다** — 안 그러면 `files.html` 이 묶음에서 빠져 1단계 안이 404 |
| `im-agent/ui/platform/build-tabs.js` | 자료 칸에 **「탭이 아닙니다」** 딱지(`inStep`) |
| `im-agent/ui/platform/build-static.js` | 조각에 iframe 을 못 실으므로 그 자리에 **무엇이 들어가는지 글자로** 적는다 |
| `im-agent/ui/platform/live-core.js` | 진행률은 **넷 그대로** 센다(생성은 실제로 도는 일) |
| 검사 6개 · `verify-nas.js` · `작업지시서-플랫폼-연동.md` | 탭 둘·단계 셋 기준으로 갱신 |

커밋 `56d6c09`.

### 2-4. 그 밖에 고친 것

| | 커밋 |
|---|---|
| 배포 기본값이 **16개 중 1개**만 올려 화면이 통째로 비었다 (M-22) | `a71cd5c` `49bc13e` |
| **덜 읽힌 파일이 조용히 빠진 채로** 올라갔다 — 2개를 골랐는데 「1개를 올렸습니다」 (M-23) | `1730d35` |
| 갈래 이름 「LinkPilot 프로젝트에서 가져오기」 · 「무료」 배지 삭제 · 한도/합계 상시 표기 | `1730d35` `a0a6764` |
| 자료 **넣는 길 셋만** 로그인을 안 묻는다 (D-82) | `a0a6764` |

### 2-5. 기록

- `MEMORY.md` — **M-22 · M-23 · M-24 · M-25** 신설. **색인에서 M-19~M-23 다섯이 빠져
  있던 것**을 채웠다(적어 두고도 못 찾는 상태였다).
- `im-agent/test/memory-index.test.js` **(신규)** — 색인 누락·죽은 링크를 막는다.
  「상태 변경 시 단일 출처 `updatedAt` 필수」 규칙과 **상태 토글 체크리스트 9항목**이
  사라지지 않게 지킨다.
- **배포 전 교차검증에 11·12·12-1 추가** — 길이 갈리는 자리는 둘 다 밟는다 /
  같은 증상이 세 번이면 내 진단부터 의심한다 / **「반영이 안 된다」는 셋이다.**
- `CLAUDE.md` §8 — **끝냈다는 답변에 판 지문을 적는다.**
- `docs/미결정-사항.md` — **D-19 ✅결정**(아침 크론 → `morning-cron`) · **D-82 ✅결정**.

---

## 3. 결정사항과 그 이유

### ✅ 채택한 것

- **「생성」을 레일에서 뺐다 — 이유는** 출력조건과 **같은 화면**이고 확정하면 그 자리에서
  바뀌기 때문. 옮겨 갈 곳이 없는 칸을 그리면 「한 칸 더 남았다」로 읽고 다음을 찾는다.
  **다만 진행률(live-core)은 넷을 그대로 센다** — 생성은 실제로 도는 일이고, 그 동안
  사람은 기다린다. 「출력조건 100%」로 뭉치면 만들어지는 중인지 끝났는지가 사라진다.

- **자료 업로드를 베끼지 않고 iframe 으로 품었다 — 이유는** 세 갈래·한도·합계·스캔이
  전부 `files.html` 에 있기 때문. 베끼면 한쪽만 고치는 날 **같은 기능이 자리마다 다르게
  돈다.**

- **구성안에서 자료 칸을 빼지 않았다 — 이유는** 1회성의 대가처럼 **반드시 읽어야 하는
  말**이 그 칸에 있고 검사가 그것을 지키기 때문. 대신 「탭이 아닙니다」를 화면에 적는다.

- **로그인은 「넣는 길」 셋만 열었다 — 이유는** 읽는 길을 열면 **남의 프로젝트에 무엇이
  들었는지**가 나가기 때문 (D-82).

- **배포가 캐시 때문에 빨간 것도 빨간 것으로 둔다 — 이유는** 화면에 안 닿았으면 배포가
  제 일을 못 한 것이기 때문. 초록으로 끝난 배포를 믿고 다음 일을 하는 것이 가장 비쌌다.

### ❌ 시도했다가 접은 것 — 다시 하지 말 것

| 무엇 | 왜 접었나 |
|---|---|
| **`?v=지문` 을 형제 파일 주소에 붙여 캐시 우회** | 검사 **54개**가 깨졌고(`href="tokens.css"` 를 글자 그대로 보는 자리가 여럿), **무엇보다 HTML 자체는 그 방법으로 못 지나간다** — 캐시된 HTML 이 옛 `?v=` 를 부른다 |
| **조각(아티팩트)에 자료 화면 끼워 넣기** | 두 번 시도해 둘 다 **빈 조각(1~2KB)**. `files.html` 은 설정을 받아야 그려지는데 조각 만드는 자리에서 먹이는 길을 못 찾았다. 지금은 **그 자리에 글자로** 적어 둔다 |
| **표식(「이 글자가 있나」)으로 배포 확인** | **두 판 전 파일**이 「새 판」으로 통과한다 — 실제로 한 번 속았다. 지금은 **지문**을 댄다 |

### ❗ 내가 **네 번** 틀린 진단 (같은 함정)

① 브라우저 캐시 → ② 배포가 안 됐다(dry run) → ③ **본문이 커서 잘렸다** →
④ **NAS 웹서버가 캐시한다**.

①②③ 의 진짜 원인은 **헤더**였다(M-24). ④ 는 **원인을 가르지도 않고 하나를 고른
것**이고, 그 바람에 사장님께 **바꿀 필요 없는 DSM 설정을** 만지시라고 했다.
본체가 앱과 같은 길(Funnel 443)로 재니 **저장소=디스크=HTTP 4/4 일치** — 캐시가
아니었다 (M-25 정정).

★ **가르지 못했으면 하나를 고르지 않는다.** 후보를 전부 적고 순서를 준다.

---

## 4. 미완료 작업 (우선순위 순)

### ✅ ① ~~DSM 웹서버 캐시 끄기~~ — **취소한다. 사장님은 아무것도 안 하셔도 된다**

> **앞 판 이 자리에 「DSM → Web Station → HTTP 캐시 끄기」라고 적었다. 그것이
> 틀렸다.** 본체(엔진 세션)가 **앱이 실제로 쓰는 길**(Funnel 443 → 80)로 다시
> 재니 네 파일이 **저장소 = 디스크 = HTTP 로 4/4 일치**했다. 캐시가 아니었다.
>
> ```
> flow-core.js 8038a16e · embed-bridge.js adfaadf5 · files.html c18323f4 · report-flow.html e064c548
> ```
>
> ★ **Web Station 설정을 바꾸지 마십시오.** 그럴 이유가 없다.

**그럼 내 탐침은 왜 달랐나** `deploy/verify-served.sh` 는 **tailnet 호스트의 포트
80** 으로 직접 받는다. 앱은 **Funnel 443 → 80** 으로 들어온다 — `Host:` 머리말이
다르고, DSM 은 **Host 로 가상 호스트를 고른다.** 즉 **다른 문서 루트를 봤을
가능성**이 크다. 지금은 탐침이 **잰 주소를 함께 적고**(`HTTP 를 받은 길: …`),
갈렸을 때 **후보 셋을 순서대로** 적는다. 하나로 단정하지 않는다.

### ✅ ① `im-flow` 단일 배포 경로 — **결정됐다 (D-84 · 2026-08-22)**

`deploy-nas` 의 쓰기(scp)를 **잠그지 않고 지웠다.** 이제 이 워크플로는 **재기만
한다.** `im-flow` 배포는 **본체 게이트**(사장님 맥의 `deploy/nas.sh`) 하나로 간다.

- 입력이 셋으로 줄었다 — `files` · `dest` · `dry_run`. `probe`·`allow_write` 없음
- `Upload` · `Verify deployed` · `Write is off` 단계가 **없다**
- 지문 대조가 **유일한 판정**이라 `|| true` 를 뗐다 — 갈리면 빨갛게 끝난다
- 재는 목록을 넷에서 **`files` 전부(16개)** 로 넓혔다

★ **잠금이 아니라 삭제다.** 잠금은 급할 때 풀리고, 그때 다시 쓰는 이가 둘이 된다.

### ✅ ② ~~엔진 코드를 NAS 에 반영~~ — **본체가 했다 (2026-08-22)**

본체가 `deploy/nas.sh` 로 `fbcfa53` 을 배포했다. **D-82(자료 넣는 길은 로그인을
묻지 않는다)·60MB 한도가 NAS 에 걸렸다.** 본체 판 지문 `202608222046` ·
`verify:nas` 23/23.

### ✅ ③ ~~앱 탭 바 셋 → 둘~~ — **본체가 했다 (2026-08-22)**

탭이 **[완성보고서] · [보고서 만들기] 둘**로 줄었고, **무료 계정에도
[보고서 만들기] 탭이 열렸다.** `TABS` 단일 출처(`flow-core.js`)를 앱 본체가
읽어 간다 — 앱 본체 파일(`linkpilot-platform.deploy.html`)은 이 저장소에 없다.

### 🟠 앱 본체에 넘길 일 — 「외부 분석 AGENT 로 만들기」 블록 삭제 〈2026-08-23 사장님 지시〉

보고서 만들기 탭 아래의 접이식 블록:

> ▼ 외부 분석 AGENT 로 만들기 — 기반정보 내보내기 (예전 경로, 그대로 동작합니다)
>   · 「기반정보를 내보낼 프로젝트」 목록
>   · 「외부 환경」 카드 (LinkPilot AGENT 저장소 · 보고서 생성 AGENT 열기)

**이 저장소에는 없다.** 문구를 전부 훑었는데 0건이다 — `linkpilot-platform.deploy.html`
(앱 본체)이 그린다. 그 파일은 배포 목록에서 일부러 빠져 있다(원본이 여기 없다).

★ `report-flow.html` 안의 **「외부 분석 환경」 카드**는 2026-08-22 에 이미 지웠다.
  이름이 비슷해 헷갈리기 쉽다 — **다른 블록**이다.
★ 지우기 전에 **그 경로를 아직 쓰는지** 확인한다. 「예전 경로, 그대로 동작합니다」
  라고 적혀 있으니 살아 있는 길일 수 있다.

### 🟠 ④ D-10 저장소 private 전환 — 소유자만 가능

Settings → Danger Zone → Change visibility → Make private.
**깨지는 것은 없다**(public 이어야 도는 참조가 하나도 없음을 확인). 전환 뒤 저장소 안의
「public 이다」 주석 정리가 남는다. **시크릿을 코드에 안 두는 규칙은 그대로 둔다.**

### 🟡 ⑤ D-19 아침 크론 이관 — 결정은 끝났고 실행이 남았다

`deploy/크론-이관.sh ~/아침크론` → 새 저장소 `morning-cron`(설명 「아침 크론」) · **Private**.
★ **Secret 은 저장소를 따라오지 않는다.** 안 넣으면 초록으로 뜨고 발송만 조용히 안 된다.

### 🟡 ⑥ 사장님 답을 기다리는 것

| | 어떻게 |
|---|---|
| 조달청 400 원인 | `IM_AGENT_DEBUG_HTTP=1 npm run im:smoke 2>&1 \| grep -A 20 "공사 낙찰"` |
| KOSIS 항목코드 | `npm run im:smoke -- --kosis-tbl DT_1F32002` |
| `.env` 에 `IM_SMOKE_ADDRESS` | 없으면 스모크가 예시 주소로 돈다 |

★ **D-71 (무료 계정 403 문구 정책)** 은 아직 미결정이다 — 본체 회신에서 확인.
★ **PR #6** (draft · 옛 제목 「Pexels 커넥터」) 을 정리할지는 **엔진 세션 결정**.

### ✅ ⑦ ~~NAS 엔진의 `req.destroy()`~~ — **본체가 했다 (2026-08-22)**

`req.destroy()` → **`413` + JSON `{error, limit}`**. 70MB POST 로 413 을 실측했다.
이제 큰 본문에서 화면이 **이유를 말할 수 있다.**

---

## 5. 주의사항 — 다음 세션이 반드시 알아야 할 것

### ⚠️ 「반영이 안 된다」는 **세 가지**다. 갈라 보기 전에 새로고침을 권하지 않는다

| 어디서 갈리나 | 원인 | 할 일 |
|---|---|---|
| 저장소 ≠ 디스크 | 올리기가 안 됐다 | 배포 다시 (M-20 · M-22) |
| **디스크 ≠ HTTP** | **셋 중 하나** — ① 잰 길이 다르다 ② 다른 이가 덮었다 ③ 캐시 | **①부터** 확인. 하나로 단정하지 않는다 (M-25) |
| 셋이 같다 | 브라우저 | 그때 비로소 새로고침 |

**재는 법** `deploy-nas` 를 `dry_run:false` 로 실행 → `Check served build`.
이 셋은 화면에서 **똑같이 보인다.** 내가 세 번 틀린 자리다.

### ⚠️ `deploy-nas` 는 **재기만 한다. 아무것도 쓰지 않는다**

쓰는 단계를 **지웠다** (D-84). 입력은 `files`·`dest`·`dry_run` 셋뿐이고 `dry_run`
기본이 `true` 다. `im-flow` 배포는 **본체 게이트**(`deploy/nas.sh`)가 맡는다.

`Check served build` 가 빨개지면 **로그의 「HTTP 를 받은 길」부터 본다.** 앱이 쓰는
길(Funnel 443)과 다르면 그 값은 **다른 파일의 지문**일 수 있다. **저장소 ≠ 디스크면**
그건 진짜로 안 올라간 것이니 본체 게이트로 다시 올린다.

### ⚠️ 화면 지문으로 판을 가린다

화면 아래 작게 `판 xxxxxxxx`. 지금 값 **`e26243f7`**.
`npm run im:stamp -- --check` 로 확인. **사진에 그 값이 다르면 옛 화면이다.**

### ⚠️ 배포 묶음은 **16개**이고 `required()` 가 단일 출처다

`build-embed.js` 의 `required()`. 워크플로 기본값·작업지시서·`verify:nas` 가 전부
그것을 읽는다. **베껴 적지 말 것** — 실제로 `workorder.test.js` 가 한 벌 더 갖고 있다가
14 대 16 으로 갈렸다.

### ⚠️ 자료 화면은 **탭이 아니지만 배포에는 실린다**

`files.html` 참조가 자바스크립트 안(iframe)에 있어 `<script src>` 훑기에 안 걸린다.
`required()` 가 `FILES_SECTION.file` 을 따로 센다. **이 줄을 지우면 배포는 초록인데
1단계 안이 404** 가 된다.

### ⚠️ 흔들리던 검사 — 크기를 **한 방향으로 줄 세운다**

**손 ≤ 탐침 < 그리기 예산.** 하나만 고치면 다음에 또 흔들린다.
가상 시계는 공짜이므로 아끼지 않는다. (`build-files.js` · `files-tab.test.js` ·
`upload-unread.test.js`)

### ⚠️ 절대 하지 말 것

- `git push --force` · `git reset --hard`
- 시크릿을 코드·로그·커밋에 평문으로 (§2) — **private 이 돼도 그대로**
- 아티팩트 주소를 저장소에 적기 (D-10)
- 모델 식별자를 커밋·PR·주석에 넣기
- **검사를 안 돌리고 push** — 이번 세션에서 두 번 어겼고 두 번 다 CI 가 빨개졌다

### 📌 열린 PR

**#6** (draft) — 제목·본문이 **옛 내용(Pexels 커넥터)** 이다. 173커밋이 쌓여 실제 범위와
많이 다르다. 정리할지는 정해지지 않았다.

---

## 6. 미리보기 주소

- 보고서 만들기 (3단계) — `4dc7680b-0f07-4b49-ab21-dfa0f3c3c118`
- 자료 업로드 화면 — `b49c43dc-6fda-4b2f-9984-24710e57d484`
- 탭 구성안 (탭 2 + 단계 안 1) — `a6a85718-25c5-4709-8099-b2d6d596d6a7`

> ★ **주소 전체를 여기 적지 않는다** — 이 저장소는 public 이다 (D-10).
> 앞은 `https://claude.ai/code/artifact/` 다. 대화에서 그대로 열 수 있다.
