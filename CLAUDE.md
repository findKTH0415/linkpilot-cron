# CLAUDE.md — LinkPilot 보고서 생성 (IM Agent)

이 저장소는 투자설명서(IM)·티저·요약·검증 보고서를 만드는 파이프라인이다.
**출처 없는 숫자는 시스템에 들어오지 못한다** — 그 한 가지가 설계의 전부다.

> ## ⚠️ 범위 변경 (2026-08-14)
>
> **아침 크론(브리핑·모닝카드·한줄생각·법령수집)은 이 저장소에서 제외되었다.**
> `send-morning-*.js` · `fetch-*.js` · `render-card.js` · `fonts/` · 크론 워크플로
> 4개를 삭제했다. 경위는 [`docs/삭제-후보.md`](docs/삭제-후보.md) B군.
>
> **아래 §1·§3 은 크론 사양이라 적용 대상이 이 저장소에 없다.**
> 지우지 않고 남겨 둔 이유는 하나다 — 크론을 다른 저장소로 옮길 때 이 사양이
> 필요하다. 옮길 곳이 정해지면 통째로 가져간다.
>
> **§4 데이터 소스 규칙은 2026-08-16 에 IM Agent 기준으로 개정되었다** —
> 이 저장소가 공공데이터 6종을 실제로 쓰게 되었기 때문이다. 크론 시절 §4 는
> `docs/삭제-후보.md` 에 옮겨 두었다.
>
> **지금 유효한 규칙은 §2 · §4 · §5 · §6 · §7 · §8 · §9 다.**
> (2026-08-16: §8 이 화면 작업으로 들어오면서 미결정 관리가 §9 로 밀렸는데
> 이 줄이 따라오지 않아 §8 을 미결정 관리로 가리키고 있었다.)
>
> 되살리려면: `git log --diff-filter=D -- send-morning-brief.js` 로 삭제 커밋을
> 찾아 `git revert` 한다.

## 1. 프로젝트 컨텍스트 〈크론 — 참고용 보존〉

- 소유: PDI GID (PDI 글로벌 인프라 개발 유한회사) 내부 플랫폼 LinkPilot의 서브 시스템
- 본체 플랫폼: React/Vite 프론트엔드 + Node.js API(포트 8181),
  Synology DS423+ NAS 에서 운영 (주소는 GitHub Secrets — CLAUDE.md §2)
- 이 저장소의 역할: NAS n8n에서 이관된 6AM 모닝 브리핑 크론
  (이관 사유: NAS 불안정 + OAuth 관리 문제 → GitHub Actions로 실행 안정성 확보)
- 연락처 데이터: contacts.json 약 1,145건 (태그: CEO, 투자대상 등)
- 발송 채널: 카카오톡 "나에게 보내기" REST API

## 2. 절대 규칙 (위반 금지)

- 시크릿(카카오 REST API 키, refresh token, 공공데이터 서비스키, NAS 접속정보)은
  코드·로그·커밋 메시지에 절대 평문으로 남기지 않는다. GitHub Secrets만 사용한다.
- 카카오 OAuth refresh token 갱신 로직을 임의로 삭제·단순화하지 않는다.
  (토큰 만료 = 브리핑 발송 전면 중단)
- cron 스케줄은 UTC 기준으로 작성한다. 06:00 KST = `0 21 * * *` (UTC 21:00).
  KST/UTC 변환 실수는 이 저장소의 1순위 사고 원인이므로 수정 시 반드시 재확인한다.
- main 브랜치에 직접 push하는 워크플로우 변경은 로컬 검증(act 또는 dry-run) 없이
  하지 않는다. 실패한 크론은 다음날 아침에야 발견된다.
- 발송 실패 시 조용히 죽지 않는다: 실패 알림(fallback) 경로를 항상 유지한다.
  - **2026-08-24 개정 〈사장님 지시 · D-103〉 — 문자(SOLAPI) 알림을 지웠다.**
    〈번호는 2026-08-25 에 D-99 → D-103 으로 옮겼다 — 저쪽 작업선의 D-99 가
     코드와 결정 기록에 이미 박혀 있어 그쪽이 번호를 지킨다 (D-102)〉
    열쇠 넷이 한 번도 안 들어와 **한 번도 안 나간 채로** 자리만 지키고 있었고,
    유료 서비스라 계정부터 만들어야 했다. **규칙 자체는 그대로다** — 다만 지금의
    실패 통보 경로는 **GitHub 이 실행 실패 시 보내는 메일**이다.
    ★ 그 메일이 안 오는 상태가 되면 **그때가 알림을 다시 만들 때**다.
      「알림이 없다」와 「알림이 있는데 안 온다」는 다른 사실이므로, 지웠다는
      사실을 여기와 `deploy-nas.yml` 두 곳에 적어 둔다.

## 3. 브리핑 사양 (확정 구조) 〈크론 — 참고용 보존〉

섹션 순서 고정 (데이터 없는 섹션은 출력 생략):
1. 오늘의 핵심 요약 (3~5줄, 🔴🟡🟢 신호등)
2. 오늘의 달력 (양력·음력·절기·공휴일 + 금융기관 영업일 여부, 말일/분기말 표시)
3. 오늘 날씨 (서초 기본 + 당일 이동지역 병기, 우산/미세먼지 한 줄 판단)
4. 오늘의 일정 (Google Calendar, 30분 미만 간격 ⚠️ 충돌 경고)
5. 이동 동선 (일정별 출발 권장시각 역산, 대중교통 vs 자동차)
6. 필수 처리 업무 (P1/P2/P3, 전날 미완료 자동 이월, 2회 이월 시 🔴)
7. 신규 접수건 (딜 분류: 부동산PF/태양광/CB/해외, 48시간 무응답 경고)
8. 프로젝트 진행현황 (오늘 액션 있는 건만 상세, D-Day 카운트다운)
9. 전날 통화분석 (후속 액션 추출 → 6번/10번 섹션에 연결)
10. 오늘 연락할 인맥 (팔로업 도래 + 90일 무연락 VIP + 생일·스페셜데이, tel: 딥링크)
11. 시장 지표 (국고채3년·CD금리·환율·SMP·REC·KOSPI)
12. 오늘의 생각한줄 (19종 테마, 요일·스페셜데이 자동 라우팅)
13. 내일 프리뷰 1줄

발송 구조 (카카오톡 200자 제한 대응):
- 풀버전 HTML 생성 → NAS 또는 GitHub Pages에 업로드
- 카카오톡에는 [1. 핵심 요약]만 + 풀버전 링크 발송
- 요일 변형: 월요일 = 주간 프리뷰 추가, 금요일 = 주간 딜 파이프라인 변동 요약 추가

## 4. 데이터 소스 규칙

〈2026-08-16 개정 — 크론 기준에서 IM Agent 기준으로. 구 조항은 `docs/삭제-후보.md`〉

★★★ **열쇠를 새로 붙이거나 다른 프로젝트에 옮길 때는
[`docs/운영지침-공공API-활용.md`](docs/운영지침-공공API-활용.md) 를 따른다**
〈2026-09-01 사장님 지침 — 「공공 API 활용 지침서」 · 같은 날 **v1.3 규정집**으로 갱신〉.

**원문은 그 파일 한 곳에 있다. 여기 옮겨 적지 않는다** — 두 벌이 되면 한쪽이 옛말을 한다
(§8-1 과 같은 결). 아래 §4.1~§4.9 는 **엔진이 그 값을 어떻게 쓰는가**의 규칙이고,
지침서는 **열쇠를 어떻게 넣고 어떻게 부르는가**의 절차서다 — 겹치지 않는다.

그 문서에서 이 저장소의 일과를 바꾸는 것 일곱만 여기 적는다.
★ **절 번호로 가리키지 않는다** — 판이 바뀌면 번호가 움직인다. v1.2 의 인벤토리는 §1,
  v1.3 은 §4 였고, 번호로 찾던 검사가 그 자리에서 네 칸 통째로 빨개졌다. **낱말로 가리킨다.**

- **Secrets 는 다시 읽을 수 없다.** 그래서 키를 꺼내는 대신 **키가 있는 자리(Actions)에서
  대신 호출하고 결과만 커밋**한다. 키는 저장소 밖으로 안 나가고 로그에도 안 남는다.
  ★ 다만 이 저장소는 **공개**다 (D-10) — **결과 파일에 개인정보·키가 섞이지 않았는지**
    커밋 전에 눈으로 센다 (지침서 「이식 체크리스트」 · §2).
- **진단부터 짠다** — 규격을 모를 때 추측으로 파라미터를 넣지 않는다. 첫 스크립트는
  응답 본문을 200자 이상 그대로 저장하고 결과 코드·메시지를 요약에 남긴다.
  ★ R-ONE 은 이것을 안 해서 **여섯 번 다시 썼다.** §4.3 「붙이기 전에 실제로 불러본다」의
    앞 단계다 — 부르기 전에 **무엇이 오는지부터 본다.**
- **서버 필터가 안 먹는 API 가 있다.** R-ONE 의 `CLS_DATANO` 는 무엇을 넣든 같은 결과가
  온다. 그런 곳은 전량 받아 **클라이언트에서 거른다.** 「필터를 넣었으니 걸러졌다」로
  넘어가면 문서에는 「적용됨」만 남는다 (§4.9 와 같은 결).
- **고친 뒤에 `Re-run` 을 누르지 않는다** 〈v1.3 「재실행 함정」〉. GitHub 의 재실행은
  **그 실행이 썼던 커밋의 코드를 그대로 다시 돌린다** — 스크립트를 고쳐 놓고 눌러도
  **옛 코드로 돌아 같은 실패가 반복된다.** 반드시 `Actions` 목록에서 **[Run workflow]** 로
  새로 건다. 화면 위가 `Manually triggered` 인지, 커밋 해시가 최신인지 한 번 본다.
  ★ 이 저장소의 M-31(「걸었다 ≠ 올라갔다」)과 같은 결이다 — **초록인데 아무것도 안 바뀐다.**
- **승인은 키 단위가 아니라 서비스 단위다** 〈v1.3 부록 D — 승인 서비스 16건〉.
  §4.2 가 말하는 그 자리인데, v1.3 은 **활용기간**까지 붙는다고 적는다 — 만료되면
  **키는 멀쩡한데 그 서비스만 거부**되고, 증상이 미승인과 구별되지 않는다.
  그러니 진단 요약에 **서비스 이름과 결과 코드를 함께** 남긴다 (`api-report.js` 가 그렇게 적는다).
- **수집 잡에 접속 자격증명을 넣지 않는다** 〈v1.3 2-8〉. NAS SSH·터널 키는 **배포 잡에만**
  넣는다. 공공 API 를 부르는 워크플로에 섞어 두면, 그 잡이 부르는 **바깥 서버 쪽 오류 본문**에
  섞여 나갈 자리가 생긴다 (§2). `api-smoke.yml` 에는 데이터 열쇠 16개만 있고 접속정보는 없다.
  ★ 예외는 하나 — `VWORLD_DOMAIN` 은 자격증명이 아니라 **등록 도메인 문자열**이라
    `VWORLD_KEY` 와 **함께** 넣는다. 한쪽만 넣으면 거부된다 (§4.1).
- **`_summary.md` 를 반드시 만든다.** JSON 원본은 크고 안 읽힌다. 사람이 읽는 요약이
  있어야 결과 확인이 빠르고, 다른 도구에 넘길 때도 그 파일 하나면 된다.

★★ **「미검증」을 재는 길이 있다 — `Actions → 공공 API 실측 진단`** 〈2026-09-01 신설〉.
  이 자리에는 열쇠가 없어 `im:smoke` 가 전부 「미설정」으로 나온다(§4.3). 그래서
  **열쇠가 있는 자리에서 돌리고 결과만 커밋**한다 — `data/_api/_summary.md`.
  ★ 이 저장소는 **공개**다. `tools/api-report.js` 가 쓰기 전에 **`process.env` 의 실제 값이
    본문에 있는지** 세고, 걸리면 **파일을 안 쓰고 빨갛게 끝난다**. 이름만 말하고 값은 안 찍는다.
  ★★ 「진단이 통째로 죽은 것」과 「한 항목이 실패한 것」을 **갈라 적는다** — 뭉뚱그리면
    무엇을 못 쟀는지 사라진다 (§8 「못 잰 것은 통과가 아니다」와 같은 결).

★ **열쇠 이름이 갈리는 것은 `npm test` 가 잡는다** (`im-agent/test/api-guide.test.js`).
  지침서 표 · 커넥터 · `SECRET_ENV` 셋이 어긋나면 빨개진다. 이름이 어긋나면
  **아무 오류도 안 나고 조용히 값이 죽기 때문**이다 — 실제로 두 번 났다
  (`ECOS_API_KEY`/`ECOS_BOK_KEY` · `LAW_OC`/`LAW_OPEN_DATA`). 답은 **둘 다 읽는 것**이다.

공공데이터는 이 시스템에서 **독립된 두 번째 출처**다. 사업계획서가 말하는 숫자와
공부(公簿)가 말하는 숫자가 다를 때 그것을 드러내는 것이 존재 이유이지, 화면을
채우는 장식이 아니다.

### 4.1 기관과 키

**기관마다 키가 다르고 서로 통용되지 않는다.** 한 곳 키를 다른 곳에 넣으면
인증 오류만 나온다.

| 기관 | 키 | 커넥터 | 쓰는 곳 |
|---|---|---|---|
| VWorld | `VWORLD_KEY` + `VWORLD_DOMAIN` | `vworld` `nsdi` | 지오코딩·지적·토지특성·용도지역·공시지가 |
| 공공데이터포털 | `DATA_GO_KR_KEY` | `molit` `kpx` `fsc` | 실거래가·건축물대장·인허가·REC·법인정보 |
| 기상청 API허브 | `KMA_APIHUB_KEY` | `kma` | 일사량·일조시간 (태양광) |
| 한국부동산원 | `REB_API_KEY` | `reb` | 지가지수 → 공시지가 시점수정 |
| 한국은행 | `ECOS_API_KEY` | `ecos` | 시장금리(기준선)·생산자물가지수(단가 시점수정) |
| 통계청 | `KOSIS_API_KEY` | `kosis` | 제조업 가동률·출하액 → **대조용**(값을 채우지 않는다) |
| 법제처 | `LAW_OC` | `law` | 법령·시행령 조문 원문 · **지자체 건축조례** → 법정 한도의 근거 |

- **`VWORLD_DOMAIN` 은 콘솔의 서비스URL 을 글자 그대로 넣는다.** 스킴·경로를
  벗기면 `ned/*` 계열이 **간헐적으로** 인증을 거부한다(실측 5회 중 2회).
  간헐적 실패는 "가끔 값이 안 들어온다"로만 보여 원인 추적이 가장 오래 걸린다.
- **`LAW_OC` 는 키가 아니라 「OC」다** 〈2026-08-25 사장님 지시 · 신설〉.
  신청에 쓴 **이메일의 `@` 앞부분 그대로**다. 그리고 **사용 승인이 따로** 있어야
  돈다(신청 후 1~2일) — 승인 전 오류가 **OC 오타와 구분되지 않으므로**
  `law.js` 의 `diagnose()` 가 승인·OC·없음·형식 넷으로 갈라 준다.
  ★ 짧다고 공개해도 되는 값이 아니다. 그 값이 곧 **승인된 이용자 식별자**다 (§2).
  ★★ **시행령 상한을 조례 한도로 쓰지 않는다.** 용적률·건폐율의 실제 한도는
  조례가 정한다 — `law.ordinance()` 로 그 조례를 찾아 근거를 붙인다.
- **`DATA_GO_KR_KEY` 는 Decoding(일반) 인증키다.** Encoding 키는 한 번 더
  인코딩되어 인증만 실패하고, 증상이 "키가 틀렸다"와 구분되지 않는다.
- 새 Connector 를 붙이면 `connectors/http.js` 의 `SECRET_ENV` 에 키 이름을 더한다.
  거기 없으면 로그·오류 메시지에 평문으로 남을 수 있다 (§2).

### 4.2 활용신청은 API 하나하나에 필요하다

키가 유효해도 신청하지 않은 API 는 거부된다. **키 문제와 구분하려면 응답 본문을
봐야 한다** — 상태코드만 보면 둘이 같아 보인다.

| 응답 | 뜻 |
|---|---|
| `SERVICE_KEY_IS_NOT_REGISTERED_ERROR` (30) | 그 API 에 활용신청이 안 됨 |
| `NO_OPENAPI_SERVICE_ERROR` (12) | 엔드포인트가 없거나 폐기됨 |
| `403 활용신청이 필요한 API` (API허브) | 위와 같음 |

### 4.3 붙이기 전에 실제로 부르고, 보고서까지 돌려 본다

검증은 **두 단계**다. 하나만 하면 반쯤 틀린 채로 나간다.

**① 스모크 — 커넥터가 응답을 받는가**

`npm run im:smoke` 로 실측하고 응답 필드가 기대와 같은지 확인한 뒤 배선한다.
스모크에 항목을 추가하는 것이 커넥터 작업의 일부다.
이 단계가 없던 동안 폐기된 엔드포인트(`BldRgstService_v2`)를 붙잡고 있었다.

★★ **스모크로 「운영 키가 들어 있는지」를 재지 않는다** 〈2026-08-25 · 실측〉.
열쇠는 **GitHub Secrets 와 NAS 의 열쇠 파일**에만 있고, 이 저장소나 개발
컨테이너에는 없다. 그래서 여기서 `im:smoke` 를 돌리면 **전부 「미설정」**이
나온다(실측: `0/0 통과`) — 키가 멀쩡히 들어 있어도 그렇다. **없는 것과
구분이 안 된다.**

- 스모크가 재는 것은 **「이 커넥터가 응답을 제대로 읽는가」**다. 키가 있는
  자리에서 돌려야 뜻이 있다.
- **운영 키가 실제로 들어 있고 살아 있는지**는 배포가 잰다 —
  `deploy-nas` 의 「Check map power」·「Why is the key not read」가
  `tools/env-doctor.js --keys` · `--live` 로 **NAS 안에서** 묻는다.
  이름과 길이만 찍고 값은 한 글자도 안 남긴다 (§2).

**② 파이프라인 — 그 값이 보고서에 실리는가**

`npm run im:demo` 를 돌리고 **세 곳을 눈으로 확인한다.**

1. `01_Project/dataset.json` 의 `facts` — 값이 들어갔는가, 출처가 맞는가
2. `09_IM/im.md` 본문과 출처표 — **실제로 문서에 나오는가**
3. Agent 경고 로그 — 조용히 건너뛴 것이 없는가

**스모크 통과는 보고서 반영을 뜻하지 않는다.** 2026-08-16 에 스모크 14/14 인
상태로 파이프라인을 돌렸더니 셋이 나왔다 — 시점수정이 통째로 빠져 있었고
(`slice(0,6)` 로 `'20260'`), 엉뚱한 지역의 지수로 보정하고 있었고
(통계표별 지역코드 체계 차이), 지역·지구가 `dataset` 에만 있고 IM 에는
없었다(`im-outline.js` 의 `dataKeys` 누락).

**셋 다 결과가 그럴듯했다.** 평가액은 정상으로 나오고 출처 표시도 멀쩡했다.
로그를 보지 않으면 알 수 없다. 그래서 값이 하나 늘 때마다 `dataKeys` 와
경고 로그를 함께 본다.

### 4.4 응답 정렬 방향을 먼저 확인한다

**국내 공공 API 는 오래된 것부터 주는 경우가 많다.** 개별공시지가·토지특성·
REC 현물시장·지가지수가 그랬다. 앞에서부터 집으면 몇 년 전 값을 최신으로 쓰고,
**출처 표시는 멀쩡해서 문서만 봐서는 안 잡힌다.**

- 최신값이 필요하면 **정렬을 직접 하거나 조회 기간을 명시한다.**
- 기간 누적이 필요하면 **변동률을 곱해 쌓지 말고 지수의 비를 쓴다.**
  변동률 누적은 구간 경계에서 한 달을 더 세기 쉽다.

### 4.5 호출을 늘리지 않는다

- data.go.kr 계열은 **일 10,000건 한도**다. `connectors/cache.js` 를 반드시 경유한다.
- **이미 채워진 값을 다시 부르지 않는다.** 예: 건축물대장이 대지면적을 채웠으면
  토지특성을 호출하지 않는다.
- 자산군 전용 조회는 **해당 딜에서만** 부른다 (일사량·REC 는 태양광 딜만).
- 목록·색인처럼 큰 조회는 TTL 을 길게 잡는다 (지역 색인 180일).

### 4.6 실패는 격리한다

3회 재시도(1s/2s/4s) 후 예외 대신 `{ok:false}` 를 돌려준다. **한 소스가 죽어도
IM 생성 전체를 죽이지 않는다.** 인증·요청 오류(400/401/403/404)는 재시도하지 않는다.

**키가 없으면 `unavailable` 을 반환하고 그 절을 비운다. 지어내지 않는다.**

### 4.7 출처에 무엇을 적는가

값만 옮기지 않는다. **그 값이 어디서·언제·어떤 조건으로 나왔는지**를 함께 남긴다.

- 기관명과 기준시점 — 예: `개별공시지가(2026년 고시)`
- **대리값이면 그 사실** — 일사량은 부지가 아니라 관측소에서 잰 값이므로
  `관측소명 · 부지에서 N km · 관측연도` 를 적는다. 거리를 모르면 **모른다고 적는다.**
- **모수** — 평균값은 표본 수와 기간을 함께 적는다 (`99회 개장`, `47건 중앙값`).
- 결측이 있으면 결측 일수를 적는다. 결측을 0 으로 세면 합계가 조용히 작아진다.

### 4.8 가정이 섞이면 자동으로 내지 않는다

출처 있는 값끼리의 계산만 자동화한다. **가정계수가 하나라도 들어가면 사람이 넣는다.**

- 일사량은 낸다. **발전량은 내지 않는다** — 시스템효율(PR)이 가정이다 (D-25).
- REC·SMP 단가는 낸다. **매출은 내지 않는다** — 가중치와 발전량이 가정이다.
- 공시지가 시점수정은 **공표 통계라 가정이 아니다.** 현실화계수는 가정이므로
  `assumption` 에 그 구분을 적는다.

### 4.9 조회는 되지만 값을 못 쓰는 경우

**자동으로 하나를 고르지 않는다.** 틀렸을 때 문서만 보고는 잡히지 않기 때문이다.

- 동명 법인(`삼성물산` 은 실제로 2곳) → 후보를 내고 사람이 특정한다
- 한 필지의 인허가 이력 여러 건 → 이력을 보여 주고 사람이 고른다
- 지역을 특정하지 못한 시점수정 → **전국 값으로 대체하지 않고 적용을 포기한다**

대체값으로 메우면 문서에는 "적용됨"만 남아 무엇이 빠졌는지 사라진다.

## 5. 코드 작업 규칙

- 코드는 부분 스니펫이 아닌 완전한 작동 파일 단위로 작성한다.
- 수정 전 관련 파일을 먼저 읽고, 기존 패턴(모듈 구조, 에러 처리 방식)을 따른다.
- 버그·엣지케이스는 지적받기 전에 선제적으로 찾아 보고한다.
- 새 라이브러리 추가는 최소화한다. 표준 라이브러리와 기존 의존성을 우선한다.
- 스택 변경(예: 다른 CI, 다른 런타임) 제안 금지.
- 타임존 처리는 항상 명시적으로 한다 (Asia/Seoul 지정, 서버 로컬타임 의존 금지).
- 답변은 한국어, 결론 먼저, 실행 가능한 단계로 정리한다.
- **안내할 때는 쉽게 풀어서 말한다** 〈2026-08-21 사용자 지시〉.
  - 사용자가 **직접 손으로 해야 하는 일**(설정·등록·설치)을 안내할 때 특히 그렇다.
    코드 이야기는 짧게 해도 되지만, 사람이 화면을 눌러 가며 따라 하는 일은
    **모르는 낱말이 하나만 나와도 거기서 멈춘다.**
  - 낯선 말은 **처음 나올 때 한 줄로 풀어 준다** — OAuth·ACL·tailnet·개인키처럼.
    「아는 사람은 아는 말」로 쓰지 않는다.
  - **무엇을 위해 하는지 먼저** 말하고 그다음에 절차를 준다. 이유를 모르면
    중간에 막혔을 때 스스로 판단할 수가 없다.
  - **어디를 눌러야 하는지**를 적는다 (메뉴 이름 그대로). 「설정에서 등록한다」는
    안내가 아니다.
  - 막히기 쉬운 곳은 **미리** 「여기서 대개 막힌다」로 짚는다.
- **대화 전체를 쉬운 말로 한다** 〈2026-08-26 사용자 지시 — 위 규칙을 넓힘〉.
  - 위 규칙은 「손으로 해야 하는 일을 안내할 때」였다. **이제 모든 답변에 적용한다.**
    보고·분석·기술 설명도 마찬가지다.
  - **낯선 말을 쓰지 않는 것이 아니라, 쓰고 나서 바로 풀어 준다.**
    없앨 수 없는 말이 있다 — 파일 이름·명령어·오류 메시지는 그대로 적어야
    사장님이 화면에서 찾으신다. 대신 **그 옆에 한 줄로 뜻을 붙인다.**
  - 짧게 쓰는 것보다 **알아듣게 쓰는 것이 먼저다.** 줄이려다 뜻이 사라지면
    그 줄은 없는 것과 같다.
  - **비유를 하나쯤 쓴다.** 「합집합」보다 「둘을 합쳐 중복을 뺀 것」이 낫다.
  - 다만 **숫자와 사실은 줄이지 않는다.** 쉬운 말로 바꾸는 것이지
    뭉뚱그리는 것이 아니다 (출력 스타일 §「짧게 쓰지 않는 곳」과 같은 뜻).
  - **붙여 넣을 명령에 「바꿔 넣을 자리」를 두지 않는다** 〈2026-08-23 · 세 번 당했다〉.
    - `사용자@주소` 같은 예시를 넣으면 **그대로 붙여 넣어 실행된다.** 실제로
      세 번 그랬고, 그때 나오는 말은 `hostname contains invalid characters`
      뿐이라 **예시를 안 바꿨다는 생각 자체가 안 든다.**
    - ★ 그러니 **바꿀 것이 없는 길을 먼저 찾는다.** 같은 답을 주는 로컬 경로가
      있으면 그것을 준다 — SSH 를 안 쓰면 주소도 필요 없다.
    - 정말 값이 필요하면 **환경변수로 한 번만 받고**(`export LP_NAS_HOST=…`),
      명령 자체에는 그 이름만 쓴다. 그리고 예시가 그대로 들어온 것을
      **스크립트가 잡아 사람 말로 알려 준다** (`deploy/screens.sh` 가 그 예다).
  - **읽으라고 준 것과 실행하라고 준 것을 섞지 않는다** 〈2026-08-23 · 실제로 당했다〉.
    - 「이러면 틀린 것 / 이러면 맞는 것」 같은 **비교표를 명령처럼 생긴 줄로**
      적었더니 사장님이 그것을 터미널에 붙여 넣으셨다. 표에는 `←` 화살표까지
      있었는데도 그랬다 — **명령처럼 생기면 명령으로 읽힌다.**
    - ★ 비교·설명은 **표나 문장으로** 적고, 코드블록은 **실제로 칠 것에만** 쓴다.
      값을 비교해야 하면 코드블록 대신 「`IM_AGENT` 가 `/volume1/linkpilot/…` 로
      되어 있으면 틀린 것입니다」처럼 **문장 안에 인용**한다.
    - ★★★ **남에게 전달할 코드도 코드블록에 넣지 않는다** 〈2026-08-23 · 같은 날
      두 번째로 당했다〉. 본체 개발자에게 넘길 자바스크립트 한 줄
      (`window.LINKPILOT_EMBED = {…}`)을 코드블록으로 적었더니 사장님이
      **터미널에 붙여 넣으셨다** — `zsh: parse error near '}'`.
      - **내가 「이건 본체가 코드에 넣을 줄입니다」라고 적어 두어도 소용없다.**
        코드블록은 **누가 어디에 넣을 것인지와 무관하게** 「여기 복사해서
        실행하라」로 읽힌다. 사장님 자리에서 실행할 수 있는 곳은 터미널뿐이다.
      - ★ 그러니 전달할 코드는 **파일로 만들어** `docs/` 에 두고
        **「이 파일을 본체에 전달해 주십시오」**로 준다. 답변 본문에는
        **한 글자도 코드로 적지 않는다.**
      - 정 답변에 보여야 하면 **문장 안에 짧게 인용**한다 — 예: 「`LINKPILOT_EMBED`
        전역에 `api` 와 `token` 을 채워 달라고 전해 주십시오」.
  - **마우스로 되는 길이 있으면 그쪽을 먼저 준다** 〈2026-08-23〉.
    터미널은 오타·셸 차이(zsh 의 glob·따옴표)·예시 미치환이 겹쳐 **막히는 자리가
    셋**이다. File Station 처럼 눌러서 되는 길이 있으면 그것을 먼저 적는다.

## 6. 출력물 스타일

### 6-1. 그림은 SVG 와 JPEG 을 **둘 다** 낸다 〈2026-08-16 규칙〉

- **SVG 를 만들면 같은 이름의 JPEG 을 반드시 함께 만든다** (`core/raster.js` 의
  `alongside()`). SVG 를 지우지 않는다 — 벡터라 인쇄에서 안 깨지고 글자를 고를 수 있다.
- 왜: **SVG 는 브라우저에서만 확실히 열린다.** 카카오톡·메일 미리보기·파워포인트
  붙여넣기·인쇄 대화상자에서는 빈 칸이 되거나 아예 안 뜬다. 그림을 보라고 보냈는데
  빈 칸이 오는 것은 안 보낸 것보다 나쁘다 — 고장으로 읽힌다.
- **라이브러리를 들이지 않는다.** sharp·canvas·resvg 는 네이티브 의존성이라 §5 에
  걸린다. 이미 쓰는 **헤드리스 크로미움**이 확장자만 보고 JPEG 을 써 준다.
- **브라우저가 없으면 조용히 넘어가지 않는다.** 없는 서버가 실제로 있다 — 그때는
  경고와 YELLOW 플래그를 남긴다. 「JPEG 이 있어야 하는데 없다」를 아무도 모르는
  상태를 만들지 않는다.
- 인쇄까지 견디게 **2배 해상도**로 그린다 (`DEFAULT_SCALE`).

### 6-1-1. 설계 도면은 도면 규격으로 그린다 〈2026-08-25 사장님 지시 · M-53〉

「배치도·평면도·단면도」를 달라는 말은 **개념도가 아니라 도면**을 달라는 뜻이다.
설계사무소 납품물과 개념도를 가르는 것은 그림 솜씨가 아니라 **아래 일곱**이다.

- **도면틀 + 표제란** — 프로젝트명·도면명·도면번호·축척·작성일·개정란.
  대외 도면에는 `NOT FOR CONSTRUCTION` 과 §6-2 의 기밀 표기를 함께 박는다.
- **축척이 진짜여야 한다.** 시트를 A2(594×420) 같은 실제 규격으로 두고
  **도면 단위를 mm 로** 잡는다. 그러면 `stroke-width:0.7` 이 곧 0.7mm 이고
  `font-size:2.5` 가 도면 글자 2.5mm 다 — 자를 대면 맞는다.
- **선 굵기 위계** — 절단선 0.7 / 외곽 0.5 / 일반 0.35 / 보조 0.25 / 치수 0.13.
  도면의 표정은 색이 아니라 여기서 나온다.
- **치수선** — 보조선 + 사선(45°) 마감 + 치수값, 전체·주간·세부 여러 단.
  화살표는 기계도면이고 건축은 사선이다. 치수는 **mm**로 적는다(120,000).
- **통심선(구조 격자)** X1·Y1 버블 · **방위표** · **레벨 ▽** · **절단선 A-A'**.
- **해칭·먹칠** — 콘크리트·토양·조경. 다만 **축척이 표현을 정한다**:
  1:500 의 슬래브는 0.5mm 라 무늬가 안 들어간다 → 먹칠(poche).
- **면적표** — 그린 사각형에서 **계산해** 찍는다. 손으로 적지 않는다.

★ **도면은 다크 모드에서도 흰 종이다.** 검은 바탕 도면은 실무에 없다.
★ **그리고 네 장을 눈으로 본다.** 도면은 오류를 안 내고 틀린다 (M-53).

### 6-1-2. 투자자에게 가기 전 「거르는 팩」의 규격 〈2026-08-25 사장님 지시 · M-54〉

사장님 지시: **「실제 투자자에게 사업을 보여주기 전 LinkPilot 이 자동으로 사업을
걸러내는 수준」**. 그래서 도면·모델은 최종 산출물이 아니라 **DD 와 CAPEX 검토의
원재료**다. 이 관점이 규격을 정한다.

- **슬라이드는 16:9 고정.** PPT 에 그대로 들어가야 하므로 `aspect-ratio:16/9` 로
  못박고 SVG viewBox 는 `0 0 1920 1080` 을 쓴다. 도면(A2)과는 다른 규격이다.
- **자동·반자동·사람을 색으로 갈라 적는다.** 초록=자동 · 주황=반자동 · 회색=사람.
  ★ **회색 칸이 곧 실사 항목이다** — 비어 있음을 숨기지 않는 것이 이 팩의 값이다.
- **자동화 등급은 「지금 배선된 커넥터」 기준으로 적는다.** 계획을 적으면 그 순간
  이 팩은 거짓이 된다. 없는 것은 「사람」이다 (§4.8 — 가정계수가 들어가면 사람).
- **DD 는 여섯 갈래다** — 법률 · 토지 · 인허가·행정 · 기술 · 공사비내역 산출 ·
  수요시장 트렌드. 자본 흐름은 투자자 → SPC → 토지/인허가/자산 → 사업개발 →
  CAPEX → Financing.
- **표지 포스터는 글자 뒤에 스크림을 깐다.** 없으면 수치가 건물 위에 얹혀 안 읽힌다.
  장식이 아니라 가독성 장치다 (§6-3 ④).
- **포스터 이미지는 모델에서 뽑는다** — 렌더 함수를 창구로 내보내고 헤드리스로
  1920×1080 을 찍어 심는다. 모델 코드를 두 벌로 만들지 않는다.

### 6-2. 브리핑 HTML

- 브리핑 HTML은 모바일 우선(카카오톡 인앱 브라우저 기준)으로 작성한다.
- 폰트 Arial/맑은 고딕, 포인트 컬러 #C00000.
- 모든 항목은 액션 가능하게: 일정→지도 링크, 인맥→tel: 딥링크, 프로젝트→문서 링크.
- 브리핑은 내부용이므로 이모지 사용 가능. 단, 대외 발송 문서 생성 기능을
  이 저장소에 추가할 경우 PDI 하우스 스타일(이모지 금지, 개인 성명 미표기,
  "Strictly Private and Confidential" 헤더)을 적용한다.

### 6-2-1. 대외로 나가는 문서는 **HTML 주소 + PDF 둘 다** 낸다 〈2026-08-31 사장님 지시〉

사장님 지시 그대로다 — 「**외부신청은 항상 HTML 파일을 만들어 url 연결 열어줘 그리고 pdf 파일로 만들어줘**」.

밖으로 나가는 문서(투자제안·IM·티저·제출서류)는 **둘 다** 낸다. 하나만 내지 않는다.

- **왜 둘 다인가** — 받는 사람의 자리가 둘이라서다. 주소는 **지금 바로 열어 보는 길**이고,
  PDF 는 **메일에 붙이고 인쇄해서 회의실에 들고 들어가는 길**이다. 주소만 주면 메일에 못 붙이고,
  PDF 만 주면 고칠 때마다 새 파일이 돌아다녀 **어느 것이 최신인지 알 수 없게 된다.**
- ★ **인쇄용 파일을 따로 만들지 않는다.** 같은 HTML 하나에 `@media print` 와 `@page` 를 넣어
  거기서 PDF 를 뽑는다. 두 벌로 만들면 §6-1-2 와 같은 결로 **한쪽이 옛말을 한다** —
  화면은 고쳤는데 PDF 는 안 고친 상태가 눈에 안 보인다.
- ★★★ **`npm run im:pdf` 로 뽑는다 — 손으로 조립하지 않는다** 〈2026-08-31 사장님 지시:
  「npm run im:pdf 명령으로 만들어줘」〉. 앞 판은 규칙만 적어 두었는데, 그러면 그 규칙이
  **사람의 기억에 얹힌다** — 이 저장소가 그 자리에서 이미 여섯 번 빠뜨렸다 (M-31).
  `npm run im:pdf -- <입력.html>` 하나로 글꼴 갖추기·뽑기·검사까지 돈다.
  라이브러리를 들이지 않는다 — 헤드리스 크로미움만 쓴다 (§6-1 과 같은 이유).
- ★★ **웹폰트가 안 잡히면 PDF 가 화면과 다른 글꼴로 나온다.** 이 컨테이너에서는 크로미움이
  Google Fonts 에 못 닿는다(프록시가 크로미움에 안 걸린다) — 화면은 멀쩡한데 **PDF 만 조용히
  대체 글꼴로 나온다.** `im:pdf` 가 뽑기 전에 글꼴을 내려받아 `~/.fonts` 에 깔고 `fc-cache -f`
  를 돌린다. 오래된 User-Agent 로 CSS 를 받으면 쪼개진 woff2 대신 **TTF 한 벌**이 온다.
  - ★★★ **「깔렸는가」와 「그 이름으로 쓰이는가」는 다른 사실이다** 〈2026-08-31 · 실측〉.
    `fc-list` 를 글자로 훑어 「IBM Plex Mono」가 있으면 있다고 봤는데, 굵기 500 짜리 TTF 는
    자기 이름을 **「IBM Plex Mono Medium」**으로 등록한다. 글자로는 맞는데 브라우저가
    `font-family:"IBM Plex Mono"` 로 물으면 fontconfig 가 못 찾아 **DejaVu Sans Mono 로
    바꿔치기했다.** 검사는 초록인데 PDF 의 숫자만 다른 글꼴이었고, **그 상태로 두 번 내보냈다.**
  - ★ 그래서 **`fc-match` 로 묻는다** — 브라우저와 같은 방식이다. 돌아온 이름이 물어본 이름과
    다르면 없는 것이다. `fc-list` 는 「깔려 있는가」를, `fc-match` 는 「그 이름으로 쓰이는가」를 잰다.
- ★★★ **뽑고 나서 셋을 확인한다** (`im:pdf` 가 자동으로 잰다): 쪽수가 0 이 아닌지,
  A4(210×297mm)가 맞는지, **PDF 안에 그 글꼴이 실제로 박혔는지**. 셋째가 이 도구의 급소다 —
  앞의 둘은 통과하면서 글꼴만 틀린 PDF 가 실제로 나왔다. **표와 카드는 쪽 경계에서 쪼개지지 않게**
  `break-inside:avoid` 로 묶는다 — 표 한 줄만 다음 장에 넘어간 문서는 대충 만든 것으로 읽힌다.
- ★★ **PDF 가 화면보다 옛말을 하지 않는지 `guard` 가 잰다** 〈2026-08-31 · 열한째 칸 신설〉.
  주소와 PDF 는 **만드는 시점이 다르다** — HTML 을 고쳐 다시 발행하면 주소는 새 판이 되는데
  `im:pdf` 를 다시 안 돌리면 **PDF 만 옛 판으로 남고, 그 상태가 아무 오류도 안 낸다.**
  M-25(화면 지문 어긋남)와 같은 결이라 같은 방식으로 막는다 — `tools/pdf-fresh.js` 가
  짝을 이룬 HTML·PDF 의 나이를 재고, PDF 가 뒤처지면 `guard` 가 빨개진다.
- ★ **밖으로 보낼 때는 수령자 번호를 박는다** — `npm run im:pdf -- <입력.html> --to "○○캐피탈"`.
  기밀 문서는 **어디로 새는지가 문제**인데, 똑같은 파일을 열 곳에 보내면 한 부가 돌아다녀도
  어디서 나온 것인지 알 방법이 없다. 받는 곳마다 다른 번호를 박아 두면 그때 갈린다.
  - 번호는 **수령자·파일·날짜를 해시로 접은 것**이다. 순번(001·002)을 쓰지 않는다 —
    받는 쪽이 「나는 몇 번째인가」를 알게 되고, **그것 자체가 정보**다.
  - **원본 HTML 은 안 고친다.** 임시 사본에만 찍는다 — 원본을 고치면 아티팩트에
    남의 이름이 박힌 채로 올라간다.
  - 박을 자리(기밀 머리)가 없으면 **빨갛게 끝난다.** 도장 없는 PDF 가 나가면
    「번호를 박았다」고 믿은 채로 추적 불가능한 사본이 돈다.
- 답변에는 **주소를 먼저, 파일을 그다음에** 적는다 (§8 — 기본 전달은 주소로 여는 페이지).

### 6-2-2. 투자조건은 **RCPS 가 기본값**이다 〈2026-08-31 사장님 지시〉

사장님 지시 그대로다 — 「**IM 투자조건(형태·밸류에이션·회수)은 특별한 지시 없으면 RCPS 규정해줘**」.

- **형태**는 **상환전환우선주(RCPS)** 로 쓴다. 보통주로 바꿀 권리(전환권)와 원금 상환을 요구할
  권리(상환권)를 함께 갖는 우선주다 — **상방은 보통주처럼, 하방은 채권처럼** 갖는다.
- ★ **왜 기본값을 두는가.** 앞 판은 조건이 비어 있으면 「협의 사항」으로 두고 넘어갔다.
  그러면 IM 이 「좋은 회사네요」에서 멈추고 **다음 미팅으로 안 넘어간다** — 투자자는
  조건이 없는 제안을 검토 대상으로 올리지 못한다. 매번 물어보지 않아도 되게 기본값을 둔다.
- **적는 것은 셋이다.** 형태(RCPS 표준 조항) · 밸류에이션(투자 전 기업가치별 지분율) ·
  회수(IPO · M&A · 상환권 · 동반매도).
- ★★ **숫자를 정해진 값으로 쓰지 않는다.** 우선배당·상환이자·전환비율은 협상으로 정해진다.
  통상 범위를 적고 **「협의」 배지**를 붙인다 — 정해진 것처럼 적으면 §4.8(가정이 섞이면
  자동으로 내지 않는다)을 어기는 것이고, 첫 미팅에서 바로 깨진다.
- ★ **밸류에이션은 제안하지 않고 계산해 보인다.** 투자금을 고정하고 투자 전 기업가치별
  지분율을 표로 낸다 — 산술이라 틀릴 자리가 없고, 협상의 출발점이 눈에 보인다.
- ★★★ **상환권에는 반드시 단서를 함께 적는다.** 상법상 상환 재원은 **배당가능이익**으로
  한정된다. 회사가 결손이면 상환을 요구해도 못 받는다. 「하방이 막혀 있다」는 말은
  **회사가 이익을 내고 있을 때만 참이다** — 이것을 안 적으면 문서가 거짓말을 한다.
- 사장님이 다른 형태(보통주·CB·대출)를 지시하시면 그것을 따른다. 이 절은 **지시가 없을 때**의 값이다.

### 6-2-3. **산출물을 만들기 전에 출력 방식을 옵션으로 묻는다** 〈2026-08-31 사장님 지시〉

사장님 지시 그대로다 — 「**출력방식 조건을 내게 옵션으로 묻기**」. 물을 것은 다섯이다.

1. **가로 · 세로**
2. **PDF · PPT · 워드 · 한글**
3. **미리보기**
4. **다운로드 · 카카오톡 전달**
5. **조감도 포함**

- ★ **왜 묻는가.** 앞 판은 내가 정했다. 그런데 **틀렸을 때 드는 값이 크다** — 12쪽짜리
  세로 문서를 다 만든 뒤에 「PPT 로 가로였어야 한다」가 나오면 처음부터 다시 짜야 한다.
  **묻는 데 드는 것은 한 번의 물음이고, 안 묻고 틀리면 한 벌을 통째로 버린다.**
- ★★ **이것은 `core/outputspec.js` 의 앞문이다.** 거기 이미 같은 뜻이 적혀 있다 —
  「AI 가 페이지 수·크기·방향·파일형식·언어·파일명을 임의로 정하지 않는다 ·
  사양이 LOCK 되기 전에는 최종 산출물을 만들지 않는다」. 그 규칙이 **엔진 안에만**
  있어서 **대화에서 내가 바로 만들 때는 안 걸렸다.** 이 절이 그 자리를 메운다.
  기본값은 `PRESETS` 를 그대로 쓴다 — 새로 만들지 않는다.
- ★★★ **초안(DRAFT)까지는 물어보기 전에 만들어도 된다.** 묻느라 아무것도 없는 채로
  기다리게 하지 않는다 (§「Delivering work」와 같은 결). 다만 **최종본은 답을 받고 만든다.**
- **답이 없으면 기본값으로 가되 무엇을 골랐는지 적는다.** 사장님이 물음을 지나치실 수 있다 —
  그때 멈춰 있으면 일이 안 나간다. 「세로·PDF·미리보기·조감도 없음으로 만들었습니다」처럼
  **고른 값을 답변에 적어** 다르면 한 마디로 고칠 수 있게 한다.
- ★ **조감도는 근거가 없으면 켜도 안 나온다** (§7 · `outputspec.js` 의 `VISUAL_DEFAULT`).
  「포함」을 고르셔도 지적 필지 형상이 없으면 건너뛰고 사유를 남긴다 — **켰다고 그림이
  나오는 것이 아니다.** 그 사실을 물을 때 함께 적는다.
- ★★ **한글(HWP)·워드·PPT 는 이 저장소가 아직 못 만든다** 〈2026-08-31 실측〉.
  지금 나가는 것은 **HTML 주소와 PDF** 뿐이다 (§6-2-1). 고르시면 **못 만든다고 말하고**
  가장 가까운 것(PDF)을 낸다 — 조용히 PDF 로 바꿔치기하지 않는다.

### 6-3. 산출물은 **엣지 있게** — 다만 장식으로가 아니다 〈2026-08-22 사용자 지시〉

「좀 더 엣지 있게」는 **더 꾸미라는 뜻이 아니다.** 이 저장소의 산출물이 밋밋한
이유는 색이 적어서가 아니라 **똑같이 생겨서**다. 엣지는 아래 넷에서 나온다.

**① 첫 화면이 주장을 한다.** 제목 + 요약 문단으로 시작하지 않는다. 그 자리에
**가장 중요한 판정 하나**를 크게 놓는다 — 「됩니다/안 됩니다」, 「366ms → 2358ms」.
읽는 사람이 스크롤하기 전에 답을 갖게 한다.

**② 숫자를 문장에 숨기지 않는다.** 잰 값은 표·큰 글씨·대비로 **보이게** 한다.
「빨라졌습니다」는 엣지가 없고 「366 → 2358」은 있다.

**③ 활자로 승부한다.** 크기 단계를 분명히 두고(제목과 본문이 1.5배 이내로
붙지 않게), 제목은 자간을 좁히고, 숫자는 `tabular-nums` 로 세로를 맞춘다.
**색을 더 쓰는 것보다 활자를 정리하는 쪽이 언제나 더 크게 달라진다.**

**④ 대비는 한 곳에만.** 강조를 두 군데 이상 주면 둘 다 죽는다.

**⑤ 표 셀은 한 줄 — 엑셀의 자동 맞춤처럼** 〈2026-08-25 사용자 지시 · M-50〉.
셀 안에서 줄바꿈이 생기면 행 높이가 들쭉날쭉해 표가 안 읽힌다.
`td { white-space: nowrap }` 로 셀을 한 줄로 고정하고, 폭이 모자라면 셀을
접는 대신 **표를 가로 스크롤**시킨다 (감싸개 `overflow-x: auto`).
한 줄에 안 들어갈 긴 설명은 셀에 넣지 않는다 — 표 밖 문단·각주로 옮긴다.

#### 하지 않는 것 — 넷

- **라임(`--lime`)으로 큰 면을 채우지 않는다.** 디자인 시스템 §11 이다.
  강조에만 쓴다. 채우면 「완료」로 읽혀 뜻이 흐려진다.
- **의미 없는 이모지·아이콘을 넣지 않는다.** 뜻을 나르지 않는 그림은 잡음이다.
  대외 발송 문서는 §6-2 대로 **이모지 금지**가 그대로 유효하다.
- **없는 데이터를 그럴듯하게 그리지 않는다.** 예시면 예시라고 화면에 박는다 (§8).
- **애니메이션을 장식으로 넣지 않는다.** 진행 그래프처럼 **상태를 나르는**
  움직임만 둔다. `prefers-reduced-motion` 을 존중한다.

#### 어디에 적용되나

`npm run im:*` 로 나오는 미리보기·아티팩트, 배포하는 화면, 그리고 보고서
아티팩트. **엔진이 만드는 IM·티저 본문은 여기서 제외한다** — 그쪽은 PDI 하우스
스타일과 출력 사양(`core/outputspec.js`)이 정한다.

## 7. 세션 운영

- **SketchUp 갈래 작업은 [`docs/작업지침서-스케치업-엔진-운용.md`](docs/작업지침서-스케치업-엔진-운용.md) 를
  따른다** 〈2026-08-25 사용자 지시 — 「분석하고 작업시 반영」〉. 특히 셋:
  ① 내보내기 전 검사는 `npm run guard` 하나로 돈다(따로 조립하지 않는다),
  ② **새 이름·D/M 번호는 열려 있는 갈래와 이 운용서를 먼저 보고 딴다** —
  같은 날 두 번 깨진 자리다(D-102·D-104/D-105 재번호),
  ③ `im:smoke` 를 키 없는 자리에서 돌려 운영 열쇠를 판정하지 않는다 —
  실키 스모크는 NAS 처럼 키 있는 자리에서만 뜻이 있다.
- **실사(AI) 조감도 렌더의 표준은 Veras 4.0 (Nano Banana Pro 기반)이다 — 반드시**
  〈2026-08-25 사장님 지시〉. 값은 `core/outputspec.js` 의 `RENDER_STANDARD`
  한 곳에만 있다 — 계획 Agent 가 계획 파일에 실어 알리고, 수령 Agent 가
  비표준 도구·세대에 `RENDER_TOOL_NONSTANDARD` YELLOW 를 낸다 (규격 §3-1).
  무료 경로는 gemini + 같은 기반 모델만 표준으로 친다.
  ★ **표기가 온전한 렌더는 IM 본문에도 싣는다** 〈2026-08-25 사장님 지시 —
  D-34 2차 개정〉. 앞 판의 「표지·티저 한정」은 풀렸다. 대신 **순서와 표기로**
  막는다 — 근거(매스 표·기하 조감도)가 먼저·실사 렌더가 뒤, 그림 바로 밑에
  「AI 렌더 — 실제 설계안이 아님」+원본 장면+도구 세대를 인쇄한다. 기하
  조감도를 대체하지 않고 나란히 둔다.
- **문제가 생기면 [`MEMORY.md`](MEMORY.md) 를 먼저 찾는다.** 같은 종류를 이미 겪었을
  가능성이 높고, 경위·해법·막는 장치가 적혀 있다. "처음 보는 문제 같다"는 느낌은
  자주 틀린다 — 검색부터 한다. 새 사고가 나면 **그날 바로** 항목을 추가한다.
- **배포·전달 전에 교차검증을 통과한 것만 내보낸다** (MEMORY.md 「배포 전 교차검증」).
  하나라도 못 했으면 무엇을 못 했는지 **말하고** 내보낸다 — 조용히 넘어가지 않는다.
- 새 작업 시작 시 /clear 사용. 이 파일은 자동으로 다시 로드된다.
- 긴 작업 중간에는 /compact로 압축한다.
- 세션 종료 전 결정사항이 남으면 /handover를 실행해 HANDOVER.md를 생성하고,
  다음 세션 첫 프롬프트에서 "HANDOVER.md 읽고 이어서 진행"으로 시작한다.
- **새 규칙·지침·규정은 그 자리에서 이 파일에 적는다** 〈2026-08-23 사장님 지시:
  「앞으로 작업에 본건 지침을 포함 새로운 규칙 지침, 규정은 기억할 수 있도록」〉.
  - 앞 판은 「**두 번** 하게 되는 규칙은 적는다」였다. 그런데 두 번째가 오기
    전까지는 **대화에만 있고**, 세션이 끝나면 사라진다. 실제로 사장님이 같은
    지시를 **세 번** 하신 일이 있다 — 적혀 있었는데 적혔다는 것이 안 보였다.
  - 그래서 **한 번 들은 규칙도 그 턴에 적는다.** 어느 절에 넣을지 애매하면
    가장 가까운 절 끝에 붙이고, 〈날짜 · 사장님 지시〉와 **그렇게 정한 이유**를
    한 줄로 남긴다. 이유가 없으면 반년 뒤에 되돌린다.
  - **적었으면 답변에서 「어디에 적었는지」를 말한다.** 「기억하겠습니다」는
    확인이 아니다 — 사장님은 그것을 확인할 방법이 없다. 절 번호를 적는다.
  - 이미 적혀 있는 것을 다시 지시받으면 **「이미 있습니다」로 끝내지 않는다.**
    어디에 무엇이 적혀 있는지 **그 자리에 옮겨 보여 준다** — 못 찾으신 것이
    사장님 잘못이 아니라 내가 안 보여 준 탓이다.
- 같은 지시를 두 번 하게 되는 규칙은 이 파일에 추가한다 (# 단축키 사용).
- **답변 끝에 「권하는 개선안」을 항상 붙인다** 〈2026-08-25 사장님 지시:
  「너가 알고 있는 더 나은 개선안 (권장, 재확인) 솔루션을 **항상** 추천해줘」〉.
  - 앞 판은 사장님이 그때그때 물으셔야 나왔다. 그러면 **내가 아는 것과 사장님이
    아시는 것이 갈린다** — 나는 다음에 무엇이 가장 값싸고 크게 달라지는지 알면서
    묻기 전까지 말하지 않았다. 실제로 「스캔 진행을 파일 단위로」는 하루를 묵혔다.
  - ★ **일을 마치는 답변마다** 넷 안팎으로 적는다. 각 줄에 **왜 그것이 지금
    값이 큰지**를 한 줄로 붙인다 — 목록만 있으면 고르실 수가 없다.
  - ★ **이미 드린 권고 중 답을 못 받은 것을 함께 되짚는다.** 새것만 쌓으면
    앞엣것이 조용히 사라진다. 「재확인」이 그 뜻이다.
  - ★ **사람 손이 필요한 것**(키 등록·설정)은 §5 대로 **눌러야 할 자리까지**
    적는다. 권고만 적고 방법을 안 적으면 그 권고는 안 된다.
  - ★ **「권장」과 「재확인」을 갈라 적는다** 〈2026-08-25 · SketchUp 갈래에서 합침〉.
    권장은 **새로 하자는 것**, 재확인은 **이미 있어서 안 해도 되는 것**이다.
    갈라 적지 않으면 이미 있는 장치를 다시 만들게 된다.
- **반복·동일 패턴의 지시는 별도 지침이 없어도 규칙으로 일반화해 그 자리에서
  이 파일과 MEMORY.md 에 반영한다** 〈2026-08-25 사용자 지시 · SketchUp 갈래 M-38〉.
  「두 번 하게 되면」이 아니라 **한 번이라도 패턴이 보이면** 스스로 지침화하고,
  경위를 함께 남긴다. 색인 누락은 `memory-index.test.js` 가 잡는다.

## 8. 작업은 미리보기까지가 완성이다

- **작업이 끝나면 언제나 미리보기를 다시 만들어 전달한다.** 화면 작업이 아니어도
  마찬가지다. 코드만 올리고 "만들었습니다"로 끝내지 않는다 — 사용자가 확인할
  방법이 없으면 만든 것이 아니다.
- **3D 모델이 생기면 언제나 돌려볼 수 있는 HTML 뷰어로 보여준다**
  〈2026-08-25 사용자 지시 · M-39 — 규칙지침〉. 사장님은 **3D 가 어떻게
  보이는지**를 항상 궁금해하신다 — 모델을 만들거나 받은 턴에는 자체 렌더
  (외부 라이브러리 없음, 드래그 회전) 3D 미리보기 아티팩트를 **같은 주소**
  (`3D 모델 뷰어` 페이지, 같은 파일 경로 재발행)에 갱신해 주소를 드린다.
  SketchUp 썸네일·다운로드 주소는 임시 토큰이라 이것을 대체하지 못한다.
- **작업이 끝나면 결과가 어떻게 반영되었는지 「반영 샘플」을 만들어 보여준다**
  〈2026-08-25 사용자 지시 · M-38 — 규칙지침〉. 미리보기 주소만으로 끝내지
  않는다 — 실제 산출물의 **발췌**(바뀐 규칙 원문·생성된 JSON·파일 한 토막)를
  답변에 함께 실어, 열어 보지 않아도 무엇이 어떻게 반영됐는지 보이게 한다.
  샘플은 실제 결과에서 오려 온다 — 손으로 지어 쓰면 M-05(옛말 하는 화면)다.
- **끝냈다고 말하는 답변에는 반드시 미리보기 주소가 있어야 한다** 〈2026-08-20〉.
  「했습니다」 + 커밋 번호로 끝내지 않는다. 커밋은 사용자가 열어 볼 수 있는
  것이 아니다.
  - **이번에 건드린 것에 해당하는 주소를 전부 준다.** 하나만 주고 나머지를
    「바뀐 것 없음」으로 두지 않는다 — 사용자는 무엇이 최신인지 알 수 없다.
  - **주기 전에 다시 만든다.** 옛 판 주소를 주는 것이 안 주는 것보다 나쁘다.
  - 문서 작업도 마찬가지다. 문서는 그 자체를 아티팩트로 올려 **주소로** 준다.
  - **아티팩트 주소를 저장소에 적지 않는다** — 이 저장소는 public 이다 (D-10).
- **산출물을 「삭제」하라는 지시를 받으면 — 못 지우면 덮는다** 〈2026-08-25 사장님 지시 ·
  M-52〉. 발행한 아티팩트를 없애는 기능이 없다. 그대로 두면 옛 산출물이 살아 있는
  주소로 남아 **본건인 척 돌아다닌다**.
  - 내용을 **「본건 제외」 안내**로 바꾸고 **살아 있는 본건 주소**를 링크로 준다.
  - 안내 안에 **되살리는 법**(판 기록·작업 폴더 파일)을 적는다. 되돌릴 수 있다는
    사실이 안 보이면 못 되돌린다.
  - **중복 발행분까지 전부 덮는다.** 하나만 덮으면 나머지가 최신인 척한다.
  - 뷰어·목차처럼 **그 갈래를 가리키는 다른 화면도 함께 내린다.**
  - 답변에는 **「지웠다」가 아니라 「내렸다 · 이렇게 되살린다」**라고 적는다.
- **배포는 자동으로 돈다 — `main` 에 닿는 순간이다** 〈2026-08-23 사장님 지시:
  「모든 파일과 구조를 정립해줘 실시간 자동배포가 되도록」 · D-88〉.
  - `deploy-nas` 워크플로가 **`main` 푸시에 자동으로** 돈다. 손으로 누르는 길
    (`workflow_dispatch`)은 남아 있지만 **평소 길이 아니다.**
  - **눌러야 도는 장치는 안 돈다.** 앞 판은 「아무도 안 보는 사이에 덮지 않으려고」
    수동으로 뒀는데, 실제로 일어난 일은 반대였다 — 안 눌러서 NAS 가 조용히 옛
    판으로 남았다 (D-86). 같은 결이 `verify:nas` 에도 있었다 (M-11 · M-12).
  - **엔진(`im-agent/**`)도 함께 올린다.** 화면만 자동으로 올리면 「화면은 새 판,
    서버는 옛 라우팅 표」를 **더 빨리 만들 뿐**이다 (D-87).
  - 엔진은 잘못되면 **서비스가 멈춘다.** `deploy/engine.sh` 가 되돌릴 자리를
    먼저 만들고, 안 살아나면 **스스로 되돌린다.** 되돌렸어도 **빨갛게 끝난다** —
    되돌린 것은 서비스를 살린 것이지 배포가 된 것이 아니다.
  - **자동이 된 것이지 검사가 빠진 것이 아니다.** `guard`(교차검증 전체)가
    초록이 아니면 한 파일도 안 올라간다.
- **작업이 끝나면 「배포 확인」 한 장을 만들어 주소로 드린다** 〈2026-08-27 사장님 지시:
  「항상 작업이 끝나면 배포본에 반영이 잘되었는지 오류가 없는지 즉시 교차검증하고
  HTML 파일로 URL 로 열어서 보여줘」〉.
  - **말로 하지 않고 장치로 한다** — `npm run deploy:report` 가 만든다.
    같은 확인을 말로만 하면 **바쁜 날 빠진다** (M-31: 손으로 챙기다 하루 반에 여섯 번).
  - 한 장에 셋을 담는다: ① **배포가 NAS 안에서 잰 것**(단계별 통과)
    ② **내 자리에서 잰 것**(`guard` 열 칸) ③ **어디부터 사람 손인지**(D-143).
  - ★ **못 잰 것을 통과로 그리지 않는다.** 이 자리에서는 NAS 를 직접 못 본다 —
    근거가 무엇인지 그 장에 적는다.
  - ★★ 첫 줄은 **판정 하나**다 — 「반영됐다」 / 「반영되지 않았다」 (§6-3 ①).
    실패가 하나라도 있으면 초록으로 그리지 않는다.
  - 주소는 **같은 파일 경로로 다시 발행**해 유지한다.
- **배포가 닿았는지 직접 확인하고 알린다 — 「걸었다」로 끝내지 않는다**
  〈2026-08-23 사장님 지시: 「작업이 끝난 즉시 로딩하여 반영 되었는지 확인하고 알려줘」〉.
  - **워크플로를 건 것**과 **파일이 닿은 것**은 다른 사실이다. 줄을 서면 앞의
    것이 끝나야 시작하므로, 걸자마자 「반영했습니다」라고 말하면 **그 사이에
    사장님이 옛 화면을 보신다.** 하루에 세 번 그렇게 어긋났다.
  - 그래서 **`Verify deployed` 가 초록인 것을 보고** 말한다. 그때까지는
    「걸었습니다 · 아직 안 닿았습니다」라고 **있는 그대로** 적는다.
  - 초록을 본 뒤에는 **그 판의 지문을 함께 적는다.** 사장님이 화면 아래
    여덟 글자와 대 보시면 한 번에 갈린다 (M-27).
  - 도는 동안 답변을 미루지 않는다. **지금 무엇이 실려 있고 무엇이 오는
    중인지**를 나눠 적는다 — 「곧 됩니다」는 확인이 아니다.
- **끝냈다는 답변에는 그 판의 지문도 함께 적는다** 〈2026-08-22 사용자 지시 · M-25〉.
  화면 아래에 작게 `판 xxxxxxxx` 가 찍혀 있다(`build-stamp.js`). 답변에 같은
  값을 적어 두면 **사장님이 사진 한 장으로 「내가 보는 것이 그 판인가」를
  가릴 수 있다.** 이것이 없어서 네 번 헤맸다 — 나는 「고쳤습니다」라고 하고
  사장님은 옛 화면을 보고 계셨는데, 둘 다 그 사실을 몰랐다.
  - 지문은 `npm run im:stamp -- --check` 로 확인한다.
  - **「반영이 안 된다」는 셋이다** — 저장소≠디스크 / 디스크≠HTTP / 브라우저.
    갈라 보기 전에 **새로고침을 권하지 않는다** (MEMORY M-25).
- **기본 전달은 「주소로 여는 페이지」다.** `npm run im:artifact` 로 조각을 만들어
  아티팩트로 올리고 **주소를 준다.** 옆 창에서 확실히 열리는 것이 이것뿐임을
  네 번의 실패로 확인했다 (MEMORY.md M-01). 파일 전달은 내려받기 카드로만 뜨는
  환경이 있고, 그때 사용자는 **아무것도 못 본다.**
  - 직접 눌러 봐야 하면 `section-preview.html` 을 **함께** 보낸다. 대신 보내지 않는다.
  - 같은 파일 경로로 다시 올리면 **주소가 유지된다.** 새 주소를 만들지 않는다.
  - 답변 맨 끝이 아니라 **작업을 마치는 그 턴에** 준다 — 다음 턴으로 미루면
    사용자가 옛 화면을 본다.
- **★★★ 기준은 「앱에서 보이는 것」이다 — 낱개 화면이 아니다** 〈2026-08-30 사장님 지시:
  「지금부터 (NAS 뿌리 주소) 여기로 보여지는 **앱환경의 기준으로 대화가 되어야함** ·
  그것을 html url 로 링크하여 **항상 미리보기 창에 보여줘**」〉.
  - **뿌리 주소가 앱이다.** 사장님이 여시는 창은 거기 하나다 — 로그인 화면이 뜨고,
    그 안에서 보고서 화면이 틀에 얹혀 보인다. 실측으로 확인했다 (D-183).
  - ★ 그러므로 **「낱개 화면에서는 멀쩡합니다」로 끝내지 않는다.** 이 세션에서 실제로
    그렇게 어긋났다 — `report-flow.html` 을 직접 열면 배너가 한 벌도 없어 멀쩡한데,
    앱 안에서는 **두 벌**로 보였다 (D-179). **혼자 뜬 판이 맞다는 것은 판정이 아니다.**
  - ★★ 무엇을 고치든 **「앱에서 어떻게 보이나」를 함께 적는다.** 못 재는 자리면
    (여기서는 tailnet 을 못 본다) **못 쟀다고 적는다** — 「될 것이다」로 적지 않는다.
  - ★★★ **답변마다 앱 주소를 준다.** 낱개 화면 주소만 주면 사장님이 그것으로 판정하시게
    되고, 그러면 위의 어긋남이 또 안 보인다. 앱 주소가 **첫 번째**, 낱개는 그다음이다.
  - ★ 그리고 **미리보기 창에 여는 판**을 함께 낸다 (`npm run im:appview` →
    아티팩트로 발행). 그 판은 **앱 자리 지도**다 — 어느 주소가 무엇인지, 지금 실린
    판 지문이 무엇인지, 어디를 눌러야 하는지. 아티팩트는 tailnet 밖이라 **앱 화면
    자체를 그려 보일 수는 없다** — 그 사실을 그 판에 적어 둔다 (없는 것을 그리지 않는다).
  - ★★ **주소를 저장소에 적지 않는다** (D-10 — 이 저장소는 public). 주소는 답변에만 적는다.
- **부르면 그 자리에서 연다.** "보여줘" · "미리보기" · "옆 창에" 같은 말이 나오면
  그것만으로 충분한 지시다. **되묻지 않고** 같은 턴에 `npm run im:section` 으로
  다시 만들어 옆 창에 연다. 어느 화면인지 안 밝혔으면 **보고서 생성 섹션**
  (`section-preview.html`)이 기본이다.
  - 방금 만든 것이 있어도 **다시 만들어서 보낸다.** 재생성은 몇 초면 되고,
    옛 화면을 보여 주는 쪽이 훨씬 비싸다.
  - 판이 둘이다. **눌러 봐야 하면** `npm run im:section` → `section-preview.html`
    (접기·펼치기·입력이 동작한다). **그냥 보이기만 하면 되면**
    `npm run im:static` → `section-static.html` (빌드할 때 헤드리스로 미리 그려
    넣어 스크립트 없이 뜬다. 대신 눌리지 않는다).
  - **파일이 다운로드 카드로만 뜨는 환경이 있다.** 그때는 파일 전달을 반복하지
    말고 `npm run im:artifact` 로 만든 조각을 **주소로 여는 페이지(아티팩트)로
    올려 링크를 준다.** 지금까지 확인된 바로는 이것이 옆 창에서 확실히 열리는
    유일한 길이다. 대신 눌리지 않으므로, 직접 만져 봐야 하면 파일도 함께 준다.
  - 옆 창에서 안 열린다·빈 칸이 뜬다고 하면 **미리 그려 넣은 판을 보낸다.**
    안내를 반복하지 않는다.
- **한 일은 전부 미리보기 안에서 확인되어야 한다.**
  - 화면에 보이는 작업 → `changes.js` 에 등록하고 `shows` 에 **어디를 보면
    확인되는지** 적는다.
  - 화면이 없는 작업(파서·커넥터·검증) → 미리보기의 **[눈으로 확인]** 패널에
    실제 실행 결과를 넣는다. 정말 화면에 안 나오는 것이면 `shows` 에
    **"화면에 안 보인다"고 적는다** — 없는 것을 찾느라 헤매는 쪽이 더 나쁘다.
  - 확인 패널에 들어가는 결과는 **빌드할 때 실제로 돌려서** 넣는다. 손으로 적으면
    코드가 바뀐 날부터 화면만 옛말을 하고, 그때는 아무도 눈치채지 못한다.
- 미리보기는 **파일 하나로 열리는 자체 완결 HTML** 이어야 한다. 서버·빌드 도구가
  있어야 열리는 것은 미리보기가 아니다.
- 실제 데이터가 없으면 **예시임을 화면에 박아 둔다.** 데모 데이터를 실제로
  오해하면 그것을 근거로 판단한다.
- **표본이 거짓말을 하면 잡히는 것도 거짓이다** 〈2026-08-23 · 두 번 걸렸다〉.
  - 미리보기의 가짜 서버가 파일마다 지문을 똑같이 `'demo'` 로 주고 있었고,
    시험용 파일 둘은 **바이트가 똑같았다**(전부 0). 그 상태로 「같은 파일 접기」를
    넣었더니 **서로 다른 두 파일이 한 줄로 접혔다** — 시험은 빨개졌는데
    **접기는 옳게 돌고 있었다.** 고칠 곳은 코드가 아니라 표본이었다.
  - ★ 그러니 표본은 **재려는 성질을 지켜야 한다.** 진짜 값일 필요는 없지만
    「다른 파일은 다른 지문」 같은 성질은 지킨다.
  - ★ 빨간 줄을 보면 **표본부터 의심한다** — 코드를 고치기 전에.
- 전달 전에 **헤드리스 브라우저로 실제 렌더를 확인한다.** DOM 이 비었는데
  오류가 안 뜨는 경우가 있다 (닫는 script 태그, 세미콜론 누락 등).
- 미리보기 산출물이 소스와 갈리지 않도록 **재생성 결과가 커밋본과 같은지
  테스트로 고정한다.**
- **소스를 글자로 대조하는 검사는 주석을 떼고 본다** 〈2026-08-23 · 하루에 네 번 걸렸다〉.
  - `fetch-depth: 0` · `required()` · `verify-served.sh` 세는 곳 · `d.height + 8` —
    넷 다 **바로 위 주석에 적어 둔 경위**를 코드로 읽고 빨개졌다. 코드는 멀쩡했다.
  - ★ **경위를 잘 적어 둘수록 검사가 눈이 먼다.** 이 저장소는 주석이 길어서
    이 함정이 특히 자주 온다.
  - 그러니 `read(...)` 한 것을 그대로 `match` 하지 말고, **주석 줄을 걸러 낸 뒤**
    본다. 반대로 「주석에 경위가 남아 있는가」를 재는 검사는 그대로 둔다 —
    그때는 주석이 재려는 대상이다.
  - **주석 안의 글자가 코드를 깨뜨린다** 〈2026-08-23 · 하루에 두 종류〉.
    - **역따옴표(`` ` ``)를 주석에 쓰면 안 되는 자리가 있다.** `build-files.js` 의
      `uploadDriver()` 는 통째로 **템플릿 문자열**이다. 거기 주석에 역따옴표를
      넣었더니 문자열이 그 자리에서 끝나 **테스트 열여덟 개가 빨개졌다.**
      오류는 `Unexpected token 'new'` 한 줄뿐이라 주석이 원인이라는 생각이 안 든다.
    - ★ 그러니 **템플릿 문자열 안의 주석에서는 코드를 인용하지 않는다.**
      「길이 2048 짜리 0 바이트」처럼 **말로** 적는다.
  - **검사가 찾는 표시 이름을 주석에 글자로 적지 않는다** 〈2026-08-24 · 세 번째〉.
    - 새 지킴이 주석에 「짝 확인(`data-lp-pair`)은 …」이라고 적었더니,
      그 표시를 **첫 등장 위치로 찾던 검사**가 주석을 코드로 읽고
      **엉뚱한 스크립트 블록을 떼어 냈다** — 테스트 다섯이 빨개졌다.
    - ★ 표시 이름은 **한 곳(진짜 코드)에만** 두고, 설명에서는
      「짝 확인 조각」처럼 **이름으로 부르지 말고 말로** 가리킨다.
  - **거꾸로도 온다 — 주석에 「여는 html 태그」를 글자 그대로 적지 않는다**
    〈같은 날 두 번〉. 조각(fragment) 검사가 주석을 가리지 않고 보기 때문에
    **진짜 문서로 오인해 배포를 거부한다.** `embed-bridge.js` 주석 한 줄에
    적었다가 테스트 여덟 개가 빨개졌다. 태그를 설명해야 하면
    「문서 뿌리(`documentElement`)」처럼 **이름으로** 적는다.
- **상태를 바꾸는 기능은 갱신시각을 함께 남긴다. 그 시각은 한 곳에서만 만든다**
  (`core/kst.js` 의 `kstStamp()`). 상태 토글을 만들거나 고칠 때는 MEMORY.md 의
  「상태 토글 기능 검증 체크리스트」(M-02)를 전부 확인한다.
- **「반영됐다」와 「사용자가 보고 있다」는 다른 사실이다** 〈2026-08-23 · M-29〉.
  - 배포가 초록이고 지문이 새 판이어도 **브라우저가 형제 스크립트만 옛것을**
    들고 있을 수 있다. 그때 화면은 **멀쩡히 뜨고** 「… is not a function」
    한 줄만 남는다 — 캐시가 원인이라는 생각 자체가 안 든다.
  - 그래서 형제 파일 주소에 **판 표시(`?v=지문`)**를 붙이고(`build-stamp.js`),
    화면 안에 **짝 확인 조각**을 둔다. 화면·스크립트 판이 다르면 빨간 띠로 알린다.
  - 화면과 형제 스크립트를 함께 고쳤으면 MEMORY.md M-29 의
    「짝이 깨지는 배포를 막는 체크리스트」를 전부 확인한다.
  - **`?v=` 는 파일 이름이 아니다.** 주소에서 파일을 찾는 자리는 전부 떼고 본다.
- **배포·전달 전에는 교차검증을 전부 통과한 것만 내보낸다** 〈2026-08-23 사장님 지시:
  「배포전 항상 교차검증을 통해 완전할 경우만 배포해줘」〉. 최소 넷이다 —
  `npm test` 전부 초록 · `npm run im:stamp -- --check` 초록 · 미리보기 재생성 ·
  헤드리스로 실제 렌더 확인. **하나라도 못 했으면 무엇을 못 했는지 말하고** 낸다.
  - ★★★ **넷을 손으로 챙기지 않는다 — `npm run guard` 한 줄로 돈다**
    〈2026-08-24 · M-31〉. 손으로 챙기다 하루 반 동안 여섯 번 빠뜨렸다
    (미리보기 세 번 · 지문 두 번 · 헤드리스 한 번). 「할 줄 몰라서」가 아니라
    **「빠뜨려서」**다. 규칙을 문서에만 적으면 그 규칙은 사람의 기억에 얹힌다.
  - ★ **못 잰 것은 통과가 아니다.** `guard` 는 크로미움이 없으면 「못 쟀다」로
    적고 초록으로 안 끝낸다 (되돌아오는 값 2).
- **배포를 걸 때는 `dry_run=false` · `engine=yes` 를 반드시 준다**
  〈2026-08-24 · M-31 · 두 번 헛돌았다〉.
  - `workflow_dispatch` 의 `dry_run` 기본값은 **`true`** 다. 값을 안 주고 걸면
    **초록으로 끝나는데 아무것도 안 올라간다** — 「Dry run summary」만 돌고
    나머지가 전부 skipped 라 **성공한 것처럼 보인다.**
  - ★ **푸시와 배포는 다른 일이다.** 커밋만 하고 「올렸습니다」라고 말한 적이
    있다. 「커밋했다 / 배포를 걸었다 / NAS 에 닿았다」는 **셋 다 다른 사실**이고,
    한 마디로 말하면 그 사이에 사장님이 옛 화면을 보신다 (M-25 와 같은 결).
- **그래도 문제가 나면 MEMORY.md 부터 찾는다** 〈같은 지시〉. 「처음 보는 문제
  같다」는 느낌은 자주 틀린다 — 오늘 난 사고 셋이 전부 이미 적혀 있던 결이었다.

## 8-2. 두 저장소의 경계 〈2026-08-26 사장님 지시 · D-120〉

사장님 지시 그대로다 —

```
linkpilot-platform → 유저 접촉
linkpilot-cron     → 유저가 보고서 요청 및 생산
```

**저장소 이름이 아니라 「일」로 가른다.** 어디에 둘지 헷갈리면 이 한 줄로 정한다.

★★★ **그리고 이것은 두 칸이 아니라 「한 줄기」다** 〈2026-08-26 사장님 지시 · 위 지시를 다시 주심〉.

사장님이 그리신 순서는 **유저 접촉 → `linkpilot-platform` → `linkpilot-cron` →
보고서 생산** 이다. 앞 판은 두 저장소를 **나란한 두 칸**으로 적어 두었는데,
그렇게 읽으면 「둘 중 어디에 둘까」만 답이 나오고 **어느 쪽이 먼저인지**가 안 보인다.

- **유저는 `linkpilot-platform` 만 만난다.** 사람이 이 저장소를 직접 여는 일은 없다.
- **`linkpilot-cron` 은 그 뒤에 선다.** 요청을 platform 에서 받아 보고서를 만든다.
- ★ 그래서 **방향이 정해져 있다** — platform 이 cron 을 부르는 것이지 그 반대가 아니다.
  이쪽이 저쪽 화면을 부르거나 저쪽 저장소에 쓰는 설계가 나오면 **줄기를 거스른 것**이다.
- ★★ 보고서 화면 16개가 저쪽에 **사본**으로 있는 이유도 이것이다. 유저가 만나는
  자리는 저쪽뿐이라 화면이 저쪽에 있어야 하고, **만드는 자리는 이쪽**이다.
  둘 중 하나를 고르는 문제가 아니라 **줄기의 앞뒤**다.

- **유저를 만나는 것**(CRM · 태양광 · 통화분석 · 회원·요금)은 `linkpilot-platform`
  갈래 `platform` 이 본체다. 정본은 `linkpilot-platform.deploy.html` 이고 **비공개**다.
- **보고서를 요청하고 만드는 것**(접수 화면 · 엔진 · 검증)은 이 저장소가 본체다.
  **공개**다 (D-10) — 그래서 앱 코드를 이쪽으로 옮기면 **그 순간 공개된다.**

★ **보고서 화면 16개는 이쪽 것이고, 저쪽 `im-flow/` 는 사본이다.**
  `npm run imflow:manifest` 로 「보낸 것」을 적고, `npm run imflow:check` 가
  그 기록이 지금 화면과 같은지 잰다 (교차검증 아홉째 칸).

★★ **어긋나는 것이 예외가 아니라 기본값이다.** 화면마다 판 지문이 박혀 있어
  이쪽이 **한 번만 바뀌어도 16개 전부**에 새 지문이 찍힌다. 실측에서 하루 만에
  15개가 어긋났다. 그러니 이쪽을 내보낼 때 **사본도 함께 보낸다.**
  ★ 지문이 어긋난 사본은 사장님이 화면 아래 여덟 글자로 판을 가리실 때
    **틀린 답을 보여 준다** (M-25 가 막으려던 바로 그 상태다).

~~★ **저쪽 저장소에 이쪽 세션이 쓰지 않는다.**~~ 〈2026-08-30 폐지 — 아래로 대체〉

★★★ **보고서 자리의 「머리」는 앱이 아니라 이쪽 화면이 단다** 〈2026-08-30 사장님 지시:
「LinkPilot Platform 보고서 생성에서 **상단 바 만들지 말 것** — 보고서 생성 엔진에서
배너를 달고 띄어짐」〉.

- 앱의 [보고서 만들기] 자리는 **아래 틀에 이쪽 화면을 통째로 얹는 자리**다. 앱이 제
  배너를 그리면 같은 것이 두 벌이 되고 사이가 뜬다.
- ★ **같은 일을 하는 자리가 둘이면 한쪽만 고쳐진다.** 저쪽은 2026-08-16 에 「상단
  고정바」를 같은 이유로 이미 한 번 지웠다 — 두 번째다. 그래서 이번에는 **어느 쪽이
  머리를 다는지 못박는다**: 머리는 **이쪽 화면**이 단다.
- ★★ **탭 줄은 배너가 아니다.** `[보고서 만들기]`·`[완성 보고서]` 는 틀의 주소를 바꾸는
  **동작**이라 저쪽에 남는다. 지우면 완성 보고서로 갈 길이 없어진다.
- ★★★ **그러므로 이쪽 화면에 머리를 넣을 때는 「앱 안에서 어떻게 보이나」로 잰다** (§8).
  혼자 뜬 판에서 멀쩡한 것은 판정이 아니다.
- 저쪽에 걸어 둔 검사: `test-report-no-hero.js` — 배너 글자가 0번인지, 탭 둘이 살아
  있는지 **실제로 그려서** 센다. 「지웠다」는 소스에서만 확인되고 「안 보인다」는 그려
  봐야 안다.

★★★ **이쪽 세션이 `linkpilot-platform` 에 직접 손댈 수 있다** 〈2026-08-30 사장님 지시:
「AI Project Manager Orchestrator 는 지금 이후부터는 LinkPilot 플랫폼이 저장소에
조치를 직접 할 수 있도록 권한을 부여한다」 · 「전달한 내용을 Platform Manager 를
모니터링하고 직접 지시 및 관리해줘」〉.

- **왜 바꿨나** — 앞 판은 전달 문서만 쓰고 손을 뗐다. 그런데 실제로 일어난 일은
  **문서가 쌓이고 아무것도 안 고쳐지는 것**이었다. 사장님이 직접 넘기셔야 했고,
  넘긴 뒤에도 됐는지 아무도 안 봤다. 「알려 주기」와 「고쳐지기」는 다른 사실이다.
- ★ **그래도 합치는 것은 사람이 한다 — 초안(draft) PR 까지가 이쪽 몫이다.**
  저쪽은 로그인·회원·CRM 데이터가 도는 자리라, 조용히 들어간 변경이 사고가 되면
  **되돌릴 사람이 그날 안 계실 수 있다.**
- ★★ **줄기의 방향은 그대로다** (위 ★★★). 권한이 생긴 것이지 이쪽이 저쪽의
  본체가 된 것이 아니다. 저쪽 화면·기능을 이쪽으로 옮기지 않는다.
- ★★ **`.gitignore` 를 함부로 넓히지 않는다.** 저쪽은 **허용목록** 방식이고,
  코드 옆에 투자자 기밀문서·CRM 백업·API 키 평문이 함께 있다. 한 줄을 넓히면
  그것들이 딸려 올라간다. 새로 추적할 것은 **폴더 하나씩 이름을 박아** 연다.
- ★★★ **커밋 전에 무엇이 담겼는지 눈으로 센다.** `git status --short` 로 파일
  목록을 보고, 비밀이 될 수 있는 이름(`*_key*` · `*backup*` · `linkpilot_data*`)이
  하나라도 있으면 멈춘다.
- **자동화 도구가 저쪽 쓰기를 막을 수 있다.** 막히면 **막혔다고 말하고**
  전달 문서로 낸다 — 조용히 넘어가지 않는다 (§8 「못 한 것을 먼저 말한다」).

## 8-1. Agent 를 하나 더 붙일 때

〈2026-08-25 사장님 지시: 「Agent 를 더 붙일 때 규칙 업무지침을 만들어줘」〉

**규칙은 [`docs/업무지침-Agent-붙이는-규칙.md`](docs/업무지침-Agent-붙이는-규칙.md)
한 곳에 있다.** 여기 옮겨 적지 않는다 — 두 벌이 되면 한쪽이 옛말을 한다.

- **배선은 다섯 곳이다.** 하나만 빠져도 **아무 오류가 안 나고 조용히 안 돈다.**
  `npm run agent:check` 가 등록된 **전부**에 대해 그 다섯을 센다.
  ★ 앞 판은 이 검사가 **한 Agent 에만** 있었다 — 그러면 다음에 붙는 Agent 는
  또 아무도 안 센다. 그것이 D-48 이 났던 자리다.
- **이름·D 번호·M 번호는 열려 있는 다른 갈래와 함께 본다** (D-101 · D-102).
  번호를 비워 두는 약속으로는 못 막혔다 — 실제로 여섯이 두 뜻을 가졌다.
  ★ **파일 겹침은 이제 `npm run branch:check` 가 센다** — 특히 **양쪽이 새로
  만든 같은 경로**를 따로 잡는다. 번호(D·M)는 여전히 사람이 본다.
- **교차검증은 이제 열두 칸이다** — 위 둘이 일곱째·여덟째, 대외 문서 PDF 가 열한째,
  **화면 크기가 열두째**다 〈2026-09-01 · M-71 · M-72〉.
  ★ 열두째를 왜 두었나: 아홉째 「헤드리스 렌더」는 **「그려지는가」**만 본다. 그려지기는
    하는데 **자리가 틀린** 고장은 그것으로 안 잡힌다 — 같은 날 둘이 났고
    (폰에서 잘림 · 앱 틀이 안 줄어듦) **둘 다 사장님이 눈으로 찾으셨다.**
    `npm run probe:viewport` 가 **폰(390)·맥(1440) 두 폭에서 실제로 그려** 가로 넘침 ·
    화면 밖으로 나간 것 · 상자가 내용보다 작은 것 · 잘린 글자를 센다.
  ★★ **헤드리스 창은 390px 로 안 줄어든다**(실측 485px). 그래서 폭이 정확한 **틀**에
    넣고 재고, **재려던 폭으로 못 쟀으면 「못 쟀다」로 끝낸다** — 다른 폭을 재고
    초록으로 끝내면 이 칸을 둔 뜻이 없어진다.
- 붙이고 나서 `npm run guard` → `npm run im:demo` 로 **그 Agent 의 줄이 실제로
  찍히는지** 본다. 등록·호출이 맞아도 입력이 안 닿으면 조용히 건너뛴다.

## 9. 미결정 사항 관리

- 결정되지 않은 것을 **추측으로 메우고 넘어가지 않는다.** 추측값은 반드시
  `docs/미결정-사항.md` 에 ID를 부여해 기록하고, 코드에도 추측임을 남긴다
  (예: `catalog.js` 의 `byGuide: false`).
- **세션을 시작할 때 `docs/미결정-사항.md` 를 읽고, 🔴 항목이 있으면 먼저 알린다.**
  작업 지시가 그 항목과 무관해도 알린다 — 미결정은 시간이 지날수록 비싸진다.
- 작업 중 새 미결정이 생기면 그 자리에서 등록부에 추가한다. 세션 끝에 몰아서
  쓰면 빠뜨린다.
- 결정된 항목은 **지우지 않는다.** `✅ 결정됨` 으로 바꾸고 결정일·결정자·반영
  커밋을 남긴다. 지우면 반년 뒤 같은 논의를 다시 한다.
- 사양(이 파일)과 코드가 다르면 어느 쪽이 맞는지 정하기 전까지 **양쪽 다 고치지
  않는다.** 먼저 등록부에 올린다.

## 10. 정기 운영점검 — 매일 08:30 KST 〈2026-08-30 사장님 지침 · 「반영해줘」〉

**지침 원문은 [`docs/운영지침-정기-운영점검-및-보고.md`](docs/운영지침-정기-운영점검-및-보고.md)
한 곳에 있다.** 여기 옮겨 적지 않는다 — 두 벌이 되면 한쪽이 옛말을 한다 (§8-1 과 같은 결).

그 문서에서 **이 저장소의 일과를 바꾸는 것 넷**만 여기 적는다.

- **증빙 없는 것을 정상으로 적지 않는다.** 확인 못 한 칸은 `UNVERIFIED` 다.
  ★ 이 저장소의 「못 잰 것은 통과가 아니다」(§8 · guard)와 **같은 규칙**이다.
- **점검하는 자리가 운영 코드를 고치지 않는다.** 문제를 찾으면 별도 작업으로 배정한다.
  ★ 이쪽이 `linkpilot-platform` 에 손댈 수 있게 된 뒤(§8-2)에도 **초안 PR 까지**인 이유가
    이것과 같다 — 찾는 손과 고치는 손을 갈라 둔다.
- **문제마다 담당자·목표일·완료조건·증빙이 있어야 한다.** 없으면 완료로 판정하지 않는다.
  ★ `docs/미결정-사항.md` 의 관리대장이 그 자리다. 지금 10건 중 인계자·인수자가 비어 있다.
- **`main` 병합·운영배포·운영 데이터 변경·외부 공개는 사장님 승인 없이 하지 않는다.**

★ **보고는 카톡 「나에게 보내기」로 알리고 본문은 아티팩트 주소로 준다.**
  카톡은 **200자 제한**이라 본문을 통째로 못 보낸다 — 요약 한 줄과 주소만 간다
  (2026-08-30 실측: 발송 성공). 주소는 저장소에 적지 않는다 (D-10).

★★ **시각은 UTC 로 적는다** — 08:30 KST = `30 23 * * *` UTC. KST/UTC 변환은 이
  저장소의 1순위 사고 원인이다 (§2).

## 11. 배포 전 교차검증과 API 관리 — 역할·게이트 〈2026-09-01 사장님 지침〉

**원문은 [`docs/운영지침-배포전-교차검증-및-API관리.md`](docs/운영지침-배포전-교차검증-및-API관리.md)
한 곳에 있다.** 여기 옮겨 적지 않는다 — 두 벌이 되면 한쪽이 옛말을 한다 (§8-1 과 같은 결).
그 문서의 **부록 A** 는 원문이 아니라 **이 저장소에서 실제로 재 본 결과**다.

그 문서에서 **이 저장소의 일과를 바꾸는 것 다섯**만 여기 적는다.

- **`guard` 는 검사이지 승인이 아니다.** 초록은 「낼 수 있다」이지 「내도 된다」가 아니다.
  ★ 지침 §4.1 「만든 Agent 는 자기 기능을 최종 승인할 수 없다」가 지금 **절반만** 지켜진다 —
    만드는 손과 재는 손이 같다. 독립 검증군은 **아직 실체가 없다** (D-202).
    없는 것을 있는 것처럼 적지 않는다.
- **Gate 3(Staging 실환경 검증)은 지금 한 칸도 못 잰다.** 이 시스템에 **Staging 이 없다** —
  `deploy-nas` 는 `main` 푸시에 곧바로 운영 자리로 나간다 (D-88). 그러니 Gate 3 을
  초록으로 적지 않는다. **`UNVERIFIED` 로 적고 그 위험을 승인서에 쓴다** (D-201).
- **같은 이름이 다른 뜻이다 — 넷.** `VERIFIED`·`BLOCKED`·`FAILED` 는 오케스트레이터
  지침의 **작업 단계**이자 이 지침의 **검증 판정**이고, `READY_TO_DEPLOY` 는 코드에서
  **디자인 게이트의 마지막 칸**(`core/design-gate.js`)이자 이 지침의 **릴리스 판정**이다.
  ★ 그래서 **어느 게이트인지 접두어로 부른다** — `DESIGN:READY_TO_DEPLOY` ·
    `RELEASE:READY_TO_DEPLOY`. 새로 겹치는 낱말이 생기면 `role-guide.test.js` 가 빨개진다.
- **비용 한도 초과·유료 전환·새 유료 API 는 사장님 승인이다.** 지침 §3.6 은 API Manager 가
  「단독 승인할 수 없다」고만 적고 **누가 승인하는지**를 안 적었다 — 그러면 넘긴 순간
  아무도 못 멈춘다. 그리고 **한도의 80% 에서 미리 알린다**(넘긴 뒤 알리는 것은 보고가 아니다).
- **Rollback 은 사전 승인 없이 실행한다.** 되돌리는 것은 지키는 일이라 승인을 기다리는
  동안 손해가 는다. ★ 다만 **되돌렸어도 빨갛게 끝난다** — 되돌린 것은 서비스를 살린
  것이지 배포가 된 것이 아니다 (§8 · `deploy/engine.sh` 와 같은 규칙).

★ **조직도에 역할을 더하면 부록 A-9 대조표에도 더한다.** 안 더하면 그 역할은 **그림에만
  있는 채로** 아무도 안 맡는다 — 실제로 `Design Manager Agent` 가 그 상태였다.
  `npm test` 의 `role-guide.test.js` 가 조직도와 대조표를 대 본다.

★★ **번호(`D-`)는 `npm run d:next` 로 받는다** — 그런데 그 도구가 **이미 쓴 번호를 내주고
  있었다** 〈2026-09-01 실측〉. 결정된 항목은 `docs/결정-기록.md` 로 옮겨 가는데 그 파일은
  **표**라 도구의 머리말 규칙에 안 걸렸다. `D-191` 을 내줬는데 그 번호는 결정 기록에
  **세 번** 나온다. 이제 그 파일에서 **번호만** 함께 줍는다.
