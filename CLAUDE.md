# CLAUDE.md — LinkPilot 보고서 생성 (IM Agent)

이 저장소는 투자설명서(IM)·티저·요약·검증 보고서를 만드는 파이프라인이다.
**출처 없는 숫자는 시스템에 들어오지 못한다** — 그 한 가지가 설계의 전부다.

> ## ⚠️ 범위 변경 (2026-08-14)
>
> **아침 크론(브리핑·모닝카드·한줄생각·법령수집)은 이 저장소에서 제외되었다.**
> `send-morning-*.js` · `fetch-*.js` · `render-card.js` · `fonts/` · 크론 워크플로
> 4개를 삭제했다. 경위는 [`docs/삭제-후보.md`](docs/삭제-후보.md) B군.
>
> **아래 §1·§3 은 크론 사양이라 적용 대상이 이 저장소에 없다.**
> 지우지 않고 남겨 둔 이유는 하나다 — 크론을 다른 저장소로 옮길 때 이 사양이
> 필요하다. 옮길 곳이 정해지면 통째로 가져간다.
>
> **§4 데이터 소스 규칙은 2026-08-16 에 IM Agent 기준으로 개정되었다** —
> 이 저장소가 공공데이터 6종을 실제로 쓰게 되었기 때문이다. 크론 시절 §4 는
> `docs/삭제-후보.md` 에 옮겨 두었다.
>
> **지금 유효한 규칙은 §2 · §4 · §5 · §6 · §7 · §8 · §9 다.**
> (2026-08-16: §8 이 화면 작업으로 들어오면서 미결정 관리가 §9 로 밀렸는데
> 이 줄이 따라오지 않아 §8 을 미결정 관리로 가리키고 있었다.)
>
> 되살리려면: `git log --diff-filter=D -- send-morning-brief.js` 로 삭제 커밋을
> 찾아 `git revert` 한다.

## 1. 프로젝트 컨텍스트 〈크론 — 참고용 보존〉

- 소유: PDI GID (PDI 글로벌 인프라 개발 유한회사) 내부 플랫폼 LinkPilot의 서브 시스템
- 본체 플랫폼: React/Vite 프론트엔드 + Node.js API(포트 8181),
  Synology DS423+ NAS 에서 운영 (주소는 GitHub Secrets — CLAUDE.md §2)
- 이 저장소의 역할: NAS n8n에서 이관된 6AM 모닝 브리핑 크론
  (이관 사유: NAS 불안정 + OAuth 관리 문제 → GitHub Actions로 실행 안정성 확보)
- 연락처 데이터: contacts.json 약 1,145건 (태그: CEO, 투자대상 등)
- 발송 채널: 카카오톡 "나에게 보내기" REST API

## 2. 절대 규칙 (위반 금지)

- 시크릿(카카오 REST API 키, refresh token, 공공데이터 서비스키, NAS 접속정보)은
  코드·로그·커밋 메시지에 절대 평문으로 남기지 않는다. GitHub Secrets만 사용한다.
- 카카오 OAuth refresh token 갱신 로직을 임의로 삭제·단순화하지 않는다.
  (토큰 만료 = 브리핑 발송 전면 중단)
- cron 스케줄은 UTC 기준으로 작성한다. 06:00 KST = `0 21 * * *` (UTC 21:00).
  KST/UTC 변환 실수는 이 저장소의 1순위 사고 원인이므로 수정 시 반드시 재확인한다.
- main 브랜치에 직접 push하는 워크플로우 변경은 로컬 검증(act 또는 dry-run) 없이
  하지 않는다. 실패한 크론은 다음날 아침에야 발견된다.
- 발송 실패 시 조용히 죽지 않는다: 실패 알림(fallback) 경로를 항상 유지한다.
  - **2026-08-24 개정 〈사장님 지시 · D-103〉 — 문자(SOLAPI) 알림을 지웠다.**
    〈번호는 2026-08-25 에 D-99 → D-103 으로 옮겼다 — 저쪽 작업선의 D-99 가
     코드와 결정 기록에 이미 박혀 있어 그쪽이 번호를 지킨다 (D-102)〉
    열쇠 넷이 한 번도 안 들어와 **한 번도 안 나간 채로** 자리만 지키고 있었고,
    유료 서비스라 계정부터 만들어야 했다. **규칙 자체는 그대로다** — 다만 지금의
    실패 통보 경로는 **GitHub 이 실행 실패 시 보내는 메일**이다.
    ★ 그 메일이 안 오는 상태가 되면 **그때가 알림을 다시 만들 때**다.
      「알림이 없다」와 「알림이 있는데 안 온다」는 다른 사실이므로, 지웠다는
      사실을 여기와 `deploy-nas.yml` 두 곳에 적어 둔다.

## 3. 브리핑 사양 (확정 구조) 〈크론 — 참고용 보존〉

섹션 순서 고정 (데이터 없는 섹션은 출력 생략):
1. 오늘의 핵심 요약 (3~5줄, 🔴🟡🟢 신호등)
2. 오늘의 달력 (양력·음력·절기·공휴일 + 금융기관 영업일 여부, 말일/분기말 표시)
3. 오늘 날씨 (서초 기본 + 당일 이동지역 병기, 우산/미세먼지 한 줄 판단)
4. 오늘의 일정 (Google Calendar, 30분 미만 간격 ⚠️ 충돌 경고)
5. 이동 동선 (일정별 출발 권장시각 역산, 대중교통 vs 자동차)
6. 필수 처리 업무 (P1/P2/P3, 전날 미완료 자동 이월, 2회 이월 시 🔴)
7. 신규 접수건 (딜 분류: 부동산PF/태양광/CB/해외, 48시간 무응답 경고)
8. 프로젝트 진행현황 (오늘 액션 있는 건만 상세, D-Day 카운트다운)
9. 전날 통화분석 (후속 액션 추출 → 6번/10번 섹션에 연결)
10. 오늘 연락할 인맥 (팔로업 도래 + 90일 무연락 VIP + 생일·스페셜데이, tel: 딥링크)
11. 시장 지표 (국고채3년·CD금리·환율·SMP·REC·KOSPI)
12. 오늘의 생각한줄 (19종 테마, 요일·스페셜데이 자동 라우팅)
13. 내일 프리뷰 1줄

발송 구조 (카카오톡 200자 제한 대응):
- 풀버전 HTML 생성 → NAS 또는 GitHub Pages에 업로드
- 카카오톡에는 [1. 핵심 요약]만 + 풀버전 링크 발송
- 요일 변형: 월요일 = 주간 프리뷰 추가, 금요일 = 주간 딜 파이프라인 변동 요약 추가

## 4. 데이터 소스 규칙

〈2026-08-16 개정 — 크론 기준에서 IM Agent 기준으로. 구 조항은 `docs/삭제-후보.md`〉

★★★ **열쇠를 새로 붙이거나 다른 프로젝트에 옮길 때는
[`docs/운영지침-공공API-활용.md`](docs/운영지침-공공API-활용.md) 를 따른다**
〈2026-09-01 사장님 지침 — 「공공 API 활용 지침서」 · 같은 날 **v1.3 규정집**으로 갱신〉.

**원문은 그 파일 한 곳에 있다. 여기 옮겨 적지 않는다** — 두 벌이 되면 한쪽이 옛말을 한다
(§8-1 과 같은 결). 아래 §4.1~§4.9 는 **엔진이 그 값을 어떻게 쓰는가**의 규칙이고,
지침서는 **열쇠를 어떻게 넣고 어떻게 부르는가**의 절차서다 — 겹치지 않는다.

그 문서에서 이 저장소의 일과를 바꾸는 것 일곱만 여기 적는다.
★ **절 번호로 가리키지 않는다** — 판이 바뀌면 번호가 움직인다. v1.2 의 인벤토리는 §1,
  v1.3 은 §4 였고, 번호로 찾던 검사가 그 자리에서 네 칸 통째로 빨개졌다. **낱말로 가리킨다.**

- **Secrets 는 다시 읽을 수 없다.** 그래서 키를 꺼내는 대신 **키가 있는 자리(Actions)에서
  대신 호출하고 결과만 커밋**한다. 키는 저장소 밖으로 안 나가고 로그에도 안 남는다.
  ★ 다만 이 저장소는 **공개**다 (D-10) — **결과 파일에 개인정보·키가 섞이지 않았는지**
    커밋 전에 눈으로 센다 (지침서 「이식 체크리스트」 · §2).
- **진단부터 짠다** — 규격을 모를 때 추측으로 파라미터를 넣지 않는다. 첫 스크립트는
  응답 본문을 200자 이상 그대로 저장하고 결과 코드·메시지를 요약에 남긴다.
  ★ R-ONE 은 이것을 안 해서 **여섯 번 다시 썼다.** §4.3 「붙이기 전에 실제로 불러본다」의
    앞 단계다 — 부르기 전에 **무엇이 오는지부터 본다.**
- **서버 필터가 안 먹는 API 가 있다.** R-ONE 의 `CLS_DATANO` 는 무엇을 넣든 같은 결과가
  온다. 그런 곳은 전량 받아 **클라이언트에서 거른다.** 「필터를 넣었으니 걸러졌다」로
  넘어가면 문서에는 「적용됨」만 남는다 (§4.9 와 같은 결).
- **고친 뒤에 `Re-run` 을 누르지 않는다** 〈v1.3 「재실행 함정」〉. GitHub 의 재실행은
  **그 실행이 썼던 커밋의 코드를 그대로 다시 돌린다** — 스크립트를 고쳐 놓고 눌러도
  **옛 코드로 돌아 같은 실패가 반복된다.** 반드시 `Actions` 목록에서 **[Run workflow]** 로
  새로 건다. 화면 위가 `Manually triggered` 인지, 커밋 해시가 최신인지 한 번 본다.
  ★ 이 저장소의 M-31(「걸었다 ≠ 올라갔다」)과 같은 결이다 — **초록인데 아무것도 안 바뀐다.**
- **승인은 키 단위가 아니라 서비스 단위다** 〈v1.3 부록 D — 승인 서비스 16건〉.
  §4.2 가 말하는 그 자리인데, v1.3 은 **활용기간**까지 붙는다고 적는다 — 만료되면
  **키는 멀쩡한데 그 서비스만 거부**되고, 증상이 미승인과 구별되지 않는다.
  그러니 진단 요약에 **서비스 이름과 결과 코드를 함께** 남긴다 (`api-report.js` 가 그렇게 적는다).
- **수집 잡에 접속 자격증명을 넣지 않는다** 〈v1.3 2-8〉. NAS SSH·터널 키는 **배포 잡에만**
  넣는다. 공공 API 를 부르는 워크플로에 섞어 두면, 그 잡이 부르는 **바깥 서버 쪽 오류 본문**에
  섞여 나갈 자리가 생긴다 (§2). `api-smoke.yml` 에는 데이터 열쇠 16개만 있고 접속정보는 없다.
  ★ 예외는 하나 — `VWORLD_DOMAIN` 은 자격증명이 아니라 **등록 도메인 문자열**이라
    `VWORLD_KEY` 와 **함께** 넣는다. 한쪽만 넣으면 거부된다 (§4.1).
- **`_summary.md` 를 반드시 만든다.** JSON 원본은 크고 안 읽힌다. 사람이 읽는 요약이
  있어야 결과 확인이 빠르고, 다른 도구에 넘길 때도 그 파일 하나면 된다.
- ★★★ **열쇠가 있는 자리가 곧 「닿는 자리」는 아니다** 〈2026-09-12 · 실측 · D-206〉.
  **GitHub Actions 러너에서 `apis.data.go.kr` 이 안 열린다.** 특일정보 첫 실행에서
  5년 × 4갈래 **스무 칸 전부**가 응답 없이 죽었고(`fetch failed`), 같은 날 진단에서도
  data.go.kr 계열 넷(KOSIS·REC·금융위·조달청)이 전부 같은 모습이었다 — **한국은행·부동산원은
  살아 있었다.** 러너가 해외 IP 라서다. 활용신청·키와는 **아무 상관이 없다.**
  ★ 그러니 data.go.kr 계열 수집은 **국내 자리(NAS)에서** 돈다. Actions 는 그 외 기관에만 쓴다.
    ★★ **도는 자리가 생겼다** 〈2026-09-12〉 — `im-agent/tools/calendar-nas.sh` 다. NAS 가 스스로
      자기 것을 부르므로 **이 스크립트에는 접속 자격증명도 데이터 열쇠도 없다** (규정집 2-8) —
      열쇠는 엔진 뿌리의 `linkpilot.env` 에서 `core/env.js` 가 읽는다. 부르는 것은
      DSM 「작업 스케줄러」이고, `npm run calendar:nas` 로도 같은 것이 돈다.
      ★★★ **「깔려 있다」와 「스케줄러가 찾는다」는 다른 사실이다** 〈2026-09-13 · 사장님 화면 ·
        실측〉. 패키지 센터에 Node.js v22 가 「실행 중」이어도, DSM 「작업 스케줄러」가 주는
        **PATH 가 좁아** 그 자리를 못 볼 수 있다. 그때 옛 판이 내던 말은 「Node.js 패키지를
        켜야 한다」였는데, **이미 켜 두신 것을 또 하시게 만든다** (§4.6 「원인을 사람 말로
        적는다」와 같은 결). 이제 DSM 이 까는 자리 둘을 **판 번호 없이** 함께 훑는다 —
        v22 로 박아 두면 판을 올리시는 날 **조용히** 깨지고 증상이 「안 깔렸다」와 같아진다.
        ★ 재는 자리: `calendar-fetch.test.js` 가 **PATH 를 실제로 좁혀 돌려** 잰다(12·13·14칸).
          되돌려 보니 **옛 판이 그 자리에서 빨개진다** — 지금 NAS 에 있는 판이 그것이다.
      ★★★ **자리가 `im-agent/` 안인 것이 규격이다** — `deploy/engine.sh` 가 NAS 로 올리는 것은
        `im-agent/` 뿐이다. 처음에 `deploy/` 에 두었더니 **저장소에는 있고 NAS 에는 없었고,
        검사는 초록이었다.** 「만들었다」와 「닿는다」는 다른 사실이다 (§8 과 같은 결).
        `calendar-fetch.test.js` 가 이제 **배포가 빼는 폴더에 걸리지 않는지까지** 잰다.
      ★ **「돌았다」와 「채워졌다」를 갈라 센다** — 받은 해가 0 개면 빨갛게 끝난다(값 4).
      앱이 읽을 자리로 못 옮긴 것은 또 다른 값(5)이다. 뭉뚱그리면 무엇이 빠졌는지 사라진다.
    ★ Actions 쪽 `calendar-fetch.yml` 은 **지우지 않았다** — 러너 쪽이 나중에 바뀌는지
      다시 재 보는 자리로 남긴다. 그 워크플로 머리에 「여기서는 못 돈다」를 적어 두었다.
  ★★ 그리고 **「못 닿음」을 「승인 안 됨」으로 적지 않는다.** 앞 판은 `HTTP undefined` 만
    찍었는데, 그 글이 승인 문제처럼 읽혀 **이미 하신 활용신청을 또 하시게 만든다.**
    `kasi.js` 의 `diagnose()` 가 이제 `unreachable` 로 따로 갈라 준다 (재는 자리:
    `im-agent/test/calendar-fetch.test.js`).
  ★★★ **그리고 그 가름이 «국내 자리 첫 실행»에서 값을 했다** 〈2026-09-13 · 실측 · NAS 에서 잼〉.
    DSM 「작업 스케줄러」가 `calendar-nas.sh` 를 처음 돌렸다. **같은 스무 칸인데 말이 달랐다** —
    러너에서는 전부 `fetch failed`(응답 없음)였는데 NAS 에서는 **서버가 대답했다**:
    「이 API 에 **활용신청이 안 됐다** — data.go.kr 에서 「특일정보」를 신청한다. **키 문제가 아니다**」.
    ★ **대답이 왔다는 것이 곧 「닿았다」다.** D-206 이 세운 「국내 자리에서 돈다」가 이제 **추정이 아니라
      잰 값**이다 — 그 전까지는 「NAS 에서는 열릴 것이다」였고, 그렇게 적혀 있었다.
    ★★ **갈라 놓지 않았으면 여기서 또 헛일이 났다.** 두 자리의 증상이 겉으로는 똑같이 「값이 안 온다」인데
      **할 일이 정반대**다 — 러너 쪽은 **자리를 옮기는 일**이고, NAS 쪽은 **활용신청 한 번**이다.
      뭉뚱그렸으면 이미 하신 신청을 또 하시게 하거나(M-86), 될 자리를 안 된다고 접었을 것이다.
    ★ **막고 있는 것은 이제 하나다** — `SpcdeInfoService`(`B090041`) 활용신청. 키는 그대로 쓴다:
      **승인은 키 단위가 아니라 서비스 단위**라 새 키를 받을 이유가 없다 (위 v1.3 부록 D).
    ★★ 함께 잰 것 둘: 도는 자리가 `/volume1/docker/linkpilot` 로 **맞고**, Node 가 `v22.19.0` 로
      **DSM 작업 스케줄러의 좁은 PATH 에서도 잡힌다** — 판 번호 없이 훑기로 한 것이 그대로 통했다.

★★★ **2026-09-17 — D-206 의 「러너에서 안 열린다」가 «이제 틀린 말»이다** 〈사장님이 돌리신 실행 · 실측〉.

사장님이 Actions 에서 특일정보 수집을 거셨고 화면은 ❌ 둘이었다. 그런데 **로그를 갈라 읽으니
러너에서 실제로 열렸다** — 2026년 **135건** · 2027년 **155건** · 2028년 **150건**.
2029·2030 만 0건이었고 **오류 줄은 한 줄도 안 찍혔다.**

- ★ **그러니 「Actions 에서는 못 돈다」를 지금 값으로 적지 않는다.** D-206 때는 스무 칸 전부
  `fetch failed` 였던 것이 **잰 값이었고**, 지금은 열리는 것이 **잰 값이다.** 둘 다 실측이고
  **사이에 무엇이 바뀌었는지는 모른다**(러너 대역·기관 쪽) — 모르는 것은 모른다고 적는다 (§4.7).
- ★★ **그래도 NAS 자리를 안 내린다.** 한 번 열렸다고 늘 열린다는 뜻이 아니다 — 두 자리를
  **둘 다 두고**, 어느 쪽이 실제로 채우는지는 결과 파일이 말한다. 「열렸다」와 「계속 열린다」는
  다른 사실이다.
- ★★★ **그리고 「활용신청이 막고 있다」도 틀린 말이 됐다** — 셋이 왔으니 그 서비스는 **이미 승인돼 있다.**
  §13 에 그렇게 적혀 있던 줄을 함께 고쳤다. **한 곳을 고치면 같은 말이 적힌 다른 자리도 센다.**

★★★ **0 건이 «셋째 갈래»였다 — 「못 받았다」가 아니라 「아직 공표 전」이다** 〈같은 날 · 고침〉.

`kasi.year()` 가 `ok: out.length > 0` 하나로 끝나, **갈래 넷이 전부 성공했는데 0건**인 것과
**전부 실패한 것**이 **같은 글자**로 나왔다 — 「✗ 한 건도 못 받았다」. 그리고 그것이 `exit 1` 이라
화면이 빨개진다. **§12-12 의 「못 물었다와 없다가 같은 값이면 멀쩡한 파일을 고치라고 말한다」와
같은 고장**이고, 실제로 사장님이 그 빨강을 보고 이 화면을 주셨다.

- ★ **가르는 잣대는 「오류가 있었는가」다** — 오류 0개 + 건수 0개면 **서버가 멀쩡히 대답한 것**이다.
  공휴일은 해마다 **관보로 확정된 뒤에** 이 API 에 실린다. 2029·2030 이 아직 그 전이다.
- ★★ **고칠 자리가 없는 일을 실패로 세지 않는다.** 날마다 빨개지면 그 빨강이 **뜻을 잃고**,
  더 나쁘게는 「활용신청을 또 하라」로 읽힌다 (D-206 이 겪은 그 자리).
  이제 `◦ 2029년 — 서버는 대답했는데 그 해 자료가 아직 없다 (공표 전 · 고칠 것 없음)` 으로 적고
  **실패로 안 센다.** 통째로 못 받은 해는 **여전히 빨갛다.**
- ★★★ **요약도 셋으로 갈라 적는다** — 받음 · 공표 전 · 못 받음. 표의 ✓/✗ 둘뿐이면
  요약만 봐서는 못 가린다.
- 재는 자리: `im-agent/test/calendar-fetch.test.js` 세 칸(17칸) — 사보타주 셋으로 확인했다.
  ★ 그 셋을 처음 쓸 때 **둘이 옳은 코드에 빨개졌다** — 창을 400자로 잡아 **옆 블록**을 제 것으로
    읽었고(§12-6), `[^)]*` 가 **화살표 함수의 인자 괄호**에서 막혔다. 재려던 성질은 그대로 두고
    **창을 괄호 짝으로, 정규식을 한 줄 안으로** 옮겼다 (§6-2-5 의 잣대).

★★ **「미검증」을 재는 길이 있다 — `Actions → 공공 API 실측 진단`** 〈2026-09-01 신설〉.
  이 자리에는 열쇠가 없어 `im:smoke` 가 전부 「미설정」으로 나온다(§4.3). 그래서
  **열쇠가 있는 자리에서 돌리고 결과만 커밋**한다 — `data/_api/_summary.md`.
  ★ 이 저장소는 **공개**다. `tools/api-report.js` 가 쓰기 전에 **`process.env` 의 실제 값이
    본문에 있는지** 세고, 걸리면 **파일을 안 쓰고 빨갛게 끝난다**. 이름만 말하고 값은 안 찍는다.
  ★★ 「진단이 통째로 죽은 것」과 「한 항목이 실패한 것」을 **갈라 적는다** — 뭉뚱그리면
    무엇을 못 쟀는지 사라진다 (§8 「못 잰 것은 통과가 아니다」와 같은 결).

★ **열쇠 이름이 갈리는 것은 `npm test` 가 잡는다** (`im-agent/test/api-guide.test.js`).
  지침서 표 · 커넥터 · `SECRET_ENV` 셋이 어긋나면 빨개진다. 이름이 어긋나면
  **아무 오류도 안 나고 조용히 값이 죽기 때문**이다 — 실제로 두 번 났다
  (`ECOS_API_KEY`/`ECOS_BOK_KEY` · `LAW_OC`/`LAW_OPEN_DATA`). 답은 **둘 다 읽는 것**이다.

공공데이터는 이 시스템에서 **독립된 두 번째 출처**다. 사업계획서가 말하는 숫자와
공부(公簿)가 말하는 숫자가 다를 때 그것을 드러내는 것이 존재 이유이지, 화면을
채우는 장식이 아니다.

### 4.1 기관과 키

**기관마다 키가 다르고 서로 통용되지 않는다.** 한 곳 키를 다른 곳에 넣으면
인증 오류만 나온다.

| 기관 | 키 | 커넥터 | 쓰는 곳 |
|---|---|---|---|
| VWorld | `VWORLD_KEY` · `LINKPILOT_VWORLD_REPORT_KEY` · `LINKPILOT_VWORLD_WEB_KEY` (셋 다 읽는다) + `VWORLD_DOMAIN` | `vworld` `nsdi` | 지오코딩·지적·토지특성·용도지역·공시지가 |
| 공공데이터포털 | `DATA_GO_KR_KEY` | `molit` `kpx` `fsc` | 실거래가·건축물대장·인허가·REC·법인정보 |
| 기상청 API허브 | `KMA_APIHUB_KEY` | `kma` | 일사량·일조시간 (태양광) |
| 한국부동산원 | `REB_API_KEY` | `reb` | 지가지수 → 공시지가 시점수정 |
| 한국은행 | `ECOS_API_KEY` | `ecos` | 시장금리(기준선)·생산자물가지수(단가 시점수정) |
| 통계청 | `KOSIS_API_KEY` | `kosis` | 제조업 가동률·출하액 → **대조용**(값을 채우지 않는다) |
| 법제처 | `LAW_OC` | `law` | 법령·시행령 조문 원문 · **지자체 건축조례** → 법정 한도의 근거 |

- **`VWORLD_DOMAIN` 은 콘솔의 서비스URL 을 글자 그대로 넣는다.** 스킴·경로를
  벗기면 `ned/*` 계열이 **간헐적으로** 인증을 거부한다(실측 5회 중 2회).
  간헐적 실패는 "가끔 값이 안 들어온다"로만 보여 원인 추적이 가장 오래 걸린다.
- **`LAW_OC` 는 키가 아니라 「OC」다** 〈2026-08-25 사장님 지시 · 신설〉.
  신청에 쓴 **이메일의 `@` 앞부분 그대로**다. 그리고 **사용 승인이 따로** 있어야
  돈다(신청 후 1~2일) — 승인 전 오류가 **OC 오타와 구분되지 않으므로**
  `law.js` 의 `diagnose()` 가 승인·OC·없음·형식 넷으로 갈라 준다.
  ★ 짧다고 공개해도 되는 값이 아니다. 그 값이 곧 **승인된 이용자 식별자**다 (§2).
  ★★ **시행령 상한을 조례 한도로 쓰지 않는다.** 용적률·건폐율의 실제 한도는
  조례가 정한다 — `law.ordinance()` 로 그 조례를 찾아 근거를 붙인다.
- **`DATA_GO_KR_KEY` 는 Decoding(일반) 인증키다.** Encoding 키는 한 번 더
  인코딩되어 인증만 실패하고, 증상이 "키가 틀렸다"와 구분되지 않는다.
- 새 Connector 를 붙이면 `connectors/http.js` 의 `SECRET_ENV` 에 키 이름을 더한다.
  거기 없으면 로그·오류 메시지에 평문으로 남을 수 있다 (§2).

### 4.2 활용신청은 API 하나하나에 필요하다

키가 유효해도 신청하지 않은 API 는 거부된다. **키 문제와 구분하려면 응답 본문을
봐야 한다** — 상태코드만 보면 둘이 같아 보인다.

| 응답 | 뜻 |
|---|---|
| `SERVICE_KEY_IS_NOT_REGISTERED_ERROR` (30) | 그 API 에 활용신청이 안 됨 |
| `NO_OPENAPI_SERVICE_ERROR` (12) | 엔드포인트가 없거나 폐기됨 |
| `403 활용신청이 필요한 API` (API허브) | 위와 같음 |

### 4.3 붙이기 전에 실제로 부르고, 보고서까지 돌려 본다

검증은 **두 단계**다. 하나만 하면 반쯤 틀린 채로 나간다.

**① 스모크 — 커넥터가 응답을 받는가**

`npm run im:smoke` 로 실측하고 응답 필드가 기대와 같은지 확인한 뒤 배선한다.
스모크에 항목을 추가하는 것이 커넥터 작업의 일부다.
이 단계가 없던 동안 폐기된 엔드포인트(`BldRgstService_v2`)를 붙잡고 있었다.

★★ **스모크로 「운영 키가 들어 있는지」를 재지 않는다** 〈2026-08-25 · 실측〉.
열쇠는 **GitHub Secrets 와 NAS 의 열쇠 파일**에만 있고, 이 저장소나 개발
컨테이너에는 없다. 그래서 여기서 `im:smoke` 를 돌리면 **전부 「미설정」**이
나온다(실측: `0/0 통과`) — 키가 멀쩡히 들어 있어도 그렇다. **없는 것과
구분이 안 된다.**

- 스모크가 재는 것은 **「이 커넥터가 응답을 제대로 읽는가」**다. 키가 있는
  자리에서 돌려야 뜻이 있다.
- **운영 키가 실제로 들어 있고 살아 있는지**는 배포가 잰다 —
  `deploy-nas` 의 「Check map power」·「Why is the key not read」가
  `tools/env-doctor.js --keys` · `--live` 로 **NAS 안에서** 묻는다.
  이름과 길이만 찍고 값은 한 글자도 안 남긴다 (§2).

**② 파이프라인 — 그 값이 보고서에 실리는가**

`npm run im:demo` 를 돌리고 **세 곳을 눈으로 확인한다.**

1. `01_Project/dataset.json` 의 `facts` — 값이 들어갔는가, 출처가 맞는가
2. `09_IM/im.md` 본문과 출처표 — **실제로 문서에 나오는가**
3. Agent 경고 로그 — 조용히 건너뛴 것이 없는가

**스모크 통과는 보고서 반영을 뜻하지 않는다.** 2026-08-16 에 스모크 14/14 인
상태로 파이프라인을 돌렸더니 셋이 나왔다 — 시점수정이 통째로 빠져 있었고
(`slice(0,6)` 로 `'20260'`), 엉뚱한 지역의 지수로 보정하고 있었고
(통계표별 지역코드 체계 차이), 지역·지구가 `dataset` 에만 있고 IM 에는
없었다(`im-outline.js` 의 `dataKeys` 누락).

**셋 다 결과가 그럴듯했다.** 평가액은 정상으로 나오고 출처 표시도 멀쩡했다.
로그를 보지 않으면 알 수 없다. 그래서 값이 하나 늘 때마다 `dataKeys` 와
경고 로그를 함께 본다.

### 4.4 응답 정렬 방향을 먼저 확인한다

**국내 공공 API 는 오래된 것부터 주는 경우가 많다.** 개별공시지가·토지특성·
REC 현물시장·지가지수가 그랬다. 앞에서부터 집으면 몇 년 전 값을 최신으로 쓰고,
**출처 표시는 멀쩡해서 문서만 봐서는 안 잡힌다.**

- 최신값이 필요하면 **정렬을 직접 하거나 조회 기간을 명시한다.**
- 기간 누적이 필요하면 **변동률을 곱해 쌓지 말고 지수의 비를 쓴다.**
  변동률 누적은 구간 경계에서 한 달을 더 세기 쉽다.

### 4.5 호출을 늘리지 않는다

- data.go.kr 계열은 **일 10,000건 한도**다. `connectors/cache.js` 를 반드시 경유한다.
- **이미 채워진 값을 다시 부르지 않는다.** 예: 건축물대장이 대지면적을 채웠으면
  토지특성을 호출하지 않는다.
- 자산군 전용 조회는 **해당 딜에서만** 부른다 (일사량·REC 는 태양광 딜만).
- 목록·색인처럼 큰 조회는 TTL 을 길게 잡는다 (지역 색인 180일).

### 4.6 실패는 격리한다

3회 재시도(1s/2s/4s) 후 예외 대신 `{ok:false}` 를 돌려준다. **한 소스가 죽어도
IM 생성 전체를 죽이지 않는다.** 인증·요청 오류(400/401/403/404)는 재시도하지 않는다.

**키가 없으면 `unavailable` 을 반환하고 그 절을 비운다. 지어내지 않는다.**

★★★ **「기다리면 낫는 것」과 「기다려도 안 낫는 것」을 갈라 다룬다**
〈2026-09-13 사장님 지시: 「명함 촬영후 목록에 리스팅되는 과정이 너무 오래걸리고 느림 ·
 OCR인식이 과거보다 실패가 많고 오류가 반복됨」 · 화면 사진〉.

- **무엇이었나.** 사진에 원인이 그대로 찍혀 있었다 — `Your credit balance is too low`.
  **OCR 성능이 아니라 계정 잔액**이다. 그런데 코드는 이것을 **한도 초과와 같이** 다뤄
  장마다 3회 재시도 + 최대 30초씩 두 번을 기다렸다. 8장이면 그 헛기다림이 **분 단위**다.
- ★ **한도(429)·과부하·네트워크는 기다리면 낫는다. 잔액·열쇠·권한(401·403)은 절대 안 낫는다.**
  둘을 같은 것으로 다루면 기다림이 통째로 버려진다. 판정식을 한 곳에 두고 갈라 부른다.
- ★★ **계정 문제는 «장마다» 묻지 않는다.** 한 장이 그 오류면 나머지도 같다 — 남은 장은
  아예 안 부르고 같은 사유로 표시한다. 분 단위가 초 단위가 된다.
  ★ 다만 **새 묶음마다 그 표시를 비운다** — 안 비우면 충전하신 뒤에도 계속 막힌다.
- ★★★ **원인을 사람 말로 적는다.** 「AI 분석 실패 — Claude API 오류 (400) Your credit
  balance is too low」는 사장님께 **「OCR이 나빠졌다」로 읽힌다** — 실제로 그렇게 읽히셨고,
  그래서 **엉뚱한 곳을 고치라는 지시**가 나왔다. 글이 원인을 가리면 그 시간이 통째로 버려진다.
- ★ **고치는 방향이 반대로 가는 것도 함께 막는다.** 「전부 재시도 안 함」으로 넘어가면
  한도 회복이 깨져 **무료 AI 가 평소에도 실패한다.** 검사가 **두 갈래를 각각** 잰다 —
  「못 낫는 것 여섯을 잡는가」와 **「기다리면 낫는 것 여섯을 안 잡는가」**.
- 재는 자리: `linkpilot-platform` 의 `test-card-ai-fatal.js` (교차검증 81번째 칸).

★★★ **실패 사유는 «어느 길로 갔는지»까지 싣는다 — 그러지 않으면 고칠 자리가 추측이 된다**
〈2026-09-15 사장님 지시: 「구글아이디로 접속과정에 반복적인 에러 오류 발생 교차검증하고 개선해줘」 ·
 화면 사진〉.

- **무엇이 났나.** 화면이 「서버가 구글에 닿지 못했습니다 … **NAS 의 바깥 연결(방화벽·DNS)**을
  봐야 합니다 (사유: 못 닿았다 · 응답 없음)」로 적고 있었다. 그런데 그 사유 글자는 서버의
  **`file_get_contents` 갈래에서만** 나온다 — cURL 쪽이었으면 「curl 로 못 닿았다」로 적혔다.
  즉 **「PHP 에 curl 확장이 없다」가 사유 안에 이미 들어 있었는데**, 화면이 그것을 버리고
  **방화벽을 가리켰다.** 사장님은 방화벽을 보러 가시고, 거기에는 고칠 것이 없다.
- ★ **그래서 규칙은 둘이다.** ① 서버는 **어느 길로 갔는지**를 사유에 싣는다(두 갈래의 글자가
  겹치면 앱이 못 가른다). ② 앱은 그 갈래를 **갈라 읽어** 서로 다른 곳을 가리킨다.
  하나만 하면 반쯤 틀린 채로 나간다 (§4.3 과 같은 결).
- ★★ **그리고 추측하지 않는다 — «재는 창구»를 둔다.** `gcal-oauth.php` 의 `?action=diag` 가
  NAS **안에서** curl·allow_url_fopen·DNS·닿기·열쇠 등록 다섯을 재서 돌려주고, 화면이 그
  답을 그대로 보인다. ★ **잴 길이 없으면 「못 쟀다」로 적는다** — 통과로 안 적는다 (§8).
- ★★★ **진단 답을 통째로 찍지 않는다.** 응답에 열쇠가 섞이면 그 자리에서 샌다 (§2) —
  담는 것은 **있다/없다**와 PHP 판뿐이고, 화면도 줄을 하나씩 골라 찍는다.
  ★ 검사가 **미끼 비밀**을 응답에 섞어 두고 화면에 나오는지 센다. 누가 나중에
    `JSON.stringify(diag)` 로 바꾸면 그때 빨개진다.
- ★ **진단 창구는 아무것도 안 바꾼다.** 재는 손과 고치는 손을 갈라 둔다 (§10 과 같은 결).
- ★★ **낱말이 아니라 «시키는가»를 잰다.** 고친 글은 「**방화벽이 아니라** «PHP 설정»입니다」라고
  부정으로 쓴다 — 「방화벽」이라는 낱말만 세면 그 좋은 글이 빨개지고, 그때 「검사를 약하게
  고칠까」가 된다. 재려던 성질(**틀린 곳을 가리키는가**)은 그대로 두고 **세는 자리를 옮겼다**
  (§6-2-5 「검사를 약하게 고치는 것과 재려던 것을 재게 고치는 것은 다르다」와 같은 잣대).
- 재는 자리: `linkpilot-platform` 의 `test-gcal-diag.js` (교차검증 **100번째 칸**) —
  서버 사유 · 앱 갈래(**돌려서**) · 진단 창구 · **그려서** 누르기 전후를 함께 본다.

★★ **무료 AI 열쇠는 «서로의 몫을 대신한다» — 그것이 도는지 잰다**
〈2026-09-13 사장님 지시: 「구글 무료 API 6개 자동충전처리 검증해줘」〉.

- 잰 것 여덟: 열쇠를 **한 바퀴 다** 도는가 · 된 열쇠부터 **다음에 시작**하는가 ·
  막힌 것을 쉬게 했다 **풀어 주는가** · **전부 잠겨도** 한 번 걸어 보는가 ·
  열쇠 **값이 새지 않는가**(§2) · 전부 막히면 **몇 개 중 몇 개**인지 말하는가 ·
  무료를 **다 써 본 뒤에만** 유료로 가는가 · 무료가 **1순위**인가. **여덟 다 초록이다.**
- ★★★ **그러므로 사장님 화면의 원인은 「자동충전이 안 돈 것」이 아니라 「열쇠가 다 소진된 것」**이다.
  구조가 멀쩡한데 원인을 구조에서 찾으면 고칠 것이 없는 자리를 고치게 된다.
- ★ **열쇠가 실제로 몇 개인지는 이 자리에서 못 잰다** — 열쇠는 NAS 에만 있다 (§4.3 과 같은 결).
  그래서 이 검사가 재는 것은 **「몇 개가 있든 그 전부를 돌려 쓰는 구조인가」**다.
  개수는 앱의 `내 정보 → AI` 가 말한다. **못 잰 것을 통과로 적지 않는다.**
- ★★ **처음엔 「폴백 조건이 너무 넓다」를 재려 했는데 그것이 틀렸다.** 읽어 보니 그 조건은
  이미 열쇠·모델을 **전부 돌고 던진 뒤**에만 닿는다. 재야 할 것은 조건의 넓이가 아니라
  **「다 돌기 전에 새는 길이 없는가」**였다 — 재려던 성질은 그대로 두고 **재는 자리를 옮겼다**.
- 재는 자리: `linkpilot-platform` 의 `test-gemini-keypool.js` (교차검증 82번째 칸).

★★★ **열쇠는 «있는 자리에서» 모아 올린다 — 꺼내 오지 않는다**
〈2026-09-13 사장님 지시: 「GEMINI_API_KEY_1~15 넣어두었어 자동으로 돌려줘」〉.

- ★ **왜 규칙인가.** 사장님이 Secrets 에 15개를 넣어 두셨는데 **읽는 코드가 한 줄도
  없었다.** 넣으신 것과 도는 것이 갈려 있었고 **아무 오류도 안 났다** — 앱은 옛 열쇠만
  썼고 화면은 멀쩡했다. 「넣었다」와 「쓰인다」는 다른 사실이다 (§8 「걸었다 ≠ 올라갔다」와 같은 결).
- **Secrets 는 다시 못 읽는다**(§4). 그러니 값을 꺼내 코드·문서로 옮기는 길은 없다.
  **키가 있는 자리(Actions)에서 모아 국내 자리(NAS)로 올리고, 로그에는 개수만** 남긴다.
- ★★★ **빈 것으로 덮지 않는다.** 열쇠가 하나도 안 들어오면 **기존 파일을 그대로 둔다.**
  덮으면 지금 도는 열쇠가 사라지고 **되돌릴 길이 없다** — 값은 우리 쪽에 없기 때문이다.
- ★★ **올린 뒤 「몇 개가 있는지」 되묻는다.** 「올렸다」와 「그만큼 있다」는 다른 사실이다.
  다르면 경고로 적는다 — 뭉뚱그리면 앱이 실제로 몇 개를 쓰는지 사라진다.
- ★★★ **덮는다는 사실을 함께 적는다.** 화면(`[내 정보 → AI]`)에서 올린 공용 열쇠와
  **같은 파일**을 쓰므로 배포가 돌면 **Secrets 쪽이 이긴다.** 그것이 지시의 뜻이지만,
  **화면에서 넣은 것이 다음 배포에 사라지는 것**도 같은 사실이라 양쪽을 다 적는다.
  ★ 개인 열쇠(브라우저에 저장한 것)는 안 건드린다 — 그쪽은 그 파일이 아니다.
- ★ **새 규격을 만들지 않는다.** 앱이 이미 읽는 형식(404 가드 + 줄바꿈)을 그대로 쓴다.
  가드 없이 올리면 **주소만 알면 누구나 내려받는다** (§2).
- 재는 자리: `linkpilot-platform` 의 `test-gemini-keypool.js` — 열쇠 나르기 다섯 칸이
  같은 파일에 붙었다(13칸 · 못 잼 1). ★ **못 잼 1 을 지우지 않는다** — NAS 에 실제로 몇 개가
  들어갔는지는 이 자리에서 못 잰다. 배포 로그의 「확인: NAS 에 N 개가 있다」가 잰다.

★★★ **그리고 「Actions 가 읽었다」와 「앱이 쓴다」도 다른 사실이다 — 세는 잣대가 갈리면
  «올라간 열쇠»가 「0 개」로 적힌다** 〈2026-09-15 · 실측 · 배포 116〉.

- **무엇이 났나.** 배포가 「무료 AI 열쇠 **0 개**」로 두 번 적었다. 나는 그것을 근거로
  사장님께 **「열쇠가 안 갔습니다」**라고 적고, 비밀 칸을 다시 보시라고까지 안내했다.
  그런데 로그를 갈라 읽어 보니 **「한 개도 안 들어왔다」 갈래는 안 탔다** — 비밀은
  읽혔고 파일도 NAS 에 올라갔다.
- ★★★ **틀린 것은 «세는 법»이었다.** 되묻기가 `^AIza` 로 시작하는 줄만 셌는데,
  **앱은 그것을 안 따진다** — `lpSplitKeys` 의 잣대는 **「20자 이상 · 꺾쇠 없음」**이다.
  그래서 앱이 멀쩡히 쓰는 열쇠가 로그에서는 0 으로 세졌다.
- ★ **잣대는 하나다 — 「그 숫자를 재는 법이 재려는 것을 다 덮는가」** (§6-2-6 의 46 → 105 ·
  §8 의 쪼개기 래칫과 **같은 규칙**이다. 그때는 덜 세었고, 이번엔 **다른 것을** 세었다).
- ★★ **고침은 잣대를 맞추는 것이다.** 요약에 싣는 수는 **앱이 쓰는 수**로 바꾸고,
  `AIza` 개수는 **버리지 않고** 갈래를 가르는 데만 쓴다(앞뒤 따옴표·빈칸이 붙은 값을
  그것으로 잡는다). 둘이 다르면 **그 사실을 적는다.**
- ★★★ **「0 개」를 본 순간 「없다」로 적지 않는다.** 「못 쟀다」·「다른 잣대로 셌다」·
  「정말 없다」가 다 0 으로 보인다 (§4.7 · S-54 「소진과 없음은 다르다」와 같은 결).
  **먼저 그 0 이 어느 갈래인지 가른 뒤에** 사장님께 적는다.
- 재는 자리: `linkpilot-platform` 의 `test-gemini-keypool.js` — 앱의 잣대를 소스에서 읽어
  워크플로의 잣대와 **견준다**(합치지 않고 어긋남을 센다).
  ★ 견주는 **변수 이름은 박지 않는다** — 그날 이름을 옮겼더니 고침이 옳은데도 빨개졌다.

★★★ **「비밀에 넣었다」와 「Actions 가 읽는다」는 다른 사실이다** 〈2026-09-13 · 실측 · 첫 실행〉.

- **무엇이 났나.** 위 배선을 올리고 처음 돈 배포에서 **열다섯 칸이 전부 빈 값**으로 들어왔다
  (로그 본문: `K1:` … `K15:` 가 모두 빈칸 · 「한 개도 안 들어왔다」). 사장님은 화면에서
  분명히 넣으셨는데도 그랬다.
- ★ **갈라 보는 법이 로그 안에 이미 있었다** — **같은 워크플로의 다른 비밀**(NAS 접속·
  타일스케일)은 `***` 로 멀쩡히 찍혔다. 그러니 「워크플로가 비밀을 못 읽는다」가 아니라
  **「그 이름이 이 저장소의 Actions 비밀에 없다」**로 좁혀진다.
- ★★★ **GitHub 의 비밀은 세 군데에 따로 있고 Actions 는 그중 하나만 읽는다** —
  `Actions` · `Codespaces` · `Dependabot`. 그리고 `Environment secrets` 는 그 잡이
  `environment:` 를 쓸 때만 닿는다(이 잡은 안 쓴다). **넣은 화면이 다르면 아무 오류 없이 빈 값**이다.
- ★★ **그래서 「이름을 확인한다」로 끝내지 않는다.** 앞 판의 경고가 그랬는데, 그러면
  **어느 화면을 여셔야 하는지가 안 보인다** — §5 의 「어디서·무엇을·어떻게·그러면」이
  로그에도 그대로 필요하다. 이제 볼 딱지 이름과 못 읽는 자리 셋을 로그가 직접 적는다.
- ★ **일부만 들어온 경우 «몇 번 칸이 비었는지» 번호로 적는다.** 「열다섯 중 셋」으로
  뭉뚱그리면 어느 칸을 다시 넣어야 하는지 사라진다.
- ★★★ **그리고 이 경고는 아직 «차단이 아니다».** 열쇠가 0개여도 배포는 초록으로 끝난다 —
  실제로 그래서 「됐다」로 읽혔다. 막는 것과 알리는 것을 가르는 잣대는 하나다:
  **그것 없이도 서비스가 도는가.** 열쇠 0개는 옛 열쇠로 계속 도므로 알림이 맞지만,
  **「알렸는데 아무도 안 본 상태」**를 만들지 않으려면 다음 배포 보고에 그 줄을 싣는다.

### 4.7 출처에 무엇을 적는가

값만 옮기지 않는다. **그 값이 어디서·언제·어떤 조건으로 나왔는지**를 함께 남긴다.

- 기관명과 기준시점 — 예: `개별공시지가(2026년 고시)`
- **대리값이면 그 사실** — 일사량은 부지가 아니라 관측소에서 잰 값이므로
  `관측소명 · 부지에서 N km · 관측연도` 를 적는다. 거리를 모르면 **모른다고 적는다.**
- **모수** — 평균값은 표본 수와 기간을 함께 적는다 (`99회 개장`, `47건 중앙값`).
- 결측이 있으면 결측 일수를 적는다. 결측을 0 으로 세면 합계가 조용히 작아진다.

### 4.8 가정이 섞이면 자동으로 내지 않는다

출처 있는 값끼리의 계산만 자동화한다. **가정계수가 하나라도 들어가면 사람이 넣는다.**

- 일사량은 낸다. **발전량은 내지 않는다** — 시스템효율(PR)이 가정이다 (D-25).
- REC·SMP 단가는 낸다. **매출은 내지 않는다** — 가중치와 발전량이 가정이다.
- 공시지가 시점수정은 **공표 통계라 가정이 아니다.** 현실화계수는 가정이므로
  `assumption` 에 그 구분을 적는다.

### 4.9 조회는 되지만 값을 못 쓰는 경우

**자동으로 하나를 고르지 않는다.** 틀렸을 때 문서만 보고는 잡히지 않기 때문이다.

- 동명 법인(`삼성물산` 은 실제로 2곳) → 후보를 내고 사람이 특정한다
- 한 필지의 인허가 이력 여러 건 → 이력을 보여 주고 사람이 고른다
- 지역을 특정하지 못한 시점수정 → **전국 값으로 대체하지 않고 적용을 포기한다**

대체값으로 메우면 문서에는 "적용됨"만 남아 무엇이 빠졌는지 사라진다.

## 5. 코드 작업 규칙

- 코드는 부분 스니펫이 아닌 완전한 작동 파일 단위로 작성한다.
- 수정 전 관련 파일을 먼저 읽고, 기존 패턴(모듈 구조, 에러 처리 방식)을 따른다.
- 버그·엣지케이스는 지적받기 전에 선제적으로 찾아 보고한다.
- 새 라이브러리 추가는 최소화한다. 표준 라이브러리와 기존 의존성을 우선한다.
- 스택 변경(예: 다른 CI, 다른 런타임) 제안 금지.
- 타임존 처리는 항상 명시적으로 한다 (Asia/Seoul 지정, 서버 로컬타임 의존 금지).
- 답변은 한국어, 결론 먼저, 실행 가능한 단계로 정리한다.
- **안내할 때는 쉽게 풀어서 말한다** 〈2026-08-21 사용자 지시〉.
  - 사용자가 **직접 손으로 해야 하는 일**(설정·등록·설치)을 안내할 때 특히 그렇다.
    코드 이야기는 짧게 해도 되지만, 사람이 화면을 눌러 가며 따라 하는 일은
    **모르는 낱말이 하나만 나와도 거기서 멈춘다.**
  - 낯선 말은 **처음 나올 때 한 줄로 풀어 준다** — OAuth·ACL·tailnet·개인키처럼.
    「아는 사람은 아는 말」로 쓰지 않는다.
  - **무엇을 위해 하는지 먼저** 말하고 그다음에 절차를 준다. 이유를 모르면
    중간에 막혔을 때 스스로 판단할 수가 없다.
  - **어디를 눌러야 하는지**를 적는다 (메뉴 이름 그대로). 「설정에서 등록한다」는
    안내가 아니다.
  - 막히기 쉬운 곳은 **미리** 「여기서 대개 막힌다」로 짚는다.
- ★★★ **사장님께 무엇을 부탁드릴 때는 넷을 반드시 적는다 — 「어디서 · 무엇을 · 어떻게 ·
  그러면 어떻게 되는지」** 〈2026-09-05 사장님 지시: 「어디서 어떻게 무엇을 그럼 어떻게 되는지 →
  풀어서 설명 (앞으로 나에게 요청하는 업무)」〉.
  - **왜 정했나.** 앞 판은 「무엇을·어떻게」만 적었다. 그러면 **어디서 하는 것인지가 안 보여**
    사장님이 **터미널에 붙여 넣으신다.** 실제로 그랬다 — 「PR 을 합쳐 주십시오」라고만 적었더니
    맥 터미널에서 `git pull` 을 치셨고, 그 일은 **웹브라우저에서 마우스로** 하는 일이었다.
    §8 의 「읽으라고 준 것과 실행하라고 준 것을 섞지 않는다」와 같은 결인데, 그때는
    **코드블록**이 원인이었고 이번엔 **자리를 안 적은 것**이 원인이다.
  - **① 어디서** — 웹브라우저인지 · 맥 터미널인지 · 폰인지 · NAS 관리화면(DSM)인지를 **맨 앞에**
    적는다. 「GitHub 홈페이지에서 마우스로」처럼 **도구와 손놀림**까지 적는다.
    ★ 주소가 필요하면 **누를 수 있는 링크로** 준다.
  - **② 무엇을** — 누를 단추 이름을 **화면에 쓰인 글자 그대로** 적는다(`[Ready for review]`).
    번역하지 않는다 — 화면은 영어인데 안내가 한국어면 못 찾으신다.
  - **③ 어떻게** — 몇 번째로 누르는지, 화면 **어느 쪽**에 있는지(맨 위·맨 아래·오른쪽 위),
    거기까지 어떻게 가는지(「아래로 끝까지 내린다」)를 적는다.
    ★ 막히는 자리는 **미리** 짚는다 — 「위쪽 딱지가 아니라 아래 상자 안의 단추입니다」.
  - **④ 그러면 어떻게 되는지** — 누른 **직후 화면이 어떻게 바뀌는지**와 **그것이 무슨 뜻인지**를
    적는다(「회색 자리가 초록 [Merge] 로 바뀝니다 = 이제 합칠 수 있다는 뜻」).
    ★★ 이것이 **성공 확인 표시**다. 이게 없으면 사장님이 **됐는지 안 됐는지 모르신 채로**
    다음 칸으로 넘어가시고, 그 상태가 아무 오류도 안 낸다 (§8 「걸었다 ≠ 올라갔다」와 같은 결).
    ★ 실패했을 때 보이는 것도 함께 적는다 — 「404 가 뜨면 다른 계정으로 로그인된 것입니다」.
  - ★★ **일이 여럿이면 순서를 번호로 매기고, 각 칸마다 넷을 다시 적는다.** 「그다음도 똑같이」로
    넘어가지 않는다 — 자리가 바뀌는 순간(브라우저 → 터미널)이 가장 잘 어긋난다.
  - ★ **내가 대신할 수 있는 칸은 그렇게 말한다.** 「제가 ③까지 눌러 드릴 수 있습니다」처럼
    적어, 사장님이 손으로 하실 몫을 **줄여 드린다**.
- **대화 전체를 쉬운 말로 한다** 〈2026-08-26 사용자 지시 — 위 규칙을 넓힘〉.
  - 위 규칙은 「손으로 해야 하는 일을 안내할 때」였다. **이제 모든 답변에 적용한다.**
    보고·분석·기술 설명도 마찬가지다.
  - **낯선 말을 쓰지 않는 것이 아니라, 쓰고 나서 바로 풀어 준다.**
    없앨 수 없는 말이 있다 — 파일 이름·명령어·오류 메시지는 그대로 적어야
    사장님이 화면에서 찾으신다. 대신 **그 옆에 한 줄로 뜻을 붙인다.**
  - 짧게 쓰는 것보다 **알아듣게 쓰는 것이 먼저다.** 줄이려다 뜻이 사라지면
    그 줄은 없는 것과 같다.
  - **비유를 하나쯤 쓴다.** 「합집합」보다 「둘을 합쳐 중복을 뺀 것」이 낫다.
  - 다만 **숫자와 사실은 줄이지 않는다.** 쉬운 말로 바꾸는 것이지
    뭉뚱그리는 것이 아니다 (출력 스타일 §「짧게 쓰지 않는 곳」과 같은 뜻).
  - **붙여 넣을 명령에 「바꿔 넣을 자리」를 두지 않는다** 〈2026-08-23 · 세 번 당했다〉.
    - `사용자@주소` 같은 예시를 넣으면 **그대로 붙여 넣어 실행된다.** 실제로
      세 번 그랬고, 그때 나오는 말은 `hostname contains invalid characters`
      뿐이라 **예시를 안 바꿨다는 생각 자체가 안 든다.**
    - ★ 그러니 **바꿀 것이 없는 길을 먼저 찾는다.** 같은 답을 주는 로컬 경로가
      있으면 그것을 준다 — SSH 를 안 쓰면 주소도 필요 없다.
    - 정말 값이 필요하면 **환경변수로 한 번만 받고**(`export LP_NAS_HOST=…`),
      명령 자체에는 그 이름만 쓴다. 그리고 예시가 그대로 들어온 것을
      **스크립트가 잡아 사람 말로 알려 준다** (`deploy/screens.sh` 가 그 예다).
  - **읽으라고 준 것과 실행하라고 준 것을 섞지 않는다** 〈2026-08-23 · 실제로 당했다〉.
    - 「이러면 틀린 것 / 이러면 맞는 것」 같은 **비교표를 명령처럼 생긴 줄로**
      적었더니 사장님이 그것을 터미널에 붙여 넣으셨다. 표에는 `←` 화살표까지
      있었는데도 그랬다 — **명령처럼 생기면 명령으로 읽힌다.**
    - ★ 비교·설명은 **표나 문장으로** 적고, 코드블록은 **실제로 칠 것에만** 쓴다.
      값을 비교해야 하면 코드블록 대신 「`IM_AGENT` 가 `/volume1/linkpilot/…` 로
      되어 있으면 틀린 것입니다」처럼 **문장 안에 인용**한다.
    - ★★★ **남에게 전달할 코드도 코드블록에 넣지 않는다** 〈2026-08-23 · 같은 날
      두 번째로 당했다〉. 본체 개발자에게 넘길 자바스크립트 한 줄
      (`window.LINKPILOT_EMBED = {…}`)을 코드블록으로 적었더니 사장님이
      **터미널에 붙여 넣으셨다** — `zsh: parse error near '}'`.
      - **내가 「이건 본체가 코드에 넣을 줄입니다」라고 적어 두어도 소용없다.**
        코드블록은 **누가 어디에 넣을 것인지와 무관하게** 「여기 복사해서
        실행하라」로 읽힌다. 사장님 자리에서 실행할 수 있는 곳은 터미널뿐이다.
      - ★ 그러니 전달할 코드는 **파일로 만들어** `docs/` 에 두고
        **「이 파일을 본체에 전달해 주십시오」**로 준다. 답변 본문에는
        **한 글자도 코드로 적지 않는다.**
      - 정 답변에 보여야 하면 **문장 안에 짧게 인용**한다 — 예: 「`LINKPILOT_EMBED`
        전역에 `api` 와 `token` 을 채워 달라고 전해 주십시오」.
  - **마우스로 되는 길이 있으면 그쪽을 먼저 준다** 〈2026-08-23〉.
    터미널은 오타·셸 차이(zsh 의 glob·따옴표)·예시 미치환이 겹쳐 **막히는 자리가
    셋**이다. File Station 처럼 눌러서 되는 길이 있으면 그것을 먼저 적는다.

## 6. 출력물 스타일

### 6-1. 그림은 SVG 와 JPEG 을 **둘 다** 낸다 〈2026-08-16 규칙〉

- **SVG 를 만들면 같은 이름의 JPEG 을 반드시 함께 만든다** (`core/raster.js` 의
  `alongside()`). SVG 를 지우지 않는다 — 벡터라 인쇄에서 안 깨지고 글자를 고를 수 있다.
- 왜: **SVG 는 브라우저에서만 확실히 열린다.** 카카오톡·메일 미리보기·파워포인트
  붙여넣기·인쇄 대화상자에서는 빈 칸이 되거나 아예 안 뜬다. 그림을 보라고 보냈는데
  빈 칸이 오는 것은 안 보낸 것보다 나쁘다 — 고장으로 읽힌다.
- **라이브러리를 들이지 않는다.** sharp·canvas·resvg 는 네이티브 의존성이라 §5 에
  걸린다. 이미 쓰는 **헤드리스 크로미움**이 확장자만 보고 JPEG 을 써 준다.
- **브라우저가 없으면 조용히 넘어가지 않는다.** 없는 서버가 실제로 있다 — 그때는
  경고와 YELLOW 플래그를 남긴다. 「JPEG 이 있어야 하는데 없다」를 아무도 모르는
  상태를 만들지 않는다.
- 인쇄까지 견디게 **2배 해상도**로 그린다 (`DEFAULT_SCALE`).

### 6-1-1. 설계 도면은 도면 규격으로 그린다 〈2026-08-25 사장님 지시 · M-53〉

「배치도·평면도·단면도」를 달라는 말은 **개념도가 아니라 도면**을 달라는 뜻이다.
설계사무소 납품물과 개념도를 가르는 것은 그림 솜씨가 아니라 **아래 일곱**이다.

- **도면틀 + 표제란** — 프로젝트명·도면명·도면번호·축척·작성일·개정란.
  대외 도면에는 `NOT FOR CONSTRUCTION` 과 §6-2 의 기밀 표기를 함께 박는다.
- **축척이 진짜여야 한다.** 시트를 A2(594×420) 같은 실제 규격으로 두고
  **도면 단위를 mm 로** 잡는다. 그러면 `stroke-width:0.7` 이 곧 0.7mm 이고
  `font-size:2.5` 가 도면 글자 2.5mm 다 — 자를 대면 맞는다.
- **선 굵기 위계** — 절단선 0.7 / 외곽 0.5 / 일반 0.35 / 보조 0.25 / 치수 0.13.
  도면의 표정은 색이 아니라 여기서 나온다.
- **치수선** — 보조선 + 사선(45°) 마감 + 치수값, 전체·주간·세부 여러 단.
  화살표는 기계도면이고 건축은 사선이다. 치수는 **mm**로 적는다(120,000).
- **통심선(구조 격자)** X1·Y1 버블 · **방위표** · **레벨 ▽** · **절단선 A-A'**.
- **해칭·먹칠** — 콘크리트·토양·조경. 다만 **축척이 표현을 정한다**:
  1:500 의 슬래브는 0.5mm 라 무늬가 안 들어간다 → 먹칠(poche).
- **면적표** — 그린 사각형에서 **계산해** 찍는다. 손으로 적지 않는다.

★ **도면은 다크 모드에서도 흰 종이다.** 검은 바탕 도면은 실무에 없다.
★ **그리고 네 장을 눈으로 본다.** 도면은 오류를 안 내고 틀린다 (M-53).

### 6-1-2. 투자자에게 가기 전 「거르는 팩」의 규격 〈2026-08-25 사장님 지시 · M-54〉

사장님 지시: **「실제 투자자에게 사업을 보여주기 전 LinkPilot 이 자동으로 사업을
걸러내는 수준」**. 그래서 도면·모델은 최종 산출물이 아니라 **DD 와 CAPEX 검토의
원재료**다. 이 관점이 규격을 정한다.

- **슬라이드는 16:9 고정.** PPT 에 그대로 들어가야 하므로 `aspect-ratio:16/9` 로
  못박고 SVG viewBox 는 `0 0 1920 1080` 을 쓴다. 도면(A2)과는 다른 규격이다.
- **자동·반자동·사람을 색으로 갈라 적는다.** 초록=자동 · 주황=반자동 · 회색=사람.
  ★ **회색 칸이 곧 실사 항목이다** — 비어 있음을 숨기지 않는 것이 이 팩의 값이다.
- **자동화 등급은 「지금 배선된 커넥터」 기준으로 적는다.** 계획을 적으면 그 순간
  이 팩은 거짓이 된다. 없는 것은 「사람」이다 (§4.8 — 가정계수가 들어가면 사람).
- **DD 는 여섯 갈래다** — 법률 · 토지 · 인허가·행정 · 기술 · 공사비내역 산출 ·
  수요시장 트렌드. 자본 흐름은 투자자 → SPC → 토지/인허가/자산 → 사업개발 →
  CAPEX → Financing.
- **표지 포스터는 글자 뒤에 스크림을 깐다.** 없으면 수치가 건물 위에 얹혀 안 읽힌다.
  장식이 아니라 가독성 장치다 (§6-3 ④).
- **포스터 이미지는 모델에서 뽑는다** — 렌더 함수를 창구로 내보내고 헤드리스로
  1920×1080 을 찍어 심는다. 모델 코드를 두 벌로 만들지 않는다.

### 6-2. 브리핑 HTML

- 브리핑 HTML은 모바일 우선(카카오톡 인앱 브라우저 기준)으로 작성한다.
- 폰트 Arial/맑은 고딕, 포인트 컬러 #C00000.
- 모든 항목은 액션 가능하게: 일정→지도 링크, 인맥→tel: 딥링크, 프로젝트→문서 링크.
- 브리핑은 내부용이므로 이모지 사용 가능. 단, 대외 발송 문서 생성 기능을
  이 저장소에 추가할 경우 PDI 하우스 스타일(이모지 금지, 개인 성명 미표기,
  "Strictly Private and Confidential" 헤더)을 적용한다.

### 6-2-1. 대외로 나가는 문서는 **HTML 주소 + PDF 둘 다** 낸다 〈2026-08-31 사장님 지시〉

사장님 지시 그대로다 — 「**외부신청은 항상 HTML 파일을 만들어 url 연결 열어줘 그리고 pdf 파일로 만들어줘**」.

밖으로 나가는 문서(투자제안·IM·티저·제출서류)는 **둘 다** 낸다. 하나만 내지 않는다.

- **왜 둘 다인가** — 받는 사람의 자리가 둘이라서다. 주소는 **지금 바로 열어 보는 길**이고,
  PDF 는 **메일에 붙이고 인쇄해서 회의실에 들고 들어가는 길**이다. 주소만 주면 메일에 못 붙이고,
  PDF 만 주면 고칠 때마다 새 파일이 돌아다녀 **어느 것이 최신인지 알 수 없게 된다.**
- ★ **인쇄용 파일을 따로 만들지 않는다.** 같은 HTML 하나에 `@media print` 와 `@page` 를 넣어
  거기서 PDF 를 뽑는다. 두 벌로 만들면 §6-1-2 와 같은 결로 **한쪽이 옛말을 한다** —
  화면은 고쳤는데 PDF 는 안 고친 상태가 눈에 안 보인다.
- ★★★ **`npm run im:pdf` 로 뽑는다 — 손으로 조립하지 않는다** 〈2026-08-31 사장님 지시:
  「npm run im:pdf 명령으로 만들어줘」〉. 앞 판은 규칙만 적어 두었는데, 그러면 그 규칙이
  **사람의 기억에 얹힌다** — 이 저장소가 그 자리에서 이미 여섯 번 빠뜨렸다 (M-31).
  `npm run im:pdf -- <입력.html>` 하나로 글꼴 갖추기·뽑기·검사까지 돈다.
  라이브러리를 들이지 않는다 — 헤드리스 크로미움만 쓴다 (§6-1 과 같은 이유).
- ★★ **웹폰트가 안 잡히면 PDF 가 화면과 다른 글꼴로 나온다.** 이 컨테이너에서는 크로미움이
  Google Fonts 에 못 닿는다(프록시가 크로미움에 안 걸린다) — 화면은 멀쩡한데 **PDF 만 조용히
  대체 글꼴로 나온다.** `im:pdf` 가 뽑기 전에 글꼴을 내려받아 `~/.fonts` 에 깔고 `fc-cache -f`
  를 돌린다. 오래된 User-Agent 로 CSS 를 받으면 쪼개진 woff2 대신 **TTF 한 벌**이 온다.
  - ★★★ **「깔렸는가」와 「그 이름으로 쓰이는가」는 다른 사실이다** 〈2026-08-31 · 실측〉.
    `fc-list` 를 글자로 훑어 「IBM Plex Mono」가 있으면 있다고 봤는데, 굵기 500 짜리 TTF 는
    자기 이름을 **「IBM Plex Mono Medium」**으로 등록한다. 글자로는 맞는데 브라우저가
    `font-family:"IBM Plex Mono"` 로 물으면 fontconfig 가 못 찾아 **DejaVu Sans Mono 로
    바꿔치기했다.** 검사는 초록인데 PDF 의 숫자만 다른 글꼴이었고, **그 상태로 두 번 내보냈다.**
  - ★ 그래서 **`fc-match` 로 묻는다** — 브라우저와 같은 방식이다. 돌아온 이름이 물어본 이름과
    다르면 없는 것이다. `fc-list` 는 「깔려 있는가」를, `fc-match` 는 「그 이름으로 쓰이는가」를 잰다.
- ★★★ **뽑고 나서 셋을 확인한다** (`im:pdf` 가 자동으로 잰다): 쪽수가 0 이 아닌지,
  A4(210×297mm)가 맞는지, **PDF 안에 그 글꼴이 실제로 박혔는지**. 셋째가 이 도구의 급소다 —
  앞의 둘은 통과하면서 글꼴만 틀린 PDF 가 실제로 나왔다. **표와 카드는 쪽 경계에서 쪼개지지 않게**
  `break-inside:avoid` 로 묶는다 — 표 한 줄만 다음 장에 넘어간 문서는 대충 만든 것으로 읽힌다.
- ★★ **PDF 가 화면보다 옛말을 하지 않는지 `guard` 가 잰다** 〈2026-08-31 · 열한째 칸 신설〉.
  주소와 PDF 는 **만드는 시점이 다르다** — HTML 을 고쳐 다시 발행하면 주소는 새 판이 되는데
  `im:pdf` 를 다시 안 돌리면 **PDF 만 옛 판으로 남고, 그 상태가 아무 오류도 안 낸다.**
  M-25(화면 지문 어긋남)와 같은 결이라 같은 방식으로 막는다 — `tools/pdf-fresh.js` 가
  짝을 이룬 HTML·PDF 의 나이를 재고, PDF 가 뒤처지면 `guard` 가 빨개진다.
- ★ **밖으로 보낼 때는 수령자 번호를 박는다** — `npm run im:pdf -- <입력.html> --to "○○캐피탈"`.
  기밀 문서는 **어디로 새는지가 문제**인데, 똑같은 파일을 열 곳에 보내면 한 부가 돌아다녀도
  어디서 나온 것인지 알 방법이 없다. 받는 곳마다 다른 번호를 박아 두면 그때 갈린다.
  - 번호는 **수령자·파일·날짜를 해시로 접은 것**이다. 순번(001·002)을 쓰지 않는다 —
    받는 쪽이 「나는 몇 번째인가」를 알게 되고, **그것 자체가 정보**다.
  - **원본 HTML 은 안 고친다.** 임시 사본에만 찍는다 — 원본을 고치면 아티팩트에
    남의 이름이 박힌 채로 올라간다.
  - 박을 자리(기밀 머리)가 없으면 **빨갛게 끝난다.** 도장 없는 PDF 가 나가면
    「번호를 박았다」고 믿은 채로 추적 불가능한 사본이 돈다.
- 답변에는 **주소를 먼저, 파일을 그다음에** 적는다 (§8 — 기본 전달은 주소로 여는 페이지).

### 6-2-2. 투자조건은 **RCPS 가 기본값**이다 〈2026-08-31 사장님 지시〉

사장님 지시 그대로다 — 「**IM 투자조건(형태·밸류에이션·회수)은 특별한 지시 없으면 RCPS 규정해줘**」.

- **형태**는 **상환전환우선주(RCPS)** 로 쓴다. 보통주로 바꿀 권리(전환권)와 원금 상환을 요구할
  권리(상환권)를 함께 갖는 우선주다 — **상방은 보통주처럼, 하방은 채권처럼** 갖는다.
- ★ **왜 기본값을 두는가.** 앞 판은 조건이 비어 있으면 「협의 사항」으로 두고 넘어갔다.
  그러면 IM 이 「좋은 회사네요」에서 멈추고 **다음 미팅으로 안 넘어간다** — 투자자는
  조건이 없는 제안을 검토 대상으로 올리지 못한다. 매번 물어보지 않아도 되게 기본값을 둔다.
- **적는 것은 셋이다.** 형태(RCPS 표준 조항) · 밸류에이션(투자 전 기업가치별 지분율) ·
  회수(IPO · M&A · 상환권 · 동반매도).
- ★★ **숫자를 정해진 값으로 쓰지 않는다.** 우선배당·상환이자·전환비율은 협상으로 정해진다.
  통상 범위를 적고 **「협의」 배지**를 붙인다 — 정해진 것처럼 적으면 §4.8(가정이 섞이면
  자동으로 내지 않는다)을 어기는 것이고, 첫 미팅에서 바로 깨진다.
- ★ **밸류에이션은 제안하지 않고 계산해 보인다.** 투자금을 고정하고 투자 전 기업가치별
  지분율을 표로 낸다 — 산술이라 틀릴 자리가 없고, 협상의 출발점이 눈에 보인다.
- ★★★ **상환권에는 반드시 단서를 함께 적는다.** 상법상 상환 재원은 **배당가능이익**으로
  한정된다. 회사가 결손이면 상환을 요구해도 못 받는다. 「하방이 막혀 있다」는 말은
  **회사가 이익을 내고 있을 때만 참이다** — 이것을 안 적으면 문서가 거짓말을 한다.
- 사장님이 다른 형태(보통주·CB·대출)를 지시하시면 그것을 따른다. 이 절은 **지시가 없을 때**의 값이다.

### 6-2-3. **산출물을 만들기 전에 출력 방식을 옵션으로 묻는다** 〈2026-08-31 사장님 지시〉

사장님 지시 그대로다 — 「**출력방식 조건을 내게 옵션으로 묻기**」. 물을 것은 다섯이다.

1. **가로 · 세로**
2. **PDF · PPT · 워드 · 한글**
3. **미리보기**
4. **다운로드 · 카카오톡 전달**
5. **조감도 포함**

- ★ **왜 묻는가.** 앞 판은 내가 정했다. 그런데 **틀렸을 때 드는 값이 크다** — 12쪽짜리
  세로 문서를 다 만든 뒤에 「PPT 로 가로였어야 한다」가 나오면 처음부터 다시 짜야 한다.
  **묻는 데 드는 것은 한 번의 물음이고, 안 묻고 틀리면 한 벌을 통째로 버린다.**
- ★★ **이것은 `core/outputspec.js` 의 앞문이다.** 거기 이미 같은 뜻이 적혀 있다 —
  「AI 가 페이지 수·크기·방향·파일형식·언어·파일명을 임의로 정하지 않는다 ·
  사양이 LOCK 되기 전에는 최종 산출물을 만들지 않는다」. 그 규칙이 **엔진 안에만**
  있어서 **대화에서 내가 바로 만들 때는 안 걸렸다.** 이 절이 그 자리를 메운다.
  기본값은 `PRESETS` 를 그대로 쓴다 — 새로 만들지 않는다.
- ★★★ **초안(DRAFT)까지는 물어보기 전에 만들어도 된다.** 묻느라 아무것도 없는 채로
  기다리게 하지 않는다 (§「Delivering work」와 같은 결). 다만 **최종본은 답을 받고 만든다.**
- **답이 없으면 기본값으로 가되 무엇을 골랐는지 적는다.** 사장님이 물음을 지나치실 수 있다 —
  그때 멈춰 있으면 일이 안 나간다. 「세로·PDF·미리보기·조감도 없음으로 만들었습니다」처럼
  **고른 값을 답변에 적어** 다르면 한 마디로 고칠 수 있게 한다.
- ★ **조감도는 근거가 없으면 켜도 안 나온다** (§7 · `outputspec.js` 의 `VISUAL_DEFAULT`).
  「포함」을 고르셔도 지적 필지 형상이 없으면 건너뛰고 사유를 남긴다 — **켰다고 그림이
  나오는 것이 아니다.** 그 사실을 물을 때 함께 적는다.
- ★★ **한글(HWP)·워드·PPT 는 이 저장소가 아직 못 만든다** 〈2026-08-31 실측〉.
  지금 나가는 것은 **HTML 주소와 PDF** 뿐이다 (§6-2-1). 고르시면 **못 만든다고 말하고**
  가장 가까운 것(PDF)을 낸다 — 조용히 PDF 로 바꿔치기하지 않는다.

### 6-2-4. **한 번 이어 붙인 바깥 연동은 로그아웃으로 끊지 않는다** 〈2026-09-07 사장님 지시〉

사장님 지시 그대로다 — 「**일정연동이 한번 등록되면 로그아웃하더라도 지속유지되어야함.
끊어지지 않도록 법적규정화 해 만들어줘**」.

- **끊는 것은 사람이 [해제]를 누를 때뿐이다.** 로그아웃 · 계정 전환 · 캐시 비우기 ·
  기기 교체는 **기기 사본만** 지운다. 서버에 보관된 이어짐(refresh token)은 건드리지 않는다.
- ★ **왜 이 규칙이 필요한가.** 앱의 `handleLogout` 이 `gcalSignOut()` 을 부르고 있었고,
  그것은 **서버 보관분까지 revoke** 했다. 그래서 로그아웃 한 번에 연동이 통째로
  사라지고, 다시 들어오면 **구글 동의부터 다시** 받아야 했다. 사장님이
  「자동등록이 잘 안 된다」고 하신 자리가 그것이다.
- ★★ **그때 적어 둔 이유는 옳았다 — 답이 틀렸을 뿐이다.** 주석은 「토큰이 계정이 아닌
  **기기**에 저장됨 → 다음 계정으로 새는 것 방지」였다. 새는 걱정은 진짜다. 다만
  **서버는 이미 계정별로 보관한다** (`gcal-oauth.php` 의 `md5(email)` · 세션 토큰으로
  신원 확인). 그러므로 **기기 사본만 지우면 새지 않는다.**
  ★ 「샐 수 있다」의 답은 **끊는 것**이 아니라 **계정에 매는 것**이다.
- ★★★ **로그인하면 서버에 「내 계정이 연동됐나」를 물어본다.** 기기에 남은 표만 보고
  판단하지 않는다 — 그 표는 로그아웃·새 기기·캐시 비우기로 사라지는데, **연동은
  사라지지 않았기 때문**이다. 물어서 있으면 스스로 이어 붙인다. 사람이 누를 것이 없다.
  ★ **못 물어본 것은 「연동 안 됨」이 아니다.** 조용히 두고 다음에 다시 묻는다.
    없다고 단정해 「연결하세요」를 띄우면 사람이 **이미 한 일을 또 하게 된다.**
- **이 규칙은 일정 연동에만 해당하지 않는다.** 바깥 서비스와 한 번 이어 붙인 것
  (캘린더 · 메일 · 저장소 · 결제)은 전부 같다. **이어짐은 계정의 것이지 기기의 것이 아니다.**
- 재는 자리: `linkpilot-platform` 의 `test-gcal-persist.js` (교차검증 42번째 칸).
  로그아웃이 끊지 않는지 · 서버에 묻는지 · 서버가 계정별로 보관하는지 셋을 함께 본다.

### 6-2-5. **지인에게 보내는 인사말은 그날을 넷으로 읽는다** 〈2026-09-07 사장님 지시 · S-40〉

사장님 지시 그대로다 — 「**카톡인사말 3줄 생성 섹션만들어줘 · 요일별, 공휴일 생일등
그날그날 상황에 맞게 지인들에게 인사말 전달 · 명언카드와 함께 전달할 센스 문장**」.

- **넷을 읽는다** — 요일(S-24) · 계절(S-37) · 공휴일 · **생일**(S-40). 셋만 읽던 자리에
  생일이 없었다. 사람에게 가는 인사에서 그날 그분의 생일을 모르는 것이 가장 크게 어긋난다.
- **세 줄은 역할이 다르다** — ① 오늘이 어떤 날인지 ② 오늘에 맞는 한마디 ③ 맺음.
  ★ **역할이 같은 자리끼리만 갈아 끼운다.** 공휴일은 ①, 계절은 ②, 생일은 **①과 ③**이다.
  ③까지 바꾸는 이유는 하나다 — 맺음이 「좋은 하루 되시길」로 남으면 **축하가 아니라
  평범한 문안**이 된다. 오류가 안 나고 **받는 사람만 서운한** 종류다.
- ★★ **겹치면 사람이 이긴다.** 공휴일·추모일과 같은 날이어도 생일이 첫 줄을 가진다 —
  이 글은 나라의 날이 아니라 **그분 한 분에게** 가는 인사다.
- ★★★ **이름을 못 뽑으면 그 인사를 안 쓴다.** 「오늘은 님의 생일입니다」가 그대로
  나가는 쪽이 **안 보내는 것보다 훨씬 나쁘다** (§4.6 「지어내지 않는다」와 같은 결).
- ★ **없는 입력칸을 만들지 않는다.** 생일은 **달력의 오늘 일정**에서 읽는다 — 연락처에
  생일 칸을 새로 만들어 1,145건을 손으로 채우시게 하지 않는다.
- ★★ **조건부로 그려지는 칸은 그려서 잰다** (§6-3 ⑥과 같은 결). 소스만 보면 늘
  「있다」로 읽힌다 — `test-kakao-greeting.js` 가 달력에 생일을 넣고 **실제로 그려**
  딱지·첫 줄·[다른 문장으로] 를 센다.
- ★★★ **인사말은 「누구에게」까지 있어야 일이 끝난다** 〈2026-09-07 사장님 지시 · S-41:
  「생일을 달력에 넣고 오늘의 인사이트에서 카카오톡친구들 노출되어 그날그날 알고 인사말보내기」〉.
  글을 잘 지어 놓고 **보낼 사람이 화면에 없으면** 사장님이 다른 앱을 열어 이름을 찾으신다 —
  그 순간 이 기능은 안 쓰인다. 그래서 **그날 보낼 사람이 같은 화면에** 있어야 하고,
  **오늘이 그 사람의 날이면 맨 위로** 올라와야 한다.
  ★ **선언만 되고 안 그려지는 자리를 조심한다.** 이 자리의 친구 명단이 실제로 그랬다 —
    상세 화면은 살아 있는데 **여는 길이 사라져** 아무도 못 열었고, 코드가 멀쩡해서
    소스만 보면 「있다」로 읽혔다. 그래서 **그려서 센다** (§6-3 ⑥과 같은 결).
  ★★ **안 되는 길을 되는 척 그리지 않는다.** 카카오톡은 친구에게 바로 보내는 공개 창구가
    없다 — 복사하고 공유 화면을 여는 것이 실제로 되는 전부다. 그 사실을 **화면에 적는다**.
    적지 않으면 눌러도 안 가는 것이 **고장으로 읽힌다** (§8 — 없는 것을 그리지 않는다).
- ★★ **검사를 약하게 고치는 것과 「재려던 것을 재게」 고치는 것은 다르다** 〈같은 날 · 실측〉.
  「복사를 부르는 자리가 **정확히 하나**」를 세던 칸이 [인사말 보내기]가 생기자 빨개졌다.
  둘 다 안전한 한 벌을 쓰는 것이 옳으므로, **개수**가 아니라 **우회가 없는지**로 바꿨다.
  ★ 가르는 잣대: **재려던 성질이 그대로인가.** 성질이 남으면 고쳐도 되고, 성질이
    빠지면 그것이 약하게 고치는 것이다 (§8 「검사를 약하게 고치지 않는다」).
- ★★★ **인사말은 「누구에게 보내는가」부터 고른다** 〈2026-09-09 사장님 지시:
  「너무 상투적인 멘트 · 추천 문자도 여러가지 타입으로 · 친구 지인 사업파트너 회사동료
  선배외 관계성에 따라 문구 추천」〉.
  - **왜 정했나.** 앞 판은 문장이 **한 벌**뿐이었고 그 한 벌이 전부 격식체 문안이었다
    (「문안 인사 올립니다」·「기원합니다」). 그래서 **친구에게도 거래처에도 같은 말**이
    나갔다. 상투적으로 읽힌 이유는 문장 솜씨가 아니라 **받는 사람을 안 보고 있었기** 때문이다.
  - **여섯으로 가른다** — 친구 · 지인 · 회사동료 · 선배 · 사업파트너 · 어른·윗분.
    어투는 셋이다: 반말 · 해요체 · 합쇼체. ~~기본값은 **지인**이다(가장 두루 쓰인다).~~
    〈2026-09-13 사장님 지시로 **가족**으로 바뀜 — 아래 ★★★ 「시작화면이 무엇을 골라 두는가」〉
  - ★ **관계는 사람이 고른다. 연락처 태그로 추정하지 않는다.** 태그(CEO·투자대상)는
    분류이지 사이가 아니다. 틀리면 **그 사람에게 실례가 그대로 나간다** (§4.9 와 같은 결 —
    자동으로 하나를 고르지 않는다).
  - ★★ **계절·공휴일·생일이 갈아 끼우는 줄도 함께 갈라야 한다.** 안 그러면 친구에게
    보내는 글에서 **그 한 줄만 격식체로 튄다** — 오류가 안 나고 읽는 사람만 어색해한다.
  - ★★★ **딱지가 여섯인데 글이 같으면 그 딱지는 거짓이다.** 그래서 「표가 있는가」가
    아니라 **「눌러서 실제로 다른 글이 나오는가」**를 잰다 (§6-3 ⑥ 과 같은 결 — 그려서 잰다).
  - ★ **고른 사이를 기억한다.** 매번 다시 고르시게 하면 그 칸은 안 쓰인다.
  - ★★ 이때 **기존 검사가 「존댓말로 맺는가」를 재고 있었다.** 친구에게는 반말이
    무례가 아니므로, 재려던 성질은 「존댓말인가」가 아니라 **「받는 사이에 맞는 말투인가」**다.
    그래서 **관계별로 기대 어투를 재도록** 넓혔다 — 약하게 고친 것이 아니다
    (위 「검사를 약하게 고치는 것과 …」 와 같은 잣대).
- ★★★ **「오늘이 무슨 날인지」는 사람이 고를 수 있어야 한다** 〈2026-09-12 사장님 지시 —
  경쟁 화면(동료 축하 메시지 생성기)을 보여 주시며 「분석하여 오늘의 인사이트에 반영해줘」〉.
  - **왜 정했나.** 앞 판은 **달력이 말해 주는 것만** 읽었다 — 요일·계절·공휴일·생일. 그래서
    「김부장 오늘 승진했는데 한마디 보내야겠다」를 **할 방법이 아예 없었다.** 달력에 없는 일은
    이 기능에 존재하지 않는 일이었다.
  - **일곱으로 가른다** — 평소 · 생일 · 승진 · 성취 · 작별 · 근속 · 출산. 기본값은 **평소**이고,
    평소는 앞 판과 **글자 하나 안 달라진다** (기본값이 조용해야 나머지가 뜻을 가진다).
  - ★ **경조사는 첫 줄(①)과 맺음(③)을 갈아 끼운다. 둘째 줄(계절)은 그대로 둔다** — 생일과 같은 결이다.
    그리고 **받는 사이의 말투를 그대로 따라간다.** 안 그러면 축하 한 줄만 격식체로 튄다.
  - ★★ **고른 경조사는 기억하지 않는다.** 사이(관계)는 잘 안 바뀌어 기억하지만, 경조사는 그날
    한 번 쓰고 끝난다. 기억해 두면 **다음 날 엉뚱하게 「승진 축하」가 떠 있다.**
  - ★★★ **이름이 없으면 「이름을 부르는 앞머리」만 떼어 낸다.** 문장 한가운데 이름이 얽힌 변형은
    떼면 말이 안 되므로 **그 변형을 안 쓴다.** 「님의 승진 소식을」이 이름만 빠진 채 나가는 쪽이
    안 보내는 것보다 훨씬 나쁘다 (S-40 「이름을 못 뽑으면 그 인사를 안 쓴다」와 같은 결).
  - ★ **달력이 생일을 잡은 날에는 달력이 이긴다** — 이름이 든 인사가 이름 없는 인사보다 낫다.
  - ★★ **저쪽 화면에서 «안» 가져온 것 셋을 적어 둔다** — 자유 입력칸 · 길이 선택 · 크레딧.
    저쪽은 **AI 가 문장을 짓고** 이쪽은 **표에서 고른다.** 없는 것을 있는 척 그리지 않는다 (§8).
    세 줄 고정은 S-40 이 정한 규격이라 그대로 둔다. 나중에 문장 생성을 붙이게 되면 그때 다시 본다.
  - ★★★ **화면에 보이는 글과 «나가는» 글이 갈리지 않는지 함께 잰다** 〈같은 날 · 실측〉.
    [인사말 보내기]가 **고른 사이를 통째로 무시하고** 있었다 — 미리보기는 관계를 따라 바뀌는데
    나가는 글은 늘 기본값(지인·해요체)이었다. **딱지가 여섯인데 나가는 글이 하나면 그 딱지는 거짓이다.**
    딱지를 더할 때는 **미리보기뿐 아니라 보내는 자리까지** 같은 값을 쓰는지 본다.
  - ★★ **새 칸은 접힘 안에 둔다** (§6-3 ⑥). 기본 화면에 할 일은 하나여야 한다 — 딱지 여섯 위에
    여섯을 더 펴 놓으면 「내가 뭘 골라야 하나」가 된다. 대신 **접힘을 여는 자리에 무엇인지 적고**,
    그것이 실제로 그려지는지 **그려서 센다** (선언만 되고 아무도 못 여는 자리가 실제로 있었다).
- ★★★ **고르는 줄에는 «머리글»을 붙이고, 같은 것을 두 자리에 두지 않는다**
  〈2026-09-12 사장님 지시: 「생일, 축결혼식, 상가조문탭은 (오늘은 특별한 날인가요?)탭 안으로
  이동배치하여 넣어줘」 · 「1.대상: (동생/후배) 머리글 표기해줘 · 2.특별한날: · 3.특정일」〉.
  - **왜 정했나.** 딱지 줄이 둘·셋 나란히 있으면 **무엇을 고르는 줄인지 안 보인다.**
    사장님이 「내가 뭘 골라야 하나」를 물으시는 자리가 그것이다. 그래서 **번호가 붙은
    머리글**을 준다 — 「1. 대상 / 2. 특별한 날 / 3. 특정일」.
  - ★ **같은 것이 두 자리에 있으면 한쪽만 고쳐진다** (§6-3 ⑥과 같은 결). 명언 테마 목록의
    「생일·축결혼식·상가조문」은 경조사 줄의 「생일」과 **같은 화면에 두 번** 나오고 있었다.
    셋을 접힘 안 「3. 특정일」로 옮기고 **테마 목록에서는 뺐다.** 값은 그대로 살아 있다 —
    목록에서만 빠지는 것이지 기능이 빠지는 것이 아니다.
  - ★★ **글자가 겹치면 낱말을 갈라 적는다.** 옮긴 딱지는 「생일 **예문**」으로 적는다 —
    고르는 것이 인사말이 아니라 **명언 카드의 예문**이라는 것도 그 한 낱말이 말해 준다.
  - ★★★ **관계에 「동생·후배」를 더할 때 친구 표를 그대로 쓰지 않는다.** 어투는 같은 반말이지만
    **딱지가 일곱인데 글이 같으면 그 딱지는 거짓이다.** 친구에게는 나란한 말, 동생·후배에게는
    **챙겨 주는** 말이 들어간다(「무리하지 마」·「막히면 물어봐」).
    ★ 판은 **넷**을 채운다 — 주마다 판이 하나 넘어가므로 넷이 아니면 한 달 안에 같은 말이 돌아온다.
  - ★★ **개수를 검사에 손으로 적지 않는다.** 「관계 딱지 **여섯**」·「갈래 소제목 **다섯**」으로
    박아 두면, 사장님 지시로 늘린 날 **고침이 옳은데도 빨개지고** 그때 「검사를 약하게 고칠까」가
    된다. **표에서 세어 견준다** — 재려던 성질(「표에 있는데 화면에 안 그려진다」)은 그대로다.
- ★★★ **배경 테마는 사장님이 «직접» 더하고 지운다** 〈2026-09-12 사장님 지시: 「이미지 테마를
  자동으로 추가[등록], [삭제] 만들수 있도록 해줘」 · 「종교이미지에 불교, 힌두교, 카톨릭,
  이슬람등 추가배치 구분」 · 「AI 이미지 생성 옆에 내이미지 등록 [업로드] 필드 만들어줘」 ·
  「큰분류에 기타 (작가작품), (사물) 포함」〉.
  - **왜 정했나.** 표가 코드 안에 박혀 있어 테마 하나를 더하려면 **내가 고쳐 배포해야** 했다.
    그리고 종교 갈래 다섯이 **전부 기독교**여서, 다른 종교를 가진 분께 보낼 그림이 **하나도
    없었다** — 오류가 안 나고 **그분만 서운한** 종류다 (S-40 과 같은 결).
  - ★ **기본 테마는 못 지운다.** 지우면 되돌릴 길이 사장님 손에 없다. [삭제]는 **직접 더한 줄에만**
    붙는다.
  - ★★ **종교는 한 갈래에 섞지 않고 «갈래를 나눈다»** — 섞으면 「사찰 옆에 십자가」가 되어
    고르는 손이 멈춘다. 종파에 안 매이는 것(기도·묵상·순례길)은 **종교 공통**으로 따로 둔다.
  - ★ **[내 이미지]는 AI 칸 바로 옆에 둔다.** 그림이 아닌 파일·너무 큰 사진은 **이유를 말하고**
    받지 않는다 — 조용히 안 되는 자리를 만들지 않는다.
- ★★ **마지막 인사와 이름은 사람이 넣는다** 〈2026-09-12 사장님 지시: 「오늘의 생각 한줄의
  하단 밑에 마지막인사 및 이름 넣는 필드 만들어줘」〉. 앞 판은 둘 다 기계가 정해 **고칠 길이
  없었다.** 비워 두면 앞 판 그대로 돌고, 한 번 치면 기억한다 — 매번 다시 치시게 하면 그 칸은
  안 쓰인다. ★ **칸을 더할 때는 「미리보기뿐 아니라 나가는 자리까지」 같은 값을 쓰는지 잰다**
  (S-41 과 같은 결 — 딱지가 있는데 나가는 글이 하나면 그 딱지는 거짓이다).
- ★★★ **달력이 말해 주는 날은 인사말도 읽는다 — 다만 «못 믿을 것은 안 쓴다»**
  〈2026-09-12 사장님 지시: 「카톡인사말에 달력과 각종 특정일보 를 반영해줘 인사말 추천에」
  + 캘린더 화면 사진(공휴일·법정기념일·음력·세시풍속·24절기)〉.
  - **왜 정했나.** 앞 판은 공휴일과 법정기념일만 읽었다. 그래서 「추분」·「한로」 같은 날이
    인사말에 **한 줄도 안 들어갔고**, 사장님 달력에는 또렷이 찍혀 있었다.
  - ★ **24절기는 표로 박지 않고 «계산»한다.** 절기는 태양의 겉보기 황경이 15도의 배수가
    되는 순간이라 해마다 날짜가 움직인다 — 손으로 적은 표는 그 해가 지나면 **조용히 틀린다**
    (음력 명절 표가 세 해만 덮고 있던 것과 같은 결).
  - ★★★ **계산이 하루 밀릴 수 있다. 그때는 «안 쓴다»** 〈실측〉. 저정밀 태양 위치식은
    15분쯤 오차가 있다. 사장님 달력으로 검산했더니 추분(9/23)·한로(10/8)는 맞았는데
    **백로만 하루 어긋났다** — 계산은 9월 7일 **23시 41분**, 달력은 9월 8일. 절기 순간이
    자정 바로 앞이라 오차가 날짜를 넘긴 것이다.
    ★ 그래서 **자정에서 한 시간 안에 드는 절기는 안 쓴다.** 2026년은 스물넷 중 **스물**을 쓴다.
      **틀린 날을 자신 있게 말하는 쪽이 안 말하는 것보다 훨씬 나쁘다** (§4.6 과 같은 결).
    ★★ 그리고 **몇 개를 못 쓰는지 답변과 검사에 적는다** — 「다 된다」로 뭉뚱그리지 않는다.
  - ★★ **차례가 뜻을 정한다** — 음력 명절 → 양력 기념일 → 절기. 공휴일이 있는 날에 절기를
    말하면 「오늘은 추분입니다」가 **추석을 덮는다.**
  - ★★★ **아직 안 읽는 것을 적어 둔다** — 잡절(초복·중복·말복·한식 등 세시풍속) · **대체공휴일** ·
    2029년 이후 음력 명절. 셋 다 특일정보 API(`getSundryDayInfo`·`getRestDeInfo`)가 주는 것이고,
    그 API 는 **해외 IP(GitHub Actions)에서 안 열린다**(§4). 국내 자리에서 수집이 돌면 그때
    갈아 끼운다. **없는 것을 있는 척 적지 않는다.**
- ★★★ **고를 것이 많아지면 «한 겹 더 접는다»** 〈2026-09-12 사장님 지시:
  「큰분류 [+내이미지] / [자연], [일상], [취미], [종교], [경조사]」 · 인사이트 지침서 v1.2 의
  `LP-INS-005`(테마가 한꺼번에 많이 노출됨)〉.
  - **왜 정했나.** 종파를 가르고 「기타」를 더하니 갈래가 **열둘**이 되고 폰에서 **스무 줄**이 됐다.
    **전부 보이는 것이 곧 「고를 수 있다」는 아니다.**
  - ★ **큰분류(자연·일상·취미·종교·경조사·기타) 한 줄**을 격자 위에 두고, 고른 분류의 낱장만
    그린다. 기본값은 **전체**이고, 전체는 앞 판과 **글자 하나 안 달라진다** —
    기본값이 조용해야 고르는 것이 뜻을 가진다.
  - ★★ **세부 갈래 → 큰분류 한 방향만 적는다.** 낱장 쉰둘에 큰분류를 따로 달면 두 벌이 되어
    한쪽이 옛말을 한다 (§8-1 과 같은 결). 모르는 갈래(직접 더한 테마)는 **「기타」로 접힌다.**
  - ★★★ **「칩이 있는가」가 아니라 «누르면 격자가 실제로 줄어드는가»를 잰다** — 실측 53 → 19칸.
    눌러도 안 줄면 그 칩은 거짓이다. 그리고 **[전체] 로 돌아오는 길**이 있어야 한다.
  - ★ **[+ 내 이미지] 는 큰분류 줄 맨 앞**이다 (사장님이 적으신 자리). 격자 안에도 두면
    같은 것이 두 자리가 된다 — 격자에서 뺐다.
- ★★★ **경조사를 더할 때는 «어투 셋 × 네 판»을 함께 쓴다** 〈2026-09-12 사장님 지시:
  「(오늘은 특별한 날인가요?) 항목 추가해줘 — (출산), (입합), (졸업), (취업)」〉.
  - **입학·졸업·취업**을 더했다(출산은 이미 있었다). 하나에 **24문장**(어투 3 × 첫줄 4 + 맺음 4)이
    들어간다 — 그보다 적게 쓰면 딱지만 늘고 글은 안 갈린다.
  - ★ **「입합」은 「입학」으로 적는다** — 지침서 v1.2 §4.7 이 「오기로 보고 사용자 화면과
    데이터값에서는 `입학`으로 통일한다」고 이미 정해 두었다. 지시를 고친 것이 아니라
    **지침서가 정한 표기를 따른 것**이다.
  - ★★ **이름 없이도 쓸 수 있는 변형을 둘 이상 둔다.** 네 변형이 모두 `{이름}` 을 물고 있으면
    이름을 못 뽑은 날 그 경조사가 **통째로 안 나간다** (S-40 과 같은 결).
  - ★ 지침서가 1차 추천으로 적은 **합격·결혼**은 아직 안 넣었다 — 사장님이 넷만 말씀하셨다.
    **없는 것을 있는 척 적지 않는다.**
- ★★★ **고른 값은 «화면에 써 넣은 글자»로 판단하지 않는다 — 한 곳에 둔다**
  〈2026-09-12 사장님 지시: 「1. 대상 (교인) +추가 · 2. 특별한 날 (합격) +추가 · (평소) -삭제 ·
  [지금공유. 카톡.SNS] → [SNS 공유] · 카톡 인사말 → SNS 인사말 · 이미지선택하면 테두리가 굵은
  테마색 라인 및 체크표시 · 오늘의 명언 [편집] 스크롤 발생안되게 전체 노출」〉.
  - **왜 정했나.** 「평소」 딱지를 지우면서 **돌아올 길**로 「고른 딱지를 다시 누르면 풀린다」를
    두었다. 그런데 「지금 뭘 골랐나」를 **딱지에 붙은 글자**(`data-on`)로 판단하고 있어서,
    화면이 한 번 다시 그려지면 그 글자가 0 으로 되돌아가 **두 번 눌러도 안 풀렸다.**
  - ★★ 더 나쁜 것은 그 사이 상태다 — **딱지는 꺼진 것처럼 보이는데 나가는 글은 축하 인사
    그대로**다. 오류가 한 줄도 안 나고 화면과 글만 갈린다 (S-41 과 같은 결).
  - ★ **그래서 고른 값은 한 곳에 두고 화면이 그것을 읽는다.** 다만 **기억하지는 않는다** —
    경조사는 그날 한 번 쓰고 끝이다(위 ★★). 기억용으로 **선언만 돼 있고 안 쓰는 열쇠**도 지운다:
    두면 누가 배선하는 순간 그 규칙이 깨진다 (§6-2-6 「꺼 두기는 안 쓴다가 아니다」와 같은 결).
  - ★★★ **이 고장은 검사가 아니라 «화면을 찍다가» 나왔다.** 그려서 재는 칸이 있어도 그 칸이
    **다시 그려진 뒤**를 안 지나가면 못 잡는다. 되돌릴 길을 지울 때는 **새 길을 눌러 본다.**
  - ★ **딱지를 더할 때는 문장도 함께 쓴다** — 「교인」은 지인 표를 베끼지 않고 84줄을 새로 지었다.
    가르는 것은 낱말이 아니라 **한 주의 모양**이다: 이 사이에서 일요일은 한 주의 끝이 아니라
    **주일**, 곧 한 주를 여는 날이다. ★★ 종파에 매이는 말(교리·예식)은 안 넣는다 — 교파가 갈린다.
  - ★ **이름을 바꿀 때는 「옛 이름이 안 남았는가」까지 잰다.** 새 이름만 더하고 옛 이름을 안 지우면
    같은 것이 두 자리가 된다 (§6-3 ⑥과 같은 결).
  - ★★★ **「가족」을 더할 때 다른 해요체 표를 베끼지 않는다** 〈2026-09-12 사장님 지시:
    「가족대상도 추가해줘」〉. 어투는 지인·교인·회사동료와 같은 **해요체**인데 — 합쇼체로 두면
    집에 보내는 글이 거래처 문안처럼 읽히고, 반말로 두면 웃어른께 못 쓴다 — **글은 전혀 다르다.**
    가르는 것은 어투가 아니라 **무엇을 걱정하는가**다: 이 사이의 글은 일이 아니라
    **끼니·건강·날씨·전화**를 말한다. 바깥 사이에 쓰면 지나친 참견이 되는 말들이다.
    ★ 그래서 맺음도 다르다 — 「좋은 하루 되세요」가 아니라 **「저녁에 전화 한 번 드릴게요」**다.
    ★★ **어투가 같다고 표를 돌려 쓰면 딱지만 늘고 글은 안 갈린다.** 재는 자리가 그것을 잡는다 —
      가족 표를 빼고 딱지만 남겨 봤더니 **두 칸이 빨개졌다**(「아홉이 모두 다른 글」·「판 넷」).
  - 경위: `linkpilot-platform` 의 MEMORY.md **S-47**.
- ★★★ **배경 테마의 큰분류는 「테마·기념일 지침 v1.0」 §4 를 따른다 — 다만 셋은 «앞 지시가 이긴다»**
  〈2026-09-12 사장님 지시: 「교차검증후 카테고리 재구성 반영해줘」 + 지침서 v1.0〉.
  - **열 칸이다** — 전체 · 자연 · 계절·날씨 · 명절·기념일 · 일상·감정 · 축하·경조사 · 종교·마음 ·
    업무·성공 · 취미·여행 · 기타. 낱장은 51 → **113**, 세부 갈래는 12 → **16**.
  - ★★★ **지침과 이 저장소가 부딪히는 자리 셋 — 앞 지시가 이긴다.** 지침이 새것이라고
    앞 지시를 덮으면, 사장님이 따로 적어 주신 것이 **조용히 사라진다.**
    · 「직접 추가」를 **맨 뒤**에 두라 → **안 옮긴다.** 사장님이 「[+ 내 이미지] 는 큰분류 줄
      **맨 앞**」이라고 자리를 적으셨다 (위 ★).
    · 「기타」가 지침 목록에 **없다** → **남긴다.** 사장님이 「큰분류에 기타 (작가작품), (사물)
      포함」이라고 따로 적으셨다.
    · 종교를 **셋**으로 묶으라 → **일곱으로 둔다.** 사장님이 종파를 나누라 하셨고,
      섞으면 「사찰 옆에 십자가」가 된다 (위 ★★).
  - ★★ **「오늘 추천」은 «재료가 생긴 뒤에» 넣었다** 〈2026-09-13 · 앞 판의 「안 넣었다」를 고침〉.
    - 앞 판은 「읽는 배선이 없어 늘 빈 칸이 된다」고 적고 안 만들었다. 그 판단은 그때 옳았다.
      그런데 §6-2-5 의 **절기·음력 명절 계산**이 들어오면서 **오늘이 무슨 날인지 읽는 재료가
      생겼다** — 그러자 안 만들 이유가 사라졌다.
    - ★★★ **다만 「늘 빈 칸」을 막는 장치는 그대로 둔다.** 오늘에 걸리는 낱장이 **둘 이상일
      때만** 칩을 그린다(`_todayHit().length>=2`). 하나짜리 칩은 눌러 봐야 한 칸이라
      **누를 값이 없고**, 0 이면 §8 의 「없는 것을 그리지 않는다」를 어긴다.
    - ★★★ **그 칩을 «먼저 계산»해 두면 조용히 안 나온다** 〈실측 · 이 저장소에서 실제로 났다〉.
      큰분류 목록을 `const _IMG_BIGS=(()=>{…_IMG_TAGS…})()` 로 **낱장 표보다 앞에서** 만들었더니
      TDZ(아직 값이 안 든 상수를 읽는 것)에 걸렸는데, 그 자리가 `try{}catch(_){}` 안이라
      **오류가 한 줄도 안 나고 빈 배열**이 됐다. 칩은 **영원히 안 나오는데 화면은 멀쩡했다.**
      ★ 그래서 **부를 때 계산한다**(`_imgBigs()`). 표를 읽는 값은 **미리 굳히지 않는다.**
      ★★ 잡힌 경위도 적어 둔다 — 검사가 「큰분류 표를 못 읽었다 — 이 칸은 아무것도 안 잰다」로
        **못 잰 것을 통과로 안 적었기** 때문이다 (§8 과 같은 결). 초록으로 끝냈으면 못 잡았다.
  - ★★★ **지침의 목록을 낱장으로 그대로 옮기지 않는다** — 이것이 §6-2-7 의 「테마」 낱말 겹침이
    실제로 나타난 자리다. 지침 §5 의 목록은 **행사 목록**(`CONTENT:테마`)이고 코드의 `_IMG_TAGS` 는
    **배경 사진 검색어**(`IMAGE:테마`)다. 「선거일」·「대체공휴일」은 **그 뜻의 사진이 없다** —
    이미지 갈래에 넣으면 빈 칸이 되고, 행사 자료로 가야 한다(2단계).
  - ★ **무거운 낱장을 더하면 검색어 갈래도 함께 넓힌다.** 「병문안」·「현충일」이 그냥 들어오면
    조문 자리에 「일출·황금빛」이 나온다 — `solemn` 판정에 낱말을 더한다 (§6-2-6 과 같은 결).
  - ★★★ **처음 여는 화면은 「전체」가 아니다** 〈2026-09-12 사장님 지시: 「기본 큰분류를
    「전체」가 아닌 것으로」 — 내가 잰 값을 드리고 여쭤 받은 답이다〉.
    - **왜.** 낱장이 51 → 113 이 되면서 「전체」가 폰에서 **44줄**이 됐다(앞 판 21줄).
      큰분류로 접는 장치가 있어도 **처음 여시는 화면이 44줄**이면 그 손이 가기 전에 이미 길다 —
      지침 §1 이 풀려던 문제(테마가 한꺼번에 많이 노출됨)를 오히려 키운다.
    - **무엇으로 여는가는 지침이 정해 두었다** — §4 「추천할 특별한 날이 없으면 계절·날씨와
      일상 중심」, §3.3 「오늘과 가까운 행사를 앞쪽에」. 그래서 **오늘이 기념일·명절이면
      「명절·기념일」, 아니면 「계절·날씨」**로 연다. 실측 21칸.
      ★ 「오늘 추천」 칩을 안 만들고도 **그 뜻의 절반을 지금 재료로** 지킨다.
    - ★★ **고르신 값이 언제나 이긴다.** 기억된 것이 있으면 그것으로 연다 — 안 그러면
      사장님이 고르신 것이 매번 덮인다. 「전체」를 고르셨으면 「전체」로 열린다.
    - ★★★ **[전체] 로 가는 길은 그대로 둔다.** 기본값을 바꾸는 것과 길을 없애는 것은
      다른 일이다 — 검사가 **둘 다** 잰다(「전체가 아닌가」·「전체 칩이 있는가」).
    - ★ **개수를 세는 칸은 [전체] 로 데려가서 센다.** 기본값이 좁아지면 「표에 있는 것이
      다 그려지는가」를 재던 칸들이 빨개지는데, 그것은 **재는 자리가 옮겨진 것**이지
      고장이 아니다. 재려던 성질은 그대로 두고 자리만 옮긴다 (§6-2-5 와 같은 잣대).
  - ★ **칩은 줄바꿈으로 둔다.** 지침 §4 는 폰에서 「가로 스크롤」을 적지만, 가로 스크롤은
    **뒤에 있는 칩이 안 보인다** — 열 칸 중 넷이 숨는다. 다르게 한 것을 여기 적어 둔다.
  - ★★ **이름도 개수도 검사에 손으로 안 적는다** — 표에서 세어 견준다. 앞 판이 「큰분류 일곱」을
    손으로 박아 두어, 지시대로 늘린 날 **고침이 옳은데도 빨개졌다** (위 ★★와 같은 잣대).
- ★★★ **시작화면이 «무엇을 골라 두는가»는 사장님이 정하신다** 〈2026-09-13 사장님 지시:
  「오늘의 인사이트 시작화면 — 1.오늘의 한줄생각 → 오늘의 추천(선택) · 2.SNS인사말 →
  가족(선택) · 3.보낼카드 미리보기 → 후보(선택), 오늘의 추천(선택)」〉.
  - **지금 값 셋** — 명언 테마 = **오늘 요일의 첫 추천**(`lpDayRec()`) · 인사말 대상 = **가족** ·
    배경 큰분류 = **오늘 추천**. 앞 판은 각각 「자동」·「지인」·「오늘 추천」이었다.
  - ★ **고치기 전에 «그려서» 잰다.** 셋 중 **하나(큰분류)는 이미 맞았다** — 소스만 보고
    고쳤으면 멀쩡한 것을 건드릴 뻔했다. 「후보」도 토글이 아니라 늘 진한 단추라 고칠 것이 없었다.
  - ★★ **기본값은 «기억이 없을 때»만 쓰인다.** 고르신 값이 언제나 이긴다 — 안 그러면
    사장님이 고르신 것이 **매번 조용히 되돌아간다**.
  - ★★★ **그 「이긴다」를 잴 때 «내가 심은 값을 되읽지 않는다»** 〈같은 날 · 되돌림 시험이
    거짓 통과했다〉. `localStorage` 를 읽으면 **앱이 그 값을 통째로 무시해도 통과한다**.
    **화면에서 «검게 칠해진 칸»을 읽는다** — 사장님이 보시는 것이 그것이다.
  - ★ **기본값을 바꿀 때 그 값이 표에 있는지 확인하고, 없으면 조용히 옛 기본값으로 돌아간다.**
    없는 테마를 골라 두면 그 화면이 **빈 채로 열린다**.
  - 재는 자리: `linkpilot-platform` 의 `test-insight-defaults.js` (교차검증 84번째 칸).
- 재는 자리: `linkpilot-platform` 의 `test-kakao-greeting.js` (교차검증 33번째 칸).

### 6-2-6. 남의 사진을 쓰는 규칙 — 일곱 〈2026-09-12 사장님 지침〉

사장님 지침 그대로다 — 「**Pexels·Unsplash 공식 API만 사용 · 검색화면에는 사진가와 출처 표시 ·
사용자에게 원본사진 다운로드 기능을 제공하지 않음 · 문구·레이아웃·디자인이 결합된 완성 카드만
저장·공유 · 인물·로고·상표·예술작품 사진은 기본적으로 제외 · 조문·질병·정치·종교 카드에는
자연·꽃·추상 이미지 우선 사용 · 모든 이미지의 출처와 라이선스를 Asset Registry에 보관**」.

**재는 자리는 `linkpilot-platform` 의 `test-image-license.js` 다** (교차검증 78번째 칸).
이 일곱은 **화면에 안 보이는 규칙**이라 어기고 있어도 카드는 예쁘게 나온다 — 문제는 밖으로
나간 뒤에 오고, 그때는 되돌릴 수가 없다. 그래서 사람의 기억에 얹지 않고 검사로 못박는다.

교차검증에서 **넷이 안 지켜지고 있었다.** 그중 하나는 **내가 그날 만든 것**이었다.

- ★★★ **「안 적은 것」과 「받자마자 버린 것」은 다르다.** 사진가를 화면에 못 적은 이유는
  깜빡한 것이 아니라 **응답에서 주소 하나만 집고 나머지를 통째로 버렸기** 때문이다.
  버리는 코드는 **아무 오류도 안 낸다.** 그러니 커넥터를 붙일 때 **무엇을 버리는지**를 본다.
- ★★★ **「꺼 두기」는 「안 쓴다」가 아니다.** Pixabay 가 `pb&&false` 로 꺼져 있었는데
  키를 읽는 코드와 부르는 코드가 남아 있었다 — 누가 `&&false` 를 지우면 돈다.
  원칙이 「안 쓴다」면 **코드를 지운다.**
- ★★★ **확인 안 한 것을 「아니다」로 적지 않는다.** 사람·상표·작품이 찍혔는지는 기계가
  판정할 수 없다. `false` 로 적으면 **확인한 것이 되어** 기록이 오히려 위험해진다 —
  `null` 로 두고 「안 쟀다」로 남긴다 (§4.7 「모른다고 적는다」와 같은 결).
- ★★ **예술작품은 「사진이 자유여도 그림이 자유가 아니다」.** Pexels·Unsplash 라이선스는
  **사진**에만 미치고 화면 속 작품·조각·상표의 권리는 따로 있다. 그래서 검색어에
  `painting`·`artwork`·`museum`·`statue`·`mandala` 같은 낱말을 **안 넣는다** — 검사가 센다.
- ★★ **무거운 카드는 검색어 갈래를 따로 둔다.** 조문이 「성경말씀」과 같은 검색어를 쓰면
  「일출·황금빛」이 나온다. `solemn` 갈래를 두고 **자연·꽃·추상 + `no person`** 만 부른다.
  오류가 안 나고 **받는 분만 상하는** 종류다.
- ★★ **Unsplash 는 「썼다」를 신고해야 한다.** 그 API 지침이 요구하고, `download_location`
  을 한 번 부르는 것이 그 신고다 — 안 하면 **키가 끊길 수 있다.** 그런데 앞 판은 그 주소를
  **받자마자 버리고** 있었다.
- ★ **Asset Registry 는 사장님이 주신 스키마 17칸을 그대로 쓴다.** 못 받은 것은 `null` 로
  남긴다. **지금은 기기에만 쌓인다** — 서버 보관은 별건이고, 그 사실을 코드에 적어 두었다.
- ★★★ **아직 안 지켜지는 하나 — 그리고 내가 «그 수를 틀리게 적었다»** 〈2026-09-12 개정〉.
  박아 둔 사진이 공식 API 를 안 거친다. 앞 판은 **「46개」**로 적었는데 **실제는 105장**이었다.
  - **왜 틀렸나.** 검사가 **통째 주소**(`images.unsplash.com/photo-…`)만 셌다. 그런데 이 저장소는
    **id 만 박아 두고 주소를 «조립»하는 풀**이 따로 있다(`uUrl(id)`). 그 풀 하나에 사진이
    **열여섯 장** 들어 있어도 조립하는 자리 **1 로 세졌다.**
  - ★ **그래서 「지금은 안 지킨다」를 숫자로 적는 방식 자체는 옳았지만, 그 숫자가 거짓이었다.**
    숫자를 적어 두는 규칙에는 **「그 숫자를 재는 법이 재려는 것을 다 덮는가」**가 딸려 온다.
  - **고친 뒤** — 검사가 **주소 + id 모양**을 함께 센다. 그리고 **선언만 되고 아무도 안 읽는
    사진 풀**을 따로 잡는다. 실제로 **50장이 그 상태였다** (`MORNING_IMAGES` 42 ·
    `SECTION_HERO_IMGS` 8) — **화면에 한 번도 안 나오면서** 지침 ①을 어기고 있었다.
    지우니 **105 → 55**, 화면은 한 픽셀도 안 바뀌었다.
  - ★★ **남은 55장은 전부 「폴백」이다** — 명언 카드 배경(`BRIGHT_MORNING`·`SCRIPTURE_MORNING`
    ·`SEASON_MORNING`)과 태양광·풍력 배너(`SOLAR_HERO_IMGS`·`WIND_HERO_IMGS`). 셋 다
    **공식 API 를 먼저 부르고 못 받을 때** 쓰는 길이라, 지우면 **화면이 바뀐다.** 별건이다.
  - ★★★ **CDN 주소로는 공식 API 를 되물을 수 없다.** `photo-<숫자>-<해시>` 는 Unsplash 의
    **내부 파일 식별자**이지 API 의 사진 id 가 아니다. 그래서 **「사진은 그대로 두고 출처만
    붙이는」 길이 없다** — 바꾸려면 검색어로 다시 받아야 하고, 그러면 **사진이 달라진다.**
    이 사실을 모르면 「id 가 있으니 API 로 메타만 받으면 되겠네」로 하루를 쓴다.

### 6-2-7. 남의 **글**을 쓰는 규칙 — 인용·시·SNS 〈2026-09-12 사장님 지침 · 인사이트 지침서 v1.5〉

§6-2-6 이 **사진**의 규칙이면 이 절은 **글**의 규칙이다. 사장님이 주신 v1.5 가
인생시·창작시·짧은 시·SNS 인기글을 새로 두면서 같은 종류의 위험이 글 쪽에도 생겼다.

★ **원문은 지침서 한 곳에 있다** — 여기 옮겨 적지 않는다 (§8-1 과 같은 결).
  이 저장소의 일과를 바꾸는 것만 적는다.

- **`VERIFIED` 는 「근거 둘」이다.** 한 API 결과만으로 저자·원문을 확정하지 않는다 —
  독립된 근거 둘이 일치하거나, 원저작물 한 건을 직접 확인해야 한다.
  ★ **발견용과 확정용을 가른다**: Wikiquote 는 **찾는 데** 쓰고, Wikisource·원저작물로
    **다시 확인한다.** 이 구분이 없으면 「위키에 있었다」가 곧 출처가 된다.
- **출처가 충돌하면 노출을 멈춘다** (`CONFLICT`). 둘 중 그럴듯한 쪽을 고르지 않는다 —
  §4.9 (자동으로 하나를 고르지 않는다)와 같은 결이다.
- ★★★ **시의 «전문»은 셋 중 하나가 확인될 때만 낸다** — 퍼블릭 도메인 · 명시적 공개
  라이선스 · 권리자 허가. 그전에는 **제목·작가·소개만** 두고 전문은 내부에만 둔다.
  ★ 이것은 §6-2-6 의 「사진은 자유여도 그림은 자유가 아니다」와 **같은 규칙**이다 —
    공개돼 있다는 것이 자유이용을 뜻하지 않는다.
- **창작시에는 「AI 가 지었다」를 적고, 유사도를 잰다.** 8어절 이상 연속 일치나 고유한
  비유·후렴·제목이 겹치면 **자동 노출을 막는다.** 생존 시인의 문체를 그대로 모방하지 않고,
  실제 시인의 이름을 저자로 붙이지 않는다.
- **SNS 는 «공식 API 만»** 쓴다. 스크래핑·우회수집·화면캡처·비공개글·삭제된 글은 쓰지 않고,
  **원문 복제 대신 요약 + 원문 링크**를 낸다. 좋아요·조회수를 사실이나 전문성으로 세지 않는다.
  ★ **원 게시물이 지워지면 우리 쪽에서도 즉시 내린다** — 캐시까지.
  ★★ 공식 API 가 인기글 검색을 안 주면 **도입을 보류한다.** 없는 길을 웹페이지 긁기로
    메우지 않는다 (§8 「없는 것을 그리지 않는다」).
- **열쇠는 Registry 에 넣지 않는다.** 출처 기록은 저장소에 남고 열쇠는 비밀 저장소에 남는다 (§2).

#### 낱말이 겹치는 자리 — **둘** 〈같은 날 · 교차검증에서 나왔다〉

지침서와 이 저장소가 **같은 낱말을 다른 뜻으로** 쓴다. 겹침 자체는 사고가 아니지만
어느 뜻인지 안 적히면 조용히 틀린 것을 만든다 (§11 의 `Gate 3` 과 같은 결).

- ★★★ **「테마」** — 지침 §5.3.2 의 **60개 기본 테마**는 «문장의 분류»다(성장·도전 …).
  코드의 `_IMG_TAGS` **51개**는 «배경 사진 검색어»다. 숫자가 비슷해서 더 위험하다 —
  「테마를 60개로 맞춰라」를 이미지 쪽에서 읽으면 **엉뚱한 것을 아홉 개 만든다.**
  **접두어로 부른다**: `CONTENT:테마` · `IMAGE:테마`.
- ★★★ **「다운로드」** — 지침 §7.4 는 **완성 카드 이미지를 저장**하라고 요구하고,
  §6-2-6 원칙 ③ 은 **원본 사진 다운로드를 주지 말라**고 금지한다. **둘 다 맞다** —
  가르는 것은 무엇을 내려받는가다. **접두어로 부른다**: `CARD:다운로드`(허용) ·
  `PHOTO:다운로드`(금지). 이것을 안 적으면 「다운로드 기능을 구현하라」가
  **원본 사진 내려받기 단추**로 배선될 수 있고, 그러면 라이선스가 그 자리에서 깨진다.

#### 지침이 말하는 자리와 «닿는» 자리가 다른 곳 — 하나

★★★ 지침 §4.6 은 달력·음력·절기·특일의 정본을 **한국천문연구원 연계 공공데이터**로
정한다. 옳다 — 다만 그 자리는 **GitHub Actions 가 아니다.** `apis.data.go.kr` 은
러너에서 안 열린다(§4 · D-206 실측: 스무 칸 전부 `fetch failed`). 그러니 이 갈래 수집은
**국내 자리(NAS)에서** 돈다. 지침이 틀린 것이 아니라 **도는 자리가 정해지는 것**이다.

#### 지금 값을 숫자로 적어 둔다 — 「안 지켜지는 것」을 지켜지는 척 두지 않는다

지침 §5.3.1 의 목표량은 **약 5,400개**이고 초기 운영 요건이 **검증 1,500개**다.
실측(2026-09-12)은 아래와 같다. **늘면 좋고, 줄면 검사가 빨개진다** (§6-2-6 의 46개와 같은 방식).

| 유형 | 목표 | 지금 |
|---|---:|---:|
| 검증된 실제 명언 (`VERIFIED_QUOTES`) | 1,200 | **48** |
| 성경말씀 (`VERIFIED_VERSES`) | — | **12** |
| 동서양 고전문장 (`VERIFIED_CLASSICS`) | 400 | **40** |
| 한국 속담·고사성어 (`VERIFIED_PROVERBS`) | 300 | **40** |
| 인생시 · 창작시 · 짧은 시 | 1,400 | **0** |
| SNS 인기글 요약 | 600 | **0** |

★ **「부족하다」가 아니라 「몇 개인지」를 적는다.** 앞 판처럼 「확장 예정」으로 두면
  반년 뒤에도 48개다.

★★★ **「고전·사자성어」 테마가 AI 에게 «짓게» 하고 있었다** 〈2026-09-12 사장님 지시:
  「교차검증후 충돌없이 지침대로 개선되도록 반영해줘」 — 이 지침 v1.5 를 다시 주심〉.
  - **무엇이 문제였나.** 그 테마의 프롬프트가 「논어·손자병법·채근담 등 동양 고전에서
    **선정하라**」였다. 그러면 **지어낸 문장에 고전의 이름이 붙는다** — 지침 §5.3.5 가
    금지한 바로 그것이고, 받는 분은 그것이 진짜 고전인 줄 아신다.
    오류가 한 줄도 안 나고, **나간 뒤에야** 문제가 된다.
  - **고친 것.** 출전이 분명한 것만 **표**로 적고(`VERIFIED_CLASSICS` 40 ·
    `VERIFIED_PROVERBS` 40), 그 테마는 그 표에서 낸다. 프롬프트는 「**새로 짓지 말고**
    주어진 문장과 출처를 한 글자도 바꾸지 말라 · 해설만 붙이라」로 바꿨다.
  - ★ **전부 퍼블릭 도메인이다** — 논어·도덕경·맹자·손자병법·명심보감·채근담,
    세네카·에픽테토스·마르쿠스 아우렐리우스. 저작권이 살아 있는 현대 인물의 글은
    한 줄도 안 넣는다. 「근거 둘」을 **문헌 출전**으로 지키는 것이다.
  - ★★ **속담에는 저자를 안 붙인다** — 구전이다. 「한국 속담」·「고사성어」로 적고
    출전은 「구전」 또는 출전명이다. 없는 사람을 지어 붙이면 그것이 곧 거짓 출처다.
  - ★★★ **딱지 이름을 바꾸면 「고르신 값」도 옮긴다.** 「고전·사자성어」를 지침 표기인
    「고전에서 얻은 생각」으로 바꿨는데, 그 값이 기억돼 있으면 목록에 없어 **조용히
    기본값으로 돌아간다** — 사장님이 고르신 것이 아무 말 없이 사라지는 종류다.
  - ★ **표가 있는 것과 그 표에서 나오는 것은 다른 사실이다.** 검사는 둘 다 잰다 —
    「표가 40구 이상인가」·「그 테마가 그 표에 배선됐는가」·「프롬프트에 옛 지시가
    안 남았는가」. 되돌려 보니 셋 다 빨개진다.

### 6-3. 산출물은 **엣지 있게** — 다만 장식으로가 아니다 〈2026-08-22 사용자 지시〉

「좀 더 엣지 있게」는 **더 꾸미라는 뜻이 아니다.** 이 저장소의 산출물이 밋밋한
이유는 색이 적어서가 아니라 **똑같이 생겨서**다. 엣지는 아래 넷에서 나온다.

**① 첫 화면이 주장을 한다.** 제목 + 요약 문단으로 시작하지 않는다. 그 자리에
**가장 중요한 판정 하나**를 크게 놓는다 — 「됩니다/안 됩니다」, 「366ms → 2358ms」.
읽는 사람이 스크롤하기 전에 답을 갖게 한다.

**② 숫자를 문장에 숨기지 않는다.** 잰 값은 표·큰 글씨·대비로 **보이게** 한다.
「빨라졌습니다」는 엣지가 없고 「366 → 2358」은 있다.

**③ 활자로 승부한다.** 크기 단계를 분명히 두고(제목과 본문이 1.5배 이내로
붙지 않게), 제목은 자간을 좁히고, 숫자는 `tabular-nums` 로 세로를 맞춘다.
**색을 더 쓰는 것보다 활자를 정리하는 쪽이 언제나 더 크게 달라진다.**

**④ 대비는 한 곳에만.** 강조를 두 군데 이상 주면 둘 다 죽는다.

**⑤ 표 셀은 한 줄 — 엑셀의 자동 맞춤처럼** 〈2026-08-25 사용자 지시 · M-50〉.
셀 안에서 줄바꿈이 생기면 행 높이가 들쭉날쭉해 표가 안 읽힌다.
`td { white-space: nowrap }` 로 셀을 한 줄로 고정하고, 폭이 모자라면 셀을
접는 대신 **표를 가로 스크롤**시킨다 (감싸개 `overflow-x: auto`).
한 줄에 안 들어갈 긴 설명은 셀에 넣지 않는다 — 표 밖 문단·각주로 옮긴다.

**⑥ 한 화면에 **할 일 하나**만 둔다 — 나머지는 접거나 안 그린다**
〈2026-09-07 사장님 지시: 「복잡함 이것저것 설명 일정과 관련하여 요약심플하게 만들어줘
필드, 섹션, 탭 분주해 보임 되도록 1가지만 등록」〉.

- **왜 정했나.** 일정 연동 자리에 넷이 한꺼번에 펴져 있었다 — 방식 설명 상자 ·
  경고 상자 · 관리자용 입력 칸 둘 · 구독 주소 칸 **두 벌**. 그중 셋은 사장님이 하실
  일이 아니었는데, **화면에 있으면 「내가 뭘 넣어야 하나」로 읽힌다.**
  칸이 늘어난 것이 아니라 **할 일이 늘어난 것처럼 보인다.**
- ★ **기본 화면에 남기는 것은 단추 하나.** 그 화면에서 사람이 할 일이 하나면
  보이는 것도 하나여야 한다.
- ★★ **조건이 이미 채워진 칸은 안 그린다.** 서버에 값이 등록돼 있으면 그 입력 칸을
  **감추는 것이 아니라 안 그린다** — 감추면 접힘을 열었을 때 다시 나온다.
  대신 **채워지지 않았을 때는 반드시 길이 있어야 한다.** 둘 다 안 그리면 막다른 길이다.
- ★★★ **긴 안내는 누른 뒤에만 띄운다.** 누르기 전에 읽을 것을 쌓아 두면 사람은
  **누르기 전에 지친다.** 「이러면 이런 화면이 나옵니다」는 그 화면이 나온 뒤에 뜻이 있다.
- ★ **같은 것을 두 자리에 두지 않는다.** 구독 주소 칸이 두 벌이던 자리는 「어느 쪽에
  넣어야 하나」를 만든다. 한 벌만 두고 나머지 갈래는 **접힘 안**으로 넣는다.
- ★★ **그리고 그려서 잰다.** 조건부로 그려지는 칸은 소스만 보면 늘 「있다」로 읽힌다 —
  `test-cal-simple.js` 가 두 상태(등록됨·안 됨)를 각각 **실제로 그려** 보이는 입력 칸을 센다.

### 6-3-1. 디자인 시스템 값 — **팔레트가 둘이다** 〈2026-09-14 사장님 지시: 「교차검증후 반영해줘」〉

**원문은 [`docs/디자인시스템-값-CSS실측-2026-09-14.md`](docs/디자인시스템-값-CSS실측-2026-09-14.md)
한 곳에 있다.** 여기 옮겨 적지 않는다 (§8-1 과 같은 결).

★★★ **그 문서의 색 값을 앱에 그대로 넣지 않는다** 〈실측〉.

| | 그 문서 | 앱(`linkpilot-platform.deploy.html`) |
|---|---|---:|
| Ink | `#12161F` | **0회** — 앱은 `#0A1419` **191회** |
| Lime | `#B5E01F` | **0회** — 앱은 `#AAE106` **151회** |
| Critical | `#E5342B` 8건 | **0회** |

- 그 문서는 **시안 파일**(`.dc.html` 셋)을 잰 것이고, 앱은 **1단계 지시서 v1.1 의 확정
  팔레트**를 쓴다. **둘 다 「실측」인데 잰 대상이 다르다.**
- ★ 그러니 그 문서의 **후속 조치 8개를 그대로 치환하면 앱 색 346곳이 통째로 바뀐다.**
  그것은 사장님 승인이 필요한 일이다 (§10) — **어느 쪽이 정본인지 먼저 정한다.**
- ★★ **가져온 것은 «방법»이지 «값»이 아니다** — WCAG 대비를 재서 미달을 막는 규칙.
  그것은 팔레트와 무관하므로 앱 팔레트로 다시 계산했다.

★★★ **앱 팔레트로 다시 잰 결과** — 핵심 조합은 다 통과(잉크/흰 **18.63** · 잉크/라임 **11.96**),
  **미달인 «실제 짝»이 12종 · 16곳**이었다. 가장 나쁜 것은 **라임 바탕 + 흰 글자 = 1.56**
  (명함 영역 조정의 [확인] 단추) — 잉크로 고쳤다.

- ★ **재는 것은 「선언된 색」이 아니라 「실제로 짝지어진 앞·뒤 색」이다.** 배경에만 쓰이는
  빨강은 문제가 아니다 — 그 위에 흰 글자를 올릴 때 문제가 된다.
- ★★ **남은 미달을 숫자로 적는다** (§6-2-6 의 46개와 같은 방식). 「부족하다」로 두면
  반년 뒤에도 같다. 검사가 **기준선 16** 으로 잠그고, 늘면 빨개진다.
- ★★★ **못 잰 것을 통과로 적지 않는다** — `rgba` 로 적은 글자색 **30곳**은 뒤 색과
  합성해야 알 수 있어 이 검사가 **못 본다**. 그 사실을 검사가 직접 적는다.
- 재는 자리: `linkpilot-platform` 의 `test-contrast.js` (교차검증 94번째 칸).

#### 하지 않는 것 — 넷

- **라임(`--lime`)으로 큰 면을 채우지 않는다.** 디자인 시스템 §11 이다.
  강조에만 쓴다. 채우면 「완료」로 읽혀 뜻이 흐려진다.
- **의미 없는 이모지·아이콘을 넣지 않는다.** 뜻을 나르지 않는 그림은 잡음이다.
  대외 발송 문서는 §6-2 대로 **이모지 금지**가 그대로 유효하다.
  - ★★★ **화면(UI)의 이모지는 「안 넣는다」가 아니라 「라인 아이콘으로 갈아 끼운다」다**
    〈2026-09-05 사장님 확인: 「일러스트 이모지 대체 적격함 · 과거 기준 가이드라인 있음」〉.
    기준은 **저쪽 저장소에 이미 있다** — 여기 옮겨 적지 않는다(§8-1 과 같은 결).
    · **원칙**: `linkpilot-master/00-헌법.md` 16조 — 「UI 이모지 금지(라인 SVG)」.
    · **규격**: `linkpilot-master/31-1단계-로고아이콘-지시서-v1.1-확정.md` —
      획 두께 **1.8 단일**(`IC_W` 한 곳) · 라임 `#AAE106` · 잉크 `#0A1419` ·
      교체 22종 + 대상 밖 16종은 형태 유지·두께만 통일 · **중복 은유 해소**
      (`star`·`folder`·`sun` 이 각각 두세 기능을 겸하고 있다).
    · **재는 자리**: `verify-build.js` 가 배포마다 「UI 이모지 N개(M종) → <Icon/> 권장」으로
      세고, 아이콘 규격 위반은 따로 센다. **경고이지 차단이 아니다** — 그래서 안 고치면
      영원히 남는다.
    ★ **이것은 「1단계 납품 대기」다.** 디자이너 시안이 와야 22종이 바뀐다. 그 전에 이쪽이
      할 수 있는 것은 **새로 만드는 화면에 이모지를 안 넣는 것** 하나뿐이다 — 이미 있는 것을
      혼자 판단해 아이콘으로 바꾸면 시안과 두 벌이 된다 (지시서 「혼용 금지」).
- **없는 데이터를 그럴듯하게 그리지 않는다.** 예시면 예시라고 화면에 박는다 (§8).
- **애니메이션을 장식으로 넣지 않는다.** 진행 그래프처럼 **상태를 나르는**
  움직임만 둔다. `prefers-reduced-motion` 을 존중한다.

#### 어디에 적용되나

`npm run im:*` 로 나오는 미리보기·아티팩트, 배포하는 화면, 그리고 보고서
아티팩트. **엔진이 만드는 IM·티저 본문은 여기서 제외한다** — 그쪽은 PDI 하우스
스타일과 출력 사양(`core/outputspec.js`)이 정한다.

## 7. 세션 운영

- **SketchUp 갈래 작업은 [`docs/작업지침서-스케치업-엔진-운용.md`](docs/작업지침서-스케치업-엔진-운용.md) 를
  따른다** 〈2026-08-25 사용자 지시 — 「분석하고 작업시 반영」〉. 특히 셋:
  ① 내보내기 전 검사는 `npm run guard` 하나로 돈다(따로 조립하지 않는다),
  ② **새 이름·D/M 번호는 열려 있는 갈래와 이 운용서를 먼저 보고 딴다** —
  같은 날 두 번 깨진 자리다(D-102·D-104/D-105 재번호),
  ③ `im:smoke` 를 키 없는 자리에서 돌려 운영 열쇠를 판정하지 않는다 —
  실키 스모크는 NAS 처럼 키 있는 자리에서만 뜻이 있다.
- **실사(AI) 조감도 렌더의 표준은 Veras 4.0 (Nano Banana Pro 기반)이다 — 반드시**
  〈2026-08-25 사장님 지시〉. 값은 `core/outputspec.js` 의 `RENDER_STANDARD`
  한 곳에만 있다 — 계획 Agent 가 계획 파일에 실어 알리고, 수령 Agent 가
  비표준 도구·세대에 `RENDER_TOOL_NONSTANDARD` YELLOW 를 낸다 (규격 §3-1).
  무료 경로는 gemini + 같은 기반 모델만 표준으로 친다.
  ★ **표기가 온전한 렌더는 IM 본문에도 싣는다** 〈2026-08-25 사장님 지시 —
  D-34 2차 개정〉. 앞 판의 「표지·티저 한정」은 풀렸다. 대신 **순서와 표기로**
  막는다 — 근거(매스 표·기하 조감도)가 먼저·실사 렌더가 뒤, 그림 바로 밑에
  「AI 렌더 — 실제 설계안이 아님」+원본 장면+도구 세대를 인쇄한다. 기하
  조감도를 대체하지 않고 나란히 둔다.
- **문제가 생기면 [`MEMORY.md`](MEMORY.md) 를 먼저 찾는다.** 같은 종류를 이미 겪었을
  가능성이 높고, 경위·해법·막는 장치가 적혀 있다. "처음 보는 문제 같다"는 느낌은
  자주 틀린다 — 검색부터 한다. 새 사고가 나면 **그날 바로** 항목을 추가한다.
- **배포·전달 전에 교차검증을 통과한 것만 내보낸다** (MEMORY.md 「배포 전 교차검증」).
  하나라도 못 했으면 무엇을 못 했는지 **말하고** 내보낸다 — 조용히 넘어가지 않는다.
- 새 작업 시작 시 /clear 사용. 이 파일은 자동으로 다시 로드된다.
- 긴 작업 중간에는 /compact로 압축한다.
- 세션 종료 전 결정사항이 남으면 /handover를 실행해 HANDOVER.md를 생성하고,
  다음 세션 첫 프롬프트에서 "HANDOVER.md 읽고 이어서 진행"으로 시작한다.
- **새 규칙·지침·규정은 그 자리에서 이 파일에 적는다** 〈2026-08-23 사장님 지시:
  「앞으로 작업에 본건 지침을 포함 새로운 규칙 지침, 규정은 기억할 수 있도록」〉.
  - 앞 판은 「**두 번** 하게 되는 규칙은 적는다」였다. 그런데 두 번째가 오기
    전까지는 **대화에만 있고**, 세션이 끝나면 사라진다. 실제로 사장님이 같은
    지시를 **세 번** 하신 일이 있다 — 적혀 있었는데 적혔다는 것이 안 보였다.
  - 그래서 **한 번 들은 규칙도 그 턴에 적는다.** 어느 절에 넣을지 애매하면
    가장 가까운 절 끝에 붙이고, 〈날짜 · 사장님 지시〉와 **그렇게 정한 이유**를
    한 줄로 남긴다. 이유가 없으면 반년 뒤에 되돌린다.
  - **적었으면 답변에서 「어디에 적었는지」를 말한다.** 「기억하겠습니다」는
    확인이 아니다 — 사장님은 그것을 확인할 방법이 없다. 절 번호를 적는다.
  - 이미 적혀 있는 것을 다시 지시받으면 **「이미 있습니다」로 끝내지 않는다.**
    어디에 무엇이 적혀 있는지 **그 자리에 옮겨 보여 준다** — 못 찾으신 것이
    사장님 잘못이 아니라 내가 안 보여 준 탓이다.
- 같은 지시를 두 번 하게 되는 규칙은 이 파일에 추가한다 (# 단축키 사용).
- ★★★ **끝나지 않은 일은 «끝날 때까지» 계속 알린다** 〈2026-09-07 사장님 지시:
  「지속적인 리마인드 100% 해결될때까지」〉.
  - **왜 정했나.** 앞 판은 「바뀐 것이 없으면 조용히 있겠습니다」였다. 그런데 **막힌 일은
    바뀌지 않기 때문에 막힌 것**이고, 조용히 있으면 **그대로 잊힌다.** 실제로 그랬다 —
    12일치 옛 앱(M-79)이 그 상태였고, 배포는 그동안 계속 초록이었다.
  - ★ **「바뀐 것 없음」은 조용할 이유가 아니라 알릴 이유다.** 다만 **같은 말을 그대로
    되풀이하지 않는다** — 「몇 번째인지 · 그동안 무엇이 바뀌었는지 · 지금 막고 있는
    한 가지가 무엇인지」를 함께 적는다.
  - ★★ **끝난 것과 안 끝난 것을 갈라 적는다.** 목록이 줄어드는 것이 보여야 사람이 움직인다.
    「셋 중 둘 끝남 · 남은 하나는 ○○」처럼 **남은 개수를 앞에** 적는다.
  - ★★★ **100% 는 「내가 재서 초록인 것」이다.** 사장님이 「했다」고 하셔도 **재서 확인한
    뒤에** 목록에서 지운다 (M-80 — 초록을 근거로 「됐다」고 적지 않는다와 같은 결).
  - **어떻게 도나** — `send_later` 로 확인을 걸고, 끝날 때까지 **매번 다시 건다.**
    다 끝나면 그때 멈춘다. 세션이 끊기면 다음 세션 첫 답변에서 이 목록부터 확인한다.
  - ★★★ **확인 쪽지에 «사실»을 박지 않는다 — 박으면 그 사실이 굳어 되돌아온다**
    〈2026-09-15 사장님 지시: 「오류 발생 · 반복적으로 다시 되돌어옴 · 교차검증 개선해줘」 ·
    화면에 **이미 합쳐진 PR 을 「세 시간째 초안」이라 말하는 물음**이 떠 있었다〉.
    - **무엇이 났나.** 확인 쪽지에 「지금 막고 있는 것 = platform #113 합치기」를 **글로 적어**
      걸어 두었다. 그 뒤 사장님이 합쳐 주셨는데 **쪽지는 그대로 남아**, 한 시간마다
      **없는 일을 있다고 말하고 같은 것을 또 여쭈었다.** 오류가 한 줄도 안 나고
      **사장님만 같은 화면을 다시 보신다.**
    - ★ **쪽지에는 «잴 것»만 적는다** — 「#113 이 합쳐졌는지 본다」는 되고,
      「#113 이 아직 초안이다」는 안 된다. 앞은 **물음**이고 뒤는 **굳은 답**이다.
      굳은 답은 다음 시각에 **거짓이 되어 있을 수 있다.**
    - ★★ **깨어나면 «먼저 재고» 그다음에 말한다.** 쪽지에 적힌 상태를 그대로 옮기지 않는다.
      ★ 지문·번호·상태는 **그 자리에서 다시 읽는다** — 기억해 둔 값을 옮기면 그 사이에
        다시 만든 순간 옛말이 된다(같은 날 `bfb630a5` → `7375d608` 로 두 번 틀리게 적었다).
    - ★★★ **끝났으면 «지운다».** 다시 걸지 않는 것으로는 모자라다 — 이미 걸린 것이 남아
      한 번 더 온다. `delete_trigger` 로 지우고, 지웠다고 답변에 적는다.
    - ★★ **같은 물음은 두 번까지다.** 두 번 여쭈어 답이 없으면 **묻기를 멈추고**
      기본값으로 가되 무엇을 골랐는지 적는다 (§6-2-3 과 같은 결). 세 번째부터는
      **알림이 아니라 잔소리**가 된다.
    - ★ **남의 세션 쪽지는 내가 안 지운다** — 지우면 그 세션이 무엇을 기다리는지 모르게 된다.
      **찾으면 사장님께 알리고 여쭌다.** (실측: 「PR #15 상태 확인」이 **2주째** 돌고 있었다.)
  - ★★★ **「사람을 기다리는 일」에는 시계를 걸지 않는다** 〈2026-09-15 사장님: 「권하는 개선안
    진행해」 — 내가 올린 ②를 고르셨다〉.
    - **왜.** 2026-09-15 에 여섯 번 울렸고 **다섯 번이 「바뀐 것 없음」**이었다. 그리고
      「PR #15」 쪽지는 **2주 동안 한 번도 안 바뀐 것**을 한 시간마다 물었다.
      **바뀌는 것이 사람의 마음일 때, 시계는 아무것도 못 잰다** — 울리는 값만 쓴다.
    - ★ **가르는 잣대는 「기다리는 것이 기계인가 사람인가」다.**
      · **기계** (CI·배포·수집 잡) → 시계가 값을 한다. 스스로 끝나고, 끝나면 달라진다.
      · **사람** (합치기·승인·활용신청·설정) → **시계를 안 건다.** 대신 **깨어날 일이 왔을 때**
        (PR 이벤트·사장님 말씀) 그 자리에서 재서 알린다.
    - ★★ **「안 걸겠다」가 「안 챙기겠다」는 아니다.** 끝나지 않은 일은 §7 대로 목록에 남고,
      다음 답변 첫머리에서 **재서** 알린다. 없애는 것은 **시계**이지 **목록**이 아니다.
    - ★ 기계를 기다릴 때도 **끝나면 지운다** (위 ★★★).
- **답변 끝에 「권하는 개선안」을 항상 붙인다** 〈2026-08-25 사장님 지시:
  「너가 알고 있는 더 나은 개선안 (권장, 재확인) 솔루션을 **항상** 추천해줘」〉.
  - 앞 판은 사장님이 그때그때 물으셔야 나왔다. 그러면 **내가 아는 것과 사장님이
    아시는 것이 갈린다** — 나는 다음에 무엇이 가장 값싸고 크게 달라지는지 알면서
    묻기 전까지 말하지 않았다. 실제로 「스캔 진행을 파일 단위로」는 하루를 묵혔다.
  - ★ **일을 마치는 답변마다** 넷 안팎으로 적는다. 각 줄에 **왜 그것이 지금
    값이 큰지**를 한 줄로 붙인다 — 목록만 있으면 고르실 수가 없다.
  - ★ **이미 드린 권고 중 답을 못 받은 것을 함께 되짚는다.** 새것만 쌓으면
    앞엣것이 조용히 사라진다. 「재확인」이 그 뜻이다.
  - ★ **사람 손이 필요한 것**(키 등록·설정)은 §5 대로 **눌러야 할 자리까지**
    적는다. 권고만 적고 방법을 안 적으면 그 권고는 안 된다.
  - ★ **「권장」과 「재확인」을 갈라 적는다** 〈2026-08-25 · SketchUp 갈래에서 합침〉.
    권장은 **새로 하자는 것**, 재확인은 **이미 있어서 안 해도 되는 것**이다.
    갈라 적지 않으면 이미 있는 장치를 다시 만들게 된다.
- **반복·동일 패턴의 지시는 별도 지침이 없어도 규칙으로 일반화해 그 자리에서
  이 파일과 MEMORY.md 에 반영한다** 〈2026-08-25 사용자 지시 · SketchUp 갈래 M-38〉.
  「두 번 하게 되면」이 아니라 **한 번이라도 패턴이 보이면** 스스로 지침화하고,
  경위를 함께 남긴다. 색인 누락은 `memory-index.test.js` 가 잡는다.

## 8. 작업은 미리보기까지가 완성이다

- **작업이 끝나면 언제나 미리보기를 다시 만들어 전달한다.** 화면 작업이 아니어도
  마찬가지다. 코드만 올리고 "만들었습니다"로 끝내지 않는다 — 사용자가 확인할
  방법이 없으면 만든 것이 아니다.
- **3D 모델이 생기면 언제나 돌려볼 수 있는 HTML 뷰어로 보여준다**
  〈2026-08-25 사용자 지시 · M-39 — 규칙지침〉. 사장님은 **3D 가 어떻게
  보이는지**를 항상 궁금해하신다 — 모델을 만들거나 받은 턴에는 자체 렌더
  (외부 라이브러리 없음, 드래그 회전) 3D 미리보기 아티팩트를 **같은 주소**
  (`3D 모델 뷰어` 페이지, 같은 파일 경로 재발행)에 갱신해 주소를 드린다.
  SketchUp 썸네일·다운로드 주소는 임시 토큰이라 이것을 대체하지 못한다.
- **작업이 끝나면 결과가 어떻게 반영되었는지 「반영 샘플」을 만들어 보여준다**
  〈2026-08-25 사용자 지시 · M-38 — 규칙지침〉. 미리보기 주소만으로 끝내지
  않는다 — 실제 산출물의 **발췌**(바뀐 규칙 원문·생성된 JSON·파일 한 토막)를
  답변에 함께 실어, 열어 보지 않아도 무엇이 어떻게 반영됐는지 보이게 한다.
  샘플은 실제 결과에서 오려 온다 — 손으로 지어 쓰면 M-05(옛말 하는 화면)다.
- **끝냈다고 말하는 답변에는 반드시 미리보기 주소가 있어야 한다** 〈2026-08-20〉.
  「했습니다」 + 커밋 번호로 끝내지 않는다. 커밋은 사용자가 열어 볼 수 있는
  것이 아니다.
  - **이번에 건드린 것에 해당하는 주소를 전부 준다.** 하나만 주고 나머지를
    「바뀐 것 없음」으로 두지 않는다 — 사용자는 무엇이 최신인지 알 수 없다.
  - **주기 전에 다시 만든다.** 옛 판 주소를 주는 것이 안 주는 것보다 나쁘다.
  - 문서 작업도 마찬가지다. 문서는 그 자체를 아티팩트로 올려 **주소로** 준다.
  - **아티팩트 주소를 저장소에 적지 않는다** — 이 저장소는 public 이다 (D-10).
  - ★★★ **주소만이 아니다 — 「어떻게 들어가는가」는 통째로 안 적는다**
    〈2026-09-07 · 같은 날 **두 번** 검사에 걸렸다〉.
    - 방화벽 규칙(포트·허용 대역)·NAS 경로·터널 설정처럼 **망을 어떻게 여는가**는
      주소가 아니어도 **들어오는 길의 설계도**다. 공개 저장소에 적으면 그대로 공개다.
    - ★ 두 번 다 **안내 문서를 잘 쓰려다** 그랬다 — 사장님이 따라 하시기 쉽게
      값을 적었는데, `secrets.test.js` 가 잡았다. **검사가 옳았다.**
      `docs/안내-NAS-방화벽-…` 은 저장소에서 뺐고, 관리대장에서는 값만 지웠다.
    - ★★ 그러면 **어떻게 안내하나** — 절차는 저장소에 두고 **값은 답변에만** 적는다.
      문서에는 「값은 세션 답변으로 드렸다」라고 남겨, 다음 사람이 찾을 자리를 안다.
    - ★★★ **검사를 약하게 고치지 않는다.** 「이건 공개 대역이라 괜찮다」는 판단은
      한 번 열면 다음에도 열린다. 걸리면 **문서를 고친다.**
- **산출물을 「삭제」하라는 지시를 받으면 — 못 지우면 덮는다** 〈2026-08-25 사장님 지시 ·
  M-52〉. 발행한 아티팩트를 없애는 기능이 없다. 그대로 두면 옛 산출물이 살아 있는
  주소로 남아 **본건인 척 돌아다닌다**.
  - 내용을 **「본건 제외」 안내**로 바꾸고 **살아 있는 본건 주소**를 링크로 준다.
  - 안내 안에 **되살리는 법**(판 기록·작업 폴더 파일)을 적는다. 되돌릴 수 있다는
    사실이 안 보이면 못 되돌린다.
  - **중복 발행분까지 전부 덮는다.** 하나만 덮으면 나머지가 최신인 척한다.
  - 뷰어·목차처럼 **그 갈래를 가리키는 다른 화면도 함께 내린다.**
  - 답변에는 **「지웠다」가 아니라 「내렸다 · 이렇게 되살린다」**라고 적는다.
- **배포는 자동으로 돈다 — `main` 에 닿는 순간이다** 〈2026-08-23 사장님 지시:
  「모든 파일과 구조를 정립해줘 실시간 자동배포가 되도록」 · D-88〉.
  - `deploy-nas` 워크플로가 **`main` 푸시에 자동으로** 돈다. 손으로 누르는 길
    (`workflow_dispatch`)은 남아 있지만 **평소 길이 아니다.**
  - **눌러야 도는 장치는 안 돈다.** 앞 판은 「아무도 안 보는 사이에 덮지 않으려고」
    수동으로 뒀는데, 실제로 일어난 일은 반대였다 — 안 눌러서 NAS 가 조용히 옛
    판으로 남았다 (D-86). 같은 결이 `verify:nas` 에도 있었다 (M-11 · M-12).
  - **엔진(`im-agent/**`)도 함께 올린다.** 화면만 자동으로 올리면 「화면은 새 판,
    서버는 옛 라우팅 표」를 **더 빨리 만들 뿐**이다 (D-87).
  - 엔진은 잘못되면 **서비스가 멈춘다.** `deploy/engine.sh` 가 되돌릴 자리를
    먼저 만들고, 안 살아나면 **스스로 되돌린다.** 되돌렸어도 **빨갛게 끝난다** —
    되돌린 것은 서비스를 살린 것이지 배포가 된 것이 아니다.
  - **자동이 된 것이지 검사가 빠진 것이 아니다.** `guard`(교차검증 전체)가
    초록이 아니면 한 파일도 안 올라간다.
- **작업이 끝나면 「배포 확인」 한 장을 만들어 주소로 드린다** 〈2026-08-27 사장님 지시:
  「항상 작업이 끝나면 배포본에 반영이 잘되었는지 오류가 없는지 즉시 교차검증하고
  HTML 파일로 URL 로 열어서 보여줘」〉.
  - **말로 하지 않고 장치로 한다** — `npm run deploy:report` 가 만든다.
    같은 확인을 말로만 하면 **바쁜 날 빠진다** (M-31: 손으로 챙기다 하루 반에 여섯 번).
  - 한 장에 셋을 담는다: ① **배포가 NAS 안에서 잰 것**(단계별 통과)
    ② **내 자리에서 잰 것**(`guard` 열 칸) ③ **어디부터 사람 손인지**(D-143).
  - ★ **못 잰 것을 통과로 그리지 않는다.** 이 자리에서는 NAS 를 직접 못 본다 —
    근거가 무엇인지 그 장에 적는다.
  - ★★ 첫 줄은 **판정 하나**다 — 「반영됐다」 / 「반영되지 않았다」 (§6-3 ①).
    실패가 하나라도 있으면 초록으로 그리지 않는다.
  - 주소는 **같은 파일 경로로 다시 발행**해 유지한다.
- **배포가 닿았는지 직접 확인하고 알린다 — 「걸었다」로 끝내지 않는다**
  〈2026-08-23 사장님 지시: 「작업이 끝난 즉시 로딩하여 반영 되었는지 확인하고 알려줘」〉.
  - **워크플로를 건 것**과 **파일이 닿은 것**은 다른 사실이다. 줄을 서면 앞의
    것이 끝나야 시작하므로, 걸자마자 「반영했습니다」라고 말하면 **그 사이에
    사장님이 옛 화면을 보신다.** 하루에 세 번 그렇게 어긋났다.
  - 그래서 **`Verify deployed` 가 초록인 것을 보고** 말한다. 그때까지는
    「걸었습니다 · 아직 안 닿았습니다」라고 **있는 그대로** 적는다.
  - 초록을 본 뒤에는 **그 판의 지문을 함께 적는다.** 사장님이 화면 아래
    여덟 글자와 대 보시면 한 번에 갈린다 (M-27).
  - 도는 동안 답변을 미루지 않는다. **지금 무엇이 실려 있고 무엇이 오는
    중인지**를 나눠 적는다 — 「곧 됩니다」는 확인이 아니다.
- **끝냈다는 답변에는 그 판의 지문도 함께 적는다** 〈2026-08-22 사용자 지시 · M-25〉.
  화면 아래에 작게 `판 xxxxxxxx` 가 찍혀 있다(`build-stamp.js`). 답변에 같은
  값을 적어 두면 **사장님이 사진 한 장으로 「내가 보는 것이 그 판인가」를
  가릴 수 있다.** 이것이 없어서 네 번 헤맸다 — 나는 「고쳤습니다」라고 하고
  사장님은 옛 화면을 보고 계셨는데, 둘 다 그 사실을 몰랐다.
  - 지문은 `npm run im:stamp -- --check` 로 확인한다.
  - **「반영이 안 된다」는 셋이다** — 저장소≠디스크 / 디스크≠HTTP / 브라우저.
    갈라 보기 전에 **새로고침을 권하지 않는다** (MEMORY M-25).
- **기본 전달은 「주소로 여는 페이지」다.** `npm run im:artifact` 로 조각을 만들어
  아티팩트로 올리고 **주소를 준다.** 옆 창에서 확실히 열리는 것이 이것뿐임을
  네 번의 실패로 확인했다 (MEMORY.md M-01). 파일 전달은 내려받기 카드로만 뜨는
  환경이 있고, 그때 사용자는 **아무것도 못 본다.**
  - 직접 눌러 봐야 하면 `section-preview.html` 을 **함께** 보낸다. 대신 보내지 않는다.
  - 같은 파일 경로로 다시 올리면 **주소가 유지된다.** 새 주소를 만들지 않는다.
  - 답변 맨 끝이 아니라 **작업을 마치는 그 턴에** 준다 — 다음 턴으로 미루면
    사용자가 옛 화면을 본다.
  - ★★★ **그림 파일을 보내는 것은 「미리보기」가 아니다** 〈2026-09-12 사장님 지시:
    「작업이 끝나면 미리보기로 열어줘 확인할수 있도록 · 규칙 · 메모기억해줘」〉.
    - **왜 다시 정했나.** 이 규칙은 위에 이미 있었다. 그런데 나는 화면을 고칠 때마다
      **헤드리스로 찍은 PNG 를 파일로** 보냈다. 파일은 **내려받기 카드**로만 뜨는 자리가
      있어 사장님이 **아무것도 못 보신다** — 규칙이 있었는데 내가 안 지킨 것이다.
      「이미 적혀 있다」로 끝내지 않는다 (§7).
    - ★ **화면을 건드린 턴은 «주소»로 끝낸다.** 아티팩트로 올려 **누를 수 있는 링크**를 준다.
      그림은 그 페이지 **안에** 넣는다 — 답변에 파일 카드로 따로 붙이지 않는다.
    - ★★ **재 본 숫자를 그 페이지에 함께 적는다.** 사진만 있으면 「그렇게 보인다」이고,
      좌표·개수·바이트가 함께 있어야 **잰 것**이 된다 (§6-3 ②).
    - ★★ **여러 상태를 한 장에 담는다** — 기본 · 눌러 본 뒤 · 어긋났을 때.
      한 상태만 보여 주면 나머지는 「될 것이다」로 남는다.
    - ★ **같은 파일 경로로 다시 발행해 주소를 유지한다.** 새 주소를 만들면 사장님이
      **어느 것이 최신인지** 알 수 없게 된다.
    - ★ 여기서 못 재는 것(앱 틀 안의 모습·tailnet 안)은 **그 페이지에 못 쟀다고 적는다.**
- **★★★ 기준은 「앱에서 보이는 것」이다 — 낱개 화면이 아니다** 〈2026-08-30 사장님 지시:
  「지금부터 (NAS 뿌리 주소) 여기로 보여지는 **앱환경의 기준으로 대화가 되어야함** ·
  그것을 html url 로 링크하여 **항상 미리보기 창에 보여줘**」〉.
  - **뿌리 주소가 앱이다.** 사장님이 여시는 창은 거기 하나다 — 로그인 화면이 뜨고,
    그 안에서 보고서 화면이 틀에 얹혀 보인다. 실측으로 확인했다 (D-183).
  - ★ 그러므로 **「낱개 화면에서는 멀쩡합니다」로 끝내지 않는다.** 이 세션에서 실제로
    그렇게 어긋났다 — `report-flow.html` 을 직접 열면 배너가 한 벌도 없어 멀쩡한데,
    앱 안에서는 **두 벌**로 보였다 (D-179). **혼자 뜬 판이 맞다는 것은 판정이 아니다.**
  - ★★ 무엇을 고치든 **「앱에서 어떻게 보이나」를 함께 적는다.** 못 재는 자리면
    (여기서는 tailnet 을 못 본다) **못 쟀다고 적는다** — 「될 것이다」로 적지 않는다.
  - ★★★ **답변마다 앱 주소를 준다.** 낱개 화면 주소만 주면 사장님이 그것으로 판정하시게
    되고, 그러면 위의 어긋남이 또 안 보인다. 앱 주소가 **첫 번째**, 낱개는 그다음이다.
  - ★ 그리고 **미리보기 창에 여는 판**을 함께 낸다 (`npm run im:appview` →
    아티팩트로 발행). 그 판은 **앱 자리 지도**다 — 어느 주소가 무엇인지, 지금 실린
    판 지문이 무엇인지, 어디를 눌러야 하는지. 아티팩트는 tailnet 밖이라 **앱 화면
    자체를 그려 보일 수는 없다** — 그 사실을 그 판에 적어 둔다 (없는 것을 그리지 않는다).
  - ★★ **주소를 저장소에 적지 않는다** (D-10 — 이 저장소는 public). 주소는 답변에만 적는다.
- **부르면 그 자리에서 연다.** "보여줘" · "미리보기" · "옆 창에" 같은 말이 나오면
  그것만으로 충분한 지시다. **되묻지 않고** 같은 턴에 `npm run im:section` 으로
  다시 만들어 옆 창에 연다. 어느 화면인지 안 밝혔으면 **보고서 생성 섹션**
  (`section-preview.html`)이 기본이다.
  - 방금 만든 것이 있어도 **다시 만들어서 보낸다.** 재생성은 몇 초면 되고,
    옛 화면을 보여 주는 쪽이 훨씬 비싸다.
  - 판이 둘이다. **눌러 봐야 하면** `npm run im:section` → `section-preview.html`
    (접기·펼치기·입력이 동작한다). **그냥 보이기만 하면 되면**
    `npm run im:static` → `section-static.html` (빌드할 때 헤드리스로 미리 그려
    넣어 스크립트 없이 뜬다. 대신 눌리지 않는다).
  - **파일이 다운로드 카드로만 뜨는 환경이 있다.** 그때는 파일 전달을 반복하지
    말고 `npm run im:artifact` 로 만든 조각을 **주소로 여는 페이지(아티팩트)로
    올려 링크를 준다.** 지금까지 확인된 바로는 이것이 옆 창에서 확실히 열리는
    유일한 길이다. 대신 눌리지 않으므로, 직접 만져 봐야 하면 파일도 함께 준다.
  - 옆 창에서 안 열린다·빈 칸이 뜬다고 하면 **미리 그려 넣은 판을 보낸다.**
    안내를 반복하지 않는다.
- **한 일은 전부 미리보기 안에서 확인되어야 한다.**
  - 화면에 보이는 작업 → `changes.js` 에 등록하고 `shows` 에 **어디를 보면
    확인되는지** 적는다.
  - 화면이 없는 작업(파서·커넥터·검증) → 미리보기의 **[눈으로 확인]** 패널에
    실제 실행 결과를 넣는다. 정말 화면에 안 나오는 것이면 `shows` 에
    **"화면에 안 보인다"고 적는다** — 없는 것을 찾느라 헤매는 쪽이 더 나쁘다.
  - 확인 패널에 들어가는 결과는 **빌드할 때 실제로 돌려서** 넣는다. 손으로 적으면
    코드가 바뀐 날부터 화면만 옛말을 하고, 그때는 아무도 눈치채지 못한다.
- 미리보기는 **파일 하나로 열리는 자체 완결 HTML** 이어야 한다. 서버·빌드 도구가
  있어야 열리는 것은 미리보기가 아니다.
- 실제 데이터가 없으면 **예시임을 화면에 박아 둔다.** 데모 데이터를 실제로
  오해하면 그것을 근거로 판단한다.
- **표본이 거짓말을 하면 잡히는 것도 거짓이다** 〈2026-08-23 · 두 번 걸렸다〉.
  - 미리보기의 가짜 서버가 파일마다 지문을 똑같이 `'demo'` 로 주고 있었고,
    시험용 파일 둘은 **바이트가 똑같았다**(전부 0). 그 상태로 「같은 파일 접기」를
    넣었더니 **서로 다른 두 파일이 한 줄로 접혔다** — 시험은 빨개졌는데
    **접기는 옳게 돌고 있었다.** 고칠 곳은 코드가 아니라 표본이었다.
  - ★ 그러니 표본은 **재려는 성질을 지켜야 한다.** 진짜 값일 필요는 없지만
    「다른 파일은 다른 지문」 같은 성질은 지킨다.
  - ★ 빨간 줄을 보면 **표본부터 의심한다** — 코드를 고치기 전에.
- 전달 전에 **헤드리스 브라우저로 실제 렌더를 확인한다.** DOM 이 비었는데
  오류가 안 뜨는 경우가 있다 (닫는 script 태그, 세미콜론 누락 등).
- 미리보기 산출물이 소스와 갈리지 않도록 **재생성 결과가 커밋본과 같은지
  테스트로 고정한다.**
- **소스를 글자로 대조하는 검사는 주석을 떼고 본다** 〈2026-08-23 · 하루에 네 번 걸렸다〉.
  - `fetch-depth: 0` · `required()` · `verify-served.sh` 세는 곳 · `d.height + 8` —
    넷 다 **바로 위 주석에 적어 둔 경위**를 코드로 읽고 빨개졌다. 코드는 멀쩡했다.
  - ★ **경위를 잘 적어 둘수록 검사가 눈이 먼다.** 이 저장소는 주석이 길어서
    이 함정이 특히 자주 온다.
  - 그러니 `read(...)` 한 것을 그대로 `match` 하지 말고, **주석 줄을 걸러 낸 뒤**
    본다. 반대로 「주석에 경위가 남아 있는가」를 재는 검사는 그대로 둔다 —
    그때는 주석이 재려는 대상이다.
  - **주석 안의 글자가 코드를 깨뜨린다** 〈2026-08-23 · 하루에 두 종류〉.
    - **역따옴표(`` ` ``)를 주석에 쓰면 안 되는 자리가 있다.** `build-files.js` 의
      `uploadDriver()` 는 통째로 **템플릿 문자열**이다. 거기 주석에 역따옴표를
      넣었더니 문자열이 그 자리에서 끝나 **테스트 열여덟 개가 빨개졌다.**
      오류는 `Unexpected token 'new'` 한 줄뿐이라 주석이 원인이라는 생각이 안 든다.
    - ★ 그러니 **템플릿 문자열 안의 주석에서는 코드를 인용하지 않는다.**
      「길이 2048 짜리 0 바이트」처럼 **말로** 적는다.
  - **검사가 찾는 표시 이름을 주석에 글자로 적지 않는다** 〈2026-08-24 · 세 번째〉.
    - 새 지킴이 주석에 「짝 확인(`data-lp-pair`)은 …」이라고 적었더니,
      그 표시를 **첫 등장 위치로 찾던 검사**가 주석을 코드로 읽고
      **엉뚱한 스크립트 블록을 떼어 냈다** — 테스트 다섯이 빨개졌다.
    - ★ 표시 이름은 **한 곳(진짜 코드)에만** 두고, 설명에서는
      「짝 확인 조각」처럼 **이름으로 부르지 말고 말로** 가리킨다.
  - **거꾸로도 온다 — 주석에 「여는 html 태그」를 글자 그대로 적지 않는다**
    〈같은 날 두 번〉. 조각(fragment) 검사가 주석을 가리지 않고 보기 때문에
    **진짜 문서로 오인해 배포를 거부한다.** `embed-bridge.js` 주석 한 줄에
    적었다가 테스트 여덟 개가 빨개졌다. 태그를 설명해야 하면
    「문서 뿌리(`documentElement`)」처럼 **이름으로** 적는다.
- **상태를 바꾸는 기능은 갱신시각을 함께 남긴다. 그 시각은 한 곳에서만 만든다**
  (`core/kst.js` 의 `kstStamp()`). 상태 토글을 만들거나 고칠 때는 MEMORY.md 의
  「상태 토글 기능 검증 체크리스트」(M-02)를 전부 확인한다.
- **「반영됐다」와 「사용자가 보고 있다」는 다른 사실이다** 〈2026-08-23 · M-29〉.
  - 배포가 초록이고 지문이 새 판이어도 **브라우저가 형제 스크립트만 옛것을**
    들고 있을 수 있다. 그때 화면은 **멀쩡히 뜨고** 「… is not a function」
    한 줄만 남는다 — 캐시가 원인이라는 생각 자체가 안 든다.
  - 그래서 형제 파일 주소에 **판 표시(`?v=지문`)**를 붙이고(`build-stamp.js`),
    화면 안에 **짝 확인 조각**을 둔다. 화면·스크립트 판이 다르면 빨간 띠로 알린다.
  - 화면과 형제 스크립트를 함께 고쳤으면 MEMORY.md M-29 의
    「짝이 깨지는 배포를 막는 체크리스트」를 전부 확인한다.
  - **`?v=` 는 파일 이름이 아니다.** 주소에서 파일을 찾는 자리는 전부 떼고 본다.
- **배포·전달 전에는 교차검증을 전부 통과한 것만 내보낸다** 〈2026-08-23 사장님 지시:
  「배포전 항상 교차검증을 통해 완전할 경우만 배포해줘」〉. 최소 넷이다 —
  `npm test` 전부 초록 · `npm run im:stamp -- --check` 초록 · 미리보기 재생성 ·
  헤드리스로 실제 렌더 확인. **하나라도 못 했으면 무엇을 못 했는지 말하고** 낸다.
  - ★★★ **넷을 손으로 챙기지 않는다 — `npm run guard` 한 줄로 돈다**
    〈2026-08-24 · M-31〉. 손으로 챙기다 하루 반 동안 여섯 번 빠뜨렸다
    (미리보기 세 번 · 지문 두 번 · 헤드리스 한 번). 「할 줄 몰라서」가 아니라
    **「빠뜨려서」**다. 규칙을 문서에만 적으면 그 규칙은 사람의 기억에 얹힌다.
  - ★ **못 잰 것은 통과가 아니다.** `guard` 는 크로미움이 없으면 「못 쟀다」로
    적고 초록으로 안 끝낸다 (되돌아오는 값 2).
- ★★★ **이웃한 두 칸이 «서로 다른 말»을 하면 그 자체가 사고 신호다**
  〈2026-09-13 · 실측 · 거짓 초록을 그렇게 찾았다〉.
  - **무엇이 났나.** 한 배포 로그의 붙어 있는 두 줄이다 — 아이콘 확인은 「같은 주소에서
    **앱 파일도 안 온다 (HTTP 404)**」, 바로 다음 압축 확인은 「**켜짐** · 압축돼 온다 ·
    서버 nginx」. **같은 주소 · 같은 파일인데 하나는 404, 하나는 200**이다. 둘 다 참일 수 없다.
  - **원인은 한 줄이었다** — 압축 칸이 **상태코드를 안 보고** 머리말만 봤다.
    **404 안내 페이지도 압축돼 온다.** 그래서 앱이 그 주소에 아예 없는데 「압축 켜짐」이 나왔다.
  - ★ **「받았다」와 「그 파일을 받았다」는 다른 사실이다.** 아이콘 칸은 이미 그것을 배워
    고쳐 두었는데, **옆 칸에는 안 댔다.** 한 칸에서 배운 것을 옆 칸에 안 대면 그 자리에 그대로 남는다.
  - ★★ **더 나쁜 것은 내가 그 거짓 초록을 근거로 사장님께 두 번 「압축은 켜져 있습니다」라고
    적은 것**이다. 로그가 틀린 것이 아니라 **내가 두 줄을 나란히 안 읽었다.**
  - ★★★ 그러니 **한 실행의 칸들을 서로 대 본다.** 두 칸이 어긋나면 어느 한쪽이 못 잰 것이고,
    **못 잰 쪽을 찾기 전에는 초록을 믿지 않는다** (§8 「못 잰 것은 통과가 아니다」와 같은 결).
  - 재는 자리: `linkpilot-platform` 의 `test-pwa-icon.js` · 경위는 저쪽 MEMORY.md **S-53**.
- ★★ **PR 이 초록인지 볼 때 「상태」와 「검사」를 갈라 묻는다** 〈2026-09-13 · 같은 날 틀렸다〉.
  - GitHub 의 옛 **commit status** 와 지금의 **check run(Actions)** 은 **다른 것**이다.
    상태만 묻는 조회는 Actions 가 도는 저장소에서 **늘 0을 돌려준다.**
  - ★ 그것을 보고 「이 저장소는 PR 에서 도는 검사가 없다」고 사장님께 적었는데 **틀렸다** —
    `gate` 워크플로가 `pull_request` 에 멀쩡히 돌고 있었다.
  - ★★ **「검사가 0개」와 「검사를 못 읽었다」는 다른 사실이다.** 0 이 나오면 **워크플로 실행
    목록을 그 가지로 한 번 더** 묻는다. 뭉뚱그리면 **도는 검사를 없는 것으로 적게 된다.**

- ★★★ **무엇을 «쪼개면» 세는 검사가 저절로 줄어든다 — 그 줄어듦을 개선으로 적지 않는다**
  〈2026-09-13 · 앱을 세 번째로 쪼개다가 잡았다〉.
  - **무엇이 났나.** 앱을 파일 셋으로 쪼갰더니 배포 검사가 「UI 이모지 **167 → 99**개 ·
    `window.open` **1 → 0**곳 — 기준선보다 개선됨, 기준선을 낮추세요」라고 알렸다.
    그런데 그 검사는 **앱 파일 하나만** 읽는다. 이모지 36개와 그 `window.open` 한 곳은
    **없어진 것이 아니라 떼어 낸 갈래 파일로 옮겨 갔다.**
  - ★★ **그 말대로 기준선을 낮췄으면 그 순간 래칫이 거짓이 된다** — 갈래 안에서는
    얼마든지 늘어도 아무도 안 센다. 그리고 **나는 그것을 사장님께 「권하는 개선안」으로
    이미 적어 드렸다.** 숫자가 줄었다는 사실만 보고 옮겼는지 지웠는지 안 봤다.
  - ★ **잣대는 하나다 — 「그 숫자를 재는 법이 재려는 것을 다 덮는가」**
    (§6-2-6 의 46 → 105 와 **같은 규칙**이다. 그때는 조립하는 주소를 1로 셌고,
    이번엔 옮겨 간 파일을 아예 안 셌다).
  - ★★★ **고침은 기준선이 아니라 «세는 범위»다.** 래칫은 **앱 + 떼어 낸 갈래 전부**를
    함께 센다. 실측으로 쪼개기 전후가 **135 = 135** 로 같아졌다 — 쪼개기는 이 수를
    건드리지 않는 일이어야 한다.
  - ★ **구조를 보는 검사에는 갈래를 안 붙인다** — 태그 균형·`<script>` 구문은
    「이 HTML 문서」를 보는 자리라, 자바스크립트를 이어 붙이면 엉뚱한 답이 나온다.
  - ★★ **갈래가 없으면 「못 쟀다」로 적는다.** 조용히 앱만 세고 초록으로 끝내면
    이 규칙이 그 자리에서 없는 것이 된다 (§8 「못 잰 것은 통과가 아니다」).
  - 재는 자리: `linkpilot-platform` 의 `verify-build.js` · 경위는 저쪽 MEMORY.md **S-55**.

- ★★ **같은 것이 네 곳에 적혔다고 «한 곳으로 합치는 것»이 늘 답은 아니다**
  〈2026-09-13 · 합쳤다가 되돌렸다〉.
  - 떼어 낸 갈래 이름이 네 곳에 적힌다 — 검사 대상 목록 · 배포 목록 · 서비스워커 ·
    검사표. 한 곳으로 합치려고 배포 목록이 다른 파일을 «읽게» 고쳤더니,
    **워크플로 쪽이 읽는 목록에서 갈래 셋이 통째로 사라졌다**(실측). 그쪽은 배포
    파이썬 파일을 **글자로 훑어** 목록을 만드는데, 내가 그 글자 모양을 바꿔서다.
  - ★ **그 자리의 정본이 무엇인지 먼저 본다.** 저쪽 파일 머리말이 이미
    「적는 자리는 이 파이썬 하나, 나머지는 읽어 쓴다」고 정해 두었다 — 내가 그 규칙을
    모르고 반대로 뒤집었다. 오류는 한 줄도 안 났고 목록만 조용히 짧아졌다.
  - ★★★ **합치는 대신 «같은지 잰다».** 네 곳이 서로 어긋나는지를 검사 한 칸으로 세면
    합치지 않고도 어긋남이 안 남는다 — 그리고 합치다 생기는 이런 사고가 없다.
  - 재는 자리: `linkpilot-platform` 의 `test-app-split.js` (검사표 ↔ 갈래 목록 · 배포 목록 · 서비스워커).

- ★★★ **쪼갠 뒤에는 «앱만 읽는 검사»를 한 바퀴 훑는다 — 한 칸을 고쳐도 옆 칸은 그대로다**
  〈2026-09-15 사장님: 「권하는 개선안 진행해」 · 실측〉.
  - **무엇이 났나.** S-55 에서 **배포 검사 하나**의 세는 범위를 고쳤다. 그런데 **같은 고장이
    다른 검사 넷에 그대로 남아 있었다** — 전부 `linkpilot-platform.deploy.html` 하나만 읽고,
    떼어 낸 갈래 넷(**98만 자**)을 안 본다.
    · **대비 검사** — 짝 131 → **243**, 미달 16 → **41곳**. 25곳이 아무도 안 세는 자리에 있었다.
    · **두 벌 표 검사** — 표 155 → **318**, 두 벌 2종 → **6종**. 넷이 `lp-solar.js` 의 행정구역 표다.
    · **아이콘 규격** — 갈래에 두께를 숫자로 박은 곳 **82군데**, `IC_W` 참조 **0**.
      곧 「획 두께 1.8 단일」이 그 파일들에서 **아무도 안 재는 채로** 어긋나 있었다.
    · **사진 라이선스** — `readCanon()` 이 앱만 준다. 지금 값은 어디나 0장이라 **수는 안 달라지는데
      재는 범위가 모자랐다** — 「지금 0 이다」와 「0 인지 볼 수 있다」는 다른 사실이다.
  - ★ **드러난 수를 「늘었다」로 적지 않는다.** 그 25곳·4종·82군데는 **새로 생긴 것이 아니라**
    처음부터 있었고 재는 자리가 안 덮었을 뿐이다. 기준선을 올려 통과시키는 것과
    **세는 범위를 넓히는 것**은 다른 일이다 (§8 의 쪼개기 래칫과 같은 규칙).
  - ★★★ **기준선은 «파일마다» 둔다.** 합계 하나면 **코드를 갈래로 옮기는 것만으로** 한쪽이 줄고
    다른 쪽이 늘어도 총합이 같아 조용히 지나간다. 파일마다 세면 옮긴 자리가 그대로 드러난다.
  - ★★ **갈래 목록을 검사에 손으로 안 적는다** — `gate-target.js` 의 `CHUNKS` 를 읽는다.
    두 벌로 적으면 갈래가 늘 때 한쪽이 옛말을 한다 (§8-1 과 같은 결).
  - ★★★ **파일을 못 읽으면 「못 쟀다」로 빨갛게 끝낸다.** 조용히 앱만 세고 초록으로 끝내면
    그 검사가 그 자리에서 없는 것이 된다 (§8).
  - ★ **파일«마다» 「이 칸이 실제로 무언가를 찾고 있는가」를 함께 잰다.** 한 갈래의 글 모양이
    달라 0 개가 나와도 합계는 그럴듯해서 **눈이 먼 것이 안 보인다.**
  - ★★ **고치고 나서 이웃 칸이 어긋나는지 본다** — 사진 검사에서 실제로 났다. 옛 칸은
    「박아 둔 사진이 하나도 없다」, 새 칸은 「갈래에 한 장 있다」. **둘 다 참일 수 없고**,
    못 보는 쪽이 옛 칸이었다 (§8 「이웃한 두 칸이 서로 다른 말을 하면 사고 신호」).
  - 재는 자리: `linkpilot-platform` 의 `test-contrast.js` · `test-one-table.js` ·
    `test-icon-spec.js` · `test-image-license.js` — 넷 다 사보타주로 확인했다.

- ★★★ **저장소에 든 판 번호는 «날짜처럼 생긴 값»이면 안 된다**
  〈2026-09-15 · 내가 그것 때문에 사장님께 틀리게 적었다〉.
  - **무엇이 났나.** 판 번호는 **배포가 찍는다**(`stamp-version.js`). 그래서 저장소에는 마지막
    커밋된 값이 남는데, 그것이 `202608260130`(8월 26일)이라는 **진짜 날짜 모양**이었다.
    저장소 파일로 화면을 그리면 그 날짜가 찍히고, 그것이 **「사장님이 옛 판을 보고 계신다」와
    구별되지 않는다.** 실제로 내가 그 숫자를 보고 「옛 판을 보고 계신 것 같다」고 적었고,
    **M-25 가 만든 장치가 오히려 틀린 답을 보여 준 것**이다.
  - ★ **그래서 저장소에는 0 열두 개를 둔다.** 배포가 찍으면 진짜 값이 되고, 안 찍혔으면
    화면이 **「판 (배포 전)」**이라고 소리 내어 말한다 — 가짜 날짜보다 낫다
    (§8 「못 잰 것은 통과가 아니다」와 같은 결). 열두 자리 모양은 그대로라 앱·서비스워커가
    읽는 법은 안 바뀐다.
  - ★★ **0 을 화면에 그대로 찍지 않는다.** 「판 000000000000」 은 사람에게 아무 뜻이 없다.
    앱과 뿌리 화면 **둘 다** 사람 말로 적는다 — 한쪽만 고치면 다른 쪽이 옛말을 한다.
  - ★★★ **그 값을 인정하도록 검사를 넓히는 것은 «약하게 고치는 것»이 아니다.** 재려던 성질은
    「화면에서 판을 가릴 수 있는가」이고, 「(배포 전)」도 **가릴 수 있는 값**이다. 다만
    **그 둘 말고는 여전히 빨개진다** — 자리가 통째로 없어도 빨개진다 (사보타주로 확인).
  - 재는 자리: `linkpilot-platform` 의 `test-version-stamp.js`(저장소 값이 날짜 모양이면 빨개진다) ·
    `test-root-entry.js`(그려서 화면 글자를 읽는다).

- ★★★ **배포를 «거는 법»이 막히면 도구 탓부터 갈라 본다 — 저장소 탓으로 넘기지 않는다**
  〈2026-09-15 · 사장님 지적으로 잡았다: 「과거에도 이미 너가 사용했어」〉.
  - **무엇이 났나.** `workflow_dispatch` 를 **파일 이름**(`deploy-nas.yml`)으로 걸었더니
    **세 번 다 HTTP 500** 이었다. 나는 그것을 보고 「기본 가지(`main`)에 워크플로 파일이
    없어서 단추 자체가 없다」고 **단정하고 사장님께 그렇게 적었다.** 사장님이 누르셨는데
    실행이 안 생긴 것도 그 탓으로 돌렸다.
  - ★ **틀렸다. 숫자 id(`350892953`)로 거니 곧바로 `204 Queued` 였다.** 같은 저장소,
    같은 가지, 같은 입력값이다 — **바뀐 것은 부르는 법 하나**다.
  - ★★★ **어긋나는 두 사실이 이미 눈앞에 있었다** (§8 「이웃한 두 칸이 다른 말을 하면
    사고 신호」와 같은 결): 「`workflow_dispatch` 실행이 **21건** 있고 마지막이 09-13」과
    「기본 가지에 파일이 없어 못 건다」는 **둘 다 참일 수 없다.** 그 어긋남을 보고도
    **뒤엣것을 결론으로 적었다** — 앞엣것이 «실제로 돌아간 기록»인데도.
  - ★★ **500 은 「없다」가 아니다.** 없으면 404 다(`Workflow does not have workflow_dispatch
    trigger`). **500 은 서버·중개 도구 쪽 오류**이므로 **부르는 법을 먼저 바꿔 본다** —
    파일 이름 → 숫자 id, 입력값 형식, 가지 이름.
  - ★ **그리고 「사장님이 누르셨는데 실행이 없다」를 곧바로 「단추가 없다」로 읽지 않는다.**
    그것은 여러 원인 중 하나일 뿐이고, 그렇게 적으면 **사장님이 안 하신 일을 하신 것처럼
    되거나 그 반대가 된다.** 못 잰 것은 못 쟀다고 적는다 (§8).
- **배포를 걸 때는 `dry_run=false` · `engine=yes` 를 반드시 준다**
  〈2026-08-24 · M-31 · 두 번 헛돌았다〉.
  - `workflow_dispatch` 의 `dry_run` 기본값은 **`true`** 다. 값을 안 주고 걸면
    **초록으로 끝나는데 아무것도 안 올라간다** — 「Dry run summary」만 돌고
    나머지가 전부 skipped 라 **성공한 것처럼 보인다.**
  - ★ **푸시와 배포는 다른 일이다.** 커밋만 하고 「올렸습니다」라고 말한 적이
    있다. 「커밋했다 / 배포를 걸었다 / NAS 에 닿았다」는 **셋 다 다른 사실**이고,
    한 마디로 말하면 그 사이에 사장님이 옛 화면을 보신다 (M-25 와 같은 결).
- **그래도 문제가 나면 MEMORY.md 부터 찾는다** 〈같은 지시〉. 「처음 보는 문제
  같다」는 느낌은 자주 틀린다 — 오늘 난 사고 셋이 전부 이미 적혀 있던 결이었다.

## 8-2. 두 저장소의 경계 〈2026-08-26 사장님 지시 · D-120〉

사장님 지시 그대로다 —

```
linkpilot-platform → 유저 접촉
linkpilot-cron     → 유저가 보고서 요청 및 생산
```

**저장소 이름이 아니라 「일」로 가른다.** 어디에 둘지 헷갈리면 이 한 줄로 정한다.

★★★ **그리고 이것은 두 칸이 아니라 「한 줄기」다** 〈2026-08-26 사장님 지시 · 위 지시를 다시 주심〉.

사장님이 그리신 순서는 **유저 접촉 → `linkpilot-platform` → `linkpilot-cron` →
보고서 생산** 이다. 앞 판은 두 저장소를 **나란한 두 칸**으로 적어 두었는데,
그렇게 읽으면 「둘 중 어디에 둘까」만 답이 나오고 **어느 쪽이 먼저인지**가 안 보인다.

- **유저는 `linkpilot-platform` 만 만난다.** 사람이 이 저장소를 직접 여는 일은 없다.
- **`linkpilot-cron` 은 그 뒤에 선다.** 요청을 platform 에서 받아 보고서를 만든다.
- ★ 그래서 **방향이 정해져 있다** — platform 이 cron 을 부르는 것이지 그 반대가 아니다.
  이쪽이 저쪽 화면을 부르거나 저쪽 저장소에 쓰는 설계가 나오면 **줄기를 거스른 것**이다.
- ★★ 보고서 화면 16개가 저쪽에 **사본**으로 있는 이유도 이것이다. 유저가 만나는
  자리는 저쪽뿐이라 화면이 저쪽에 있어야 하고, **만드는 자리는 이쪽**이다.
  둘 중 하나를 고르는 문제가 아니라 **줄기의 앞뒤**다.

- **유저를 만나는 것**(CRM · 태양광 · 통화분석 · 회원·요금)은 `linkpilot-platform`
  갈래 `platform` 이 본체다. 정본은 `linkpilot-platform.deploy.html` 이고 **비공개**다.
- **보고서를 요청하고 만드는 것**(접수 화면 · 엔진 · 검증)은 이 저장소가 본체다.
  **공개**다 (D-10) — 그래서 앱 코드를 이쪽으로 옮기면 **그 순간 공개된다.**

★ **보고서 화면 16개는 이쪽 것이고, 저쪽 `im-flow/` 는 사본이다.**
  `npm run imflow:manifest` 로 「보낸 것」을 적고, `npm run imflow:check` 가
  그 기록이 지금 화면과 같은지 잰다 (교차검증 아홉째 칸).

★★ **어긋나는 것이 예외가 아니라 기본값이다.** 화면마다 판 지문이 박혀 있어
  이쪽이 **한 번만 바뀌어도 16개 전부**에 새 지문이 찍힌다. 실측에서 하루 만에
  15개가 어긋났다. 그러니 이쪽을 내보낼 때 **사본도 함께 보낸다.**
  ★ 지문이 어긋난 사본은 사장님이 화면 아래 여덟 글자로 판을 가리실 때
    **틀린 답을 보여 준다** (M-25 가 막으려던 바로 그 상태다).

~~★ **저쪽 저장소에 이쪽 세션이 쓰지 않는다.**~~ 〈2026-08-30 폐지 — 아래로 대체〉

★★★ **보고서 자리의 「머리」는 앱이 아니라 이쪽 화면이 단다** 〈2026-08-30 사장님 지시:
「LinkPilot Platform 보고서 생성에서 **상단 바 만들지 말 것** — 보고서 생성 엔진에서
배너를 달고 띄어짐」〉.

- 앱의 [보고서 만들기] 자리는 **아래 틀에 이쪽 화면을 통째로 얹는 자리**다. 앱이 제
  배너를 그리면 같은 것이 두 벌이 되고 사이가 뜬다.
- ★ **같은 일을 하는 자리가 둘이면 한쪽만 고쳐진다.** 저쪽은 2026-08-16 에 「상단
  고정바」를 같은 이유로 이미 한 번 지웠다 — 두 번째다. 그래서 이번에는 **어느 쪽이
  머리를 다는지 못박는다**: 머리는 **이쪽 화면**이 단다.
- ★★ **탭 줄은 배너가 아니다.** `[보고서 만들기]`·`[완성 보고서]` 는 틀의 주소를 바꾸는
  **동작**이라 저쪽에 남는다. 지우면 완성 보고서로 갈 길이 없어진다.
- ★★★ **그러므로 이쪽 화면에 머리를 넣을 때는 「앱 안에서 어떻게 보이나」로 잰다** (§8).
  혼자 뜬 판에서 멀쩡한 것은 판정이 아니다.
- 저쪽에 걸어 둔 검사: `test-report-no-hero.js` — 배너 글자가 0번인지, 탭 둘이 살아
  있는지 **실제로 그려서** 센다. 「지웠다」는 소스에서만 확인되고 「안 보인다」는 그려
  봐야 안다.

★★★ **이쪽 세션이 `linkpilot-platform` 에 직접 손댈 수 있다** 〈2026-08-30 사장님 지시:
「AI Project Manager Orchestrator 는 지금 이후부터는 LinkPilot 플랫폼이 저장소에
조치를 직접 할 수 있도록 권한을 부여한다」 · 「전달한 내용을 Platform Manager 를
모니터링하고 직접 지시 및 관리해줘」〉.

- **왜 바꿨나** — 앞 판은 전달 문서만 쓰고 손을 뗐다. 그런데 실제로 일어난 일은
  **문서가 쌓이고 아무것도 안 고쳐지는 것**이었다. 사장님이 직접 넘기셔야 했고,
  넘긴 뒤에도 됐는지 아무도 안 봤다. 「알려 주기」와 「고쳐지기」는 다른 사실이다.
- ★ **그래도 합치는 것은 사람이 한다 — 초안(draft) PR 까지가 이쪽 몫이다.**
  저쪽은 로그인·회원·CRM 데이터가 도는 자리라, 조용히 들어간 변경이 사고가 되면
  **되돌릴 사람이 그날 안 계실 수 있다.**
- ★★ **줄기의 방향은 그대로다** (위 ★★★). 권한이 생긴 것이지 이쪽이 저쪽의
  본체가 된 것이 아니다. 저쪽 화면·기능을 이쪽으로 옮기지 않는다.
- ★★ **`.gitignore` 를 함부로 넓히지 않는다.** 저쪽은 **허용목록** 방식이고,
  코드 옆에 투자자 기밀문서·CRM 백업·API 키 평문이 함께 있다. 한 줄을 넓히면
  그것들이 딸려 올라간다. 새로 추적할 것은 **폴더 하나씩 이름을 박아** 연다.
- ★★★ **커밋 전에 무엇이 담겼는지 눈으로 센다.** `git status --short` 로 파일
  목록을 보고, 비밀이 될 수 있는 이름(`*_key*` · `*backup*` · `linkpilot_data*`)이
  하나라도 있으면 멈춘다.
- **자동화 도구가 저쪽 쓰기를 막을 수 있다.** 막히면 **막혔다고 말하고**
  전달 문서로 낸다 — 조용히 넘어가지 않는다 (§8 「못 한 것을 먼저 말한다」).

## 8-1. Agent 를 하나 더 붙일 때

〈2026-08-25 사장님 지시: 「Agent 를 더 붙일 때 규칙 업무지침을 만들어줘」〉

**규칙은 [`docs/업무지침-Agent-붙이는-규칙.md`](docs/업무지침-Agent-붙이는-규칙.md)
한 곳에 있다.** 여기 옮겨 적지 않는다 — 두 벌이 되면 한쪽이 옛말을 한다.

- **배선은 다섯 곳이다.** 하나만 빠져도 **아무 오류가 안 나고 조용히 안 돈다.**
  `npm run agent:check` 가 등록된 **전부**에 대해 그 다섯을 센다.
  ★ 앞 판은 이 검사가 **한 Agent 에만** 있었다 — 그러면 다음에 붙는 Agent 는
  또 아무도 안 센다. 그것이 D-48 이 났던 자리다.
- **이름·D 번호·M 번호는 열려 있는 다른 갈래와 함께 본다** (D-101 · D-102).
  번호를 비워 두는 약속으로는 못 막혔다 — 실제로 여섯이 두 뜻을 가졌다.
  ★ **파일 겹침은 이제 `npm run branch:check` 가 센다** — 특히 **양쪽이 새로
  만든 같은 경로**를 따로 잡는다. 번호(D·M)는 여전히 사람이 본다.
- **교차검증은 이제 열두 칸이다** — 위 둘이 일곱째·여덟째, 대외 문서 PDF 가 열한째,
  **화면 크기가 열두째**다 〈2026-09-01 · M-71 · M-72〉.
  ★ 열두째를 왜 두었나: 아홉째 「헤드리스 렌더」는 **「그려지는가」**만 본다. 그려지기는
    하는데 **자리가 틀린** 고장은 그것으로 안 잡힌다 — 같은 날 둘이 났고
    (폰에서 잘림 · 앱 틀이 안 줄어듦) **둘 다 사장님이 눈으로 찾으셨다.**
    `npm run probe:viewport` 가 **폰(390)·맥(1440) 두 폭에서 실제로 그려** 가로 넘침 ·
    화면 밖으로 나간 것 · 상자가 내용보다 작은 것 · 잘린 글자를 센다.
  ★★ **헤드리스 창은 390px 로 안 줄어든다**(실측 485px). 그래서 폭이 정확한 **틀**에
    넣고 재고, **재려던 폭으로 못 쟀으면 「못 쟀다」로 끝낸다** — 다른 폭을 재고
    초록으로 끝내면 이 칸을 둔 뜻이 없어진다.
- 붙이고 나서 `npm run guard` → `npm run im:demo` 로 **그 Agent 의 줄이 실제로
  찍히는지** 본다. 등록·호출이 맞아도 입력이 안 닿으면 조용히 건너뛴다.

## 9. 미결정 사항 관리

- 결정되지 않은 것을 **추측으로 메우고 넘어가지 않는다.** 추측값은 반드시
  `docs/미결정-사항.md` 에 ID를 부여해 기록하고, 코드에도 추측임을 남긴다
  (예: `catalog.js` 의 `byGuide: false`).
- **세션을 시작할 때 `docs/미결정-사항.md` 를 읽고, 🔴 항목이 있으면 먼저 알린다.**
  작업 지시가 그 항목과 무관해도 알린다 — 미결정은 시간이 지날수록 비싸진다.
- 작업 중 새 미결정이 생기면 그 자리에서 등록부에 추가한다. 세션 끝에 몰아서
  쓰면 빠뜨린다.
- 결정된 항목은 **지우지 않는다.** `✅ 결정됨` 으로 바꾸고 결정일·결정자·반영
  커밋을 남긴다. 지우면 반년 뒤 같은 논의를 다시 한다.
- 사양(이 파일)과 코드가 다르면 어느 쪽이 맞는지 정하기 전까지 **양쪽 다 고치지
  않는다.** 먼저 등록부에 올린다.

## 10. 정기 운영점검 — 매일 08:30 KST 〈2026-08-30 사장님 지침 · 「반영해줘」〉

**지침 원문은 [`docs/운영지침-정기-운영점검-및-보고.md`](docs/운영지침-정기-운영점검-및-보고.md)
한 곳에 있다.** 여기 옮겨 적지 않는다 — 두 벌이 되면 한쪽이 옛말을 한다 (§8-1 과 같은 결).

그 문서에서 **이 저장소의 일과를 바꾸는 것 넷**만 여기 적는다.

- **증빙 없는 것을 정상으로 적지 않는다.** 확인 못 한 칸은 `UNVERIFIED` 다.
  ★ 이 저장소의 「못 잰 것은 통과가 아니다」(§8 · guard)와 **같은 규칙**이다.
- **점검하는 자리가 운영 코드를 고치지 않는다.** 문제를 찾으면 별도 작업으로 배정한다.
  ★ 이쪽이 `linkpilot-platform` 에 손댈 수 있게 된 뒤(§8-2)에도 **초안 PR 까지**인 이유가
    이것과 같다 — 찾는 손과 고치는 손을 갈라 둔다.
- **문제마다 담당자·목표일·완료조건·증빙이 있어야 한다.** 없으면 완료로 판정하지 않는다.
  ★ `docs/미결정-사항.md` 의 관리대장이 그 자리다. 지금 10건 중 인계자·인수자가 비어 있다.
- **`main` 병합·운영배포·운영 데이터 변경·외부 공개는 사장님 승인 없이 하지 않는다.**

★ **보고는 카톡 「나에게 보내기」로 알리고 본문은 아티팩트 주소로 준다.**
  카톡은 **200자 제한**이라 본문을 통째로 못 보낸다 — 요약 한 줄과 주소만 간다
  (2026-08-30 실측: 발송 성공). 주소는 저장소에 적지 않는다 (D-10).

★★ **시각은 UTC 로 적는다** — 08:30 KST = `30 23 * * *` UTC. KST/UTC 변환은 이
  저장소의 1순위 사고 원인이다 (§2).

## 11. 배포 전 교차검증과 API 관리 — 역할·게이트 〈2026-09-01 사장님 지침〉

**원문은 [`docs/운영지침-배포전-교차검증-및-API관리.md`](docs/운영지침-배포전-교차검증-및-API관리.md)
한 곳에 있다.** 여기 옮겨 적지 않는다 — 두 벌이 되면 한쪽이 옛말을 한다 (§8-1 과 같은 결).
그 문서의 **부록 A** 는 원문이 아니라 **이 저장소에서 실제로 재 본 결과**다.

그 문서에서 **이 저장소의 일과를 바꾸는 것 다섯**만 여기 적는다.

- **`guard` 는 검사이지 승인이 아니다.** 초록은 「낼 수 있다」이지 「내도 된다」가 아니다.
  ★ 지침 §4.1 「만든 Agent 는 자기 기능을 최종 승인할 수 없다」가 지금 **절반만** 지켜진다 —
    만드는 손과 재는 손이 같다. 독립 검증군은 **아직 실체가 없다** (D-202).
    없는 것을 있는 것처럼 적지 않는다.
- **Gate 3(Staging 실환경 검증)은 지금 한 칸도 못 잰다.** 이 시스템에 **Staging 이 없다** —
  `deploy-nas` 는 `main` 푸시에 곧바로 운영 자리로 나간다 (D-88). 그러니 Gate 3 을
  초록으로 적지 않는다. **`UNVERIFIED` 로 적고 그 위험을 승인서에 쓴다** (D-201).
- **같은 이름이 다른 뜻이다 — 넷.** `VERIFIED`·`BLOCKED`·`FAILED` 는 오케스트레이터
  지침의 **작업 단계**이자 이 지침의 **검증 판정**이고, `READY_TO_DEPLOY` 는 코드에서
  **디자인 게이트의 마지막 칸**(`core/design-gate.js`)이자 이 지침의 **릴리스 판정**이다.
  ★ 그래서 **어느 게이트인지 접두어로 부른다** — `DESIGN:READY_TO_DEPLOY` ·
    `RELEASE:READY_TO_DEPLOY`. 새로 겹치는 낱말이 생기면 `role-guide.test.js` 가 빨개진다.
  ★★★ **2026-09-12 — 다섯째가 생겼다: 「Gate 번호」** 〈인사이트 지침서 **v1.5** §13 · D-208〉.
    〈판 갱신: 같은 날 **v1.2 → v1.5** 까지 받았다. Gate 0~6 의 **뜻은 v1.1 과 같고**,
     담기는 칸이 늘었다 — `INSIGHT:Gate 1` 에 Quote Source Registry·SNS 약관 확인이,
     `INSIGHT:Gate 3` 에 인용 교차검증·창작시 표절검사·SNS 원문복제 방지가 붙었다.
     ★ **접두어 규칙은 그대로 유효하다** — 판이 올라갔다고 다시 정하지 않는다.
     ★★ 그리고 v1.5 에서 **낱말 겹침이 둘 더 생겼다** — 「테마」와 「다운로드」다.
       둘 다 §6-2-7 에 적었고, 부르는 법도 거기서 정했다 (`CONTENT:테마`·`IMAGE:테마` ·
       `CARD:다운로드`·`PHOTO:다운로드`). 이제 **일곱**이다.〉
    v1.1 이 **Gate 0~6** 을 새로 두는데 그 번호가 다른 것을 가리킨다 — 특히 **Gate 3** 이
    이쪽에서는 **Staging 실환경**(못 재는 그 게이트), 저쪽에서는 **콘텐츠·디자인**이다.
    ★ 낱말이 아니라 **숫자**라 `role-guide.test.js` 에는 안 걸린다. 그래서 **사람이 적는다** —
      부록 A-1 에 표로 두었다. 같은 방식으로 **접두어를 붙인다**: `INSIGHT:Gate 3` ·
      `RELEASE:Gate 3`. **접두어 없이 「Gate 3」만 적힌 글은 이 지침 것으로 읽는다.**
- **비용 한도 초과·유료 전환·새 유료 API 는 사장님 승인이다.** 지침 §3.6 은 API Manager 가
  「단독 승인할 수 없다」고만 적고 **누가 승인하는지**를 안 적었다 — 그러면 넘긴 순간
  아무도 못 멈춘다. 그리고 **한도의 80% 에서 미리 알린다**(넘긴 뒤 알리는 것은 보고가 아니다).
- **Rollback 은 사전 승인 없이 실행한다.** 되돌리는 것은 지키는 일이라 승인을 기다리는
  동안 손해가 는다. ★ 다만 **되돌렸어도 빨갛게 끝난다** — 되돌린 것은 서비스를 살린
  것이지 배포가 된 것이 아니다 (§8 · `deploy/engine.sh` 와 같은 규칙).

★ **조직도에 역할을 더하면 부록 A-9 대조표에도 더한다.** 안 더하면 그 역할은 **그림에만
  있는 채로** 아무도 안 맡는다 — 실제로 `Design Manager Agent` 가 그 상태였다.
  `npm test` 의 `role-guide.test.js` 가 조직도와 대조표를 대 본다.

★★ **번호(`D-`)는 `npm run d:next` 로 받는다** — 그런데 그 도구가 **이미 쓴 번호를 내주고
  있었다** 〈2026-09-01 실측〉. 결정된 항목은 `docs/결정-기록.md` 로 옮겨 가는데 그 파일은
  **표**라 도구의 머리말 규칙에 안 걸렸다. `D-191` 을 내줬는데 그 번호는 결정 기록에
  **세 번** 나온다. 이제 그 파일에서 **번호만** 함께 줍는다.

## 12. 오늘의 브리핑 v2.0 — 「보여주는 화면」에서 「다음 행동을 알려주는 화면」으로
〈2026-09-13 사장님 지침: 「교차검증후 충돌없이 반영해줘」 · D-209〉

**원문은 [`docs/운영지침-오늘의브리핑-v2.0.md`](docs/운영지침-오늘의브리핑-v2.0.md)
한 곳에 있다.** 여기 옮겨 적지 않는다 — 두 벌이 되면 한쪽이 옛말을 한다 (§8-1 과 같은 결).

★ **화면은 `linkpilot-platform` 의 것이다** (§8-2 — 유저 접촉). 이 저장소는 **지침 원문과
규칙**을 갖고, 구현·검사는 저쪽이 한다. 줄기를 거스르지 않는다.

그 문서에서 **이 저장소의 일과를 바꾸는 것 여섯**만 여기 적는다.

- **먼저 실사한다 — 고치기 전에.** 지침 §16 Phase 1 과 §22 의 1·2번이 **읽기 전용 실사**를
  첫 칸으로 못박는다. 각 기능에 `IMPLEMENTED / PARTIAL / DOCUMENTED / NOT IMPLEMENTED /
  UNVERIFIED` 를 붙인 뒤에 고친다. ★ 이 저장소의 「못 잰 것은 통과가 아니다」(§8)와 **같은 결**이다 —
  `UNVERIFIED` 는 이미 §10·§11 이 쓰는 낱말이라 뜻이 겹치지 않는다.
- ★★★ ~~**브리핑이 지금 «두 벌»이다**~~ → **2026-09-16 끝났다.** 옛 벌은 09-15 자동발송
  삭제 때 이미 내려갔고(생성기 `callMorningBriefing` 제거), 09-16 에 **쓰는 자리가 0곳이던
  `brief` 상태와 읽기 한 줄까지** 지웠다. **날씨는 그 전에 새 벌로 옮겨 두었다**(아래 ★★★).
  ★ 아래 경위는 **왜 그 규칙이 생겼는지**를 남기려고 둔다 — 지금 값이 아니다. 〈2026-09-13 실사〉.
  한 화면이 `brief2`(코드가 고르고 AI 는 설명만 한다)를 그린 **바로 다음에** 옛 `brief`
  (AI 가 이모지 9섹션을 통째로 쓴 글)를 **이어서** 그린다. 같은 일정·같은 할 일이 두 번 나온다.
  ★ **재 보니 새 벌은 «거의» 지침대로였다** — 캐시를 **날짜로 걸러** 어제 것을 오늘 것으로
  안 쓰고〈§14.1〉, AI 가 안 붙으면 **「AI 요약을 준비하지 못했습니다 — 일정과 우선업무는
  그대로 보입니다」**로 적는다〈§11.3 권장 문구 그대로〉.
  ★★ **다만 「거의」였다** 〈2026-09-13 · Phase 2 에서 찾음〉. 새 벌도 **통째로 던진 경우
  (`catch`)에는** `❌ ` + 오류 원문을 그대로 찍고 있었다. 앞 판의 이 줄은 「새 벌은 지침대로」로
  적었는데 **반만 맞았다** — 재는 자리가 옛 벌만 보고 있어서다. 둘 다 사람 말로 고쳤고,
  검사도 **두 자리를 함께** 본다. §4.6 에서 「OCR이 나빠졌다」로 읽힌 그 자리다.
  ★★ **그러니 고칠 것은 두 벌을 각각 고치는 것이 아니라 «옛 벌을 내리는 것»이다.**
  두 벌로 두면 §6-3 ⑥(같은 것을 두 자리에 두지 않는다)을 그대로 어긴다.
  ★★★ 다만 **옛 벌에만 있는 것이 있다** — **날씨**다. 내리기 전에 그것을 새 벌로 옮긴다.
    안 옮기고 내리면 **되던 것이 없어진다.**
- ★★★ **모바일 첫 화면 필수 넷 중 «셋이 찼다»** 〈§3.1 · 2026-09-13 Phase 2 로 하나를 채움〉.
  ① 오늘 한 줄 요약 · ② 최우선 업무 · **③ 날씨·옷차림**(Phase 2 에서 넣음)이 있고,
  **④ 첫 일정의 권장 출발시각은 아직 0곳**이다. 일정 충돌 판정도 0곳이다〈§6.6〉.
  ★★★ **날씨는 「받는 값」이 아니라 「나르는 값」이 빠져 있었다** — `buildBriefPayload` 는
  이미 날씨를 받아 오는데 **새 벌이 저장하는 값에 안 실려** 화면까지 못 갔다.
  받는 것과 나르는 것과 그리는 것은 **셋 다 다른 사실**이다 (§8 「걸었다 ≠ 올라갔다」와 같은 결).
  ★★ **옷차림은 «기온»이 아니라 «체감온도»로 정한다** 〈§5.3〉. 지금 값으로 정할 수 있는 것은
  셋이다 — 체감온도 · 일교차 · 강수확률. **바람 · 실내/야외 비중 · 격식 미팅 여부는 값이
  없어서 안 쓴다**(지어내지 않는다). 건강상태·질병은 **어떤 경우에도** 추정하지 않는다.
  ★★★ **못 받으면 계절 감으로 지어내지 않는다** 〈§17.2〉 — 그 사실을 화면에 그대로 적는다.
  날씨가 틀린 것보다 **틀린 줄 모르는 것**이 훨씬 나쁘다.
  ★ 재는 자리: `linkpilot-platform` 의 `test-brief-v2.js` — 옷차림 함수를 **돌려서** 잰다
  (「옷차림이라는 낱말이 있는가」는 아무것도 안 재는 것이다).
  ★ 남은 것이 **Phase 3·5 의 일감**이고, 지금 상태를 「거의 됐다」로 적으면 그 일이 사라진다.
- **일정과 동선을 «한 카드»에 둔다** 〈지침 §6.1〉. ~~같은 화면에 「이동 동선」 칸이 또 있다.~~
  ★★★ **2026-09-15 합쳤다** 〈사장님 지시: 「오늘의 일정 안에 대중교통 및 자동차동선 도착장소까지
  소요시간 정보를 제공」 · 화면 사진에 「일정행사와 합침」〉.
  - **무엇이 문제였나.** 따로 서 있던 것만이 아니다 — 그 칸의 길찾기는 **첫 일정 한 곳**으로만
    만들어졌다(`firstLoc`). 일정이 셋이어도 길은 **한 줄**이었고, **둘째·셋째 미팅으로 가는 길은
    화면에 아예 없었다.** 「합치기」만 보고 지나갔으면 그 빠짐이 그대로 남는다.
  - ★ **출발지는 «직전 일정의 장소»다.** 둘째 미팅은 집이 아니라 **첫 미팅 자리에서** 떠난다.
    첫 일정만 기준점(선정릉)에서 출발한다.
  - ★★ **대중교통·자동차를 «둘 다» 낸다.** 하나만 두면 그날 어떻게 가실지를 **앱이 대신 고른 것**이다
    (§4.9 — 자동으로 하나를 고르지 않는다).
  - ★★★ **소요시간은 «아직 못 잰다» — 그 사실을 화면에 적는다.** 이 앱에 길찾기 소요시간을 주는
    출처가 **하나도 없다**. 지도 딥링크는 «여는 주소»일 뿐 시간을 안 돌려준다. 거리 어림으로
    「약 40분」을 적으면 사장님이 그 시각을 믿고 **늦으신다** — 값이 없는 것보다 훨씬 나쁘다
    (§4.6 · 지침 v2.0 §17.2). 그래서 카드 아래에 못 잰다고 **한 번** 적고 여는 길만 드린다.
    ★ **그 줄이 사라지는 날이 소요시간을 실제로 재게 된 날이다.** 그 전에 지우면
      「시간이 맞나 보다」로 읽히는 자리가 생긴다 (§8 「없는 것을 그리지 않는다」).
    ★★ 막고 있는 것은 **길찾기 열쇠 하나**다 — 지도 딥링크로는 못 푼다.
    ★★★ **2026-09-15 — 출처를 정했다: «둘 다»** 〈사장님 승인: 「둘 다 — 자동차+대중교통」〉.
      자동차와 대중교통은 **한 곳에서 둘 다 받는 길이 없다** — 그래서 둘로 나눈다:
      **카카오모빌리티**(자동차) · **ODsay**(대중교통). 하나만 붙이면 그날 어떻게 가실지를
      앱이 대신 고른 것이 된다 (§4.9 와 같은 결).
      ★ **아직 배선하지 않았다 — 열쇠가 없다.** 지금 한 것은 셋뿐이다:
        ① 지침서 표에 이름 넷을 적고 ② `SECRET_ENV` 에 넣어 **넣으신 날 바로 가려지게** 하고
        ③ 진단 표에 올렸다. **넣기 전에 가림부터 등록하는 것**이 요점이다 —
        나중에 넣으면 그 사이의 값이 로그에 평문으로 남는다 (§2).
      ★★ **베이스 URL·인증 파라미터를 추측으로 적지 않는다.** 열쇠를 받은 뒤 **진단부터**
        돌려 응답 본문을 그대로 보고 채운다 (§4.3). 지금 적으면 그것이 곧 거짓이 된다.
      ★★★ **이 자리에서는 네 곳 모두 안 열린다 — 그것을 「안 된다」로 적지 않는다** 〈실측〉.
        카카오모빌리티·ODsay·TMAP·네이버가 전부 응답 없음인데, **같은 자리에서
        `api.github.com` 은 열린다.** 곧 **이 개발 컨테이너의 나가는 길이 막힌 것**이지
        러너·NAS 에서 안 열린다는 뜻이 아니다 (D-206 의 「못 닿음을 승인 안 됨으로 적지
        않는다」와 같은 결). **도는 자리는 열쇠가 들어온 뒤 그 자리에서 잰다.**
      ★ **이름을 둘씩 읽는다** — `KAKAO_MOBILITY_KEY`/`KAKAOMOBILITY_KEY` ·
        `ODSAY_API_KEY`/`ODSAY_KEY`. 이름이 갈리면 **아무 오류도 안 나고 조용히 값이 죽는다**
        (`ECOS_API_KEY`/`ECOS_BOK_KEY` · `LAW_OC`/`LAW_OPEN_DATA` 에서 두 번 당했다).
      ★★ **화면의 「소요시간은 아직 못 잽니다」 줄은 그대로 둔다** — 값이 실제로 들어온 날
        지운다. 열쇠를 넣은 것과 값이 오는 것은 다른 사실이다 (§8 「걸었다 ≠ 올라갔다」).
  - ★ **표본도 함께 고쳤다.** 검사 표본의 일정에 출발지·길찾기 주소가 없으면 단추가 안 그려지는데,
    그것은 「앱이 안 그려서」가 아니라 **「안 줘서」**다 — 그 상태로 재면 고장을 못 잡는다
    (「표본이 거짓말을 하면 잡히는 것도 거짓이다」와 같은 결).
  - 재는 자리: `linkpilot-platform` 의 `test-brief-ux.js` **조건 13** — 실제로 **그려서** 넷을 센다
    (동선 줄 · 대중교통·자동차 둘 다 · 소요시간 안내 · 따로 선 절 0개).
- ★★★ **아침 브리핑의 차례는 «다섯»이다 — 그리고 일정 칸은 «세로로 이어진 시간선»이다**
  〈2026-09-15 사장님 지시: 「오늘의 아침브리핑에서 1.날씨 2.할일 3.일정/동선 4.통화분석
  5.신규 프로젝트 현황 · 일정별 라인(선)을 수직 연결 · 시간 타임별 구분 ·
  동선(대중교통/자동차)정보제공 · 준비체크[확인]탭 만들어」〉.
  - **차례가 어긋나 있던 자리** — 「밀린 일」과 「이어서 중요한 일」은 **할일**인데 «통화» 뒤,
    «신규 프로젝트» 앞에 끼어 있었다. 할일이 화면 두 자리로 나뉘고, 3번(일정)과 4번(통화)
    사이가 할일로 끊겼다. ★ **자리만 옮기고 안은 한 글자도 안 고쳤다** — 옮기면서 고치면
    무엇 때문에 달라졌는지 알 수 없게 된다.
  - ★★ **세로선은 «순서를 나르는 선»이지 장식이 아니다** (§6-3 「애니메이션을 장식으로 넣지
    않는다」와 같은 잣대). 앞 판은 카드가 따로따로 떠 있어 「이 다음이 저것」이 안 보였다.
    ★ 선은 **첫 점에서 시작해 마지막 점에서 끝난다** — 위아래로 삐져나가면 「위에도 뭔가 있다」로
    읽힌다. 일정이 하나면 **선을 안 그린다**(이을 것이 없다).
  - ★★★ **「시간 타임별 구분」은 «잰 값»일 때만 적는다.** 두 일정의 시각을 읽어 사이가 얼마인지
    적고(「↓ 3시간 뒤」), 시각을 못 읽으면 **그 줄을 아예 안 그린다** — 「곧」·「잠시 뒤」로
    얼버무리면 그것이 지어낸 값이다 (§4.6).
  - ★★ **준비체크는 누른 «시각»을 함께 남긴다** (M-02) — 「확인됨」만 있으면 언제 확인한 것인지
    알 수 없다. 시각은 **한 곳에서만** 만들고 `Asia/Seoul` 을 명시한다 (§5).
    ★ **날짜가 바뀌면 스스로 빈다** — 어제 확인한 것이 오늘 확인된 것으로 남으면 그 표시가
      거짓이 된다 (`brief2` 캐시를 날짜로 거르는 것과 같은 규칙).
    ★★ **다시 누르면 풀린다** — 잘못 누른 것을 되돌릴 길이 없으면 막다른 길이다 (§6-3 ⑥).
  - ★★★ **「단추가 있는가」와 「눌러서 달라지는가」는 다른 사실이다.** 검사가 실제로 **누르고**,
    다시 그려진 뒤의 점·시각·단추 글자를 읽는다 — 그리고 **한 번 더 눌러 풀리는지**까지 본다
    (§6-2-5 「되돌릴 길을 지울 때는 새 길을 눌러 본다」).
  - ★★ **표본에 날씨가 없어 「날씨가 맨 위인가」를 못 재고 있었다.** 실제 payload 는 싣는데
    표본에 없으면 그 카드가 안 그려진다 — **못 잰 것을 통과로 적지 않으려면 표본이 재려는
    성질을 지켜야 한다** (「표본이 거짓말을 하면 잡히는 것도 거짓이다」와 같은 결).
  - ★ **차례는 «이름»이 아니라 «자리»로 잰다.** 절이 다 있어도 순서가 다르면 지시를 어긴 것이다.
    ★★ 그날 자료가 없어 **안 그려지는 절**(통화가 없는 날)은 실패로 세지 않는다 — 그러면
      「통화가 없는 날」마다 빨개진다. **있는 것들의 상대 차례**만 본다.
  - ★★★ **그리고 그 「눌러 본다」가 «내 자리에서만» 통했다** 〈2026-09-15 · CI 에서 빨개졌다〉.
    - **무엇이 났나.** 누른 뒤 **고정 220ms** 를 기다리고 읽었다. 내 자리에서는 됐는데
      러너에서는 그 사이에 점을 **0개**로 읽어 「다시 눌러 `[]`」로 빨개졌다.
      **같은 코드가 자리에 따라 다른 답을 내면 그 검사는 못 믿는다.**
    - ★ **고친 방향이 중요하다** — 기다림을 늘리거나 그 칸을 빼는 것이 아니라,
      **«달라질 때까지» 재는 것**으로 바꿨다. 재려던 성질(눌러서 달라지는가)은 그대로다.
    - ★★ **「있는가」로는 아무것도 못 기다린다.** 점은 누르든 안 누르든 늘 있다 —
      처음에 그렇게 짰다가 **누르기 전 값을 그대로 읽어** 거짓 빨강이 났다.
      **이전 값과 견주어** 바뀔 때까지 기다려야 한다.
    - ★★★ **못 기다린 것을 「깨졌다」로 적지 않는다.** 2초가 지나도 안 바뀌면 그 사실을
      그대로 쓴다 — 「되돌리기가 안 되는 것이거나, 화면이 그때까지 안 그려진 것이다.
      둘 중 어느 쪽인지 이 칸은 못 가른다」 (§8 — 못 잰 것을 통과로도 실패로도 뭉뚱그리지 않는다).
  - 재는 자리: `linkpilot-platform` 의 `test-brief-ux.js` **조건 14 · 15**.
- ★★★ **「지금 할 일」은 «할 일»에서 온다 — 최근 일자 셋. 그리고 「밀린 일」 상자를 내렸다**
  〈2026-09-15 사장님 지시: 「지금할일은 할일에서 소싱 최근일자별로 3개 노출배치해줘」 ·
   화면 사진에 일정 줄과 「밀린 일」 상자를 빨갛게 두르시고 **「삭제」**〉.
  - **무엇이 틀려 있었나.** 그 자리가 **일정(schedule)**과 urgent 를 그리고 있었다. 그런데
    일정은 **3번 카드(오늘의 미팅·준비와 동선)**가 이미 통째로 그린다 — 같은 것이 두 자리에
    있었고, 그 줄에 **[길찾기] 까지 붙어** 동선 카드와 두 벌이었다 (§6-3 ⑥).
    그래서 **정작 할 일은 이 칸에 한 줄도 안 나왔다.**
  - ★ **날짜는 `dueDate||date` 로 읽는다** — 이 앱의 다른 세 자리가 쓰는 것과 **같은 식**이다.
    두 벌로 읽으면 한쪽이 옛말을 한다.
  - ★★ **AI 가 지어낸 줄로 덮지 않는다.** 앞 판은 `ai.actions` 가 있으면 그것이 이겼는데,
    그러면 「할일에서 소싱」이 **AI 가 오는 날에만 거짓**이 된다. 소스는 하나여야 한다
    (AI 의 한 줄 설명은 우선업무·이어서 중요한 일 카드에 그대로 남는다).
  - ★★★ **차례를 화면에 보이게 적는다** — 왼쪽 칸에 그 날짜(`09-14`)를 찍는다. 「최근 순」이라고
    말만 하면 사장님이 **확인할 길이 없다** (§6-3 ②). 날짜 없는 할 일은 뒤로 민다.
  - ★ **완료 단추가 그 할 일을 «그대로» 들고 온다.** 앞 판은 제목 글자를 맞춰 찾아서,
    **제목이 비슷한 다른 할 일을 완료 처리할 수 있었다** — 오류가 안 나고 엉뚱한 것이 지워진다.
  - ★★★ **「밀린 일」 상자는 «중복»이라 내렸다.** `b.priority` 와 `b.top3` 에서만 뽑아,
    바로 위 「우선업무」와 바로 아래 「이어서 중요한 일」이 이미 그리는 것을 한 번 더 적었다 —
    사장님 화면에서 실제로 **네 건 중 셋이 다른 카드에 그대로 또** 있었다.
    ★ **내리기 전에 「없어지는 사실」부터 확인했다** — 「며칠 지났는가」는 두 카드가 각각
      「· 77일 지남」·「마감이 97일 지남 +60」으로 그대로 적는다 (§12 의 날씨 교훈).
  - ★★★ **표본이 재려는 성질을 지켜야 한다.** 할 일 표본에 날짜를 섞고 **완료된 것(가장 최근)**과
    **날짜 없는 것**을 일부러 넣었다. 「셋이 나오는가」만 재면 **아무거나 셋**이어도 통과한다 —
    **어느 셋인지**와 **차례**까지 본다.
  - ★★★ **그리고 이 고침이 다른 칸 하나를 «지시의 결과»로 깨뜨렸다** — 일정이 첫 화면에서
    사라졌다(**587px → 1007px**, 창 813px). 앞 판이 첫 화면에서 일정을 보여 준 것은
    **일정이 두 자리에 중복돼 있었기 때문**이다. 지침 v2.0 §3.1 ④ 와 부딪히지만
    **앞 지시가 이긴다** (§12-2 와 같은 잣대).
    ★ **그것을 「실패」로도 「삭제」로도 적지 않는다.** 실패로 두면 **고칠 것이 없는 자리를
      날마다 고치라**고 말하고, 칸을 지우면 **그 사실이 사라진다.** 그래서 상태를 하나 더 두었다 —
      `superseded`: **재기는 계속하고 숫자도 찍되** 실패로 안 센다. 까닭을 반드시 적게 해서
      조용한 도피처가 되지 않게 한다 (§8 「못 잰 것을 통과로 적지 않는다」와 같은 결).
    ★★ 그리고 **원래 한 칸이던 것을 둘로 갈랐다** — 「최우선 업무가 첫 화면에」는 **그대로
      빨갛게 막는다.** 둘을 묶어 두면 일정 때문에 최우선 업무까지 안 재게 된다.
  - 재는 자리: `linkpilot-platform` 의 `test-brief-ux.js` **조건 17 · 18 · 19** — 셋 다
    사보타주로 확인했다(다섯 개 노출 · 차례 뒤집기 · 밀린 일 되살리기).
- ★★★ **지도는 «고를 수 있게» 하고 기본은 구글이다 — 그리고 «검색으로 여는 지도»에는 주소를 다듬어 넘긴다**
  〈2026-09-15 사장님 지시: 「기본셋팅은 구글맵, 지도선택가능하도록 만들어줘 · 그지도별 클릭시
   오류 없도록 교차검증후 반영해줘」 · 화면 사진 둘〉.
  - **무엇이 났나.** 네이버가 **「조건에 맞는 업체가 없습니다」**를 냈다. 넘어간 글자가
    「718-1, 대한민국 경기도 안산시 상록구 이동」 — **구글 캘린더가 주는 모양**이다.
    네이버·카카오는 그것을 **업체 이름으로 검색**하므로 못 찾는다. 같은 목적지가
    **구글에서는 멀쩡히 열렸다** — 구글만 `dir/?api=1&origin=…&destination=…` 로
    **진짜 길찾기**를 열기 때문이다.
  - ★ **기본값은 «실제로 열리는 것»으로 둔다.** 되는 것을 기본에 두는 것이 규칙이다.
  - ★★ **주소를 다듬는 자리는 한 곳이다** (`lpMapAddr`). 지도가 셋이라 자리마다 적으면
    한쪽이 옛말을 한다 (§6-3 ⑥). 다듬는 것은 **확실히 아는 넷**뿐이다 — 나라 이름 ·
    우편번호 · **앞머리 번지** · 쉼표. 건물 이름·영문 주소까지 손대면 멀쩡한 것을 망가뜨린다.
  - ★★★ **길찾기 주소를 추측해 만들지 않는다.** 네이버·카카오의 출발지 지정 길찾기는
    좌표를 요구하고 공개 규격이 확실치 않다 — 추측해 박으면 **그것이 또 다른 「조건에 맞는
    업체가 없습니다」**가 된다 (§4.3). 그래서 그 둘은 **도착지를 여는 것까지만** 하고
    **왜 그런지 화면에 적는다** (§8 — 안 되는 길을 되는 척 그리면 고장으로 읽힌다).
  - ★ **주소가 없으면 그 단추를 아예 안 그린다** — 「클릭시 오류 없도록」이 그 뜻이다.
  - ★★ **고르는 칩을 «채운 단추»로 칠하지 않는다.** 그 줄은 고르는 줄이지 그 카드의 주 행동이
    아니다 — 채웠더니 「주 행동이 둘」로 세져 **검사가 실제로 빨개졌다** (§6-3 ④).
  - ★★★ **이 링크가 실제로 «열리는지»는 이 자리에서 못 잰다** — 개발 컨테이너가 그 호스트에
    못 닿는다(실측). 잴 수 있는 것은 **글자가 바로잡히는가**와 **화면이 무엇을 그리는가**까지다.
  - 재는 자리: `linkpilot-platform` 의 `test-brief-ux.js` **조건 20** — 칩을 **눌러** 단추가
    실제로 달라지는지 본다(「칩이 셋인가」만 재면 아무 뜻이 없다).

- ★★★ **사람마다 달라야 하는 자료는 «사람 자리»에 둔다 — 그리고 «장치가 이미 있는지» 먼저 본다**
  〈2026-09-16 사장님 지시: 「정보가 혼재됨 오류·버그 · 사용자별 개인일정 정보가 섞여
   뒤죽박죽 · 교차검증후 개선해줘」 · 「진행해」〉.
  - **무엇이 났나.** 저장 창구(`lsGet`/`lsSet`)가 열쇠를 **그대로** 썼다. 그래서
    `lp_todos`·`lp_events`·`lp_projects`·`lp_contacts` 가 **기기 하나에 한 벌**이었고,
    한 기기에서 **계정이 바뀌어도 같은 자리**를 봤다.
  - ★★★ **장치는 이미 있었다 — 배선만 없었다.** `lpUidFor`/`lpSetUid` 가 `window.LP_UID` 를
    만들어 두는데 **읽는 곳이 한 군데도 없었다**(§6-2-6 「선언만 되고 아무도 안 읽는 자리」).
    새로 만들기 전에 **이미 있는 것부터 찾는다** — 실제로 고침은 **그 배선을 잇는 것**이었다.
  - ★★ **창구만 고치면 반쯤이다 — «우회하는 곳»을 세어야 한다.** 실측으로 하나 나왔다:
    구독 캘린더(ICS) 저장이 `localStorage` 에 **곧장** 써서 **회원 일정이 관리자 자리로** 갔다.
    창구를 고쳐도 그 자리는 안 갈린다.
  - ★★★ **«되풀이 자리»까지 갈라야 한다.** 용량 초과 재시도 경로가 옛 열쇠를 쓰고 있었다 —
    **저장공간이 찼을 때만 조용히 어긋나** 눈에 안 띄는 종류다.
  - ★ **베끼지 않는다.** 옛 자리 값을 새 자리로 복사하면 **관리자 자리의 것이 회원에게
    복사되어 오히려 더 섞인다.** 회원은 **제 빈 자리에서 시작**하고, 옛 값은 **안 지운다** —
    되돌릴 길을 남긴다 (§4.6 「빈 것으로 덮지 않는다」와 같은 결).
  - ★★ **관리자가 안 움직이는지 확인하고 시작한다.** 이 앱은 `lpUidFor` 가 관리자에게
    **빈 문자열**을 주어 열쇠가 그대로다 — 그래서 **사장님 자료는 한 글자도 안 움직였다.**
    그 성질이 없었다면 **운영 자료 이전**이라 먼저 승인을 받아야 한다 (§10).
  - ★ **가르는 열쇠는 좁게 시작한다.** 사람마다 달라야 하는 여섯만 — 회원 명단·기기 번호·
    설정처럼 **기기 한 벌이어야 하는 것**은 안 건드린다.
  - 재는 자리: `linkpilot-platform` 의 `test-user-scope.js` (교차검증 **101번째 칸**) —
    창구가 붙이는가 · **되풀이 자리까지** · **우회하는 곳 0인가** · 관리자는 안 움직이는가.
    사보타주 둘 다 빨개진다.

- ★★★ **「최근 일자」는 «날짜가 큰 순»이 아니다 — 칸의 이름이 차례를 정한다**
  〈2026-09-16 · 내가 만든 버그를 사장님이 화면에서 찾으셨다〉.
  - **무엇이 났나.** 「지금할일은 할일에서 소싱 **최근일자별로** 3개」를 **내림차순**으로 읽었다.
    그랬더니 **미래 날짜가 가장 「최근」**으로 세져, 오늘(9/16)에 **11일 뒤인 09-27** 이 맨 위에
    왔다. **「지금 할 일」인데 먼 미래 것이 먼저** 나온 것이다.
  - ★ **칸의 이름이 답이다.** 「지금 할 일」이면 **지금 해야 할 것**이 위로 온다 —
    지난 것 → 오늘 → 가까운 미래(**오름차순**). 말의 뜻이 애매하면 **그 칸이 무엇을 하는
    자리인지**로 정한다.
  - ★★ **표본이 그 성질을 지켜야 잡힌다.** 앞 판 표본은 전부 과거라 **내림차순이어도 통과**했다 —
    그래서 못 잡았다. **먼 미래 하나를 넣으니** 그 자리에서 빨개진다
    (「표본이 거짓말을 하면 잡히는 것도 거짓이다」와 같은 결).

- ★★ **앱이 크기 한도에 «붙어 있다» — 다음에 무엇을 넣든 걸린다**
  〈2026-09-16 · 같은 날 두 번 걸렸다〉. 고침을 넣을 때마다 gzip 600KB 한도를 넘어
  `test-app-split.js` 가 막았고, 두 번 다 **기준선을 안 올리고 주석을 압축**해 통과시켰다.
  ★ **그 방식이 오래 못 갔다.** 여유가 1KB 안쪽이었다. ★★★ **2026-09-16 — 실제로 뗐다**
    (다섯째 갈래 `lp-crm.js` · 연락처 화면). **앱 gzip 600 → 577 KB · 여유 23 KB** 다.
    경위와 그때 깨진 검사 일곱은 §12-7.
  ★★ 그때도 **세는 범위를 함께 넓힌다** (§8 의 쪼개기 래칫) — 안 그러면 줄어든 수가
    「개선」으로 보인다.

- ★★★ **캘린더가 빈 날에 «왜 비었는지»를 함께 적는다**
  〈2026-09-15 사장님 지시: 「일정(구글캘린더) 반영이 안됨 교차검증후 개선해줘」 · 화면 사진〉.
  - **무엇이 났나.** 빈 날에 늘 **「이 날 일정이 없습니다」** 한 문장만 적었다. 그런데 그 문장이
    **셋을 덮는다** — ① 정말 없는 날 ② 구글이 연결 안 됨 ③ 연결은 됐는데 아직 못 받음.
    사장님은 ②·③ 을 보고도 **「없는 날」로 읽으시게 된다** (§4.6 의 「OCR이 나빠졌다」와 같은 자리).
  - ★ **앱이 이미 쓰는 값만 읽는다** — `lp_gcal_tok`(연결됐는가) · `lp_gcal_last`(마지막으로
    받은 시각). 새 규격을 만들지 않는다.
  - ★★ **모르는 것은 안 적는다** — 받은 적이 없으면 시각을 안 쓴다 (§4.7).
  - ★★★ **어디를 눌러야 하는지까지 적는다** (§5) — 「연결하세요」로 끝내면 그 자리를 찾아 헤매신다.
  - ★ **함께 잰 것 둘 — 둘 다 «아니었다».** 캘린더 일정이 `todos` 로 새는 자리 **0곳**이고,
    `toISOString()`(UTC)를 쓰는 **17곳은 전부 회원·팔로업·결제 쪽**이라 일정 거르는 자리에
    없다. **날짜 밀림도 할 일 오염도 원인이 아니다** — 재서 지웠다.

- ★★★ **「첫 일정을 첫 화면으로」는 «차례를 건드리지 않고» 푼다** 〈2026-09-15 사장님 지시〉.
  - 일정이 **1007px**(창 813)까지 내려가 있었다. 두 길이 있었는데 — ① 「이어서 중요한 일」을
    미팅 아래로 내리기 ② 위쪽 줄이기 — **②를 골랐다.** ①은 사장님이 정하신 차례
    (1.날씨 2.할일 3.일정)를 깨기 때문이다 (§12-2 — 앞 지시가 이긴다).
  - **줄인 것 넷** — 개발 문구 줄 제거 · compact 배너 120→**100**(지침 §4.1 의 96~128 안) ·
    「이어서 중요한 일」 **접기** · 카드 여백. 결과 **802px**.
  - ★★ **여유가 11px 뿐이다.** 위에 한 줄만 더 붙어도 다시 밀린다 — 그래서 그 칸을 **경고가
    아니라 막는 칸**으로 되돌렸다(`superseded` 를 풀었다). 밀리면 그 자리에서 빨개진다.
  - ★ **접은 것을 기억하지 않는다** — 기억하면 다음 날 펼쳐진 채로 열려 이 고침이 사라진다.
  - ★★★ **개발 문구를 지우는 것이 가장 값쌌다** — 「한줄요약·GPS날씨·캘린더(미팅준비)…(무료 AI)」는
    **이 화면이 무엇을 읽어 만들어졌는지**를 적은 줄이라 사장님이 하실 일이 없었다
    (지침 v2.0 §2.2 ⑥). **값이 사라지지 않는다** — 날씨·TOP3·연락처는 아래 카드로 각각 보인다.

- ★★ **크기 한도에 걸리면 «기준선을 올리지 않는다» — 내가 쓴 주석부터 줄인다**
  〈2026-09-15 · 실측〉. 위 고침들을 넣으니 앱이 **gzip 601KB** 로 한도(600)를 넘어
  `test-app-split.js` 가 막았다. **검사가 옳다.** 한도를 올리는 대신 **내가 새로 넣은 긴 주석
  다섯을 압축**해 통과시켰다 — 경위는 남기되 되풀이되는 설명을 접었다.
  ★ 주석은 이 저장소의 자산이지만 **한도를 먹는 것도 주석**이다. 새 규칙을 적을 때
    「왜」는 남기고 「어떻게」는 코드가 말하게 둔다.
- ★★★ **일정 충돌은 «잰 값»으로 가르고, 아는 데까지만 말한다** 〈2026-09-15 · 지침 v2.0 §6.6 ·
  크론 사양 §3 의 「30분 미만 간격 ⚠️ 충돌 경고」 — 앞 판은 **0곳**이었다〉.
  - **셋으로 가른다** — 같은 시각(0분) · 빠듯함(30분 미만) · 넉넉함. 뭉뚱그리면
    「같은 시각에 두 건」이 「1분 뒤」와 같은 말로 읽힌다.
  - ★★★ **「늦습니다」라고 적지 않는다.** 이 앱은 아직 «이동에 몇 분 걸리는지»를 모른다 —
    아는 것은 **일정 사이가 몇 분인가** 하나뿐이다. **「빠듯하다」까지가 잰 값이고
    「늦는다」는 지어낸 값**이다 (§4.6). 소요시간 출처가 생기는 날 그 말을 바꾼다.
  - ★ **시각을 못 읽거나 차례가 거꾸로면 그 줄을 아예 안 그린다.** 표본에서 실제로 그 자리를
    밟았다 — 셋째 일정을 잘못 끼워 넣었더니 그 짝이 조용히 안 그려졌고, **그것이 옳은 동작**이다.
  - ★★ **경고에 이모지를 안 쓴다** (00-헌법 16조 · 1단계 시안 대기). 래칫이 `⚠` 둘을 잡았고,
    **기준선을 올리지 않고** 글자와 색으로 바꿨다 (§8 「검사를 약하게 고치지 않는다」).
    ★ 그러면 뜻을 나르는 것이 **색뿐**이므로, 검사가 **색이 실제로 다른지까지** 잰다 —
      「빠듯하다는 글자가 있는가」로는 회색으로 칠해 놓아도 통과한다.
  - ★★ **「그 짝에만 붙었는가」를 잰다.** 전부에 붙으면 경고가 아니라 잡음이고,
    아무 데도 안 붙으면 없는 것이다.
  - ★ **표본에 빠듯한 짝을 넣었다** — 없으면 그 갈래를 영영 못 잰다
    (「표본이 거짓말을 하면 잡히는 것도 거짓이다」와 같은 결).
  - 재는 자리: `linkpilot-platform` 의 `test-brief-ux.js` **조건 16**.
- ★★★ **자동발송은 «지웠다» — 끈 것이 아니다** 〈2026-09-15 사장님 지시: 「자동발송 기능 삭제」 ·
  화면 사진에 「자동발송 켜져 있음」 딱지를 빨갛게 두르시고 「자동발송 삭제」〉.
  - **왜 규칙으로 적나.** 같은 자리가 **두 번째**다. 명언 카드 쪽은 앞서 같은 요청을 받고
    `useEffect(()=>{return; …})` 로 **첫 줄에서 돌아서기만** 했고, 그 주석조차
    「매일 자동발송 기능 삭제(요청)」였다. **지우라는 요청에 끄기만 한 것**이고,
    그 `return;` 한 줄을 누가 빼는 순간 되살아난다 (§6-2-6 「꺼 두기는 안 쓴다가 아니다」).
  - **다섯 조각을 함께 뺀다** — 시계(타이머) · 상태 · 화면 딱지·설정 줄 · 보내는 창구
    (`sendBriefKakao`) · 옛 벌 생성기(`callMorningBriefing`). ★ **한 조각만 남아도 글은 나간다** —
    딱지만 지우면 화면에는 아무것도 없는데 **카톡은 계속 온다.**
  - ★★ **옛 벌 프롬프트(`MORNING_BRIEF_SYSTEM`)는 일부러 남겼다.** 부르는 자리는 0곳이지만
    그 글이 `test-brief-sections.js` 가 섹션 목록 넷을 대 보는 **재는 기준**이다 — 지우면
    그 칸이 아무것도 못 재게 된다. 내리는 것은 위 「옛 벌 내리기」의 몫이고, 그때 검사도 함께 옮긴다.
    ★ **남긴 것을 통과로 적지 않는다** — `test-brief-v2.js` 가 그 사실을 **대기**로 적는다.
  - 재는 자리: `linkpilot-platform` 의 `test-brief-v2.js` — 다섯 조각이 전부 없는지 · 화면 글자에
    「자동발송」이 안 남았는지를 센다.
- **개수를 줄인다 — 「전부 보이는 것」이 곧 「고를 수 있다」는 아니다.** 할 일 **TOP 3**〈§8〉은
  새 벌이 `코드선정_TOP3` 로 이미 지킨다. 남은 것은 **프로젝트 최대 3건**〈§9.1〉 —
  옛 벌이 단계별 10건씩을 나열한다. ★ §6-3 ⑥ 과 **같은 규칙**이다.
- **사용자 화면에 관리자·테스트·판·JSON·내부 오류를 안 남긴다** 〈§2.2 ⑥ · §17.7〉.
  ★ 다만 **판 지문(`판 xxxxxxxx`)은 예외가 아니라 «다른 것»이다** — 그것은 M-25 가 만든
  장치이고 사장님이 사진 한 장으로 판을 가리시는 유일한 길이다. 지침이 말하는 것은
  **관제·테스트 화면과 오류 원문**이다. 이 구분을 안 적으면 다음 사람이 지문을 지운다.

### 12-1. 낱말이 겹치는 자리 — **셋** 〈교차검증에서 나왔다〉

겹침 자체는 사고가 아니지만 어느 뜻인지 안 적히면 조용히 틀린 것을 만든다
(§6-2-7 · §11 과 같은 결). **접두어로 부른다.**

- ★★★ **「Gate 번호」 — 여덟째 겹침이다.** §11 이 이미 `RELEASE:Gate 3`(Staging 실환경)과
  `INSIGHT:Gate 3`(콘텐츠·디자인)을 갈라 두었는데, v2.0 §20 이 **Gate 0~7** 을 또 둔다.
  거기서 **Gate 3 은 「기능 E2E」**이고 **Gate 7 이 「사용자 승인」**이다. 숫자라
  `role-guide.test.js` 에 안 걸리므로 **사람이 적는다** — `BRIEF:Gate 3`.
  **접두어 없이 「Gate 3」만 적힌 글은 §11 것으로 읽는다**(그 규칙은 그대로다).
- ★★★ **「오늘의 인사이트」 — 두 뜻이다.** v2.0 §11 은 **브리핑 안의 카드 둘**
  (업무 인사이트 + 소중한 사람에게 보낼 말)이고, §6-2-5 의 그것은 **별도 화면**이다
  (명언 카드 · SNS 인사말 · 배경 테마 113장 · 관계 아홉 · 경조사 일곱).
  ★ v2.0 이 「브리핑 전체를 가리는 대형 콘텐츠가 아니라」고 적는데, 그것을 저 **화면**에
  대고 읽으면 **사장님이 열두 번에 걸쳐 지시하신 것을 지우는 말**이 된다.
  **접두어로 부른다**: `BRIEF:오늘의 인사이트`(카드 둘) · `SCREEN:오늘의 인사이트`(화면).
- ★★ **「브리핑」 — 크론의 그것이 아니다.** §1·§3 의 브리핑은 **삭제된 아침 크론**의 사양이고
  (2026-08-14 범위 변경), v2.0 의 브리핑은 **앱 안의 화면**(`tab: 'brief'`)이다.
  §3 의 13섹션과 v2.0 의 9섹션을 견주면 안 된다 — **다른 물건**이다.

### 12-2. 앞 지시가 이기는 자리 — **둘**

지침이 새것이라고 앞 지시를 덮으면, 사장님이 따로 적어 주신 것이 **조용히 사라진다**
(§6-2-5 의 「테마·기념일 지침과 부딪히는 자리 셋」과 같은 잣대).

- **「카카오톡으로 보내기」 단추**〈v2.0 §11.2〉 → **`[SNS 공유]` 로 둔다.**
  사장님이 2026-09-12 에 「[지금공유. 카톡.SNS] → **[SNS 공유]** · 카톡 인사말 → **SNS 인사말**」
  로 이름을 정하셨다 (§6-2-5). 그리고 **카카오톡은 친구에게 바로 보내는 공개 창구가 없다** —
  되는 것은 복사와 공유 화면뿐이라, 그 이름의 단추를 만들면 **안 되는 길을 되는 척 그리는 것**이다 (§8).
  ★ 「자동발송 금지」는 **이미 지켜지고 있고 더 엄하다** — 사람이 공유 화면을 눌러야 나간다.
- **섹션 제목의 이모지**〈v2.0 §17.7 은 이모지를 안 다루지만 §2.2 ⑥ 이 「깔끔한 화면」을 말한다〉
  → **혼자 판단해 아이콘으로 바꾸지 않는다.** `linkpilot-master/00-헌법.md` 16조와
  1단계 로고·아이콘 지시서가 정한다 (§6-3 — **1단계 납품 대기**, 혼용 금지).
  지금 할 수 있는 것은 **새로 만드는 칸에 이모지를 안 넣는 것** 하나뿐이다.

### 12-4. **401 을 한 덩어리로 다루지 않는다 — 「다시 하면 낫는 것」만 다시 시킨다**
〈2026-09-16 사장님 지시: 「앞전에는 잘 사용됨 · 1.매번 접속창으로 팝업로그 ·
 2.일정반영 안됨, 끝내 오류 · 3.잘이용하던 캘린더 과거 교차검증」〉

- **무엇이 났나.** 앱은 `sync.php` 가 401 을 주면 **무조건** 토큰을 지우고 로그인 화면으로
  보냈다. 그런데 401 에는 **할 일이 정반대인 넷**이 숨어 있다 —
  ⓐ 기한 지남(다시 로그인하면 낫는다) ⓑ **서버가 못 알아봄**(다시 로그인해도 같다) ⓒ 중간에서
  **헤더가 떨어짐**(멀쩡한 토큰을 지워 버린다) ⓓ 서버에 못 닿은 채 로그인함(또 그렇게 될 뿐이다).
  뒤의 셋에서 로그인 화면으로 보내면 **열 때마다 되풀이된다** — 그것이 「매번 접속창」이다.
  ★ §4.6 의 「기다리면 낫는 것과 기다려도 안 낫는 것을 갈라 다룬다」와 **같은 규칙**이다.
    그때는 기다림이 버려졌고, 이번엔 **사람의 손이** 버려진다.
- ★★★ **서버가 넷을 갈라 말해야 앱이 갈라 다룰 수 있다.** 앞 판은 무엇이 막았든
  `reason:'no-token'` 이라는 **고정 글자**였다 — §6-2-1 의 `exchange-failed` · §4.6 의
  「(사유: null)」과 같은 결이다. **고정 글자는 사유가 아니다.**
- ★★★ **한 열쇠가 두 기능을 여는데 재는 창구는 한쪽만 쟀다.** 같은 세션 토큰이
  `sync.php`(자료)와 `gcal-oauth.php`(구글 캘린더)를 **둘 다** 연다. 그래서 토큰이 안 먹으면
  **로그인이 풀리고 일정이 안 오는 것이 한 뿌리**인데, 진단 창구는 **바깥 길(curl·DNS)만**
  재고 세션을 안 쟀다 — 화면이 늘 방화벽을 가리켰다 (§4.6 과 같은 결).
  ★ 열쇠를 새 곳에 쓰면 **재는 창구도 그 자리를 함께 재게** 넓힌다.
- ★★ **삼킨 401 은 「없는 것」이 된다.** 캘린더의 「내 계정이 연동됐나」 물음이 401 을
  `catch` 로 통째로 삼켜 **아무 일도 안 일어났고**, 한 번만 묻는 장치 때문에 **그 한 번으로
  끝났다.** 구글도 열쇠도 멀쩡한데 일정만 영영 안 온다. ★ **못 물어본 것은 「연동 안 됨」이
  아니다**(§6-2-4) — 그러니 「안 물어본 것」으로 치고 다음에 다시 묻는다.
- ★★★ **「열쇠가 매 요청마다 새로 만들어지는 것」은 모든 검사를 초록으로 통과한다.**
  서명 열쇠 파일을 못 쓰는 자리에서는 `lp_secret()` 이 요청마다 새 값을 낸다 — 발급받은
  토큰이 **다음 요청에서 곧바로 무효**가 되고, 로그인·캘린더가 함께 죽는다. 그런데
  「토큰 없이 부르면 401 인가」·「login.php 가 사는가」는 **그 상태에서도 전부 통과**한다.
  ★ 그래서 배포가 **NAS 안에서** 그 열쇠가 남아 있는지 직접 묻는다. **못 물으면 「못 쟀다」**다.
- ★ **화면에 머무르게 하는 것이 「조용히 두는 것」이면 안 된다.** 안 낫는 갈래는 로그인으로
  안 보내는 대신 **무엇이 막는지 한 줄**을 띄우고, **누를 자리**([연결 진단])를 함께 준다 (§5).
- 재는 자리: `linkpilot-platform` 의 `test-session-401.js` (교차검증 **102번째 칸**) —
  서버 갈래를 **PHP 로 돌려서**, 앱의 401 처리를 **네 갈래를 먹여 돌려서** 잰다
  (「if 문이 있는가」는 아무것도 안 재는 것이다). 다섯 칸 전부 사보타주로 확인했다.

### 12-5. **「호출당 기다림」을 고쳐도 「오래 걸림」은 안 풀린다 — 총 «예산»이 있어야 한다**
〈2026-09-16 사장님 지시: 「분석이 오래걸리는 버그를 교차검증후 개선해줘」 ·
 「통화음성 파일 여러개 선택 가능하도록하여 동시 여러개 파일이 종합분석이 가능하도록 만들어줘」〉

- **무엇이 났나.** §4.6 의 앞 고침(S-65)이 통화 분석의 **호출당** 기다림을 25초 → 150초로
  옳게 늘렸다. 그런데 고리가 **모델 6 × 열쇠 N** 이고, 끊긴 호출(`AbortError`)이 `continue` 라
  **다음 열쇠로 넘어가 또 150초를 기다렸다.** 열쇠 15개면 한 모델에서만 **37분**이다.
- ★★★ **끊김은 다른 열쇠로 낫지 않는다.** 파일이 긴 것이지 열쇠가 막힌 것이 아니다 —
  앞 판은 그것을 «열쇠 문제»처럼 다뤘다. §4.6 이 가른 둘(기다리면 낫는 것 · 안 낫는 것)에
  **셋째**가 있다: **다른 열쇠로 낫지 않는 것.** 그때는 그 모델을 접고 다음 모델로 간다.
- ★★ **좋은 고침이 다른 자리에서 시간을 늘릴 수 있다.** S-65 가 「끊긴 것을 나쁜 모델로
  안 센다」를 옳게 고쳤는데, 모델을 안 끄니 **고리가 끝까지 돌게** 됐다. 한 자리를 고치면
  **그 값이 흘러가는 자리**를 함께 본다.
- ★★★ **한 일에 «총 예산»을 둔다.** 호출당 기다림만으로는 전체 시간이 안 잡힌다.
  예산을 넘으면 멈추고 **사람 말**로 적는다 — 「한도 초과」로 적으면 사장님이 **열쇠를
  보러 가신다.** 「정해 둔 시간 안에 안 끝났습니다 — 나눠 올리시면 빨리 끝납니다」다.
- ★★ **여러 개를 받을 때 못 낫는 오류는 «장마다» 묻지 않는다** — §4.6 의 명함 규칙과
  **같은 것**이다. 판정식은 **한 벌**을 쓴다(앱의 `lpAiFatal` 을 갈래가 읽는다) —
  두 벌이면 한쪽이 옛말을 한다 (§8-1).
- ★★★ **여러 개를 고르셨는데 하나만 하는 것은 «오류 없는 고장»이다.** 고르는 칸에
  `multiple` 이 없고 `files[0]` 만 읽으면 **말없이 하나만** 분석한다. 낱개 기록은 그대로
  남기고 그 위에 **종합**을 더한다 — 종합만 남기면 어느 통화에서 나온 말인지 사라진다.
  그리고 **몇 개 중 몇 개가 됐는지** 적는다 (§4.7).
- ★★★ **낱말로 세면 옳은 코드가 빨개진다.** 「`files[0]` 이 있는가」로 쟀더니 **첫 파일의
  날짜·이름을 쓰는** 옳은 코드가 걸렸다. 재려던 성질은 「고른 것을 전부 분석하는가」이므로
  **돌려서** 잰다 — 약하게 고친 것이 아니라 **재는 자리를 옮긴 것**이다 (§6-2-5 와 같은 잣대).
- ★★ **고리에 새 의존을 넣으면 «그 고리를 잘라 돌리는 검사»가 거짓으로 빨개진다.**
  `test-card-batch-quota.js` 가 그 고리만 잘라 `new Function` 으로 돌리는데, 예산 둘이
  밖에 있어 **호출 0회**가 됐고 「429 처리가 깨졌다」로 읽혔다. **없던 인자만 채운다** —
  재려던 성질은 그대로다.
- ★ **앱이 gzip 한도(600KB)에 붙어 있다.** 기준선을 올리지 않고 둘을 했다 — 선언만 되고
  아무도 안 읽는 덩어리 일곱을 지우고, **부르는 자리가 0곳**인 옛 벌 프롬프트를 갈래로 옮겼다.
  ★★ 옮길 때 **그 글을 읽는 검사도 앱+갈래를 읽게 넓힌다** — 안 넓히면 그 칸이 **조용히
  아무것도 못 재게** 된다 (§8 · S-56 과 같은 규칙).
  ★★★ **다음에 또 부딪히면 그때는 갈래를 하나 더 떼야 한다** — 그렇게 했다(§12-7 · 여유 23 KB).
- 재는 자리: `linkpilot-platform` 의 `test-voice-multi.js` (교차검증 **103번째 칸**) —
  `onPick` 과 AI 호출을 **둘 다 돌려서** 잰다. 사보타주 셋으로 확인했다
  (하나만 분석 · 장마다 다시 부름 · 끊겨도 열쇠를 돎 → 호출 6회가 39회).

### 12-6. **통화 카드 — 「요약」이 요약이 아니었고, 접힘이 «안 접혔다»**
〈2026-09-16 사장님 지시: 「통화분석은 할일밑에 이동배치해줘 · 통화분석은 요약으로 ·
 해드라인 표기 클릭하면 아래로 요약본 노출 · [확인하러가기] 탭은 전화통화분석 섹션으로 이동」〉

- **차례는 «나중 지시»가 이긴다.** 통화를 **할 일 바로 밑**(일정 앞)으로 올렸다 —
  09-15 의 번호(3.일정 4.통화)와 부딪히지만 이번 것이 나중이다. ★ **두 읽기가 정반대라
  고르지 않고 여쭈었다** (§4.9). ★★ **자리만 옮기고 안은 한 글자도 안 고쳤다.**
  ★★★ 그 결과 첫 일정이 첫 화면에서 밀렸다(802 → 1032px) — 그 칸은 **`superseded`** 로
  되돌린다(재기는 계속하고 실패로 안 센다). **지시의 결과를 실패로도 삭제로도 적지 않는다.**
- ★★★ **「요약」이 요약이 아니었다.** 분석 원문(11개 절짜리 보고서)을 **앞에서 140자 자른 것**을
  `요약` 이라 불렀다 — 잘린 앞머리는 대개 **제목과 머리말**이라 화면에 녹취 토막처럼 보인다.
  고침은 **첫 절의 내용만 뽑는 것**이고, **못 뽑으면 «못 뽑았다»고 함께 돌려준다** —
  화면이 그때 「요약이 아닙니다」를 적는다 (§8 — 뽑은 척 안 한다).
  ★ **화면 검사가 그 뽑기를 못 지나간다** — 표본을 캐시에 곧장 넣기 때문이다. 그 함수를
  **꺼내 돌려서** 따로 잰다. 「화면이 초록이니 됐다」로 넘어가면 이 갈래가 통째로 안 재진다.
- ★★★ **닫힌 `details` 가 «안 접혔다»** 〈실측: `open=false` 인데 자식이 312×63〉.
  브라우저 기본(UA 스타일)은 **작성자 스타일보다 약하다** — 문서에 `*{...}` 같은 규칙이 있으면
  **조용히 진다.** 그러니 **접힘을 우리가 적는다.** 안 적으면 「눌러서 펼친다」가 **아무 일도
  안 하는 칸**이 된다. ★ 「그 요소가 있는가」로는 못 잡는다 — **눌러서 달라지는가**를 잰다.
- ★★★ **차례를 재던 칸이 「통화」를 «한 번도 안 재고» 초록이었다.** 머리글 목록에 그 이름이
  없어 「자료가 없어 안 그려진 절」로 건너뛰었다 — 칸의 제목은 「… → 통화 → …」인데
  **정작 그 절을 안 봤다** (§8 — 눈이 먼 채로 초록). **사보타주가 안 잡혀서 드러났다.**
  ★ 그래서 **「이 칸이 실제로 몇 개를 찾았는가」를 함께 잰다** — 너무 적으면 빨갛게 끝낸다.
- ★★ **「없어서 못 만든다」는 주석을 만나면 «재 보고» 고친다.** 옛 주석의 「탭을 바꿀 수단을
  안 받는다」는 **옳았다** — 직접 불렀더니 미정의 식별자 게이트가 그 자리에서 빨개졌다.
  내가 「그 이유가 틀렸다」고 적었다가 잡혔다. **창구 하나를 내고 아는 값만 받게** 했다.
  ★ 사장님이 말씀하신 단추가 **앱에 한 곳도 없을 수 있다** — 그때는 **「못 쟀다」고 적고 여쭌다.**
- ★★★ **같은 검사의 창이 «세 번» 어긋났다.** 글자 모양을 박아 둔 칸은 코드가 조금만
  움직여도 **고침이 옳은데 빨개진다.** 창을 **덩어리(그 함수)**로 잡아 성질만 본다 (§6-2-5).
- ★ **앱 gzip 한도에 세 번 부딪혔다.** 기준선을 안 올리고 셋으로 넘겼다 — 주석 압축 ·
  같은 글을 세 번 뽑던 것을 한 번으로 · **선언만 되고 아무도 안 읽는 덩어리 여섯**(1,293자) 제거.
  ★★★ **다음에 또 부딪히면 갈래를 떼는 것 말고 길이 없다.** → 뗐다 (§12-7).
- 재는 자리: `linkpilot-platform` 의 `test-brief-ux.js` **조건 15 · 22~26** ·
  `test-brief-v2.js`(요약 뽑기를 돌려서) · `test-call-summary.js`. 사보타주로 전부 확인했다.
  경위: 저쪽 MEMORY.md **S-72**.

### 12-7. **갈래를 떼는 일은 「파일을 옮기는 일」이 아니다 — 재는 자리가 일곱 곳 눈이 먼다**
〈2026-09-16 사장님: 「진행해」 — 내가 올린 권고 ①(갈래를 하나 더 뗀다)을 고르셨다〉

- **왜 했나.** 앱이 gzip 600 KB 한도에 **하루에 세 번** 부딪혔고, 그때마다 기준선을 안 올리고
  주석을 압축해 넘겼다. **더 지울 것이 없었다** — 다음 기능은 못 들어간다.
  다섯째 갈래로 **연락처 화면(`CRMView`)**을 뗐다: `lp-crm.js` · 83,169 바이트 ·
  **앱 gzip 600 → 577 KB · 여유 23 KB**.
- ★★★ **떼고 나서 검사 일곱이 «기능은 멀쩡한데» 빨개졌다.** 전부 같은 고장이다 —
  **앱 하나만 읽는 자리**(§8 의 쪼개기 래칫 · S-56 과 **같은 규칙**).
  갈래 셋은 소스를 `readCanonAll()`·`readChunks()` 로 넓혔고(`test-card-batch-quota.js` ·
  `test-card-ai-fatal.js` · `test-card-queue-persist.js`), 기준선 셋은 **파일마다** 한 줄을
  더했다(`test-icon-spec.js` 6 · `test-contrast.js` 2 · `test-one-table.js` 0).
  ★ **그 수를 「늘었다」로 적지 않는다** — 앱 쪽이 정확히 그만큼 줄었다(획 두께 69 → 63,
    대비 미달 16 → 14). **옮긴 것이지 새로 생긴 것이 아니다.**
  ★★ 그래도 **앱 기준선은 낮춘다.** 안 낮추면 그만큼 새것이 들어와도 안 걸린다 —
    파일마다 적는 이유가 그것이다.
- ★★★ **그리고 「줄었다」가 «지운 것»인지 «옮긴 것»인지 그때마다 가른다.** 이모지 래칫은
  앱+갈래를 함께 세는데 150 → 149 가 나왔다 — 갈래로 옮긴 것은 이 수를 안 줄이므로
  **정말 하나가 없어진 것**이다(죽은 덩어리를 지울 때 딸려 나갔다). 그래서 낮췄다.
- ★★ **그려서 재는 검사는 «가짜 서버가 갈래를 내주는지»부터 본다.** `test-card-shelf-render.js`
  가 그 자리에서 **「아무것도 안 쟀다」**로 끝났다 — 연락처 화면이 `lp-crm.js` 에 사는데
  서버가 404 를 줘서 화면이 안 열렸다. `gate-target.js` 의 `serveChunk()` 한 줄이면 된다.
  ★ **빨갛게 안 끝나고 「못 쟀다」로 끝난 것이 옳았다** — 초록이었으면 못 잡았다 (§8).
- ★★★ **화면이 그려진 것을 재는 낱말은 «자료가 없어도 나오는 것»으로 고른다.** 검사표에
  「명함」을 적었다가 빨개졌는데, 그 낱말은 **연락처가 있을 때만** 그려진다. 재려던 성질
  (그 갈래 화면이 실제로 그려졌는가)은 그대로 두고 **세는 자리를 옮겼다** — 「인맥관리」+
  「스코어순」(앱 본체에 0회라 갈래가 붙어야만 나온다). 사보타주로 확인했다.
- ★★★ **파이썬과 자바스크립트는 글자 자리를 다르게 센다.** 파이썬으로 잰 자리를 자바스크립트
  쪽 자리로 알고 잘랐더니 **앞 23자가 잘린 채** 갈래 파일이 만들어졌다(이모지가 섞이면 JS 는
  한 글자를 둘로 센다). **자르는 자리는 자른 쪽에서 다시 잰다.**
- ★ **이름이 적히는 네 곳은 그대로다** — `gate-target.js` 의 `CHUNKS` · 배포 목록
  (`deploy-filestation.py`) · 서비스워커(`sw.js`) · 검사표(`test-app-split.js` 의 `PARTS`).
  **합치지 않고 같은지 잰다** (S-56).
- 재는 자리: `linkpilot-platform` 의 `test-app-split.js` (**68칸**) · 경위는 저쪽 MEMORY.md **S-73**.

### 12-8. **「오래 걸린다」의 원인은 대개 AI 가 아니다 — «있는 것부터 먼저 그린다» · «기다림에 상한을 둔다»**
〈2026-09-16 사장님 지시: 「오늘의 브리핑 너무 로딩이 오래걸림 · 개선책을 만들어줘」 ·
 「명언생성이 너무 오래걸림 · 개선책을 만들어줘 · 교차검증해줘」 · 화면 사진 둘〉

- **둘 다 AI 가 느린 것이 아니었다.** 사진에는 「만드는 중…」과 「명언 큐레이션 중…」만 떠 있었고,
  실제로 시간을 먹는 자리는 **AI 앞**과 **화면을 그리는 차례**에 있었다.
- ★★★ **「먼저 보여 드립니다」가 «나중에»였다.** 브리핑은 `sBrief2()` 를 **맨 끝에 한 번만**
  불렀다. 코드가 고른 일정·할일·날씨가 **이미 준비돼 있는데도** 뉴스(≤16초)와 AI(≤45초)가
  끝날 때까지 화면이 빈 채였다. 그런데 화면 글은 「39초 안에 안 되면 일정·우선업무만 **먼저**
  보여 드립니다」였다 — **글이 사실과 반대**다 (§4.6 「원인을 사람 말로 적는다」와 같은 결).
  ★ 고침은 **차례**다: 코드가 고른 것이 준비되는 «즉시» 한 번 그리고, AI 가 오면 덧그린다.
  ★★ **첫 판은 캐시에 안 넣는다** — 넣으면 다음에 열 때 **반쪽(AI 없는 것)**이 오늘 것으로
    읽힌다. 「그렸다」와 「그것이 오늘 판이다」는 다른 사실이다.
- ★★★ **시간 상한 없는 `fetch` 는 화면을 통째로 세운다.** 명언은 AI 를 부르기 **전에**
  `quote.php` 를 **상한 없이 두 번** 기다렸다. 실측으로 앱의 `fetch` **70곳 중 59곳**에
  상한이 없었다 — 그중 셋(날씨·위치이름·열쇠)이 **브리핑 첫 단계**에 있었다.
  ★ **59곳을 한 번에 고치지 않는다.** 창구(`lpTFetch`)를 한 곳에 두고 **지금 아픈 길부터**
    옮기고, 남은 수를 **파일마다 숫자로 적어** 래칫이 막는다 (§6-2-6 의 46개와 같은 방식).
  ★★ **값은 한 벌이다** — 8초. 뉴스 호출이 이미 쓰던 값이라 새 규격을 안 만든다 (§5).
  ★★★ **끊긴 것을 「없다」로 삼키지 않는다.** 던져 주어야 부르는 쪽이 「못 받았다」와
    「값이 없다」를 가른다 (§12-4 와 같은 결).
- ★★★ **예산이 «두 벌»로 세지고 있었다.** 명언의 중복회피 재시도가 `Date.now()`(재시도 시작)로
  예산을 재서, 앞에서 40초를 썼어도 **거기서 45초를 더** 쓸 수 있었다. **예산은 「이 일 전체」에
  하나다** (§12-5 와 같은 규칙) — 부르는 쪽의 시작 시각을 받아 그것으로 잰다.
- ★★ **재는 법: 「낱말이 있는가」로는 아무것도 안 잰다.**
  · **차례를 잰다** — 첫 그리기가 AI 호출보다 «앞»에 있는가(위치). 변수 이름이 바뀌어도 남는다.
  · **돌려서 잰다** — 상한 창구를 소스에서 뽑아 **가짜 fetch 로 실제 실행**해 끊는지 본다.
  · **파일마다 센다** — 합계 하나면 갈래로 옮기는 것만으로 줄어 보인다 (§8 의 쪼개기 래칫).
- 재는 자리: `linkpilot-platform` 의 `test-load-first.js` (교차검증 **104번째 칸**) —
  다섯 사보타주로 확인했다. 경위는 저쪽 MEMORY.md **S-74**.

### 12-9. **높이가 모자라면 «차례»가 아니라 «높이»를 고친다**
〈2026-09-16 · 권고안 ②를 사장님이 「진행해」로 고르셨다〉

- **무엇이 문제였나.** 통화 칸을 할 일 바로 밑으로 올리라는 지시(§12-6)를 따르자
  첫 일정이 **1032px**(창 813)까지 밀렸다. 지침 v2.0 §3.1 ④ 와 부딪히는데,
  **차례는 사장님이 정하신 것**이라 못 건드린다 (§12-2 와 같은 잣대).
- ★ **먼저 «구간»을 잰다 — 추측으로 깎지 않는다.** 실측: 날씨 138 · 오늘먼저 108 ·
  지금할일 290 · **통화 230**px. 가장 큰 자리가 통화였고, 그것을 접는 것이
  **지시를 더 따르는 것**이기도 했다(「해드라인 표기 클릭하면 아래로」 — §12-6).
- ★★ **접어도 «값»은 보여야 한다** — 머리글 옆에 몇 건인지 적는다. 안 적으면
  「눌러 볼 이유」가 없어 아무도 안 연다 (§6-3 ⑥).
- ★★★ **펼침을 기억하지 않는다** — 기억하면 다음 날 펴진 채로 열려 이 고침이 조용히 사라진다.
- ★★★ **접으면 «그 안을 재던 검사»가 눈이 먼다.** 조건 둘이 그 자리에서 0 으로 읽었다 —
  기능은 멀쩡한데. 검사가 **먼저 펴고** 재도록 고쳤다: 재려던 성질(눌러서 자세히가 나오는가)은
  그대로 두고 **재는 자리만 옮긴 것**이다 (§6-2-5 의 잣대).
- ★★ **되찾았으면 `superseded` 를 푼다.** 실측 **1032 → 803px · 여유 10px** 이라
  **막는 칸**으로 되돌렸다 — 여유가 10px 뿐이라 경고로 두면 다시 밀려도 조용해진다.
  ★ 그때 다시 `superseded` 로 두는 것도 정당한 답이다. 다만 **까닭을 적는다.**
- 재는 자리: `test-brief-ux.js` **조건 19**(사보타주: 카드를 펴 두면 998px 로 빨개진다).

### 12-10. **「썼다」와 「남았다」는 다른 사실이다 — 저장 실패를 버리면 「매번 로그인」이 된다**
〈2026-09-16 사장님 지시: 「캘린더 일정 오류 발생 · 매번 로그인해함 · 교차검증하고 개선해줘」 ·
 화면 사진(구글 「…에 다시 로그인하는 중입니다」)〉

- **무엇이 났나.** `gcal-oauth.php` 의 `gc_write()` 는 **실패하면 `false` 를 돌려주는 함수**인데
  (`file_put_contents` 가 false 이거나 `rename` 이 실패한다), `exchange` 가 **그 반환값을 버렸다.**
  refresh_token 이 **디스크에 안 남았는데도** `ok:true · persistent:true` 가 나갔고, 앱은
  「연결 완료」로 적었다. 다시 여시면 `not-linked` → **또 구글 동의 화면**이다.
  ★ **오류가 한 줄도 안 난다.** 같은 파일의 `setup` 은 이미 500 으로 막고 있었는데
    **이 자리에만 안 댔다** (S-53 「한 칸에서 배운 것을 옆 칸에 안 대면 그대로 남는다」).
- ★★★ **실패를 돌려주는 함수는 «부르는 쪽이 본다».** 반환값을 버리는 코드는 **아무 오류도 안 낸다** —
  §6-2-6 의 「받자마자 버린 것」과 같은 결이다. 커넥터든 저장이든, **무엇을 버리는지**를 본다.
- ★★★ **「썼다」로 끝내지 않고 «되읽어» 확인한다.** `rename` 이 조용히 실패하면 쓰기는 참인데
  남지 않는다. 그리고 「남았는가」를 **메모리 값으로 적지 않는다** — 디스크에서 되읽은 값으로 적는다
  (§8 「걸었다 ≠ 올라갔다」와 같은 규칙).
- ★★ **막는 것과 재는 것은 다른 일이다.** 막기만 하면 화면에 「저장 실패」만 뜨고 **무엇을 손볼지**가
  안 보인다. 진단 창구가 **그 자리**를 재게 넓힌다(있는가·쓸 수 있는가·`.tmp` 를 만들 수 있는가·
  내 계정 것이 남아 있는가). ★ 담는 것은 **있다/없다뿐**이다 — 값은 한 글자도 안 담는다 (§2).
- ★★★ **사람 말이 «맨 앞»에 놓는 것을 조심한다.** 저장 자리가 막혔으면 구글도 세션도 멀쩡하다 —
  순서를 잘못 두면 사장님이 **방화벽을 보러 가신다** (§4.6 · §12-4 와 같은 결).
  그래서 「**방화벽이 아니라 «폴더 권한»입니다**」로 **부정으로** 적는다.
- ★ **「다시 해 보세요」로 적지 않는다** — 다시 해도 같은 갈래다 (§12-4 의 「안 낫는 것」과 같다).
- ★★ **재는 법: PHP 를 «돌린다».** 「`if (!gc_write(...))` 가 있는가」는 아무것도 안 재는 것이다.
  `gc_write`·`gc_read` 를 떼어 내 **실제로 실행**해, 못 쓰는 자리에서 `false` 를 주는지 본다.
  ★ **표본이 재려는 성질을 지켜야 한다** — 처음에 `chmod 500` 으로 막았더니 **root 로 돌 때
    권한이 안 먹어** 거짓으로 빨개졌다. **없는 폴더 아래**를 쓰면 누가 돌리든 실패한다.
- ★★★ **그리고 같은 날 «검사 셋»이 기능은 멀쩡한데 빨갰다** — 셋 다 재는 자리의 문제였다.
  · 잘라 돌리는 고리에 새 의존(`lpTFetch`)이 생겨 실패 → **없던 인자만** 채운다 (§12-5 와 같은 결).
  · **주석 제거기가 «줄 첫 글자»만 봤다** — 여러 줄 주석의 가운데가 코드로 남아, 내가 적은
    `refresh_token` 이라는 **낱말**이 「열쇠가 샌다」로 읽혔다. §8 의 「경위를 잘 적어 둘수록
    검사가 눈이 먼다」 그대로다. **공용 제거기 한 벌을 쓴다** — 두 벌로 만들면 한쪽이 옛말을 한다.
  · 위의 표본 문제.
- 재는 자리: `linkpilot-platform` 의 `test-gcal-store.js` (교차검증 **105번째 칸**) ·
  경위는 저쪽 MEMORY.md **S-75**.

### 12-11. **셋째 갈래: 「보내는 것을 바꾸면 낫는 것」** 〈2026-09-17 사장님 화면 · 실측〉

〈사장님 지시: 「교차검증하고 개선해줘」 · 명함 화면에 「무료 AI 오류: **Thinking budget is
not supported for this model.** (4/4)」 · 4장이 **1분 29초 동안 0장**〉

- **무엇이 났나.** 열쇠도 한도도 과부하도 아니었다. 우리가 **모든 모델에 `thinkingConfig` 를
  붙여 보내는데** 첫 폴백 모델(`gemma-4-31b-it`)이 그 칸을 안 받는다.
- ★★★ **§4.6 이 가른 둘로는 이것을 못 다룬다.** 「기다리면 낫는 것(한도·과부하)」도 아니고
  「기다려도 안 낫는 것(잔액·열쇠·권한)」도 아니다 — 기다려도 안 낫고 열쇠를 바꿔도 안 낫지만
  **보내는 것을 바꾸면 그 자리에서 낫는다.** **셋째 갈래**다.
  ★ 그러니 답도 다르다: 재우지도 끄지도 말고 **몸통을 고쳐 같은 모델·같은 열쇠로 곧바로 다시** 건다.
- ★★ **그 오류를 「그 외」로 보고 즉시 던지면 뒤의 길이 통째로 덮인다.** 앞 판이 그랬다 —
  뒤의 두 모델은 **한 번도 안 불렸다.** 오류 한 줄이 멀쩡한 길 둘을 가렸다.
- ★ **모델 이름으로 기억한다.** 장마다 다시 배우면 4장이면 네 번 헛걸음이다.
  그러고도 실패하면 그때는 **못 낫는 것**이므로 `lpAiFatal` 이 잡아 장마다 또 걸지 않는다.
- ★★★ **세는 잣대가 갈리면 고친 것이 안 고친 것으로 보인다.** `_aiFatal` 이 이제 표 **넷**을
  보는데 검사가 **셋**만 합쳐 재고 있었다 — 검사도 넷을 보게 맞췄다 (§4.7 의 「0 개」와 같은 결).
- ★★ **사보타주에서 한 칸이 빠져나갔다** — 「`temperature:0.4,thinkingConfig:`」라는 **앞 판의
  글자 모양**을 찾고 있어서, 그 줄을 옮긴 사보타주를 놓쳤다. **「그 자리가 조건 뒤에 있는가」**로
  바꿨다 (§8 「그 숫자를 재는 법이 재려는 것을 다 덮는가」).
- 재는 자리: `linkpilot-platform` 의 `test-ai-nothink.js` (교차검증 **107번째 칸**) ·
  경위는 저쪽 MEMORY.md **S-76**.

★★★ **그리고 「잰 값」과 「추정」이 어긋나면 «잰 값»이 이긴다** 〈2026-09-17 사장님 지시:
「촬영시 가로 세로도 구분 못함 오류버그」 · 연속촬영 7장 중 **3·5·7번이 세로로 누움**〉.

- **무엇이 났나.** 명함을 돌릴지 말지를 **글자축 판정 하나로** 정했다. 그 판정은 어두운 픽셀의
  행/열 변동계수 비라 **바탕·로고·여백에 쉽게 흔들린다.** 그래서 `'upright'` 가 나오면
  **사진이 아무리 세로여도 안 돌렸다.**
- ★ **명함의 가로세로는 실물이 정한다**(대개 91×55). 사진이 세로라는 것은 거의 언제나 눕혀 찍은
  것이고 **그것이 잰 값**이다. 축 판정은 추정이다. 어긋나면 **모양이 이긴다.**
- ★★ **반대 증상이 같은 뿌리다** — 가로 사진을 축 판정이 「누웠다」고 하면 **멀쩡한 가로를 눕혔다.**
  한 갈래만 고치면 다른 갈래가 그대로 남는다 (§4.6 「고치는 방향이 반대로 가는 것도 함께 막는다」).
- ★★★ **한쪽이 이미 본 것을 다른 쪽이 또 보면 제자리가 된다.** 앞단이 모양을 보고 90을 돌렸는데
  뒷단에 「축이 null 이고 세로면 90」이 남아 있으면 **90+90=180** 이다. 규칙을 한 벌로 모을 때는
  **옛 자리를 지웠는지**까지 센다.

★★ **「오래 걸린다」의 또 한 갈래 — 같은 것을 두 번 디코드한다** 〈같은 날 · 사장님 지시:
「명함촬영후 등록소요시간도 너무느림」〉.

- 한 장마다 원본을 960px 로 줄여 놓고, **기기 저장은 원본을 다시 받아** 600px 로 또 줄였다.
  어차피 남는 것은 600px 다 — **줄인 것을 먹이면 디코더가 받는 바이트가 13배 준다**(실측).
- ★ **고칠 자리가 둘이면 둘 다 고친다** — 새로 넣는 자리와 병합하는 자리. 한쪽만 고치면 절반만 빨라진다.
- ★★★ **화질 기준을 낮춘 것이 아님을 검사가 함께 잰다** — 서버로 가는 것은 그대로 960px 이다.
  안 재면 다음 사람이 「빨라졌으니 더 줄이자」로 간다.
- 재는 자리: `test-card-upright.js` (교차검증 **108번째 칸**) — 판정을 **떼어 내 돌려서** 재고,
  기기 저장은 **가짜 디코더를 끼워 받아 가는 바이트를 센다.** 경위는 저쪽 MEMORY.md **S-77**.

★★★ **「한글이 섞였다」와 「우리가 쓴 글이다」는 다른 사실이다** 〈2026-09-17 사장님 지시:
「명언이 생성이 안됨 · 교차검증하고 개선해줘」 · 화면이 「명언 큐레이션 중…」에 머묾〉.

- **무엇이 났나.** 오류를 사람 말로 바꾸는 자리(`lpAiSay`)의 **맨 앞줄**이
  「한글이 한 글자라도 있으면 이미 사람 말이므로 그대로 내보낸다」였다. 그런데 AI 창구가
  던지는 글은 **언제나 「무료 AI 오류: 」 + 영어 원문**이다 — 그 **접두어의 한글** 때문에
  **아래 표 다섯이 한 번도 안 쓰였다**(잔액·열쇠·한도·과부하·네트워크).
- ★★ **앞 판의 그 줄은 옳았다.** 우리가 «직접 쓴» 우리말을 낱말 하나 보고 갈아치우던
  사고를 막은 자리다. **순서만 뒤집으면 그 사고가 되살아난다** — 둘이 부딪힌다.
- ★★★ **그러니 순서가 아니라 «가르는 잣대»를 옮긴다** — 「한글이 있는가」가 아니라
  **「창구가 붙인 접두어로 시작하는가」**. 접두어가 있으면 뒤는 바깥 원문이므로 표를 본다.
  재려던 성질 **둘이 함께** 지켜진다 (§6-2-5 「재는 자리를 옮긴 것과 약하게 고친 것은 다르다」).
- ★★★ **표본이 고장의 «크기»를 정한다.** 처음엔 순수 영어(`'Failed to fetch'`)로 쟀더니
  다섯 갈래가 **전부 통과**했다. 실제 모양(`'무료 AI 오류: Failed to fetch'`)으로 바꾸니
  그 자리에서 다섯이 빨개졌다 — **접두어 하나가 표본과 실제를 갈랐다.**

★★ **실패 글은 «저절로 사라지지 않는다»** 〈같은 날〉.

- 명언 화면의 알림 **28곳**이 몇 초 뒤 스스로 지워졌는데 **실패도 같이**였다. 그 몇 초를
  놓치면 화면에 **아무것도 안 남고**, 그것이 「눌렀는데 아무 일도 안 일어났다」로 보인다 —
  사장님이 「생성이 안 됨」이라 하신 겉모습이 그것이다.
- ★ **가르는 잣대는 「사람이 할 일이 남았는가」다.** 안내(「준비됐습니다」)는 읽고 나면
  할 일이 없으니 사라져도 된다. **실패는 할 일이 남는다.**
- ★★ **28곳을 한 줄씩 고치지 않는다** — 지우는 자리가 **규칙 하나를 지나가게** 한다 (§8-1).
  그 규칙이 실패 글만 남긴다. 영영 남지는 않는다 — 다음 [생성]이 덮는다.

★★ **「도는 중」과 「멈춤」은 잰 값으로 가른다** 〈같은 날〉. 화면 글이 3초째와 40초째에
**글자 하나 안 달랐다** — 그러면 멈췄는지 가릴 길이 없다. **경과 초를 붙이고**, 정해 둔
예산을 넘기면 **그 사실을 적는다**(「오래 걸립니다」가 아니라 몇 초를 정해 뒀는지). ★ 다만
**실패 글에는 안 붙인다** — 읽는 중에 계속 바뀌면 다 못 읽는다.

★★ **떼어 내는 법을 «꼬리 글자»로 적지 않는다** 〈같은 날 · §12-6 에서 세 번 겪은 자리〉.
검사가 함수를 **마지막 `return` 한 줄의 글자 모양**으로 떼어 내고 있었다. 그 줄을 고치자
**고침이 옳은데 「못 떼어 냈다」**가 됐다. 화살표 함수는 이름 뒤 첫 `{` 가 **인자 분해**라
기존 도구를 못 쓴다 — **`gate-target.arrowFn` 한 벌**로 모았다.
★ 그리고 떼어 낸 조각을 **돌리는** 검사는, 새 의존이 생기면 「is not defined」로 던진다.
  **없던 인자만 채운다** (§12-5 와 같은 결).

- 재는 자리: `linkpilot-platform` 의 `test-quote-fail.js` (교차검증 **109번째 칸**) ·
  경위는 저쪽 MEMORY.md **S-78**.

★★★ **「남긴다」로 정하면 넷이 따라온다** 〈2026-09-17 사장님 지시:
「명언생성후 리셋 또는 삭제전까지는 유지되도록 만들어줘」〉.

지금까지 화면 상태로만 들고 있던 것을 **남기기로** 바꿀 때, 한 줄(저장)만 더하면
나머지 셋이 조용히 어긋난다. **넷을 함께 한다.**

- **① 시작할 때 읽는다.** 저장만 하고 안 읽으면 **아무것도 안 바뀐 것과 같다.**
- **② 저장하는 자리를 «한 곳»으로 모은다.** 명언 본문을 바꾸는 자리가 **넷**이었다
  (생성 · 편집 · 나의 글 · 테마 변경). 한 줄씩 고치면 **한 곳이라도 우회하는 순간
  화면과 저장이 갈린다** — 화면은 비었는데 다음에 열면 옛것이 돌아온다.
  ★ 그래서 **상태 함수를 숨기고 창구 하나**만 쓰게 한다. 네 자리가 **글자 하나 안 고치고**
    전부 그 창구를 지나간다 (§12-4 「우회하는 곳 0인가」와 같은 잣대).
- **③ 사람마다 갈린다.** 남기기로 한 이상 **한 기기를 둘이 쓰면 남의 것이 뜬다.**
  가르는 열쇠 목록에 더한다. ★ 관리자는 uid 가 빈 문자열이라 **안 움직인다** —
  그 성질이 없으면 «운영 자료 이전»이라 먼저 승인을 받아야 한다 (§10).
  ★★ **기기 한 벌이어야 하는 것까지 가르지 않는다** — 회원 명단·탭·설정은 그대로 둔다.
- **④ 지우는 길을 함께 만든다.** 사장님이 「리셋 또는 삭제」라고 하셨다 — 남기기만 하고
  지울 길이 없으면 **막다른 길**이다 (§6-3 ⑥). ★ 되돌릴 수 없는 것(고치신 문구)이
  함께 사라지면 **한 번 묻는다.**

★★★ **그리고 «지우기»도 같은 자리를 지나가야 한다.** 저장은 `lp_…__uid` 에 하는데
지우기가 **맨 열쇠**를 지우면 **지워도 다음에 열면 되살아난다.** 이 저장소의 `lsDel` 이
실제로 그랬다 — 지금 그 창구를 쓰는 여섯이 가르는 목록에 없어 값이 안 바뀌었을 뿐,
**새 열쇠를 가르는 순간 새는 자리**였다. 「가르기」를 더할 때는 **읽기·쓰기·지우기 셋**을 센다.

★★ **「남긴다」와 「날짜로 비운다」는 부딪힌다 — 지시가 이긴다.** 브리핑 캐시는 날짜가
바뀌면 스스로 비우지만(§12), 여기는 **「삭제 전까지」**가 지시다. **자정에 사라지면 그 지시를
어긴다.** 다음 사람이 「날짜로 걸러야지」로 되돌리지 않게 **검사가 그 자리를 막는다.**

★ **무엇을 남길지 가른다.** 본문(글자)은 작지만 **카드 그림(base64)은 크다** — 남기면
저장공간이 차서 **다른 자료 저장이 조용히 실패한다.** 본문이 있으면 카드는 다시 만든다.
「본문을 남긴다」와 「카드를 남긴다」는 다른 일이다.

★ **새 단추를 만들면 대비 검사가 그것을 잰다.** [지우기] 글자색이 흰 바탕 대비 **3.26** 이라
미달이 14 → 15 로 늘었다. **기준선을 안 올리고 색을 고쳤다**(5.07) — §6-3-1 의 그 잣대다.

- 재는 자리: `linkpilot-platform` 의 `test-quote-keep.js` (교차검증 **110번째 칸**) ·
  경위는 저쪽 MEMORY.md **S-79**.

### 12-3. 이미 있어서 새로 만들지 않는 것 — **넷**

- **「독립 검증 Agent 를 분리한다」**〈§15〉 → **미결로 이미 올라가 있다** (§11 · **D-202**).
  v2.0 이 그 자리에 `Validation Manager` 라는 **이름을 주었다.** 없는 것을 있는 척 적지 않는다 —
  **지금도 만드는 손과 재는 손이 같다.**
- **「사용자 승인 전 main 병합·운영배포·운영 DB 변경·외부 공개 금지」**〈§20〉 → §10 과 **같다.**
  v2.0 이 **자동 메시지 발송**과 **비용 발생 작업**을 더한다 — 그 둘은 §11(비용 한도는
  사장님 승인)과 이어 읽는다.
- **「화면과 DB 값이 다르면 정상으로 추정하지 않고 `UNVERIFIED`」**〈§13.2〉 → §10 의
  「증빙 없는 것을 정상으로 적지 않는다」와 **같은 규칙**이다.
- **모바일 화면규칙**〈§3.3〉 → 재는 자리가 이미 있다 — `npm run probe:viewport`
  (교차검증 열두째 칸 · 폰 390 · 맥 1440). ★ 다만 **새로 잴 것 셋이 생겼다**:
  **터치영역 44×44** · **본문 글자 15px** · **사이드바와 하단 내비 동시 노출 금지**.
  이 셋은 「그려지는가」로는 안 잡히고 **자리와 크기를 재야** 잡힌다 (M-72 와 같은 결).

### 12-12. **「못 물었다」와 「없다」가 같은 값이면 화면이 «멀쩡한 파일»을 고치라고 말한다**
〈2026-09-17 사장님 지시: 「교차검증 해줘」 · 화면 사진 — 판 `202609170937`〉

- **어긋나는 두 사실이 눈앞에 있었다.** 배포 로그는 「무료 AI 열쇠 **11 개**」, 사장님 화면은
  「무료 AI 열쇠가 전부 거부됐습니다 **(1/1개)**」. **둘 다 참일 수 없다** (§8 「이웃한 두 칸이
  서로 다른 말을 하면 그 자체가 사고 신호」). 먼저 **어느 쪽이 못 잰 것인지** 갈랐다.
- ★★★ **원인은 열쇠가 아니라 «읽는 함수»였다.** `lpFetchKey` 가 **다섯 갈래를 전부 빈 문자열
  하나**로 돌려줬다 — 로그인 없음 · **401/403(세션)** · 끊김(6초) · HTML 이 옴 · 그리고
  **서버에 정말 없음(204)**. 그래서 서버 열쇠 11개를 «못 물어본 것»이 「서버에 없다」로 세졌고,
  개인 열쇠 하나만 남아 **(1/1개)** 가 됐다.
  ★ 그리고 그 글이 「**서버의 `gemini_key.txt` 를 고쳐 주세요**」였다 — **멀쩡한 파일**이다.
    사장님은 고칠 것이 없는 자리를 보러 가신다 (§4.6 · §12-4 · §12-10 과 **같은 결**).
  ★★ 고침은 **사유를 남기는 것**이다 — `LP_KEYFETCH` 에 갈래 이름만 적는다.
    **값은 한 글자도 안 담는다**(§2). 검사가 **미끼 열쇠**를 응답에 섞어 그것을 센다.
  ★★★ **401 은 열쇠 문제가 아니다** — 같은 세션 토큰이 `key.php` 도 연다. 그래서 그 갈래는
    [내 정보] 가 아니라 **[연결 진단]** 을 가리킨다 (§12-4 가 세운 그 자리).
- ★★★ **「거부」를 「오늘 몫을 다 썼다」로 적고 있었다** 〈돌려서 재 보고 잡았다〉.
  한도는 기다리면 낫고 **거부는 기다려도 절대 안 낫는다**(§4.6 의 그 잣대). 그런데 글은
  「그대로 두시면 한도가 풀릴 때 돌아옵니다」였다 — 사장님이 **될 때까지 계속 누르시게 된다.**
  ★ **반대로 가는 것도 함께 막는다** — 소진은 여전히 「기다리면 된다」로 적는다. 검사가
    **두 갈래를 각각** 잰다 (§4.6 「못 낫는 것 여섯을 잡는가 · 낫는 것 여섯을 안 잡는가」와 같은 결).
- ★★★ **같은 판정이 두 벌이었다.** 명함 화면은 `_aiFatalSay` 로 사람 말을 적는데, 명언 화면은
  `lpAiSay` 를 쓰고 **거기엔 그 갈래가 아예 없었다.** 그래서 던져진 글
  「`Claude API 오류 (400) Your credit balance is too low…`」이 **우리말 접두어 때문에**
  §12-11 의 한글 규칙에 걸려 **영어 원문 그대로** 나갔다.
  ★ 고침은 **표를 두 벌로 만드는 것이 아니라 같은 자리를 부르는 것**이다 (§8-1).
  ★★ 그 자리의 꼬리(「명함 인식 성능 문제가 아닙니다」)는 **부르는 쪽이 정한다** — 명언
    화면에서는 틀린 말이다. 고정해 두면 화면마다 엉뚱한 말이 붙는다.
- ★★ **문자열 안의 별표(★)도 이모지 래칫이 센다** 〈같은 날 · 실측 152 vs 기준선 149〉.
  주석은 떼고 보지만 **화면에 나가는 글자**는 센다 — 맞다. **기준선을 안 올리고 글자를 바꿨다**
  (§8 「검사를 약하게 고치지 않는다」).
- ★★★ **이 자리에서는 NAS 를 못 본다** — 「서버에 11개가 실제로 있는가」는 **배포 로그**가 잰다.
  그래서 이 칸이 재는 것은 **「몇 개든 그 둘을 갈라 말하는 구조인가」**다. **못 잰 것을 통과로 적지 않는다.**

★★★ **그리고 「앱이 본 것」과 「서버 자리의 상태」는 또 다른 사실이다**
〈2026-09-17 사장님: 「권고안대로 진행해」 — 제가 올린 권장 ③〉.

- **왜 한 칸을 더 두나.** 위 고침은 앱이 **왜 못 받았는지**를 말하게 한 것이다. 그런데 그것은
  **앱이 본 것**이고, **서버 자리에 열쇠가 실제로 어떤 상태인지**는 NAS 안에서 재야 안다.
  배포 로그의 「11 개」는 **배포가 도는 순간**의 값이지 사장님이 화면을 여시는 순간의 값이 아니다.
  ★ §4.6 의 「추측하지 않는다 — «재는 창구»를 둔다」와 **같은 규칙**이다.
- **넣은 것 넷** — 파일이 있는가 · 읽히는가 · **앱 잣대로 몇 개인가** · 옛 평문이 내용을
  가진 채 남았는가. **값은 한 글자도 안 담는다**(§2) — 검사가 미끼 열쇠로 그것을 센다.
- ★★★ **사람 말 줄을 «따로» 둔다.** 기존 진단의 한 줄은 「**캘린더**를 막는 가장 앞의 것」이고
  AI 열쇠는 캘린더를 안 막는다. 한 줄로 합치면 **한쪽이 다른 쪽을 덮어 틀린 곳을 가리킨다**
  (§4.6 · §12-10 의 「사람 말이 맨 앞에 놓는 것을 조심한다」와 같은 결).
- ★★★ **세션이 막혔을 때가 이 창구의 급소다.** 그때 글은 「서버에 열쇠 N개가 **멀쩡히
  있는데** 이 기기가 **못 받고 있습니다** — **열쇠 파일 문제가 아닙니다**」다. 이것을 안 적으면
  사장님이 **고칠 것이 없는 파일**을 보러 가신다.
- ★★ **세는 잣대는 앱과 같다** — 20자 이상 · 꺾쇠 없음. `AIza` 로 재면 배포 116 의 사고가
  그대로 되살아난다 (§4.6 「세는 잣대가 갈리면 올라간 열쇠가 0 개로 적힌다」).
- ★★★ **표본이 재려던 성질을 안 지켜 사보타주 하나가 빠져나갔다** 〈실측〉. 표본 열쇠를
  **전부 `AIza` 로 시작**하게 뒀더니, 잣대를 `AIza` 로 바꿔도 **수가 같아 안 잡혔다.**
  앞가지가 다른 열쇠 둘을 섞으니 그 자리에서 빨개진다 (§8 「표본이 거짓말을 하면
  잡히는 것도 거짓이다」 — 이 저장소가 그 자리에서 또 걸렸다).
- ★ **앱이 그 줄을 그리되, 서버가 안 주면 아예 안 그린다** — 지어내지 않는다 (§4.6).
- 재는 자리: `linkpilot-platform` 의 `test-ai-keyprobe.js` (교차검증 **112번째 칸** · 12칸) —
  판정 덩어리를 **소스에서 오려 내** 진짜 파일과 함께 **PHP 로 돌린다**(베끼면 한쪽이 옛말을 한다).
  ★ PHP 가 없는 자리에서는 **「못 쟀다」**로 적는다 — 통과가 아니다 (§8). 사보타주 일곱으로 확인했다.

- 재는 자리: `linkpilot-platform` 의 `test-ai-keysrc.js` (교차검증 **111번째 칸**) —
  `lpFetchKey` 를 가짜 fetch 로 **일곱 갈래 먹여 돌리고**, 글 짓는 함수 셋을 떼어 내
  **사장님 화면에 실제로 떴던 글**을 먹인다. 사보타주 일곱으로 전부 확인했다.
  ★ `test-gemini-keypool.js` 의 한 칸도 함께 옮겼다 — **표본이 「거부」인데 기대가 「소진」의 글**이었다.
    재려던 성질(「무엇을 하면 되는지가 글에 있는가」)은 그대로 두고 **소진 표본을 따로 두었다**
    (§6-2-5 「재는 자리를 옮긴 것과 약하게 고친 것은 다르다」와 같은 잣대).


### 12-20. **배포 실패의 «넷째 갈래» — 워크플로 파일 자체가 거부된 것(`startup_failure`)**
〈2026-09-17 사장님 지시: 「교차검증 개선해줘」 · 화면에 `deploy-im.yml` 이 **1초**에 ❌〉

- **무엇이 났나.** `.github/workflows/deploy-im.yml` 이 **1초**에 죽었고 결론이
  `startup_failure` 였다 — GitHub 이 그 `.yml` 을 **읽다가 물린 것**이라
  **잡도 단계도 로그도 아예 안 만들어진다.**
- ★★★ **§12-14 의 도구가 이것을 「코드 쪽(값 1)」으로 적었다.** 그리고
  **「멈춘 단계의 로그를 열어 원인을 봅니다」**라고 말했다 — **열 로그가 없다.**
  사장님이 열면 **404** 이고, 없는 자리를 찾아 헤매신다. §12-14 가 고치려던
  바로 그 고장을 **그 도구가 다른 갈래에서 되풀이했다** (S-53 「한 칸에서 배운 것을
  옆 칸에 안 대면 그 자리에 그대로 남는다」).
- ★ **갈래가 넷이 됐다** — 0 성공·비켜남·도는 중 / 1 **돌다가** 실패(로그가 있다) /
  3 러너를 못 받음(결제) / **4 시작 전에 거부됨(로그가 없다)**. 값을 안 가르면
  **할 일이 정반대인 것들이 한 글자로 뭉개진다.**
- ★★★ **YAML 문법이 맞아도 난다.** 실측에서 그 파일은 `yaml.safe_load` 로
  **멀쩡히 파싱됐다** — GitHub 의 워크플로 **스키마**는 따로다(없는 잡을 `needs` 로
  가리킴 · `uses` 경로 · 표현식 오류 등). 그러니 **「YAML 이 맞으니 괜찮다」로
  넘어가지 않는다.** 그 사실을 글에 적어 다음 사람이 그 덫에 안 걸리게 한다.
- ★★ **사유를 못 받으면 「못 받았습니다」로 적는다.** 그 빨간 띠(「Invalid workflow
  file: line N, col M」)는 **실행 화면에만** 있고 이 자리에서는 못 읽는다.
  **무엇이 틀렸는지 지어내지 않는다** (§4.7 · §12-17 과 같은 규칙).
  ★ 받으면 그대로 싣는다 — 둘을 갈라 잰다.
- ★ **고칠 «그 파일»을 이름으로 짚는다**(`run.path`). 「워크플로가 잘못됐습니다」로
  끝내면 워크플로가 열 개인 저장소에서 **어느 것인지 안 보인다.** 경로를 못 받으면
  **「경로를 못 받았습니다」**로 적는다.
- 재는 자리: `im-agent/test/deploy-why.test.js` 세 칸(11칸) — **사장님 화면의 실제
  응답 모양**을 먹여 돌린다. 사보타주 셋으로 확인했다.
  ★ **낱말이 아니라 「시키는가」를 잰다** — 「로그를 열어 보라고 말하지 않는가」·
    「결제 자리가 아니라고 적는가」 (§4.6 의 그 잣대).

★★★ **그리고 «왜 거부됐는지»를 실제로 찾았다 — 부르는 자리는 남고 대상이 지워졌다**
〈같은 날 · 사장님이 같은 화면을 다시 주셔서 「교차검증 개선해줘」〉.

- **갈래를 말하게 한 것만으로는 안 고쳐진다.** 도구가 「워크플로 파일을 보십시오」라고
  옳게 적어도 **그 파일은 여전히 깨져 있다** — 사장님이 다시 누르시면 또 1초에 죽는다.
  ★ **「무엇이 잘못됐는지 말하는 것」과 「고치는 것」은 다른 일이다** (§8 과 같은 결).
- **원인**: `deploy-im.yml` 의 `alert` 잡이 `uses: ./.github/workflows/alert-failure.yml`
  을 부르는데 **그 파일이 없다.** D-99(`e551ca4`)가 문자 알림과 함께 그 워크플로를
  지우면서 **부르는 자리만 남겼다.** `deploy-nas.yml` 은 그때 제 `alert` 를 지웠다 —
  **§8-2 의 「같은 일을 하는 자리가 둘이면 한쪽만 고쳐진다」** 그대로다.
- ★★★ **한 달 넘게 아무도 몰랐다.** 그 워크플로는 `workflow_dispatch` 전용이라
  **사장님이 오늘 처음 누르실 때까지 한 번도 안 돌았다**(실행 번호 1).
  ★ **도는 것이 없으면 깨진 줄도 모른다** — 그러니 **누르기 전에** 검사가 잡아야 한다.
- ★★ **고침은 부르는 자리를 지우는 것이다.** 지금의 실패 통보 경로는 **GitHub 이
  실행 실패 시 보내는 메일**이고(§2), `deploy-nas.yml` 이 이미 그렇게 정해 두었다.
  ★ §2 가 「지웠다는 사실을 두 곳에 적어 둔다」고 했는데 **그 두 곳에 이 파일이 없었다** —
    이제 세 곳이다.

★★★ **교차검증을 넓혔다 — 「YAML 이 맞다」로는 이 갈래를 영영 못 잡는다.**

- **① `uses:` 가 가리키는 로컬 워크플로가 실제로 있는가.** 없으면 그 워크플로는
  **시작 전에 거부**된다. ★ 바깥 것(`owner/repo@ref`)은 여기서 못 재므로 **안 센다** —
  못 재는 것을 재는 척하지 않는다 (§8).
- **② 잡마다 `runs-on` 이나 `uses` 중 하나가 있는가.** 둘 다 없으면 같은 갈래로 죽는다.

★★★ **그리고 「고쳤다」에서 멈추지 않았다 — 배포 길을 «하나»로 줄였다**
〈2026-09-17 사장님: 「권고안대로 진행해」 — 제가 올린 권장 ②〉.

- **왜.** 이 고장은 **부르는 자리를 지우면 안 난다**가 아니라, **배포 길이 둘이라서**
  났다. 한쪽만 고쳐지는 자리는 다시 생긴다 (§6-3 ⑥ · §8-2 와 같은 결).
  `deploy-im.yml` 을 **내렸다** — `deploy-nas.yml` 하나가 남는다.
- ★★★ **내리기 전에 «없어지는 사실»부터 재고 내렸다** (§12 의 날씨 교훈과 같은 잣대).
  16단계 중 `deploy-nas` 에 없던 것은 **하나**였다:
  · `npm run check:reachable` → **옮겼다**(guard 잡 끝). `npm test` 는 `reachable.js` 의
    **함수 성질**만 잰다(`nas-guard.test.js`) — 저장소 전체를 훑는 것은 그 CLI 뿐이라,
    **안 옮겼으면 조용히 없어졌다.**
  · `npm run im:embed`(화면 만들기) → **옮길 것이 없었다.** 산출물이 저장소 원본과
    **바이트가 같다**(실측: 다른 파일 0개). 더 나오는 `manifest.json` 은 NAS 에서
    **아무도 안 읽는다** — `sync-im-flow.js` 가 보는 것은 **저쪽 저장소**의 im-flow 다.
  · 「NAS 가 v20 이다」는 **주석이 저쪽에만** 있었다 — 함께 옮겼다. 안 옮기면 다음 사람이
    러너만 v22 로 올리고, 그러면 **러너에서는 도는 코드가 NAS 에서만 죽는다.**
- ★★ **「지웠다」가 아니라 「내렸다 · 이렇게 되살린다」로 적는다** (§8 · M-52 와 같은 결).
  되살리는 법은 [`docs/자동배포-켜는-법.md`](docs/자동배포-켜는-법.md) 에 있다.
- ★★★ **목록을 손으로 적던 검사 셋을 «폴더를 훑게» 고쳤다.** `guide-paste.test.js` 가
  `['deploy-nas.yml','deploy-im.yml']` 을 글자로 박아 두어, 한쪽을 내리는 순간
  **고침이 옳은데 빨개졌다.** 표지(`NAS_SSH_HOST`)로 찾게 하니 **내려도 늘어도 맞는다** (§8-1).
  ★ **몇 개를 찾았는지 함께 잰다** — 0개면 그 칸은 **눈이 먼 것**이다.
- ★★ **새 칸 둘**: 「NAS 에 올리는 워크플로가 **정확히 하나**인가」 · 「그 길이
  `check:reachable` 을 지나가는가」. 뒤엣것은 **주석 줄을 떼고 본다** — 이 고침의 경위
  주석에 그 명령을 그대로 적었고, 안 떼면 **지워도 안 빨개진다**(§8 의 그 함정이
  **거꾸로** 온 경우다).
- ★★★ **그리고 내 사보타주가 한 번 빠져나갔다** 〈실측〉. 「표지를 못 찾는 상태」를
  만들려고 `NAS_SSH_HOST` → `NAS_SSH_HOSTZZ` 로 바꿨는데, 그 글자는 **여전히
  `NAS_SSH_HOST` 를 포함한다.** 다섯 칸이 전부 초록이었고 **검사가 옳았다** —
  틀린 것은 표본이다 (§8 「표본이 거짓말을 하면 잡히는 것도 거짓이다」).
  `ZZZ_HOST` 로 바꾸니 그 자리에서 **다섯이 빨개진다.**

★★ **권장 ③(저장소 뿌리의 사본 다섯)은 «이미 끝나 있었다»** — `df79e56` 이 지웠다.
  **내가 재지 않고 권고를 올렸다.** 권고에도 잰 값을 붙인다 (§6-3 ②).

★★★ **그리고 «그 워크플로만 읽던 Secret» 이 유령이 된다 — 검사가 그 자리에서 잡았다**
〈같은 날 · 실측 · guard 가 빨개졌다〉.

- **무엇이 났나.** `NAS_WEB_ROOT` · `NAS_ENGINE_ROOT` 를 읽는 워크플로가 `deploy-im.yml`
  **하나**였다. 내리는 순간 **아무도 안 읽는데 등록부는 「지금 등록해야 한다」**로 남았다 —
  사장님이 그 둘을 넣으시면 **아무 데도 안 쓰인다.**
- ★ **검사가 옳았다.** `secrets.test.js` 의 「① 은 실제로 워크플로가 쓰는 것뿐이다」가
  잡았다. 내가 파일만 보고 내렸으면 **등록부가 옛말을 하는 채로** 넘어갔다.
- ★★ **고친 것은 등록부다** — ① 에서 빼고, ② 의 경위에 **이름 없이** 적었다
  (그 절은 이름을 글자로 적으면 검사가 주워 반대 방향이 빨개진다).
  ★ **「이미 넣어 두셨으면 지우지 않으셔도 된다」를 함께 적는다** — 안 적으면
    사장님이 **없앨 것을 찾아 헤매신다** (§5).
- ★★★ **잣대: 워크플로를 내릴 때는 «그것만 읽던 Secret» 을 함께 센다.**
  지금 도는 `deploy-nas.yml` 은 그 자리를 **입력값(`dest`)으로 받으므로** 값이 필요 없다 —
  「없어졌다」가 아니라 **「다른 길로 이미 되고 있었다」**다. 둘을 갈라 적지 않으면
  없는 장애를 남긴다 (M-86 과 같은 결).
  ★ 처음에 파이썬으로 잴 때 **`uses` 를 잡 수준에서 안 봐서** 멀쩡한 재사용 잡을
    「빈 잡」으로 읽었다 — **재는 법이 틀리면 엉뚱한 곳을 가리킨다** (§14 의 「더 세는
    쪽으로도 틀린다」와 같은 결).
- ★★ **주석 줄을 떼고 본다.** 이 고침의 경위 주석에 **그 경로를 그대로 적었고**,
  안 떼면 그 줄이 걸려 **고침이 옳은데 빨개진다** (§8 「경위를 잘 적어 둘수록 검사가
  눈이 먼다」 — 이 저장소가 그 자리에서 또 걸릴 뻔했다).
- ★ **「찾은 것이 0개」와 「전부 멀쩡」을 갈라 적는다** — 로컬 `uses:` 가 0곳이면
  그 칸은 **눈이 먼 것**이라 그 사실을 찍는다(다만 0곳도 정상이라 실패로는 안 센다).
- 재는 자리: `im-agent/test/workflow-shape.test.js` 두 칸(6칸) — 사보타주 둘로 확인했다.

### 12-21. **화면이 «자기를 설명»하거나 «작업 도구»를 말하지 않는다**
〈2026-09-17 사장님 지시: 「플렛폼에서 작업시 스스로를 설명하거나 작업 도구를 언급하는
 대목을 찾아 모두 걷어내줘 그리고 교차검증해줘」 · 화면 사진〉

- ★★★ **가르는 잣대는 하나다 — 「이 글이 사장님께 할 일을 주는가」.**
  · **준다 → 남긴다**: 오류 원인(「열쇠가 거부됐습니다」) · 누를 자리(「내정보에서 켜세요」) ·
    **[내 정보] 의 AI 엔진 설정**(거기서 도구 이름은 «고르는 대상»이라 지우면 무엇을 넣는지
    모르게 된다 — 막다른 길 · §6-3 ⑥) · **판 지문**(M-25 가 만든 장치 · §12).
  · **안 준다 → 걷어낸다**: 자기 소개 · 엔진 자랑(「✨ Gemini OCR 활성」) ·
    보고서의 「어떻게 만들었나」 절(「위성·항공영상을 **머신러닝으로 분석해**」).
  ★ 이 구분이 없으면 §4.6·§12-4·§12-10·§12-12 가 **일부러 사람 말로 적으라 한 글**까지
    지우게 되고, 그러면 사장님이 **고칠 것이 없는 자리를 보러 가신다.**
- ★★ **출처는 남기고 도구 이름만 뺀다.** 「위성·항공영상」·「DSM/DEM」·「과거 기상」은
  §4.7 이 적으라 한 **출처**이고, 「AI·컴퓨터 비전」·「머신러닝」·「OCR」이 **도구 이름**이다.
  뭉뚱그려 지우면 근거가 사라진다.

★★★ **재는 법이 «눈이 먼» 자리 — `\uXXXX`** 〈실측〉.

이 앱은 한글을 `\uXXXX` 로 저장한 자리가 **5,466곳**이다. 그래서 **평문 `grep` 이 통째로 못 본다** —
내 첫 스캔이 60개로 셌고, 풀고 다시 세니 **85개**였다. 사장님 화면의 자기 소개 문구도 그 안에 있었다.
- ★ 잣대는 그대로다 — **「그 숫자를 재는 법이 재려는 것을 다 덮는가」** (§6-2-6 의 46 → 105).
- ★★ **`\xXX` 도 함께 푼다.** 가운뎃점이 `\xB7` 이라 안 풀면 **헛되이 빨개진다.**
  `JSON.parse` 는 `\x` 를 모르므로 **직접** 푼다.
- ★★★ **디코드가 «실제로 도는지» 검사가 스스로 잰다.** 꺼 봤더니 **초록으로 끝났다** —
  한글이 2,351 → 1,958 로 줄었는데도 「찾은 것이 0」이라 통과했다.
  **「못 찾았다」와 「없다」가 같은 값**이면 그 검사는 아무것도 안 잰다 (§12-12 의 그 고장이
  **내 검사 안에** 있었다). 이제 `\uXXXX` 가 있는데 한글이 안 늘면 **빨갛게 끝낸다.**

★★★ **190만 자를 «앞에서부터» 훑어 문자열을 세지 않는다** 〈사보타주가 빠져나가서 고쳤다〉.

정규식 하나로 리터럴을 세면 **앞쪽에서 짝이 한 번 어긋날 때 그 뒤가 통째로 밀린다** —
되살려 둔 자기 소개가 **있는데도 안 걸렸다.** 고침은 **걸릴 낱말을 먼저 찾고 그 둘레에서만
경계를 잡는 것**이다. 재려던 성질은 그대로이고 **세는 자리만 옮겼다** (§6-2-5 의 잣대).
- ★ 코드 조각은 **기호 밀도**로 거른다. 처음에 **한글 비율**로 걸렀더니
  `Gemini API 키 (여러 개는 쉼표로)` 같은 **진짜 설정 글까지** 걸러져 3 → 0 이 됐다 —
  그것은 「지운 것」이 아니라 **「안 세게 된 것」**이다 (§12-15 · §8 의 쪼개기 래칫).

★★★ **그리고 내가 «사장님이 글자까지 정해 주신 문장»을 지웠다** 〈검사가 잡았다〉.

2026-09-13 지시가 둘을 콕 집었다 — 「배너속 광고문구는 … 개인화 공유 서비스로 **변경** ·
매일 신선한 새 명언 … 문구는 오늘의 한줄생각 타이틀 밑에 **이동배치**」. 그것을 모르고
「광고조」로 보고 걷어냈더니 `test-kakao-greeting.js` **두 칸이 빨개졌다.**
- ★ **검사가 옳았다.** 없었으면 사장님이 정하신 것이 **아무 말 없이 사라졌다.**
- ★★ **되돌리고 그 둘만 예외로 적었다.** 이름을 검사에 적는 것은 「약하게 고치는 것」이
  아니다 — 재려던 성질은 **「내가 지어 넣은 자기 소개가 있는가」**이고, 그 둘은 사장님 문장이다.
  **새 자기 소개가 들어오면 여전히 빨개진다**(사보타주로 확인).
- ★★★ **이것은 「앞 지시가 이긴다」가 아니라 「두 지시가 부딪힌다」다** — 이번 것이 나중이지만
  글자까지 정해 주신 것이라 **내가 고르지 않는다** (§4.9 · §12-6). 사장님께 여쭌다.
- ★ **걷어내기로 정하시면 두 곳을 함께 고친다** — 화면과 `test-kakao-greeting.js` 의 두 칸.
  한쪽만 고치면 그 자리가 빨개진다 (§6-3 ⑥).

★★ **기준선은 파일마다 둔다** (§8 의 쪼개기 래칫) · **갈래 목록은 `gate-target.js` 의
`CHUNKS` 에서 읽는다**(손으로 안 적는다 · §8-1).

- 재는 자리: `linkpilot-platform` 의 `test-ui-self-talk.js` (교차검증 **115번째 칸**) —
  사보타주 셋으로 확인했다(새 자기 소개 · 갈래의 도구 이름 · 디코드 끄기).

★★★ **그리고 «굵게 하려고 적은 표시»가 화면에 그대로 찍힌다** 〈2026-09-17 사장님 화면 · 실측 16곳〉.

사장님 화면에 이렇게 떴다 — 「다시 누르시면 `**`다음 열쇠부터 이어서`**` 겁니다.」
**별표가 글자로 보인다.** 이 앱에는 화면 글을 굵게 그려 주는 자리가 없다(알림·오류 줄은
그냥 글자다). 고치려고 적은 표시가 **오히려 글을 더럽히고**, 사장님께는 **고장으로 읽힌다**
(§8 「안 되는 길을 되는 척 그리지 않는다」와 같은 결).

- ★ **강조는 `«…»` 로 적는다.** 새 규격을 만들지 않는다 — 화면 글에 이미 쓰이고 있었다(실측 5곳).
  `「」` 는 §5 가 **누를 단추 이름**에 쓰라고 정해 둔 자리라, 강조로 겹쳐 쓰면 둘이 안 갈린다.
- ★★ **AI 에게 주는 글(프롬프트)에서도 뺀다.** 화면 글은 아니지만 그 표시를 보여 주면
  **AI 가 그대로 따라 써서** 명언 카드에 별표가 실린다. 값이 없고 위험만 있다.
- ★★★ **메모 편집기의 [굵게] 단추는 안 센다** — 거기서 `**` 는 «넣는 물건»이다.
  코드 조각 거르개가 그 둘을 이미 걸러낸다. **따로 이름을 적지 않는다** — 이름을 적으면
  다음에 비슷한 자리가 생길 때 또 적게 되고, 그때마다 검사가 한 칸씩 무뎌진다.
- ★★★ **기준선이 0 이면 「없다」와 「못 찾았다」가 같은 값이다** (§12-12 · §12-15 와 같은 규칙).
  누가 잣대를 무디게 해도 **수가 그대로 0 이라 아무 일도 안 일어난다.** 그래서 이 칸은
  **일부러 만든 표본 둘**을 먹여 잣대가 아직 그것을 가르는지 **먼저** 잰다 — 오류 글 속 별표는
  잡고, [굵게] 단추는 안 잡는지. 무뎌지면 **실측 숫자를 보기 전에** 그 자리에서 빨개진다.

★★★ **`\xXX` 도 풀어야 한다 — 다만 «0x80 위만»** 〈같은 날 · 실측 466곳 · 한 번 깨뜨렸다〉.

§12-21 이 `\uXXXX` 를 풀게 했는데, 정본을 읽는 공용 창구(`gate-target.js` 의 `unesc`)는
여전히 `\xXX` 를 안 풀고 있었다. 가운뎃점(`·`)이 `\xB7` 로 저장되므로, **가운뎃점이 든
문구**를 찾는 검사는 **있는데도 없다고** 말한다.

- ★★★ **처음에 전부 풀었더니 교차검증이 깨졌다** 〈실측〉 — `\x3c`(`<`) 까지 풀려
  **함수 본문을 떼어 내는 검사 넷**이 「있는 것을 못 찾는다」로 빨개졌다(`lpImShell` · CSS 위험).
  ★ **가르는 잣대: 0x80 위는 «글자», 아래는 «코드 구조»다.** 재려던 것은 글자이지 구조가 아니다.
- ★ **두 벌로 만들지 않는다** — 검사마다 제 디코더를 두면 한쪽이 옛말을 한다 (§8-1).
  공용 창구 한 곳이 정본이다.
- ★★ **고치기 전에 「원래 초록이었는지」부터 쟀다.** 안 쟀으면 내가 깨뜨린 것을
  「원래 그랬다」로 적을 뻔했다 (§8 「이웃한 두 칸이 서로 다른 말을 하면 사고 신호」).

### 12-22. **NAS 에서 «필요한 칸만» 받아 오고, 받은 것을 «되돌려 보내지» 않는다**
〈2026-09-17 사장님 지시: 「전체적으로 NAS 서버로부터 정보소스를 가져오는 과정에 느리고
 무거운 상황을 교차 검증해서 개선해줘 · 최근 너무 느려졌고 무거워짐」〉

- **잰 값.** 앱이 `sync.php` 를 GET 으로 부르는 자리가 여섯인데 **넷이 자료를 통째로**
  받고 있었다. 받아서 쓰는 것은 `j.members` 한 칸(또는 `j.nda_logs` 한 칸)뿐이다.
  그 파일 크기는 **서버가 자기 주석에 적어 두었다 — 4.7MB**.
  ★ 표본으로 재 보니 부분 응답은 **137B**였다(전체 40,412B 대비 **0.3%**).
- ★★★ **더 나쁜 것은 «되돌려 보내기»였다.** 회원 등록과 명함·로고 올리기가 통째로 받은 것을
  `{...j}` 로 **그대로 다시 올렸다.** 곧 사진 한 장에 **내려받기 한 벌 + 올리기 한 벌**이 오갔다.
  ★ 그리고 명함을 등록하실수록 그 파일이 커지므로 **쓸수록 느려진다** — 사장님이
    「**최근** 너무 느려졌고 무거워짐」이라 하신 모양이 이것이다. 원인이 «최근 무엇을
    바꿨나»가 아니라 **«쌓인 양»**일 수 있다는 것을 먼저 본다.
  ★★ 되돌려 보내기는 **자료를 되살릴 수도 있다** — 받은 뒤 올리기까지 사이에 다른 기기가
    지운 것이 그대로 돌아간다 (§12-13 의 묘비와 같은 결). 느린 것만 고친 게 아니다.
- ★★ **부분 POST 는 이미 안전했다 — 서버가 그렇게 짜여 있었다.** 병합이 `$merged = $existing`
  에서 시작하고 칸마다 `array_key_exists` 가드가 있다. **앱만 그 사실을 안 쓰고 있었다** —
  §12-4 의 「장치는 이미 있었다, 배선만 없었다」와 같은 자리다. **새로 만들기 전에 이미
  있는 것부터 찾는다.**
- ★★★ **작게 주는 것과 «빠뜨리는 것»은 다른 일이다.** 부분 응답에 `ts` 를 안 실으면 앱이
  `ts.members` 없이 올리게 되고, 서버의 항목별 max 병합이 **옛 시각을 이긴 것으로 세어**
  저장이 조용히 되돌아간다. `ts` 는 작다 — **빼서 아끼는 것보다 빠뜨려 잃는 것이 크다.**
- ★ **모르는 `only` 는 거절하지 않고 전체를 준다.** 캐시된 옛 앱이 엉뚱한 값을 보내도
  화면이 죽지 않아야 한다 (같은 파일의 `pid` 하위호환과 같은 결).
- ★★★ **재는 법이 «두 번» 틀렸고, 둘 다 서버를 탓할 뻔했다** 〈실측〉.
  · `ob_start()…ob_get_clean()` 으로 받았더니 — `sync.php` 는 갈래마다 `exit` 한다.
    **그 뒤 줄이 아예 안 돌아** 파일에 «앞 실행의 응답»이 남았고, 검사는 「전체가 왔다」로 빨개졌다.
  · stdout 으로 고쳤더니 POST 가 `empty body` 였다 — **PHP CLI 는 `php://input` 을 안 준다.**
    그것을 「서버가 부분 POST 를 못 받는다」로 읽었으면 **멀쩡한 서버를 고치러 갔다.**
  ★ 답은 **내장 웹서버를 띄워 진짜 요청을 보내는 것**이다. 재려던 것은 «서버가 실제로 어떻게
    답하는가»이고 그것은 HTTP 로만 잴 수 있다 (§8 「표본이 거짓말을 하면 잡히는 것도 거짓이다」).
  ★★ **띄운 것과 받는 것은 다른 사실이다** — 서버가 뜰 때까지 기다린 뒤에 묻는다.
- ★ **여기서 NAS 의 실제 파일 크기는 못 잰다.** 이 칸이 재는 것은 **«필요한 칸만 오가는
  구조인가»**다. 실제로 얼마나 빨라졌는지는 사장님 화면이 말한다 — **못 잰 것을 통과로 적지 않는다.**
- 재는 자리: `linkpilot-platform` 의 `test-sync-partial.js` (교차검증 **117번째 칸** · 11칸) —
  앱은 소스에서, 서버는 **진짜 HTTP 로 돌려서** 잰다. 사보타주 다섯으로 확인했다
  (앱에서 부분 지정 빼기 · 되돌려 보내기 되살리기 · 서버 `only` 빼기 · `ts` 빼기 ·
  안 들어온 칸 보존 끄기).


### 12-23. **배포가 «열쇠를 한 글자도 안 싣고» 초록으로 끝났다 — 그리고 나는 그 초록을 인용했다**
〈2026-09-17 · 배포 #212 실측 · D-212〉

- **무엇이 났나.** 「Write engine keys」 가 **「써 넣은 열쇠 25개」**라고 적고 **success** 로 끝났다.
  그런데 **같은 초의 로그**에 이렇게 찍혀 있었다 — `cp: cannot open 'linkpilot.env' … Permission denied` ·
  `sh: line 6: linkpilot.env: Permission denied` · `chmod: … Operation not permitted`.
  40초 뒤 진단은 **「실제로 읽은 파일: (없다)」**였고, 지적도·OCR·둘째 읽기가 **전부 꺼짐**,
  Gemini **0/8** 이었다. 곧 **한 글자도 안 실렸다.**
  ★★★ 그리고 나는 그 초록을 근거로 사장님께 **「열쇠는 이미 들어 있습니다」**라고 적고,
    VWorld 안내에서 **콘솔을 여시라고** 안내할 뻔했다. 고칠 것이 없는 자리다 (M-86).
- ★★★ **왜 안 잡혔나 — `OUT=$(ssh …)` 의 종료코드는 «ssh 안 마지막 명령»의 것이다.**
  `cat > linkpilot.env` 가 거부돼도 그 뒤 `echo` 가 성공하면 **0 이 온다.**
  §12-10 의 **「실패를 돌려주는 함수는 부르는 쪽이 본다」**와 **같은 고장**인데, 그때는 PHP 였고
  이번엔 **배포 워크플로**다 (S-53 — 한 칸에서 배운 것을 옆 칸에 안 대면 그대로 남는다).
- ★★★ **「보냈다」로 판정하지 않는다 — «되읽은 수»가 판정이다.** 25 는 러너가 «모은» 수이고,
  NAS 에 «있는» 수가 아니다. 이제 NAS 에서 `grep -c` 로 되읽어 세고, **보낸 수와 다르면 적는다**
  (§4.6 「올린 뒤 몇 개가 있는지 되묻는다」와 같은 규칙).
- ★★ **고침은 «임시 이름에 쓰고 `mv`» 다.** 옛 파일을 다른 계정이 갖고 있으면 그 자리에 바로
  쓰는 길이 막힌다. 같은 폴더에 `deploy/engine.sh` 는 **성공한다** — 곧 폴더 쓰기는 열려 있다.
  ★ **못 읽는 옛 파일은 지우지 않고 이름만 바꿔 남긴다**(`linkpilot.env.unreadable-…`) —
    되돌릴 길을 없애지 않는다 (§4.6 「빈 것으로 덮지 않는다」와 같은 결).
- ★★★ **「못 쟀다」 안에 갈래 셋이 숨어 있었다.** 백업·복원시험이 `EACCES: permission denied`
  로 죽었는데 글은 **「ssh 가 안 붙었거나 node 가 없다」**였다 — **ssh 도 node 도 멀쩡했고**
  막은 것은 **자료 파일 한 개의 권한**이다. 그리고 **바로 아래 칸이 「붙기: 된다」**를 찍고 있었다.
  **이웃한 두 칸이 서로 다른 말을 하면 그 자체가 사고 신호다** (§8) — 눈앞에 있었는데 안 읽었다.
  ★ 이제 `EACCES`·`ENOSPC`·ssh/node 셋으로 갈라 적고, **부정으로** 쓴다
    (「**ssh·node 문제가 아니다**」). 낱말이 아니라 **「시키는가」**를 잰다 (§4.6 의 그 잣대).
- ★★★ **마지막에 «한 줄로» 판정하는 칸을 두었다** (`Deploy verdict`). 가르는 잣대는 하나다 —
  **그것 없이도 서비스가 도는가.** 열쇠 0개면 지적도·OCR·둘째 읽기가 **안 돈다** → **차단**이다.
  ★ **배포를 막지는 않는다** — 화면·엔진은 이미 올라간 뒤다. 올릴 것은 올리고 **판정만 빨갛다**
    (§8 「되돌렸어도 빨갛게 끝난다」와 같은 모양). **「못 쟀다」도 초록으로 안 끝낸다.**
- ★★★ **사보타주 다섯 중 하나가 «표본 때문에» 빠져나갔다** 〈실측〉. `BACKCOUNT=` 를 **한 자리만**
  바꿨더니 다른 자리가 남아 통과했다. 그리고 그것이 **검사의 약함도 드러냈다** — 「그 글자가
  있는가」만 세면 **되읽어 놓고 안 쓰는** 코드가 그대로 지나간다.
  ★ 재려던 성질은 **「되읽은 수로 판정하는가」**이므로 **판정 조건 안**을 보게 옮겼다
    (§6-2-5 — 재는 자리를 옮긴 것이지 약하게 고친 것이 아니다).
- ★★ **창을 «덩어리»로 잡는다.** 「못 쟀다」 갈래를 글자 모양으로 찾다가 **고침이 옳은데
  빨개졌다** — `if` 블록 통째로 잡으니 성질만 남는다 (§12-6 에서 세 번 겪은 자리).
- ★ **막고 있는 것은 이제 하나다 — NAS 의 파일 권한.** 열쇠는 GitHub Secret 에 **멀쩡히 있다**
  (25개가 모였다). **사장님이 열쇠를 다시 넣으실 일이 아니다.**
- 재는 자리: `im-agent/test/workflow-shape.test.js` 세 칸(14칸) — 사보타주 다섯으로 확인했다.

★★★ **같은 뿌리가 «백업»도 통째로 막고 있었다 — 한 파일 때문에 한 벌을 잃는다**
〈같은 날 · 실측〉.

- **무엇이 났나.** 백업과 복원시험이 **둘 다** 이렇게 죽었다 —
  `EACCES: permission denied, open '…/02_Source_Data/….pdf'` (`sha256` → `inventory` → `write`).
  앱이 만든 자료 파일 **하나**를 배포 계정이 못 읽는데, **던지고 끝나므로 한 벌도 안 떴다.**
  그리고 그 사실이 「못 쟀다」로만 적혀 **지금 이 디스크가 죽으면 그대로 잃는 상태**가
  조용히 이어졌다 (H-1).
- ★ **잣대는 둘이다** — ① **일부라도 뜬다** ② **무엇이 빠졌는지 반드시 말한다.**
  ①만 하면 「백업이 있다」고 믿는 채로 그 파일만 없다 (§8 · §4.7).
  ★★ 빠진 것은 **첫 줄에** 적는다 — 뒤에 적으면 「백업이 떴다」만 보고 다 있는 줄 안다 (§6-3 ①).
  ★★★ **백업 «안»에도 적는다**(`BACKUP-MANIFEST.json` 의 `skipped`) — 되살릴 때
    무엇이 없는지 그 파일 하나로 보여야 한다.
- ★ **멀쩡할 때는 그 말을 안 붙인다** — 없는 걱정을 만들지 않는다. 검사가 그 갈래도 잰다.
- ★★★ **표본이 재려는 성질을 지켜야 한다.** `chmod` 로 막으면 **root 로 돌 때 권한이 안 먹어
  거짓으로 초록**이 된다 (§12-10 에서 이미 당했다). 그래서 **읽기 자체를 던지게 끼운다** —
  누가 돌리든 같은 답이 나온다.
- ★★ **개수를 검사에 손으로 박지 않는다** — 같은 파일이 두 번(목록 뜰 때·복사할 때) 걸려
  「1개」로 박아 둔 칸이 **고침이 옳은데 빨개졌다.** 고유 목록으로 적고 **이름으로** 센다 (§6-2-5).
- 재는 자리: `im-agent/test/backup.test.js` 네 칸(15칸) — 사보타주 넷으로 확인했다.

### 12-24. **「0/5 인데 초록」 — 그리고 «대답이 왔다» 안에 할 일이 다른 셋이 또 있었다**
〈2026-09-17 사장님: 「권하는 개선안대로 진행해」 — 제가 올린 권장 셋 · 실측 · D-213〉

- **무엇이 났나.** 브이월드 수집이 **5필지 전부 실패한 실행을 초록으로 끝냈다.**
  스크립트가 걸린 것을 요약 §4 에 **적기만** 하고 종료 코드가 늘 0 이었다.
  초록이라 아무도 안 열어 보고, **정작 하려던 수집은 한 번도 안 됐다.**
  ★ §4 의 `kasi.year()` 가 「못 받았다」와 「아직 공표 전」을 같은 글자로 냈던 것과
    **같은 고장**이다 — 뭉뚱그리면 할 일이 정반대인 것들이 한 값으로 묻힌다.
- ★★★ **그리고 「서버가 대답했다」를 한 덩어리로 보면 «틀린 곳»을 가리킨다** 〈실측〉.
  처음에 갈래를 넷으로 두고 「대답이 왔으면 콘솔의 서비스URL·활용신청을 보라」 하나만
  적었다. 그런데 응답을 갈라 세니 **`fetch failed` 10건 · `HTTP 502` 6건**이고
  **인증 거부(`INVALID_KEY`·권한)는 0건**이었다.
  ★ **열쇠나 도메인이 틀리면 VWorld 는 `INVALID_KEY` 를 준다.** `502` 는 **그쪽
    게이트웨이**다 — 그 상태로 콘솔을 여시게 하면 **거기에는 고칠 것이 없다**
    (§4.6 · §12-11 · §12-12 와 **같은 결**).
- ★ **그래서 갈래가 다섯이다** — 값마다 사장님이 하실 일이 다르다.
  · **0** 전부 받음 · **1** 일부만(요약 §4 에서 어느 필지인지 본다)
  · **2** **못 쟀다** — 대답이 한 번도 없었다 → **도는 자리를 옮긴다.** 열쇠 문제가 아니다
  · **3** **그쪽 서버가 5xx** → **기다렸다 다시.** 우리 쪽에 고칠 것이 **없다**
  · **4** **인증 거부** → 콘솔 서비스URL · **지오코더** 활용신청
  · **5** 인증 통과, **주소 미매칭** → 주소 표기. 열쇠 문제가 아니다
  ★★ **섞이면 「고칠 것이 있는 쪽」을 먼저 가리킨다** — 인증 > 주소 > 5xx.
    5xx 를 먼저 말하면 「기다리면 된다」로 읽혀 진짜 고칠 것이 묻힌다.
- ★★ **판정은 요약 «맨 앞»에 넣는다** (§6-3 ①). 앞 판은 §4 맨 끝에 적었고 아무도 못 봤다.
- ★★★ **그 판정을 «나르는 자리»가 버리면 화면에 한 줄도 안 온다** (§12-19 의 그 자리).
  워크플로가 `2>&1 | tee` 로 받고 `PIPESTATUS[0]` 로 종료 코드를 집는다 — `tee` 의 0 이
  판정을 덮는다. 그리고 **요약 줄은 스스로 처방을 안 적는다** — 가르는 일은 도구가 한다.
  ★ **판정으로 끝내는 것은 맨 마지막이다** — 결과 파일·아티팩트를 먼저 남긴 뒤에 빨갛게
    끝내야 **빨간 실행에서도 받을 것이 남는다.** `결과 커밋` 도 `if: always()` 로 바꿨다.

★★★ **검사가 «자기 논리»를 재고 있었다 — 사보타주 둘이 빠져나가서 드러났다** 〈실측〉.

정규식만 소스에서 읽고 **갈래 순서는 검사 안에 베껴** 두었다. 그래서 스크립트에서
5xx 갈래를 통째로 지워도, 순서를 뒤집어도 **검사는 초록이었다.**

- ★ **고침은 판정 사슬을 «오려 내 실제로 돌리는 것»이다.** 순서·조건·문구가 전부 그
  파일에서 온다 (§8-1 「두 벌이면 한쪽이 옛말을 한다」 · §12-12 의 그 잣대와 같다).
- ★★ **오려 낼 자리를 «꼬리 글자»로 찾지 않는다** — 중괄호 짝을 세어 블록 끝을 잡는다
  (§12-6 에서 세 번 겪은 자리).
- ★ **이스케이프를 손으로 붙이지 않는다** — `1)` 를 `\1)` 로 감쌌다가 **후방참조**가 되어
  「정규식이 깨졌다」로 빨개졌다. 고침이 옳은데 **재는 자리가 틀린 것**이다.

★★ **백업이 「19개 빠졌다」만 적고 «어디인지»를 안 적었다** 〈같은 날 · 실측〉.

§12-23 의 고침으로 백업이 **560개 파일 · 5,098KB** 로 떴고 **19개만 빠졌다**(앞 판은
**한 벌도 없었다**). 그런데 로그에 **개수만** 있어 사장님이 **어느 폴더를 여실지** 알 수 없었다.

- ★ **폴더까지만 적는다. 파일 이름은 안 적는다** — 프로젝트 자료 이름에는 사람 이름·거래
  상대가 섞일 수 있고 그 줄은 Actions 로그로 나간다 (§2). 폴더만 있으면 권한을 고치기에 충분하다.
- ★★ 많으면 **앞의 셋만** 적고 「그리고 N곳 더」로 센다 — 로그를 덮지 않는다.
- ★★★ **표본이 재려는 성질을 지켜야 한다** — 뿌리의 파일로 재면 「폴더를 적는가」를
  영영 못 잰다. **폴더 안**의 파일로 잰다.

★ **guard 의 「미리보기 재생성」이 빨개진 것은 고장이 아니었다** — 미리보기가 모듈 개수를
  **실제로 돌려** 넣는데 검사 파일을 더해 **394 → 395** 가 됐다 (§8 「확인 패널에 들어가는
  결과는 빌드할 때 실제로 돌려서 넣는다」). 재생성 결과를 커밋하면 맞다.
  ★★ **그것을 「실패」로 적지 않는다** — 빨강의 뜻이 「고칠 것이 있다」와 「다시 만들어
    커밋하라」로 갈리는데, 뭉뚱그리면 없는 고장을 찾게 된다.

- 재는 자리: `im-agent/test/vworld-verdict.test.js` 여섯 칸(새 파일) · `backup.test.js` 한 칸(16칸).
  사보타주 열로 확인했다.

★★★ **그리고 그 고침을 «재 보는 순간» 다른 것이 깨졌다 — 결과 커밋이 PR 머리를 움직인다**
〈같은 날 · 실측 · 사장님: 「권하는 개선안대로 진행해」〉.

- **무엇이 났나.** 고친 코드를 재려고 이 워크플로를 **작업 가지로** 걸었다. 조회·판정은
  뜻대로 돌았고 **값 3(그쪽 서버 5xx)** 으로 빨갛게 끝났다 — 앞 판이었으면 초록이다.
  그런데 「결과 커밋」이 그 가지에 결과를 밀어 넣어 **열려 있던 PR 의 머리가 움직였고**,
  봇이 민 커밋이라 새로 선 검사가 **`action_required`**(사람이 눌러 줘야 도는 상태)가 됐다.
  **초록이던 PR 이 초록도 빨강도 아닌 자리로 내려앉았다.**
- ★ **가르는 잣대는 하나다 — 「이 결과를 저장소에 남기는 것이 그 가지의 일인가」.**
  기본 가지는 그렇다(그것이 이 워크플로의 몫이다). 작업 가지에서 도는 것은 **재 보려고
  도는 것**이라 남길 자리가 저장소가 아니라 **아티팩트**다.
- ★★ **조용히 건너뛰지 않는다.** 왜 안 남겼는지와 **어디서 받는지**를 적는다 — 적지 않으면
  「커밋이 없다」가 고장으로 읽힌다 (§8 · §6-3 ⑥).
- ★★★ **기본 가지 이름을 «못 받은 것»을 「기본 가지다」로 읽지 않는다.** 빈 값이면 견줌이
  늘 참이 되어 어느 가지에서든 밀어 넣는다 — 갈래를 따로 두고 경고로 적는다 (§8).
- ★ **건너뛰는 것은 커밋뿐이다** — 아티팩트는 어느 가지에서든 `if: always()` 로 남는다.
  검사가 그것까지 함께 잰다(뗐더니 빨개졌다).
- ★★ **이것이 「한 칸을 고치면 그 값이 흘러가는 자리를 함께 본다」의 또 한 자리다**
  (§12-5 · §12-19 와 같은 규칙). 판정을 고친 것은 옳았는데, **그 판정을 재 보는 방법**이
  다른 것을 깨뜨렸다.
- 재는 자리: 같은 파일 한 칸이 더 붙었다(**7칸**) — 사보타주 다섯으로 확인했다
  (견줌 지우기 · 빠져나가기를 `git push` 뒤로 · 까닭·받을 곳 지우기 · 빈 값 갈래 지우기 ·
  아티팩트 `always()` 떼기).

★★★ **그리고 「어디에 있다」가 «없는 길»을 가리키고 있었다 — 내가 그 길로 네 번 되감았다**
〈2026-09-17 · 실측 · 사장님: 「권하는 개선안 대로 진행해」〉.

- **무엇이 났나.** 백업 경고가 「어느 파일인지는 **위 로그에 있다**」였다. 그런데 그 줄은
  **그 단계 로그의 가운데**다 — 배포 #214 에서 그 한 줄(폴더 셋)을 읽으려고 로그를
  **네 번** 되감았고(끝에서 320줄), 그 저장소의 로그 파일 주소는 이 자리에서 **막혀 있었다.**
  사장님이라면 못 찾으시고, 찾다 지치면 **「고장」으로 읽힌다** (§12-19 의 그 자리와 같은 결).
  ★ 정작 그 값은 **실행 요약(Summary) 첫 화면**에 폴더까지 이미 실려 있었다 —
    **가리키는 자리만 틀렸다.**
- ★ **고침은 글이 가리키는 자리를 옮기는 것이다** — 「이 실행의 요약 첫 화면 「자료 백업」
  줄에 있다 · 로그를 파고 들어가지 않으셔도 됩니다」. 재는 것은 낱말이 아니라
  **어디를 가리키는가**다 (§4.6 「낱말이 아니라 시키는가를 잰다」).
- ★★ **폴더 수를 «기계가 읽는 표»에도 적는다** (`LP_OPS backupdirs=N`). 산문 줄은 사람용이라
  정기 운영점검이 못 읽는다 — 개수가 표에 있으면 **「줄었는가」를 스스로 센다** (D-196).
  ★ **폴더 이름은 그 표에 안 넣는다** (§2) — 개수만이다. 검사가 그 줄에 다른 것이
    실리면 빨개진다.
- ★★★ **세는 법이 «넷을 넘는 순간» 거짓이 됐다** 〈돌려서 재 보고 잡았다〉. 처음에 가운뎃점을
  셌는데, 정본은 많으면 「A · B · C **그리고 5곳 더**」로 줄인다 — **8곳인데 3 으로** 세진다.
  ★ **고침은 정본이 총수를 적는 것**이다(`그 자리(8곳): …`). 세는 쪽이 짐작하게 두지 않는다
    (§8-1 — 두 벌로 세면 한쪽이 옛말을 한다 · §6-2-6 의 46 → 105 와 **같은 규칙**).
  ★★ 그리고 검사는 **그 수가 실제 폴더 수와 같은지**까지 본다 — 「괄호가 있는가」만 세면
    **99곳**이라고 적어도 통과한다(사보타주로 확인).
- ★★ **못 셌으면 그 줄을 아예 안 찍는다** — 0 으로 찍으면 「빠진 폴더가 없다」로 읽혀
  `partial` 과 어긋난다 (§8 · §12-12 「0 을 본 순간 없다로 적지 않는다」와 같은 결).
- ★★★ **정규식을 «돌려서» 잰다.** 글자만 보면 못 잡는다 — 처음 쓴 식이 괄호 **밖**의
  「곳」을 찾아 **네 갈래 전부 빈 값**이었고, 돌려 보고서야 알았다. 검사도 그 `sed` 규칙을
  워크플로에서 **뽑아 실제로 실행**해 셋을 먹인다(3 · 8 · 빈 줄).
- ★ **글자 모양을 박아 둔 옛 칸이 «고침이 옳은데» 빨개졌다** — `그 자리:` 를 찾던 자리다.
  재려던 성질(「어디인지를 적는가」)은 그대로 두고 **세는 자리를 옮겼다** (§6-2-5 의 잣대).
- 재는 자리: `im-agent/test/workflow-shape.test.js` 한 칸(**13칸**) ·
  `im-agent/test/backup.test.js` 한 칸을 고쳤다(16칸) — **사보타주 일곱**으로 확인했다
  (「위 로그」로 되돌리기 · `backupdirs` 줄 지우기 · 가운뎃점 세기로 되돌리기 ·
  폴더 이름을 표에 싣기 · 못 셌을 때 갈래 지우기 · 정본에서 괄호 떼기 · 정본이 99곳이라 적기).

### 12-19. **「몫이 0」과 「오늘 몫을 다 썼다」는 둘 다 429 로 온다 — 가르는 것은 `limit: 0` 하나다**
〈2026-09-17 사장님 화면: `render-smoke` 빨간 ❌ 둘 · 실측〉

- **무엇이 났나.** 사장님이 3주 전 실행 둘을 보여 주셨다. 로그를 갈라 읽으니
  **열쇠는 살아 있었다** — 구글이 인증을 통과시키고 이렇게 답했다:
  「`Quota exceeded … generate_content_free_tier_requests, **limit: 0**` · Please retry in 59s」.
  곧 **무료 등급에 이미지 모델 몫이 «아예 0»**이다.
- ★★★ **§4.6 의 「기다리면 낫는 것(한도)」에 이것을 넣으면 안 된다.** 서버가 「59초 뒤에
  다시 하라」고 **말하는데도** 기다려서 안 낫는다 — 결제를 붙여야만 열린다. 겉모습(429·retry)이
  같아서 **뭉뚱그리기 가장 쉬운 자리**다.
- ★★ **그리고 반대로도 막는다.** 결제를 붙이신 뒤의 «진짜» 한도 초과에 「결제를 붙이세요」라고
  적으면 **이미 하신 일을 또 하시게 된다** (M-86). 그때는 기다리면 된다고 적는다.
  가르는 것은 **`limit: 0` (또는 `free_tier` 표시) 하나**다.
- ★★★ **그때 화면이 가리킨 곳이 틀렸다.** 마지막 줄이 「모델 이름이 다르면 `GEMINI_RENDER_MODELS`
  로 바꿔 다시 시도한다」였다 — 사장님은 **모델 이름을 고치러 가신다.** 실제 원인은 결제다
  (§4.6 「원인을 사람 말로 적는다」 · §12-12 와 같은 결).
- ★ **그 가름은 그 뒤에 이미 들어와 있었다 — 그런데 «재는 칸이 0개»였다.** 누가 조건을
  건드리면 **조용히 되돌아가고**, 그때 증상은 또 「모델 이름을 고치라」다. 그래서 못박았다.
  ★★ **이미 고쳐진 것을 「아직 안 고쳐졌다」로 적지 않는다** — 돌려서 재 보고 갈랐다
    (§8 「이웃한 두 칸이 서로 다른 말을 하면 사고 신호」).
- ★★ **죽은 트리거를 남겨 두지 않는다.** 그 워크플로가 `push: branches: [<3주 조용한 갈래>]`
  로 묶여 있었다 — 그때는 「main 에 파일이 없어 dispatch 가 404」라 우회한 것인데,
  **그 조건이 사라졌다**(파일이 main 에 있다). 남겨 두면 그 갈래를 건드릴 때마다 또 빨개진다.
- 재는 자리: `im-agent/test/birdseye.test.js` 세 칸(17칸) — **사장님 화면에 실제로 찍힌 글**을
  먹여 돌린다. 사보타주 둘로 확인했다.
  ★ **표본을 줄여 쓰지 않는다** — 짧게 줄이면 `limit: 0` 이 빠져 네 갈래 중 둘이 안 갈린다
    (§12-11 「표본이 고장의 크기를 정한다」).

★★★ **그런데 그 고침이 «사장님께 닿는 자리»에는 한 번도 안 왔다** 〈2026-09-17 · 같은 화면을
다시 주심 · 실측〉.

사장님이 `render-smoke` 실패 화면을 다시 주셨는데 요약 첫 줄이 **여전히** 「… (모델 이름은
`GEMINI_RENDER_MODELS` 로 조정)」이었다. 위 칸 셋은 초록인데도.

- ★★★ **갈라 보니 도구는 멀쩡했고 «그 답이 요약에 한 줄도 안 왔다».** 그 도구는 판정을
  **stderr** 로 적는데 워크플로가 `| tee` 로 **stdout 만** 받았다 — 실측으로 그 파일에 담긴
  것은 **「원본: …」 한 줄**이었고, **사장님 화면에 정확히 그 한 줄만** 있었다.
  판정은 Actions 로그에만 있었다. **「만들었다」와 「닿는다」는 다른 사실이다** (§8 과 같은 결).
- ★★ **그리고 요약 줄이 «스스로 처방을 적고» 있었다.** 무엇이 막았든 그 한 줄이 늘
  모델 이름을 가리켰다 — 그 자리가 **판정을 잘라 낸 자리 바로 위**였으니, 사장님이 보시는
  것은 **엉뚱한 처방 하나뿐**이었다. 가르는 일은 도구가 한다 — 나르는 자리는 **그 답을
  그대로 싣기만 한다.**
- ★★★ **한 칸을 고치면 «그 값이 흘러가는 자리»를 함께 본다** (§12-5 와 같은 규칙).
  §12-19 가 도구에서 끝나 **재는 자리가 거기까지**였고, 그래서 그 칸들이 **아무것도 못 막았다.**
  ★ 내가 §12-19 에 「그 가름은 그 뒤에 이미 들어와 있었다」라고 적은 것이 **반만 맞았다** —
    들어와 있었지만 **닿지는 않았다.**
- ★ **나르는 줄 수를 손으로 박지 않는다.** `tail -8` 이었는데 판정은 최악 13줄이라,
  stderr 를 받게 고쳐도 **잘리는 첫 줄이 하필 「무엇이 막았는가」**다. 검사가 갈래 넷을
  **돌려서 최악 길이를 세어** 견준다 (§6-2-6 의 46 → 105 와 같은 규칙).
- ★★ **주석 줄을 떼고 본다.** 이 고침의 경위를 워크플로 주석에 그대로 적었으므로,
  안 떼면 **한 칸은 주석으로 통과하고 다른 칸은 주석 때문에 빨개진다** — §8 의 그 함정이
  **양쪽으로** 온 경우다.
- ★★★ **그리고 사보타주 하나가 빠져나갔는데 «검사가 옳았다».** 판정 글을 12줄 늘려도
  최악이 23줄이라 24줄이 여전히 덮었다 — **표본이 모자랐던 것**이다. 14줄로 늘리니
  그 자리에서 빨개진다 (§8 「표본이 거짓말을 하면 잡히는 것도 거짓이다」).
- 재는 자리: `im-agent/test/birdseye.test.js` **세 칸이 더 붙었다(20칸)** — 워크플로를
  **주석을 떼고** 읽어 ① stderr 를 받는가 ② 요약이 스스로 처방을 적지 않는가
  ③ 나르는 줄 수가 판정 최악 길이를 덮는가. 사보타주 넷으로 확인했다.

### 12-13. **「눌러도 아무 일이 없다」의 뿌리는 둘이다 — «첫 줄에서 돌아가기»와 «안 끝나는 기다림»**
〈2026-09-17 사장님 지시 셋: 「삭제 했는데 되살아 남」 · 「AI 생성 이미지 작동이 안됨」 ·
 「계정을 입력하면 창이 닫힘」 — 하루에 셋, 생김새는 다른데 속이 같았다〉

**셋 다 오류가 한 줄도 안 난다.** 그래서 사장님이 화면에서 찾아 주셔야 잡혔다.

- ★★★ **① 첫 줄에서 조용히 돌아간다.** AI 이미지 만들기가 문구(`prem`)가 없으면
  **AI 호출 0회**로 끝나고, 안내는 3.5초 뒤 사라졌다. 화면에는 **아무 일도 안 일어난 것처럼**
  보인다 — 그것이 「작동이 안됨」의 겉모습이다.
  ★ **같은 규칙을 옆 자리(`pickBg`)에는 이미 댔다** — 고른 그림을 버리지 않고 받아 둔다.
    **이 칸에만 안 댄 자리**였다 (S-53 「한 칸에서 배운 것을 옆 칸에 안 대면 그대로 남는다」).
  ★★ **막지 말고 «받아 두라».** 사람이 값을 넣고 단추를 누른 것은 명백한 의사다 —
    되돌려 보내지 말고, 만든 뒤 받아 두는 자리로 넘기고 **미리 그 사실을 적는다.**
- ★★★ **② 상한 없는 기다림이 화면을 통째로 세운다.** 구글 로그인 창의 `initCodeClient` 에
  **실패를 받는 자리(`error_callback`)가 없어**, 팝업이 닫히면 성공 콜백도 안 오고
  **약속이 영영 안 풀렸다**(실측: 화면 글 **0줄**). 부르는 쪽의 `await` 가 그 자리에서 멈춘다.
  ★ §12-8 의 「상한 없는 `fetch`」와 **같은 뿌리**다 — 그때는 망, 이번엔 콜백이다.
  ★★ **콜백을 받는 창구에는 «실패 갈래»가 반드시 있어야 한다.** 성공만 받는 코드는
    실패했을 때 **아무 말도 안 한다.**
- ★★★ **③ 안 낫는 것을 계속 다시 건다** (§4.6 · §12-5 · §12-11 의 그 잣대).
  AI 이미지가 **400/403/404 에도 열쇠를 전부 돌아** 열쇠 11 · 모델 3 이면 **33회**,
  예산 120초를 꽉 채우고 끝났다 — 화면에서는 **막대만 차고 아무것도 안 나온다.**
  **33 → 3회.** ★ 판정식은 **한 벌**을 쓴다(`window.lpAiFatal` · §8-1).
  ★★ **반대로도 안 가게 한다** — 한도(429)는 열쇠마다 다르므로 **여전히 다 돈다.**
    검사가 두 갈래를 **각각** 잰다.
  ★★★ **셋째 갈래(§12-11)가 여기에도 있었다** — 이미지 모델이 `responseModalities` 에
    `TEXT` 를 함께 요구하는 판이 있다. 기다려도 안 낫고 열쇠를 바꿔도 안 낫지만
    **몸통을 고쳐 같은 모델·같은 열쇠로 다시 걸면 그 자리에서 낫는다**(실측
    `IMAGE → TEXT+IMAGE → 성공`). **모델 이름으로 기억한다**(`LP_NOTHINK` 과 같은 모양 —
    새 규격을 만들지 않는다).

★★★ **그리고 「같은 표시를 두 자리가 다르게 읽는 것」이 삭제를 되살렸다.**
지울 때 남기는 표시(묘비)를 **병합은 «시각을 보고»** 첫 화면은 **«아예 안 보고»** 있었다.
서버 쪽 값이 묘비보다 **나중에 갱신되면 되살아난다**(돌려서 잼).
- ★ **「지운 것」에는 시각이 필요 없다.** 갱신은 되돌릴 수 있지만 삭제는 아니다 —
  다만 **묘비를 되돌리는 자리가 0곳**임을 먼저 재고 뺐다. 안 재고 뺐으면 「되살리기」가
  있는 줄도 모르고 없앴을 것이다.
- ★★ 고침은 **판정을 한 곳으로 모으는 것**이다 (§8-1). 지나는 자리를 **세어서** 막는다.

#### 검사 쪽에서 배운 것 — 셋

- ★★★ **검사가 재려던 고장을 «자기가 겪을 수 있다».** 로그인 칸을 처음 쓸 때 그
  **안 풀리는 약속**을 `await` 해서, **아무 글도 안 찍은 채 0 으로 끝났다.**
  **되돌렸는데도 초록인 검사는 아무것도 재지 않는 검사다.**
- ★★★ **사보타주가 «꼬리·창» 때문에 두 번 빠져나갔다.**
  · 「걸어 본 횟수」를 **바깥에서만** 세어, 모양을 바꿔 다시 건 호출이 안 세졌다(실제 6, 적힌 3).
  · 두 갈래를 **같은 말**로 되돌려도 글 끝의 「(사유: …)」에 갈래 이름이 들어 있어 **늘 달라 보였다.**
  · 창구를 지나는지 재던 칸이 **뒤 220글자**를 보다 **옆 줄의 다른 컬렉션**을 제 것으로 읽었다.
  ★ 셋 다 **재려던 성질은 그대로 두고 세는 자리만 옮겨** 고쳤다 (§6-2-5 의 잣대).
- ★★★ **파일을 만들기 «전에» 있는지부터 본다.** `test-ai-image.js` 를 새로 만든 줄 알고
  **덮어썼다**(19.5KB · 12칸). `git status --short` 로 **눈으로 세다가** 잡았다 (§8-2).
  그대로 넘어갔으면 검사 **열두 칸이 아무 오류 없이** 사라졌을 것이다. 되돌리고
  **그 파일에 이어 붙였다**(12 → 21칸) — 같은 주제를 두 파일로 만들지 않는다 (§6-3 ⑥).

★ **못 잰 것 둘** — 서버(`sync.php`)가 묘비를 받아 **자기 쪽에서도 지우는지**,
그리고 AI 이미지가 부르는 **모델 이름 셋이 구글에 실제로 있는지**. 둘 다 통과로 안 적는다 (§8).

- 재는 자리: `linkpilot-platform` 의 `test-todo-delete.js`(교차검증 **113번째 칸**) ·
  `test-ai-image.js`(12 → **21칸**) · `test-gcal-why.js`(이어 붙임) ·
  경위는 저쪽 MEMORY.md **S-83 · S-84 · S-85**.


### 12-14. **「검사가 빨개진 것」과 「일이 시작조차 못 한 것」은 다른 사실이다**
〈2026-09-17 사장님: 「권하는 개선안 진행해」 — 제가 올린 권장 ① · 실측〉

**무엇이 났나.** platform #122 가 합쳐지자 배포가 **3초 만에** 실패했다. 앞선 배포들은
전부 **11~14분**이었다. 로그를 내려받으면 **404** 다 — 단계가 하나도 안 돌았으니
로그 자체가 없다. 사유는 **check-run 의 주석**에만 있었다:

> The job was not started because **recent account payments have failed or your
> spending limit needs to be increased.**

- ★★★ **코드 실패가 아니다.** 할 일이 **정반대**다 — 앞은 **결제 자리**, 뒤는 **코드**다.
  뭉뚱그리면 사장님이 **고칠 것이 없는 자리**를 보러 가신다 (§4.6 · §12-4 · §12-10 과 같은 결).
- ★★ **「걸렸다」와 「닿았다」 사이에 한 칸이 더 있었다** — 「걸렸는데 **시작조차 못 했다**」.
  §8 의 「커밋했다 / 배포를 걸었다 / NAS 에 닿았다」에 **넷째**가 붙는다.
- ★ **무엇으로 아는가**: 잡의 단계가 **0개**이고 러너 이름이 **비어 있으면** 시작도 못 한 것이다.
  ★★ **둘 중 하나만 보면 안 된다** — 단계가 비는 다른 경우(스킵)와 러너 이름이 비는 다른
    경우가 따로 있다. **둘이 함께**일 때만 그 갈래다 (실측으로 사보타주에 걸렸다).
- ★★★ **사유가 없으면 「사유를 못 받았다」로 적는다** — 없는데 결제라고 단정하면
  그것이 또 엉뚱한 곳을 가리킨다 (§4.6 「지어내지 않는다」).
- ★ **`cancelled` 은 실패가 아니다** — 같은 커밋을 두 번 재지 않으려고 스스로 비켜난 것이다.
  이 저장소의 배포·검사는 `push` 와 `pull_request` 가 **둘 다** 걸려 한쪽이 늘 비켜난다.
  그래서 PR 이 `unstable` 로 보이는데 **합쳐도 된다** (실측: #120·#121 도 같은 모양이었다).

**재는 자리** — `npm run deploy:why -- --run <run.json> --jobs <jobs.json> --ann <ann.json>`
(`im-agent/tools/deploy-why.js`). 되돌아오는 값을 **갈라 준다**:
**0** 성공·비켜남·도는 중 · **1** 코드 쪽 · **3** 시작조차 못 함 · **2** 못 쟀다.
★ **「못 쟀다」를 0 으로도 1 로도 뭉개지 않는다** (§8 · `run-gate.js` 와 같은 잣대).
검사는 `im-agent/test/deploy-why.test.js` 8칸 · 사보타주 다섯으로 확인했다.

### 12-15. **「사람이 누른 뒤 아무 글도 안 남는 길」을 사람의 눈 대신 센다**
〈2026-09-17 · 권장 ② · 실측〉

§12-13 의 셋이 **하루에** 났고 **셋 다 사장님이 화면에서 찾아 주셔야** 잡혔다.
그 자리를 검사가 대신 센다 — **실측 `onClick` 165곳 중 조용한 자리 넷**.

- **무엇을 조용한 자리로 치나** — `onClick` 안에서 `if(…) return` 하는데 **말도 안 하고
  화면도 안 바꾸고 일도 안 하는** 곳.
- ★ **정당한 침묵은 안 센다** — 말을 남긴다 · 화면 상태를 바꾼다 ·
  이미 도는 중이거나 취소다 · 돌아가기 **전에** 일을 한다.
- ★★ **콜백 창구에 «실패 갈래»가 있는지 함께 센다** — 성공 콜백만 받는 코드는
  실패했을 때 **아무 말도 안 한다**. 구글 로그인이 실제로 그랬다 (§12-13 ②).
- ★★ **기준선은 파일마다 둔다** (§8 의 쪼개기 래칫).
- ★ **이 칸이 실제로 무언가를 찾고 있는지** 함께 잰다 — `onClick` 이 0개면 눈이 먼 것이다.

★★★ **그리고 이 검사는 «자기 잣대»를 먼저 잰다** 〈사보타주가 빠져나가서 넣었다〉.
「일을 하는가」 판정을 **조건까지 보게** 무디게 되돌리면 조용한 자리가 **4 → 0** 이 되는데,
앞 판은 「안 늘었다」로 **초록이었다.** 줄어든 것이 **「지운 것」인지 「안 세게 된 것」인지**
안 갈랐기 때문이다 (§8 의 쪼개기 래칫 · §6-2-6 의 46 → 105 와 **같은 규칙**).
- ★ 그래서 **일부러 만든 표본 다섯**을 먹여 잣대가 여전히 그것을 잡는지 본다.
  무뎌지면 **실측 숫자를 보기 전에** 그 자리에서 빨개진다.
- ★★ **실제로 값을 했다** — 사보타주를 되돌린 줄 알았는데 안 되돌려져 있었고,
  새 표본이 **그 자리에서** 잡았다.

★ **못 재는 것**: `onChange` · `onSubmit` · `onKeyDown` 은 **안 본다** — 좁게 시작했다
(§12-4 「가르는 열쇠는 좁게 시작한다」). 그 사실을 검사가 직접 적는다.

- 재는 자리: `linkpilot-platform` 의 `test-silent-click.js` (교차검증 **114번째 칸**).


### 12-16. **예산이 끝났으면 «무엇을 해 봤는지»까지 적는다 — 그러지 않으면 「또 처음부터」로 읽힌다**
〈2026-09-17 사장님 화면: 「❌ 정해 둔 시간(40초) 안에 안 끝났습니다. 잠시 뒤에 다시 눌러 주세요.」
 · 판 `202609172015` · 「교차검증후 문제오류 버그 개선해줘」〉

- **글은 옳게 나왔다** — §12-11 의 꼬리 규칙이 도는 증거다(통화 이야기가 안 붙었다).
  **문제는 여전히 예산을 넘긴다는 것**이다.
- ★★★ **잰 값**: 예산 **45초** · 고리는 **열쇠 N × 모델 6** · 호출당 최소 **8초**.
  열쇠가 느리면 **다섯·여섯 개만 걸어 보고** 예산이 끝난다 — 열쇠 11개면 **절반도 못 돈다.**
- ★★★ **그런데 「다시 누르세요」는 원래 참말이었다.** 이 앱은 `_gemIdx` 로 **다음에 그 다음
  열쇠부터** 시작한다(§4.6 이 이미 초록으로 잰 그 성질). 그 사실이 **글에 없어서**
  사장님은 **「또 처음부터 40초」**로 읽으신다 — 실제로는 **이어서** 건다.
  ★ 참말인데 **참말로 안 읽히는 글**은 거짓말과 값이 같다 (§4.6 「원인을 사람 말로 적는다」).
- **고침은 둘이다** — ① **몇 개를 걸어 봤는지** 센다(`_hit`) ② **「다음 열쇠부터 이어서 건다」**를
  글에 싣는다. 열쇠를 못 읽었으면 **개수 이야기를 안 붙인다**(없는 것을 있는 척 안 한다).

★★★ **그리고 사보타주 하나가 «글 모양만 재느라» 빠져나갔다.**
세는 자리(`_hit++`)를 통째로 지워도 검사가 **초록**이었다 — 검사가 `_hit` 을 **인자로 먹여**
돌리기 때문이다. 그러면 화면에 늘 **0개**가 찍히고 그것이 **「한 개도 못 걸었다」**로 읽힌다
(§4.6 「0 을 본 순간 없다로 적지 않는다」와 같은 결).
- ★ 고침은 **「열쇠를 고르는 자리마다 세는 자리가 붙어 있는가」**를 함께 재는 것이다.
  잣대는 하나다 — **「그 숫자를 재는 법이 재려는 것을 다 덮는가」** (§6-2-6 의 46 → 105).

★★ **고리에 새 값이 들어가면 «그 고리를 잘라 돌리는 검사»가 거짓으로 빨개진다.**
`test-card-batch-quota.js` 가 그 자리에서 또 걸렸다 — **그 파일 주석에 이미 적혀 있던
바로 그 자리**다(S-71 때 예산 둘이 들어왔을 때). **없던 인자만 채운다** (§12-5 와 같은 결).
- ★ 같은 덫에 두 번 걸린 것이므로 적어 둔다: **고리에 값을 더할 때는 그 고리를 잘라
  돌리는 검사를 함께 연다.**

- 재는 자리: `linkpilot-platform` 의 `test-load-first.js` (20 → **24칸**) — 예산 초과 글을
  **떼어 내 돌려서** 재고, 세는 자리가 **고리 둘 모두**에 붙어 있는지 함께 본다.
  사보타주 넷으로 확인했다.

### 12-17. **배포 실패를 «자동으로» 갈라 적는다 — 다만 못 하는 것을 먼저 적는다**
〈2026-09-17 사장님: 「권하는 개선안 대로 진행해」 — 권장 ①〉

§12-14 가 만든 `deploy:why` 는 **내가 손으로 부르는** 도구였다. 이제 배포가 끝나는 순간
`linkpilot-platform` 의 **`deploy-watch` 워크플로**가 같은 갈래를 **스스로** 적는다.

- **판정은 `.github/scripts/deploy-why.py` 한 곳**에 있다 — 워크플로에 안 적는다.
- ★★★ **이 장치가 «못 하는 것»을 워크플로 머리말에 적었다** — 배포가 **결제 때문에**
  러너를 못 받았다면 **이 감시 잡도 같은 이유로 못 돈다.** 그때는 아무 글도 안 남는데,
  그것을 **「괜찮다」로 읽으면 안 된다** (§8 — 침묵은 통과가 아니다).
  ★ 결제가 풀린 뒤 **[Run workflow] 로 손수 걸면** 그 과거 실행을 다시 읽어 적는다 —
    막다른 길을 안 만든다 (§6-3 ⑥).
- ★★ **판정이 두 벌인 사실을 양쪽에 적는다.** 저장소가 달라 코드를 나눠 쓸 수 없다 —
  `deploy-why.js`(cron)와 `deploy-why.py`(platform)가 **같은 규칙**임을 서로 가리킨다.
  합치지 않고 **같은지 적어 둔다** (§8 「같은 것이 네 곳에 적혔다고 합치는 것이 늘 답은 아니다」).
- 재는 자리: `linkpilot-platform` 의 `test-deploy-watch.js` (교차검증 **115번째 칸**) —
  실제 GitHub 응답 모양을 **먹여 돌린다**. ★ **못 잼 1** 을 남긴다: 「결제로 막힌 그 순간
  이 감시가 실제로 도는지」는 그때 러너를 받느냐에 달렸다.

### 12-18. **상한 없는 `fetch` — 「아직 안 옮겼다」와 「옮기면 안 된다」를 숫자와 사유로 가른다**
〈2026-09-17 · 권장 ② · 실측 **66 → 29곳** — 앱 55 → 21 · `lp-voice.js` 7 → 4 · 나머지 넷 4 그대로〉

★★★ **그리고 이 줄을 «내가 처음에 55 → 16 으로 적었다.»** 앞의 55 는 **앱 하나만** 센 값이고
16 은 아예 잰 값이 아니었다 — 검사가 적는 수(**29**)와 견주지 않고 적었다. §8 의 쪼개기 래칫과
**같은 규칙**에 이 저장소가 또 걸린 것이다: **「그 숫자를 재는 법이 재려는 것을 다 덮는가」.**
★ 그래서 **숫자는 검사가 찍은 줄에서 옮긴다** — 머리로 더하지 않는다.

- **옮긴 것**: 구글 캘린더(남은 넷) · 태양광 보고서 셋 · 예약 · 프로젝트 그림 ·
  자료동기화 probe · 질문답변 · 친구 · 배경 · 명언 · 파일목록 · 주소이름찾기 · 번역 ·
  사진 「썼다」 신고 · 통화 파일조회.
- ★★★ **남은 것은 대부분 «옮기면 안 되는 것»이고, 그 사유를 검사에 적었다.**
  안 적으면 다음 사람이 **업로드까지 8초로 끊어 놓는다** (§4.7).
  · **맨 앞 부팅 블록** — 창구가 **아직 없는 자리**라 옮기면 그 자리에서 죽는다.
  · **파일 올리기** — 큰 몸통이라 8초로 끊으면 깨진다.
  · **`fetch(dataUrl)` 계열** — 망 호출이 아니라 **blob 만드는 것**이다. 상한이 무의미하다.
  · **AI 호출** — 이미 **자기 AbortController** 가 있다.
  · **결제** — 돈이 오가는 자리라 **중간에 끊으면 상태가 갈린다.** 사람이 정할 일이다.
- ★ **그러니 이 수가 0 이 되는 것이 목표가 아니다.** 늘면 막고, 진짜로 옮겼으면 낮춘다.


### 12-25. **열쇠가 «이름 셋»이 되면 — 넘어가는 잣대를 먼저 정한다**
〈2026-09-17 사장님: 「VWORLD_DOMAIN · LINKPILOT_VWORLD_WEB_KEY ·
 LINKPILOT_VWORLD_REPORT_KEY — 3대 키 넣었어 업데이트 했음 반영해줘」 · 실측〉

- **무엇이었나.** 새 이름 둘이 **저장소에 0회**였다 — 넣으신 열쇠를 **읽는 코드가
  한 줄도 없었다.** §4.6 의 「GEMINI 열쇠 15개」와 **같은 자리**이고, 그 상태는
  **아무 오류도 안 낸다.** 답은 언제나 같다 — **다시 넣으시라가 아니라 둘 다 읽는 것.**
- ★★ **이름 목록은 «한 곳»에만 둔다** (`connectors/vworldkey.js`). `vworld.js`(req)와
  `nsdi.js`(ned)가 **같은 값**을 읽으므로 이름표를 두 벌 두면 다음 사람이 한 곳만
  고치고, 그러면 「일부 기능만 된다」가 된다 — 그 증상은 「키가 틀렸다」와 구분되지 않는다
  (§8-1 · `datakey.js` 와 같은 결).
- ★★★ **여럿이 되는 순간 «어디서 다음 열쇠로 넘어가는가»가 새 물음이 된다.**
  · **인증 거부** → 넘어간다. VWorld 는 키마다 등록 도메인·활용신청이 따로라
    하나가 거부돼도 다른 하나는 될 수 있다. **거부 한 줄로 접으면 멀쩡한 열쇠가
    한 번도 안 불린다** (§12-11 에서 겪은 그 자리).
  · **5xx · 못 닿음 · 주소 미매칭** → **안 넘어간다.** 다른 열쇠로 낫지 않는다 —
    거기서 돌면 **호출만 배로 늘고**(§4.5) 시간이 버려진다 (§12-5 의 그 잣대).
  ★ **판정식은 한 곳에 둔다**(`isAuthReject`). 두 벌이면 재시도는 열쇠를 돌았는데
    진단은 「인증 아님」이라 말하는 어긋남이 난다 (§8-1).
- ★ **순서를 «추측»으로 정했으면 그렇게 적는다.** `REPORT`/`WEB` 중 어느 것이 서버용인지는
  **콘솔을 봐야 알고 여기서는 못 잰다.** 그래서 둘 다 후보로 두고, **실제로 어느 이름이
  먹었는지**를 `usedName()` 이 말하게 했다 — 그 잰 값이 나오면 순서를 고친다 (§4.3).
- ★ **같은 값을 두 이름으로 넣으실 수 있다** — 값으로 중복을 걸러 **한 번만** 부른다.
- ★★ **이름을 더하면 따라오는 자리가 일곱이다** — `SECRET_ENV`(가림 · §2) ·
  `router.js`(「그중 하나」) · `design/check.js`(산출물에 값이 샜는지) · 진단 표 ·
  지침서 인벤토리 표 · 수집 워크플로 `env:` · 배포의 `NAMES`.
  **하나만 빠져도 아무 오류 없이 조용히 죽는다** — 검사가 일곱을 다 센다.

★★ **그리고 진단 글이 «요약 판정과 정반대»를 시키고 있었다** 〈같은 날 · 권장 ①을 함께 고침〉.
필지별 줄이 무엇이 막았든 「`IM_AGENT_DEBUG_HTTP=1` 로 다시 실행해 원문을 확인한다」로
끝났다 — 그런데 §12-24 가 넣은 맨 앞 판정은 「VWorld 쪽 5xx · **우리 쪽에 고칠 것이 없다**」다.
**한 화면에서 두 줄이 정반대를 시킨다** (§8 「이웃한 두 칸이 서로 다른 말을 하면 사고 신호」).
원문을 봐도 5xx 는 그쪽 게이트웨이라 고칠 것이 없다 — 이제 5xx·못 닿음에 **각각 다른 말**을 적는다.
★ **낱말이 아니라 「시키는가」를 잰다** (§4.6 의 그 잣대).

- 재는 자리: `im-agent/test/vworldkey.test.js` (14칸) — 커넥터를 **가짜 망으로 돌려서**
  열쇠 넘어가기 두 갈래를 각각 잰다. **사보타주 여덟**으로 확인했다.

### 12-26. **카드가 «어느 표에서 오는가»를 지시대로 옮긴다 — 소스를 바꾸면 따라오는 자리가 넷이다**
〈2026-09-17 사장님 지시: 「오늘 브리핑에서 · 지금할일 → 플렛폼에서 할일로부터 소스를 가져와 ·
 이어서 중요한 일 0건 → 할일로부터 최근 소스 3개씩 매번교체 · 신규 프로젝트 · 최근 15일 →
 프로젝트로부터 최근 소스 3개씩 매번교체」〉

- **잰 값으로 갈랐다.** 「지금 할 일」은 **이미 할일에서 오고 있었다** — 고칠 것이 없다고 적었다.
  나머지 둘만 소스가 달랐다.
  · **「이어서 중요한 일」** ← `top.slice(1,4)` ← **우선순위 큐**. 그 큐는 할 일의 마감을
    **`t.date` 로만** 읽는데 화면의 「지금 할 일」은 **`dueDate||date`** 를 읽는다. 그래서
    `dueDate` 만 있는 할 일은 큐에서 **점수 0** 이 되어 `점수>0` 필터에 걸려 빠졌다 — **0건**이다.
  · **「신규 프로젝트 · 최근 15일」** ← **단계가 「접수」이고 최근 15일** 안인 것만. 그날 그런
    프로젝트가 없으면 칸이 **통째로 안 그려진다.**
  ★ 둘 다 **오류가 한 줄도 안 난다.** 화면이 그냥 비어 있고, 사장님만 그것을 보신다.
- ★★★ **같은 목록을 두 곳이 «각자» 거르면 한쪽이 옛말을 한다** (§8-1). 위 고장이 바로 그것이다 —
  같은 할 일 목록을 큐와 화면이 **다른 잣대**로 읽었다. 이제 둘 다 `_b2Todos` 한 벌을 부른다.
  ★ 「이어서」는 「지금 할 일」 셋을 **뺀** 나머지다 — 안 빼면 같은 것이 두 칸에 나온다.
- ★★★ **「매번 교체」의 회전 자리는 «한 번만» 올린다.** 브리핑을 만드는 함수가 `mkCode` 를
  **두 번** 부른다(먼저 그리기 · 최종). 부를 때마다 올리면 한 브리핑에서 **두 칸이 뛰어
  첫 화면과 최종이 달라 보인다.** 그리고 **조용히 저장한다** — 자료 바뀜 알림을 내보내면
  그것이 브리핑을 또 만들 수 있다.
  ★ **사람마다 가르지 않는다** — 이것은 자료가 아니라 «어디부터 보여 줄지 가리키는 손가락»이다.
    한 기기를 둘이 써도 시작 자리만 달라질 뿐 **남의 것이 보이지 않는다** (§12-4 「가르는 열쇠는
    좁게 시작한다」).
- ★★★ **소스를 바꾸면 «그 소스를 말하는 글»도 함께 고친다.** 머리글 「신규 프로젝트 · 최근 15일」과
  출처 줄 「· 최근 15일 안」은 그 순간 **거짓이 된다.** 한 곳을 고치면 같은 말이 적힌 다른 자리도 센다.
- ★★ **점수를 모르면 «등급 색을 안 칠한다».** 할 일에서 온 항목은 큐에 없을 수 있다 —
  그때 `_b2Grade(0)` 을 칠하면 **「LOW 로 판정했다」**가 되어 지어낸 값이다 (§4.6).
  큐에 같은 이름이 있으면 그 점수·근거를 **그대로** 싣고, 없으면 회색 점으로 「등급을 못 냈다」를 말한다.
- ★★★ **「이 문이 무엇을 감싸는가」를 «괄호를 세어» 확인한 뒤에 고친다** 〈실측 · 고치다 되돌렸다〉.
  「큐가 비면 할 일 카드가 통째로 사라진다」고 보고 `b.priority&&` 를 넓혔는데, 괄호를 세어 보니
  그 문이 감싸는 것은 **메타 줄과 「오늘 먼저 할 일」 상자 둘뿐**이었다 — 「지금 할 일」·「이어서」·
  통화·일정은 **형제**라 애초에 안 걸린다. 넓혔으면 `b.priority` 가 없는 날 `b.priority.종류` 에서
  **그 자리로 죽었다.** 눈으로 읽은 구조를 고침의 근거로 쓰지 않는다.
- ★★★ **재려던 성질은 그대로 두고 «재는 자리»를 옮겼다.** 기존 칸 하나가 소스 이름
  (`신규접수_최근15일`)과 코드 모양(`newProjects:(()=>{`)에 글자를 박아 두어 **고침이 옳은데
  빨개졌다.** 재려던 것은 **「왜 신규인지(날짜·날짜종류)를 지고 가는가」**이므로 그것은 그대로 잰다
  (§6-2-5 의 잣대). 이제 **`mkCode` 를 통째로 떼어 내 실제로 돌려** 나오는 값을 본다 —
  「`_b2Next` 라는 낱말이 있는가」는 이름만 바꿔 끼워도 통과하는, 아무것도 안 재는 검사다.
- 재는 자리: `linkpilot-platform` 의 `test-brief-sourcing.js` (7 → **14칸**) — **사보타주 여덟**으로
  확인했다(옛 소스 되돌리기 둘 · 회전 자리 mkCode 안으로 · 시끄럽게 저장 · 두 벌 잣대 되살리기 ·
  등급 색 되돌리기 · 머리글 되돌리기 · 셋을 안 빼기).

★★★ **그리고 «뿌리»는 카드가 아니라 큐였다 — 카드를 옮겨 피한 것이 고친 것은 아니다**
〈2026-09-18 사장님: 「권하는 개선안대로 진행해」 — 제가 올린 권장 ①〉.

- **무엇이 남아 있었나.** 위 고침은 「이어서 중요한 일」의 **소스를 옮겨** 0건을 피했다.
  그런데 두 잣대가 갈려 있던 **그 자리(`buildPriorityQueue`)는 그대로**였다 —
  `마감:t.date||''` 와 `calcPriorityScore({date:t.date…})`.
  ★★★ **「오늘 먼저 할 일」(`b.priority`)은 여전히 그 큐에서 온다.** 그래서 `dueDate` 만 있는
    급한 일은 **점수 0** 이 되어 `점수>0` 필터에서 빠지고 **최우선으로 안 뽑혔다.**
    카드 하나가 안 비게 된 것과 **뿌리가 고쳐진 것은 다른 사실이다.**
- ★★ **고침은 «새 잣대를 만드는 것」이 아니라 있는 것을 부르는 것**이다 — 화면이 쓰는
  `_b2Due` 한 벌을 큐가 그대로 부른다. 두 벌로 적으면 한쪽이 옛말을 한다 (§8-1).
  ★ 프로젝트·딜 갈래는 **안 건드렸다** — 그쪽은 `p.dueDate`·`c.followUp` 으로 이미 한 벌이다.
    좁게 고친다 (§12-4 「가르는 열쇠는 좁게 시작한다」와 같은 잣대).
- ★ **고치는 방향이 반대로 가는 것도 함께 막는다** — `date` 만 있는 할 일이 오히려 깨지지
  않는지 같은 칸에서 잰다 (§4.6 의 그 잣대).
- ★★ **표본이 재려는 성질을 지켜야 한다** — `dueDate` 만 있는 할 일이 표본에 없으면
  이 갈래를 **영영 못 잰다.** 넷을 넣었다: `dueDate` 만 · `date` 만 · 날짜 없음 · 완료.
- 재는 자리: `linkpilot-platform` 의 `test-score-sourcing.js` (5 → **7칸**) — 큐를 **통째로
  떼어 내 돌린다**. **사보타주 셋**으로 확인했다(둘 다 되돌리기 · 마감만 · 점수만).

★★ **고정 시간으로 기다리는 검사는 «자리에 따라 다른 답»을 낸다 — 두 번째다**
〈같은 날 · 권장 ② · 실측: 같은 커밋에서 한 번 빨갛고 두 번 초록〉.

- `test-html-preview.js` 가 탭을 누른 뒤 **고정 3초**를 기다리고 읽었다. 내 자리에서는 됐는데
  1MB 문서를 받아 그리는 시간이 더 걸리는 자리에서는 틀을 **0개**로 읽어 빨개졌다.
  그리고 나는 그 빨강을 보고 **내 고침을 의심했다** — 기능은 멀쩡했다.
  ★ §12 조건 14·15 가 `test-brief-ux.js` 에서 이미 정한 규칙인데 **이 파일에는 안 댔다**
    (S-53 「한 칸에서 배운 것을 옆 칸에 안 대면 그 자리에 그대로 남는다」).
- ★★★ **고침은 기다림을 늘리는 것이 아니라 «설 때까지» 재는 것**이다(`until`).
  재려던 성질(얹히는가 · 눌러서 움직이는가)은 그대로다.
- ★★ **「있는가」로는 아무것도 못 기다린다** — 스크롤 값은 누르든 안 누르든 늘 있다.
  **이전 값과 견주어** 달라질 때까지 기다린다.
- ★★★ **그리고 「못 쟀다」를 「깨졌다」로 적지 않는다.** 틀의 실행 문맥이 사라지면
  `scrollY` 가 `undefined` 로 오는데, 앞 판은 그것을 **`undefined->undefined`** 로 찍고
  「개요를 눌러도 안 움직인다」로 적었다 — **깨진 것은 재는 자리**다. 이제 문맥을
  **그때그때 다시 집고**, 끝내 못 읽으면 그 사실을 그대로 적는다 (§8).
- ★ **기다림이 «통과»로 안 끝나는지 사보타주로 확인했다** — 누르는 줄을 지우니
  시간을 다 쓴 뒤 **다섯 칸이 빨개졌다.** 조용히 초록이 되지 않는다.

### 12-27. **「고정 시간으로 기다리는 검사」를 없애려다 배운 것 — «설 조건»을 잘못 고르면 더 나빠진다**
〈2026-09-18 사장님: 「권하는 개선안 대로 진행해」 — 제가 올린 권고 · 실측 · D-214〉

- **왜 했나.** §12-26 이 「고정 기다림은 자리에 따라 다른 답을 낸다」를 한 파일에서 고쳤고,
  **같은 모양이 다른 검사에도 남아 있었다** — 실측 **120곳**(그중 「누른 뒤」가 **78곳**).
  S-53 의 「한 칸에서 배운 것을 옆 칸에 안 대면 그 자리에 그대로 남는다」 그대로다.
- ★★★ **먼저 «재 봤고, 잰 값이 답을 바꿨다».** 그려서 재는 검사 열 개를 각 3회 —
  **30회 전부 초록**이었다. 지금 흔들리는 것은 **없다.**
- ★★★ **그리고 옮기다 «멀쩡한 검사를 깨뜨렸다»** 〈실측: 초록이던 것이 **16칸 빨강**〉.
  원인은 **설 조건을 잘못 고른 것**이다 — `document.readyState==='complete'` 와
  `innerWidth===390` 은 **앱이 다시 그리기 전에 이미 참**이라, 기다림이 **0ms** 가 되어
  그리는 중에 눌렀다. 고정 대기보다 **더 빨리** 읽는다.
  ★ **그래서 규칙이 하나 붙는다 — 설 조건은 「다음 칸이 실제로 쓸 것」이어야 한다.**
    「문서가 떴다」는 「앱이 그렸다」가 아니고, 「폭이 바뀌었다」는 「그 폭으로 다시
    그렸다」가 아니다 (§8 「걸었다 ≠ 올라갔다」와 **같은 결**).
  ★★ **되돌렸다.** 흔들리지도 않는 자리를 추측으로 옮겨 **잃는 것이 더 컸다.**
    「고칠 수 있다」와 「고쳐야 한다」는 다른 사실이다.
- ★ **그러므로 한 것은 셋이다** — ① 기다림 창구를 **한 벌**로 모으고(`gate-target` 의
  `sleep`·`until`·`untilChange`) ② §12-26 이 고친 파일이 **제 것 대신 그 창구를 부르게**
  하고(두 벌이면 한쪽이 옛말을 한다 · §8-1) ③ **래칫으로 늘지 않게 막았다**
  (파일마다 · 지금 **69곳**). **옮기는 것은 흔들리는 자리가 나왔을 때 그 자리부터**다.
- ★★ **이 수가 0 이 되는 것이 목표가 아니다.** 시간 자체를 재는 검사(느린지 빠른지)는
  **그 기다림이 재려는 대상**이다. 그래서 **늘면 막고, 진짜로 옮겼으면 낮춘다**
  (§12-18 의 「아직 안 옮겼다」와 「옮기면 안 된다」를 숫자와 사유로 가르는 방식과 같다).
- ★★★ **검사가 «자기 잣대»를 먼저 잰다.** 기준선만 보면 잣대를 무디게 한 순간 수가
  **줄어** 개선으로 보인다 — 일부러 만든 표본 다섯을 먼저 먹여 「누른 뒤」를 아직
  가르는지 본다 (§12-15 가 그 자리에서 실제로 당했다 · §6-2-6 의 46 → 105 와 같은 규칙).
- ★★★ **창구는 「낱말이 있는가」가 아니라 «돌려서» 잰다** — 값이 서면 그 자리에서 주는가 ·
  시간을 다 쓰면 **던지지 않고 `undefined`** 를 주는가(「못 기다렸다」와 「깨졌다」는 다른
  사실이다 · §8) · 재는 쪽이 던져도 이어서 묻는가 · **이전 값과 같으면 안 주는가**.
  마지막이 급소다 — **「있는가」로는 아무것도 못 기다린다**(스크롤·점 개수는 늘 있다).
- ★★ **사보타주에서 «표본이 두 번» 틀렸다** 〈실측〉. 고정 대기를 하나 더 넣었는데
  그 자리가 **「누른 뒤」가 아니어서** 안 잡혔고, 제 기다림을 심는 치환은 **그 파일에
  없는 글자**를 노려 아예 안 들어갔다. **둘 다 검사가 옳았고 틀린 것은 표본이다**
  (§8 「표본이 거짓말을 하면 잡히는 것도 거짓이다」 — 이 저장소가 그 자리에서 또 걸렸다).
- 재는 자리: `linkpilot-platform` 의 `test-fixed-wait.js` (교차검증 **118번째 칸**) ·
  기준선은 `fixed-wait-baseline.json`. 사보타주 다섯으로 확인했다.

### 12-28. **한 화면에 «두 글이 동시에» 떴다 — 물어본 자리가 두 벌이고, 실패를 굳혔다**
〈2026-09-18 사장님 지시: 「반복적인 오류 근본적인 해결책을 만들어줘」 · 화면 사진 · 판 `202609181831` · D-215〉

- **무엇이 났나.** 화면에 「이 서버에 Google 클라이언트가 등록돼 있지 않습니다 —
  [관리자 설정] 에서 등록해 주세요」와 「Google 로그인 창을 여는 중…」이 **나란히** 떠 있었다.
  **둘 다 참일 수 없다** (§8 「이웃한 두 칸이 서로 다른 말을 하면 그 자체가 사고 신호」).
  ★ 그리고 **제 목록이 화면과 달랐다** — 저는 「PHP `curl` 확장」을 막는 것으로 적어 왔는데
    화면은 「클라이언트 미등록」을 말했다. **재기 전에는 어느 쪽도 적지 않는다.**

- ★★★ **뿌리 ① — 「못 물었다」와 「없다」가 같은 값이었다** (§12-12 와 **같은 고장**).
  `?action=config` 는 서버에서 **로그인 없이도 늘 `ok:true`** 를 준다. 그러니 앱이 `ok` 를
  못 받는 것은 전부 **「못 물었다」**다 — 못 닿음 · 8초 초과 · 404/500 · JSON 이 아님.
  그런데 앱은 그 넷을 `{configured:false}` 로 접고 **「등록하세요」**라고 적었다.
  **그 자리에는 고칠 것이 없을 수 있다.**
- ★★★ **뿌리 ② — 그 실패를 «굳혔다». 그래서 «반복»이다.** 첫 조회가 실패하면 그 답이
  캐시에 박혀 **다시 누르셔도 같은 글**이 나온다. 화면을 새로 열기 전에는 안 바뀐다.
  ★ **굳히는 것은 «답이 온 것»뿐이다** — 못 물은 것은 안 굳혀야 다시 눌러 볼 수 있다.
- ★★★ **뿌리 ③ — 물어본 자리가 두 벌이고, 값 읽는 자리가 «경합»했다.**
  카드(`lp-member.js`)와 바깥(앱)이 **각자** 물었다 — 그래서 한쪽은 창을 열고 다른 쪽은
  「등록 안 됨」을 적었다. 그 위에, 카드가 `__lpGoogleClientId(v)` 로 값을 심고 **60ms 뒤**
  바깥 흐름을 부르는데 그 심기는 **React 상태**라 아직 반영 전이고, 바깥은 클로저에 잡힌
  **옛 빈 값**을 읽었다. **값이 기기에 멀쩡히 있는데도 「등록 안 됨」**이 떴고 오류는 한 줄도 안 났다.

- **고친 것 넷.**
  · **창구 한 벌** — `lpGcalCfg()` 가 **yes · no · unknown(왜 못 물었는지)** 셋으로 갈라 준다.
    갈래 파일도 같은 것을 부른다 (§8-1). `?action=config` 를 직접 부르는 자리가 **하나**인지 센다.
  · **실패를 안 굳힌다** — `yes`·`no` 만 굳히고 `unknown` 은 부를 때마다 다시 묻는다.
  · **글이 틀린 곳을 안 가리킨다** — `unknown` 은 **[연결 진단]**, `no` 만 **[관리자 설정]**.
    ★ **반대로도 막는다** — 정말 미등록인데 진단을 가리키면 그것도 틀린 곳이다.
  · **못 물은 상태에서는 로그인 창을 «안 연다»** — 열면 그 뒤에 「등록 안 됨」이 적혀
    두 글이 또 동시에 뜬다. 그리고 **관리자 칸도 그때는 안 그린다**(「내가 뭘 넣어야 하나」가 된다).
    그리고 client id 는 **상태가 아니라 그때 저장소에서** 읽는다.

- ★★ **이 고침이 «무엇을 고친 것이 아닌지» 적어 둔다.** 서버가 왜 `config` 를 못 주는지는
  **이 자리에서 못 잰다** — NAS 를 못 본다. 고친 것은 **화면이 어느 갈래인지 말하게** 한 것이고,
  그 답은 사장님이 **[연결 진단]** 을 누르시면 나온다. **못 잰 것을 통과로 적지 않는다** (§8).
- ★ **사보타주에서 «표본이 또 틀렸다»** — 가짜 `setTimeout` 이 콜백을 안 불러 「창을 안 열었다」가
  늘 참이 됐다. 실제 코드는 60ms 뒤에 연다. **검사가 옳았고 틀린 것은 표본이다**
  (§8 「표본이 거짓말을 하면 잡히는 것도 거짓이다」 — 이 저장소가 그 자리에서 또 걸렸다).
- 재는 자리: `linkpilot-platform` 의 `test-gcal-cfg.js` (교차검증 **119번째 칸**) —
  창구를 **떼어 내 가짜 망으로 여섯 갈래를 먹여 돌리고**, 카드의 연결 단추도 **돌려서** 잰다.
  사보타주 여섯으로 확인했다.

## 13. 달력·음력·절기·기념일 통합 〈2026-09-13 사장님 지침 v1.0 · 「정식 업무실행지침으로 정리」〉

**원문과 실행판은 [`docs/운영지침-달력음력절기기념일-v1.0.md`](docs/운영지침-달력음력절기기념일-v1.0.md)
한 곳에 있다.** 여기 옮겨 적지 않는다 — 두 벌이 되면 한쪽이 옛말을 한다 (§8-1 과 같은 결).

★ **화면은 `linkpilot-platform` 것이다** (§8-2). 이 저장소는 **지침 정본과 규칙**을 갖는다.

그 문서에서 **이 저장소의 일과를 바꾸는 것 여섯**만 여기 적는다.

- ★★★ **지침이 「기존 8개 설정항목」이라 부른 것 중 «넷은 우리 코드에 없다»** 〈실측〉 —
  **간지 · 세시풍속 · 메모 제목 보기 · 손 없는 날이 두 저장소에 0회**이고, **그 여덟을 켜고 끄는
  설정 스위치는 한 개도 없다.** 음력은 **명절만** 있고 날마다의 음력 날짜는 없다.
  ★ 그러므로 이 일은 **「재구성」이 아니라 «신규 개발»**이다. 그렇게 안 적으면 일정이 틀리게 잡힌다.
  ★★ 그리고 지침 Gate 2 「기존 설정값 100% 보존」은 **보존할 것이 없어** 그대로는 거짓 초록이 된다 —
    진짜 보존 대상은 **인사말·명언·배경 테마의 기억된 값**이다.
- ★★★ **낱말 겹침이 둘 늘어 «아홉»이 됐다.**
  · **「Gate 번호」 — 아홉째 겹침**: 이 지침이 Gate 0~8 을 또 둔다(여기서 **Gate 3 은 기능**,
    **Gate 8 이 승인**). **`CAL:Gate 3`** 으로 부른다. 접두어 없는 「Gate 3」은 여전히 §11 것이다.
  · **「3안」과 「세 줄」**: 지침의 `GREET:3안` 은 **짧음/따뜻함/격식 후보 셋**이고, §6-2-5 의
    `GREET:3줄` 은 **한 글을 이루는 세 줄**이다. **다른 축이다** — 안 가르면 「3안을 만들라」가
    세 줄을 쪼개는 일로 배선된다. 지금 있는 것은 `GREET:3줄` 이고 `GREET:3안` 은 **새로 만드는 일**이다.
- **앞 지시가 이긴다 — 둘.** 인사말 전달은 **`[SNS 공유]`** 다(§12-2 와 같은 잣대, 카카오톡은
  친구에게 바로 보내는 공개 창구가 없다). UI 이모지는 **1단계 시안 납품 대기 · 혼용 금지**라
  혼자 판단해 아이콘으로 바꾸지 않는다 (§6-3).
- ★★★ **`CAL:Gate 1`(데이터)은 «국내 자리 수집»이 한 번이라도 돌 때까지 안 열린다.**
  음력 날짜·잡절·대체공휴일·2029년 이후 명절이 전부 **손으로 적은 표**이고, 대체공휴일은
  **「일요일이면 다음날」 어림**이다. **어림을 공식값으로 적지 않는다** (§4 · D-206 과 같은 결).
  ★★★ **2026-09-13 — 「돌았다」는 됐고 「채워졌다」는 아직이다** 〈실측 · NAS 첫 실행〉.
    수집이 국내 자리에서 **실제로 돌았고 서버가 대답도 했다.** 다만 **받은 해가 0개**였다.
    ★ **그러니 이 칸을 「돌았으니 열렸다」로 적지 않는다.** 「돌았다」와 「채워졌다」는 다른 사실이고,
      스크립트도 그 둘을 되돌아오는 값으로 갈라 준다(값 4 · 값 5).
  ★★★ **2026-09-17 — «채워졌다»도 됐다. 그리고 「활용신청이 막고 있다」는 틀린 말이었다**
    〈실측 · 사장님이 Actions 에서 돌리신 실행〉. **2026년 135건 · 2027년 155건 · 2028년 150건**이
    왔다 — 그 서비스는 **이미 승인돼 있다.** 남은 2029·2030 은 **아직 관보로 확정되지 않은 해**라
    API 에 자료가 없는 것이고, **우리가 고칠 자리가 없다** (§4 의 「셋째 갈래」).
    ★ 그러므로 `CAL:Gate 1` 을 막던 것은 **활용신청이 아니라 「음력 날짜·잡절·대체공휴일을 그 값으로
      갈아 끼우는 배선」**이다. 막는 것의 이름이 바뀌었으니 그대로 적는다 — **없는 장애를 남겨 두면
      사장님이 이미 하신 일을 또 하신다** (M-86 과 같은 결).
- **24절기는 2026년에 스물넷 중 «스물»을 쓴다** — 자정에서 한 시간 안에 드는 넷은 안 쓴다
  (§6-2-5). **그 수를 늘리는 것이 개선 목표**이지, 다 쓰는 것이 목표가 아니다.
- ★★ **새 열쇠가 들어왔다** 〈2026-09-13 사장님: 「기상청_관측-통계 · 월드뉴스 · 공공데이터
  API 인증키 — 활용해줘」 · 같은 날 「APIS_DATA 이름으로 넣었어」 · 「WEATHER_GO 날씨정보 API
  걸어 넣었어」〉. 규격은 모르므로 **진단부터** 돌린다(§4.3).
  ★★★ **이름은 «화면에서» 읽는다 — 말씀으로 받은 이름을 그대로 박지 않는다** 〈실측〉.
    앞 판은 말씀으로 들은 `WORLD_NES_KEY` 를 적고 「`NEWS` 가 아니라 `NES` 다」라고까지
    적어 두었는데, **비밀 목록에 그 이름은 없었다.** 실제로 들어 있는 것은
    **`WORLDWIDE_NEWS`** 와 **`WORLD_NEWS_API`** 둘이다.
    ★ 그 줄을 그대로 뒀으면 다음 사람이 **없는 이름을 배선**하고, 그 값은 가려지지도 않아
      로그에 평문으로 샐 수 있었다 (§2). 「들었다」와 「들어 있다」는 다른 사실이다.
    ★★ **그리고 이 줄 자체가 그 덫이었다** — 고친 뒤에도 **이 한 줄만 옛말을 하고 있었다.**
      한 곳을 고치면 **같은 이름이 적힌 다른 자리도 함께 센다**.
  ★ **지금 읽는 이름 넷** — `WORLDWIDE_NEWS` · `WORLD_NEWS_API`(월드뉴스) ·
    **`APIS_DATA`**(공공데이터포털 — `DATA_GO_KR_KEY` 의 다른 이름, `connectors/datakey.js`) ·
    **`WEATHER_GO`**(날씨 — **어느 기관인지 아직 안 쟀다**. 가리고 나르기만 하고 배선은 안 했다).
  ★ 기상청 관측·통계로 **바람·기상특보**가 열릴 수 있지만 **도는 자리를 먼저 잰다** — 포털 계열은
    러너에서 안 열리고 API허브는 다른 호스트라 **재 보기 전에는 모른다.**

## 14. 명함등록 · 휴대전화 연락처 자동연동 〈2026-09-14 사장님 지침 v1.0 · 「정식 업무실행지침으로 정리」〉

**원문과 실행판은 [`docs/운영지침-명함등록-연락처연동-v1.0.md`](docs/운영지침-명함등록-연락처연동-v1.0.md)
한 곳에 있다.** 여기 옮겨 적지 않는다 — 두 벌이 되면 한쪽이 옛말을 한다 (§8-1 과 같은 결).

★ **화면은 `linkpilot-platform` 것이다** (§8-2). 이 저장소는 **지침 정본과 규칙**을 갖는다.

그 문서에서 **이 저장소의 일과를 바꾸는 것 여섯**만 여기 적는다.

- ★★★ **지침이 요구하는 «필수 식별자» 중 핵심 일곱이 두 저장소에 0회다** 〈실측〉 —
  `contact_id` · `company_id` · `device_contact_id` · `business_card_id` · `call_id` ·
  `link_status` · `approval_status`. 반면 **명함·OCR 자체는 이미 돈다**(「명함」 159회 ·
  「OCR」 93회 · `cardImage` 140회 · `lp_cimg` 55회).
  ★ 그러므로 이 일은 **둘로 갈린다** — 찍고 읽는 것은 **고치는 일**, 사람을 가리키는 번호를
    놓고 그것으로 잇는 것은 **새로 만드는 일**이다. 안 갈라 적으면 「연동만 하면 된다」로
    읽혀 **일정이 몇 배 틀린다** (§13 의 「재구성이 아니라 신규 개발」과 같은 결).
- ★★★ **재는 법이 «더 세는» 쪽으로도 틀린다** 〈실측 · 하나 잡았다〉. 처음에 `vCard` 가
  **10회**로 나왔는데 그 열 번이 전부 `srvCardRef` **안의 글자**였다 — 낱말 경계 없이 셌다.
  낱말 경계로 다시 세니 **0회**이고, 그대로 적었으면 **없는 기능을 있다고 보고**할 뻔했다.
  ★ 잣대는 그대로다 — **「그 숫자를 재는 법이 재려는 것을 다 덮는가」** (§6-2-6 의 46 → 105 ·
    §8 의 쪼개기 래칫과 **같은 규칙**이다. 그때는 덜 세었고 이번엔 더 세었다).
- ★★★ **낱말 겹침이 둘 늘어 «열하나»가 됐다.**
  · **「단계 번호」 — 열째**: 이 지침이 **1~6단계**를 또 둔다. **`CARD:1단계` … `CARD:6단계`**
    로 부른다. 접두어 없는 「Gate 3」은 여전히 §11 것이다.
  · **「연동」 — 열한째**: 이 지침의 「자동연동」은 **명함 ↔ 기기 연락처**이고, §6-2-4 의
    그것은 **바깥 서비스 OAuth 이어짐**이다. **`CARD:연동` · `OAUTH:연동`** 으로 부른다.
    ★ 안 가르면 §6-2-4 의 「로그아웃으로 끊지 않는다」가 **연락처 권한에 그대로 적용되어**,
      권한을 거두셔도 계속 동기화하는 코드가 된다 — 이 지침 §14 가 **금지**한 것이다.
- ★★★ **자동으로 합치지 않는다.** 전화번호 완전일치는 **연결 제안**, 이름+회사만 같으면
  **후보로만** 보인다. **서로 다른 인물을 자동통합하는 것은 HIGH 장애**다 —
  틀리면 그 사람의 연락처가 망가지고 **되돌릴 수가 없다** (§4.9 와 같은 결).
  ★ **검색 로그에 전화번호 원문을 안 남긴다.** 번호는 `SECRET_ENV` 에 없으므로
    **코드가 직접** 가려야 한다 — 열쇠와 달리 자동으로 안 가려진다 (§2).
- ★★ **웹에서 기기 연락처를 읽는 공개 창구는 «거의 없다».** `ContactsManager` 는 안드로이드
  크롬 일부에서만 돌고 **읽기 전용**이며 iOS 사파리는 아예 없다 — 실측으로 두 저장소에 **0회**다.
  ★ 그러니 **되는 길부터 적는다**(내보내기·딥링크로 사람이 저장). 안 되는 것을 단추로 그리면
    **눌러도 안 가는 것이 고장으로 읽힌다** (§8 · §6-2-5 와 같은 결).
  ★★ **권한을 거부해도 LinkPilot 안에는 저장된다** — 막다른 길을 만들지 않는다 (§6-3 ⑥).
- ★★★ **지금 막고 있는 것은 하나다 — `contact_id` 다.** 사람을 가리키는 번호가 없어
  통화·일정·할일·프로젝트·딜이 **이름 글자로** 이어져 있다. **이름이 같은 두 사람이 들어오면
  조용히 섞인다.** 그래서 지침 §16 의 차례(OCR·정규화가 2단계)보다 **식별자 놓기가 먼저**로
  보이는데, 차례를 바꾸는 것은 **사장님 판단을 받는다** (§4.9 — 자동으로 하나를 고르지 않는다).
