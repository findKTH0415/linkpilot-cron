# 삭제 후보 — 보고서 생성과 무관한 기능

작성 2026-08-14 (KST) · **아직 아무것도 지우지 않았다.** 승인 후 실행한다.

기준: **「보고서 생성」 체인이 끊기면 안 된다.**
체인 = `reports.html` → `report-api.cjs` → `run-queue.cjs` → `cli.js` → `pipeline.js` → Agent 11종

> 지운 것은 git 이력에 남아 되살릴 수 있다. 되돌릴 수 없는 건 **B군을 지웠을 때
> 멈추는 운영**이다. A군과 B군을 같은 무게로 보면 안 된다.

---

## 유지 — 지우면 보고서 생성이 죽는다

의존관계를 코드로 추적한 결과다. 아래는 삭제 대상이 아니다.

| 대상 | 이유 |
|---|---|
| `im-agent/agents/` **11개 전부** | `core/registry.js:23-33` 이 11개를 전부 파이프라인에 등록한다. 하나 빠지면 순서가 깨진다 |
| `im-agent/core/` 19모듈 | `pipeline.js` 가 직접 부른다 (runtime·facts·store·gate·outputspec·monitor·reports…) |
| `im-agent/design/` | 보고서 레이아웃(A4)·테마·서식 추천 |
| `im-agent/connectors/` | `07_geo` 가 쓴다 (부동산 보고서일 때만 — 아래 C-1 참조) |
| `im-agent/ui/report-api.cjs` · `run-queue.cjs` · `api-router.cjs` | 생성 API·실행 큐·읽기 API |
| `platform/reports.html` | **보고서 생성 화면 본체** |
| `platform/gate-core.js` | `reports.html` 이 로드한다 (무료/유료 판정) |
| `platform/catalog.js` | `reports.html` 이 로드한다 + 서버 `DOC_PLANS` 와 정합성 테스트 |
| `platform/inapp.js` | `reports.html` 이 로드한다 (인앱 브라우저 배너) |

---

## A군 — 삭제 가능 (보고서 생성과 무관, 안전)

의존하는 쪽이 없음을 확인했다. 지워도 `reports.html` 은 그대로 뜬다.

### A-1. 업무지시 보드 · 716줄

| 파일 | 줄 |
|---|--:|
| `platform/board.html` | 562 |
| `platform/board-core.js` | 154 |
| `test/board.test.js` | (20건) |

- 어차피 `api: null` 로 **데모 데이터로만 돈다.** 새로고침하면 되돌아간다
- `board-core.js` 는 `gate-core.js` 에 의존하지만 **역방향 의존은 없다** — 지워도 게이트는 멀쩡하다

### A-2. 멤버십 안내 화면 · 1,019줄

| 파일 | 줄 |
|---|--:|
| `platform/membership.html` | 533 |
| `platform/upgrade.html` | 486 |

- **요금제 안내 화면일 뿐이다.** 보고서 생성의 유료 판정은 `gate-core.js` + 서버 `report-api.cjs` 가 하며 이 화면들과 무관하다
- 결제 연동도 없다 (`billingUrl: null`)
- ⚠️ `guide.test.js:92` 가 이 두 파일의 **결제 입력 필드 부재**를 검사한다 → 해당 테스트만 함께 제거

### A-3. 외부 업무지침 화면 · 530줄

| 파일 | 줄 |
|---|--:|
| `platform/guide.html` | 530 |

- 지침 전문을 보여주는 읽기 전용 화면
- ⚠️ `guide.test.js` 는 **남긴다.** 이 테스트의 핵심은 화면이 아니라
  `catalog.js` ↔ 서버 `DOC_PLANS` 등급 일치 검사다. `guide.html` 을 읽는 3건만 제거

### A-4. LinkPilot 플랫폼 화면 · 3,323줄

| 파일 | 줄 |
|---|--:|
| `platform/linkpilot-platform.html` | 2,174 |
| `platform/app.js` | 782 |
| `platform/app.css` | 260 |
| `platform/build.js` | 107 |
| `test/platform.test.js` | (18건) |

- 오늘 / 프로젝트 / 인맥 탭은 보고서 생성과 무관
- 🔶 **단, 이 파일 안에 「IM 제작현황」(Control Tower) 탭이 들어 있다.**
  그건 보고서 생성 진행률 화면이다. 통째로 지우면 진행 상황을 볼 곳이 없어진다
  → **C-2 참조. 이 항목은 통째 삭제가 아니라 분리가 맞다**

**A군 합계 ≈ 5,588줄 · 테스트 38건**

---

## B군 — 아침 크론 일체 (⚠️ 지우기 전에 반드시 확인)

**보고서 생성과는 전혀 무관하다.** 그러나 —

> ### 이 저장소의 존재 이유가 아침 크론이다
> CLAUDE.md §1: "LinkPilot CEO 아침 브리핑을 매일 생성하여 카카오톡으로 발송하는
> GitHub Actions 크론 자동화 저장소다."
> **지금 운영 중이며, 지우면 내일 아침부터 브리핑이 오지 않는다.**

| 파일 | 줄 | 내용 |
|---|--:|---|
| `send-morning-brief.js` | 232 | 아침 브리핑 발송 (**운영 중**) |
| `send-morning-card.js` | 179 | 모닝카드 발송 (**운영 중**) |
| `fetch-daily-quote.js` | 211 | 오늘의 한줄생각 (**운영 중**) |
| `fetch-laws.js` | 60 | 주간 법령 수집 (**운영 중**) |
| `render-card.js` | 99 | 카드 이미지 생성 |
| `fonts/` | — | **6.6 MB** (카드 렌더링용 3종) |
| `.github/workflows/morning-brief.yml` 외 3 | — | 크론 스케줄 |
| `test/cron.test.js` | — | (12건) |
| `alert-failure.js` + `.yml` | 171 | 실패 알림 — 🔶 `deploy-nas.yml` 도 쓴다 |

**B군 합계 ≈ 781줄 + 6.6MB**

- [ ] **B군은 지우지 않는다** (권장 — 보고서 생성만 남기려면 크론을 **다른 저장소로 옮기는 편**이 낫다)
- [ ] B군 전부 삭제 → CLAUDE.md §1~§4 를 보고서 생성 기준으로 다시 써야 한다
- [ ] `alert-failure` 만 남기고 삭제 (배포 워크플로가 계속 쓴다)

---

## C군 — 판단 필요

### C-1. 부동산 전용 Agent 3종 + Connector · 4,222줄

| 대상 | 줄 |
|---|--:|
| `agents/07-geo.js` · `08-appraisal.js` · `09-massing.js` | (agents 2,789 중 다수) |
| `connectors/` (vworld·molit·nsdi·pnu·cache·http·xml) | 901 |

- `registry.js:37` 이 `REAL_ESTATE_AGENTS` 로 묶어 두었고, **데이터가 없으면 경고만 남기고 건너뛴다**
- **보고서가 부동산개발 IM 이면 유지, 아니면 삭제 대상**
- 지우면 D-15(VWorld 진단)·B-4(공공데이터 스키마 검증)·Secret 2종이 함께 불필요해진다

**보고서 종류가 무엇인지에 따라 갈린다.** 먼저 답이 필요하다.

### C-2. IM 제작현황(Control Tower) — 분리 대상

| 파일 | 줄 | 판단 |
|---|--:|---|
| `ui/lib.js` · `ui/controlTower.css` | (ui 1,739) | 보고서 진행률 → **유지** |
| `ui/vanilla/control-tower.js` | 2,910 중 | 같은 화면의 순수 JS 판 → **한 판만 남기면 절반 삭제** |
| `test/ui-parity.test.js` | (4건) | 두 판이 같은지 검사 — 한 판만 남기면 불필요 |

- 본체가 React 면 `ui/lib.js`, 단일 HTML 이면 `vanilla/` — **둘 중 하나만 쓴다**
- 본체 소스가 없어 어느 쪽인지 정할 수 없다 → **본체 확인 후 한 판 삭제**

### C-3. NAS 배포 워크플로

`deploy-nas.yml` — 보고서 화면을 NAS 에 올리는 경로다. A군을 지워도 `reports.html`
배포에 계속 쓴다. **유지 권장.**

---

## 요약

| 군 | 내용 | 규모 | 판단 |
|---|---|--:|---|
| **A** | 보드·멤버십·지침·플랫폼 화면 | 5,588줄 / 테스트 38 | 삭제 가능 (A-4 는 분리) |
| **B** | 아침 크론 일체 | 781줄 / 6.6MB | ⚠️ **운영 중** — 삭제 전 확인 |
| **C-1** | 부동산 Agent + Connector | 4,222줄 | 보고서 종류에 따라 결정 |
| **C-2** | Control Tower 두 판 | 약 1,400줄 | 본체 확인 후 한 판 삭제 |

**A군만 지우면 약 5,600줄이 빠지고 보고서 생성은 그대로 동작한다.**
B군까지 지우면 저장소 성격이 바뀌므로 CLAUDE.md 를 다시 써야 한다.

---

## 실행 전 필요한 답 3개

1. **B군(아침 크론)을 지우는가?** — 지우면 내일 아침 브리핑이 멈춘다
2. **보고서 종류가 부동산개발 IM 인가?** — 아니면 C-1(4,222줄)도 삭제 대상
3. **본체가 React 인가 단일 HTML 인가?** — C-2 에서 어느 판을 남길지 정해진다
