# 자동 배포를 켜는 법 — **tailnet 열쇠 한 길 + NAS 셋**

| | |
|---|---|
| **작성일** | 2026-08-21 (KST) |
| **왜** | 지금까지 배포는 사람이 손으로 쳤다. 개발 세션에서는 NAS 로 나가는 길이 막혀 있어 **「코드는 올렸는데 화면은 옛 판」**이 반복됐다 |
| **상태** | ✅ **켜졌다** 〈2026-08-21〉 — Secret 넷을 넣고 러너가 tailnet 을 거쳐 NAS 에 실제로 닿는 것까지 확인했다 |

---

## 0. 지금 어디까지 됐나 — **실측** 〈2026-08-21 갱신〉

| | 확인 | 결과 |
|:--:|---|---|
| ① | 개발 세션에서 워크플로를 띄울 수 있나 | ✅ **된다** |
| ② | 올릴 파일이 검증을 지나나 | ✅ **된다** — 이름·크기까지 찍혔다 |
| ③ | Secret 넷이 들어왔나 | ✅ **다 찼다** (`Check secrets` · dry run) |
| ④ | 러너가 tailnet 에 붙나 | ✅ **붙는다** (`TS_AUTHKEY` · auth key 길) |
| ⑤ | 러너가 NAS 에 ssh 로 닿나 | ✅ **닿는다** (`echo ok` · probe) |
| ⑥ | **실제로 올라가나** | ✅ **올라갔다** (`report-flow.html` → 웹 루트) |
| ⑦ | 올라간 것이 **같은 것인가** | ✅ **지문 일치** (sha256 · 40,658 bytes) |

**넷이 전부 맞다는 뜻이다** — 열쇠·주소·계정·출입증.

★★ ⑦ 을 따로 세는 이유: **「보냈다」는 「같다」가 아니다.** 「전송 성공」이
찍힌 서버가 통째로 옛 판이었던 적이 있다 (MEMORY.md M-12). 그래서 올린 뒤
**받는 쪽에서 지문을 다시 재고** 대조했다.

★ ①~⑤ 는 NAS 를 바꾸지 않는다. ⑥ 부터가 실제로 덮어쓴다 (백업은 자동).

### 켜기까지 넘어진 자리 — **여섯**

같은 종류가 반복됐다. **「확인하는 방법이 곧 위험한 행동」**이거나,
**「붙여 넣으라고 준 칸에 붙여 넣을 수 없는 것이 적혀 있었다」**이다.

| | 무슨 일 | 어디에 반영했나 |
|:--:|---|---|
| 1 | 콘솔에 OAuth 자리가 없었다 | 열쇠를 **두 길로** 받는다 (1-⑧) |
| 2 | dry run 이 열쇠를 아예 안 봤다 — 확인하려면 **올려 보는 수밖에** | `Check secrets` 가 **늘 돈다** (③-④) |
| 3 | 열쇠가 통하는지 아는 첫 방법이 **운영 화면 덮기**였다 | `probe` — 닿는 것까지만 (③-④ 2단) |
| 4 | 워크플로 기본값이 **없는 파일**을 가리켰다 | 검사가 지킨다 (`workflow-default.test.js`) |
| 5 | 명령의 따옴표가 **휘어진 따옴표**로 바뀌어 셸이 멈췄다 | ② 절에서 **따옴표를 걷어냈다** (2-⓪-2) |
| 6 | 「보는 값」을 **주소창에 넣었다** · 새 창이라 **값이 비었다** | 2-⓪-2 · 3) 붉은 상자 |

---

## 0-1. **화면을 올리는 길은 둘이다** — 어느 쪽이든 된다 〈2026-08-21 추가〉

Secret 을 기다리는 동안 **화면은 지금도 올릴 수 있다.** 둘의 차이만 알면 된다.

| | **사람이 손으로** (지금 바로 된다) | **Actions 로** (Secret 넷 필요) |
|---|---|---|
| 무엇을 올리나 | 화면 사본 (`im-flow`) | 화면 사본 **+ 엔진** |
| 어디서 | 내 Mac 에서 두 저장소를 오가며 | 저장소 Actions 단추 하나 |
| 지문 대조 | 앱 쪽 게이트가 한다 | `verify:nas` 가 한다 |
| 언제 쓰나 | **화면만 고쳤을 때** | 엔진까지 고쳤을 때 · 손이 못 닿을 때 |

**손으로 올리는 최단 경로** — 경로를 한 번만 정해 두면 그 뒤로는 세 줄이다:

```bash
export LP_APP_DIR="$HOME/…/24.홈페이지"      # ← 한 번만. ~/.zshrc 에 넣어 둔다
cd ~/linkpilot-cron && npm run im:embed      # 사본을 앱 폴더로 낸다
cd "$LP_APP_DIR" && node test-integration-contract.js && python3 deploy-filestation.py
```

★★ **자리표시를 그대로 붙여 넣어 죽은 적이 있다** (2026-08-21). 그래서
`npm run im:embed` 는 자리표시·없는 폴더·`im-flow` 아닌 곳을 **거절하고 무엇이
잘못됐는지 말한다.** 그래도 첫 줄의 경로는 **직접 고쳐야 한다** — 그 한 곳뿐이다.

★ **엔진을 고쳤으면 이 길로는 안 간다.** `deploy:nas` 나 Actions 가 필요하다.
  화면만 올리고 엔진이 옛 판이면 **화면이 없는 API 를 부른다.**

---

## 1. 넣어야 하는 것 — 저장소 `Settings › Secrets and variables › Actions`

### 화면만 올리려면 — **열쇠 한 길 + NAS 셋**

★★ **tailnet 열쇠는 ㉮ 나 ㉯ 중 한 길만** 넣는다 (①-⑧). 둘 다 넣을 필요가 없고,
둘 다 있으면 ㉮ 를 쓴다.

**㉯ 로 가면 넷** — 콘솔에 OAuth 자리가 없을 때의 길이다:

| Secret | 무엇 | 없으면 |
|---|---|---|
| `TS_AUTHKEY` | Tailscale auth key (Reusable · Ephemeral · tag:ci) | **지금 여기서 멈춘다** |
| `NAS_SSH_HOST` | NAS 의 **tailnet** 주소 (2-⓪-1) | 붙어도 어디로 보낼지 모른다 |
| `NAS_SSH_USER` | ssh 계정 | 〃 |
| `NAS_SSH_KEY` | ssh **개인키 전문** | 〃 |

**㉮ 로 가면 다섯** — `TS_AUTHKEY` 자리에 `TS_OAUTH_CLIENT_ID` 와
`TS_OAUTH_SECRET` **둘 다**가 들어간다. 한쪽만 넣으면 안 된다.

★ `NAS_SSH_*` 셋은 어느 길이든 똑같이 필요하다 (D-12 목록).

### 엔진까지 올리고 **잰 것을 남기려면** — 셋 더

| Secret | 무엇 | 없으면 |
|---|---|---|
| `LP_BASE` | 공개 주소 (`https://…`) | **올리긴 했는데 잰 것이 없다.** 지문 대조를 못 해 「초록인데 옛 판」을 못 잡는다 (M-12) |
| `NAS_WEB_ROOT` | 웹 루트 (기본 `/volume1/web`) | 기본값으로 간다. 다르면 엉뚱한 곳에 올라간다 |
| `NAS_ENGINE_ROOT` | 엔진 루트 (기본 `/volume1/docker/linkpilot`) | 〃 |

> **값은 여기에도, 코드에도, 커밋에도 적지 않는다.** GitHub Secrets 에만 넣는다
> (CLAUDE.md §2 · D-10). 워크플로에 주소가 한 글자도 없는 것을 검사가 지킨다.

---

## 1-1. 먼저 — **이게 다 무슨 이야기인가** (쉬운 말로)

낯선 낱말이 여러 개 나옵니다. 하나씩 풀면 이렇습니다.

### 왜 이 일이 필요한가

지금 상황을 사람에 비유하면 이렇습니다.

> 제가 **원고를 다 썼는데**, 그 원고를 **인쇄소(NAS)에 갖다 줄 수가 없습니다.**
> 인쇄소가 회사 건물 안에 있고 저는 출입증이 없기 때문입니다.
> 그래서 **심부름꾼(GitHub 서버)** 에게 맡기려는 것인데,
> 그 심부름꾼도 **출입증이 있어야** 건물에 들어갑니다.
>
> **지금 넣어 달라는 넷이 그 출입증입니다.**

### 낱말 풀이 — 다섯 개만 알면 됩니다

| 낱말 | 쉬운 말 |
|---|---|
| **NAS** | 사무실에 있는 저장 장치. 여기에 화면 파일이 올라가야 앱에 반영됩니다 |
| **Tailscale** | 사무실 밖에서도 NAS 에 안전하게 들어가게 해 주는 **전용 통로**. 이 통로가 있어야 바깥 컴퓨터가 NAS 를 볼 수 있습니다 |
| **OAuth 클라이언트** | 그 통로를 쓸 수 있는 **출입증 한 장**. 사람 계정이 아니라 **기계 전용** 출입증입니다 |
| **ACL** (액세스 컨트롤) | 「누가 어디까지 들어갈 수 있나」를 적어 둔 **출입 규정표** |
| **SSH 키** | NAS 문을 여는 **열쇠**. 두 조각으로 나옵니다 — **공개키**(자물쇠)는 NAS 에 붙이고, **개인키**(열쇠)는 GitHub 금고에 넣습니다 |

### 그래서 하는 일이 셋입니다

```
① 출입 규정표에 「CI 라는 이름표를 단 기계는 들어와도 된다」를 적는다   (Tailscale)
② 그 이름표를 단 출입증을 한 장 발급한다                              (Tailscale)
③ NAS 문 열쇠를 만들어, 자물쇠는 NAS 에 붙이고 열쇠는 GitHub 금고에    (SSH)
```

그다음 **GitHub 금고(Secrets)** 에 넷을 넣으면 끝입니다.

> **금고에 넣는 이유**: 출입증과 열쇠를 코드에 적으면 **누구나 볼 수 있습니다.**
> 이 저장소는 공개(public)라 더 그렇습니다. GitHub Secrets 는 넣고 나면
> **본인도 다시 못 봅니다** — 그게 정상이고, 그래서 안전합니다.

### 얼마나 걸리나

처음 하시면 **15~20분**, 막히면 30분쯤 봅니다. 막히는 자리는 아래에 미리 표시해 뒀습니다.

---

## 1-2. 어떻게 만드나 — **순서대로**

> **값을 채팅·커밋·문서에 붙여넣지 않는다.** GitHub Secrets 칸에 **직접** 넣는다.
> 한 번 넣으면 다시 못 본다 — 그것이 정상이다.

### ① Tailscale OAuth — `TS_OAUTH_CLIENT_ID` · `TS_OAUTH_SECRET`

**★ 순서가 중요하다. 태그를 먼저 만들지 않으면 ②에서 막힌다.**

> **2026-08-21 개정 — 실제로 따라 하다 세 번 넘어졌다.** 넘어진 자리를 그대로
> 반영했다. 앞 판은 ⓐ 콘솔 메뉴 이름이 옛것이었고 ⓑ 예시에 **자리표시**(`<…>` ·
> `...`)를 남겨 두어 그대로 붙여 넣게 만들었다. **이 절에는 자리표시가 없다 —
> 적힌 그대로 치면 된다.**

#### 1-①. 무엇을 하는 것인가

> 심부름꾼(GitHub 서버)에게 **출입증**을 주려고 합니다. 그런데 출입증을 만들려면
> **먼저 「이런 이름표를 단 기계는 들어와도 된다」는 규정이 있어야** 합니다.
> 규정에 없는 이름표는 출입증에 찍을 수가 없습니다.
>
> **`tag:ci` 가 그 이름표이고, 규정표에 그 이름을 올리는 것이 이 단계입니다.**

| 낱말 | 쉬운 말 |
|---|---|
| **태그(tag)** | 기기에 붙이는 **이름표.** 사람 계정이 아니라 기계에 붙인다 — CI 러너는 매번 새로 생겼다 사라지므로 사람 계정을 줄 수 없다 |
| **`tagOwners`** | 「이 이름표를 **누가 붙일 수 있나**」를 적는 칸 |
| **정책 파일** | 출입 **규정표 전체.** 콘솔에서 글로 고친다 |

#### 1-②. ★ 맥에 깔린 **Tailscale 앱에서는 못 한다**

메뉴막대의 Tailscale 아이콘 › **Settings** 창에는 이 기능이 **없다.**
**브라우저**로 관리자 콘솔에 들어가야 한다.

```
https://console.tailscale.com
```

★ 옛 주소(`login.tailscale.com/admin`)도 여기로 넘어온다.

#### 1-③. 어디를 눌러야 하나

왼쪽 차림표 **`Policies`** › **`JSON editor`**

★ 콘솔이 새로 바뀌면서 **`Access controls` 라는 이름은 없어졌다.** `Policies` 가
그것이고, 글로 고치는 곳은 그 아래 **`JSON editor`** 다.

★ `Policies` 첫 화면의 **`All users and devices → All ports and protocols`** 줄이
기본 「전부 허용」 규칙이다. **지우지 않는다** — 지우면 본인 접속이 끊긴다.

#### 1-④. ★★ 시작 전에 — **지금 내용을 통째로 복사해 둔다** 🔴

글상자 내용을 전부 복사해 메모장에 붙여 두십시오. 30초면 되고, **잘못되면
그대로 되돌리면 된다.** 아래 1-⑦ 의 사고 둘이 전부 이 백업으로 해결된다.

#### 1-⑤. **넣을 것 — 이 세 줄이 전부다**

```
	"tagOwners": {
		"tag:ci": ["autogroup:admin"],
	},
```

**넣는 자리**: 파일 **맨 위의 여는 `{` 바로 다음 줄.**

파일은 이렇게 생겼다. 1번 줄은 주석이고, **`{` 는 그 아래**에 있다:

```
// Example/default ACLs for unrestricted connections.
{
```

넣고 나면 이렇게 된다:

```
// Example/default ACLs for unrestricted connections.
{
	"tagOwners": {
		"tag:ci": ["autogroup:admin"],
	},
	// (원래 있던 내용이 여기부터 그대로 이어진다 — 아무것도 지우지 않는다)
```

★★ **여는 `{` 안에 넣는다.** 파일 끝에 붙이면 **덩어리가 둘**이 되어 저장이 거부된다.
★ 이 파일은 **주석(`//`)과 끝쉼표를 허용**한다 (HuJSON). 보통 JSON 과 다르다.
★ 원래 파일에 `tagOwners` 가 **주석으로** 들어 있을 수 있다(`// "tagOwners": {`).
  그건 그냥 두면 된다 — 주석은 세지 않는다. **주석이 아닌 것이 둘이면** 거부된다.

#### 1-⑥. 저장 — **오류가 나면 줄 번호를 읽는다**

위쪽 **`Preview changes`** 로 늘어난 것이 `tagOwners` 뿐인지 보고, **`Save`**.

오류는 **몇 번째 줄인지 정확히 알려 준다.** 그 줄만 보면 된다.

#### 1-⑦. 실제로 넘어진 자리 — **셋**

| 오류 · 증상 | 무슨 일인가 | 고치는 법 |
|---|---|---|
| 저장이 안 되고 파일에 `{` 가 **두 번** 보인다 | 파일 **끝에** 붙였다. 정책 파일은 중괄호 **한 쌍**이어야 한다 | 붙인 덩어리를 지우고, **여는 `{` 다음 줄**에 다시 넣는다 (1-⑤) |
| `parsing value: unexpected EOF` | 잘못 붙인 것을 지우면서 **맨 끝 `}` 까지 지웠다** | 마지막 줄에 **`}` 한 글자**를 넣는다 |
| `Tags` 칸에 `tag:ci` 가 안 뜬다 | `tagOwners` 가 저장되지 않았다 | 1-⑤~⑥ 을 다시 한다 |

★ 무엇이든 꼬이면 **1-④ 에서 복사해 둔 내용을 그대로 붙여 넣고 저장**하면
원래대로 돌아간다.

#### 1-⑧. 출입증을 만든다 — **길이 둘이다. 되는 쪽으로 간다**

★★ **콘솔 판에 따라 OAuth client 를 못 만드는 경우가 있다** 〈2026-08-21 실측〉.
`Settings › Keys` 에 **Auth keys** 와 **API access tokens** 둘뿐이고 OAuth 자리가
없었다. 그래서 **워크플로가 둘 다 받도록 고쳤다** — 만들 수 있는 쪽으로 가면 된다.

| | **㉮ OAuth client** | **㉯ Auth key** |
|---|---|---|
| 만료 | **없다** | **있다** (최대 90일 — 때가 되면 다시 만든다) |
| 어디에 | `Settings › Trust credentials` 로 보인다 | `Settings › Keys` › `Generate auth key…` |
| Secret 이름 | `TS_OAUTH_CLIENT_ID` · `TS_OAUTH_SECRET` | `TS_AUTHKEY` |

**㉮ 를 먼저 찾아본다.** 만료가 없어서 손이 덜 간다. 없으면 **㉯ 로 간다 —
지금 바로 되고, 되는 것이 안 되는 것보다 낫다.**

**㉮ OAuth client** — `Settings` › **`Trust credentials`**

| 고를 것 | 값 |
|---|---|
| Scope | **`Auth Keys`** — `write` |
| Tags | **`tag:ci`** |

**㉯ Auth key** — `Settings` › **`Keys`** › **`Generate auth key…`**

**★ 셋을 반드시 켠다. 하나라도 빠지면 붙어도 안 된다.**

| 켤 것 | 왜 |
|---|---|
| **Reusable** | 배포할 때마다 쓴다. 1회용이면 두 번째 배포가 죽는다 |
| **Ephemeral** | 러너는 끝나면 사라진다. 안 켜면 **기기 목록에 시체가 쌓인다** |
| **Tags** → **`tag:ci`** | 이름표가 없으면 정책이 안 걸린다 — **1-⑤ 가 헛일이 된다** |

- 만료(Expiration)는 고를 수 있는 **가장 길게** 잡는다. 달력에 적어 둔다.
- ★★ **`Tags` 칸에 `tag:ci` 가 뜨는지**가 1-⑤ 성공의 확인표다. 안 뜨면 1-⑦.

> ★★ **열쇠 값은 만드는 화면에서 한 번만 보인다.** 창을 닫으면 다시 못 본다.
> **그 자리에서 바로** GitHub 에 붙여넣는다 (③). 잃어버리면 지우고 새로 만든다.

★ **사람 계정 로그인 정보를 CI 에 주지 않는다.** 이 열쇠 하나만 준다. 나중에
이것만 지우면 CI 접근이 깨끗이 끊긴다.

#### 1-⑨. (선택) 나중에 **좁힌다** — 성공한 뒤에

기본 정책이 「전부 허용」이라 위까지만 해도 배포는 돈다.
**한 번 성공한 뒤에** 좁히는 것을 권한다.

★★ **문법이 두 가지다.** 파일을 열어 **어느 쪽이 있는지 먼저 보고** 그쪽에 맞춘다.

`"grants"` 가 있으면 (새 문법):

```
	{"src": ["tag:ci"], "dst": ["NAS기기이름"], "ip": ["22"]},
```

`"acls"` 가 있으면 (옛 문법):

```
	{"action": "accept", "src": ["tag:ci"], "dst": ["NAS기기이름:22"]},
```

- `NAS기기이름` 은 콘솔 **`Machines`** 목록에 있는 이름을 그대로 쓴다.
- ★ **성공하기 전에 좁히지 않는다.** 안 될 때 원인이 둘(정책·열쇠)이 되어
  어느 쪽인지 가릴 수가 없다.

### ② NAS ssh 열쇠 — `NAS_SSH_HOST` · `NAS_SSH_USER` · `NAS_SSH_KEY`

#### 2-⓪. **네 값이 각각 어디서 나오나** 〈2026-08-21 추가〉

넷을 한꺼번에 보면 헷갈린다. **나오는 곳이 서로 다르다.**

| Secret | 어디서 나오나 | 한 줄로 |
|---|---|---|
| `TS_AUTHKEY` | **Tailscale 콘솔**에서 **만든다** | ①-⑧ ㉯ 에서 새로 발급받는 문자열 |
| `NAS_SSH_HOST` | **Tailscale 콘솔 `Machines`** 목록에서 **베껴 온다** | 이미 있는 값이다. 만드는 것이 아니다 |
| `NAS_SSH_USER` | **이미 알고 있다** | NAS 에 로그인할 때 쓰는 계정 이름 |
| `NAS_SSH_KEY` | **내 Mac 에서 만든다** | 아래 2)에서 만드는 개인키 파일의 내용 전체 |

#### 2-⓪-1. ★★ `NAS_SSH_HOST` 는 **tailnet 주소**다 — 여기서 대개 막힌다 🔴

> **집·사무실 안에서 쓰던 주소를 넣으면 안 된다.** `192.168.…` 같은 내부 주소나
> DDNS 주소(`…synology.me`)를 넣으면 **러너가 영영 못 찾는다.** 그런데 오류가
> 「접속 시간 초과」로만 나와서 **열쇠가 틀린 것처럼 보인다.**
>
> 러너는 남의 컴퓨터다. 그 컴퓨터가 NAS 에 닿는 길은 **tailnet 하나뿐**이므로
> 주소도 tailnet 것이어야 한다.

**어디서 보나**: 브라우저 `https://console.tailscale.com` › 왼쪽 **`Machines`**

- NAS 줄의 **이름**(예: `nas`)이나 그 옆의 **`100.` 으로 시작하는 주소**, 둘 중
  아무거나 쓰면 된다.
- ★ **NAS 가 목록에 없으면 여기서 멈춘다.** NAS 에 Tailscale 이 안 깔린 것이다.
  DSM › **패키지 센터** 에서 Tailscale 을 설치하고 로그인해야 목록에 뜬다.


> **아래 명령에는 자리표시가 없다.** 맨 처음 두 줄만 자기 값으로 바꿔 두면,
> 그다음은 **적힌 그대로 붙여 넣으면 된다.** (①에서 자리표시를 그대로 붙여
> 넣어 세 번 넘어졌다 — 같은 일을 여기서 되풀이하지 않는다.)

#### 2-⓪-2. 🔴 **따옴표를 쓰지 않는다** 〈2026-08-21 · 실제로 여기 갇혔다〉

> 앞 판은 명령에 따옴표가 들어 있었다. 그대로 붙여 넣었더니 셸이 이렇게 멈췄다:
>
> ```text
> dquote>
> ```
>
> **「따옴표 짝이 안 맞으니 나머지를 마저 치라」**는 뜻이다. 원인은 **메모(Notes)
> 앱**이다 — 「똑똑한 따옴표」가 기본으로 켜져 있어 곧은 따옴표를 **휘어진
> 따옴표로 자동으로 바꾼다.** 눈으로는 거의 같은데 셸은 그것을 따옴표로 안 본다.
> 안내서를 메모에 옮겨 두고 거기서 복사하면 걸린다.
>
> 빠져나오려면 **Control + C**.

★★ **그래서 이 절의 명령에는 따옴표가 하나도 없다.** 넣을 값에 띄어쓰기가
없으니 애초에 필요가 없었다. **없앨 수 있는 함정은 설명하지 말고 없앤다.**

**0) 먼저 두 값을 정해 둔다** — 이 창에서만 쓰는 임시 이름이다

```bash
NAS_USER=여기에NAS계정이름
NAS_HOST=여기에tailnet주소
echo $NAS_USER@$NAS_HOST
```

★ **위 두 줄의 `=` 뒤만 자기 값으로 바꾼다.** 그 두 곳이 전부다.
★ **마지막 줄이 찍는 값을 눈으로 확인한다.** 여기가 틀리면 아래가 전부 헛돈다.
  여기서부터는 **적힌 그대로** 붙여 넣으면 된다.

> 🔴 **찍힌 값은 「보는 것」이지 「넣는 것」이 아니다** 〈2026-08-21 · 실제로 그랬다〉
>
> `계정@주소` 꼴로 찍히니 **주소처럼 보인다.** 실제로 그것을 브라우저 주소창에
> 넣어 검색 결과가 뜬 일이 있었다. 어디에도 넣지 않는다 — 내 컴퓨터가 **어느
> NAS 에 어느 계정으로** 붙을지 제대로 기억했는지 **확인만** 하는 줄이다.
>
> **맞게 찍혔으면 그 단계는 끝난 것이고, 바로 다음 줄로 간다.**

★★ **아래는 전부 같은 터미널 창에서 이어서 친다.** 창을 닫으면 위에서 정한
  두 값이 사라진다 — 그때는 0)부터 다시 한다.

**1) NAS 에서 SSH 를 켠다** — DSM › 제어판 › **터미널 및 SNMP** › `SSH 서비스 활성화`

**2) 열쇠 한 쌍을 만든다** — 암호를 물으면 **그냥 Enter 를 두 번** 누른다
   (CI 는 암호를 칠 수 없다)

```bash
ssh-keygen -t ed25519 -C linkpilot-ci -f ~/.ssh/lp_ci
```

**3) 공개키를 NAS 에 넣는다** — NAS 비밀번호를 한 번 물어본다

> 🔴 **창이 바뀌었으면 두 값이 비어 있다** 〈2026-08-21 · 실제로 그랬다〉
>
> 새 터미널 창에서 이 줄만 치면 `$NAS_USER@$NAS_HOST` 가 **`@` 하나로**
> 줄어들고, ssh 가 이렇게 뱉는다:
>
> ```text
> /usr/bin/ssh-copy-id: ERROR: usage: ssh [-46AaCfGgKkMNnqsTtVvXxYy] ...
> ```
>
> (`text` 로 표시한 칸은 **화면에 나오는 것**이지 치는 것이 아니다)
>
> **명령을 잘못 친 것처럼 보이지만 명령은 맞았다. 값이 없었을 뿐이다.**
> 이 오류를 보면 **0)의 두 줄부터** 다시 친다.

★ 그래서 **세 줄을 함께** 붙여 넣는 편이 안전하다. 값 정하기와 쓰기가 붙어
있으면 창이 바뀌어도 헛돌지 않는다.

```bash
NAS_USER=여기에NAS계정이름
NAS_HOST=여기에tailnet주소
ssh-copy-id -i ~/.ssh/lp_ci.pub $NAS_USER@$NAS_HOST
```

나오는 말에 따라 갈린다:

| 나온 말 | 뜻 | 다음 |
|---|---|---|
| `Are you sure you want to continue connecting?` | 처음 붙는 중 (정상) | `yes` → NAS 비밀번호 (화면에 안 보여도 정상) |
| `Number of key(s) added: 1` | **성공** | 4)로 |
| `Connection refused` | NAS 에서 **SSH 가 꺼져 있다** | 1)을 다시 한다 |
| `Operation timed out` | 맥의 **Tailscale 이 꺼져 있다** | 메뉴막대 아이콘에서 `Connected` 확인 |
| `usage: ssh …` | **두 값이 비었다** | 0)부터 다시 (위 붉은 상자) |

**4) ★ 권한을 맞춘다 — Synology 는 이것이 틀리면 열쇠를 조용히 무시한다**

세 줄을 따로 친다. 한 줄로 묶으면 따옴표가 필요해진다.

```bash
ssh $NAS_USER@$NAS_HOST chmod 700 .ssh
ssh $NAS_USER@$NAS_HOST chmod 600 .ssh/authorized_keys
ssh $NAS_USER@$NAS_HOST chmod 755 .
```

- **홈 디렉터리가 그룹 쓰기 가능이면 거부된다.** 오류가 「Permission denied
  (publickey)」로만 나와서 **열쇠가 틀린 것처럼 보인다** — 두 번째로 흔한 막힘이다.
- 그래도 안 되면 `/etc/ssh/sshd_config` 의 `PubkeyAuthentication yes` 와
  `AuthorizedKeysFile .ssh/authorized_keys` 가 주석 처리되어 있지 않은지 본다.

**5) 되는지 먼저 확인한다** — GitHub 에 넣기 **전에** 내 Mac 에서

```bash
ssh -i ~/.ssh/lp_ci $NAS_USER@$NAS_HOST echo ok
```

`ok` 가 안 나오면 GitHub 에 넣어도 안 된다. **여기서 먼저 통과시킨다.**

**6) 개인키를 클립보드로** — 다음 단계에서 그대로 붙여넣는다

```bash
pbcopy < ~/.ssh/lp_ci
```

★ **`.pub` 이 아니다.** 확장자 없는 `lp_ci` 가 개인키다.
★ `-----BEGIN` 부터 `-----END` 줄까지 **전부** 들어가야 한다. 잘리면 `invalid format`.

### ③ GitHub 에 넣는다

#### 3-⓪. 🔴 **GitHub 에 `Settings` 가 두 곳이다** 〈2026-08-21 · 실제로 여기로 갔다〉

> **오른쪽 위 프로필 사진 › `Settings`** 는 **내 계정** 설정입니다. 여기에는
> Secrets 칸이 **없습니다.** 왼쪽 차림표에 `Public profile` · `Account` ·
> `Appearance` 가 보이면 **잘못 온 것**입니다.
>
> 넣어야 하는 곳은 **저장소(repository)의 `Settings`** 입니다. 왼쪽 차림표에
> `General` · `Collaborators` · `Branches` 가 보여야 맞습니다.

**헷갈릴 것 없이 이 주소로 바로 갑니다**:

```
https://github.com/findKTH0415/linkpilot-cron/settings/secrets/actions
```

★★ 계정 설정 차림표의 **`SSH and GPG keys` 도 여기가 아닙니다.** 그것은
「내 컴퓨터에서 GitHub 에 push 할 때」 쓰는 열쇠입니다. **NAS 개인키를 거기
넣으면 아무 일도 일어나지 않습니다** — 오류도 안 나서 넣은 줄 알고 넘어갑니다.


저장소 › **Settings › Secrets and variables › Actions** › `New repository secret`

| Name | Secret 칸에 넣을 것 |
|---|---|
| `TS_OAUTH_CLIENT_ID` | ①-⑧ ㉮ 의 Client ID — **㉯ 로 갔으면 넣지 않는다** |
| `TS_OAUTH_SECRET` | ①-⑧ ㉮ 의 Client Secret — 〃 |
| `TS_AUTHKEY` | ①-⑧ ㉯ 의 auth key — **㉮ 로 갔으면 넣지 않는다** |
| `NAS_SSH_HOST` | NAS 의 **tailnet 이름 또는 주소** (콘솔 Machines 에 있는 것) |
| `NAS_SSH_USER` | ②에서 쓴 NAS 계정 이름 |
| `NAS_SSH_KEY` | `cat ~/.ssh/lp_ci` 전체 — **`-----BEGIN`/`-----END` 줄까지** |

★ `NAS_SSH_KEY` 는 **개인키**(`lp_ci`)다. `.pub` 이 아니다.
★ 마지막 줄바꿈까지 그대로 붙여넣는다. 잘리면 「invalid format」이 난다.

### ④ 됐는지 확인 — **NAS 를 건드리지 않고 먼저 잰다** 〈2026-08-21 개정〉

> ★★ 앞 판은 여기가 잘못돼 있었다. 열쇠를 재는 단계에 「실제 배포일 때만」이라는
> 조건이 붙어 있어서, **안전한 판(dry run)은 열쇠에 대해 아무 말도 하지 않았다.**
> 그러면 「들어갔나?」를 확인할 방법이 **실제로 올려 보는 것**밖에 없다 —
> 확인하려고 운영 화면을 덮는 것은 순서가 거꾸로다.
>
> 이제 **dry run 이 여섯 자리를 하나씩 「있음/없음」으로 알려 준다.** 값은
> 찍지 않는다. NAS 에는 손도 대지 않는다.

**1) 먼저 안전한 판으로 잰다** — 저장소 › **Actions › deploy-nas › Run workflow** ›
`dry_run` 을 **켠 채로** 실행. `Check secrets` 단계를 펴면 이렇게 나온다:

```
── tailnet 열쇠 (둘 중 한 길만 있으면 된다) ──
  있음 — TS_OAUTH_CLIENT_ID
  있음 — TS_OAUTH_SECRET
  없음 — TS_AUTHKEY
── NAS 접속 (셋 다 있어야 한다) ──
  있음 — NAS_SSH_HOST
  있음 — NAS_SSH_USER
  있음 — NAS_SSH_KEY
```

- **「여섯 자리가 다 찼다」**는 알림이 뜨면 준비가 끝난 것이다.
- 아니면 **못 넣은 것을 이름으로** 알려 준다. 그것만 넣으면 된다.
- ★ tailnet 열쇠는 **㉮ 두 개** 또는 **㉯ 한 개**, 한 길만 있으면 된다.
  ㉮ 로 갔는데 `TS_OAUTH_SECRET` 이 빠지면 **경고로 짚어 준다** — 예전에는
  이 경우 붙는 단계까지 가서야 죽었고, 그 오류가 「열쇠가 틀렸다」와 똑같아 보였다.

**2) 닿는지 확인한다 — 아직 올리지 않는다** 〈2026-08-21 추가〉

`dry_run` 을 **끄고**, `probe` 를 **켜고** 실행합니다.

> ★★ 앞 판은 「재기」와 「올리기」 둘뿐이었습니다. 그래서 **열쇠가 실제로 통하는지
> 확인하는 첫 방법이 곧 운영 화면을 덮는 것**이었습니다 — dry run 에서 고친 잘못이
> 한 단 아래에 똑같이 있었던 셈입니다.
>
> `probe` 는 tailnet 에 붙고 ssh 로 `echo ok` 까지만 합니다. **읽기만 합니다.**
> 넷(`TS_AUTHKEY` · `NAS_SSH_HOST` · `NAS_SSH_USER` · `NAS_SSH_KEY`)이 **전부 맞아야**
> 통과하므로, 여기가 초록이면 실제 배포도 됩니다.

여기서 죽으면 넷 중 하나가 틀린 것이고, 오류 메시지가 어느 쪽인지 짚어 줍니다.
**그래도 NAS 는 아무것도 안 바뀐 상태입니다.**

**3) 그다음에 실제로 올린다** — `dry_run` 과 `probe` 를 **둘 다 끄고** 실행.

**말해 주시면 제가 1)·2) 를 돌리고 결과를 확인해 드립니다.** 둘 다 NAS 를
바꾸지 않으므로 몇 번을 돌려도 괜찮습니다. 3) 은 운영 화면을 덮으므로
**말씀하신 뒤에만** 돌립니다.

성공하면 이슈 [#7](https://github.com/findKTH0415/linkpilot-cron/issues/7) 이 닫힙니다.

---

## 2. 넣은 뒤 — 무엇이 저절로 되나

### 지금 당장 되는 것 (`deploy-nas.yml` · **이미 main 에 있다**)

화면 16개를 웹 루트로 올린다. `npm run im:embed` 는 **원본을 그대로 복사**하므로
(`build-embed.js` — 변환이 없다) 저장소 파일을 바로 올려도 지문이 같다.

### `deploy-im.yml` 이 main 에 올라간 뒤 되는 것

```
npm ci → npm test → check:reachable → im:embed → (tailnet) → 화면 → 엔진 → verify:nas
```

- **올리기 전에 시험을 통과해야 한다.** 빨간 판은 거기서 멈춘다 (§7)
- **올리고 끝내지 않는다.** `verify:nas` 가 지문을 대조한다
- 덮어쓰기 전에 **백업을 뜬다**. 앱 본체는 건드리지 않는다
- 실패하면 **알린다** (`alert-failure.yml`)

> ⚠ **`workflow_dispatch` 는 기본 브랜치에 있는 워크플로만 띄울 수 있다.**
> `deploy-im.yml` 은 아직 작업 브랜치에만 있어서 지금은 못 띄운다 (API 가 404).
> PR #6 이 병합되면 그때부터 된다.

---

## 3. 안 되는 것 — 솔직히

| | 왜 |
|---|---|
| **앱 본체 배포** (`deploy-filestation.py`) | 이 저장소에 앱 정본이 없다. 그 파일은 덮어쓰면 되돌릴 수 없어 **일부러 막아 두었다** |
| **개발 세션에서 직접 배포** | 프록시가 NAS 로 나가는 길을 막는다. 그래서 러너를 거친다 — 이것이 우회가 아니라 **유일한 길**이다 |

---

## 4. 확인하는 법

배포 뒤 `verify:nas` 가 자동으로 돈다. 사람이 눈으로 볼 것은 인수 조건뿐이다 —
[`작업인계-지시서-2026-08-20.md`](작업인계-지시서-2026-08-20.md) §7.

★ **「200 이 왔다」를 「최신이 올라갔다」로 읽지 않는다.** `verify:nas` 는 지문을
대조한다. 상태코드만 보면 옛 판이 그대로 서빙되는 것을 못 잡는다 — 실제로
「11통과 0실패」가 나온 서버가 통째로 옛 판이었다.
