# 인수인계서 — 보고서 생성 엔진을 LinkPilot 앱에 붙인다

작성 2026-08-16 (KST) · **2차 개정 — 두 작업선 병합 반영** ·
대상 저장소 `findKTH0415/linkpilot-cron` ·
브랜치 `claude/linkpilot-agent-github-plan-0af7xs` (PR #3)

> **받는 쪽에서 먼저 볼 것**
> §2 붙이는 곳 세 군데 → §5 반영 체크리스트 → §6 인수 조건.
> 그 셋만 하면 붙습니다. 나머지는 왜 그렇게 만들었는지에 대한 설명입니다.

---

## 0. 한 장 요약 — 지금 앱과 무엇이 달라지는가

지금 앱의 **[보고서 생성]** 은 이렇게 적혀 있다.

> 「보고서는 외부 분석 환경에서 만듭니다. 이 화면에서 보고서를 직접 만들지 않습니다.
> 앱은 **내부환경 기반정보**를 내보내고, 외부 분석 AGENT 툴이 그것을 분석해
> 보고서를 만듭니다.」

그래서 **PROJECT CONTROL TOWER** 에 「변환기 — 연결 대기」 「등록된 완성본 0건」이
남아 있고, 값 입력·검증·사양 확정이 전부 앱 밖에서 일어난다.

**이 인수인계의 목적은 그 바깥을 앱 안으로 들여오는 것이다.**

| | 지금 (캡처 기준) | 붙인 뒤 |
|---|---|---|
| 값 입력 | 앱 밖 AGENT 가 받는다 | 앱 2단계에서 받는다 — **출처 없으면 저장이 안 열린다** |
| 계산 항목 | 사람이 적어 넣을 수 있다 | 입력란을 아예 만들지 않는다 (19개) |
| 자료 읽기 | 「보관만 됩니다」 8종 | **그대로 읽음 19종 · 옮겨 적어 읽음 6종 · 변환 필요 3종** |
| 사양 확정 | 없음 | 3단계에서 잠근다 — 확정 전에는 생성이 시작되지 않는다 |
| 등록된 완성본 | 0건 (밖에서 받아 등록) | 앱이 만들고 목록에 바로 뜬다 |
| 검증 | 밖에서 | 05·11 이 값 충돌·범위·정합성을 RED/YELLOW/GREEN 으로 |

> **내보내기 경로는 지우지 않는다.** 「기반정보 복사·내려받기」와 외부 AGENT 링크는
> 그대로 둔다. 둘 다 쓸 수 있어야 한다 — 붙이는 일이 기존 흐름을 끊으면 안 된다.

---

## 1. 무엇을 인수하는가

의존성 추가 **0건** (`solapi` 하나뿐, 그것도 크론 잔재). Node 20+. 테스트 **613개 통과**.

| | 개수 | |
|---|---:|---|
| 파이프라인 Agent | 15 | 그중 12개는 LLM 을 한 번도 안 부른다 (부르는 것은 추출·리서치·IM 작성 셋뿐) |
| 코어 모듈 | 52 | 사전·자산군·검증·파서(IFC 포함)·저장소·이미지 변환·PDF 출력·글꼴 대조·ESS 열화·태양광·탁상검토·법인가치·자료 보관(vault)·연결 장부(linked)·**1회성 업로드(oneshot)**·**연결 접근권 임시보관(handoff)**·**연결 자료 읽기(linked-fetch)**·**Task 상태기계(tasks)**·**업무 분해(taskplan)**·**능력 배정(router)**·**산출물 등록부(artifacts)**·**오케스트레이터(orchestrator)**·**Dataset 병합 단일출처(agent-merge)**·**등록부 읽는 곳 한 곳(registry-doc)**·**디자인 게이트 7단계(design-gate)**·**요청 한 건(request)**·**근거 기여도·품질(evidence — 실물은 화면 폴더)** |
| 공공데이터 커넥터 | 26 | 공부·시장·공시 (+ 자체 호스팅 Compute · **사용자 저장소 연결**) |
| 공공데이터 커넥터 | 24 | 공부·시장·공시 (+ 자체 호스팅 Compute · **사용자 저장소 연결**) |
| 사전 항목 | 111 | 계산 전용 19개는 별도 (입력란을 만들지 않는다) · 대조 선택값 12개 포함 |
| 화면 | 4단계 | + 섹션 셸 · 미리보기 3종 |
| API | 17 | 읽기 6 · 쓰기 11 |

### 1-1. 파이프라인 Agent 15개 (`im-agent/agents/`)

| 순서 | Agent | 하는 일 | LLM |
|--:|---|---|:--:|
| 0 | `10-output-spec` | 출력 사양 확정(LOCK) — 확정 전에는 생성이 시작되지 않는다 | 미사용 |
| 1 | `01-project` | 요청문 → Project ID + 13개 표준 폴더 | 미사용 |
| 2 | `02-extraction` | 규칙 기반 추출 + LLM 보완. PDF·한글·오피스 직접 파싱, 스캔본만 OCR | 보조 |
| 3 | `07-geo` | 공공데이터 — 지오코딩·지적도·용도지역·건축물대장·인허가 | 미사용 |
| 4 | `03-research` | 시장분석 + DART 대조. 산출물 전량 `verified=false` | 사용 |
| 5 | `04-financial` | Base/Upside/Downside 3종 + 민감도 | 미사용 |
| 6 | `08-appraisal` | 감정평가 3방식 (공시지가·거래사례·수익환원) | 미사용 |
| 7 | `09-massing` | 매스 검토 + 용적률·건폐율 법정한도 | 미사용 |
| 8 | `12-sketchup-plan` | SketchUp 모델 계획(`model-plan.json`) — 값은 fact 로 안 올린다(D-96) | 미사용 |
| 9 | `13-sketchup-intake` | 사람이 만든 모델 결과를 계획과 대조 — 어긋나면 RED/YELLOW | 미사용 |
| 10 | `05-validation` | 값충돌·정합성·범위·법률 → RED/YELLOW/GREEN + QC Score | 미사용 |
| 11 | `06-im-writer` | 20개 절 IM + Teaser + 수치 출처표 | 문장만 |
| 12 | `11-final-validation` | 배포 직전 재검증 | 미사용 |

### 1-2. 공공데이터 커넥터 (`im-agent/connectors/`)

| 커넥터 | 무엇을 가져오는가 | 키 |
|---|---|---|
| `vworld` | 지오코딩 · 연속지적도 | `VWORLD_KEY` + `VWORLD_DOMAIN` |
| `nsdi` | 토지이용계획(지역·지구) · 개별공시지가 · 토지특성 | 〃 |
| `molit` | 실거래가 3종 · 건축물대장 · 건축인허가 · 주택인허가 | `DATA_GO_KR_KEY` |
| `kpx` | REC 현물시장 단가 (태양광 매출 교차검증) | 〃 |
| `kma` | 일사량 · 일조시간 (태양광) | `KMA_APIHUB_KEY` **별개 시스템** |
| `reb` | 지가지수 · 지가변동률 (공시지가 시점수정) | `REB_API_KEY` **또 별개** |
| `ecos` | 국고채·CD 금리 (차입금리 **기준선**) · 생산자물가지수 (제조 딜 **단가 시점수정**) | `ECOS_API_KEY` |
| `kosis` | 제조업 가동률·출하액 — **업종 평균과 대조만 한다** (값을 채우지 않는다) | `KOSIS_API_KEY` **또 별개** |
| `factory` | 공장등록 — 용지·건축·**제조시설**·부대 면적 (제조 딜의 건축물대장 격) | `DATA_GO_KR_KEY` |
| `customs` | 수출입 실적 → **HS 코드별 kg당 단가** (단가 대조. 원화 환산은 안 한다) | 〃 |
| `enviro` | 환경 인허가 **딜브레이커 점검** — 「확인되지 않음」을 통과로 세지 않는다 | 〃 |
| `kepco` | **계통 여유용량** (데이터센터·태양광). 변전소 **거리는 못 낸다** — 좌표가 비식별 처리된다 | `KEPCO_BIGDATA_KEY` **또 별개** |
| `nts` | 사업자등록 **휴폐업·진위**. **납세증명은 못 한다** — 국세기본법이 제3자 조회를 막는다 | `DATA_GO_KR_KEY` |
| `nps` | 국민연금 사업장 **인원·고지금액** (실체 확인). **완납 여부는 공개되지 않는다** | 〃 |
| `pexels` | 표지·간지의 **참고 이미지** (공공데이터가 아니다). 캡션 강제 · 스톡 폴더 고정 | `PEXELS_API_KEY` **또 별개** |
| `dart` · `fsc` | 시행사가 실재하는 법인인지 **대조** | `DART_API_KEY` · `DATA_GO_KR_KEY` |
| `law` | 법령·시행령 **조문 원문** · 지자체 **건축조례** — 법정 한도의 근거 | `LAW_OC` 또는 `LAW_OPEN_DATA` — **키가 아니라 OC** (둘 다 읽는다) |
| `kict` | 건설공사비지수 (공사비 **시점수정**의 두 번째 출처) — ⚠ **엔드포인트 미확정, 호출 안 함** (D-106) | `KICT_API_KEY` + `KICT_API_BASE` |
| `cache` `http` `xml` `pnu` | TTL·쿼터 · 재시도·키 마스킹 · 파싱 · 코드 파생 | — |

★ **키가 일곱으로 갈린다.** data.go.kr · 기상청 API허브 · 한국부동산원 · 한국은행 ·
통계청 · **한전 전력데이터개방포털** · **법제처**는 서로 다른 시스템이고 키도 각각이다. 한쪽 키를 다른 쪽에 넣으면 인증만
실패하고 증상이 "키가 틀렸다"와 똑같다.

★ **`law` 만 인증이 키가 아니라 `OC`(신청 이메일의 @ 앞부분)다.** 게다가 **사용 승인**이
따로 나야 돌고, 승인 전 오류가 **OC 오타와 구분되지 않는다** — `law.js` 의 `diagnose()` 가
승인·OC·없음·형식 넷으로 갈라 준다. 신청 절차는 `docs/안내-국가법령정보-API.md`.

★ **`law` 는 시행령 상한을 조례 한도로 쓰지 않게 막는 자리다.** 용적률·건폐율의 실제 한도는
조례가 정한다 — 지금까지 그 자리는 「조례 확인 사항」이라는 말로만 남아 있었다.

★ **`kosis` 는 값을 채우지 않는다.** 개별 공장의 가동률은 공적으로 나오지 않고
업종 평균만 나온다. 「이 딜의 85% 는 업종 평균 73% 보다 12%p 높다」가 낼 수 있는
말이고, 「가동률은 73% 다」가 아니다. 그래서 fact 로 등록하지 않는다.

- 일 10,000건 한도를 `cache.js` 가 강제한다. 캐시 히트는 쿼터를 안 쓴다 → **재실행 시 호출 0건**
- 키가 없으면 데이터를 지어내지 않고 `unavailable` 을 돌려준다
- 프로젝트당 공공데이터 호출 **최대 5회** (PNU 하나에서 코드를 파생시킨다)

### 1-3. 자료 읽기 (2026-08-16 신규)

| 방법 | 형식 | 모듈 |
|---|---|---|
| 그대로 읽음 | txt·md·csv·json·html·xml·docx·xlsx·pptx·**hwpx** | `core/unzip.js` |
| 그대로 읽음 | **pdf** (텍스트 레이어) | `core/pdftext.js` |
| 그대로 읽음 | **hwp·doc·xls·ppt** (CFBF) | `core/ole.js` |
| 옮겨 적어 읽음 | jpg·jpeg·png·webp·heic·heif · 글자 없는 PDF | `core/ocr.js` (Gemini) |
| 변환 필요 | gif·tif·tiff | 화면이 무엇으로 바꿀지 알려 준다 |

**틀린 글자를 내놓느니 못 읽었다고 한다** — 판정 기준 둘: ToUnicode 없는 글자 15% 초과(PDF),
읽을 수 있는 글자 75% 미만(OLE). 그러면 자동으로 OCR 로 넘어간다.

### 1-4. 화면 (`im-agent/ui/platform/`)

`report-flow.html` (섹션 본체) · `outputs.html` (**완성 보고서 탭**) · `intake.html` · `fields.html` · `reports.html` ·
`flow-core.js` (4단계 단일 출처) · `fields-core.js` · `gate-core.js` · `catalog.js` · `inapp.js`

### 1-5. 서버 (`im-agent/ui/`)

`api-router.cjs` (읽기 6) · `report-api.cjs` (쓰기 11) · `run-queue.cjs` (생성 큐)

---

## 2. 붙이는 곳은 세 군데뿐이다

### 2-1. 서버 — 라우터 두 개

읽기와 쓰기를 **일부러 갈라 두었다.** 쓰기는 파일을 만들고 LLM 을 부르고 공공데이터
쿼터를 쓴다. 같은 라우터에 얹으면 대시보드 폴링과 같은 권한으로 돈 드는 동작이 열린다.

```js
// 본체 server.js (포트 8181)
const path = require('path');
const AGENT = '/volume1/linkpilot/im-agent';        // 이 저장소를 놓은 경로
const ROOT  = '/volume1/linkpilot/im-projects';     // 산출물이 쌓이는 곳

const read    = require(path.join(AGENT, 'ui/api-router.cjs'));
const reports = require(path.join(AGENT, 'ui/report-api.cjs'));
const { createQueue } = require(path.join(AGENT, 'ui/run-queue.cjs'));

const queue = createQueue({ agentRoot: ROOT, concurrency: 1 });

app.use('/api/linkpilot', read.createRouter({ agentRoot: ROOT, agentModulePath: AGENT }));

app.use('/api/linkpilot', reports.createRouter({
  agentRoot: ROOT,
  agentModulePath: AGENT,
  authenticate: (req) => req.session && req.session.user,   // ★ 없으면 마운트에서 예외
  startRun: (projectId, spec, user) => queue.startRun(projectId, spec, user),
  runningFor: (projectId) => {                              // ★★ 빠뜨리기 쉽다
    const r = queue.latestFor(projectId);
    return r && (r.status === 'running' || r.status === 'queued') ? r : null;
  },
}));

process.on('SIGTERM', () => queue.stop());
```

**★ `runningFor` 를 안 넘기면 조용히 규칙이 꺼진다.** `report-api` 는 생성이 도는
중에는 값 저장을 거부하는데, 그 판정을 이 함수로 물어본다. 안 넘기면 **검사를 건너뛴다** —
오류도 경고도 없다. 생성 중에 값을 고친 그날 산출물이 데이터와 어긋난다.
큐에는 `runningFor` 라는 이름의 메서드가 없다 (`latestFor` 다). 위처럼 직접 이어 준다.

### 2-2. 화면 — 섹션 자리에 파일 몇 개

`im-agent/ui/platform/` 의 파일들을 앱과 **같은 출처**에 올리고, `report-flow.html` 의
설정 블록만 채운다. **다른 곳에 값을 박지 않는다.**

```js
window.LINKPILOT_REPORT_FLOW = {
  api:  '/api/linkpilot',
  base: '/im/',                 // 단계 화면들이 놓인 경로 (같은 폴더면 '')
  session: currentUser,         // { authenticated, planId, status, name }
  projectId: null,
  inTab: false,                 // ★ 앱 탭 안에 얹으면 true (아래 2-2-1)
  external: {                   // 지금 쓰는 외부 분석 경로 — 지우지 않는다
    repoUrl:  'https://…',
    agentUrl: 'https://…',
  },
};
```

- 앱 사이드바·로고는 **끼울 때만 자동으로 감춰진다** (`flow-core.js` 의 `EMBED_CSS`)
- 단계 화면을 **복사하지 말 것** — 복사하면 「출처 없는 값은 저장할 수 없다」 같은
  검사가 두 벌이 되고, 한쪽만 고치는 날 규칙이 갈린다
- 4단계를 늘리거나 순서를 바꾸려면 `flow-core.js` **한 곳만** 고친다

### 2-2-1. 「보고서 생성」 **탭**으로 붙이는 법 〈2026-08-17 · 이름 2026-08-18 변경〉

**지금 붙일 수 있다.** 지침 §2 에 **「보고서 생성 (Pro)」** 로 이미 배포되어 있어
**지침을 고치지 않아도 된다.** 새로 지침에 실어야 하는 것은 **「자료」 탭 하나뿐**이고
(D-63), 그것만 재발행을 기다린다.

**① 탭 이름을 복사해 적지 않는다.** `flow-core.js` 의 `SECTION` 에서 읽어 간다.

```js
import FLOW from '/im/flow-core.js';   // 또는 <script src> 뒤 window.LinkPilotFlow

FLOW.SECTION
// { id: 'make', title: '보고서 생성', tab: '보고서 생성',
//   tabNote: '외부 엔진(IM Agent) 4단계', plan: 'pro' }
```

- **탭 바에 다는 이름** = `SECTION.tab` · 부제 = `SECTION.tabNote`
- **단독으로 열 때 화면이 스스로 그리는 제목** = `SECTION.title`
- 둘이 다른 이유: 탭 바에는 「완성 보고서」가 나란히 서므로 **「새」가 있어야**
  구분되고, 단독 화면에는 견줄 상대가 없어 「보고서 생성」이 맞다

**② 탭 안에서는 `inTab: true` 를 켠다.**

```js
window.LINKPILOT_REPORT_FLOW = { …, inTab: true };
```

★ 이걸 안 켜면 **탭 이름 바로 아래에 「보고서 생성」이 한 번 더 나온다.**
  탭 이름과 섹션 제목이 **같은 글자**라 더 도드라진다 (2026-08-18).
★ 반대로 **단독으로 열 때 켜 두면 이름 없는 화면**이 된다 — 기본값은 `false` 다.

**③ 탭 자리에는 이 파일 하나만 얹는다.**

```
/im/report-flow.html        ← 탭 내용 (나머지는 이 파일이 알아서 부른다)
```

`intake.html` · `fields.html` · `reports.html` 은 이 화면이 iframe 으로 부르므로
**탭 바에 따로 달지 않는다.** 따로 달면 4단계가 7개 탭처럼 보인다.

**④ 세 탭 구성 전체**는 `npm run im:tabs` → `tabs-artifact.html` 에 그려 두었다
(눌러 볼 수 있다). 나머지 두 탭은 **아직 화면이 없다** — 「완성 보기」는
`GET /projects/:id/reports` 를 그리는 뷰가 필요하고, 「자료」는 지침 재발행 뒤다.

### 2-2-2. 「완성 보고서」 **탭**으로 붙이는 법 〈2026-08-17〉

**이것도 지금 붙일 수 있다.** 지침 §2 의 「보고서 생성 (Pro)」 안에서 나오는
산출물을 보여 주는 화면이라 **새로 지침에 실을 것이 없다.**

```
/im/outputs.html        ← 탭 내용
```

```js
import FLOW from '/im/flow-core.js';
FLOW.OUTPUTS_SECTION
// { id:'done', title:'완성 보고서', tab:'완성 보고서',
//   tabNote:'만들어진 산출물 목록', file:'outputs.html', plan:'pro' }
```

```js
window.LINKPILOT_OUTPUTS = {
  api: '/api/linkpilot',
  session: currentUser,
  projectId: null,        // 정해 두면 고르는 칸을 감춘다
  inTab: true,            // ★ 탭 안이면 켠다
};
```

**이 화면이 하는 일과 안 하는 일**

| | |
|---|---|
| 목록 | `GET /projects/:id/reports` 가 준 것을 **그대로 그린다.** 화면이 목록을 만들지 않는다 |
| 진행률 | **서버가 준 분모**를 쓴다 (`progress`). 화면에서 다시 계산하지 않는다 |
| 분모 | 「이 프로젝트에서 **나와야 하는 것**」 — 조건부 산출물(탁상검토·법인가치)은 **분모 밖** |
| 배포 차단 | **목록보다 위**에 띄우고, 차단이면 **내려받기를 열지 않는다** |

★ **조건부를 분모에 넣으면** 어떤 딜도 100% 가 되지 않아 다 끝났는데도 덜 된
것처럼 보인다. 반대로 **목록에서 아예 빼면** 「나올 수 있는 문서가 있었다」는
사실이 사라진다 — 그래서 **분모 밖에 따로** 낸다 (「이 딜에서는 안 나오는 것」).

★★ **100% 는 「만들어졌다」이지 「보내도 된다」가 아니다.** 화면이 그렇게 말하고,
`GET /projects/:id/file` 도 검증을 통과하지 못하면 **403** 을 돌려준다 —
목록에서는 「차단」인데 파일은 열린다면 검증 GATE 가 아무 의미도 없다.

**서버 쪽 변경 (이미 반영)** — `GET /projects/:id/reports` 응답에 `all` ·
`progress` · `specKnown` 이 늘었다. `files`(나온 것만)는 **그대로 두었으므로
기존 호출부는 고치지 않아도 된다.**

### 2-3. 환경변수 — 본체 프로세스에 넣는다

**GitHub Secrets 만으로는 아무 일도 일어나지 않는다.** 실제로 공공데이터를 쓰는 것은
본체(NAS)에서 도는 프로세스이므로 값은 **거기에** 들어가야 한다.

`.env.example` 을 그대로 복사해 채우면 된다 (`cp .env.example .env`).

| 이름 | 없으면 |
|---|---|
| `IM_AGENT_ROOT` | 프로젝트 폴더 위치를 모른다 (**필수**) |
| `VWORLD_KEY` · `VWORLD_DOMAIN` | 07 Geo 통째로 건너뜀 — 입력란이 8→9칸으로 는다 |
| `DATA_GO_KR_KEY` | 실거래가·건축물대장·인허가·REC 건너뜀 |
| `KMA_APIHUB_KEY` (+ `KMA_STN`) | 태양광 딜의 일사량·일조시간이 빈다 |
| `REB_API_KEY` | **공시지가 시점수정이 안 된다** — 8월 IM 이 1월 1일 값을 쓴다 |
| `ECOS_API_KEY` | 금리가 가정치로만 남는다 · 제조 딜 단가의 시점수정이 안 된다 |
| `KEPCO_BIGDATA_KEY` | 계통 여유를 못 본다 — **수전용량이 계통에 실제로 있는지 확인할 방법이 사라진다** (데이터센터·태양광의 딜브레이커) |
| `PEXELS_API_KEY` | 표지·간지 이미지를 못 받는다 — **장식이라 그 자리를 비운다** (지어내지 않는다) |
| `KOSIS_API_KEY` | **제조 딜의 가동률을 업종 평균과 못 견준다** — 가동률이 근거 0인 가정치로 남는다 |
| `DART_API_KEY` | 시행사를 대조하지 않는다 |
| `GEMINI_API_KEY` | 추출·문장이 규칙 기반만 + **스캔본·사진을 아예 못 읽는다** |
| `RHINO_COMPUTE_URL` (+ `RHINO_COMPUTE_KEY`) | 매스 스터디를 못 돌린다 (**공공 API 가 아니라 자체 호스팅 서버**) |
| `DROPBOX_APP_KEY` · `BOX_CLIENT_ID` · `GOOGLE_CLIENT_ID` · `MS_CLIENT_ID` | 그 제공자의 **「폴더를 연결해서」가 안 열린다** — 「아직 열려 있지 않습니다」로 뜨고 조용히 남는다. **client secret 은 넣지 않는다** (범위를 좁게 잡으면 필요 없다) |
| (환경변수 아님) 헤드리스 크로미움 | **SVG 그림의 JPEG 이 안 만들어진다** — SVG 는 남지만 메일·PPT 에서 안 열린다. `CHROME_PATH` 로 알려 줄 수 있다 |

- `DATA_GO_KR_KEY` 는 **Decoding(일반) 인증키**다. Encoding 쪽을 넣으면 증상이
  "키가 틀렸다"와 똑같다 (커넥터가 부르기 전에 막고 알려 준다)
- `VWORLD_DOMAIN` 은 콘솔의 **서비스URL 을 글자 그대로** 넣는다 (스킴·경로 포함).
  호스트만 넣으면 `ned/*` 계열이 간헐적으로 거부한다 — 실측 5회 중 2회
- `RHINO_COMPUTE_URL` 은 **우리가 세운 서버**다. Compute 소프트웨어는 오픈소스지만
  **그 서버에 Rhino 라이선스가 필요하다** — 무료가 아니다 (등록부 D-38).
  그리고 이 결과는 **생성물**이라 fact 로 등록하지 않는다 (§3-2)

---

## 3. API 계약 (17개)

### 읽기 — `api-router.cjs` (인증 선택)

| 메서드 | 경로 | 쓰임 |
|---|---|---|
| GET | `/intake` | **자산군 12종**·읽는 방법 묶음·용량 한도·발행 주체 |
| GET | `/fields` | 사전 74항목 · 계산 19항목(한글·영문) · **자산군 12종** · 채움 계획 |
| GET | `/projects` | 프로젝트 목록 (id·name·assetType·status **4개만**) |
| GET | `/projects/:id/control-tower` | 진행률·검증·산출·승인 스냅샷 |
| GET | `/projects/:id/lineage/:key` | 값 하나의 계보 |
| GET | `/projects/:id/impact/:key` | 그 값이 바뀌면 무엇이 다시 계산되는가 |

### 쓰기 — `report-api.cjs` (**인증 필수**, 없으면 마운트에서 예외)

| 메서드 | 경로 | 쓰임 |
|---|---|---|
| POST | `/projects` | 요청문 → 프로젝트 생성 |
| POST | `/projects/:id/sources` | 자료 업로드 (파일당 30MB · 요청당 45MB) |
| PUT | `/projects/:id/issuer` | 발행 주체 수정 |
| GET·POST | `/projects/:id/spec` | 출력 사양 조회·저장 |
| POST | `/projects/:id/spec/confirm` | **사양 확정(LOCK)** |
| GET·PUT | `/projects/:id/facts` | 값 조회·저장 (**출처 없으면 거부**) |
| GET | `/projects/:id/reports` | 산출물 6종 목록 |
| GET | `/projects/:id/file?rel=` | 산출물 열기 (화이트리스트 + CSP + nosniff) |
| POST | `/projects/:id/reports` | 생성 시작 |

**산출물 6종**: IM 원문(`09_IM/im.md`) · A4 인쇄본(`12_Final/im-a4.html`) ·
뷰어 데이터(`12_Final/content.json`) · Teaser · 검증 보고서 · RED FLAG 보고서

---

### 3-1. 자산군 12종 (2026-08-16 신규)

**필수 항목이 자산군마다 달라진다.** 그전에는 문서 종류(IM·티저·재무모델)로만
갈렸고, 그래서 호텔 딜에서도 객실 수를 안 물었다. 그 값이 없으면 매출 가정에
근거가 없는데 화면은 「필수 17개 중 17개」라고 다 됐다고 말한다.

단일 출처는 `im-agent/core/assetclass.js` 하나다. **앱이 목록을 복사해 두지 않는다.**

| 자산군 | 재무 템플릿 | 더 받는 값 |
|---|---|---|
| 제조 Manufacturing | generic | 생산능력 · 수전용량 · 대지면적 |
| 인프라스트럭처 Infrastructure | generic | 운영권 기간 · 운영기간 · 연간 매출 |
| 물류창고 Logistics Warehouse | realestate | 임대면적 · 유효층고 · 바닥하중 · 도크 수 |
| 호텔 Hotel | realestate | 객실 수 · ADR · 점유율 |
| 데이터센터 Data Center | datacenter | IT Load · 수전용량 · PUE |
| 태양광 Solar PV | solar | 설비용량 **DC·AC(짝)** · 일사량 · 부지 유형 · 이격거리 조례 |
| ESS Energy Storage System | ess | 저장용량 · **출력용량(짝)** · 연간 열화율 · 수익모델 |
| 해상풍력 Offshore Wind | wind_offshore | 설비용량 · 풍속 · **허브 높이** · 수심 · 이안거리 |
| 육상풍력 Onshore Wind | wind_onshore | 설비용량 · 풍속 · **허브 높이** · 지역·지구 |
| 오피스텔 Officetel | realestate | 세대수/호실수 · 임대면적 · 단가 |
| 주상복합 Mixed-use Residential | realestate | 세대수/호실수 · 임대면적 · 주용도 |
| 아파트 Apartment | realestate | 세대수/호실수 · 단가 · 인허가 현황 |
| 오피스빌딩 Office Building | realestate | 임대면적 · 단가 · 임대율 |
| 공장 Factory | realestate | 용도지역 · 수전용량 · 연면적 |
| 산업단지 Industrial Park | realestate | 분양가능 면적 · 대지면적 · 지역·지구 |
| 리조트 Resort | realestate | 객실 수 · ADR · 점유율 · 대지면적 |

**붙일 때 알아야 할 것 넷:**

1. **자산군(16)과 재무 템플릿(8)은 다른 축이다.** 호텔과 오피스빌딩은 같은
   재무 템플릿을 쓰지만 IM 에 실려야 할 값이 전혀 다르다. 하나로 합치면
   둘 중 하나가 반드시 틀린다. 자산군이 `template` 으로 템플릿을 가리키기만 한다.
2. **더해지기만 한다.** 자산군이 문서 종류 필수를 빼앗지 않는다. 빼면 호텔
   딜에서 대지면적이 필수가 아니게 되는 식으로 규칙이 조용히 무너진다.
3. **못 정하면 `null` 이다.** 요청문에 「주상복합 아파트」처럼 두 자산군이 함께
   읽히면 자동으로 고르지 않는다. 01 Project 가 경고를 남기고, 화면은
   「자산군을 못 정했습니다」를 띄운다 — 그대로 두면 전용 필수가 통째로 빠진 채
   「다 채웠다」가 된다.
4. **왜 묻는지가 API 로 함께 내려간다** (`assetClasses[].requires[].why`).
   이유 없이 늘어난 입력란은 대충 채워지고, 대충 채운 값이 IM 에 실린다.

넘어가는 값 — `POST /projects` 는 `assetClass`(id) 를 받고,
`GET /projects/:id/facts` 는 그 프로젝트의 `assetClass`·`assetType`·`templateId` 를
함께 돌려준다. 화면은 이걸 받아 진행률의 분모를 정한다.


### 3-2. 생성물 커넥터 — 근거가 아니라 검증 대상이다 (2026-08-16 신규)

| 무엇 | 어디 | 성격 |
|---|---|---|
| BIM 수량 | `core/ifc.js` (.ifc 읽기, 의존성 0) | 모델이 내보낼 때 써 넣은 수량만 읽는다. 형상에서 새로 계산하지 않는다 |
| 공사 낙찰 | `connectors/g2b.js` (조달청) | 관급 낙찰 실적. **㎡당 단가를 만들지 않는다** — 공고에 연면적이 없다 |
| 매스 스터디 | `connectors/rhino.js` (Rhino.Compute) | **생성물**. `assumption: true` 가 붙고 fact 로 등록되지 않는다 |

**붙일 때 지켜야 할 한 줄:** 이 셋은 §4 의 공공데이터와 **성격이 다르다.**
공공데이터는 사업계획서를 대조하는 독립된 출처지만, 이 셋 중 매스 스터디는
**우리가 만든 값**이다. IM 에 「연면적 32,000㎡(출처: 매스 스터디)」라고 적으면
우리가 만든 것으로 우리 주장을 증명하는 셈이 된다.

---

## 4. 화면 4단계와 데이터 흐름

```
1 보고서 생성 입력   POST /projects → POST /projects/:id/sources
     ↓  요청문에서 뽑은 값은 「미확인」으로 표시
2 가이드 필드 입력   GET /fields → PUT /projects/:id/facts
     ↓  사전 60항목 중 아무도 못 채우는 9칸만 묻는다
     ↓  출처가 비면 그 줄이 빨갛게 서고 저장이 안 열린다
     ↓  계산 19항목은 입력란을 만들지 않는다
3 출력 사양 확정     POST /projects/:id/spec → /spec/confirm (LOCK)
     ↓  확정 전에는 생성이 시작되지 않는다
4 생성               POST /projects/:id/reports → GET /reports → GET /file
        검증 통과한 산출물만 열린다
```

**필수 17개 중 사람이 직접 넣는 것은 9개** — 시행사/스폰서 · 대지면적 · 토지 확보상태 ·
연면적 · 총사업비 · 토지비 · 공사비 · 연간 매출(안정화) · 인허가 현황.
(대지면적은 `VWORLD_KEY` 가 없어서 내려온 것이다. 키가 있으면 8개)

---

## 5. 반영될 모든 요소 — 체크리스트

### 5-1. 앱(본체)에서 해야 하는 일

- [ ] `server.js` 에 읽기 라우터 마운트 (§2-1)
- [ ] `server.js` 에 쓰기 라우터 마운트 — `authenticate` · `startRun` · **`runningFor`**
- [ ] `SIGTERM` 에 `queue.stop()` (안 하면 배포마다 고아 프로세스가 쌓인다)
- [ ] `[보고서 생성]` 섹션을 `report-flow.html` 로 교체 (같은 출처에 올린다)
- [ ] 설정 블록 4줄 채우기 — `api` · `base` · `session` · `external`
- [ ] **「기반정보 복사·내려받기」와 외부 AGENT 링크는 그대로 둔다**
- [ ] 환경변수 7종 등록 (§2-3) — `IM_AGENT_ROOT` 는 필수
- [ ] `IM_AGENT_ROOT` 폴더 생성 + 쓰기 권한
- [ ] 프론트 라우팅: `/im/*` 정적 서빙

### 5-2. Control Tower 표시가 바뀌는 항목

캡처의 「연결 대기」·「0건」이 이렇게 채워진다.

- [ ] **② 변환 (H.264)** — 이 엔진과 무관하다. 영상 변환기는 별개이므로 **그대로 둔다**
- [ ] **③ 기반정보 추출 「준비됨」** — 유지 (내보내기 경로는 남는다)
- [ ] **③ 외부 분석 AGENT 「연결됨」** — 유지. 앱 안 생성이 추가되는 것이지 대체가 아니다
- [ ] **③ 등록된 완성본** — `GET /projects/:id/reports` 의 존재하는 파일 수로 바뀐다
- [ ] **① 자료 수신** — `POST /sources` 결과와 이어 붙일 수 있다 (선택)

### 5-3. 이 저장소에서 이미 끝난 것

- [x] Agent 12개 · 커넥터 **24개** · 코어 39개 모듈
- [x] 화면 4단계 + 섹션 셸 + 미리보기 3종
- [x] API 17개 + 생성 큐
- [x] 자료 읽기 (PDF·한글·옛 오피스·OCR)
- [x] 사전 **64항목** · 계산 19항목 한글/영문
- [x] 공시지가 시점수정 · 주택인허가(주택법 트랙) · 지역·지구 IM 반영
- [x] 테스트 **613개**
- [x] 재발 방지 기록 `MEMORY.md` (M-01~M-07)

### 5-4. 사용자만 할 수 있는 일 (막혀 있음)

- [ ] **저장소 공개 범위 결정** (D-10) — 지금 **public** 이다
- [ ] **VWORLD_KEY 재발급** (git 이력 노출) · **DATA_GO_KR_KEY 재발급** (대화 노출)
- [ ] GitHub Secrets · 본체 환경변수 등록 (D-12)
- [ ] PR #3 · #4 병합 시점 (D-11)
- [ ] `npm run im:smoke` 출력 — 공공데이터 응답 필드명 실측 (B-4)
- [ ] `npm run im:read -- <실무파일>` 출력 — 문서 파서 실측 (D-31)
- [x] ~~두 번째 작업선 병합~~ — 2026-08-16 완료 (D-30 닫힘)

---

## 6. 인수 조건 — 순서대로, 건너뛰지 말 것

```bash
# ① 엔진만 먼저 — 서버·키 없이 돈다
npm test                                   # 613 pass / 0 fail 이어야 한다
npm run im:demo                            # 샘플로 파이프라인 1회

# ② 키를 넣고 공공데이터 실측 — 응답 필드명까지 대조한다
IM_AGENT_DEBUG_HTTP=1 npm run im:smoke

# ③ 자료 읽기 실측 — 실무 파일 몇 개로
npm run im:read -- ~/사업계획서.pdf ~/감정평가서.hwp

# ④ 화면만 먼저 — 서버 없이 열린다
npm run im:section                         # 눌러 볼 수 있는 판
npm run im:static                          # 스크립트 없이도 보이는 판
```

**②를 건너뛰지 말 것.** 커넥터의 응답 필드명은 공식 문서 기준으로 작성했고
**실제 키로 검증한 적이 없다.** 필드명이 다르면 조회는 성공하는데 값이 전부 비고,
화면에는 아무 경고도 안 뜬다 — 가장 잡기 어려운 종류의 오류다.

### 붙인 뒤 첫 확인

| 보이는 것 | 뜻 |
|---|---|
| 단계가 전부 잠기고 「서버(api)가 연결되어 있지 않습니다」 | `api` 를 안 채웠다 |
| 2~4단계만 잠기고 「먼저 「보고서 생성 입력」에서 프로젝트를 만듭니다」 | 정상 |
| 화면이 아래쪽에서 잘린다 | 단계 화면이 **다른 출처**에 있다 |
| 서버가 안 뜨고 `authenticate` 예외 | 정상 — 인증 없이 생성 API 를 열 수 없다 |

---

## 7. 지금 못 하는 것

| | 막고 있는 것 |
|---|---|
| 서버에서 PDF·PPTX **생성** | 의존성 추가 미승인 (D-13). 브라우저 인쇄는 동작한다 |
| 결제 | 결제대행사 미선정 (D-09). `billingUrl` 한 줄이면 붙는다 |
| SMP 시계열 | REC 는 붙었다 (`kpx`). SMP 는 아직 — 태양광 매출 = 발전량 × (SMP + REC×가중치) 중 한쪽만 실측이다 |
| gif·tif·tiff 읽기 | 이미지 변환 라이브러리를 안 들인다. 무엇으로 바꿀지 화면이 알려 준다 |
| 차입금리 스프레드 | 시장 판단이라 미정 (D-22). 지어내면 출처의 뜻이 사라진다 |

---

## 8. 함께 넘기는 규칙과 기록

| 파일 | 무엇 |
|---|---|
| `CLAUDE.md` | 절대 규칙 · 코드 작업 규칙 · **§8 미리보기 전달** · §9 미결정 관리 |
| `MEMORY.md` | **재발 방지 기록 M-01~M-07** + 배포 전 교차검증 7항목 |
| `docs/미결정-사항.md` | 미결정 등록부 D-01~D-31 (지우지 않는다, ✅ 로 바꾼다) |
| `docs/붙이는-법.md` | 붙이는 절차 (이 문서의 요약판) |
| `docs/작업지시서-플랫폼-연동.md` | 작업 지시서 형식 |
| `README.md` | 설계 원칙 세 가지와 파이프라인 |

**문제가 생기면 `MEMORY.md` 를 먼저 찾는다.** 같은 종류를 이미 겪었을 가능성이 높고,
경위·해법·막는 장치가 적혀 있다.

---

## 9. 되돌리는 법

라우터 두 줄을 빼고 섹션을 원래 화면으로 되돌리면 끝이다.
**엔진은 앱 밖에서 파일로만 돌아가므로 본체 데이터에 손대지 않는다** —
프로젝트 폴더(`IM_AGENT_ROOT`)만 남고, 그것도 지우면 흔적이 없다.
