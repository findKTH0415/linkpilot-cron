# 결정 기록 — 무엇을 언제 누가 정했나

> **이 표는 `docs/미결정-사항.md` 에서 떼어 냈다** 〈2026-08-26 · D-134〉.
> 열린 항목과 닫힌 결정이 한 파일에 있으면 **두 갈래가 늘 같은 파일을 고친다** —
> 결정 하나를 적을 때마다 열려 있는 다른 작업선과 충돌했다.
>
> **세는 규칙은 그대로다** (`im-agent/test/flow.test.js`):
> **결정** = 아래 표의 `| D-nn |` 줄 수. 머리말이 아니다.
>
> **결정의 본문(경위·근거)은 여전히 `docs/미결정-사항.md` 의 `### ✅ D-nn.` 항목에 있다.**
> 여기 있는 것은 **한 줄 요약과 날짜**다 — 왜 그렇게 정했는지는 그쪽을 본다.
> ★ 두 곳이 갈리지 않게 **번호는 한 곳에서만 딴다** (`npm run d:next`).

| ID | 결정 내용 | 결정일 | 결정자 | 반영 커밋 |
|---|---|---|---|---|
| D-159 | **화면 캐시를 역방향 프록시에 달았다 — 그리고 배포가 재는 길을 사장님이 보시는 길로 옮겼다** 〈2026-08-28 실측〉. 실측 셋: ① 이 NAS 의 백엔드는 **Nginx** 다(Web Station → 웹 서비스 → 편집에서 눈으로 확인) — **배포가 매번 놓아 온 `.htaccess` 는 한 번도 일한 적이 없다.** ② Web Station 에는 사용자 지정 헤더 UI 가 **아예 없다**(포털 편집 창은 포트·HSTS 뿐, 웹 서비스 편집 창은 백엔드·PHP 뿐). ③ 앱이 쓰는 길은 **제어판 → 로그인 포털 → 고급 → 역방향 프록시**의 `PDI-CRM`(`https://*:843` → `http://localhost:80`)이고, 그 규칙의 **사용자 지정 머리글**에 `Cache-Control: no-cache, must-revalidate` 를 넣어 저장했다. ★★★ **함께 고친 것 — 배포의 재기가 「헤더가 없다」와 「못 쟀다」를 안 가르고 있었다.** `CC` 가 비면 딴 포트로 물러나 판정에 `포트 8080` 이 찍혔는데, **헤더가 없는 것이 정상 결과**다. 그래서 843 에 헤더를 달아도 8080 을 재는 한 판정은 **영영 「안 켜짐」**이었다 — 켜 두고도 안 켜졌다고 믿게 되는 자리다(M-25 와 같은 결). 이제 **응답을 받았는가**(`GOT`)로 갈리고, 앱이 쓰는 길을 못 쟀으면 판정에 그렇게 적는다. 안 켜졌을 때의 안내도 `.htaccess` 가 아니라 역방향 프록시를 가리킨다. ★ `Connection: $connection_upgrade` 는 원래 있던 줄이라 **지우지 않는다** — WebSocket 이 그것으로 돈다 | 2026-08-28 | 사용자 | (이 커밋) |
| D-158 | **3절에 [미리보기]·[수정 요구], 4절에 [다운로드] 를 붙였다** 〈사장님이 그리신 아코디언 넷의 마지막 두 칸〉. D-157 은 칸만 넷으로 묶고 이 셋을 **안 붙였다** — 그림에 있는 것이 화면에 없었다. ★ **[수정 요구]는 요청을 접수하지 않는다.** 서버에 그런 창구가 **없다** — 있는 척하면 사용자는 보냈다고 믿고 아무도 받지 않는다. 대신 **고치는 자리로 데려간다**: 값은 4단계(가이드 필드), 쪽수·형식은 5단계(출력조건). 어느 쪽인지는 사람이 고른다(§4.9). ★★ [미리보기]·[다운로드]는 `outputs.html` 과 **같은 창구**(`GET /projects/:id/file?rel=`)를 쓴다 — 두 벌로 만들면 한쪽만 고치는 날 갈린다. ★★★ 문서 목록을 **못 받은 것과 문서가 없는 것을 가른다**(`facts.reports === null` vs `[]`) — 넷 갈래로 사유를 적는다(서버 없음·배포 차단·못 물어봄·정말 없음). 배포가 막힌 문서는 열지 않는다. 도구줄은 **[확인]보다 앞**에 그린다 — 미리보기·수정은 칸을 끝내기 전에 하는 일이고 [확인]은 끝냈다는 말이라, 순서가 곧 뜻이다 | 2026-08-28 | 사용자 | (이 커밋) |
| D-157 | **보고서 만들기를 아코디언 넷으로 다시 짰다** 〈사장님 지시: 「거울 미러링처럼 복잡함 혼란스러워 · 화면 전면적으로 재구성한다」〉. 절 다섯 → **넷**: ①기본정보·무엇을 만들까요(basics+ask) ②관련자료 업로드(sources) ③보고서 생성(fields+spec) ④완성 보고서(done — 딴 탭이던 것을 흐름 안으로). ★ **한 절이 여러 단계를 품고**, 화면은 그 절의 단계를 함께 펴고 맨 아래에 **[확인]** 을 둔다. 다음 칸이 잠겨 있으면 단추를 안 그리고 이유를 적는다. ★★ 절 번호와 단계 번호는 더 이상 같지 않다 — 규칙은 「모든 단계는 정확히 한 절에만」으로 남는다. 진행률도 넷(열쇠가 절 id)이고, ④는 **여기서 못 잰다**고 두었다(§4.9). ★★★ 미러링 자체는 **앱이 자기 페이지를 자기 안에 넣은 것**이라 이 저장소가 못 고친다 — 근본과 고치는 법은 전달 문서 §8(`lp:base`) | 2026-08-28 | 사용자 | (이 커밋) |
| D-156 | **빨간 띠가 「둘 중 하나」로 끝나지 않는다 — 물어봐서 가른다** 〈사장님 화면〉. 앞 판은 「서버에 두 판이 섞여 있을 수 있습니다」로 끝났는데, 그 말로는 **읽는 사람이 할 수 있는 일이 없다** — 캐시를 지워야 하는지 배포가 안 된 것인지 안 갈린다(오늘 그 갈림에서 세 번 헤맸다). ★ 이제 띠가 **이 화면 주소를 캐시 없이 다시 받아** 거기 적힌 판을 본다: 지금 그려진 것과 **같으면** 서버가 옛 화면을 주는 것(캐시 지워도 소용없다), **다르면** 브라우저가 옛것을 든 것(앱 껐다 켜고 캐시 삭제). 못 받으면 **못 쟀다고 적는다.** ★★ 두 답이 **서로 다른 할 일**을 말하는 것이 이 결정의 전부다. 배포되는 화면 **여섯 전부**에 넣었고, 검사가 `build-embed` 목록으로 **하나도 안 빠졌는지**까지 잰다 | 2026-08-27 | 사용자 (오류 신고) | (이 커밋) |
| D-155 | **칸 안에 앱이 통째로 한 벌 더 그려지던 것을 막는다** 〈사장님 화면 · 심각〉. 「자료 업로드」 칸 안에 사이드바·동기화·배너까지 **앱 전체**가 들어와 있었다. 원인: 그 칸은 `files.html` 을 **상대 주소**로 품는데, 화면이 앉은 자리가 형제 폴더가 아니면 서버가 없는 파일 대신 **앱 첫 화면**을 200 으로 돌려준다. ★★ **오류가 하나도 안 난다** — 200 이고 그림도 멀쩡해서 「원래 이런 화면인가」로 읽힌다(M-56 · D-151 과 같은 결). ★ 그래서 실린 뒤 **주소를 되짚어** 부른 파일이 아니면 ① 틀을 지우고 ② 무엇을 부르려 했고 무엇이 왔는지 적고 ③ 새 탭 링크를 준다. 다른 출처면 **아무 말도 안 한다**(못 읽는 것을 고장이라 적지 않는다). ★★★ **근본은 앱 쪽**이다 — `<meta name="lp:base">` 로 자리를 알려 주면 아예 안 난다. 이것은 났을 때 **보이게** 하는 장치다 | 2026-08-27 | 사용자 (오류 신고) | (이 커밋) |
| D-154 | **근거가 얇아도 막지 않는다 — 승인하는 자리에 적는다** 〈사장님 확정〉. ① **막지 않는다**: 근거 50% 미만이 RED 로 떠도 생성은 그대로 된다(막는 검사는 사람이 꺼 버린다 · D-127). ★ `05_validation` 에는 **안 넣었다** — 거기서는 `red > 0` 이면 곧바로 REJECT 라 넣는 순간 「막지 않는다」가 거짓이 된다. 대신 **최종검증 보고서의 「종합 판정」 바로 뒤**, 사람이 승인 전에 읽는 자리에 넣었다. 못 쟀으면 **못 쟀다고 적는다.** ② **Crosscheck 12개는 100% 에 넣어 둔다**(사장님이 세신 목록이다) — 대신 「값이 아니라 무엇과 대조할지 고르는 칸 · 자동으로 못 고릅니다」를 갈래 옆에 적는다(D-48 · §4.9). 안 적으면 「12칸이 비었다」로 읽히고 사람이 채우려 든다. ③ **`LP_PUBLIC_BASE` 를 켠다** — D-99 의 「주소를 안 넣는다」를 뒤집는다. 안 넣는 동안 「앱이 쓰는 길로는 못 쟀다」만 적혔고 캐시인지 다른 파일인지 갈리지 않았다(M-25). 넣는 자리는 `docs/안내-LP-PUBLIC-BASE-등록.md` · **사람 손이 남아 있다** | 2026-08-27 | 사용자 | (이 커밋) |
| D-153 | **가이드 필드에서 값 입력칸을 없앤다** 〈사장님 지시 「입력값 삭제해줘」〉. D-152 의 짝이다 — 값을 자료·API 에서만 채우기로 했으면 93칸을 내밀며 「채워 넣으라」고 하는 것은 그 결정과 어긋난다. 빈 줄마다 [직접 넣기] 가 있으면 사람은 **모르는 값을 지어내서** 넣는다. ★ 읽는 판에서 [직접 넣기]·[고치기] 를 안 그린다(실측 입력칸 0 · 검색칸만 남음). 빈 줄에는 **무엇을 하면 채워지는지** 적는다. ★★ 아주 지우지 않고 **잠가** 두었다 — 자료에도 API 에도 없는 값이 실제로 있어(아직 안 정해진 SPC 이름) 넣을 길이 없으면 그 문서를 영영 못 끝낸다. 근거 패널의 [사람이 넣기] 를 눌러야 열리고, 켜서 넣은 값은 **「사람이 넣음」으로 따로** 선다. 켜는 쪽을 기본으로 두면 아무도 안 끄고 예전으로 돌아간다 | 2026-08-27 | 사용자 | (이 커밋) |
| D-152 | **값들의 100% 를 놓고 정보기여도와 품질을 잰다** 〈사장님 지시〉. 지금까지는 **채워진 것만** 셌다 — 열 칸 중 셋만 채워도 「문서 100%」가 나왔다. **분모가 틀리면 좋은 숫자가 거짓말이 된다.** ★ 분모는 화면과 **같은 규칙**(사전 전체 − 다른 산업 전용)이다: datacenter 94 · solar 95 · generic 93 — 사장님이 세신 목록과 같은 수다(`requiredFor('im')` 은 10개뿐이라 못 쓴다). ★★ 갈래 일곱(올린 자료·공공 API·요청문·계산·가정·사람·**미확보**) — 계산과 가정을 가른 것은 §4.8 이 그으라 한 경계이고, 미확보를 세운 것이 이 측정을 만든 이유다. ★★★ **기여도와 품질을 한 숫자로 섞지 않는다**(할 일이 정반대다) · **품질은 채워진 것에 대해서만** 낸다. 품질은 다섯 등급에 점수를 박았다(검증됨 100 … 충돌 0). 막지 않고 **적는다**(D-127 과 같은 결). 실리는 곳 셋: 화면 맨 위 근거 패널 · IM **21절** · `11_QC/evidence.json`. 셈은 `ui/platform/evidence-core.js` **한 곳**이고 엔진이 그대로 받아 쓴다 | 2026-08-27 | 사용자 | (이 커밋) |
| D-151 | **칸이 부르는 주소가 그 단계로 되짚어지지 않으면 화면이 말한다.** 신고: 「4번 가이드 필드에 3번 관련자료 업로드가 다시 나온다」. ★ 지금 소스를 헤드리스로 그려 보니 **그 화면이 안 나온다** — 4번은 `fields.html` 하나만 부른다(실측). 앱이 보는 것은 이 저장소의 지금 판이 아니다(사본은 사람이 올린다 · D-143). ★★ **더 나쁜 것은 그 어긋남이 아무 말도 안 한다는 것**이다 — 칸 머리는 「4. 가이드 필드」인데 안에는 다른 화면이 멀쩡히 그려지니 원래 그런 화면인 줄 안다. 앞의 세 단계는 같은 파일을 `?part=` 로 나눠 쓰므로, 목록이 옛 판이면 표시가 빠져 **세 칸이 한꺼번에** 뜬다 — 신고된 모습이 그것이다. ★★★ 그래서 칸을 그릴 때 **부르려는 주소를 스스로 되짚어**(`stepOfFile`) 안 맞으면 빨간 안내를 붙이고 부른 것과 되짚은 결과를 나란히 적는다. 안쪽 화면은 안 들여다본다(출처가 다르면 못 읽는다). 어느 쪽이 옛 판인지는 **단정하지 않고** 판 표시 여덟 글자를 묻는다. 멀쩡한 칸에는 아무 말도 안 한다 | 2026-08-27 | 사용자 (오류 신고) | (이 커밋) |
| D-150 | **화면(HTML)을 캐시하지 말라고 서버에 일러둔다.** D-149 는 빨간 띠가 **뜬 뒤** 화면이 스스로 한 번 다시 받게 한 것이다 — 애초에 옛 화면을 안 받으면 그 한 번도 없다. 어긋나는 쪽이 늘 화면인 이유는 형제(스크립트·CSS)는 주소에 판 표시가 붙어 판이 바뀌면 **주소가 바뀌는데** 화면에는 그런 것이 없기 때문이다. ★ 배포가 `im-flow` 에 설정 파일을 매번 놓되 **HTML 에만** 걸고 `IfModule` 로 감싼다(`mod_headers` 가 없으면 `Header` 한 줄이 **그 폴더 전체를 500** 으로 만든다). ★★ **「올렸다」로 끝내지 않는다** — 그 파일은 Apache 일 때만 읽히고 nginx 면 조용히 무시되므로, 배포가 **실제 응답 머리말**을 받아 「매번 확인 / 아직 안 켜짐」을 요약에 적는다. ★★★ 못 켜졌다고 배포를 세우지는 않는다(나머지는 DSM 을 눌러야 하는 사람 손이고 D-149 가 받쳐 준다) — 대신 **안 켜졌다는 사실을 크게 적는다.** 형제는 그대로 캐시하게 둔다(판 표시가 있어 캐시가 이득) | 2026-08-27 | 사용자 | (이 커밋) |
| D-149 | **짝 어긋남 띠가 거꾸로 말하고 있었다 · 이제 화면이 한 번은 스스로 다시 받는다.** 사장님 화면: 「화면 479c745f · **옛 판** LinkPilotGate (64478fa1)…」 — 그런데 `64478fa1` 이 **새 판**이었다. 띠가 **스크립트를 옛것으로 지목**하고 있었다(M-56 과 같은 결: 아는 척하는 진단). ★ 지문은 해시라 그 자체로는 앞뒤가 없지만 **자리 관계가 답을 준다**: 배포는 자산 먼저·HTML 나중으로 올리고(실측: `Upload` 단계가 이미 그렇다), 서버는 형제 파일의 판 표시를 무시하고 지금 파일을 준다 — 그래서 어긋나면 **거의 언제나 화면(HTML)이 옛것**이다. 문구를 그대로 고쳤다(단정하지 않고 「가능성이 큽니다」). ★★ 그리고 **새로고침을 사람에게 떠넘기지 않는다**(M-58 과 같은 결) — 어긋나면 주소에 표를 붙여 **세션당 한 번** 스스로 다시 받는다. 서버에 두 판이 섞여 있으면 다시 받아도 같으므로 **한 번만** 한다(끝없이 도는 것을 막는다). 그러고도 어긋나면 띠를 띄우고 **여덟 글자 두 개를 알려 달라**고 한다. ★★★ 고치다 §8 의 함정에 걸렸다 — 주석에 판 표시 이름을 **글자 그대로** 적었더니 지문 검사가 그것을 코드로 읽었다. 주석에서는 이름으로 부르도록 고쳤다 | 2026-08-27 | 사용자 (오류 신고) | (이 커밋) |
| D-148 | **서버 주소를 정하는 자리를 한 곳으로 모았다 — 다만 「모두 똑같이」는 아니다** 〈M-59〉. 앱이 「보고서 생성 입력」을 **통째로**(iframe 이 아니라 페이지 전체로) 열자 화면이 「API 주소가 설정되지 않았습니다」로 멈췄다. **화면은 정직했다** — 다리가 부모에서 설정을 못 받으면 주소가 빈다. ★★ 진짜 결함은 **화면마다 달랐다는 것**이다: 같은 상황에서 `files`·`report-flow` 는 `resolveApi()` 로 같은 출처 기본 주소에 떨어져 멀쩡히 돌았고 `intake`·`fields` 는 `C.api` 만 봤다. `flow-core.js` 주석이 「화면마다 따로 짐작하면 어느 화면은 되고 어느 화면은 안 된다가 되어 **원인이 안 보인다**」고 미리 경고한 바로 그 상태였다. 이제 읽는 화면 **다섯**이 한 곳에서 받는다. ★ **짐작으로 불렀으면 문장에 붙인다** — 실측에서 이 자리가 「Failed to fetch」 한 줄로 끝나 서버가 죽은 줄로 읽혔다. 지금은 「(앱이 서버 주소를 넘기지 않아 /api/linkpilot 로 불렀습니다 — 단독으로 열렸습니다)」가 붙는다(헤드리스로 실제 문장을 읽어 확인). ★★★ **`reports.html` 만은 일부러 안 짐작한다** — 산출물 **파일 링크**를 만드는 화면이라 짐작 주소로 링크를 만들면 누르면 엉뚱한 곳으로 간다. 처음에 함께 고쳤다가 `open-file.test.js` 가 잡아 되돌렸다 | 2026-08-27 | 사용자 (교차검증 지시) | (이 커밋) |
| D-147 | **작업이 끝나면 「배포 확인」 한 장을 만들어 주소로 드린다** 〈사장님 지시: 「항상 작업이 끝나면 배포본에 반영이 잘되었는지 오류가 없는지 즉시 교차검증하고 HTML 파일로 URL 로 열어서 보여줘」〉. 말로 하지 않고 **장치로** 한다 — `npm run deploy:report`. 같은 확인을 말로만 하면 바쁜 날 빠진다(M-31: 손으로 챙기다 하루 반에 여섯 번). 한 장에 셋: ① **배포가 NAS 안에서 잰 것**(단계별) ② **내 자리에서 잰 것**(`guard` 열 칸) ③ **어디부터 사람 손인지**(D-143). ★ **못 잰 것을 통과로 그리지 않는다** — 이 자리에서 NAS 를 직접 못 본다는 사실을 그 장에 적는다. ★★ 첫 줄은 판정 하나이고, 실패가 하나라도 있으면 초록으로 안 그린다(§6-3 ①). ★★★ 만들면서 잡은 것 둘: 처음 판이 guard 열 칸 중 **일곱만** 잡았다(이름 안에 공백이 있는 「[저장] 막대 자리」가 「두 칸 이상 띄면 경계」 규칙에 안 걸렸다) — **빠진 줄은 화면에서 안 보여** 아무도 눈치 못 챈다. guard 와 **같은 규칙(칸 너비 16)** 으로 갈랐다. 그리고 제목에 판 지문을 넣었다가 뺐다 — 발행할 때마다 이름이 바뀌면 목록에서 같은 장을 못 알아본다. CLAUDE.md §8 에 규칙을 적었다 | 2026-08-27 | 사용자 (지시) | (이 커밋) |
| D-146 | **눌러 보는 검사 · 다 올린 뒤 접기 · 엔진이 뜬 시각** 〈사장님 「권고안대로」 셋을 한 번에〉. ① **502 를 실제로 받아 보는 검사**를 세웠다 — 가짜 서버에 `__lpForce5xx` 를 걸어 올리는 길만 502 를 내게 하고(목록까지 죽이면 재려는 것과 다른 것을 잰다), 화면에 「잠깐 나는 고장」·「다시 고르실 필요 없습니다」·[다시 보내기]가 실제로 그려지는지 읽는다. 앞의 소스 검사는 **「그리는 코드는 있는데 그 자리까지 안 간다」를 못 잡는다**(M-05·M-25·M-57 이 그 결이다). ② **다 올린 뒤에는 ②칸을 접는다** — 위는 「완료」인데 아래에 파일 고르기가 그대로 있어 **「또 올려야 하나」**로 읽혔고 실제로 두 번 올리셨다. ①(어느 프로젝트)과 ③(무엇이 올라갔나)은 **안 접는다** — 끝난 뒤에 더 봐야 하는 것이다. [＋ 자료 더 올리기]로 되돌린다(접는 것이 막는 것이 되면 안 된다). ③ **배포마다 엔진이 뜬 시각을 요약에 남긴다** — 다음에 502 가 나면 그 시각과 대 보는 것만으로 「그 사이에 엔진이 새로 떴다=죽었다」가 갈린다. ★ 이 단계는 **배포를 막지 않는다**(못 쟀어도 `exit 0`) — 재는 장치가 배포를 세우면 바쁜 날 그 장치부터 꺼진다. 살아있음·안뜸·못잼 세 갈래를 가짜 ssh 로 **실제로 돌려** 확인했다 | 2026-08-27 | 사용자 (권고안대로) | (이 커밋) |
| D-145 | **잠깐 나는 고장(5xx)을 「내가 뭘 잘못했나」로 보이게 하지 않는다** 〈M-58〉. 사장님이 「올리지 못했습니다 · HTTP 502」만 보시고 **같은 자료를 처음부터 다시** 올리셨다 — 「2번이나 올려야 하는 번거로움」이 그것이다. 화면은 `401` 은 정성껏 갈라 설명하면서 **5xx 는 이름 한 줄로 끝내고 있었다.** 이제 ① 「보내신 파일 때문이 아닙니다」를 먼저 적고 ② **고른 파일을 그대로 둔 채 [다시 보내기]** 를 그 자리에 두고 ③ 세 번을 눌러도 같으면 무엇을 전할지 적는다. ★★ **자동 재시도는 안 한다** — 502 는 엔진이 이미 받아 읽다가 끊긴 것일 수 있어 자동으로 다시 보내면 **같은 자료가 두 벌** 생긴다. ★ 잰 것: 그 시각(07:48 KST)은 우리 배포의 엔진 재시작 창(07:30:44~58)과 **겹치지 않는다** — 배포 탓으로 돌리지 않았다. **원인은 아직 모르고, 모르는 것을 아는 척 적지 않았다** | 2026-08-27 | 사용자 (지시) | (이 커밋) |
| D-143 | **배포 요약이 「어디까지 닿았고 어디부터 사람 손인지」를 매번 적는다 · 앱에 판 지문을 박아 달라고 요청했다.** 오늘 왕복이 있었다 — 사장님은 「디자인 반영이 아직 안 됐다」, 이쪽은 「반영했고 배포도 초록」. **둘 다 맞는 말이었다**: 사장님이 보신 화면(아침 브리핑)은 이 저장소가 만드는 것이 아니고(실측 — `TODAY'S PRIORITY`·`AI INSIGHT`·`자동발송` **0건**), 우리 화면 16개는 NAS 에는 올라갔지만 **앱 저장소 사본은 사람이 올린다.** 그 경계가 배포 화면 어디에도 없어 **「초록 = 앱에 반영됨」**으로 읽혔다 (M-25·D-87 과 같은 결). ★ 그래서 배포마다 요약에 **네 줄 표**를 박는다 — NAS 화면·엔진은 배포가, `im-flow/` 사본은 사람이, 앱 자체 화면은 저쪽이. **지문은 파일에서 읽어서** 적는다(손으로 박으면 그날부터 요약이 옛말을 한다). 실패한 배포에서도 적는다 — 빨간 배포일수록 「어디까지 갔나」가 궁금하다. ★★ 그리고 앱 쪽에 **판 지문을 박아 달라**고 전달 문서에 요청했다: 지금 앱 화면에는 판을 가리키는 표시가 없어 「반영이 안 된다」가 캐시인지·안 올라간 것인지·다른 화면인지 **가릴 방법이 없다** | 2026-08-27 | 사용자 (권고안대로) | (이 커밋) |
| D-142 | **교차검증이 「그려진 글자」를 읽는다 · 열린 PR 답을 검사가 스스로 찾는다.** ① 사장님 화면에 `선정릉 → [object Object]` 가 찍혔다 — **오류를 안 내는 고장**이라 「떴는가」만 보는 우리 검사로는 안 잡혔다(M-57). 헤드리스 렌더 칸이 이제 그려진 글자에서 `[object Object]`·`undefined`·`NaN` 을 낱말 경계로 잡고 **빨갛게 끝낸다**. 검사가 늑대야가 되지 않는지(멀쩡한 글자를 안 잡는지)도 함께 잰다. ② 여덟째 칸이 늘 「열린 PR 을 못 물어봐 **나이로 어림했다**」로 끝나던 것을 고쳤다 — `.lp-open-prs.json` 이 있으면 **자동으로** 쓴다. ★★ 다만 **낡은 기록은 안 쓴다**: 어제 답으로 「지금 물어봤다」고 적는 것이 못 물어본 것보다 나쁘다(화면에 「물어봤다」만 남아 아무도 의심하지 않는다 — M-05 와 같은 결). **시각이 적혀 있고 12시간 안쪽**일 때만 쓰고, 아니면 까닭을 남긴다. `npm run prs:write` 로 한 줄에 갱신하고, 이름 없이 부르면 **거부한다**(빈 배열이 되면 겹침을 하나도 안 세면서 「물어봤다」고 적는다 — 가장 비싼 거짓말). 기록은 커밋하지 않는다(남의 세션이 옛 답을 물려받으면 안 된다) | 2026-08-27 | 사용자 (권고안대로) | (이 커밋) |
| D-141 | **뉴스 열쇠를 확정했다 — 이름은 `WORLDWIDE_NEWS` 를 앞에 두되 `WORLD_NEWS_API` 도 함께 읽는다. 그리고 배포가 「앱이 읽는 봉인 파일」을 놓게 했다.** 실측 2026-08-27: 사장님 Secrets 에 뉴스 열쇠가 **두 이름**으로 들어와 있는데 앱 화면은 「서버 없음」이었다. **원인은 이름이 아니라 파일이었다** — 앱은 GitHub Secrets 를 못 읽고 웹루트의 봉인 파일(`worldnews_key.store.php`)을 읽는데, **그 파일을 놓는 단계가 이 저장소에 없었다.** 앱 세션이 그 단계를 만들어 `docs/패치-앱-봉인-열쇠-단계.patch` 로 넘겼는데(2026-08-25, PAT 에 workflow 권한이 없어 직접 못 밀었다) **이틀 동안 파일로만 있었다** — 파일로만 있는 패치는 아무것도 안 한다. ★★ 적용하면서 **이름을 하나로 고르지 않고 둘 다 읽게** 했다 (M-55 · M-40 의 되풀이 패턴): 한 이름만 보면 다른 쪽에 넣으신 값이 조용히 죽는다. 앞 이름이 우선이고 **어느 이름을 썼는지 로그에 대며**, 둘 다 있으면 경고로 알린다. ★ 덮기 전에 옛 봉인 파일을 백업한다(지금 살아 있는 열쇠일 수 있다) · 옛 평문 `<이름>_key.txt` 는 빈 파일로 덮는다 · 값은 stdin 으로만 간다(§2). 검사 둘을 걸었다: 단계가 있는가 · **읽는다고 적은 이름이 전부 `env:` 로 들어오는가**(목록에만 있으면 언제나 빈 값이다) | 2026-08-27 | 사용자 (지시) | (이 커밋) |
| D-139 | **지시서 rev.B 를 반영했다 — 선택 색이 고쳐져 왔고, 그 김에 우리 화면의 라임 글자 셋을 잡았다.** rev.A 의 선택 색은 되돌려 보냈고(대비 2.34), rev.B 가 **밝은 바탕 `#5A7A00`(실측 4.98) · 어두운 바탕 `#B5E01F`(11.76)** 로 갈라 정해 왔다. `--lp-brand-deep` 을 그 값으로 바꿔 **11px 라벨이 작은 글자 기준 4.5 를 처음으로 넘겼다**(3.02 → 4.98). ★★★ **rev.B 규칙을 우리 화면에 대 보니 세 자리가 어기고 있었다** — 「밝은 바탕에서 라임을 글자 색으로 쓰지 말라」(대비 1.54)인데 `.side__mark`·`.logo__mark` 가 흰 사이드바에 라임 글자였고 `.logo em` 워드마크가 24px 라임 글자였다. 앞 둘은 **라임 채움 + 잉크 글자**(11.76)로, 워드마크는 딥으로 고쳤다(화면 여섯). **지시서를 「저쪽 것」이라고 넘겼으면 못 잡았을 자리다.** ★ 폐기된 rev.A 색을 **주석에서도** 지웠다 — rev.B 07-B ④ 가 「검색 0건」을 검수 항목으로 걸었다. 검사 둘을 새로 걸었다(폐기 색 0건 · 밝은 바탕 라임 글자 금지), 그리고 두 번째 검사가 부모 규칙의 어두운 바탕을 못 봐 **멀쩡한 코드를 빨갛게** 만들던 것을 고치면서 **그 느슨함이 구멍인지 재는 검사**를 나란히 뒀다. ★★ 저쪽에 알린 것 둘: `Critical #E5342B` 는 적힌 4.5 가 아니라 **실측 4.32** · `#5A7A00` 은 회색 면(`#F2F2F7`) 위에서 **4.46** 으로 아슬하게 미달 | 2026-08-27 | 사용자 (지시서 rev.B) | (이 커밋) |
| D-138 | **브랜드 색을 1단계 작업지시서 값으로 맞췄다 — 다만 선택 색 하나는 안 받았다.** 지시서가 브랜드 색 셋을 못박았는데 이 저장소는 셋 다 조금씩 다른 값이었다(라임 `#AAE106`→`#B5E01F` · 잉크 `#0A1419`→`#12161F`). 그대로 두면 **사이드바 라임과 보고서 화면 라임이 미묘하게 어긋난다.** ★★★ **선택 색 `#8CB80F` 는 안 받았다 — 흰 바탕 대비 2.34 로 큰 글자 기준(3.0)도 못 넘긴다.** 그 토큰은 11px 글자 색으로 쓰이므로 4.5 가 필요하다. 지금 값 `#7BA10F`(3.02)가 그보다 낫다 — **더 나쁜 쪽으로 바꾸지 않는다.** ★ 대상 범위는 platform 이다(실측: 보고서 화면 16개의 아이콘·svg **0개**, 파비콘 없음) — WO-01·WO-02 는 이쪽에 반영할 것이 없다. ★★ 지시서 결함 셋을 저쪽에 알렸다: 선택 색 대비 · 라임 면적 12~30% 인데 프리미엄 38% · 로고 꼭짓점 폭 0(「최소 획 두께 8px」 위반) | 2026-08-26 | 사용자 (권고안대로) | (이 커밋) |
| D-137 | **복원시험을 `deploy-nas` 에 걸었다 — 나갈 때마다 NAS 안에서 돈다.** D-136 의 장치는 **손으로 돌려야 돌았고, 그러면 안 돈다** (D-88 과 같은 결). 그리고 **개발 자리 통과는 「이 코드가 맞다」이지 「그 디스크가 되살아난다」가 아니다** — 지침 §11-4 가 요구하는 것은 뒤쪽이다. ★ **배포를 막지 않는다**: 막으면 자료가 커진 날 배포가 서고 그러면 이 검사가 꺼진다. ★★ `--max-mb` 한도(기본 2000MB · `LP_BACKUP_MAX_MB`)를 넣어 **넘치면 「못 쟀다」로 끝낸다** — 두 벌을 만드는 시험이라 자리가 모자랄 때 반쯤 하고 통과하는 것이 가장 나쁘다. **표본을 몰래 줄이지 않는다.** ★★★ 곁가지로 잡은 버그: `backup.js` 가 뜰 자리를 `<저장소>/im-projects` 로 박아 둬서, 운영 자리(`IM_AGENT_ROOT`)에서는 **빈 폴더를 뜨고 「되살아난다」**고 말할 뻔했다 — `store.root()` 에서 가져오도록 고쳤다 | 2026-08-26 | 사용자 (권고안대로) | (이 커밋) |
| D-136 | **`request_id` 를 만들었다 (`core/request.js`) — 감사 H-3.** 공통 식별자 열 중 **이것만 코드에 0곳**이었다(`project_id` 82개 파일 · `status` 114개). 그래서 **같은 프로젝트를 두 번 요청하면 갈라 볼 수 없었다** — 지침 §11-1 이 완료의 첫 조건으로 삼은 자리이고, 필수 게이트 2를 막던 유일한 항목이다. `REQ-<projectId>-<3자리>` 꼴로 **세어서** 만든다(무작위·시각을 안 쓴다 — 같은 입력이 다른 답을 내거나 자정에 걸린다). 장부는 **이어 붙이기만** 한다. 파이프라인이 열고 닫으며 **어떻게 끝났는지**를 함께 적는다. ★ 만들면서 낸 버그를 잡았다: 다음 번호를 `list()` 로 세어 **닫는 줄까지 세어져 두 번째가 `-003`** 이 되었다 — **값도 형식도 멀쩡해 문서만 봐서는 안 잡히는** 꼴이다. `opened()` 로 고치고 검사로 박았다 | 2026-08-26 | 사용자 (권고안대로) | (이 커밋) |
| D-135 | **디자인 게이트 7단계를 상태기계로 만들었다(`core/design-gate.js`) · 「외출모드」를 문서에서 실제로 지웠다.** ① 지시서 §8.4 의 일곱 중 코드에는 `DESIGN_VERIFIED` **하나만** 있었다 — 지침이 그 게이트를 완료 조건으로 삼는데 **완료 판정의 근거가 문서에만 있었다.** 규칙 셋: **한 칸씩만** 간다(건너뛰면 게이트는 장식이다) · **뒤로는 간다**(앞으로만 가면 한 번 틀리고 끝까지 거짓말한다) · **승인 칸 둘은 사람만**(`WIREFRAME_APPROVED`·`READY_TO_DEPLOY` — 기계가 스스로 승인하면 승인이 아니다). ★ 승인 절차를 새로 만들지 않고 **이미 있는 출력 사양 확정**(AI 가 못 하게 막힌 것)을 읽는다 — 하나를 더 만들면 사람이 두 번 승인하고 **둘 중 하나는 반드시 형식이 된다.** ② 외출모드는 D-121·D-124 로 이미 「없앤다」였는데 **문서에서 지운 적이 없어** 감사에서 다시 잡혔다 — **결정이 있는데 반영이 없으면 결정이 없는 것과 같다.** 지시서 §2.2·§8.1 과 플랫폼 지침 §4 에서 뺐고, 경위를 적은 절은 `✅` 로 바꿔 남겼다 | 2026-08-26 | 사용자 (지시) | (이 커밋) |
| D-134 | **결정 표를 `docs/결정-기록.md` 로 떼고, 등록부를 읽는 곳을 `im-agent/core/registry-doc.js` 한 곳으로 모았다.** 열린 항목과 닫힌 결정이 한 파일에 있으면 **두 갈래가 늘 같은 파일을 고친다** — 오늘 실측에서 이 갈래와 `claude/platform-manager-t22` 가 그 파일 하나로 겹쳐 교차검증 여덟째 칸이 노랑으로 끝났다. 저쪽이 표를 한 줄도 안 건드리고 있어 지금이 옮기기 가장 싼 때였다. 읽는 곳을 모은 이유는 따로다 — 다음에 또 옮길 때 **한 곳이 빠져도 오류가 안 나고**(읽긴 읽으니까) 그 한 곳만 조용히 옛 파일을 읽는다. ★ 결정 **본문**(경위)은 등록부에 그대로 두었다: 3,095줄로 진짜 덩치지만 저쪽 갈래가 결정 항목 셋(D-06·D-08·D-120)을 지금 고치고 있어, 옮기면 저쪽에 파일 통째 충돌을 넘긴다 — 저쪽이 합쳐진 뒤에 옮긴다. ★★ 곁가지로 `branch-doctor.js` 가 git 이 감싼 한글 파일 이름을 안 풀어 **문서를 「같이 고친 코드」로 세던 것**을 잡았다 | 2026-08-26 | 사용자 (권고안대로) | (이 커밋) |
| D-129 | **테마 넷(다크·고대비·인쇄·색약)을 지금 만들지 않는다 — 문체 셋은 대기다.** 디자인 지시서 §6 이 테마 넷을 적어 두었지만, 지금 이 저장소에서 나가는 산출물의 테마 요구는 **도면(§6-1-1: 다크 모드에서도 흰 종이)** 하나뿐이고 그것은 이미 지켜지고 있다. 넷을 미리 만들면 **쓰이지 않는 네 벌을 계속 맞춰야 하고**, 화면이 바뀔 때마다 넷이 함께 어긋난다 — 어긋나도 아무도 안 본다. ★ 대신 **순서 규칙**을 `design-rights.test.js` 주석에 박았다: 테마가 필요해지면 **토큰(`tokens.css`)을 먼저 갈래 짓고** 화면을 고치는 것이 아니다. 문체 셋(격식·중립·간결)은 IM 본문 쪽이라 `core/outputspec.js` 가 정할 일이고, 그쪽 사양이 서기 전에는 짓지 않는다 | 2026-08-26 | 사용자 (권고안대로) | (이 커밋) |
| D-125 | **다음부터 갈래를 나눈다 — 그리고 영역 표를 실측대로 고쳤다.** 한 갈래에 화면·엔진·문서·도구가 섞여 있어서 되돌릴 단위가 없었다. 앞으로는 `docs/갈래-주인.json` 의 `_앞으로` 에 적은 주인별로 나눈다(화면 / 엔진 / Engineering / SketchUp 을 **따로** 둔다 — 앞 판은 뒤 둘을 한 주인으로 묶어 두었는데 실제로는 서로 안 겹친다). ★ **표 자체가 틀려 있었다** — `_영역` 의 짝짓기 순서 탓에 `im-agent/ui/` 밑 파일 **12개가 엉뚱한 주인**에게 갔다. 화면을 먼저 보게 순서를 바꾸고 `raster`→`core/raster.js` · `render`→`render-` 로 좁히니 **12 → 0**. 순서에 기대는 짝짓기는 이렇게 조용히 틀린다 | 2026-08-26 | 사용자 (권고안대로) | (이 커밋) |
| D-118 | **비밀 노출 검사는 지금은 YELLOW(경고)로 두고, 「막기」로 올릴 때는 세어서 정한다.** 지침 §8·§12 와 CLAUDE.md §2 가 배포 전 비밀 검사를 요구하는데 게이트에 그것이 없었다 — `design/check.js` 의 `checkRights()` 가 키·전화번호를 값으로 찾는다. **바로 막지 않는 이유는 오탐이다**: 한 번 헛되이 막으면 다음부터 아무도 그 경고를 안 본다(늑대야). 그래서 `npm run rights:count` 가 **몇 번 돌았고 몇 번 헛걸렸는지**를 센다 — **20번 넘게 돌고 오탐 비율 10% 미만**이면 그때 RED 로 올린다. 오탐은 `docs/권리검사-오탐.json` 에 사람이 적는다. 지금은 `3번 돌았다 · 걸린 것 0건` 이라 **아직 올릴 때가 아니다**. ★ 「느낌」이 아니라 **숫자가 때를 정한다** | 2026-08-26 | 사용자 (권고안대로) | (이 커밋) |
| D-114 | **미리보기가 자기 판 지문을 따로 찍는다 — 지문이 둘이 됐다.** 화면 아래 `판 xxxxxxxx` 는 **배포 묶음 16개**에서만 만드는데 미리보기를 만드는 `changes.js`·`build-preview.js` 는 그 16개에 없다. 그래서 미리보기 내용이 통째로 달라져도 **지문이 안 바뀌었다**(실측: 내용 `24e0ca68→d52943ea`, 지문 `3f9e0360` 그대로). §8 이 그 여덟 글자를 「사진 한 장으로 판을 가리는 것」으로 적었는데 미리보기에서는 **틀린 답**을 주고 있었다. `ui/platform/preview-stamp.js` 를 만들어 `판 앱-xxxxxxxx · 미리보기-xxxxxxxx` 둘을 나란히 찍는다. ★ **아티팩트 자신이 아니라 만드는 소스 넷을 해싱한다** — 지문이 든 파일을 해싱하면 자기 자신을 못 센다. 실측으로 독립을 확인했다(엔진만 고치면 앱 지문만, 미리보기 소스만 고치면 미리보기 지문만 바뀐다) | 2026-08-26 | 사용자 (권고안대로) | (이 커밋) |
| D-120 | **두 저장소를 이름이 아니라 「일」로 가른다 — `linkpilot-platform` 은 유저 접촉, `linkpilot-cron` 은 보고서 요청·생산.** 그래서 보고서 화면 16개는 **cron 이 본체**이고 platform 의 `im-flow/` 는 사본이다(실측한 복사 방향과 일치). 지침 §6 이 말한 `sync-im-flow.js` 가 두 저장소 어디에도 없어 **16개가 같은 것은 약속이 아니라 우연**이었다 — 만들어서 `guard` 아홉째 칸으로 넣었다. ★ 재어 보니 **사본은 이미 15개가 옛 판**이다. 내용이 아니라 판 지문이 다르다 — 화면마다 `LP_BUILD` 가 박혀 있어 **cron 이 한 번 바뀌면 16개 전부** 새 지문이 찍힌다. **어긋나는 것이 예외가 아니라 기본값**이므로 「가끔 확인」으로는 못 잡는다. 남은 일 둘은 사람 손이다 — 사본 재전송 · platform 문서에 「이것은 사본」 적기(지금 넷 다 `im-flow` 를 한 번도 언급하지 않는다) | 2026-08-26 | 사용자 | (이 커밋) |
| D-131 | **옛 커밋에 남은 평문 개인 휴대폰 번호는 그대로 둔다.** 지금 판(모든 갈래의 맨 위)에서는 이미 지웠으므로 저장소를 여는 사람이 보통 마주치는 자리에는 없다. 역사를 다시 쓰지 않는 이유는 값이 아니라 **깨지는 범위**다 — 모든 커밋 번호가 바뀌면 열린 갈래·다른 세션 작업본·등록부와 커밋 메시지의 번호가 한꺼번에 어긋난다. 나중에 비공개로 바꾸고 싶어지면 D-10 을 다시 여는 것이지 이 결정을 무르는 것이 아니다. 후속은 **다시 안 생기게 하는 쪽**이다 — `design/check.js` 의 `checkRights()` 가 번호를 값으로 찾아 낸다(D13) | 2026-08-26 | 사용자 (권고안대로) | (이 커밋) |
| D-130 | **뿌리가 다른 옛 크론 갈래 셋은 `claude/ops-cron-alerts-and-deploy` 를 본체로 모은다.** 셋 다 `main` 과 공통 조상이 없어 영원히 안 합쳐진다. 가른 기준은 파일 수가 아니라 **검사와 CI 를 가진 것**이다 — 이 갈래만 `test/cron.test.js` 와 `cron-ci.yml` 을 갖고 있어 고쳐도 맞는지 잴 수 있다(고유 파일 7개 · 가장 최신 08-13). `linkpilot-deployment-setup-w8yiqz` 의 고유분(`preflight.js` + 두 커밋)을 여기로 합쳤고(`2dc8ce3`), `start-luizd0` 은 부분집합이라 옮길 것이 없었다. **셋 다 지우지 않았다.** `docs/갈래-주인.json` 에 `합치지않음: true` 를 붙여 감시 화면의 「아직 안 합친 갈래」에서 빼되 따로 적는다 — 영영 안 합쳐지는 것을 할 일로 세면 그 숫자가 0 이 되는 날이 안 온다. ★ 이 갈래들에만 있던 **평문 개인 휴대폰 번호**를 지금 판에서 지웠다(§2 · 저장소 공개). 옛 커밋은 D-131 로 갈랐다 | 2026-08-26 | 사용자 (판단 위임) | (이 커밋) |
| D-83 | **MCP 는 값을 들여오는 길이 아니라 LinkPilot 을 내보내는 길로 쓴다 — 읽기 전용으로.** 바깥 컨텐츠는 `connectors/` 의 REST 커넥터 그대로 두고, 엔진이 **이미 가진 것을 꺼내 주는** MCP 서버를 만들었다(`im-agent/mcp/`). 들여오는 쪽에 놓으면 다섯이 깨진다 — 출처(§4.7)·호출 한도(§4.5)·시크릿 마스킹(§2)·가정 섞임(§4.8)·실패 격리(§4.6). 내보내는 쪽은 값을 만들지 않으므로 출처가 그대로 따라온다. **도구 6개는 전부 `ui/api-router.cjs` 의 GET 라우트 위에 서고 쓰기 라우트 23개는 하나도 안 나간다** — 표를 베껴 적지 않고 검사가 고정한다. **포트를 열지 않는다**(표준입출력) — 부를 수 있는 사람은 그 폴더를 이미 읽을 수 있는 사람이고, 남을 대신해 띄울 때만 `LINKPILOT_MCP_PROJECTS` 로 좁힌다. 상주 프로세스가 없어 NAS RAM 1.8GB 를 안 건드린다. 쓰기를 열 것인가는 **D-107** 로 분리 | 2026-08-25 | 사용자 | (이 커밋) |
| D-113 | **검토 넷 중 인허가·법률만 새 Agent 로 만든다.** T17 기술검토는 Engineering, T18 Risk 는 Engine 이 넓히고, T19 PPT 는 담당이 아니라 **기능**이다(내용은 Engine 이 만들고 형식만 바꾼다). 담당을 늘리면 경계 다툼도 같이 는다 — Engineering 과 SketchUp 이 한 갈래에 섞인 것이 그 예다. 병합 뒤에 만든다: 지금 열면 견줄 짝이 6→15 | 2026-08-26 | 사용자 | (이 커밋) |
| D-115 | **작업상태는 저장을 한 벌로 두고 지침 이름을 파생으로 낸다.** 서로 다른 이름 43개인데 넷 다 가진 것은 BLOCKED·FAILED 둘뿐이다. 네 벌을 따로 저장하면 반드시 어긋난다 — `confidence.js` 가 A~E 에서 이미 겪고 적어 둔 그것이다. **`PLANNED` 는 이름을 바꾼다** — 지침은 「곧 돈다」 코드는 「못 돈다」로 정반대이고 같은 낱말이라 어떤 검사에도 안 걸린다. 병합 뒤에 한다 | 2026-08-26 | 사용자 | (이 커밋) |
| D-116 | **신뢰도는 파생으로만 낸다.** 저장은 `confidence`(0~1) + `verified` 그대로. HIGH=A · MEDIUM=B · LOW=C·D · UNVERIFIED=E. 표기가 느는 것은 괜찮고 **저장이 느는 것이 위험하다** | 2026-08-26 | 사용자 | (이 커밋) |
| D-122 | **작업지시서는 판정에 쓰이는 셋만 먼저 넣는다** (`scope_in`·`scope_out`·`acceptance_criteria`). 지침 §5 의 「입력값·완료조건·제외범위가 없으면 NEEDS_INPUT」 판정에 실제로 쓰이는 것이 이 셋뿐이고, 지금은 볼 칸이 없어 판정 자체가 안 된다. 20개를 한 번에 넣으면 빈 칸으로 남고 **빈 칸이 많으면 아무도 안 본다** | 2026-08-26 | 사용자 | (이 커밋) |
| D-123 | **Design Manager 를 병합 뒤에 하나만 만들고 네 전문영역은 모드로 운영한다.** 지시서 §2.2 가 스스로 그 길을 열어 두었다. 값을 재어 두었다 — 갈래 하나면 견줄 짝 6→10, 넷이면 6→28. 경계는 §8.4 가 이미 정했다: **Platform Manager 가 만들고 Design Manager 가 막는다**(`DESIGN_VERIFIED` 못 통과하면 완료 아님). 바탕은 이미 있다 — `design/` 8파일 | 2026-08-26 | 사용자 | (이 커밋) |
| D-126 | **문체 프로필을 테마에 칸으로 더한다.** 새 모듈을 만들지 않는다 — 따로 두면 「테마는 A 인데 문체는 B」가 가능해지고 어긋난 문서는 읽는 사람이 먼저 알아챈다. 9종을 `WRITING` 한 곳에서 정의하고 테마는 **이름만** 가리킨다(글꼴 D-52 에서 겪은 13군데 복붙을 되풀이하지 않는다). **셋은 일부러 비웠다** — 연구·법률·법원은 맞는 테마가 없다(D-129) | 2026-08-26 | 사용자 | (이 커밋) |
| D-127 | **권리·비밀 검사를 경고(YELLOW)로 먼저 붙인다.** 오탐 하나에 문서가 아예 안 나가면 사람들은 **검사를 꺼 버린다** — 그러면 없느니만 못하다. 세어 본 뒤 RED 로 올린다(D-118 의 첫 판이다). **열쇠는 이름이 아니라 값으로 찾고**(이름이면 안내문까지 걸린다) **찾은 값을 메시지에 넣지 않는다**(§2). 권리는 확정하지 않고 사람에게 넘긴다(§10.5) | 2026-08-26 | 사용자 | (이 커밋) |
| D-128 | **`outputspec` 의 PDF 선언을 `supported: true` 로 고친다.** `core/pdf.js`(D-53)가 만들고 `pipeline.js` 가 부르고 데모에 `im-a4.pdf` 가 실제로 있는데 선언만 `false` 로 남아 있었다 — **고친 뒤 선언을 안 되돌렸다.** 화면이 「PDF 안 됩니다」라고 말하면 **사용자는 되는 기능을 안 쓴다.** pptx·docx·hwp 는 새 의존성이 필요해 그대로 false | 2026-08-26 | 사용자 | (이 커밋) |
| D-124 | **외출모드는 없앤다 — D-121 이 이긴다.** 디자인 Agent 지시서 §2.2·§8.1 과 플랫폼 지침 §4 에 외출모드가 나오지만, 그것은 지침을 쓸 때 D-121 결정이 반영되지 않은 것이지 새 지침이 옛 결정을 덮은 것이 아니다. **지침 원문은 고치지 않고** 그 줄들을 「모바일」로 읽는다 — 고치면 무엇이 원문이고 무엇이 내 해석인지 사라진다. 모바일 지원은 남는다. 되풀이를 막으려고 `npm run d:conflict` 를 붙였다 — 「안 하기로 정한 것」이 새 문서에 다시 나오면 그 자리에서 말한다 | 2026-08-26 | 사용자 | (이 커밋) |
| D-119 | **`LinkPilot Platform Manager` 를 만든다.** 지침 §3.2 의 12가지(화면·API·DB 연결·상태 표시·반응형·테스트·빌드·PR)를 맡는다. 나는 「병합 뒤」를 권했으나 사장님이 지금 만들라 하셨다 — **두 층으로 갈라** 위험을 줄였다: 역할 등록(`router.js`·`taskplan.js`)은 지금(이 갈래에서 새로 만든 파일이라 충돌 0), 실제로 코드를 미는 세션은 열되 **병합까지 push 금지**. §3.1 에 따라 Orchestrator 는 이제 화면 코드를 직접 만들지 않는다 | 2026-08-26 | 사용자 | (이 커밋) |
| D-121 | **「외출모드」를 만들지 않는다.** 플랫폼 자동완성 지침 §4 의 그 줄에서 외출모드를 범위에서 뺀다(모바일 지원은 남는다). 물어본 이유가 그대로 답이었다 — 두 저장소에 「외출」이 0건이라 **무엇을 만들지 알 수 없었고**, 짐작으로 만들면 같은 지침 §14 의 「합의되지 않은 계약 변경」이 된다. 지침 원문은 사장님이 내리신 것이라 고치지 않고 부록 B 에 결정만 적는다 | 2026-08-26 | 사용자 | (이 커밋) |
| D-10 | **저장소를 public 으로 유지한다.** 8월 29일 Actions 한도가 초기화돼도 private 로 되돌리지 않는다. Actions 분을 안 쓰고(무료) 갈래 넷의 CI 가 계속 돈다. **두 가지가 따라온다** — ① `CLAUDE.md §8` 의 「아티팩트 주소를 저장소에 적지 않는다」가 임시가 아니라 **상시 규칙**이 된다 ② git 이력의 실명·전화번호(`f25ced6`)는 **여전히 공개된 채**이고, 이력 재작성은 남은 숙제다 | 2026-08-26 | 사용자 | (이 커밋) · **앞선 결정(2026-08-22)**: private 으로 전환했었다 — 익명 404 실측, 인증 경로는 정상. Actions 무료 분이 소진되어 2026-08-25 에 되돌렸고 08-26 에 public 유지로 확정했다 |
| D-105 | **부지에 접한 도로필지(지목 「도로」)를 계획 파일에 동봉한다** — 연속지적도 bbox 1회(parcelAt 과 같은 활용신청) + 접함 3m 필터 + 토지특성 지목 확인분만 `site.road_polygons_mm` 에(지적선과 같은 원점). 지목 미확인은 도로로 단정하지 않고 미확보면 null + notes. 호출 bbox 1 + 토지특성 ≤12(30일 캐시, 가드 테스트 5→7 근거 주석). **bbox 는 실키 스모크 전** — 통과해야 배선 완료 | 2026-08-25 | 사장님 | (이 커밋) |
| D-104 | **개념 동 길이를 GFA 로 자른다** — 건폐율 기준만으로 그리면 다 채웠을 때 연면적이 문서 GFA 를 31% 초과했다(용적률 323% 상당, 그림과 숫자가 다른 건물). `GFA ÷ (층수 × 동수 × 깊이)` 상한 + notes 기록. 데모 98.2→75.0m (75×13×2×20 = 39,000㎡ = 문서 GFA). 테스트가 「동 총량 ≤ GFA」 고정 | 2026-08-25 | 사장님 | (이 커밋) |
| D-103 | **문자(SOLAPI) 실패 알림을 지웠다** (구 D-99 — 2026-08-25 재번호). 열쇠 넷이 한 번도 안 들어와 **한 번도 안 나간 채로** 자리만 지키고 있었고, 유료 서비스라 계정부터 만들어야 했다. 규칙(§2 「조용히 죽지 않는다」)은 그대로이고, 지금의 실패 통보 경로는 **GitHub 이 실행 실패 시 보내는 메일**이다. ★ 그 메일이 안 오는 상태가 되면 그때가 알림을 다시 만들 때다. ★★ 번호를 옮긴 까닭: 저쪽 작업선이 D-99 를 「계산 자리는 09 하나」로 이미 쓰고 있었고 **코드와 결정 기록에 박혀 있다** — D-77 의 기준(코드가 참조하는 쪽이 번호를 지킨다)대로 이쪽을 밀었다 | 2026-08-24 | 사장님 | (재번호: 이 커밋) |
| D-101 | **같은 이름의 Agent 가 두 벌이던 것을 한 벌로 합쳤다.** 두 작업선이 같은 갈림점에서 나와 각자 `12_sketchup_plan` 을 만들었고 **같은 파일(`04_Property/model-plan.json`)을 쓰고 있었다** — 병합이 「한쪽을 고르는 것」으로 끝나면 진 쪽 설계가 오류 하나 없이 사라진다. ★ 합친 방식: 계획 쪽 스키마(`model-plan/1`)가 규격 문서와 `13_sketchup_intake` 의 계약이라 **그쪽을 그대로 두고**, 요청 쪽(`deliverables` — 출력조건에서 켠 [지적도면]·[조감도])을 한 절로 얹었다. **요청은 거절 규칙보다 먼저 정한다** — 매스가 없거나 RED 라 계획을 안 낼 때도 「켜 두신 시각자료를 못 만든다」는 말해야 하기 때문이다. ★ 결과 수령은 `13_sketchup_intake` 가 그대로 맡는다(Agent 13개). order 는 정수로 다시 매겼고 진행률 가중치는 09=8·12=2·13=2 (합계 100). ★★ 배울 것: **번호를 비워 두는 약속으로는 못 막는다** — 실제로 D-96~D-98 과 M-33·M-34 가 두 뜻을 갖게 됐다 (D-102) | 2026-08-25 | 사장님 「권고안대로 확정」 | (이 커밋) |
| D-34 | **조감도 자동 렌더를 연다 — 생성은 자동(Gemini · Nano Banana Pro 계열), 채택은 사람.** 「API 실측 전 보류」를 개정으로 닫음: `npm run im:render` 가 기하 조감도에서 후보를 만들고, 사람이 `--adopt` 로 눈 확인 후 등록해야 renders 에 실린다. disclaimer·도구 세대 기록은 후보 단계부터 자동(규격 §3-1). 2차 개정(같은 날)으로 **IM 본문에도 싣는다** — 표기·순서로 막고 기하 조감도는 대체하지 않는다. 실측 완료(render-smoke #1): 모델 이름·인증 통과, 무료 티어 이미지 한도 0 — 남은 것은 결제 연결(사람 몫) | 2026-08-25 | 사장님 「보류하지 말고 자세히 안내」 | (이 커밋) |
| D-92 | **3·4·5단계가 토큰 없이 떠 401 이 나던 원인은 본체가 아니라 우리였다.** 앞 판은 「앱이 `window.LINKPILOT_EMBED` 를 안 채운다」로 단정하고 본체에 인계서까지 냈다. 실측하니 앱은 계약대로 매 렌더마다 채우고 있었고(`ReportHubView`), 못 읽은 쪽은 `embed-bridge.js` 였다 — `files`·`fields`·`intake` 는 `report-flow.html` **안에 한 번 더 들어간** iframe 이라 `window.parent` 가 앱이 아니라 `report-flow` 다. 직계 부모만 보던 `fromParent()` 가 거기서 빈손이 되어 토큰 없이 떴다. ★ 고침: 조상을 **위로 8단계까지** 거슬러 처음 만나는 `LINKPILOT_EMBED` 를 쓴다(같은 출처 안에서만). 못 찾으면 이유에 **몇 단계까지 봤는지**를 적는다. ★ `resolveApi` 의 같은 출처 기본값(`/api/linkpilot`)은 **그대로 둔다** — 계약이 깨져도 화면이 죽지 않게 하는 안전망이고, 짐작으로 불렀다는 사실은 `apiNote` 가 문장에 붙인다. ★★ 배울 것: 「부모가 채우지 않았습니다」는 **브리지가 스스로 만든 문장**이었는데 그것을 증거로 읽었다. 남의 잘못으로 단정하기 전에 우리가 볼 수 있는 자리를 다 봤는지 먼저 본다 | 2026-08-23 | 사용자 | `0b58998` |
| D-94 | **자료 업로드를 로그인 없이 되게 한다 — 「만드는 것까지」만 연다.** 원인은 둘이었다: ① `createProject` 가 `gate(ctx,'pro')` 라 잠겨 있었는데 `oneshotUpload` 는 D-82 로 이미 열려 있어 **자료를 넣는 길은 열려 있고 넣을 그릇은 못 만드는** 상태였다 — 열어 둔 셋이 통째로 못 쓰였다. ② 화면이 처음 부르는 `GET /projects`(목록)는 일부러 로그인을 묻는데, 그 401 하나가 `state.gate` 를 세워 **카드를 통째로 덮었다** — 열려 있는 길까지 막혔다. `localGate` 가 그 자리를 위해 이미 있었는데 여기서만 안 쓰고 있었다. ★ 연 선: `createProject` · `linkSource` · `oneshotUpload` · `scanSources` **넷**. `uploadSources`(보관) · `saveSpec` · `generate` 와 읽기·지우기는 **그대로 묻는다**. ★★ 치르는 값: 이 서버는 Funnel 443 으로 **바깥에서도 닿는다** — 주소를 아는 사람이 로그인 없이 프로젝트를 만들고 1회성 자료를 올릴 수 있다. **읽어 가는 길은 안 열었으므로 남의 자료가 나가지는 않는다**(새는 것이 아니라 쌓이는 쪽). 되돌리는 법: `createProject` 의 `gate(ctx, FILES_PLAN, ANON)` → `gate(ctx,'pro')`. 검사 셋이 바로 빨개진다. ★ 함께 알게 된 것: `saveSpec` 은 **문지기가 둘**이라 하나만 열어도 안 뚫린다 — 검증하다 확인했고 그 층은 그대로 뒀다 | 2026-08-23 | 사용자 | (이 커밋) |
| D-87 | **엔진은 멀쩡했다 — 404 는 앞단(포트 80)에서 났다.** 엔진을 새 판으로 올린 뒤 안팎에서 같은 주소를 두드려 갈랐다: 엔진 직행 8181 `/api/linkpilot/intake` **200** · 바깥 포트 80 같은 주소 **404** · 8181 의 `/intake` 는 404(접두사로 붙어 있다 — 정상). ★★★ **이 항목은 처음부터 「서버가 옛 라우팅 표를 들고 있다」 하나로 적혀 있었고, 나는 그 하나로 단정해 사장님께 「NAS 만 옛 판을 들고 있는 것이 맞습니다」라고 말했다. 엔진을 갈아 보니 아니었다.** M-25 가 통째로 이 이야기다 — 잰 길(포트 80)이 앱이 쓰는 길(Funnel 443)과 다르면 다른 것을 본다. 그때는 **화면 파일**에 대해 적었는데 이번엔 **API** 에서 같은 함정이 자리를 옮겨 다시 왔다. 딸린 결과: `Check routes` 는 갈라 재기 판정이 `front` 이고 `LP_BASE` 가 없을 때만 **경고로 낮춘다**(`engine`·`접두사`·`못 갈랐다` 는 그대로 치명) — 그대로 두면 멀쩡한 배포가 늘 빨갛고, **늘 빨가면 빨간 것을 아무도 안 본다**(`verify-served.sh` 가 HTTP 경고를 두고 적어 둔 그 이유). 남은 일: **`LP_BASE` 를 넣으면** 앱과 같은 길로 재서 이 경고가 사라진다 | 2026-08-23 | 실행 #57 실측 | (이 커밋) |
| D-88 | **배포를 자동으로 돌린다 — `main` 에 닿는 순간. 그리고 엔진도 함께 올린다.** 〈사장님 지시: 「모든 파일과 구조를 정립해줘 실시간 자동배포가 되도록」〉 ★ **D-84 의 「수동 전용」을 뒤집는다.** 그때의 이유는 「아무도 안 보는 사이에 운영 화면을 덮지 않으려고」였는데 **실제로 일어난 일은 반대**였다 — 눌러야 도니까 안 눌렀고, 그사이 NAS 가 조용히 옛 판으로 남았다(D-86). 같은 결이 `verify:nas` 에도 있었다: 만들어 두고 아무도 안 쳐서 같은 404 가 다시 났다(M-11 · M-12 · D-87). **막는 장치는 돌아야 막는다.** ★ 「쓰는 이는 하나」(D-84 의 핵심)는 **그대로 지킨다** — 워크플로가 그 하나이고 `deploy/screens.sh`(화면) · `deploy/nas.sh`(엔진)는 대비다. ★★ **엔진을 함께 올린다**: 지금까지 엔진은 아무도 안 올렸고(NAS 의 `im-agent.bak-*` 스무 개가 증거) 그래서 D-87 이 났다. 화면만 자동으로 올리면 그 어긋남을 **더 빨리 만들 뿐**이다. ★★★ 엔진은 잘못되면 **서비스가 멈추고 자동배포에는 지켜보는 사람이 없다** → `deploy/engine.sh` 가 되돌릴 자리를 먼저 만들고, 30초 안에 안 살아나면 **스스로 되돌린다**(되돌려도 빨갛게 끝난다 · 되돌린 것도 안 살면 종료코드 6 으로 사람을 부른다). ★ 화면만 바뀐 날에는 **엔진을 재시작하지 않는다** — 그 몇 초에 걸린 보고서 생성이 죽는다. ★ 딸린 결과: `push` 에서는 `inputs.*` 가 통째로 비므로 판정을 `plan` 판 한 곳으로 모았고(안 그러면 **빈 목록을 올리고 초록으로 끝난다**), 입력이 넷이 됐다(`engine` — 「쓸까 말까」가 아니라 「무엇까지」), 백업을 최근 5개만 남기고 정리한다 | 2026-08-23 | 사용자 | (이 커밋) |
| D-84 | **`im-flow` 에 쓰는 이는 본체 게이트 하나다.** `deploy-nas` 의 쓰기(scp)를 **잠그지 않고 지웠다** — 잠금은 급할 때 풀리고, 그때 다시 쓰는 이가 둘이 된다. 쓰는 이가 둘이면 「올렸는데 다르다」의 원인을 **영영 못 가른다**: 재는 그 순간에도 다른 길이 덮을 수 있어서다. 실제로 그 상태에서 「웹서버 캐시」로 잘못 짚고 사장님께 DSM 설정 변경을 안내했다 (M-25). 남겨 둔 명분이던 「맥이 꺼졌을 때의 대비」는 **화면만 올리는 반쪽 배포**라 값이 낮았다. 딸린 결과: 지문 대조가 유일한 판정이 되어 `|| true` 를 떼고, 재는 목록을 넷에서 `files` 전부(16개)로 넓혔다 | 2026-08-22 | 사용자 | (이 커밋) |
| D-86 | **앱은 `/im-flow/` 를 읽는다 — 배포 경로는 그대로 둔다.** 화면 맨 아래 `판 6a6f5201` 이 그 시각 저장소 지문과 같았다. 반나절 전 화면이 옛것이었던 것은 다른 폴더도 캐시도 아니라 **아직 안 올라간 것**이었다(동기화 09:26 · 배포 도착 10:50). ★ 나는 후보를 둘로만 세웠는데 답은 셋째였다 — M-25 가 「셋을 갈라 놓고 말한다」고 적어 둔 그 자리에서 또 그랬다. ★★ 한 판에 끝난 이유는 **지문이 화면에 보였기 때문**이다(M-27, 그날 아침에 고쳤다). 남는 일: `LP_PUBLIC_BASE` 를 넣으면 배포가 앱과 같은 길로 매번 재서 이 판단을 사람이 안 해도 된다 | 2026-08-23 | 사용자 실측 | (이 커밋) |
| D-99 | **SketchUp 트랙은 09 Massing 과 병행이고 계산 자리는 09 하나다** — `12_sketchup_plan` 은 09 모델을 계획 파일로 옮겨 적을 뿐 다시 계산하지 않는다(매스 없으면 계획도 없다, 테스트 고정). 조감도 역할 분리(본문 birdseye · 표지 Veras, D-34)·실행 위치(D-95)·MCP 한도(D-95 비고)로 나머지 갈래도 닫힘 | 2026-08-24 | 사장님 | (이 커밋) |
| D-96 | **모델에서 나온 수량은 fact 로 등록하지 않는다** — 검토 결과로만 내고, 사람이 채택할 때 사람 이름으로 들어간다 (D-33·마스터 프롬프트 §37 과 같은 결). 코드로 고정: `12_sketchup_plan`·`13_sketchup_intake` 의 `facts` 는 항상 빈 배열(테스트가 지킨다). 딸린 결과: 작업지시서 단계 2 의 im-outline `dataKeys` 추가는 대상이 없어져 하지 않는다 | 2026-08-24 | 사장님 | (이 커밋) |
| D-95 | **SketchUp 은 맥북에서 사장님이 직접 돌린다.** 평면 B 의 자리 결정 — 단계 3 절차서(`docs/스케치업-모델-만드는-법.md`)가 이것으로 풀렸다. 같은 날 실측 추가: 원격 세션에서 `build_model` 성공 — SketchUp 은 클라우드에서 돌고 `.skp` 는 다운로드 주소로 오므로 **맥북에 SketchUp 설치가 필요 없다.** 준비물은 Claude + Trimble 커넥터 연결뿐. 크론·예약 실행의 커넥터 존속과 무료 구간 한도는 미실측 — 자동 실행에 걸지 않는 원칙 유지 | 2026-08-24 | 사장님 | (이 커밋) |
| D-81 | **한 번에 올리는 한도를 45MB 로 내린다** — 엔진은 100MB 를 약속했는데 NAS 서버는 본문 64MB 에서 `req.destroy()` 로 끊었다. 그 사이 크기는 화면이 「됩니다」라고 해 놓고 **응답 없이** 끊겼다. NAS 를 올리는 길(㉮)도 있었지만 RAM 1.8GB 라 큰 본문 동시 처리를 견디지 못한다 — **약속을 실제에 맞췄다.** **45MB 이지 48MB 가 아니다**: 64÷1.333≈48MB 는 벽 그 자체라 파일 이름·JSON 껍데기에 넘어간다. 45MB → 본문 60MB 로 4MB 를 남긴다. 함께: **화면이 합계를 재고 보내기 전에 막는다** — 전에는 파일 하나씩만 재고 합계는 아무도 안 쟀다 | 2026-08-22 | 사용자 | (이 커밋) |
| D-30 | **두 번째 작업선을 푸시해 병합한다** (구 D-24) — `desktop-work-0816` 으로 푸시 후 병합. 키 이름은 `land.use_districts` 로 통일. 문서 셋(README·HANDOVER·미결정)에 양쪽이 같이 남은 것은 2026-08-16 후속 정리로 해소 | 2026-08-16 | 사용자 | `6d6495c` |
| D-15 | **시장조사 Connector 를 붙인다** — LLM 기억 대신 출처 있는 통계로 채운다. 1차 ECOS(시장금리) 완료, 나머지 5종은 작업판 C-01~C-05. 값의 성격(기준선/실측치)은 D-23 으로 분리 | 2026-08-15 | 사용자 | `4ce35fc` + (이 커밋) |
| D-27 | **판단 기준을 IM Agent 로 확정하고 CLAUDE.md §4 를 개정한다.** 〈2026-08-16 §4.3 보강 — 검증을 스모크·파이프라인 **두 단계**로. 스모크 14/14 인 상태에서 파이프라인을 돌리니 결함 3건이 나왔다〉 크론이 빠진 뒤에도 §4 는 크론 사양(Calendar·단기예보·천문연)으로 남아 있어, 정작 이 저장소가 쓰는 공공데이터 6종에는 적용할 규칙이 없었다. 2026-08-16 실측에서 드러난 것을 규칙으로 굳혔다 — 기관별 키 4종·API 단위 활용신청·붙이기 전 실측·**정렬 방향 확인**·호출 최소화·실패 격리·출처 표기(대리값·모수·결측)·가정 섞이면 자동 산출 금지·모호하면 고르지 않기. 구 §4 는 `docs/삭제-후보.md` B-2 에 보존 | 2026-08-16 | 사용자 | (이 커밋) |
| D-25 | **태양광 딜에서 일사량까지만 낸다 — 발전량·매출은 사람이 넣는다.** 발전량 = 일사량 × 설비용량 × **시스템효율(PR)** 인데 PR 은 가정치(통상 75~85%)고, 그 10%p 가 매출을 그대로 흔든다. 가정치를 IM 에 넣지 않는다는 원칙을 지킨다. 출처는 관측소명·부지로부터의 거리·관측연도까지 적는다 | 2026-08-15 | 사용자 | (이 커밋) |
| D-11 | **PR #4 · #1 · #2 를 닫는다 — 옮길 것이 없다.** 남은 가치라고 본 `alert-failure`·`deploy-nas` 셋이 **이미 main 에 있었고 파일 해시까지 같았다**(PR #3 병합 때 함께 넘어감). 남은 것을 병합하면 충돌 5건이 전부 「main 에서 지운 것을 PR 이 고쳤다」라 **어느 쪽으로 풀어도 깨진다** — PR 쪽이면 없는 스크립트를 부르는 크론이 매일 실패하고, main 쪽이면 `test/cron.test.js` 가 삭제된 스크립트를 require 해 CI 가 깨진다. PR #1·#2 도 같은 사유로 닫았다(삭제된 스크립트를 고치는 변경). 작업물은 브랜치 `00b0e59`·`b2e4c0b`·`148ec55` 에 남으므로 **D-19 이관처가 정해지면 거기서 가져간다 — 그때까지 브랜치를 지우지 않는다** | 2026-08-16 | 사용자 | (이 커밋) |
| 자료 붙이기 배치 | **갈래 셋을 한 벌의 토글로 둔다 — 단(段)을 나누지 않는다.** 〈2026-08-22 사용자 지시 「라인을 구분하지 않는 통합 토글로 만들어줘」〉. **2026-08-21 의 「1단·2단·3단」 결정을 뒤집는다** — 단을 나누면 앱 갈래만 한 칸을 통째로 차지해 커 보이고, 셋이 **같은 층위의 선택지**라는 것이 흐려진다. ★ 다만 **첫 판(`auto-fit`)으로 되돌아가지는 않는다.** 첫 판의 문제는 「한 줄」이 아니라 **「몇 칸이 될지 모른다」**였다(창 너비에 따라 2+1 도 1+1+1 도 됐다). 열은 **셋으로 고정**하고 좁아지면 **세로로 세운다** — 실측 1100px `[3]` · 430px `[1,1,1]`. 함께: 앱 갈래의 설명 두 줄을 뺐고(이름만으로 뜻이 서고 바로 아래에서 프로젝트를 고른다), 카드 안내 문구를 「신규 프로젝트로 만들거나 기존 프로젝트 보고서를 만들 때 … 관련자료를 분석하고 읽어서」로 바꿨다 | 2026-08-22 | 사용자 | (이 커밋) |
| D-72 | **그때그때 고르기(chooser)를 유지한다 — 폴더 연결로 가지 않는다.** 「클라우드 폴더」 지시서 §2·§9·§10 이 폴더를 계속 붙들고 지켜보라고 했지만, 그러려면 **refresh token 을 보관해야 한다.** D-69 의 이유가 그대로 유효하다 — **보관하지 않는 열쇠는 샐 수 없다.** 치르는 값: 자료가 늘 때마다 다시 고르고, 접근권이 30분이면 만료된다. 그래서 **「원본이 그대로인지 확인」을 사람이 누르는 길**을 화면에 붙였다 — 서버 길(`POST /linked/verify`)은 있었는데 화면이 한 번도 안 부르고 있었다. 만료를 **빨간 오류로 그리지 않는다** — 고장이 아니라 이 선택의 값이다. 딸린 결과: §14 클라우드 저장이 닫히고(D-73), 폴더 체계 충돌이 당장은 사라진다(D-74) | 2026-08-21 | 사용자 | (이 커밋) |
| D-62 | **교차검증에서 잡은 것 — 고아 모듈을 이었다.** `core/ess.js`·`core/solar.js` 를 **테스트에서만 부르고 있었다**(D-48 과 같은 실패를 같은 날 재발). ESS 지속시간·열화, 태양광 과적률이 **어디에도 안 나오고 있었다** → `05-validation` 의 ⑩ 짝 용량 검사로 이었다. **§4.5 가 명시한 「지역 색인 180일」과 코드가 갈려** 색인 4종이 기본 7일로 떨어지던 것도 고쳤다. `pexels` 스모크 목록 누락도 채웠다 | 2026-08-16 | 사용자 | (이 커밋) |
| D-60 | **신용조사에서 되는 둘을 붙이고, 법이 막은 것은 코드가 거절하게 한다.** 국세청 **휴폐업·진위**(`nts`)와 국민연금 **인원·고지금액**(`nps`)은 기존 data.go.kr 키로 붙였다 — 휴폐업은 국세청만 알고, 고지금액은 회사가 만든 숫자가 아니다. **납세증명·완납증명·체납은 어떤 키로도 안 된다**(국세기본법 §81조의13 — 본인 또는 본인 동의를 증명한 제3자만). 「받아야 하는 서류」로 남기고 거절 함수를 뒀다. **신용평가사는 무료 API 가 없어** 붙이지 않았다(유료 계약은 사용자 결정). **법인등록번호(13자리)를 사업자등록번호로 쓰면 거절한다** — 잘라 쓰면 엉뚱한 사업자가 조회되는데 증상이 「없는 회사」와 같다 | 2026-08-16 | 사용자 | (이 커밋) |
| D-59 | **법인가치 검토 보고서를 만든다 — 법령 산식을 인쇄해서.** 토지는 탁상검토가 생겼는데 법인은 실재 대조만 있었다. 위험은 D-57 과 같아(평가의견서로 읽힌다) 고지를 **다섯 곳**에 넣었다. 계산은 **상증법 시행령 제54조 보충적 평가방법** — 법령 산식이라 가정이 아니지만 **개정되므로 적용 산식을 문서에 인쇄한다**(REC 가중치 D-58 과 같은 자리). **부동산 비율을 모르면 계산 자체를 안 한다**(가중치가 3:2↔2:3 으로 뒤집힌다). **순손익 3개 연도가 다 있어야 한다**(0 과 자료 없음은 다르다). 유사기업 배수법·최대주주 할증은 하지 않되 **「하지 않았다」를 문서가 말한다** | 2026-08-16 | 사용자 | (이 커밋) |
| D-58 | **태양광 자산군을 만든다 — 템플릿만 있고 자산군이 없었다.** 그래서 태양광 딜에서 전용 필수값이 하나도 안 뜨면서 화면은 「필수 다 채웠다」라고 했다. **DC 와 AC 를 짝으로** 받는다(「1MW 태양광」은 사업을 특정하지 못한다 — 과적이면 인버터가 잘라 낸다). **부지 유형**을 받되 **REC 가중치 값은 내지 않는다** — 고시가 정하고 개정되므로 박으면 조용히 틀린다. 대신 갈리는 축(부지 유형·용량·설치 형태)을 말한다. **수평면→경사면 일사량 변환**과 **발전량**은 거절한다(D-25 의 결정에 코드 자리가 없던 것을 채웠다). 이격거리 조례는 숫자가 아니라 성립 여부라 필수로 받는다 | 2026-08-16 | 사용자 | (이 커밋) |
| D-57 | **탁상검토 보고서를 문서로 만든다 — 감정평가서로 오해될 수 없게.** 3방식 계산은 이미 있었는데 문서가 없어 토지가치만 따로 낼 방법이 없었다. 이 문서는 **틀리지 않아도 사고가 난다** — 형식이 그럴듯하면 정식 감정평가로 읽히기 때문이다. 「감정평가서가 아니다」를 **다섯 곳**에 넣고, **확인하지 않은 것 7항목을 결론보다 앞에** 뒀다. 방식이 하나뿐이면 결론값을 내지 않고 편차가 2배를 넘으면 범위로만 낸다 — 표지의 큰 숫자 하나는 그것만 읽힌다. LLM 을 쓰지 않는다 | 2026-08-16 | 사용자 | (이 커밋) |
| D-56 | **ESS 를 자산군·재무 템플릿으로 만든다 — 발전과 한 묶음으로 두지 않는다.** 자원이 없고 배터리가 해마다 줄어 운영기간 상한이 다르다. **저장용량(MWh)과 출력용량(MW)을 짝으로** 받는다 — 「100MWh」만으로는 1시간짜리인지 4시간짜리인지 몰라 사업이 특정되지 않는다. **열화율을 필수로** 받고 용량 곡선을 낸다(초기 용량으로 깔면 후반이 부풀려지는데 매출표는 멀쩡해 보인다). **매출은 내지 않는다** — 사이클 수·왕복효율·단가차가 전부 가정이라 곱하면 어떤 숫자든 나온다. 붙이면서 **「해상풍력」이 육상과 함께 걸려 자산군이 null 로 남던 것**을 잡았다 | 2026-08-16 | 사용자 | (이 커밋) |
| D-55 | **풍력을 육상·해상으로 나눠 자산군·재무 템플릿을 만든다.** `generic` 으로 떨어져 재생에너지에 부동산 가정이 붙고 있었다. 하나로 묶지 않은 이유 — 해상은 사업비가 배 이상이고 공사기간·금융조건이 달라, 묶으면 어느 쪽 기준인지 모를 모델이 된다. **풍속은 허브 높이와 짝으로만** 받고 10m 관측값을 환산하지 않는다(전단지수가 가정인데 발전량은 풍속의 세제곱). 붙이면서 **「해상풍력 발전소」가 태양광으로 잡히던 것**을 잡았다. 기본값은 전부 추측이라 미확인으로 남겼다 | 2026-08-16 | 사용자 | (이 커밋) |
| D-54 | **계통 여유는 자동으로 조회하고, 변전소 거리는 사람이 넣는다 (다).** 한전이 변전소 좌표를 **국가중요시설 사유로 비식별 처리**해 거리를 계산할 좌표 자체가 공개되지 않는다. 나오는 것(선로별 여유용량)은 `connectors/kepco.js` 가 조회하고, 거리는 지사 사전검토 회신을 받아 적는 가이드 필드로 뒀다. **직선/선로 기준을 안 밝히면 받지 않는다**(1.3~2배 갈린다). 인입 공사비는 내지 않는다. 태양광은 자산군이 없어 **템플릿에도 배선**했다 — 풍력은 템플릿이 없어 아직 못 한다 | 2026-08-16 | 사용자 | (이 커밋) |
| D-53 | **PDF 를 실제로 만든다 — 새 의존성 0.** `formats:['pdf']` 를 선언만 하고 산출은 HTML 까지였다. 이미 쓰는 헤드리스 크로미움에 `--print-to-pdf`(`core/pdf.js`). 쪽수를 세어 사양과 견주되 막지 않는다. 브라우저 기본 머리말은 끈다 — **파일 경로가 대외 문서에 박힌다** | 2026-08-16 | 사용자 | (이 커밋) |
| D-52 | **글꼴을 번들하지 않고 조용한 대체를 없앤다.** 한글이 중국어 글꼴로 박히고 있었다. 서브셋 4벌이 약 11MB·496파일이라(실측) 공개 저장소에 넣지 않고, 대신 `core/fonts.js` 가 요청↔실제를 대조해 **무엇으로 대체되는지까지** 말한다. CI 는 `fonts-noto-cjk` 설치로 맞춘다. **막지는 않는다** — 글꼴 때문에 문서가 아예 안 나오는 편이 더 나쁘다 | 2026-08-16 | 사용자 | (이 커밋) |
| D-49 | **PR #3 을 main 에 병합한다 — 아침 브리핑 중단을 감수한다.** fast-forward 로 139커밋. `main` 에만 있던 크론 워크플로 4개가 사라져 브리핑이 멈춘다. **알고 병합했다** — 뒤따라야 하는 것은 D-19(옮길 곳)·D-20(협력사 지침 재발행)이고, Secret 은 지우지 않는다 | 2026-08-16 | 사용자 | (이 커밋) |
| D-38 | **Rhino 서버를 세우지 않고 설계 협력사 결과를 받는다.** IM 에 실을 매스 검토 한두 장 때문에 Rhino 라이선스 서버를 세우는 값이 더 크다. 커넥터(`connectors/rhino.js`)는 남긴다 — 나중에 세우면 주소만 넣으면 된다. `.3dm` 은 받지 않고(§5) **이미지 + 수치 표 + 전제 조건**을 받는다. 생성물이므로 fact 로 등록하지 않는다 | 2026-08-16 | 사용자 | (이 커밋) |
| D-37 | **형상에서 물량을 계산하지 않는다 — 설계사가 「기본 수량 포함」으로 내보낸다.** IfcOpenShell 은 Python 런타임을 뜻해 §5 에 걸리고, 모델러가 그린 형상에서 도구가 낸 수량이 우리가 재는 것보다 정확하다. **받는다는 결정에 검사를 붙였다** — 규격 문서 + `npm run im:check-model` (협력사도 보내기 전에 돌린다) | 2026-08-16 | 사용자 | (이 커밋) |
| D-48 | **대조 커넥터를 가이드 필드로 배선한다.** 커넥터 다섯을 만들어 놓고 보고서 생성 경로에서는 한 번도 부르지 않고 있었다(교차검증에서 발견) — 스모크에서만 돌았다. `crosscheck.*` 7칸을 제조 딜의 가이드 필드로 두고, 값이 있을 때만 부른다. **안 고르면 안 부르고**(§4.9), **건너뛴 사실을 로그에 남기고**(조용히 빠지면 「대조했는데 문제 없었다」로 읽힌다), **Dataset 에 넣지 않는다**(업종 평균이 딜의 값으로 실린다) | 2026-08-16 | 사용자 | (이 커밋) |
| D-35 | **시공견적은 수량과 단가를 가른다.** 한 제품에 맡기면 어디까지가 근거이고 어디부터가 가정인지 사라진다 — 수량은 IFC(`core/ifc.js`, 사전 산출된 물량만 읽는다), 단가는 조달청 낙찰(`connectors/g2b.js`). **㎡당 단가는 자동으로 만들지 않는다** | 2026-08-16 | 사용자 | (이 커밋) |
| D-33 | **건축물 모델링은 Rhino.Compute + Grasshopper 로 붙인다.** 매스 스터디 자동화에 가장 곧다. 다만 **「무료」가 아니다** — 그 서버에 Rhino 라이선스가 필요하고(D-38 로 분리), 결과는 **생성물이라 fact 로 등록하지 않는다** | 2026-08-16 | 사용자 | (이 커밋) |
| D-18 | **아침 크론(B군)을 이 저장소에서 삭제** — 저장소를 보고서 생성으로 한정한다. 대상: `send-morning-brief.js`·`send-morning-card.js`·`fetch-daily-quote.js`·`fetch-laws.js`·`render-card.js`·`fonts/`·크론 워크플로 4개·`cron-ci.yml`·`test/cron.test.js`. `alert-failure` 는 `deploy-nas` 가 쓰므로 존치 | 2026-08-14 | 사용자 | (이 커밋) |

> **되살리는 법**: `git log --diff-filter=D -- send-morning-brief.js` 로 삭제 커밋을
> 찾아 `git revert` 한다. git 이력에 전부 남아 있다.
