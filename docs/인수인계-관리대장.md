# 인수인계 관리대장

> 근거: 「LinkPilot 총체적 업무 인수인계 완료검증 지침 v1.0」 §6
> 판정 근거는 [`인수인계-완료검증-2026-08-26.md`](인수인계-완료검증-2026-08-26.md) 에 있다.
> 이 파일은 **미완료 항목을 추적하는 장부**다 — 보고서는 그때의 사진이고, 이쪽은 살아 움직인다.

★★★ **지침 §6 의 한 줄이 이 파일의 존재 이유다** —
**「인계자 또는 인수자가 지정되지 않은 항목은 완료로 판정하지 않는다」**.

그래서 **이름을 지어내지 않는다.** 모르는 칸은 `미지정` 으로 둔다 (CLAUDE.md §9 —
추측으로 메우고 넘어가지 않는다). `npm run handover:check` 가 몇 칸이 비었는지 센다.

★★ **필수 게이트 13번은 이 표가 채워져야 열린다.** 다른 것을 다 고쳐도
담당자·목표일이 비어 있으면 전체 판정은 `INCOMPLETE` 다.

★ 목표일은 `YYYY-MM-DD` 로 적는다. 「곧」·「이번 주」는 목표일이 아니다 —
지나갔는지를 잴 수 없다.

## 표 읽는 법

| 칸 | 뜻 |
|---|---|
| 중요도 | `CRITICAL` / `HIGH` / `MEDIUM` / `LOW` (지침 §5) |
| 상태 | `NOT_STARTED` / `UNVERIFIED` / `PARTIAL` / `READY_FOR_TEST` / `TEST_FAILED` / `BLOCKED` / `VERIFIED` / `ACCEPTED` |
| 완료조건 | **무엇이 참이면 끝인가.** 「했다」가 아니라 잴 수 있는 문장으로 적는다 |

---

## 미완료 항목

| 번호 | 영역 | 인수인계 항목 | 인계자 | 인수자 | 증빙 | 상태 | 중요도 | 문제점 | 후속조치 | 완료조건 | 목표일 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | 데이터 | `request_id` | 미지정 | 미지정 | `core/request.js` · `request-id.test.js` 9개 · `im:demo` 로그 | VERIFIED | HIGH | **만들었다** (2026-08-26 · D-136) — 실측: 같은 프로젝트의 두 요청이 REQ-…-001 / -002 로 갈렸다 | — | 완료 | 2026-08-26 |
| 2 | 운영 | 자료 백업·복원 | 미지정 | 미지정 | 배포 실행 189 (2026-09-07 15:02 UTC) — 「Back up project data (H-1)」·「Restore drill (복원시험 · 지침 §11-4)」 **둘 다 초록** | VERIFIED | HIGH | **완료조건을 채웠다** (2026-09-07) — 운영 자리(NAS)에서 백업과 복원시험이 실제로 돌았다 | — | 완료 | 2026-09-07 |
| 3 | 디자인 | 게이트 7단계 | 미지정 | 미지정 | `core/design-gate.js` · `design-gate.test.js` 12개 · `im:demo` 로그 | READY_FOR_TEST | HIGH | **만들었다** (2026-08-26 · D-135) — 아직 실제 프로젝트가 7/7 을 지난 적은 없다 | 출력 사양을 사람이 확정하면 자동으로 걷는다 | 프로젝트 하나가 READY_TO_DEPLOY 까지 간다 | 미지정 |
| 4 | 코드 | 문서 활자 | 미지정 | 미지정 | `guard` 열째 칸 | PARTIAL | MEDIUM | 글꼴이 없는 자리에서 다른 활자로 그려진다 | CI·NAS 는 이미 설치 · 개발 자리는 못 잼으로 표시 | 문서를 내보내는 자리가 늘 초록 | 미지정 |
| 5 | Platform | 실제 데이터 연결 | 미지정 | 미지정 | 못 봄 | BLOCKED | MEDIUM | 이 세션이 저쪽에 안 쓴다 (§8-2) | 저쪽 세션이 확인 | 화면 값과 Agent 값이 같다 | 미지정 |
| 6 | MCP·API | 인증 실측 | 미지정 | 미지정 | 배포 실행 189 (2026-09-07) — 「Why is the key not read」·「Check Gemini key pool (열쇠 여덟)」·「Check map power (지적도·공시지가)」·「Check second reader (Claude)」·「Check reading power (OCR)」 **다섯 다 초록** | PARTIAL | MEDIUM | **막힘이 풀렸다** (2026-09-07) — 감사 자리에 열쇠가 없는 것은 그대로지만, **열쇠가 있는 자리(NAS)에서 재는 길**이 돌기 시작했다. ★ 다만 그 다섯이 「커넥터별」인지는 **로그 전문을 못 봐 확인 못 했다** — 못 잰 것은 통과가 아니다 | 다음 배포에서 그 단계들의 **로그 본문**을 읽어 커넥터 이름과 결과 코드를 대장에 옮긴다 | 커넥터별 살아 있음 확인 | 미지정 |
| 7 | 조직 | 외출모드 — 지침↔결정 어긋남 | 미지정 | 미지정 | D-135 · 지시서 §2.2·§8.1 · 플랫폼 지침 §4 | ACCEPTED | MEDIUM | **끝났다** — 사장님이 「삭제」로 정하셨고 2026-08-26 에 문서에서 실제로 뺐다 | — | 완료 | 2026-08-26 |
| 8 | 보안 | 접근권한 회수 | 미지정 | 미지정 | 없음 | NOT_STARTED | MEDIUM | 퇴사자 회수 절차 없음 | 계정·키 소유주 표 작성 | 표와 회수 절차 문서화 | 미지정 |
| 9 | 문서 | 시스템 구성도·흐름도 | 미지정 | 미지정 | 없음 | NOT_STARTED | MEDIUM | 신규 담당자가 전체를 못 본다 | 구성도 1장 · 흐름도 1장 | 인수자가 보고 설명할 수 있다 | 미지정 |
| 10 | 데이터 | 데모 프로젝트 6개 | 미지정 | 미지정 | `im-projects/` | PARTIAL | LOW | 실데이터와 섞일 수 있다 | 데모 표식 또는 분리 보관 | 화면에서 실데이터와 갈린다 | 미지정 |
| 11 | 운영 | 비용 감시 | 미지정 | 미지정 | 없음 | NOT_STARTED | LOW | 유료 호출 비용을 아무도 안 본다 | 커넥터별 호출 수 기록 | 월 호출·비용이 한 화면에 | 미지정 |
| 12 | 운영 | **웹 뿌리 캐시 끄기** | 미지정 | 미지정 | 배포 실행 36 (2026-09-07 15:13 UTC) — 「Do not cache the app or the worker」 **경고 0건** (앞 실행 35 에서는 3건) | VERIFIED | CRITICAL | **12일치 사고의 근본 원인이었다** (M-79) — `sw.js` 가 캐시돼 브라우저가 새 배달원을 안 받아 왔다. 배포는 그동안 계속 초록이었다 | — | 완료 | 2026-09-07 |
| 13 | 운영 | **NAS 방화벽 22번 영구 규칙** | 미지정 | 미지정 | 없음 — 지금은 **임시로 열려 있다** | NOT_STARTED | CRITICAL | 닫히면 **배포가 한 발도 못 나간다.** 그런데 화면에는 아무 오류도 안 떠서 며칠이 지나야 안다 (2026-09-06 에 하루를 그렇게 썼다) | DSM → 제어판 → 보안 → 방화벽 → 규칙 편집 → 22번 포트를 **tailnet 대역만** 허용 (거부 규칙보다 **위**에). ★ 넣을 값은 **저장소에 안 적는다** — 이 저장소는 public 이다 (D-10). 값은 세션 답변으로 드렸다 | 배포의 「Set up ssh」가 임시 규칙 없이도 초록 | 미지정 |

---

## 채우는 법

**한 줄에 사람 하나씩** 정하시면 됩니다. 두 칸만 채우면 그 줄은 살아납니다 —

- **인계자** : 지금 그것을 아는 사람
- **인수자** : 앞으로 그것을 맡을 사람
- **목표일** : `2026-09-15` 같은 날짜

`npm run handover:check` 가 남은 칸을 셉니다. 전부 채워지면 필수 게이트 13번이 열립니다.

## 지난 판

이 표의 첫 판은 2026-08-26 감사에서 나왔다. 항목을 지우지 않는다 —
끝난 것은 상태를 `ACCEPTED` 로 바꾸고 **증빙과 날짜를 남긴다** (CLAUDE.md §9 와 같은 규칙).
지우면 반년 뒤에 같은 것을 다시 발견한다.
